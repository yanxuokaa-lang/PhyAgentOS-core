## v9.2.1 (2026-09-11 09:36) - codex

### 变更摘要 / Change Summary

- [完成] [docs] [fix] 回填基础修复的最终实现提交引用，保持中英文日志与工作区证据一致。(local)
- [Completed] [Docs] [Fix] Backfill the final implementation commit reference so bilingual logs match workspace evidence. (local)

### Git 提交 / Git Commit

- Implementation commit: `7122faf`; Branch: `feature/planning-loop`。

## v9.2.0 (2026-09-10 15:53) - codex

### 变更摘要 / Change Summary

- [完成] [policy] [fix] 保存 Agent Loop 方向诊断，删除未提交的任务答案 YAML、专用脚本和伪 Agent Runner；不在偏离实现上打补丁。(local)
- [Completed] [Policy] [Fix] Persist the Agent Loop diagnosis and delete the uncommitted task-answer YAML, dedicated script, and pseudo-Agent runner rather than patching divergent code. (local)
- [完成] [policy] [feat] 在既有 AgentTask/AgentLoop 路径补齐模型可提交的语义节点、绑定 Skill 上下文与恢复决策，保留 Runtime、审批和终态事实所有权。(local)
- [Completed] [Policy] [Feat] Complete model-submitted semantic nodes, bound Skill context, and recovery decisions in the existing AgentTask/AgentLoop path while preserving Runtime, approval, and terminal-fact ownership. (local)
- [完成] [eval] [chore] 增加无运动回归测试，执行六维验收并记录真实场景尚未验证的边界；不启动进化或仿真动作。(local)
- [Completed] [Eval] [Chore] Add no-motion regression tests and six-dimensional acceptance, recording remaining live-scene evidence gaps without enabling evolution or simulation actions. (local)

### 预期影响文件 / Expected Files

- `docs/forge/AGENT_LOOP_FOUNDATION_DIAGNOSIS_20260910.md`, `CHANGELOG.md`.
- `PhyAgentOS/agent/loop.py`, `agent/tools/forge_task.py`, `agent/planning_loop.py`, and focused tests.
- `examples/forge-adapters/robotwin20/{profiles/forge-persistent/persistent-agent-task.yaml,scripts/run_persistent_agent_task.py,src/robotwin20_adapter/persistent_agent_runner.py}` (delete uncommitted drafts).
- `examples/forge-skills/pick-place-workflow/{SKILL.md,src/pick_place_workflow/multi_object_agent.py}`.
- Retain unrelated pending host/provider and approval changes outside this commit; no broad worktree reset.

### 文件变更详情 / File Details

- [新增 / Added] `docs/forge/AGENT_LOOP_FOUNDATION_DIAGNOSIS_20260910.md` L1-L75：记录偏离原因、PAOS 所有权、修复路线和六维验收标准。
- [新增 / Added] `PhyAgentOS/agent/plan_proposal.py` L1-L45：将模型提交的语义节点编译为现有 `PlanGraph`，由 PAOS 生成 revision、策略和图完整性元数据。
- [新增 / Added] `PhyAgentOS/agent/recovery_decisions.py` L1-L94：只读询问模型选择 stop/replay/replan；状态改变仍由 PlanningLoop/Coordinator 执行。
- [修改 / Modified] `PhyAgentOS/agent/tools/forge_task.py` L157-L227：materialize 工具支持仅提交 `nodes`，拒绝同时提交两个计划来源。
- [修改 / Modified] `PhyAgentOS/agent/experience/activation.py` L26-L35, L164-L184, L217-L221：保存激活时 Skill 指令并提供任务绑定读取。
- [修改 / Modified] `PhyAgentOS/forge/task.py` L280-L290, L527-L545, L807-L855：持久化并保护 primary Skill 指令快照的不可变性。
- [修改 / Modified] `PhyAgentOS/agent/loop.py` L566-L606、`PhyAgentOS/cli/commands.py` L699-L758, L936-L968：节点 prompt 注入原始目标/Skill，CLI 注入可选恢复 Planner。
- [修改 / Modified] `examples/forge-skills/pick-place-workflow/SKILL.md` L7-L41：增加动态任务理解、grounding、发现扩图与恢复指导。
- [新增 / Added] `tests/test_agent_foundation.py` L1-L189：变量对象数量、Skill 快照、能力校验、查询状态映射、恢复决策和无运动断言。
- [删除 / Deleted] 未提交的 `persistent_agent_runner.py`、`run_persistent_agent_task.py` 与 `persistent-agent-task.yaml`：移除任务答案硬编码和专用执行协议。

### 关键 Diff / Key Diff

```diff
- persistent_agent_runner.py + persistent-agent-task.yaml + run_persistent_agent_task.py
+ forge_task_materialize_plan(nodes=[...])
+ PAOS -> PlanGraph(task_id, revision_id, graph_digest, policy_snapshot_digest)
+ AgentTaskRecord.primary_skill_instructions = activation-time SKILL.md
+ AgentRecoveryDecisions -> stop | replay | replan
```

### 六维验收 / Six-Dimensional Acceptance

架构集成、失败恢复、权限与仿真安全、配置复现、可维护性、可观察性均通过软件无运动检查；真实模型感知、RoboTwin 运动、硬件和正式 Node 发布仍未验收。 / All six dimensions pass no-motion software checks; live model perception, RoboTwin motion, hardware, and formal Node publication remain unverified.

### 验证 / Validation

- Full suite: `313 passed`; focused foundation/planning: `49 passed`。
- 变更文件 Ruff、compileall、`git diff --check` passed；全仓 Ruff 仍报告未纳入本修复的既有 `PhyAgentOS/agent/experience/__init__.py` import-order 问题。 / Changed-file Ruff, compileall, and `git diff --check` passed; repository-wide Ruff still reports a pre-existing import-order issue outside this repair in `PhyAgentOS/agent/experience/__init__.py`.

### Git 提交 / Git Commit

- Implementation commit: `7122faf`; Branch: `feature/planning-loop`。

## v8.6.0 (2026-09-09 22:00) - codex

### 变更摘要 / Change Summary

- [完成] [policy] [feat] 让带 PlanGraph 的 AgentTask 只有在所有语义节点存在 completed NodeSettlement 时才能 finalize；保留无 PlanGraph 旧任务兼容。(local)
- [Completed] [Policy] [Feat] Require every semantic PlanGraph node to have a completed NodeSettlement before finalizing an AgentTask, while preserving legacy tasks without a PlanGraph. (local)
- [完成] [eval] [test] 将双对象 Runtime dry-run 接入 enforce Verifier，覆盖最终 verdict、Verifier rejection 以及只完成一个对象时拒绝 finalize。(local)
- [Completed] [Eval] [Test] Connect the two-object Runtime dry-run to an enforce Verifier and cover final verdict, verifier rejection, and rejection of finalization after only one object is complete. (local)
- [完成] [docs] [docs] 回填精确行号、关键 Diff、六维验收证据和提交信息。(local)
- [Completed] [Docs] [Docs] Backfill exact line ranges, key diffs, six-dimensional acceptance evidence, and commit metadata. (local)

### 影响文件 / Affected Files

- `PhyAgentOS/forge/task.py` L1543-L1578 [修改 / Modified]: finalize 前检查 active PlanGraph 的每个节点是否存在 completed settlement；无图任务保持旧行为。
- `examples/forge-skills/pick-place-workflow/tests/test_multi_object_agent.py` L54-L82, L349-L468, L471-L582 [新增/修改 / Added/Modified]: 注入确定性 Verifier，验证双对象 Runtime 的最终 verdict、全部对象/最终 scene revision/evidence 传递、Verifier rejection 与部分完成拒绝 finalize。

### 关键 Diff / Key Diff

#### [修改 / Modified] `PhyAgentOS/forge/task.py` L1561-L1578

**修改前 / Before:**
```python
if pending:
    raise AgentTaskError("cannot finalize while task-owned Action/Session invocation(s) are non-terminal: " + ", ".join(pending))
if not task.execution_records:
    raise AgentTaskError("cannot finalize an AgentTask without Tool executions")
```

**修改后 / After:**
```python
graph = task.active_revision.plan_graph
if graph is not None:
    settlements = {item.node_id: item.status for item in task.active_revision.node_settlements}
    incomplete = tuple(node.node_id for node in graph.nodes if settlements.get(node.node_id) != "completed")
    if incomplete:
        raise AgentTaskError(
            "cannot finalize while active PlanGraph has incomplete node settlements: "
            + ", ".join(incomplete)
        )
```

**修改说明 / Rationale:** 防止只完成一个对象或遗漏 verify 节点时产生用户级终态；无 PlanGraph 的历史任务不受影响。/ Prevent user-level finalization when one object or the verify node is incomplete; legacy tasks without a PlanGraph remain compatible.

#### [新增/修改 / Added/Modified] `examples/forge-skills/pick-place-workflow/tests/test_multi_object_agent.py` L54-L82, L349-L468, L471-L582

**关键新增 / Key additions:**
```python
verifier = DeterministicTaskVerifier()
verification=TaskVerificationContract(mode="enforce", ...)
assert finalized.verdict.verdict == "success"
assert any(response_facts(record.response).get("new_scene_revision") == "scene://s3" for record in verified.execution_records)
with pytest.raises(AgentTaskError, match="incomplete node settlements"):
    await coordinator.finalize_task(task.task_id)
```

**修改说明 / Rationale:** 将成功路径接入最终 Verifier，并证明 rejection 与部分 PlanGraph 完成不会误报 success。/ Route the success path through the final Verifier and prove that rejection or partial PlanGraph completion cannot report success.

### 六维验收 / Six-Dimensional Acceptance

| 维度 / Dimension | 结果 / Result | 证据 / Evidence |
| --- | --- | --- |
| 架构集成 / Architecture | PASS | 双对象路径仍为一个 AgentTask、一个 Coordinator、现有 PlanningLoop、Gateway/Runtime 与 Verifier；未新增 scheduler/store。 |
| 失败路径 / Failure paths | PASS | 既有 unknown/failed/cancelled 生命周期回归保持通过；新增 Verifier failure 与部分 PlanGraph finalize rejection。 |
| 权限与安全 / Authority and safety | PASS | 测试只使用 provider-neutral CapabilityRuntime dry-run；无 provider 直连、motion authority 或真实硬件。 |
| 配置与复现 / Configuration and reproducibility | PASS | 两对象、node/action、`scene://s0 -> scene://s2 -> scene://s3`、Verifier 输入和 evidence refs 均可从任务记录重放。 |
| 可维护性 / Maintainability | PASS | 完整性检查位于 Coordinator finalize 边界，测试复用既有 Action 生命周期、PlanningExecutionBinding 和 NodeSettlement。 |
| 可观察性 / Observability | PASS | node settlements、Action invocation、最终 scene revision、Verifier verdict/attempt 均有断言和持久任务记录。 |

### 验证 / Validation

- 聚焦测试 / Focused: `12 passed`。
- 全量核心与示例 / Full core plus example suites: `589 passed`。
- 变更文件 Ruff、compileall、`git diff --check`: passed。
- 全仓 Ruff 仍有一个既有无关问题：`PhyAgentOS/agent/experience/__init__.py` import 排序；本次变更文件检查通过。

### Git 提交 / Git Commit

- Commit: `1704e96` (implementation commit; metadata follow-up below)。
- Branch: `feature/planning-loop`; 时间 / Time: 2026-09-09 (Asia/Shanghai)。

## v8.1.0 (2026-09-09 17:29) - codex

### 预期修改 / Planned Changes

- [完成] [docs] [docs] 对照设计哲学、规划/操作架构、开发和扩展指南复核接入方向，记录文档方向与实际交付边界。(local)
- [Completed] [docs] [docs] Review integration against philosophy, planning/manipulation architecture and extension guides, distinguishing direction from delivered functionality. (local)
- [完成] [env] [feat] 通用 Runtime 支持动态 context provider，发现及新调用使用同一就绪投影；持久 Skill 明确要求宿主提供就绪依据，异常不可默认 ready。(local)
- [Completed] [env] [feat] Add dynamic context providers shared by discovery and admission; persistent Skill composition requires host readiness evidence and rejects unavailable contexts. (local)
- [完成] [eval] [fix] 无执行证据的 place 成功不得生成 placed 完成证据；验证就绪状态变化、错误、旧静态接口兼容和六维验收。(local)
- [Completed] [eval] [fix] Prevent evidence-free place success from generating completion facts; test readiness transitions, failures, static compatibility and six-dimensional acceptance. (local)
- Files: generic capability runtime, persistent Skill composition, tests, status documentation and logs. Adapter deployment implementation unchanged.

### 文件变更详情 / File Details

- `PhyAgentOS/forge/capability_runtime/runtime.py`: L51, L116, L126, L137-L149, L164-L167, L192-L195, L205 [修改 / Modified] 动态发现/准入上下文 / Dynamic discovery/admission context.
- `examples/forge-skills/pick-place-workflow/src/pick_place_workflow/persistent_runtime.py`: L42-L46, L50-L52, L139-L160 [修改 / Modified] 无证据成功归 unknown，显式宿主 context / Evidence-free success becomes unknown; explicit host context.
- `examples/forge-skills/pick-place-workflow/tests/test_dynamic_tool_context.py`: L1-L63 [新增 / Added] 动态状态、故障、取消及 HTTP 测试 / Dynamic state, faults, cancellation and HTTP tests.
- `examples/forge-skills/pick-place-workflow/tests/test_persistent_runtime.py`: L75, L81, L93-L104 [修改 / Modified] 未就绪组合及成功证据测试 / Unready composition and success evidence tests.
- `docs/forge/MULTI_PICK_PLACE_DIRECTION_REVIEW_20260909.md`: L1-L57 [新增 / Added] 规范对照、问题和六维验收 / Normative review, findings and six-dimensional acceptance.
- `docs/forge/PERSISTENT_MULTI_PICK_PLACE_STATUS_20260909.md`: L198, L212-L230 [修改 / Modified] 参数迁移及功能状态 / Argument migration and feature status.
- `CHANGELOG.md`: Archive 新增 part6，最近五版本滚动 / Add part6 archive and roll latest five versions.

### 关键代码 Diff / Key Code Diff

```diff
-return self._registration(tool_id).context.copy()
+registration = self._registration(tool_id)
+context = registration.context.copy()
+if registration.context_provider is not None:
+    try:
+        current = registration.context_provider()
+        if not isinstance(current, Mapping) or not isinstance(current.get("ready"), bool):
+            raise ValueError("context provider must return explicit readiness")
+        context.update(current)
+    except Exception as exc:
+        context.update(ready=False, binding_error=f"context_provider_error:{type(exc).__name__}")
+return context
-if registration.context.get("ready") is not True:
+context = self.get_context(tool_id)
+if context.get("ready") is not True:
+    raise CapabilityRuntimeError(str(context.get("binding_error") or "ToolEndpoint is not ready"))
+missing_evidence = success and not refs
+if missing_evidence:
+    status, success = "unknown", False
+    outcome_known = False
-runtime.register_tool(_spec(PLACE_TOOL_SPEC), PersistentActionEndpoint("place", client, resolve_preparation))
+runtime.register_tool(_spec(spec), endpoint,
+                      context_provider=lambda tool_id=spec["tool_id"]: tool_context_provider(tool_id))
```

### 验证 / Validation

- Focused: 19 passed; workspace: 762 passed, 1 skipped; isolated staged tree: 760 passed, 1 skipped. Ruff passed.
- 六维验收通过本次功能范围；架构方向一致，发现的两个 Major 问题已修复。正式 Bundle、实际 provider 健康接线、模型几何绑定及 Agent 多物体仿真不在本次通过范围。
- Six dimensions pass for this feature; architecture direction aligns and both Major findings are fixed. Formal Bundle, actual provider health wiring, model geometry grounding and Agent multi-object simulation remain outside this acceptance.
- 复现命令见状态文档第 45 行开始的测试块；本轮未运行模型、仿真运动或硬件。静态 context 行为兼容；持久 factory 新增必需 tool_context_provider 参数。
- Reproduce with the status document's test command starting at line 45. No model, simulator motion or hardware run. Static contexts remain compatible; the persistent factory now requires tool_context_provider.

### Git 提交 / Git Commit

- Branch: `feature/planning-loop`; implementation commit: `0a84e20`; validation date: 2026-09-09 (Asia/Shanghai).

## v8.1.1 (2026-09-09 18:00) - codex

### 预期修改 / Planned Changes

- [完成] [env] [feat] 增加 PAOS 宿主侧标准 evolution composition root，统一装配 `EvolutionExtension`、`EvolutionCandidateLifecycleAdapter`、episode hook 与事件记录；缺少扩展或投影端口时保持现有核心行为并 fail-open。(local)
- [Completed] [Env] [Feat] Add a standard PAOS-host evolution composition root that wires `EvolutionExtension`, `EvolutionCandidateLifecycleAdapter`, the episode hook, and event recording; preserve core behavior and fail open when the extension or projection ports are unavailable. (local)
- [完成] [eval] [test] 覆盖成功装配、未安装扩展、事件转发和重复装配，并完成六维验收。(local)
- [Completed] [Eval] [Test] Cover successful composition, missing optional extension, event forwarding, and duplicate composition, followed by six-dimensional acceptance. (local)

### 变更摘要 / Change Summary

- [完成] [env] [feat] 新增标准宿主 composition root，复用现有 ExperienceStore、candidate lifecycle 与 episode hook；扩展事件持久化失败保持 fail-open。(local)
- [Completed] [Env] [Feat] Added the standard host composition root, reusing the existing ExperienceStore, candidate lifecycle, and episode hook; extension-event persistence failures remain fail-open. (local)
- [完成] [eval] [test] 增加幂等装配、可选扩展缺失、事件转发和既有 hook/adapter 回归测试。(local)
- [Completed] [Eval] [Test] Added idempotent composition, missing optional extension, event forwarding, and existing hook/adapter regression tests. (local)

### 影响文件 / Affected Files

- `PhyAgentOS/agent/experience/evolution_composition.py`
- `PhyAgentOS/agent/experience/__init__.py`
- `PhyAgentOS/agent/loop.py`
- `tests/test_evolution_composition.py`
- `extensions/evolution/README.md`
- `docs/forge/PERSISTENT_MULTI_PICK_PLACE_STATUS_20260909.md`

### 文件变更详情 / File Details

#### [新增 / Added] `PhyAgentOS/agent/experience/evolution_composition.py` L1-L114

**修改前 / Before:** No reusable PAOS host composition entry point existed; `AgentLoop` manually attached the adapter after constructing `ExperienceCoordinator`.

**修改后 / After:** `compose_evolution_extension` explicitly accepts an extension or registry/projection ports, wires the existing candidate adapter and event sink, returns no-op when the optional distribution is unavailable, and preserves the first extension on repeated calls.

#### [修改 / Modified] `PhyAgentOS/agent/loop.py` L122-L143

**修改前 / Before:** `AgentLoop` directly mutated `candidate_lifecycle` with an inline adapter block.

**修改后 / After:** `AgentLoop` constructs the coordinator once and delegates extension wiring to `compose_evolution_extension`.

#### [新增 / Added] `tests/test_evolution_composition.py` L1-L58

**新增内容 / Added:** Tests for idempotent wiring, absent optional package no-op, and host event persistence/forwarding.

### 六维验收 / Six-dimensional Acceptance

- 架构集成 / Architecture: PASS, one host seam reuses existing coordinator/store/lifecycle; no second scheduler or store.
- 失败路径 / Failure paths: PASS, missing package and construction/event persistence failures remain fail-open.
- 权限安全 / Authority and safety: PASS, composition grants no motion, verifier, planner, or promotion authority.
- 配置复现 / Configuration and reproducibility: PASS, provider and projection ports are explicit; no implicit physical semantics.
- 可维护性 / Maintainability: PASS, adapter wiring is centralized and idempotent; focused tests cover the public helper.
- 可观察性 / Observability: PASS, extension events are persisted through the existing store and optionally forwarded.

Acceptance scope is the host wiring seam. Formal Bundle, provider health, model geometry grounding, and real Agent multi-object execution remain open.

### 验证 / Validation

- `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python -m pytest -q tests/test_evolution_composition.py tests/test_evolution_extension_hook.py tests/test_evolution_extension_adapter.py`: 14 passed.
- `cd extensions/evolution && PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -q`: 68 passed.
- `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python -m pytest -q -p pytest_asyncio.plugin tests`: 281 passed.
- Ruff, `compileall`, and `git diff --check`: passed.

### Git 提交 / Git Commit

- Commit: `ea786fc` (implementation commit)
- Branch: `feature/planning-loop`
- 时间 / Time: 2026-09-09 18:00 (Asia/Shanghai)
## v8.2.0 (2026-09-09 18:56) - codex

### 预期修改 / Planned Changes

- [完成] [policy] [feat] 将 Agent 语义 relocation 子任务投影为现有 pick-place Tool DAG，保留实体/目的地绑定并复用 PlanGraph、Gateway 与任务协调器。(local)
- [Completed] [Policy] [Feat] Project Agent semantic relocation subtasks into the existing pick-place Tool DAG, preserving entity/destination bindings and reusing PlanGraph, Gateway, and task coordinator. (local)
- [完成] [eval] [test] 增加 blocks_ranking_rgb seed 0 的分解、可执行节点顺序、admission 及失败不 finalize 测试。(local)
- [Completed] [Eval] [Test] Add decomposition, executable-node ordering, admission, and failure-no-finalize tests for blocks_ranking_rgb seed 0. (local)

### 变更摘要 / Change Summary

- [完成] [policy] [feat] 新增 `compose_executable_pick_place_plan`，为每个 Agent relocation 生成 observe、capabilities、understand、grasp、prepare、acquire、place 节点，并以 verify 汇合；依赖按 place 终点连接，支持非拓扑输入顺序。(local)
- [Completed] [Policy] [Feat] Added `compose_executable_pick_place_plan` to generate observe, capabilities, understand, grasp, prepare, acquire, and place nodes per Agent relocation, joined by verify; dependencies connect through place terminals and accept non-topological input ordering. (local)
- [完成] [docs] [docs] 更新持久多物体状态，标记 F1 已解决并明确 F2-F7 仍未进入真实执行验收。(local)
- [Completed] [Docs] [Docs] Updated persistent multi-object status, marking F1 resolved and F2-F7 outside real-execution acceptance. (local)

### 文件变更详情 / File Details

- `examples/forge-skills/pick-place-workflow/src/pick_place_workflow/agent_planning.py` L178-L246 [新增 / Added] 可执行 pick-place PlanGraph 投影及绑定传播 / executable pick-place PlanGraph projection and binding propagation.
- `examples/forge-skills/pick-place-workflow/src/pick_place_workflow/__init__.py` L11-L12, L109-L110 [修改 / Modified] 导出新投影入口 / export new projection entry point.
- `examples/forge-skills/pick-place-workflow/tests/test_agent_planning.py` L132-L183 [新增 / Added] 节点顺序、跨子任务依赖、绑定及无运动权限测试 / node order, cross-subtask dependency, bindings, and no-motion-authority tests.
- `docs/forge/PERSISTENT_MULTI_PICK_PLACE_STATUS_20260909.md` L241-L249 [新增 / Added] 产品顺序与 F1-F7 状态 / product order and F1-F7 status.

### 关键代码 Diff / Key Code Diff

```diff
 def compose_agent_plan(...):
     # existing one-node-per-semantic-obligation compatibility path
+def compose_executable_pick_place_plan(...):
+    # project each relocation onto the existing seven-tool DAG plus verify
+    node.capability = "scene.observe" ... "object.place"
+    node.input_bindings = {"entity_ref", "destination_ref"}
```

### 六维验收 / Six-dimensional Acceptance

- 架构集成 / Architecture: PASS, one projection reuses PlanGraph and existing Gateway/Coordinator boundaries; no second scheduler or task store.
- 失败路径 / Failure paths: PASS, invalid dependencies fail planning and existing admission rejects missing evidence; no success finalization logic was bypassed.
- 权限安全 / Authority and safety: PASS, projection is planning-only and admission reports `motion_authorized=False`; no provider or hardware call is made.
- 配置复现 / Configuration and reproducibility: PASS, task/revision and planner/policy digests remain explicit; seed-0 identifiers are test inputs, not hidden configuration.
- 可维护性 / Maintainability: PASS, existing semantic API remains compatible and the new projection is isolated with focused tests.
- 可观察性 / Observability: PASS, node IDs, obligation IDs, bindings, and graph digest remain available for downstream execution records.

### 验证 / Validation

- `PYTHONPATH=examples/forge-skills/pick-place-workflow/src PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python -m pytest -q examples/forge-skills/pick-place-workflow/tests/test_agent_planning.py`: 6 passed.
- `PYTHONPATH=examples/forge-skills/pick-place-workflow/src PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python -m pytest -q -p pytest_asyncio.plugin examples/forge-skills/pick-place-workflow/tests/test_full_workflow.py examples/forge-skills/pick-place-workflow/tests/test_persistent_runtime.py`: 9 passed.
- `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python -m pytest -q -p pytest_asyncio.plugin tests`: 281 passed.
- Ruff on changed files, `compileall`, and `git diff --check`: passed. Full-repository Ruff still reports a pre-existing import-order issue in `PhyAgentOS/agent/experience/__init__.py`.
## v8.3.0 (2026-09-09 19:20) - codex

### 预期修改 / Planned Changes

- [完成] [env] [feat] 增加 Robotwin20 持久 Runtime Bundle 组合根，将现有 deployment provider、CapabilityRuntime 和 HTTP transport 接到同一 ForgeToolClient 边界。(local)
- [Completed] [Env] [Feat] Add a Robotwin20 persistent Runtime Bundle composition root wiring the existing deployment providers, CapabilityRuntime, and HTTP transport to one ForgeToolClient boundary. (local)
- [完成] [eval] [test] 验证 Query/Action 注册、Action pending/terminal 生命周期及 Verifier 缺失或 unknown 结果的 fail-closed 行为。(local)
- [Completed] [Eval] [Test] Verify Query/Action registration, pending/terminal lifecycle, and fail-closed behavior for missing Verifier or unknown results. (local)

### 变更摘要 / Change Summary

- [完成] [env] [feat] 新增 `PersistentRuntimeBundle` 与 `build_persistent_runtime_bundle`，组合现有持久部署 provider、7 个 Tool endpoint 和 `CapabilityRuntimeTransport`；不 reset world、不直接启动 Action、不授予运动权限。(local)
- [Completed] [Env] [Feat] Added `PersistentRuntimeBundle` and `build_persistent_runtime_bundle` to compose existing persistent providers, seven Tool endpoints, and `CapabilityRuntimeTransport`; no world reset, direct Action start, or motion authority is introduced. (local)
- [完成] [eval] [test] 增加部署级 bundle 注册测试；既有 Runtime/Coordinator 测试验证 pending、terminal、unknown 和 Verifier fail-closed 语义。(local)
- [Completed] [Eval] [Test] Added deployment-level bundle registration coverage; existing Runtime/Coordinator tests verify pending, terminal, unknown, and Verifier fail-closed semantics. (local)

### 文件变更详情 / File Details

- `examples/forge-adapters/robotwin20/src/robotwin20_adapter/persistent_deployment.py` L1-L28, L31-L80 [新增 / Added] Runtime Bundle 类型与组合入口 / Runtime Bundle type and composition entry point.
- `examples/forge-adapters/robotwin20/tests/test_persistent_deployment.py` L35-L63 [新增 / Added] 单 transport、7 Tool 注册测试 / single-transport, seven-Tool registration test.
- `docs/forge/PERSISTENT_MULTI_PICK_PLACE_STATUS_20260909.md` L251-L265 [新增 / Added] Runtime Bundle 和 Verifier 边界状态 / Runtime Bundle and Verifier boundary status.

### 六维验收 / Six-dimensional Acceptance

- 架构集成 / Architecture: PASS, one deployment composition root reuses existing provider/runtime/transport boundaries.
- 失败路径 / Failure paths: PASS, generic Runtime preserves pending/terminal/unknown; Coordinator refuses finalization without terminal executions or Verifier.
- 权限安全 / Authority and safety: PASS, composition performs no reset or motion and leaves route admission to existing endpoint gates.
- 配置复现 / Configuration and reproducibility: PASS, deployment inputs and tool context provider are explicit; caller owns client/world lifetime.
- 可维护性 / Maintainability: PASS, no duplicate scheduler, task store, or execution protocol; focused bundle API is isolated.
- 可观察性 / Observability: PASS, Gateway identity and existing invocation/evidence records remain on the same transport.

### 验证 / Validation

- Focused: `11 passed` for deployment/runtime lifecycle tests.
- Root: `281 passed` with `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python -m pytest -q -p pytest_asyncio.plugin tests`.
- Changed-file Ruff and adapter `compileall`: passed. Full adapter-suite collection requires the RoboTwin/numpy environment and was not runnable in the PAOS interpreter.

## v8.3.1 (2026-09-09 19:32) - codex

### 预期修改 / Planned Changes

- [完成] [env] [fix] 修复持久部署可接入不同 worker client 的具体错误，使准备、观测与 Action 复用同一世界连接；使用普通对象身份检查，不增加授权门禁。(local)
- [Completed] [env] [fix] Reject mismatched worker clients so preparation, observation and Actions share one world connection, using ordinary identity checks rather than new authorization gates. (local)
- [完成] [eval] [exp] 通过 ForgeToolClient 验证 seed 0 真实观测与几何感知，绿色方块 GraspGen 重放获得 24 个候选；整体 Agent/Action/Verifier 验收仍未通过。(local)
- [Completed] [eval] [exp] Validate seed-0 public observation and measured geometry; green-cube GraspGen replay returns 24 proposals. Full Agent/Action/Verifier acceptance remains incomplete. (local)

### 文件变更详情 / File Details

- `examples/forge-adapters/robotwin20/src/robotwin20_adapter/persistent_deployment.py` L61-L66 [修改 / Modified]: 拒绝不同世界连接 / reject different world connections.
- `examples/forge-adapters/robotwin20/scripts/check_persistent_runtime.py` L10-L217 [修改 / Modified]: 公共 Query、几何/抓取配置、唯一目标选择与日志 / public Queries, geometry/grasp configuration, unique target selection and logs.
- `examples/forge-adapters/robotwin20/scripts/run_grasp_proposals.py` L21-L32 [修改 / Modified]: 失败产物与 worker 日志留存 / retain failure artifacts and worker logs.
- `examples/forge-adapters/robotwin20/tests/test_persistent_deployment.py` L46-L70 [修改 / Modified]: 单连接装配及混用拒绝 / single-client assembly and mismatch rejection.
- `examples/forge-adapters/robotwin20/tests/test_persistent_public_smoke.py` L1-L55 [新增 / Added]: 新鲜捕获、不可用、目标缺失/歧义 / fresh capture, unavailable source, missing/ambiguous target.
- `docs/forge/PERSISTENT_MULTI_PICK_PLACE_STATUS_20260909.md` L268-L313 [新增 / Added]: 真实证据、完整复现命令与未完成项 / real evidence, reproduction command and remaining work.
- `CHANGELOG.md` [修改 / Modified]: 从月度日志生成最新五版本全文，补回 v8.1.1；月度历史不变 / regenerate latest five complete entries including v8.1.1, preserving monthly history.

### 关键 Diff / Key Diff

```diff
+if any(component.client is not client for component in (
+    deployment.preparation_provider, deployment.capability_provider, deployment.prepared_routes,
+)):
+    raise ValueError("persistent runtime and deployment must share one worker client")
-observation = ObservationEndpoint(SimpleNamespace(capture=lambda request: second)).invoke(...)
+observation = asyncio.run(invoke_public_query(runtime, "scene.observe", ...))
+selected_entity = select_grasp_target(understanding, args.grasp_target_category)
-result = provider.propose(request)
+try:
+    result = provider.propose(request)
+finally:
+    # persist both the result/error and worker log
```

### 验证与六维验收 / Validation and Six Dimensions

- 原始全量 / Initial full run: 984 passed, 1 skipped, 1 failed (`test_paos_import_boundary_remains_clean`, process-wide import contamination). Isolated file passed.
- 修复后广域 / Broad regression after fixes: 986 passed, 1 skipped, 1 deselected; targeted final tests: 12 passed, including isolated import-boundary coverage and new target-selection coverage.
- Command: `PYTHONPATH=.:examples/forge-adapters/robotwin20/src:examples/forge-adapters/robotwin20/runtime:examples/forge-adapters/robotwin20/scripts:examples/forge-skills/pick-place-workflow/src PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 /home/yanxu/miniconda3/envs/paos/bin/python -m pytest -q -p pytest_asyncio.plugin tests examples/forge-adapters/robotwin20/tests examples/forge-skills/pick-place-workflow/tests -k 'not test_paos_import_boundary_remains_clean'`.
- 实测 / Measured: seed 0 sensor PASS; semantic model PASS; three cubes with nine measured geometry artifacts PASS; explicit green-cube replay 24 candidates. Broad grasp failed on a desktop-surface target; target selection corrected. No motion/Action/final Verifier result.
- 架构 / Architecture: PARTIAL, assembly fixed; Agent task entry and installed Skill Bundle still need integration.
- 失败路径 / Failures: PARTIAL, tested sensor/client/target failures and retained model diagnostics; live Action recovery not run.
- 权限安全 / Safety: existing route checks preserved; no motion performed or new authorization introduced.
- 配置复现 / Reproducibility: real dedicated environments and external profiles recorded; model wording and grasp sampling can vary.
- 可维护性 / Maintainability: reused providers/transport, no parallel scheduler; complete root-test import isolation remains an existing issue.
- 可观察性 / Observability: measured artifacts and worker logs available; task-level Action/Verifier receipts still absent.
- 整体验收未通过 / Overall task acceptance remains incomplete. Artifact paths and full live command are in the status document above.

### Git 提交 / Git Commit

- Branch: `feature/planning-loop`; implementation commit: `c560bef`; date: 2026-09-09 (Asia/Shanghai).
## v8.4.0 (2026-09-09 20:00) - codex

### 预期修改 / Planned Changes

- [完成] [policy] [feat] 新增真实多对象 AgentTask runner：从场景实体建立唯一对象/目的地绑定，组合显式 baseline 或 semantic PlanGraph，并复用 AgentLoop、PlanningLoopAdapter 与 AgentTaskCoordinator；不接入进化模块、不新增 scheduler 或 motion authority。(local)
- [Completed] [Policy] [Feat] Added a multi-object AgentTask runner with unique object/destination binding, explicit baseline or semantic PlanGraph modes, and reuse of AgentLoop, PlanningLoopAdapter, and AgentTaskCoordinator; evolution, extra schedulers, and motion authority remain out of scope. (local)
- [完成] [eval] [test] 增加实体筛选、重复绑定、混合 scene revision、任务图持久化和 semantic 模式测试；真实多对象 Action/Verifier 证据仍待后续阶段。(local)
- [Completed] [Eval] [Test] Added entity filtering, duplicate-binding, mixed-scene, task-graph persistence, and semantic-mode tests; real multi-object Action/Verifier evidence remains a later phase. (local)

### 影响文件 / Expected Files

- `examples/forge-skills/pick-place-workflow/src/pick_place_workflow/multi_object_agent.py` [新增]
- `examples/forge-skills/pick-place-workflow/src/pick_place_workflow/__init__.py` [修改]
- `examples/forge-skills/pick-place-workflow/tests/test_multi_object_agent.py` [新增]
- `docs/forge/PERSISTENT_MULTI_PICK_PLACE_STATUS_20260909.md` [补充]
- `docs/forge/MULTI_OBJECT_AGENT_LOOP_EXECUTION_PLAN_20260909.md` [新增：审核结论、执行阶段、六维验收矩阵]

### 文件变更详情 / File Details

- `examples/forge-skills/pick-place-workflow/src/pick_place_workflow/multi_object_agent.py` L1-L188 [新增 / Added]: 场景事实绑定、baseline/semantic 计划模式、单 AgentTask 创建和 controller 委托 / scene-fact binding, explicit planning modes, single AgentTask creation, and controller delegation.
- `examples/forge-skills/pick-place-workflow/src/pick_place_workflow/__init__.py` L14-L19, L117-L120 [修改 / Modified]: 导出多对象 runner 公共 API / export public multi-object runner API.
- `examples/forge-skills/pick-place-workflow/tests/test_multi_object_agent.py` L1-L91 [新增 / Added]: 多对象入口和 fail-closed 边界测试 / multi-object entry and fail-closed boundary tests.
- `docs/forge/MULTI_OBJECT_AGENT_LOOP_EXECUTION_PLAN_20260909.md` L1-L106 [新增 / Added]: 审核结论、执行阶段、六维验收矩阵 / audit conclusion, execution phases, and six-dimensional acceptance matrix.
- `docs/forge/PERSISTENT_MULTI_PICK_PLACE_STATUS_20260909.md` [新增章节 / Added section]: 阶段进展和部分六维检查 / stage progress and partial six-dimensional check.

### 关键 Diff / Key Diff

```diff
+class MultiObjectAgentRunner:
+    ...
+    plan = compose_executable_pick_place_plan(...) if planning_mode == "baseline" else compose_agent_plan(...)
+    task = coordinator.create_task(..., plan_graph=plan.graph,
+                                   plan_graph_ref=f"artifact://plans/{task_id}/{revision_id}")
+    return await controller.run(task.task_id)
```

### 验证 / Validation

- Focused: `10 passed` (`test_multi_object_agent.py` + `test_agent_planning.py`).
- Focused after formatting: `4 passed` for `test_multi_object_agent.py`.
- Broad regression: `576 passed` across `examples/forge-skills/pick-place-workflow/tests tests`.
- Ruff, compileall, and `git diff --check`: passed.

### Git 提交 / Git Commit

- Commit: `96b3631`
- Branch: `feature/planning-loop`
- 时间 / Time: 2026-09-09 (Asia/Shanghai)
- 真实模型、Action、持物接续和最终 Verifier 尚未在本轮宣称通过；六维验收为阶段性结果，完整闭环仍待真实 dry-run/runtime evidence。

### 追加修正计划 / Additional Planned Fix

- [完成] [policy] [fix] 将 benchmark 身份、observation、scene、frame、calibration 和 geometry 事实作为不透明 `PlanNode.input_bindings` 持久化，避免 runner 仅校验而未绑定。(local)
- [Completed] [Policy] [Fix] Persist benchmark identity, observation, scene, frame, calibration, and geometry facts as opaque `PlanNode.input_bindings` so the runner does not validate facts without binding them. (local)

### 追加修正详情 / Additional Fix Details

- `examples/forge-skills/pick-place-workflow/src/pick_place_workflow/multi_object_agent.py` L130-L145 [修改 / Modified]: 为每个 AgentSubtaskSpec 注入六类场景事实绑定 / inject six scene-fact bindings into each AgentSubtaskSpec.
- `examples/forge-skills/pick-place-workflow/src/pick_place_workflow/agent_planning.py` L148-L152 [修改 / Modified]: baseline 与 semantic PlanGraph 将 context_bindings 合并到每个 PlanNode / merge context_bindings into every PlanNode for baseline and semantic graphs.
- `examples/forge-skills/pick-place-workflow/tests/test_multi_object_agent.py` L62-L81, L108-L111 [修改 / Modified]: 断言任务图节点持久化完整事实 / assert complete fact persistence in task-graph nodes.
- `examples/forge-skills/pick-place-workflow/src/pick_place_workflow/__init__.py` L3-L70 [修改 / Modified]: 按 Ruff 规则整理导出 import / organize public imports per Ruff.

**关键 Diff / Key Diff**

```diff
+context_bindings=(
+    ("benchmark_object_ref", item.benchmark_object_ref),
+    ("observation_ref", item.observation_ref),
+    ("scene_revision", item.scene_revision),
+    ("frame_id", item.frame_id),
+    ("calibration_ref", item.calibration_ref),
+    ("geometry_artifact_ref", item.geometry_artifact_ref),
+)
```

### 追加验证 / Additional Validation

- Focused: `10 passed` (`test_multi_object_agent.py` + `test_agent_planning.py`).
- Broad regression: `576 passed` across `examples/forge-skills/pick-place-workflow/tests tests`.
- Ruff, compileall, and `git diff --check`: passed.

## v8.4.1 (2026-09-09 20:35) - codex

### 预期修改 / Planned Changes

- [完成] [policy] [fix] 在多对象 runner 启动前比较绑定 scene revision 与当前 Runtime scene revision；拒绝过期场景事实进入 AgentLoop，不新增 scheduler、store 或 motion gate。(local)
- [Completed] [Policy] [Fix] Compare bound and current Runtime scene revisions before starting the multi-object runner; reject stale scene facts before AgentLoop execution without adding a scheduler, store, or motion gate. (local)

### 文件变更详情 / File Details

- `examples/forge-skills/pick-place-workflow/src/pick_place_workflow/multi_object_agent.py` L190-L195 [修改 / Modified]: 启动 controller 前执行绑定场景版本一致性检查 / check bound/current scene revision before creating the controller.
- `examples/forge-skills/pick-place-workflow/tests/test_multi_object_agent.py` L108-L127 [新增 / Added]: 验证过期场景在 AgentLoop 前 fail-closed / verify stale scene fails closed before AgentLoop.

**关键 Diff / Key Diff**

```diff
+bound_scene_revision = task.active_revision.plan_graph.nodes[0].input_bindings.get("scene_revision")
+current_scene_revision = self.scene_revision_provider(task.task_id)
+if current_scene_revision != bound_scene_revision:
+    raise MultiObjectAgentError("bound scene revision is stale before AgentLoop execution")
```

### 验证 / Validation

- Focused: `11 passed`.
- Broad regression: `577 passed`.
- Ruff, compileall, and `git diff --check`: passed.

## v8.4.2 (2026-09-09 20:45) - codex

### 预期修改 / Planned Changes

- [完成] [policy] [fix] 将 scene revision 预检前移到 AgentTask 持久化之前，避免 stale 场景创建孤儿执行任务；保持 Coordinator 为唯一任务事实源。(local)
- [Completed] [Policy] [Fix] Move scene-revision preflight before AgentTask persistence so stale scenes cannot create orphan executing tasks; keep Coordinator as the sole task authority. (local)

### 文件变更详情 / File Details

- `examples/forge-skills/pick-place-workflow/src/pick_place_workflow/multi_object_agent.py` L183-L196 [修改 / Modified]: 在创建 AgentTask 前比较绑定与当前 scene revision / compare bound and current scene revisions before creating the AgentTask.
- `examples/forge-skills/pick-place-workflow/tests/test_multi_object_agent.py` L108-L129 [修改 / Modified]: 验证 stale 场景不会持久化任务 / verify stale scenes do not persist a task.

**关键 Diff / Key Diff**

```diff
+bound = bind_scene_objects(entities)
+bound_scene_revision = bound[0].scene_revision
+current_scene_revision = self.scene_revision_provider(task_id)
+if current_scene_revision != bound_scene_revision:
+    raise MultiObjectAgentError("bound scene revision is stale before AgentTask persistence")
```

### 验证 / Validation

- Focused: `5 passed`.
- Broad regression: `577 passed`.
- Ruff and `git diff --check`: passed.

## v8.4.5 (2026-09-09 21:20) - codex

### 预期修改 / Planned Changes

- [计划] [policy] [test] 增加两对象 dry-run 的 scene revision 传播回归，证明首个 world-changing 节点后的后续对象使用新场景版本，并保持最终 verify 汇合。(local)
- [Planned] [Policy] [Test] Add a two-object dry-run regression for scene-revision propagation, proving later objects use the new revision after the first world-changing node while preserving the final verify join. (local)

## v8.4.4 (2026-09-09 21:05) - codex

### 预期修改 / Planned Changes

- [完成] [policy] [test] 增加多对象 runner 对 capability/assignment evidence 持久化到 baseline 与 semantic PlanGraph 的回归断言。(local)
- [Completed] [Policy] [Test] Add regression assertions that the multi-object runner persists capability/assignment evidence into both baseline and semantic PlanGraphs. (local)

### 文件变更详情 / File Details

- `examples/forge-skills/pick-place-workflow/tests/test_multi_object_agent.py` L108-L137 [新增 / Added]: 对 baseline 和 semantic 模式检查每个计划节点的 capability/assignment 绑定 / check capability/assignment bindings on every plan node for baseline and semantic modes.

**关键 Diff / Key Diff**

```diff
+@pytest.mark.parametrize("planning_mode", ["baseline", "agent_composed"])
+async def test_runner_persists_optional_execution_evidence_in_plan_nodes(...):
+    assert all(node.input_bindings["capability_snapshot_ref"] == "artifact://capabilities/s0" for node in nodes)
+    assert all(node.input_bindings["assignment_ref"] == "artifact://assignments/s0/green" for node in nodes)
```

### 验证 / Validation

- Focused: `8 passed`.
- Broad regression: `580 passed`.
- Ruff and `git diff --check`: passed.

### Git 提交 / Git Commit

- Commit: `9ce5365`; Branch: `feature/planning-loop`; 时间 / Time: 2026-09-09 (Asia/Shanghai).

### Git 提交 / Git Commit

- Commit: `1845e1a`; Branch: `feature/planning-loop`; 时间 / Time: 2026-09-09 (Asia/Shanghai).

## v8.4.3 (2026-09-09 20:55) - codex

### 预期修改 / Planned Changes

- [完成] [policy] [feat] 允许可信场景绑定携带已有 capability/assignment evidence，并将其作为可选 opaque context bindings 传入 PlanGraph；不把 planning evidence 伪装成执行授权。(local)
- [Completed] [Policy] [Feat] Allow trusted scene bindings to carry existing capability/assignment evidence as optional opaque PlanGraph context bindings; do not treat planning evidence as execution authorization. (local)

### 文件变更详情 / File Details

- `examples/forge-skills/pick-place-workflow/src/pick_place_workflow/multi_object_agent.py` L40-L53, L151-L163 [修改 / Modified]: 增加可选 capability/assignment artifact 引用并传入子任务 context / add optional artifact references and pass them into subtask context.
- `examples/forge-skills/pick-place-workflow/tests/test_multi_object_agent.py` L38-L49 [新增 / Added]: 验证 artifact 引用格式 / validate artifact reference format.

### 验证 / Validation

- Focused: `6 passed`.
- Ruff and `git diff --check`: passed.

## v8.4.5 (2026-09-09 21:20) - codex

### 变更摘要 / Change Summary

- [完成] [policy] [test] 增加两对象 dry-run 的 scene revision 传播回归，证明首个 world-changing 节点后的后续对象使用新场景版本，并保持最终 verify 汇合。(local)
- [Completed] [Policy] [Test] Add a two-object dry-run regression for scene-revision propagation, proving later objects use the new revision after the first world-changing node while preserving the final verify join. (local)

### 文件变更详情 / File Details

- `examples/forge-skills/pick-place-workflow/tests/test_multi_object_agent.py` L173-L227 [新增 / Added]: 验证两个对象顺序执行、场景版本更新和 verify 汇合 / verify sequential execution, scene revision updates, and the verify join for two objects.

**关键 Diff / Key Diff**

```diff
+next_scene = "scene://s2" if current_scene["value"] == "scene://s0" else "scene://s3"
+current_scene["value"] = next_scene
+assert next(scene for node, scene in contexts if node == "relocate_2.observe") == "scene://s2"
+assert contexts[-1] == ("verify", "scene://s3")
```

### 验证 / Validation

- Focused: `9 passed` for the multi-object module; `23 passed` with planning-loop coverage.
- Broad regression: `581 passed`.
- Ruff, compileall, and `git diff --check`: passed.

### Git 提交 / Git Commit

- Commit: `3b95596`; Branch: `feature/planning-loop`; 时间 / Time: 2026-09-09 (Asia/Shanghai).

## v8.5.0 (2026-09-09 21:22) - codex

### 预期修改 / Planned Changes

- [完成] [policy] [feat] 增加 task-bound Action 生命周期驱动：通过现有 Coordinator/Gateway 完成 status/result 轮询、terminal 对账和 unknown 保留；不新增 scheduler、Task Store 或 motion authority。(local)
- [Completed] [Policy] [Feat] Add a task-bound Action lifecycle driver using the existing Coordinator/Gateway for status/result polling, terminal reconciliation, and preserved unknown outcomes; no new scheduler, Task Store, or motion authority. (local)
- [完成] [eval] [test] 覆盖成功、failed、unknown、poll budget、Gateway 读取失败和未确认停止，并增加双对象连续 acquire/place dry-run 与 scene revision 汇合测试。(local)
- [Completed] [Eval] [Test] Cover succeeded, failed, unknown, poll-budget exhaustion, Gateway read failure, and unconfirmed stop outcomes; add a two-object continuous acquire/place dry-run with scene-revision convergence. (local)

### 文件变更详情 / File Details

- `PhyAgentOS/agent/planning_loop.py` L147-L319 [新增 / Added]: `AgentLoopNodeExecutor` 在 Agent turn 后发现当前 node-owned Action，通过现有 Gateway `invocation_status`/`invocation_result` 轮询并经 Coordinator 持久化；超过预算或读取失败时记录 `unknown`，不重发 Action。
- `PhyAgentOS/forge/task.py` L1328-L1339, L2161-L2187 [修改 / Modified]: 增加 unknown execution 持久化辅助，并解析 `status`/`phase`/`state` 与 `pending` 状态。
- `tests/test_planning_effect_recovery.py` L114-L285 [新增 / Added]: 验证 terminal success、failed/unknown、poll budget、Gateway failure、未确认停止和单次 Action POST。
- `examples/forge-skills/pick-place-workflow/tests/test_multi_object_agent.py` L246-L397 [新增 / Added]: 在 `CapabilityRuntimeTransport` + `ForgeToolClient` + `AgentTaskCoordinator` + `AgentLoopNodeExecutor` + `PlanningLoopAdapter` 路径上验证两个对象连续执行；第一个 place 后为 `scene://s2`，第二个 observe/place 使用 `scene://s2`，第二个 place 后为 `scene://s3`，最终 verify 使用 `scene://s3`。

### 关键 Diff / Key Diff

```diff
+await self._reconcile_actions(context.task_id, context.node_id)
+status = await self.coordinator.client.invocation_status(record.invocation_id)
+self.coordinator.observe_action(task_id, record.invocation_id, status)
+result = await self.coordinator.client.invocation_result(record.invocation_id)
+self.coordinator.observe_action(task_id, record.invocation_id, result)
+# bounded polling records unknown and never retries the physical POST
```

```diff
-if node.capability == "object.relocate":
+if node.capability in {"object.acquire", "object.place"}:
+    await coordinator.start_action(task_id, node.capability, arguments, planning_binding=binding)
```

### 六维验收 / Six-Dimensional Acceptance

| 维度 / Dimension | 结果 / Result | 证据 / Evidence |
| --- | --- | --- |
| 架构集成 / Architecture | PASS（dry-run） | 单一 AgentTask、Coordinator、Gateway/Runtime 路径；无第二 scheduler/store。 |
| 失败路径 / Failure paths | PASS（覆盖） | failed、unknown、poll budget、Gateway read failure、未确认 stop 均不转为 success；world-change facts 保留。 |
| 权限与安全 / Authority and safety | PASS（范围内） | provider-neutral Runtime transport；未接 provider 直连、motion authority 或真实硬件，保持 dry-run/no-motion。 |
| 配置与复现 / Configuration and reproducibility | PASS（dry-run） | object/node/action/invocation/scene revision/evidence 均在绑定参数、execution records 和响应中可追踪。 |
| 可维护性 / Maintainability | PASS | 复用 Coordinator、PlanningLoop、Tool API；Action 生命周期集中在 `AgentLoopNodeExecutor`。 |
| 可观察性 / Observability | PASS（dry-run） | 两对象上下文、terminal Action records、scene revisions 与 verify node 可从任务记录重放。 |

### 验证 / Validation

- Focused: `51 passed`。
- Full PAOS `tests`: `286 passed`。
- Ruff、compileall、`git diff --check`: passed。
- 该验收是 provider-neutral dry-run 证据，不宣称真实机器人运动或现场 Verifier 成功。

### Git 提交 / Git Commit

- Commit: `36caf10` (implementation commit; metadata follow-up below)。
- Branch: `feature/planning-loop`。
- 时间 / Time: 2026-09-09 (Asia/Shanghai)。
## v8.7.0 (2026-09-09 22:25) - codex

### 变更摘要 / Change Summary

- [完成] [policy] [feat] 增加 provider-neutral 的语义理解到当前场景测量事实绑定层：校验实体身份、目的地、scene/frame/calibration、几何产物和歧义，并复用现有 `SceneObjectBinding`/`bind_scene_objects`，不新增 scheduler、Task Store、provider 直连或 motion authority。(local)
- [Completed] [Policy] [Feat] Add a provider-neutral binding seam from scene-understanding claims to current measured-scene facts, validating entity identity, destination, scene/frame/calibration, geometry artifacts, and ambiguity while reusing `SceneObjectBinding`/`bind_scene_objects`; no scheduler, Task Store, provider direct access, or motion authority. (local)
- [完成] [eval] [test] 增加单对象语义选择、多对象唯一绑定、重复/歧义/过期/几何证据漂移失败路径测试，并验证绑定结果进入同一个 AgentTask PlanGraph。(local)
- [Completed] [Eval] [Test] Add tests for single-object semantic selection, multi-object unique binding, duplicate/ambiguity/stale/geometry-drift failures, and propagation of resolved bindings into one AgentTask PlanGraph. (local)

### 文件变更详情 / File Details

- `examples/forge-skills/pick-place-workflow/src/pick_place_workflow/multi_object_agent.py` L104-L221 [新增 / Added]: 将可用的语义实体与同一 observation、scene revision、frame、calibration 下的 measured object 和 geometry artifact 绑定；拒绝歧义、身份重复、场景漂移和重复 artifact，并返回现有 `SceneObjectBinding`。(local)
- `examples/forge-skills/pick-place-workflow/src/pick_place_workflow/multi_object_agent.py` L311-L380 [新增 / Added]: 增加 `create_task_from_scene_understanding` 和 `run_from_scene_understanding`，复用既有 runner、Coordinator 和 PlanGraph。(local)
- `examples/forge-skills/pick-place-workflow/src/pick_place_workflow/__init__.py` L39-L45, L120-L124 [修改 / Modified]: 导出语义绑定入口。(local)
- `examples/forge-skills/pick-place-workflow/tests/test_multi_object_agent.py` L160-L226 [新增 / Added]: 覆盖单对象指定、多对象唯一绑定、重复几何 artifact、ambiguity/scene/geometry 漂移失败以及 PlanGraph 传播。(local)

### 关键 Diff / Key Diff

```diff
- derived_by_ref = {item.get("artifact_ref"): item for item in derived ...}
+ for item in derived:
+     if not isinstance(item, Mapping) or not isinstance(item.get("artifact_ref"), str):
+         raise MultiObjectAgentError("scene understanding geometry evidence is invalid")
+     artifact_ref = item["artifact_ref"]
+     if artifact_ref in derived_by_ref:
+         raise MultiObjectAgentError("scene understanding geometry artifact identities are invalid")
+     derived_by_ref[artifact_ref] = item
```

```diff
+ entities = bind_understood_scene_objects(
+     understanding, measured_objects, entity_refs=entity_refs, category=category,
+ )
+ return await self.create_task(
+     task_description=task_description, entities=entities, verification=verification,
+     task_id=task_id, revision_id=revision_id,
+ )
```

### 六维验收 / Six-Dimensional Acceptance

| 维度 / Dimension | 结果 / Result | 证据 / Evidence |
| --- | --- | --- |
| 架构集成 / Architecture | PASS（provider-neutral dry-run） | 语义绑定只进入现有 `MultiObjectAgentRunner -> AgentTaskCoordinator -> PlanGraph`；无第二 scheduler、Task Store 或 provider 直连。 |
| 失败路径 / Failure paths | PASS | 覆盖 unavailable、unresolved ambiguity、重复 entity/measured/artifact、unknown selection、scene/frame/calibration/geometry drift 和缺失测量事实。 |
| 权限与安全 / Authority and safety | PASS（范围内） | 绑定层不调用 Action、不授予 motion authority；真实 Runtime、SAPIEN 和硬件未启动。 |
| 配置与复现 / Configuration and reproducibility | PASS（dry-run） | entity、benchmark object、destination、observation、scene revision、frame、calibration、geometry 和 optional evidence 均进入稳定 PlanGraph bindings。 |
| 可维护性 / Maintainability | PASS | 复用 `SceneObjectBinding`、`bind_scene_objects`、既有 runner/Coordinator；不增加平行执行路径。 |
| 可观察性 / Observability | PASS（dry-run） | 绑定结果和证据引用可从 task 的 PlanGraph 节点读取，失败原因使用明确异常信息。 |

### 验证 / Validation

- 聚焦 / Focused: `19 passed`。
- 广域回归 / Broad regression: `778 passed, 1 skipped`。
- Ruff、compileall、`git diff --check`: passed。
- 本轮证据是 provider-neutral/dry-run 的语义绑定与 PlanGraph 传播，不等同于真实机器人运动、真实 Action 完成或现场 Verifier 成功。

### Git 提交 / Git Commit

- Commit: `22f3057`; Branch: `feature/planning-loop`; 时间 / Time: 2026-09-09 (Asia/Shanghai)。
## v8.8.0 (2026-09-09 23:10) - codex

### 变更摘要 / Change Summary

- [计划] [runtime] [feat] 在持久 Runtime 的 Action endpoint 增加 adapter-owned 持物/资源 ownership 状态迁移：empty/acquiring/holding/placing/uncertain；校验 task owner、entity 和 acquire invocation 接续，并在未知/失联/取消未确认时保留物理不确定性，不虚构释放。(local)
- [Planned] [Runtime] [Feat] Add adapter-owned possession/resource ownership transitions to persistent Runtime Action endpoints: empty/acquiring/holding/placing/uncertain; validate task owner, entity, and acquire-invocation continuity, and preserve physical uncertainty on unknown/disconnected/unconfirmed cancellation without fabricating release. (local)

### 预期影响文件 / Expected Files

- `examples/forge-skills/pick-place-workflow/src/pick_place_workflow/persistent_runtime.py`
- `examples/forge-skills/pick-place-workflow/tests/test_persistent_runtime.py`

## v9.1.0 (2026-09-10 12:38) - codex

### 变更摘要 / Change Summary

- [完成] [runtime] [feat] 新增 adapter-owned persistent host 和源码树开发 dataflow，组合一个 worker/client/world、既有 deployment、七个 Tool 与标准 HTTP transport；启动只读 snapshot，失联变为 not-ready，请求串行化，关闭路径释放 worker。(local)
- [Completed] [Runtime] [Feat] Add an adapter-owned persistent host and source-tree development dataflow that compose one worker/client/world, the existing deployment, seven Tools, and the standard HTTP transport; startup is a read-only snapshot, disconnect becomes not-ready, requests are serialized, and shutdown releases the worker. (local)
- [完成] [policy] [fix] 多对象 Runner 将后续 relocation 的 observe 显式依赖前一对象 place，scene 接续不再依赖当前遍历顺序。(local)
- [Completed] [Policy] [Fix] Make each later relocation observe explicitly depend on the previous object's place so scene continuity no longer relies on the current traversal order. (local)
- [完成] [eval] [test] 增加完整七 Tool 的两对象 AgentTask 集成测试：每个 Action 等待 terminal，`runtime-0 -> runtime-4` 来自持久 worker，最终 Verifier 只在最后 scene 汇合；增加 host 配置、并发、失联和绑定失败关闭测试。(local)
- [Completed] [Eval] [Test] Add a full seven-Tool two-object AgentTask integration test: every Action waits for terminal settlement, `runtime-0 -> runtime-4` comes from the persistent worker, and the final Verifier joins only on the last scene; add host configuration, concurrency, disconnect, and bind-failure shutdown tests. (local)
- [完成] [docs] [docs] 记录开发部署变量、运行方法、六维验收和证据边界；正式 manifest-v2 profile 因缺少已发布 Node artifact 保持未注册，进化模块保持关闭。(local)
- [Completed] [Docs] [Docs] Document development deployment variables, operation, six-dimensional acceptance, and evidence boundaries; keep the formal manifest-v2 profile unregistered until a published Node artifact exists, with evolution disabled. (local)

### 文件变更详情 / File Details

- `examples/forge-adapters/robotwin20/src/robotwin20_adapter/persistent_host.py` L1-L434 [新增 / Added]: 严格 profile 加载、worker 启动 snapshot、provider/deployment/Runtime 组合、动态就绪、串行 HTTP、信号关闭和绑定失败清理。
- `examples/forge-adapters/robotwin20/profiles/forge-persistent/dataflow.yaml` L1-L10 [新增 / Added]: adapter 源码树开发 dataflow；显式标记非正式 Bundle profile。
- `examples/forge-adapters/robotwin20/profiles/forge-persistent/persistent-host.yaml` L1-L30 [新增 / Added]: host、模型、worker、Action 时限和 materializer 外部配置。
- `examples/forge-adapters/robotwin20/profiles/robotwin20/persistent-materializer.yaml` L1-L15 [新增 / Added]: complete-route materializer 静态参数；scene/candidate 参数仍由当前请求动态注入。
- `examples/forge-adapters/robotwin20/tests/test_persistent_host.py` L1-L297 [新增 / Added]: 配置闭锁、单 worker/七 Tool、startup snapshot、runtime profile、失联、HTTP discovery、串行并发和绑定失败关闭。
- `examples/forge-adapters/robotwin20/tests/test_persistent_agent_loop.py` L1-L577 [新增 / Added]: 一个 AgentTask 中两个对象完整经过七个 Tool、Action terminal 对账、scene 更新和最终 Verifier。
- `examples/forge-skills/pick-place-workflow/src/pick_place_workflow/multi_object_agent.py` L260-L284 [修改 / Modified]: 后续 relocation 显式依赖前一 relocation。
- `examples/forge-skills/pick-place-workflow/tests/test_multi_object_agent.py` L242-L270 [修改 / Modified]: 断言第二对象 observe 依赖第一对象 place。
- `examples/forge-adapters/robotwin20/README.md` L526-L573 [新增 / Added]: persistent host 环境、开发启动方式、关闭语义及正式 Bundle 边界。
- `docs/forge/PERSISTENT_MULTI_PICK_PLACE_STATUS_20260909.md` L331-L373 [新增 / Added]: v9.1.0 checkpoint、六维验收和真实运动未验收边界。

### 关键 Diff / Key Diff

#### [修改 / Modified] `multi_object_agent.py` L260-L284

**修改前 / Before:**
```python
AgentSubtaskSpec(
    subtask_id=f"relocate_{index + 1}",
    entity_ref=item.entity_ref,
    destination_ref=item.destination_ref,
)
```

**修改后 / After:**
```python
AgentSubtaskSpec(
    subtask_id=f"relocate_{index + 1}",
    entity_ref=item.entity_ref,
    destination_ref=item.destination_ref,
    depends_on=((f"relocate_{index}",) if index else ()),
)
```

**修改说明 / Rationale:** 第二对象的 observe 在 PlanGraph 中结构性等待第一对象 place，不能提前读取旧 scene。/ The second object's observe structurally waits for the first object's place in the PlanGraph and cannot read the old scene early.

#### [新增 / Added] `persistent_host.py` L135-L322, L329-L421

**新增前 / Before:**
```python
# No adapter-owned process composed the persistent worker, deployment,
# seven Tool endpoints, and Gateway-compatible HTTP lifecycle.
```

**新增后 / After:**
```python
snapshot = client.query("snapshot", {})
bundle = build_persistent_runtime_bundle(
    deployment=deployment,
    client=client,
    understanding_provider=understanding,
    grasp_provider=grasp,
    tool_context_provider=context,
)
with dispatch_lock:
    response = asyncio.run(transport.handle_async_request(request))
```

**修改说明 / Rationale:** adapter 持有唯一物理世界生命周期，Runtime 仍持有 Action lifecycle，Coordinator/Verifier 仍持有用户级成功。/ The adapter owns the sole physical-world lifetime, while Runtime retains Action lifecycle and Coordinator/Verifier retain user-level success.

#### [新增 / Added] `test_persistent_agent_loop.py` L329-L577

**关键新增 / Key Addition:**
```python
assert dict(node_scenes)["relocate_1.observe"] == "runtime-0"
assert dict(node_scenes)["relocate_2.observe"] == "runtime-2"
assert dict(node_scenes)["verify"] == "runtime-4"
assert [(item["phase"], item["scene_revision"]) for item in worker.starts] == [
    ("acquire", "runtime-0"), ("place", "runtime-1"),
    ("acquire", "runtime-2"), ("place", "runtime-3"),
]
```

**修改说明 / Rationale:** revision 是 Runtime 事实，不是固定业务编号；每个对象完整执行七个 Tool，四个 Action 有独立 invocation/attempt，Verifier 只调用一次。/ Revisions are Runtime facts rather than fixed business identifiers; each object runs all seven Tools, all four Actions have independent invocation/attempt identities, and the Verifier runs once.

### 六维验收 / Six-Dimensional Acceptance

| 维度 / Dimension | 结果 / Result | 证据 / Evidence |
| --- | --- | --- |
| 架构集成 / Architecture | PASS | 一个 AgentTask、现有 PlanningLoop、单一 persistent client/world、现有 Tool/Runtime/Gateway/Verifier；无第二 scheduler、Task Store 或 Runtime 状态库。 |
| 失败路径 / Failure paths | PASS | Action 只有 terminal 后推进；transport 丢失变为 `ready=false`；端口绑定失败关闭 world；既有 stop/replay/replan 和 failed/unknown/cancelled 回归通过。 |
| 权限安全 / Authority and safety | PASS | startup 只有 read-only snapshot，Tool context 为 `motion_authorized=false`；本轮没有模型、仿真运动或硬件调用。 |
| 配置复现 / Configuration and reproducibility | PASS | host、worker、Action 时限、provider、materializer 和 artifact 路径均由 profile/env 提供；三份 YAML 可解析。 |
| 可维护性 / Maintainability | PASS | 复用既有 persistent deployment 和通用 Runtime；对象顺序写入 PlanGraph；未把源码路径注册为正式 manifest-v2 Bundle。 |
| 可观察性 / Observability | PASS | 15 条 Tool records、4 个独立 Action invocation/attempt、scene revision、NodeSettlement 和最终 Verifier 输入/verdict 均有断言。 |

### 验证 / Validation

- 聚焦 host + Agent Loop：`5 passed`。
- Persistent adapter 回归：`34 passed`；PlanningLoop：`25 passed`；pick-place workflow：`317 passed`。
- Core：`297 passed`；完整 RoboTwin adapter：`422 passed, 1 skipped`。
- 变更文件 Ruff、compileall、三份 YAML 解析和 `git diff --check`: passed。
- 本轮是 controlled/no-motion integration；未运行 GPT/LocateAnything/SAM2/GraspGen 全链、真实 RoboTwin acquire/place 运动或硬件。

### Git 提交 / Git Commit

- Commit: `d6eea97` (implementation commit); Branch: `feature/planning-loop`; 时间 / Time: 2026-09-10 (Asia/Shanghai)。

## v9.2.0 (2026-09-10 13:54) - codex

### 预期修改 / Planned Changes

- [计划] [runtime] [feat] 增加真实 RoboTwin persistent host 的多对象 Agent Loop 执行入口，复用现有 AgentTask、PlanningLoop、Tool API、Action lifecycle 和 Runtime world，不创建第二执行器或状态库。(local)
- [Planned] [Runtime] [Feat] Add a multi-object Agent Loop execution entry point for the real RoboTwin persistent host, reusing the existing AgentTask, PlanningLoop, Tool API, Action lifecycle, and Runtime world without creating a second executor or state store. (local)
- [计划] [policy] [feat] 将真实 Query 结果逐节点传递，语义选择绑定指定实体与目的地；`manipulation.prepare` 生成 fresh route review 后暂停，只有显式绑定该 route 的 simulation approval 才可继续 Action。(local)
- [Planned] [Policy] [Feat] Carry real Query results between nodes, bind semantic selection to specified entities and destinations, pause after `manipulation.prepare` produces fresh route review, and continue Actions only after an explicit simulation approval bound to that route. (local)
- [计划] [eval] [test] 覆盖完整七 Tool、terminal Action、第二对象 fresh observe、暂停/恢复、失败不推进和最终现场 Verifier；运行真实 no-motion 阶段并保存产物，只有已有正式审批条件满足才运行仿真运动。(local)
- [Planned] [Eval] [Test] Cover all seven Tools, terminal Actions, the second object's fresh observe, pause/resume, failure-no-advance, and final current-scene verification; run the real no-motion phase and persist artifacts, executing simulation motion only when the existing formal approval conditions are satisfied. (local)
- [计划] [docs] [docs] 更新启动、审批、恢复、证据边界和六维验收；真实 provider 失败或审批缺失保持明确未完成，不伪造成功。(local)
- [Planned] [Docs] [Docs] Update startup, approval, recovery, evidence boundaries, and six-dimensional acceptance; preserve explicit incompletion on real-provider failure or missing approval without fabricating success. (local)

### 预期影响文件 / Expected Files

- `examples/forge-adapters/robotwin20/src/robotwin20_adapter/`
- `examples/forge-adapters/robotwin20/scripts/`
- `examples/forge-adapters/robotwin20/tests/`
- `examples/forge-adapters/robotwin20/README.md`
- `docs/forge/PERSISTENT_MULTI_PICK_PLACE_STATUS_20260909.md`
- `CHANGELOG.md`

### 验证 / Validation

- Command: `PYTHONPATH=PhyAgentOS:examples/forge-adapters/robotwin20/src:examples/forge-adapters/robotwin20/runtime:examples/forge-skills/pick-place-workflow/src timeout 90s .venv/bin/python examples/forge-adapters/robotwin20/scripts/check_persistent_runtime.py --runtime-root /home/yanxu/robotwin20-runtime/RoboTwin --runtime-profile examples/forge-adapters/robotwin20/profiles/robotwin20/franka-blocks-ranking.yaml --artifact-root /tmp/paos-robotwin20-agent-preflight-20260911 --worker-python /home/yanxu/miniconda3/envs/RoboTwin20/bin/python`
- Result: `status=passed`, read-only startup snapshot scene revision `e2f84bbf24f84792b3af3cbe637ab1d6-1`; no Action, motion, model inference, or hardware IO.
- `paos skill list` returned no installed Skills. The source-tree persistent deployment remains intentionally unregistered until a self-contained `robotwin20_persistent_host` Node artifact is published.

## v9.0.0 (2026-09-10 12:14) - codex

### 变更摘要 / Change Summary

- [完成] [policy] [feat] 将失败节点恢复显式拆分为 `stop/replay/replan`：只有 completed settlement 推进依赖节点；reducer replay 只读取持久事实；执行性恢复创建新 PlanRevision、重新准入并保存 retry lineage。(local)
- [Completed] [Policy] [Feat] Split failed-node recovery into explicit `stop/replay/replan`: only completed settlements advance dependencies; reducer replay reads durable facts only; execution recovery creates a new PlanRevision, reruns admission, and persists retry lineage. (local)
- [完成] [policy] [feat] 为 Planner Plugin 增加可选恢复决策协议并接入 AgentLoop 组合入口；无 Planner 时明确 stop，旧 Planner 保持 bounded replan 兼容。(local)
- [Completed] [Policy] [Feat] Add an optional recovery-decision protocol for Planner Plugins and wire it through the AgentLoop composition root; no Planner means explicit stop while existing Planners retain bounded-replan compatibility. (local)
- [完成] [sense] [fix] world-changing success/failed/unknown 结果要求可信 admission 刷新到 `new_scene_revision`；replan proposer 获取刷新后的 scene，后续对象不能沿用旧现场。(local)
- [Completed] [Sense] [Fix] Require trusted admission to refresh to `new_scene_revision` after world-changing success, failed, or unknown results; the replan proposer receives the refreshed scene and later objects cannot use the stale scene. (local)
- [完成] [docs] [docs] 同步规划设计和多对象集成审核文档，并完成六维验收。(local)
- [Completed] [Docs] [Docs] Synchronize the planning design and multi-object integration review and complete six-dimensional acceptance. (local)

### 文件变更详情 / File Details

- `PhyAgentOS/agent/planning_loop.py` L74-L126, L141-L150, L351-L435, L479-L669 [修改 / Modified]: 恢复策略类型、正常/恢复上下文、节点推进、重启 scene refresh、只读 replay、unknown 对账与新 revision replan / recovery-policy types, execution/recovery context, node progression, restart-safe scene refresh, reducer-only replay, unknown reconciliation, and new-revision replan.
- `PhyAgentOS/agent/planner_plugin.py` L82-L94, L126-L129 [新增/修改 / Added/Modified]: 可选 `RecoveryPlannerPlugin.select_recovery()` 协议及导出 / optional recovery selection protocol and export.
- `PhyAgentOS/agent/loop.py` L326-L385 [修改 / Modified]: 在真实组合入口接入 Planner replan/recovery；无插件时不构造伪 proposer / wire Planner replan/recovery at the composition root without a fake proposer.
- `PhyAgentOS/forge/task.py` L178-L198, L875-L934, L1129-L1190 [修改 / Modified]: 在 `PlanRevision` 和 Coordinator revision 路径持久化 `retry_parent_node_id` / persist retry-parent lineage through PlanRevision and Coordinator.
- `tests/test_planning_loop.py` L100-L146, L418-L704, L740-L782 [新增 / Added]: 覆盖 stale recovery provenance、stop、reducer-only replay、world-change refresh/resume、unknown reconciliation、刷新后 replan、retry lineage 与组合入口 / cover stale recovery provenance, stop, reducer-only replay, world refresh/resume, unknown reconciliation, refreshed replan, retry lineage, and composition wiring.
- `examples/forge-skills/pick-place-workflow/tests/test_multi_object_agent.py` L400-L457 [新增 / Added]: 第一个 place 改变现场后，旧 admission 在同进程及重启后均阻断第二对象 / stale admission blocks the second object both before and after restart when the first place changes the scene.
- `docs/forge/PLANNING_MODULE_DESIGN.md` L357-L426 [修改 / Modified]: 规范恢复操作、Plugin 扩展、unknown 对账和重启恢复边界 / specify recovery operations, Plugin extension, unknown reconciliation, and restart recovery.
- `docs/forge/TASK_UNDERSTANDING_MULTI_PICK_PLACE_INTEGRATION_REVIEW_20260909.md` L161-L196 [修改 / Modified]: 保存多对象恢复方案和当前实现范围 / record multi-object recovery design and current implementation scope.
- `CHANGELOG.md` L9-L44、`changelog/2026-09_part6.md` L700-L781 [修改 / Modified]: 更新最近五条、双语详细记录、Diff 与六维验收 / update latest-five index, bilingual details, diffs, and six-dimensional acceptance.

### 关键 Diff / Key Diff

```diff
-if self.replan_proposer is None:
-    return PlanningLoopResult(...)
+decision = recovery_policy(...) if recovery_policy else default_decision
+if decision == "stop":
+    return PlanningLoopResult(...)
+if decision == "replay":
+    self.reducer_replay(task_id, ...)
+    return PlanningLoopResult(..., "reducer_replay_only:<node>")
+if pending_scene_refresh is not None:
+    admission = self.admission_context_provider(task_id)
+    if admission.scene_revision != pending_scene_refresh:
+        return PlanningLoopResult(..., f"scene_refresh_required:{pending_scene_refresh}")
+    context = context.model_copy(update={"scene_revision": admission.scene_revision})
+self.coordinator.begin_revision_from_delta(...)
```

```diff
+class RecoveryPlannerPlugin(Protocol):
+    def select_recovery(...) -> Literal["stop", "replay", "replan"]: ...

-replan_proposer=lambda ...: plugin.propose_replan(...) if plugin else None
+replan_proposer = propose_replan if planner is not None else None
+recovery_policy = choose_recovery if callable(planner.select_recovery) else None
```

```diff
 class PlanRevision(BaseModel):
+    retry_parent_node_id: str | None = None

 return self.begin_revision(
+    retry_parent_node_id=delta.retry_parent_node_id,
 )
```

### 六维验收 / Six-Dimensional Acceptance

| 维度 / Dimension | 结果 / Result | 证据 / Evidence |
| --- | --- | --- |
| 架构集成 / Architecture | PASS | 复用 `PlanningLoopAdapter`、`AgentTaskCoordinator`、Planner Plugin 与既有 Runtime/Gateway 边界；未增加 scheduler、runtime 或 store。 |
| 失败路径 / Failure paths | PASS | 覆盖 stop、replay、replan、unknown reconciliation、world refresh/resume/restart、刷新后的 Planner context 和 retry lineage；失败节点不推进后继。 |
| 权限与安全 / Authority and safety | PASS | replay 不调用 executor/Gateway；unknown 不重新 POST；无 motion authority 扩张；未接入 evolution。 |
| 配置与复现 / Configuration and reproducibility | PASS | 恢复策略由可选 Plugin 接口注入；provider-neutral/no-motion 聚焦 `45 passed`，核心加 workflow `614 passed`。 |
| 可维护性 / Maintainability | PASS | 恢复选择、执行编排、revision 持久化各归既有所有者；旧 Planner Plugin 和旧 PlanRevision 数据兼容。 |
| 可观察性 / Observability | PASS | `last_failure` 暴露 failure code、`scene_refresh_required:*`、`reducer_replay_only:*`、`reconciliation_required:*`；settlement/revision/retry lineage 持久化。 |

### 验证 / Validation

- Focused: `45 passed`。
- Core plus pick-place workflow: `614 passed`。
- Ruff、compileall、`git diff --check`: passed。
- 证据范围 / Evidence scope: provider-neutral fake/no-motion；不宣称真实 Runtime、Gateway、仿真或硬件多对象闭环成功。

### Git 提交 / Git Commit

- Commit: `a329e49` (implementation commit); Branch: `feature/planning-loop`; 时间 / Time: 2026-09-10 (Asia/Shanghai)。

### 变更摘要 / Change Summary

- [完成] [runtime] [feat] place 结果显式暴露 adapter 重新解析的 `current_scene_revision`，同时保留 acquire 的原始 scene/candidate/preparation/acquire invocation provenance；缺失当前 scene revision 时拒绝 Action。(local)
- [Completed] [Runtime] [Feat] Expose the adapter-resolved `current_scene_revision` in place results while retaining acquire source scene/candidate/preparation/acquire-invocation provenance; reject the Action when current-scene revision is missing. (local)

### 文件变更详情 / File Details

- `examples/forge-skills/pick-place-workflow/src/pick_place_workflow/persistent_runtime.py` L106-L154 [修改 / Modified]: project current place scene separately from source provenance and normalize ownership settlement after evidence checks.
- `examples/forge-skills/pick-place-workflow/src/pick_place_workflow/persistent_runtime.py` L163-L182, L226-L245 [修改 / Modified]: publish `current_scene_revision` in place schema and require adapter-resolved current scene before starting place.
- `examples/forge-skills/pick-place-workflow/tests/test_persistent_runtime.py` L146-L165 [新增 / Added]: verify cross-scene place projection retains original acquire bindings.

### 关键 Diff / Key Diff

```diff
+elif self.current_scene_revision is not None:
+    result["current_scene_revision"] = self.current_scene_revision
```

```diff
+current_scene_revision = resolved.get("scene_revision")
+if not isinstance(current_scene_revision, str) or not current_scene_revision.strip():
+    raise ValueError("place preparation lacks current scene revision")
```

### 六维验收 / Six-Dimensional Acceptance

| 维度 / Dimension | 结果 / Result | 证据 / Evidence |
| --- | --- | --- |
| 架构集成 / Architecture | PASS（provider-neutral） | 当前 scene 解析仍由 adapter `resolve_preparation` 提供；Runtime 只投影与校验，不创建平行执行路径。 |
| 失败路径 / Failure paths | PASS | 缺失 current scene revision 拒绝；跨 scene 结果保留原始 provenance；既有 unknown/evidence-free 保护持续通过。 |
| 权限与安全 / Authority and safety | PASS（dry-run/no-motion） | place 必须 task-bound caller 与 adapter 当前现场；未授予 planner/motion authority。 |
| 配置与复现 / Configuration and reproducibility | PASS | source scene 与 current scene 字段分离，candidate/preparation/acquire invocation 可追踪。 |
| 可维护性 / Maintainability | PASS | 复用现有 ProjectedDriver、PersistentActionEndpoint 和 Tool schema，无新 store。 |
| 可观察性 / Observability | PASS | terminal place result 同时保留原始绑定、当前 scene 和新 scene revision/evidence。 |

### 验证 / Validation

- Focused: `12 passed`。
- Broad PAOS + workflow: `602 passed`。
- Ruff、compileall、`git diff --check`: passed。
- 未进行真实机器人运动、真实多对象现场或最终 Verifier 验收；证据范围为 provider-neutral current-scene projection。

### Git 提交 / Git Commit

- Commit: `6cff5cd`。
- Branch: `feature/planning-loop`。

### 变更摘要 / Change Summary

- [完成] [runtime] [feat] 增加 `PersistentPossession.reconcile()`，重启后只依据 adapter 明确的 available/holding_state 和 owner/entity/acquire identity 恢复持物；失联、unknown、身份不匹配保持 `uncertain`。(local)
- [Completed] [Runtime] [Feat] Add `PersistentPossession.reconcile()` so restart recovery restores possession only from explicit adapter `available`/`holding_state` and matching owner/entity/acquire identity; disconnected, unknown, or mismatched facts remain `uncertain`. (local)

### 文件变更详情 / File Details

- `examples/forge-skills/pick-place-workflow/src/pick_place_workflow/persistent_runtime.py` L79-L103 [新增 / Added]: explicit adapter-owned restart reconciliation and fail-closed identity checks.
- `examples/forge-skills/pick-place-workflow/tests/test_persistent_runtime.py` L146-L175 [新增 / Added]: matching holding recovery, mismatch retention, explicit empty release, and disconnected uncertainty tests.

### 关键 Diff / Key Diff

```diff
+    def reconcile(self, snapshot):
+        if snapshot.get("available") is not True:
+            self.state = "uncertain"
+        elif snapshot.get("holding_state") == "empty":
+            self.state = "empty"
+        elif identities_match:
+            self.state = "holding"
+        else:
+            self.state = "uncertain"
```

### 六维验收 / Six-Dimensional Acceptance

| 维度 / Dimension | 结果 / Result | 证据 / Evidence |
| --- | --- | --- |
| 架构集成 / Architecture | PASS（provider-neutral） | reconciliation 位于 Runtime possession seam，不创建恢复 scheduler、Task Store 或 Agent 状态库。 |
| 失败路径 / Failure paths | PASS | 覆盖失联、unknown、身份不匹配、显式 empty 与 matching holding。 |
| 权限与安全 / Authority and safety | PASS（dry-run/no-motion） | 只有 adapter 明确快照可改变状态；失联不释放、不重启物理 Action。 |
| 配置与复现 / Configuration and reproducibility | PASS | 对账字段和结果状态显式、可测试，无新增 speculative hash/baseline/gate。 |
| 可维护性 / Maintainability | PASS | 单一 dataclass 方法复用既有状态机，未复制生命周期实现。 |
| 可观察性 / Observability | PASS | owner/entity/acquire identity 保留或清理规则可从状态和测试追踪。 |

### 验证 / Validation

- Focused: `11 passed`。
- Broad PAOS + workflow: `601 passed`。
- Ruff、compileall、`git diff --check`: passed。
- 未进行真实机器人运动、现场恢复或最终 Verifier 验收；证据仅覆盖 provider-neutral Runtime reconciliation。

### Git 提交 / Git Commit

- Commit: `200547f`。
- Branch: `feature/planning-loop`。

### 变更摘要 / Change Summary

- [完成] [runtime] [feat] 新增 `PersistentPossession`，在同一 Runtime 的 acquire/place endpoint 之间共享 `empty -> acquiring -> holding -> placing -> empty` 状态；失败且无世界变化回到可确定状态，未知、世界变化或证据不足进入 `uncertain`，保留 owner/entity/acquire invocation。(local)
- [Completed] [Runtime] [Feat] Add `PersistentPossession` shared by acquire/place endpoints in one Runtime, enforcing `empty -> acquiring -> holding -> placing -> empty`; known no-change failures return to a determinate state, while unknown/world-change/evidence-free outcomes enter `uncertain` and retain owner/entity/acquire invocation. (local)

### 文件变更详情 / File Details

- `examples/forge-skills/pick-place-workflow/src/pick_place_workflow/persistent_runtime.py` L30-L81 [新增 / Added]: adapter-owned possession state machine and terminal settlement rules.
- `examples/forge-skills/pick-place-workflow/src/pick_place_workflow/persistent_runtime.py` L84-L122 [修改 / Modified]: projected driver settles ownership from normalized terminal outcome and returns the generated acquire invocation reference.
- `examples/forge-skills/pick-place-workflow/src/pick_place_workflow/persistent_runtime.py` L131-L147, L159-L214, L217-L239 [修改 / Modified]: publish acquire invocation output schema, enforce owner/entity/acquisition continuity, and inject one shared possession state into both Action endpoints.
- `examples/forge-skills/pick-place-workflow/tests/test_persistent_runtime.py` L112-L143 [新增 / Added]: cover successful acquire/place continuity, owner mismatch, uncertain reacquire rejection, and unconfirmed cancel retention.

### 关键 Diff / Key Diff

```diff
+@dataclass
+class PersistentPossession:
+    state: str = "empty"
+    owner: str | None = None
+    entity_ref: str | None = None
+    acquire_invocation_ref: str | None = None
+
+    def settle(self, phase, result):
+        ...
+        self.state = "uncertain"  # unknown/world-change/evidence-free outcome
```

```diff
-PersistentActionEndpoint("place", client, resolve_preparation)
+PersistentActionEndpoint("place", client, resolve_preparation, possession=possession)
```

### 六维验收 / Six-Dimensional Acceptance

| 维度 / Dimension | 结果 / Result | 证据 / Evidence |
| --- | --- | --- |
| 架构集成 / Architecture | PASS（provider-neutral） | Ownership 仅位于持久 Runtime/adapter seam；复用同一 Gateway Action lifecycle，无第二 scheduler、Task Store 或 Agent-side provider path。 |
| 失败路径 / Failure paths | PASS（contract tests） | 覆盖 known no-change failure、unknown、world-change、evidence-free success、owner/entity/acquire mismatch 和 unconfirmed cancel。 |
| 权限与安全 / Authority and safety | PASS（dry-run/no-motion） | Action 仍需 task-bound caller；状态不授予 motion authority；cancel/stop 不宣称物理释放。 |
| 配置与复现 / Configuration and reproducibility | PASS | owner、entity、generated acquire invocation、phase/status/outcome_known/world_change_started 均由同一 Runtime seam 驱动；无新增 speculative hash/baseline/gate。 |
| 可维护性 / Maintainability | PASS | 小型 dataclass 复用既有 endpoint/driver；build factory 注入单一共享状态，未复制 scheduler 或生命周期。 |
| 可观察性 / Observability | PASS（provider-neutral） | acquire result 暴露 `acquire_invocation_ref`，terminal result 保留 scene/evidence，unknown 不生成 placed completion evidence。 |

### 验证 / Validation

- Focused: `9 passed` (`test_persistent_runtime.py`).
- Broad PAOS + workflow: `599 passed`.
- Adapter test collection was not runnable in the default PAOS interpreter because `numpy` is unavailable; no adapter result is claimed.
- Ruff、compileall、`git diff --check`: passed。
- 证据范围 / Evidence scope: provider-neutral Runtime lifecycle and no-motion contract tests; no real robot motion, simulator contact, live model inference, or final field Verifier success is claimed.

### Git 提交 / Git Commit

- Commit: `f01814c`。
- Branch: `feature/planning-loop`。
- 时间 / Time: 2026-09-09 (Asia/Shanghai)。
## v8.9.0 (2026-09-09 23:35) - codex

### 变更摘要 / Change Summary

- [计划] [runtime] [feat] 增加持久 Runtime 的显式 restart/reconciliation seam：由 adapter 现场快照对账持物状态；仅在明确 holding/empty 事实和 owner/entity/acquire identity 一致时恢复，不确定或失联保持 uncertain，不自动释放。(local)
- [Planned] [Runtime] [Feat] Add an explicit restart/reconciliation seam for the persistent Runtime: reconcile possession from an adapter snapshot; restore only on explicit holding/empty facts with matching owner/entity/acquire identity, while unknown or disconnected snapshots remain uncertain without automatic release. (local)

### 预期影响文件 / Expected Files

- `examples/forge-skills/pick-place-workflow/src/pick_place_workflow/persistent_runtime.py`
- `examples/forge-skills/pick-place-workflow/tests/test_persistent_runtime.py`
## v8.10.0 (2026-09-10 00:05) - codex

### 变更摘要 / Change Summary

- [计划] [runtime] [feat] 明确 place 的当前 scene 重新准入投影：保留 acquire 的原始 scene/candidate/preparation provenance，并将 adapter 解析出的当前 `scene_revision` 暴露为 `current_scene_revision`；增加跨 scene 接续测试。(local)
- [Planned] [Runtime] [Feat] Make place current-scene re-admission explicit: retain acquire source scene/candidate/preparation provenance while exposing the adapter-resolved current `scene_revision` as `current_scene_revision`; add cross-scene continuity tests. (local)

### 预期影响文件 / Expected Files

- `examples/forge-skills/pick-place-workflow/src/pick_place_workflow/persistent_runtime.py`
- `examples/forge-skills/pick-place-workflow/tests/test_persistent_runtime.py`
## v9.2.2 (2026-09-11 09:45) - codex

### 变更摘要 / Change Summary

- [完成] [eval] [exp] 运行真实 RoboTwin 持久 worker 的只读启动预检，记录 scene snapshot、标准 Runtime 发现状态和未满足的正式 Node 前置条件；不执行 Action 或运动。(local)
- [Completed] [Eval] [Exp] Run a read-only startup preflight of the real RoboTwin persistent worker and record the scene snapshot, standard Runtime discovery state, and unmet formal Node prerequisites without Actions or motion. (local)

### 预期影响文件 / Expected Files

- `docs/forge/AGENT_LOOP_FOUNDATION_DIAGNOSIS_20260910.md`
- `CHANGELOG.md`

### 实际变更 / Actual Changes

- [修改] `docs/forge/AGENT_LOOP_FOUNDATION_DIAGNOSIS_20260910.md:L77-L89`：补充真实 `RoboTwinPersistentEngine` 只读启动预检证据、scene revision、未执行运动声明，以及正式 Skill/Node 前置条件和下一道准入门槛。
- [Modified] `docs/forge/AGENT_LOOP_FOUNDATION_DIAGNOSIS_20260910.md:L77-L89`: recorded the real `RoboTwinPersistentEngine` read-only startup evidence, scene revision, no-motion statement, and the formal Skill/Node prerequisite and next admission gate.
- [修改] `CHANGELOG.md:L11-L18`：新增 v9.2.2 预检结果索引，保留实现提交 `1e7490d` 作为证据基线。
- [Modified] `CHANGELOG.md:L11-L18`: added the v9.2.2 preflight result index while retaining implementation commit `1e7490d` as the evidence baseline.
- [修改] `changelog/2026-09_part6.md:L1157-L1180`：将预检计划标记为完成并记录命令、结果与正式 Runtime 尚未注册的原因。
- [Modified] `changelog/2026-09_part6.md:L1157-L1180`: marked the preflight plan complete and recorded the command, result, and reason formal Runtime registration remains pending.

### 验证 / Validation

- `git diff --check` passed.
- Read-only preflight result: `status=passed`, scene revision `e2f84bbf24f84792b3af3cbe637ab1d6-1`, `motion_executed=false`.

### Git 提交 / Git Commit

- Commit: `c0007ae`; Branch: `feature/planning-loop`。

## v9.3.0 (2026-09-11 10:20) - codex

### 变更摘要 / Change Summary

- [完成] [runtime] [feat] 构建可执行 `robotwin20_persistent_host` Node artifact，为 pick-place Skill 增加正式 persistent profile 与 Node lock，并完成本地 Bundle/Node 安装校验。(local)
- [Completed] [Runtime] [Feat] Build an executable `robotwin20_persistent_host` Node artifact, add the formal persistent profile and Node lock to the pick-place Skill, and verify local Bundle/Node installation. (local)

### 预期影响文件 / Expected Files

- `scripts/build_robotwin20_node.py`
- `scripts/build_robotwin20_skill_bundle.py`
- `examples/forge-skills/pick-place-workflow/skill.yaml`
- `examples/forge-skills/pick-place-workflow/profiles/robotwin-persistent/dataflow.yaml`

### 实际变更 / Actual Changes

- [新增] `scripts/build_robotwin20_node.py:L1-L156`：以确定性 ZIP payload 生成单一可执行 tar.gz Node，内嵌 adapter 的 `src/runtime/profiles` 与 workflow `src`，启动时临时解包并运行 host。
- [Added] `scripts/build_robotwin20_node.py:L1-L156`: build a deterministic single-executable tar.gz Node embedding adapter `src/runtime/profiles` and workflow `src`, extracting them into a temporary directory at launch.
- [新增] `scripts/build_robotwin20_skill_bundle.py:L1-L54`：校验 Node lock SHA-256 后打包 manifest-v2 Skill Bundle。
- [Added] `scripts/build_robotwin20_skill_bundle.py:L1-L54`: verify the Node lock SHA-256 before packaging the manifest-v2 Skill Bundle.
- [修改] `examples/forge-skills/pick-place-workflow/skill.yaml:L15-L48`：增加 `robotwin-persistent` profile、必需环境变量、Node lock 与最终 digest `c37d2ed9122095c801359224437d3225eeff559243c22a9f6d0477f09746e5c9`。
- [Modified] `examples/forge-skills/pick-place-workflow/skill.yaml:L15-L48`: add the `robotwin-persistent` profile, required environment, Node lock, and final digest `c37d2ed9122095c801359224437d3225eeff559243c22a9f6d0477f09746e5c9`.
- [新增] `examples/forge-skills/pick-place-workflow/profiles/robotwin-persistent/dataflow.yaml:L1-L7`、`persistent-host.yaml:L1-L30`：声明正式 persistent host dataflow 与外部 runtime/model 配置边界。
- [Added] `examples/forge-skills/pick-place-workflow/profiles/robotwin-persistent/dataflow.yaml:L1-L7` and `persistent-host.yaml:L1-L30`: declare the formal persistent-host dataflow and external runtime/model configuration boundary.
- [修改] `examples/forge-adapters/robotwin20/README.md:L575-L604`：记录 artifact 构建、Bundle 打包和本地安装命令及无运动边界。
- [Modified] `examples/forge-adapters/robotwin20/README.md:L575-L604`: document artifact build, Bundle packaging, local installation commands, and the no-motion boundary.

### 验证 / Validation

- Node reproducibility: two independent builds produced identical SHA-256 `c37d2ed9122095c801359224437d3225eeff559243c22a9f6d0477f09746e5c9` (`260784` bytes).
- Skill Bundle SHA-256: `1d050426f77519230952e392cf1f7c6581413b4bb03574686fa138dee294dbbb`.
- Isolated CLI install: `paos skill install --local`, `paos forge-node install --archive`, and `paos forge-node verify` all passed; installed profiles are `fake, robotwin-persistent`.
- Embedded Node smoke: installed executable `--help` passed without importing repository paths or starting the Runtime.
- Full regression: `313 passed`; Ruff and `git diff --check` passed.

### Git 提交 / Git Commit

- Content commit: `4fa4919`; Branch: `feature/planning-loop`。

## v9.3.1 (2026-09-11 10:45) - codex

### 变更摘要 / Change Summary

- [完成] [docs] [chore] 回填 v9.3.0 artifact 发布提交引用。(local)
- [Completed] [Docs] [Chore] Backfill the v9.3.0 artifact-release commit reference. (local)

### 预期影响文件 / Expected Files

- `CHANGELOG.md`
- `changelog/2026-09_part6.md`

### 实际变更 / Actual Changes

- [修改] `CHANGELOG.md:L11-L19`、`changelog/2026-09_part6.md:L1210-L1225`：记录 v9.3.0 内容提交 `4fa4919`。
- [Modified] `CHANGELOG.md:L11-L19` and `changelog/2026-09_part6.md:L1210-L1225`: record content commit `4fa4919` for v9.3.0.

### 验证 / Validation

- `git diff --check` passed.
