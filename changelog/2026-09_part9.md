## v10.3.0 (2026-09-15 14:00) - codex

### 变更摘要 / Change summary
- [agent] [feat] [完成] 将 AgentLoop 上下文治理扩展为阶段化 Tool schema、单轮 prompt budget、权威 Forge 结果紧凑摘要与任务状态重建；默认上下文设为 272K，并在 260K 触发压缩。
- [agent] [feat] [completed] Extended AgentLoop context governance with phase-aware Tool schemas, per-turn prompt budgeting, compact authoritative Forge-result summaries, and task-state context rebuilding; set the default context to 272K and trigger compaction at 260K.

### 预期影响文件 / Planned files
- `PhyAgentOS/config/schema.py`
- `PhyAgentOS/agent/memory.py`
- `PhyAgentOS/agent/tools/registry.py`
- `PhyAgentOS/agent/loop.py`
- `tests/`（新增上下文治理回归）
- `/home/yanxu/.PhyAgentOS/config*.json`（运行配置）

### 边界 / Boundary
- 仅压缩模型上下文中的非权威叙述；保留 task/revision/scene/evidence/binding/invocation 等引用。
- 不改变 Coordinator、Gateway、Runtime、Action admission、motion authority 或 Verifier 所有权；测试不启动 Runtime、仿真或运动。
- Compacting model messages is non-authoritative; persisted task/revision/tool records remain the sole execution truth.

### 文件变更详情 / File changes

#### [新增 / Added] `PhyAgentOS/agent/prompt_context.py:L1-L625`

新增纯模型请求投影层：

- 按 task creation、discovery、planning/execution、Action reconciliation、Session reconciliation 与 terminal 阶段选择模型可见 Forge wrappers；不注销 Tool，不改变 registry execution guard。
- 每次 Provider 调用前从 Coordinator 当前 AgentTask 重建 task/revision/PlanGraph/settlement/Tool record/verdict 只读投影。
- 对 superseded Forge results 做确定性紧凑摘要；达到 260K 时移除已由任务投影覆盖的旧聊天历史，并按完整 assistant/tool 单元保留最近执行上下文。
- 保留 observation、scene revision、calibration、entity、metric/object geometry、binding、planning binding、evidence、invocation、attempt 和 ToolSpec schema；为 `maxTokens` 预留响应空间，压缩后仍超过有效输入窗口时本地失败。

#### [修改 / Modified] `PhyAgentOS/agent/loop.py:L20-L23,L69-L114,L212-L226,L547-L686`

**修改前 / Before:**
```python
tool_defs = self.tools.get_definitions()
response = await self.provider.chat_with_retry(messages=messages, tools=tool_defs)
```

**修改后 / After:**
```python
request_view = self.prompt_context.build(...)
tool_defs = self.tools.get_definitions(set(request_view.visible_tool_names))
response = await self.provider.chat_with_retry(
    messages=request_view.messages,
    tools=tool_defs,
)
```

每个 iteration 记录 phase、估算 token、window、trigger、token counter source、Tool 数量和是否压缩；请求视图不写回 Session 或 AgentTask。

#### [修改 / Modified] `PhyAgentOS/agent/memory.py:L158-L184,L239-L286`

持久会话压缩从“达到硬窗口才启动”改为独立的 `compaction_trigger_tokens`；默认 260K 触发、目标为触发值的一半。旧构造调用保持兼容。

#### [修改 / Modified] `PhyAgentOS/agent/tools/registry.py:L48-L60`

`get_definitions()` 增加可选名称过滤，保持注册顺序；过滤仅影响模型可见 schema，不影响 Tool 是否注册或可执行。

#### [修改 / Modified] `PhyAgentOS/config/schema.py:L234-L270`; `PhyAgentOS/cli/commands.py:L755-L767,L969-L981`

新增默认：

```json
"contextWindowTokens": 272000,
"contextCompactionTriggerTokens": 260000
```

显式旧窗口但未配置 trigger 时按窗口 95% 推导；trigger 大于等于 hard window 时配置校验失败。CLI Agent/Gateway 均向 AgentLoop 传递该配置。

#### [新增 / Added] `tests/test_prompt_context.py:L1-L564`

覆盖默认和旧配置兼容、Tool 可见性不改变执行、五种 lifecycle phase、ToolSpec/视觉几何/权威引用保留、260K 重建、272K 本地拒绝、superseded 结果提前压缩、AgentLoop 每轮任务投影和全部 Action invocation 引用保留。

#### [修改 / Modified] 文档

- `docs/en/04-forge-configuration-reference.md:L19-L36`
- `docs/zh/04-forge-configuration-reference.md:L18-L32`
- `docs/forge/AGENT_LOOP_FOUNDATION_DIAGNOSIS_20260910.md:L80-L101`

记录配置语义、单轮膨胀原因、阶段化 schema、确定性压缩、权威所有权和本地超窗失败行为。

#### [修改 / Modified] operator-owned 配置

- `/home/yanxu/.PhyAgentOS/config.json:L8-L9`
- `/home/yanxu/.PhyAgentOS/config-rgb-no-evolution.json:L8-L9`
- `/home/yanxu/.PhyAgentOS/config-rgb-no-evolution-long.json:L8-L9`

三份配置均为 272000 hard window / 260000 compaction trigger；模型仍为 `gpt-5.6-sol`、reasoning effort 仍为 `high`，未切换 Qwen。

### 实现 Review 修复 / Implementation-review fixes

- [Major, fixed] 初版紧凑投影可能丢失 `forge_tool_context` 的完整 input/output schema；现完整保留 ToolSpec schema 与 planning binding。
- [Major, fixed] 初版任务投影缺少 frozen Skill instructions、verification、obligation、input bindings 与 produced evidence；现全部来自 Coordinator aggregate 重建。
- [完成 / Completed] 收尾审查已修复 task/revision/binding/invocation 投影缺口、cancelling/waiting 状态的 Action schema 暴露，以及仅插入任务投影时错误报告 `compacted=true` 的问题；prompt budget 现为 `contextWindowTokens - maxTokens`。
- [Completed] The closeout review fixed task/revision/binding/invocation projection gaps, Action schema exposure in cancelling/waiting states, and false `compacted=true` reporting when only a task projection was inserted; prompt budget is now `contextWindowTokens - maxTokens`.
- [Minor, accepted] 阶段化 Tool schema 只控制模型可见性，不作为安全门禁；真正准入继续由 registry guard、Coordinator、planning binding 与 Gateway 决定。

### 收尾审查文件差异 / Closeout review file diffs

#### [修改] `PhyAgentOS/agent/prompt_context.py:L169-L231,L301-L440,L510-L625`

**修改前：**
```python
status = getattr(getattr(task, "status", None), "value", getattr(task, "status", None))
allowed = generic | _TASK_COMMON | _PLANNING
return {"task_id": task.task_id, "nodes": nodes, "tool_records": records}
```

**修改后：**
```python
status = _task_status(task)
if status == "cancelling" or task.cancellation_requested:
    allowed = generic | _TASK_COMMON | _ACTION_RECONCILIATION | _SESSION_RECONCILIATION
return {
    "primary_skill_binding": _safe_json(task.primary_skill_binding),
    "runtime_binding": _safe_json(task.runtime_binding),
    "active_revision": revision_projection,
    "tool_records": records,
    "evidence_bundle_ref": task.evidence_bundle_ref,
}
```

**修改说明**：补齐取消/等待阶段的可见性边界，并把 revision、binding、node obligation、settlement、evidence、planning-bound invocation 身份写入只读请求投影；不修改 Coordinator 持久记录。

#### [修改] `PhyAgentOS/agent/loop.py:L222-L226,L579-L609`

**修改前：**
```python
self.prompt_context = AgentPromptContextManager(
    context_window_tokens=context_window_tokens,
    compaction_trigger_tokens=self.context_compaction_trigger_tokens,
)
```

**修改后：**
```python
self.prompt_context = AgentPromptContextManager(
    context_window_tokens=context_window_tokens,
    compaction_trigger_tokens=self.context_compaction_trigger_tokens,
    reserved_output_tokens=provider.generation.max_tokens,
)
```

**修改说明**：模型请求日志增加 hard window、有效输入窗口、响应预留和 compacted 状态，避免把输出 token 误算成可用输入预算。

#### [新增测试] `tests/test_prompt_context.py:L388-L564`

新增 cancelling/waiting-for-user Tool visibility、revision/binding/obligation/evidence projection、response-token reservation 和 projection-only `compacted=false` 回归。

#### [文档同步] `docs/en/04-forge-configuration-reference.md:L27-L35`; `docs/zh/04-forge-configuration-reference.md:L26-L31`; `docs/forge/AGENT_LOOP_FOUNDATION_DIAGNOSIS_20260910.md:L94-L102`

同步说明 `contextWindowTokens - maxTokens` 的有效输入预算和本地超窗行为。

### 六维验收 / Six-dimensional acceptance

1. **架构集成 / Architecture integration：PASS** — 新逻辑位于 `agent/prompt_context.py` 和 AgentLoop 请求边界；无第二 task store、scheduler、Gateway 或 verifier。
2. **失败与恢复 / Failure and recovery：PASS** — 达到 trigger 后确定性重建；为 `maxTokens` 保留响应空间，仍超有效输入窗口时在 Provider transport 前明确失败；Action/Session in-flight 阶段只暴露 reconciliation wrappers，不自动重发 Action。
3. **权限与机器人安全 / Authority and robotics safety：PASS** — 完整 SQLite/Gateway records 不变；planning binding、Action admission、frame/calibration/readiness、motion authority 与 Verifier 路径未修改；全部测试无 Runtime/仿真/运动。
4. **配置与复现 / Configuration and reproducibility：PASS** — 272K/260K 在 schema、CLI、三份实际配置及中英文文档一致；旧自定义窗口有确定性 95% 兼容规则。
5. **可维护性 / Maintainability：PASS** — ToolRegistry 使用向后兼容的可选过滤；PromptContextManager 为纯请求投影；无 TODO、placeholder、fake success 或第二生命周期实现。
6. **可观察性 / Observability：PASS** — 每轮日志包含 session、iteration、phase、tokens/window、trigger、counter source、Tool 数与 compacted 状态；投影保留所有 record/invocation/evidence identities。

### 验证 / Verification

- Prompt-context 专项：`12 passed`。
- Agent foundation/timeout/planning/dispatch/task integration/long-horizon focused：`97 passed`（增加最后一项专项测试前的组合结果；新增测试随后单独通过）。
- Core PAOS：`386 passed`。
- Pick-place Skill：`330 passed`。
- RoboTwin20 Adapter：`465 passed`。
- 阶段 schema 实测：30 个 registered Tools -> discovery 阶段 17 个 visible Tools；Tool schema 估算 `4990 -> 2954` tokens。
- 三份配置加载：均为 `272000 / 260000`；AgentLoop 为 Provider `maxTokens` 预留响应空间。
- Changed implementation Ruff、compileall、`git diff --check`：通过。
- Adapter 初次测试命令漏加 Skill source PYTHONPATH，产生 9 个 collection import errors；补齐 `examples/forge-skills/pick-place-workflow/src` 后 `465 passed`，不是生产代码失败。
- `PhyAgentOS/cli/commands.py` 全文件 Ruff 仍报告已有 L268 import formatting；本次没有修改该历史行。
- 未启动或停止 Runtime，未调用 Query/Action，未执行仿真或硬件运动。

### Git 提交 / Git Commit

- Functional commit: `e35b03b`; branch: `feature/planning-loop`.
## v10.3.1 (2026-09-15 16:40) - codex

### 变更摘要 / Change summary [完成]
- [task] [fix] 修复 planning-bound Forge Tool 终态记录未进入 NodeSettlement 的 Coordinator 结算缺口；增加只读 reconciliation 与 Query/Action/Session 终态回归，保持 Gateway、Runtime 与 motion authority 边界不变。
- [Task] [Fix] Closed the Coordinator gap where terminal planning-bound Forge Tool records were not converted into NodeSettlement; added read-only reconciliation and Query/Action/Session terminal regressions without changing Gateway, Runtime, or motion-authority boundaries.

### 预期影响文件 / Planned files
- `PhyAgentOS/forge/task.py`
- `PhyAgentOS/planning/settlement.py`
- `tests/test_planning_task_integration.py`
- `tests/test_planning_effect_recovery.py`
- `docs/forge/MANIPULATION_DAG_DEVELOPER_GUIDE.md`

### 文件变更详情 / File changes

#### [修改] `PhyAgentOS/forge/task.py:L1211-L1301,L1590-L1607,L1734-L1748,L2214-L2235,L2529-L2642`

**修改前 / Before:**
```python
self._finish_execution(task_id, record_id, status="succeeded", response=response)
# execution record ended; no NodeSettlement was persisted
```

**修改后 / After:**
```python
self._finish_execution(task_id, record_id, status="succeeded", response=response)
# _finish_execution and terminal action/session observation now call:
self.reconcile_terminal_settlements(task_id)
```

**修改说明 / Description:** Coordinator now projects persisted terminal Query/Action/Session facts into `ToolResultEnvelope`, calls pure `settle_node`, and persists one immutable `NodeSettlement`. Reconciliation is active-revision-only, no-POST, no graph replacement, idempotent for same-status duplicate submissions, and preserves conflicting-status failures.

#### [修改] `PhyAgentOS/planning/settlement.py:L8-L35`

**修改说明 / Description:** `settle_node()` accepts an absent current scene revision for read-only terminal records while retaining stale-scene rejection whenever a prior trusted scene revision exists.

#### [修改] `tests/test_planning_task_integration.py:L92-L329`

**修改说明 / Description:** Added regressions for automatic settlement, all three Tool semantics, legacy terminal-record reconciliation, idempotent replay, and Action/Session observation paths.

#### [修改] `examples/forge-skills/pick-place-workflow/tests/test_multi_object_agent.py:L687-L699`

**修改说明 / Description:** The verifier-rejection fixture now emits the declared `placed:<entity>` evidence for relocation/place Query results so automatic settlement exercises the real produced-evidence contract.

#### [修改] `docs/forge/MANIPULATION_DAG_DEVELOPER_GUIDE.md:L329-L338`

**修改说明 / Description:** Documented Coordinator terminal settlement ownership, restart reconciliation, no-retry/no-graph-replacement behavior, and the rule that `forge_plan_ready` reads durable settlements only.

### 六维验收 / Six-dimensional acceptance

1. **架构集成 / Architecture integration：PASS** — settlement conversion remains in `AgentTaskCoordinator`; `PhyAgentOS.planning` stays pure; `forge_plan_ready`, AgentLoop, Provider, Runtime, and Gateway ownership are unchanged.
2. **失败与恢复 / Failure and recovery：PASS** — failed, unknown, stopped, cancelled, missing produced evidence, stale scene, and provider-level Query failure map through existing `settle_node`; reconciliation is active-revision-only and never retries a Tool; legacy PlanningLoop duplicate settlement is same-status idempotent while conflicting status fails closed.
3. **权限与机器人安全 / Authority and robotics safety：PASS** — no Gateway call, Action POST, Session start, Runtime mutation, simulator step, hardware IO, or `motion_authorized=true`; unknown physical effects remain non-success and require existing observation/recovery.
4. **配置与复现 / Configuration and reproducibility：PASS** — no new provider/runtime/path/credential configuration; behavior is deterministic from persisted Tool records, PlanGraph, and trusted scene/evidence references; tests use fake clients only.
5. **可维护性 / Maintainability：PASS** — one Coordinator reconciliation seam is reused by Query/Action/Session terminal paths; response fact projection is provider-neutral and bounded; no second task store, scheduler, or execution protocol introduced.
6. **可观察性 / Observability：PASS** — `node_settled` events retain revision/node/status; terminal record identities and evidence refs remain authoritative; conflicting settlement attempts are explicit errors.

### 验证 / Verification

- Core PAOS: `393 passed` with `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 pytest -p pytest_asyncio.plugin -q`.
- Planning focus: `45 passed`; Skill suite: `330 passed`; RoboTwin20 Adapter suite: `465 passed`.
- Changed-file Ruff, `python -m compileall -q`, and `git diff --check` passed.
- No Runtime, Gateway Action/Session, simulator, hardware, or motion was started.

### Git 提交 / Git Commit

- Functional commit: `523ac98`; branch: `feature/planning-loop`.

### 设计边界 / Design boundary
- Coordinator 是 task/revision/execution/settlement 的唯一持久化所有者；`forge_plan_ready` 只读取 settlement，不推断或伪造完成状态。
- Planning 模块保持纯函数；不调用 Gateway、不写 SQLite、不授权运动。
- Reconciliation 只处理当前 active revision 中已有、terminal、完整 planning binding 且尚无 settlement 的记录；不重发 Tool、不替换已执行图。

## v10.3.15 (2026-09-16 01:32) - codex

### 变更摘要 / Change Summary [完成]
- [sense] [fix] 修复 `grasp.propose` 组合边界的 provenance closure：允许候选引用请求中已完成 observation/entity/frame/calibration 绑定的直接几何 artifact，同时继续拒绝未绑定 artifact；不新增 hash、冻结 contract 或动作门禁。(local)
- [Sense] [Fix] Fix the `grasp.propose` composition-boundary provenance closure: allow candidates to cite directly bound geometry artifacts from the request while continuing to reject unbound artifacts; add no hash, frozen contract, or motion gate. (local)
- [comm] [fix] 输出校验失败时保留真实 candidate set、scene revision、frame 与 calibration 身份，避免 `unknown/unknown` 占位掩盖真实失败阶段；状态继续为 `invalid` 且候选为空。(local)
- [Comm] [Fix] Preserve the real candidate-set, scene-revision, frame, and calibration identities on output-validation failure so `unknown/unknown` placeholders do not hide the failure stage; status remains `invalid` with no candidates. (local)
- [policy] [docs] 在 provider-neutral pick/place Skill 中明确：目标位置仍被占用的重排 cycle 必须先选择经当前场景证据验证的临时 staging destination，不能生成直接覆盖占用位置的顺序计划。(local)
- [Policy] [Docs] Clarify in the provider-neutral pick/place Skill that rearrangement cycles with an occupied destination require a currently evidenced temporary staging destination instead of a sequential overwrite plan. (local)
- [eval] [test] 增加真实 RoboTwin grasp provider 经 Core Endpoint 的跨层回归，以及无关 provenance 继续 fail-closed 的反例；执行专项、Core、Skill、Adapter 验证和七维源码 review。(local)
- [Eval] [Test] Add a cross-layer regression through the real RoboTwin grasp provider and Core Endpoint plus a negative unbound-provenance case; run focused, Core, Skill, Adapter validation and a seven-dimension source review. (local)

### 失败场景与 Anti-OverDefense 说明 / Failure Scenario and Anti-OverDefense Rationale
- 真实任务 `task_cb763eca93034d2a` 中，RoboTwin provider 以已绑定点云 `artifact_ref` 作为候选直接 provenance，而 Core 白名单仅包含点云的上游 RGB/depth provenance，导致有效候选确定性返回 `invalid_provenance`。
- Git、版本号、类型与现有普通测试不能发现该问题：Core 测试使用 RGB provenance fake provider，Adapter 测试只验证 provider 输出，没有覆盖真实 Adapter→Endpoint 组合边界。修复仅补齐已有请求图中的直接证据闭包和一个跨层测试，不增加推测性 gate。
- 当前 Runtime、AgentTask 和物理世界不由本次源码修改操作；不调用 Gateway，不执行 Query、Action、Session、仿真步进或硬件运动。

### 预期影响文件 / Planned Files
- `PhyAgentOS/forge/capability_runtime/grasp_proposal.py`
- `examples/forge-adapters/robotwin20/tests/test_grasp_proposal.py`
- `examples/forge-skills/pick-place-workflow/SKILL.md`
- `examples/forge-skills/pick-place-workflow/skill.yaml`
- `examples/forge-skills/pick-place-workflow/pyproject.toml`
- `docs/forge/MANIPULATION_DAG_DEVELOPER_GUIDE.md`
- `CHANGELOG.md`

### 文件变更详情 / File Changes

#### [修改] `PhyAgentOS/forge/capability_runtime/grasp_proposal.py:L252-L297,L447-L527,L561-L733`

**修改前 / Before:**
```python
allowed_provenance = {
    ref
    for target in arguments["targets"]
    for ref in target["spatial_envelope"]["provenance"]
}
allowed_provenance.update(
    ref
    for target in arguments["targets"]
    for artifact in target.get("geometry_artifacts", [])
    for ref in artifact["provenance"]
)
return _error(snapshot_error, message, observation_ref=observation_ref)
```

**修改后 / After:**
```python
allowed_provenance_by_entity: dict[str, set[str]] = {}
for target in arguments["targets"]:
    target_provenance = allowed_provenance_by_entity.setdefault(
        target["entity_ref"], set()
    )
    target_provenance.update(target["spatial_envelope"]["provenance"])
    for artifact in target.get("geometry_artifacts", []):
        target_provenance.add(artifact["artifact_ref"])
        target_provenance.update(artifact["provenance"])
return _bound_error(
    snapshot_error,
    message,
    arguments=arguments,
    candidate_set_ref=candidate_set_ref,
)
```

**修改说明 / Description:** Core Endpoint 只接受候选自身实体的请求绑定 provenance 闭包，包括直接几何 artifact 和其上游 provenance；其他实体、其他场景、帧或标定的几何 artifact 仍在请求校验或候选校验处失败。Provider unavailable、exception 与非法输出继续返回空候选和非成功状态，同时保留已经验证的请求身份。

#### [修改] `examples/forge-skills/pick-place-workflow/tests/test_grasp_propose.py:L259-L269,L377-L460,L475-L506`

**修改说明 / Description:** 增加直接几何引用正例、无关引用与跨实体引用反例，并验证 provider exception、malformed snapshot 和 invalid provenance 都保留真实 candidate-set/scene/frame/calibration 身份且不产生候选。

#### [修改] `examples/forge-adapters/robotwin20/tests/test_grasp_proposal.py:L99-L113`

**修改前 / Before:** Adapter provider 与 Core Endpoint 分开测试。

**修改后 / After:**
```python
result = GraspProposalEndpoint(provider).invoke(REQUEST)
assert result["status"] == "available"
assert result["candidates"][0]["provenance"] == [
    REQUEST["targets"][0]["geometry_artifacts"][0]["artifact_ref"]
]
```

**修改说明 / Description:** 使用真实 `GraspGenProposalProvider -> GraspProposalEndpoint` 组合路径复现并关闭原始 `invalid_provenance` 缺口。

#### [修改] `examples/forge-skills/pick-place-workflow/SKILL.md:L34-L42`

**修改说明 / Description:** Agent 继续负责多对象重排的语义规划，但不能把对象放到仍被占用的位置；循环重排使用当前观测与绑定证据支持的 staging destination，并在世界变化后重新观测和绑定。Core 不提供硬编码坐标或固定 buffer。

#### [修改] `examples/forge-skills/pick-place-workflow/skill.yaml:L1-L3`; `examples/forge-skills/pick-place-workflow/pyproject.toml:L1-L3`

```diff
- version: 2.0.7
+ version: 2.0.8
```

**修改说明 / Description:** 同步 Skill manifest 与 Python package 版本；Node 生产源码未变，继续锁定 `robotwin20_persistent_host 0.1.6`。

#### [修改] `docs/forge/MANIPULATION_DAG_DEVELOPER_GUIDE.md:L144-L152,L184-L190`

**修改说明 / Description:** 记录 Agent/Adapter/Core 在 cycle breaking、几何可行性和 provenance validation 上的所有权，以及失败响应保留绑定身份但不授权运动的语义。

### 七维源码验收 / Seven-Dimension Source Review

1. **架构集成 / Architecture integration：PASS** — provenance closure 位于 provider-neutral `GraspProposalEndpoint` 组合边界；RoboTwin provider 仍只生成候选，Coordinator/Gateway/Runtime/Verifier 所有权不变。
2. **失败恢复 / Failure recovery：PASS** — unavailable、provider exception、malformed snapshot、invalid provenance 均返回空候选与明确非成功状态；诊断身份来自验证后的原请求，不从失败输出反推。
3. **权限与机器人安全 / Authority and robotics safety：PASS** — `grasp.propose` 仍是只读 Query，不做 IK、碰撞、轨迹或运动授权；frame/calibration/entity 绑定、prepare readiness、Action admission 与 stop 路径未放宽。
4. **配置与复现 / Configuration and reproducibility：PASS** — Core/manifest/package 版本关系明确；Skill 2.0.8 复用内容一致的 Node 0.1.6，未新增路径、坐标、模型名、凭据或设备配置。
5. **可维护性 / Maintainability：PASS** — 新参数向后兼容原全局 provenance 调用；Endpoint 使用单一按实体映射，无 Adapter 特例、第二验证器、TODO、placeholder success 或重复 lifecycle。
6. **可观察性 / Observability：PASS** — 在验证请求之后发生的失败保留 candidate set、scene revision、frame 和 calibration；状态、错误码、空候选与 funnel 仍明确。
7. **AgentLoop 自主性 / AgentLoop autonomy：PASS** — 没有新增硬编码 discovery capability 或 Tool 可见性限制；模型仍选择对象、顺序、staging 策略和 Query 参数，只在物理占位与证据不足时由既有安全边界拒绝。

### 验证与发布物 / Validation and Artifact

- 专项 Core/Skill/Adapter 组合：`69 passed`。
- Core PAOS：`402 passed`；Pick-place Skill：`333 passed`；RoboTwin20 Adapter：`472 passed`。
- Ruff、compileall、`git diff --check`：通过。
- Skill Bundle：`/tmp/paos-skill-2.0.8-review-2hf8Zr/pick-place-workflow-2.0.8.tar.gz`。
- Bundle SHA-256：`fb16d4a507af475a5a093015bebd6d4b0be6a73b21ac569edf59e5a81dd6360c`；size `112104` bytes；归档 manifest 显示 Skill `2.0.8`。
- Node：复用已验证的 `robotwin20_persistent_host-0.1.6-linux-x86_64.tar.gz`，SHA-256 `c07fa90eb526470c48f94c23d16ffaba40a33cb1ffb12e8daf13bd8c17f8f486`，与 Skill lock 一致。
- 未安装 Skill、未停止或重启 Runtime、未调用 Gateway Query/Action/Session、未执行仿真步进或硬件运动。

### Git 提交 / Git Commit

- Commit: pending; branch: `feature/planning-loop`.
## v10.4.0 (2026-09-16 10:00) - codex

### 变更摘要 / Change Summary
- [comm] [完成] 为模型驱动 Query 暴露与 GraspGen worker 配置对齐的可配置超时，避免调用方 deadline 早于 provider 正常完成窗口。
- [policy] [完成] 将可恢复的 task-bound Query 失败投影到 Coordinator 的 `awaiting_replan` 生命周期，保留无动作、fail-closed 和 Agent 自主选择 stop/replan 的边界。
- [policy] [完成] 统一 discovery completion 与 Coordinator settlement 的 Query 状态投影，拒绝把响应为 unavailable/invalid/unknown 的 succeeded 记录当作前置发现成功。
- [test] [完成] 增加超时投影、Query 失败状态迁移、失败 discovery 拒绝和不可触发 Action 的回归覆盖。
- [Comm] [Completed] Made model-backed Query deadlines configurable and aligned with the GraspGen worker budget so callers do not disconnect before a valid provider result can return.
- [Policy] [Completed] Projected recoverable task-bound Query failures into the Coordinator `awaiting_replan` lifecycle while preserving no-motion, fail-closed behavior and Agent choice between stop and replan.
- [Policy] [Completed] Unified discovery completion with Coordinator settlement status so unavailable/invalid/unknown responses cannot satisfy discovery prerequisites.
- [Test] [Completed] Added timeout, Query lifecycle, failed-discovery admission, and no-Action regression coverage.

### 影响文件 / Affected Files
- `PhyAgentOS/forge/task.py`
- `PhyAgentOS/agent/prompt_context.py`
- `PhyAgentOS/forge/binding.py`
- `PhyAgentOS/forge/capability_runtime/runtime.py`
- `PhyAgentOS/forge/capability_runtime/grasp_proposal.py`
- `examples/forge-skills/pick-place-workflow/contracts/grasp.propose.tool.yaml`
- `tests/test_prompt_context.py`
- `tests/test_planning_task_integration.py`

### 实际变更 / Implemented Changes

#### [修改] `PhyAgentOS/agent/prompt_context.py:L14-L55`

**修改前 / Before:** discovery completion counted every active-revision record whose transport status was `succeeded`.

**修改后 / After:** `_discovery_record_succeeded` also inspects the persisted response status and excludes provider-level `unavailable`, `invalid`, `stale`, `empty`, `failed`, and `unknown` outcomes.

**修改说明 / Description:** Prevent provider failures wrapped in a successful transport envelope from unlocking PlanGraph materialization.

#### [修改] `PhyAgentOS/forge/task.py:L1180-L1185,L1503-L1508,L2273-L2310`

**修改后 / After:** Bound Query timeout is raised to the ToolSpec `default_timeout_ms`; terminal planning Query failures and transport-unknown results project the task to `awaiting_replan`; discovery expansion uses normalized planning status rather than raw transport status.

**修改说明 / Description:** Keep the caller deadline no shorter than the frozen ToolSpec budget, expose a legal recovery lifecycle, and reject failed discovery evidence at Coordinator admission.

#### [修改] `PhyAgentOS/forge/binding.py:L28-L34,L189-L193,L281-L286`; `PhyAgentOS/forge/capability_runtime/runtime.py:L93-L100`; `PhyAgentOS/forge/capability_runtime/grasp_proposal.py:L50-L56`; `examples/forge-skills/pick-place-workflow/contracts/grasp.propose.tool.yaml:L1-L7`

**修改后 / After:** Added validated optional `default_timeout_ms` to ToolSpec and frozen bindings; Grasp proposal declares `180000ms` minimum Query budget.

**修改说明 / Description:** Make the timeout part of the provider-neutral ToolSpec/binding contract instead of an AgentLoop Tool-ID constant.

#### [新增] `tests/test_prompt_context.py:L261-L281`; `tests/test_planning_task_integration.py:L167-L366`

**新增内容 / Added:** Regression coverage for provider failures not unlocking discovery, ToolSpec timeout floors, planning Query failure/unknown recovery, and direct Coordinator admission.

### 代码差异摘要 / Diff Summary

```diff
- completed = {record.tool_id for record in records if record.status == "succeeded"}
+ completed = {record.tool_id for record in records if _discovery_record_succeeded(record)}
- timeout_ms = caller_timeout
+ timeout_ms = max(caller_timeout or 0, tool.default_timeout_ms)
- planning Query failure left task executing with no ready nodes
+ terminal planning Query failure -> awaiting_replan (no Action/Session)
```

### 七维源码验收 / Seven-Dimension Source Review

1. **架构集成 / Architecture integration：PASS** — timeout metadata belongs to ToolSpec/binding; lifecycle projection remains Coordinator-owned; AgentLoop only projects visibility and does not duplicate Runtime state.
2. **失败恢复 / Failure recovery：PASS** — provider-level Query failures no longer unlock discovery; admitted planning Query failure/unknown outcomes expose the existing bounded replan state without synthesizing a retry.
3. **权限与机器人安全 / Authority and robotics safety：PASS** — all modified paths are Query/control-plane paths; no Action/Session invocation, motion authorization, calibration bypass, or physical retry was added.
4. **配置与复现 / Configuration and reproducibility：PASS** — timeout is validated and frozen in ToolSpec/binding; GraspGen worker startup/request/shutdown budgets remain external adapter configuration; Skill version advances to 2.0.8.
5. **可维护性 / Maintainability：PASS** — shared normalized response facts are reused; no new provider-specific branch in Coordinator; legacy records without response remain compatible.
6. **可观察性 / Observability：PASS** — recovery event records the failed node; structured finalization and provider error identity remain available to the Agent.
7. **AgentLoop 自主性 / AgentLoop autonomy：PASS** — the model still chooses Query arguments, node composition, stop, clarification, or replan; only proven prerequisite failures and lifecycle states shape the visible surface.

### 验证 / Validation

- Core tests: `407 passed`.
- Skill tests: `334 passed`.
- Focused Core/Skill/Adapter integration: `110 passed`.
- Ruff, compileall, and `git diff --check`: passed.
- Full Adapter collection remains environment-limited for optional worker/script modules; focused GraspGen adapter tests passed.

### Git 提交 / Git Commit
- Commit: `733700f` (implementation commit)
- Branch: `feature/planning-loop`
- 时间: 2026-09-16 10:00 (America/Los_Angeles)

### 追加修复 / Additional Fix
- [policy] [完成] 将 active PlanGraph 中只读 planning Query 的 transport `unknown` 也投影到 `awaiting_replan`，避免 timeout 后无 ready 节点且无法合法恢复；pre-graph discovery 与 Action/Session 保持原语义。
- [Policy] [Completed] Projected transport `unknown` for read-only planning Queries in an active PlanGraph into `awaiting_replan` so a timeout cannot leave the task with no ready node and no legal recovery; preserved pre-graph discovery and Action/Session semantics.
## v10.5.0 (2026-09-16 03:41) - codex

## v10.6.1 (2026-09-16 20:30) - codex

### 变更摘要 / Change summary [完成]
- [env] [chore] 将包含 Qwen3-VL-4B vLLM 场景理解适配器的 `pick-place-workflow` Skill 从 `2.0.8` 升级并打包为 `2.0.9`，同时锁定新的 `robotwin20_persistent_host` Node `0.1.7`。
- [env] [chore] Safely reconciled two cancelled AgentTasks, stopped the old Runtime, installed the Skill and Node locally, and left the replacement Runtime stopped for the user's task test.
- [Env] [Chore] Upgraded and packaged `pick-place-workflow` from `2.0.8` to `2.0.9` with the Qwen3-VL-4B vLLM scene-understanding adapter and locked `robotwin20_persistent_host` Node `0.1.7`.

### 影响文件 / Affected files
- `examples/forge-skills/pick-place-workflow/skill.yaml:L1-L3,L57-L67`
- `changelog/2026-09_part9.md`（本条记录）
- Installed Skill: `/home/yanxu/.PhyAgentOS/skills/pick-place-workflow/`
- Installed Node: `/home/yanxu/.PhyAgentOS/forge_runtime/nodes/robotwin20_persistent_host/versions/robotwin20_persistent_host-0.1.7-linux-x86_64/`

### 关键 Diff / Key diff
```diff
- version: "2.0.8"
+ version: "2.0.9"
- artifact_id: robotwin20_persistent_host-0.1.6-linux-x86_64
+ artifact_id: robotwin20_persistent_host-0.1.7-linux-x86_64
- sha256: c07fa90e...17f8f486
+ sha256: 7be228e...2866c6b
```

### 边界 / Boundary
- 仅部署 Skill/Node；未启动替换 Runtime、Gateway、Action、Session 或机器人运动。
- Qwen 本地 vLLM 仍由 operator-owned endpoint 提供，Skill 不保存 `HEPHAESTUS_LOCAL_VLM_API_KEY` 或任何 secret。

### 验证 / Validation
- 旧 Runtime 初始为 `running`；`task_f7109e7bedf743f6` 与 `task_346e237172e14215` 均已确认 `cancelled`，活动绑定清空后正常停止旧 Runtime，Dora flow down。
- Node archive: `/tmp/paos-robotwin20-release/robotwin20_persistent_host-0.1.7-linux-x86_64.tar.gz`, SHA-256 `7be228edd4a810d7711d716e5c43a84138b3e0a13c13bbc34e6c7fd382866c6b`.
- Skill archive: `/tmp/paos-robotwin20-release/skills/pick-place-workflow-2.0.9.tar.gz`, SHA-256 `1c90cc8a69362eedf44f225eb094bbe26c77639849434b9f047813c4aeec5190`.
- `paos skill install --local --yes`, `paos forge-node install`, and `paos forge-node verify` passed; installed Skill reports `2.0.9`, Node `0.1.7` SHA verified, Runtime state `stopped` with no active invocation/session/task binding.
- No new scene-understanding request or physical action was launched during packaging/install; the user can start the Runtime and initiate the task test explicitly.

### Git 提交 / Git commit
- Implementation commit: `2282f14`; Branch: `feature/planning-loop`; 时间 / Time: 2026-09-16 (Asia/Shanghai).

## v10.6.0 (2026-09-16 17:00) - codex

### 变更摘要 / Change summary [完成]
- [sense] [feat] 将已在 `hephaestus-vlm` 环境验证的 Qwen3-VL-4B-Instruct vLLM OpenAI-compatible endpoint 接入 PAOS 场景理解，并在本地 provider 不可用或响应不合约时显式回退 GPT-5.6。
- [sense] [feat] 保持 `scene.understand` provider-neutral、只读和 no-motion；将模型进程生命周期留在外部 vLLM Runtime，避免每次 Query 重载权重。
- [Sense] [Feat] Integrate the Qwen3-VL-4B-Instruct vLLM OpenAI-compatible endpoint validated in the `hephaestus-vlm` environment into PAOS scene understanding, with explicit GPT-5.6 fallback on local-provider unavailability or contract failure.
- [Sense] [Feat] Keep `scene.understand` provider-neutral, query-only, and no-motion; leave model-process lifecycle to the external vLLM Runtime so each Query does not reload weights.

### 预期影响文件 / Planned files
- `examples/forge-adapters/robotwin20/src/robotwin20_adapter/qwen3_vl_vllm_scene_understanding.py`
- `examples/forge-adapters/robotwin20/src/robotwin20_adapter/scene_understanding_fallback.py`
- `examples/forge-adapters/robotwin20/src/robotwin20_adapter/persistent_host.py`
- `examples/forge-adapters/robotwin20/profiles/forge-persistent/persistent-host.yaml`
- `examples/forge-adapters/robotwin20/pyproject.toml`
- `examples/forge-adapters/robotwin20/tests/test_qwen3_vl_vllm_scene_understanding.py`
- `examples/forge-adapters/robotwin20/tests/test_scene_understanding_fallback.py`
- `examples/forge-adapters/robotwin20/tests/test_persistent_host.py`

### 边界 / Boundary
- 不在 PAOS 中启动、停止、sleep 或 wake vLLM；PAOS 只调用 operator-owned endpoint。
- 不把 fallback 变成静默成功：route/failure 状态只在 adapter 诊断面保留，公共 Tool 仍只返回 provider-neutral claims。
- 不保存任何 API key；GPT credentials remain process-environment only.

### 文件变更详情 / File details

- [新增] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/qwen3_vl_vllm_scene_understanding.py` L1-L242：通过 OpenAI-compatible `/v1/chat/completions` 调用 operator-owned Qwen3-VL-4B-Instruct vLLM，使用已验证的本地 scene-graph schema，并投影为 PAOS semantic claims；不输出 metric envelope 或 motion authorization。
- [新增] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/scene_understanding_fallback.py` L1-L54：Qwen 主路由失败/空结果时显式调用 GPT fallback；只在 adapter 诊断面保留 route/error。
- [修改] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/persistent_host.py` L264-L308：新增 `qwen3_vl_vllm_fallback` provider 分支，配置 `gpt-5.6-sol` + `reasoning_effort=high` 托底。
- [修改] `examples/forge-adapters/robotwin20/profiles/forge-persistent/persistent-host.yaml` L17-L31；`examples/forge-skills/pick-place-workflow/profiles/robotwin-persistent/persistent-host.yaml` L17-L31：切换为 `qwen3_vl_vllm_fallback`，本地 endpoint `127.0.0.1:8012`，Qwen 不使用 API key，GPT 仍读取 `ROBOTWIN20_MODEL_API_KEY`。
- [修改] `examples/forge-skills/pick-place-workflow/profiles/robotwin-persistent/dataflow.yaml` L12-L15；`skill.yaml` 环境声明；`runtime.env.example`：移除旧 Transformers 模型路径/解释器和 `HEPHAESTUS_LOCAL_VLM_API_KEY`。
- [新增] `examples/forge-adapters/robotwin20/tests/test_qwen3_vl_vllm_scene_understanding.py` L1-L94；`test_scene_understanding_fallback.py` L1-L59：覆盖多模态请求、schema 投影、主备路由和双失败 fail-closed。
- [修改] `examples/forge-adapters/robotwin20/README.md` L144-L190：记录 `paos-qwen3vl-4b-vllm` 环境克隆、vLLM 0.11.2/Torch 2.9.0+cu128/Transformers 4.57.6 设置、冷启动只在外部 Runtime 发生和 GPT fallback 运行方式。

**关键 Diff / Key diff**

```diff
- provider: qwen3_vl_local
+ provider: qwen3_vl_vllm_fallback
+ primary: http://127.0.0.1:8012/v1, qwen3-vl-4b-awq
+ fallback: gpt-5.6-sol, reasoning_effort: high
- HEPHAESTUS_LOCAL_VLM_API_KEY
+ loopback vLLM endpoint without local API key
```

### 验证 / Validation

- Dedicated environment: `paos-qwen3vl-4b-vllm` cloned from `hephaestus-vlm`; verified Python 3.11.16, Torch `2.9.0+cu128`, vLLM `0.11.2`, Transformers `4.57.6`, `qwen-vl-utils`, and OpenAI `2.46.0`.
- Live vLLM startup: profile/warmup `25.06 s`, model memory `3.701 GiB`, service ready at `127.0.0.1:8012`; no PAOS Runtime/Action/motion started.
- Live one-image semantic smoke: HTTP 200, JSON/schema/entity checks passed, `6.128668 s` while an existing RoboTwin worker occupied `3.6 GiB`, vLLM peak `4710 MiB`.
- Live six-case vLLM evaluation: JSON/schema `6/6`, entity semantic `6/6`, exact repeat `true`, mean request `2.840267 s`, aggregate `98.46491 token/s`, vLLM peak `4710 MiB`; relation semantic `1/6`, so latency/memory match the prior vLLM envelope but relation quality is not yet equivalent to the prior benchmark claim.
- PAOS provider live projection: 3 entities, 2 relations, empty spatial envelopes; focused adapter tests `11 passed`; Ruff, compileall, and `git diff --check` passed.
- Transformers comparison attempt was intentionally not treated as a result: running it concurrently with vLLM plus the existing RoboTwin worker exhausted the 16-GiB GPU and produced CUDA OOM; no false equivalence claim is made.

### Git 提交 / Git commit
- Commit: pending until commit completes; Branch: `feature/planning-loop`; 时间 / Time: 2026-09-16 (Asia/Shanghai).

### 变更摘要 / Change summary [完成]
- [agent] [refactor] 将 AgentLoop 的恢复提交改为语义节点输入，由 Coordinator/Planning Adapter 编译新 PlanRevision；保留低层完整图接口供内部使用。
- [agent] [refactor] 明确 planning selection receipt 与纯 `planning_binding` 的边界，避免模型复制诊断字段进入执行协议。
- [test] [feat] 增加通用恢复编译、绑定字段边界和无动作恢复回归验证。
- [agent] [refactor] Route AgentLoop recovery through semantic node input compiled into a new PlanRevision by Coordinator/Planning Adapter, while retaining the low-level complete-graph path for internal callers.
- [agent] [refactor] Clarify the boundary between planning selection receipts and the pure `planning_binding` execution contract so diagnostic fields are not copied into execution protocol.
- [test] [feat] Add generic recovery compilation, binding-boundary, and no-motion recovery regression coverage.

### 预期影响文件 / Planned files
- `PhyAgentOS/agent/tools/forge_task.py`
- `PhyAgentOS/agent/tools/planning.py`
- `PhyAgentOS/agent/plan_proposal.py`
- `PhyAgentOS/agent/tools/forge_tool_api.py`（未修改；保留既有执行边界）
- `tests/test_planning_task_integration.py`
- `tests/test_planning_selection.py`

### 边界 / Boundary
- 不新增 provider/tool-id 专用分支；模型仍决定语义节点、顺序和恢复策略。
- 不改变 Action/Session admission、motion authorization 或 Runtime/Verifier 所有权；恢复阶段仅产生新 revision，不执行物理动作。
- 不让模型生成 `revision_id`、graph digest、policy digest 或 planning binding 完整性字段。

### 文件变更详情 / File changes

#### [修改] `PhyAgentOS/agent/tools/forge_task.py:L142-L197`

**修改前 / Before:** `forge_task_begin_revision` 暴露 `plan_graph`/`plan_graph_ref`，允许 Agent 直接提交完整 revision 元数据，且可以创建无图 revision。

**修改后 / After:** 模型可见 schema 要求完整语义 `nodes`；工具通过 `compile_task_plan` 生成 revision/digest/ref，并拒绝空恢复或 nodes 与完整图混用。执行仍只调用 Coordinator 的 `begin_revision`，不调用 Gateway。

#### [修改] `PhyAgentOS/agent/tools/planning.py:L13-L19,L80-L116`

**修改前 / Before:** 直接把 Coordinator receipt 的所有字段作为 `planning_binding` 返回。

**修改后 / After:** 只投影五个执行 binding 字段；`task_id`、`revision_id`、`scene_revision` 放入独立 `selection` 诊断对象，并在 receipt 缺少 binding 字段时结构化失败。

#### [修改] `PhyAgentOS/agent/plan_proposal.py:L19-L73`

**修改前 / Before:** 用固定 capability 名称集合判断 root discovery 节点。

**修改后 / After:** 从绑定 ToolSpec 的 `semantics == "query"` capability 动态推导 pre-plan 能力，保留 root Query 不能预声明 produced evidence 的通用规则。

#### [新增] `tests/test_planning_task_integration.py:L249-L305`; [修改] `tests/test_planning_selection.py:L60-L73`

新增 Agent 恢复编译、revision 元数据归属、无执行记录/无 motion，以及 selection binding 与诊断字段隔离回归测试。

### 验证 / Validation

- 全量 Core tests: `408 passed`。
- Focused planning/AgentLoop tests: `49 passed`。
- `ruff check`, `python -m compileall -q PhyAgentOS tests`, `git diff --check`: passed。
- 未启动 Runtime、未调用 Gateway、未执行 Action/Session 或物理运动。

### Git 提交 / Git Commit

- Commit: `d4c1b7b`
- Branch: `feature/planning-loop`
- 时间: 2026-09-16 03:41 (America/Los_Angeles)
## v10.7.0 (2026-09-16 22:10) - codex

### 变更摘要 / Change summary [完成]
- [sense] [feat] 在 PAOS 场景理解 provider 边界增加并发安全的 vLLM wake/idle-sleep 生命周期管理；活动请求期间禁止 sleep，生命周期异常才进入既有 GPT fallback。
- [agent] [fix] 修复 AgentLoop 对 `manipulation.prepare` 的受信任 intent 派生和 bounded replan 恢复，使模型不再手工生成 Coordinator-owned digest，也不会因一次结构修正耗尽 120 秒恢复窗口。
- [eval] [test] 增加生命周期竞争/异常、planning 参数派生、恢复续租、任务归属与 no-motion 回归，并执行七维源码验收。
- [sense] [fix] 修复 Qwen 对未建模局部物体生成 ambiguity 引用时整次场景理解被丢弃的问题；仅过滤 ambiguity 中未知实体引用，关系引用仍严格失败。
- [sense] [fix] 在 Qwen 语义阶段与 LocateAnything proposal 阶段之间增加显式 GPU 生命周期交接；仅当无活动 Qwen 请求且 level-1 sleep 已确认后启动下游视觉 provider。
- [sense] [fix] 生命周期控制失败后重新安排 idle 状态检查，使 operator-owned vLLM 恢复后不会因旧 timer 已取消而长期保持 awake。
- [sense] [fix] 持久化 Host 在 lifecycle manager 创建后的装配失败路径中关闭 manager 与 timer，避免 Runtime 启动失败遗留控制客户端。
- [Sense] [Feat] Add concurrency-safe vLLM wake and idle-sleep lifecycle management at the PAOS scene-understanding provider boundary; block sleep while requests are active and use the existing GPT fallback only on lifecycle/control failure.
- [Agent] [Fix] Derive trusted `manipulation.prepare` intent in the AgentLoop planning boundary and preserve bounded replan recovery without model-authored Coordinator digests or accidental timeout during a valid correction.
- [Eval] [Test] Add lifecycle race/error, planning-argument derivation, recovery lease, task ownership, and no-motion regressions plus a seven-dimension source review.
- [Sense] [Fix] Preserve Qwen ambiguities about unmodeled partial objects by filtering unknown ambiguity entity references while keeping relation references fail-closed.
- [Sense] [Fix] Add an explicit GPU lifecycle handoff between Qwen semantics and LocateAnything proposals; downstream vision starts only after zero active Qwen requests and confirmed level-1 sleep.
- [Sense] [Fix] Reschedule idle status checks after lifecycle-control failures so a recovered operator-owned vLLM cannot remain awake because the prior timer was cancelled.
- [Sense] [Fix] Close lifecycle managers and timers when persistent-host assembly fails after manager creation, preventing leaked control clients on Runtime startup failure.

### 预期影响文件 / Planned files
- `examples/forge-adapters/robotwin20/src/robotwin20_adapter/qwen3_vl_vllm_lifecycle.py`
- `examples/forge-adapters/robotwin20/src/robotwin20_adapter/qwen3_vl_vllm_scene_understanding.py`
- `examples/forge-adapters/robotwin20/tests/test_qwen3_vl_vllm_scene_understanding.py`
- `examples/forge-adapters/robotwin20/src/robotwin20_adapter/persistent_host.py`
- `examples/forge-adapters/robotwin20/profiles/forge-persistent/persistent-host.yaml`
- `examples/forge-skills/pick-place-workflow/profiles/robotwin-persistent/persistent-host.yaml`
- `PhyAgentOS/planning/contracts.py`
- `PhyAgentOS/planning/projection.py`
- `PhyAgentOS/agent/tools/planning.py`
- `PhyAgentOS/forge/task.py`
- `examples/forge-skills/pick-place-workflow/contracts/manipulation.prepare.tool.yaml`
- `examples/forge-skills/pick-place-workflow/skill.yaml` and `pyproject.toml` release versions and Node lock.
- Focused Core/Skill/Adapter tests and developer documentation.

### 边界 / Boundary
- 生命周期管理只控制 loopback vLLM 的显存驻留，不启动 Action/Session、不授权运动、不读取 simulator truth。
- 不新增 RGB/task/tool-id Core 特例；受信任参数派生由 ToolSpec 声明，Coordinator 只生成自身拥有的 task/revision/node identity。
- 恢复续租保持有界且只响应有效修订尝试；无请求或无恢复尝试不会无限保活任务。

### 根因 / Root cause

- `green_prepare` 在 IK、碰撞和 workspace 检查之前失败：Agent 提交了 destination 与 capability snapshot，但缺少 Runtime 必需的 `manipulation_intent_v2`。旧 planning projection 无法声明 intent identity 必须由 Coordinator 派生，导致模型承担了不属于它的 task/revision/node/digest 所有权。
- 恢复日志中的后续错误不是重复运动失败，而是首个控制面错误触发恢复后暴露的三个独立缺陷：跨 revision `retry_of`、120 秒恢复窗口在合法修订期间过期、AgentLoop 使用全局 active task 并接受 `noop` 占位任务。
- `green_prepare` failed before IK, collision, or workspace checks: the Agent supplied destination and capability evidence but omitted the required `manipulation_intent_v2`. The old planning projection could not express that intent identity belongs to the Coordinator.
- The later log entries were not repeated motion failures. Recovery exposed three independent control-plane defects: cross-revision `retry_of`, expiry of the 120-second window during a valid correction, and global active-task selection plus acceptance of a `noop` placeholder.

### 文件变更详情 / File changes

- [修改] `PhyAgentOS/agent/planning_dispatch.py:L295-L402`, `PhyAgentOS/forge/task.py:L790-L836`, `PhyAgentOS/planning/admission.py:L74-L83`：ToolSpec 驱动受信任 intent 派生，selection 返回最终 Tool arguments，持久化与 admission 校验同一 input digest。
- [修改] `PhyAgentOS/agent/loop.py:L548-L571,L585-L589,L799-L803,L1058-L1067`, `PhyAgentOS/agent/tools/forge_task.py:L100-L103,L147-L198`, `PhyAgentOS/forge/task.py:L1153-L1190`：按 session 选择任务，拒绝占位描述，合法 revision 编译后仅领取一次 bounded recovery lease。
- [新增] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/qwen3_vl_vllm_lifecycle.py:L1-L258`；[修改] `persistent_host.py:L60-L66,L159-L160,L280-L341,L439-L452`：自动 wake、活动计数、idle/handoff sleep、生命周期专用 fallback 和失败装配清理。
- [修改] `scene_understanding_fallback.py:L23-L71`, `single_view_perception.py:L477`：仅生命周期异常进入 GPT fallback；Qwen 成功后在 LocateAnything 前执行显式 GPU 交接。
- [修改] `scripts/package_skill.py:L28-L36`；[新增] `tests/test_skill_packaging.py:L1-L31`：正式 Skill archive 排除 pytest/ruff/Python 缓存并增加内容回归。
- [修改] `examples/forge-skills/pick-place-workflow/skill.yaml:L1-L67`, `pyproject.toml:L1-L4`：发布 Skill `2.1.4`、Node `0.1.13` 与最终 Node SHA lock。
- [Modified] Core planning selection now derives Coordinator-owned intent identity, returns exact final Tool arguments, and verifies their persisted digest at admission.
- [Modified] AgentLoop now selects task state by origin session, rejects placeholder tasks, and grants one bounded recovery lease only after a replacement graph compiles.
- [Added] The adapter lifecycle manager wakes before inference, blocks sleep while requests are active, sleeps on idle or GPU handoff, and restricts GPT fallback to lifecycle/control errors.
- [Modified] Release packaging excludes generated cache directories and verifies archive contents with a regression test.

### 关键 Diff / Key diff

```diff
- Agent constructs immutable intent task/revision/node/digest fields
+ Agent supplies semantic goal/criteria/arm choice; Coordinator derives identity and returns final tool_arguments
- AgentLoop reads store.active() and accepts "noop"
+ AgentLoop resolves origin_session_key and rejects placeholder task descriptions
- failed recovery correction consumes the original 120-second window
+ one valid compiled revision attempt receives one bounded lease
- Qwen is assumed awake; broad provider failure may trigger GPT
+ lifecycle manager wakes/sleeps Qwen; only Qwen3VLVLLMLifecycleError triggers GPT fallback
- Skill bundle includes local .pytest_cache/.ruff_cache
+ formal package excludes generated cache directories
```

### 七维验收 / Seven-dimension acceptance

1. 架构集成 / Architecture: lifecycle 位于 adapter provider 边界；intent identity 位于 Coordinator planning 边界。
2. 失败恢复 / Recovery: 一次 bounded lease、跨图 `retry_of` 拒绝、控制失败后 idle reconciliation 重排。
3. 权限与机器人安全 / Safety: 全程 Query-only，`motion_authorized=false`，0 Action、0 Session、0 世界变化。
4. 配置与复现 / Reproducibility: endpoint/timeout/sleep level 均由 profile 配置；Skill/Node 版本与 SHA 锁定。
5. 可维护性 / Maintainability: provider-neutral ToolSpec 声明，无 RGB 任务或 RoboTwin Core 特例。
6. 可观测性 / Observability: lifecycle state/error/active count 与 replan deadline/lease 状态可见。
7. AgentLoop 自主性 / Autonomy: Agent 保留语义决策；Coordinator 独占 identity、binding 与 admission。

### 验证 / Validation

- Focused lifecycle/host: `15 passed`; package-content regression: `1 passed`.
- Core: `416 passed`; Skill: `334 passed`; RoboTwin adapter: `493 passed`.
- Changed-file Ruff、compileall、`git diff --check`、Principles Gate `--complete --agent-route`: passed.
- Final release: Skill `2.1.4`, archive SHA-256 `259792c968d3d369d010961948d3fc21fa65b5f91d5f66d2e3388a74c6449bbf`; Node `0.1.13`, SHA-256 `58bc793aa2d90a7344c4ebea8b57140c45c168527eccd65929e906de4a55151f`; install and `paos forge-node verify` passed.
- Installed Runtime: 9/9 Tools ready. Final Query started with vLLM sleeping, automatically woke, returned `status=available`, 3 entities, 2 relations, 4 explicit ambiguities, empty metric envelopes/derived artifacts in `13.225 s`, then returned to sleep.
- Lifecycle fault injection from the same acceptance run: operator-owned vLLM stopped -> GPT fallback `status=available` in `121.011 s`; 3 entities, 5 relations, 3 metric envelopes; no Action or Session.
- 现场验证未调用 `object.acquire`、`object.place`，未创建 Action/Session，未发生模拟器步进或物理运动。

### Git 提交 / Git Commit

- Commit: `ead479d`
- Branch: `feature/planning-loop`
- 时间 / Time: 2026-09-17 00:00 (Asia/Shanghai)
## v10.7.1 (2026-09-17 00:20) - codex

### 变更摘要 / Change summary [完成]
- [agent] [fix] 移除 session-bound AgentLoop 对无归属 legacy active task 的隐式全局回退，防止混合旧任务再次被误选。
- [sense] [fix] 将 Qwen GPU handoff lifecycle failure 纳入显式 GPT fallback 路径；保持活动 Qwen 请求和 GPU 交接失败时的安全边界。
- [test] [feat] 增加 mixed-session legacy task 与 handoff-failure fallback 回归测试。
- [Agent] [Fix] Remove implicit global fallback to an unbound legacy active task for session-bound AgentLoop execution.
- [Sense] [Fix] Route Qwen GPU-handoff lifecycle failures through the explicit GPT fallback path while preserving request and GPU safety boundaries.
- [Test] [Feat] Add mixed-session legacy-task and handoff-failure fallback regressions.

### 实际修改与验收 / Completed changes and validation

- [agent] [fix] `PhyAgentOS/agent/loop.py:L549-L599` 移除全局 `store.active()` 回退；仅从当前 session 的结构化 `forge_task_get(task_id)` tool call 恢复未绑定 legacy task，并尊重 `include_terminal=False`。(local)
- [test] [feat] `tests/test_prompt_context.py:L134-L232` 增加无关 global task、显式 session 引用、纯文本 task ID、terminal stop-path 四项回归。(local)
- [sense] [fix] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/scene_understanding_fallback.py:L73-L103`, `single_view_perception.py:L475-L490,L701-L720,L844-L860` 将 lifecycle handoff failure 纳入同一请求 GPT fallback，并在未确认 Qwen sleep 时跳过下游 GPU provider。(local)
- [test] [feat] `examples/forge-adapters/robotwin20/tests/test_scene_understanding_fallback.py:L132-L157`, `test_single_view_perception.py:L289-L310` 覆盖 handoff fallback 与 semantic-only 输出。(local)
- [env] [chore] 发布并安装 Skill `2.1.5`、Node `0.1.14`; Node SHA-256 `1ff841511a31caaf1ce555bd7e510ac6a34a6ed44decddf689795fa90e789a92`; Skill SHA-256 `b4246219f7872787507284b749faf776400022dde9096d7adf6e6dfcd218c3a8`。(local)

- [Agent] [Fix] `PhyAgentOS/agent/loop.py:L549-L599` removed global `store.active()` fallback; only a structured session-local `forge_task_get(task_id)` call can resume an unbound legacy task, and `include_terminal=False` remains respected. (local)
- [Test] [Feat] `tests/test_prompt_context.py:L134-L232` added four regressions for unrelated global tasks, explicit session references, plain-text IDs, and terminal stop paths. (local)
- [Sense] [Fix] `scene_understanding_fallback.py:L73-L103` and `single_view_perception.py:L475-L490,L701-L720,L844-L860` route lifecycle handoff failure through same-request GPT fallback and skip downstream GPU providers until Qwen sleep is confirmed. (local)
- [Test] [Feat] Adapter tests cover handoff fallback and semantic-only output. (local)
- [Env] [Chore] Installed Skill `2.1.5` and Node `0.1.14`; archive SHAs recorded above. (local)

### 代码 Diff / Code diff

```diff
- legacy = self.forge_task_coordinator.store.active()
- return legacy
+ legacy = self._legacy_task_for_session(session_key)
+ return legacy only when the session history contains a structured forge_task_get(task_id)

- Qwen release failure surfaced as generic cleanup error
+ lifecycle failure -> same-request GPT fallback -> semantic-only result
```

### 七维验收 / Seven-dimension acceptance

1. 架构集成 / Architecture: PASS — lifecycle remains adapter-owned; task selection and intent derivation remain AgentLoop/Coordinator seams.
2. 失败恢复 / Recovery: PASS — bounded lease/deadline/cross-revision rejection and lifecycle fallback are covered.
3. 权限与机器人安全 / Safety: PASS — Query-only validation; 0 Action, 0 Session, no motion authorization or world change.
4. 配置与复现 / Reproducibility: PASS — profile-owned endpoint/timeouts, Skill `2.1.5`, Node `0.1.14`, and archive SHAs verified.
5. 可维护性 / Maintainability: PASS — no second task store or lifecycle owner; compatibility path is structured and narrow.
6. 可观察性 / Observability: PASS — lifecycle route/error/active state and task projection remain visible; fallback route is explicit.
7. AgentLoop 自主性 / Autonomy: PASS — semantic choice stays with Agent, identity/admission stays with Coordinator, and unrelated legacy tasks cannot be adopted.

### 验证 / Validation

- Focused AgentLoop/lifecycle: `53 passed`; Core: `420 passed`; Skill: `334 passed`; Adapter: `495 passed`.
- Ruff, compileall, `git diff --check`, and Principles Gate `--complete --agent-route`: passed.
- Installed Runtime: 9/9 Tools ready; `scene.observe` `0.506 s`; `scene.understand` `12.358 s`; 3 entities, 2 relations, 4 explicit ambiguities; vLLM automatically slept again after idle.
- No Action, Session, simulator step, physical motion, or motion authorization.

### Git 提交 / Git commit

- Commit: `7b9e0d5`
- Branch: `feature/planning-loop`
- 时间 / Time: 2026-09-17 (Asia/Shanghai)

### 预期影响文件 / Planned files
- `PhyAgentOS/agent/loop.py`
- `examples/forge-adapters/robotwin20/src/robotwin20_adapter/scene_understanding_fallback.py`
- `examples/forge-adapters/robotwin20/src/robotwin20_adapter/single_view_perception.py`
- `tests/test_prompt_context.py`
- `examples/forge-adapters/robotwin20/tests/test_scene_understanding_fallback.py`

## v10.7.2 (2026-09-17 15:06) - codex

### 变更摘要 / Change summary [完成]

- [sense] [fix] 修复 Qwen 内部 `attributes` 在投影到公共 scene entity 时丢失，确保颜色等可见识别属性能进入下游单视图定位查询。
- [sense] [fix] 对齐 RGB 语义边界：不得仅因缺少 metric scale、3-D geometry 或 support geometry 产生阻塞性语义歧义。
- [test] [feat] 增加颜色属性投影、重复描述避免与提示边界回归；在同一 RGB task 上继续 Query/AgentLoop 验证。
- [Sense] [Fix] Preserve Qwen visible identifying attributes when projecting into the public scene-entity category consumed by downstream localization.
- [Sense] [Fix] Align the RGB semantic boundary so missing metric scale, 3-D geometry, or support geometry alone is not a blocking semantic ambiguity.
- [Test] [Feat] Add regressions for color projection, duplicate descriptors, and prompt scope, then continue the same RGB task through Query/AgentLoop validation.

### 预期影响文件 / Planned files

- `examples/forge-adapters/robotwin20/src/robotwin20_adapter/qwen3_vl_vllm_scene_understanding.py`
- `examples/forge-adapters/robotwin20/tests/test_qwen3_vl_vllm_scene_understanding.py`
- `examples/forge-skills/pick-place-workflow/skill.yaml`
- `examples/forge-skills/pick-place-workflow/pyproject.toml`
- `docs/forge/QWEN3_VL_LIFECYCLE_AGENT_LOOP_ACCEPTANCE_20260916.md`

## v10.7.3 (2026-09-17 15:36) - codex

### 变更摘要 / Change summary [完成]

- [agent] [fix] 区分跨 session 任务冲突与 Forge 工具未注册，保留任务归属边界并返回可操作的 owner-session 诊断。
- [agent] [fix] 统一 `manipulation_intent_v2` 的 Agent/Skill/Coordinator 参数契约，支持文档声明的扁平语义输入并保持旧嵌套 `intent` 兼容。
- [agent] [fix] 让 long-horizon node turn 显式携带 task identity，并让单次 CLI 在关闭 AgentLoop/Gateway client 前等待已启动任务到达终态或受治理阻塞点。
- [eval] [test] 增加跨 session、prepare selection、task-aware Tool projection、CLI wait 和 no-motion 回归，并完成七维 code review。
- [Agent] [Fix] Distinguish cross-session task conflicts from missing Forge registration while preserving task ownership and returning actionable owner-session diagnostics.
- [Agent] [Fix] Align the Agent, Skill, and Coordinator contract for `manipulation_intent_v2`, accepting the documented flat semantic input while retaining nested `intent` compatibility.
- [Agent] [Fix] Carry explicit task identity into long-horizon node turns and make one-shot CLI wait for started tasks before closing the AgentLoop/Gateway client.
- [Eval] [Test] Add cross-session, prepare-selection, task-aware Tool projection, CLI-wait, and no-motion regressions, followed by a seven-dimension code review.

### 预期影响文件 / Planned files

- `PhyAgentOS/agent/loop.py`
- `PhyAgentOS/agent/long_horizon.py`
- `PhyAgentOS/agent/planning_dispatch.py`
- `PhyAgentOS/agent/tools/forge_task.py`
- `PhyAgentOS/cli/commands.py`
- `examples/forge-skills/pick-place-workflow/SKILL.md`
- Core/Skill focused tests and acceptance documentation.

### 安全边界 / Safety boundary

- 仅修复 Agent control-plane、参数派生和 CLI 生命周期；测试使用 fake/no-motion provider。
- 不调用 `object.acquire`、`object.place`，不创建真实 Action/Session，不授权运动，不改变当前 RoboTwin 场景。

### 实际变更 / Implemented changes

- `PhyAgentOS/forge/task.py:L77-L88,L575-L584` [修改] `AgentTaskBusyError` 记录 `task_id` 与 `owner_session_key`，Store 查询并返回 owner；未改变全局单活跃任务约束。
- `PhyAgentOS/agent/tools/forge_task.py:L13-L18,L103-L127` [修改] 将同步和异步 busy 冲突转换为结构化 `agent_task_busy` 响应，明确 `forge_task_get` 只读且不转移归属。
- `PhyAgentOS/agent/planning_dispatch.py:L338-L392` [修改] 统一平铺/嵌套 intent 语义，先校验对象形状、检测相互冲突，并保留 Coordinator 派生身份与 `motion_authorized=false`。
- `PhyAgentOS/agent/loop.py:L351-L367,L619-L643,L785-L822` [修改] 增加 one-shot 等待入口，并让 node turn 显式携带 `active_task_id` 与原 task session attribution。
- `PhyAgentOS/agent/long_horizon.py:L149-L160` [新增] 提供受治理的 `wait()` join，不绕过 Coordinator/Adapter。
- `PhyAgentOS/cli/commands.py:L1061-L1071` [修改] 单次 CLI 在关闭 MCP/Gateway 前等待已启动任务。
- `examples/forge-skills/pick-place-workflow/SKILL.md:L156-L164`、`skill.yaml:L3`、`pyproject.toml:L3` [修改] 对齐 flat/nested intent 文档并升级 Skill `2.1.7`。
- `tests/test_agent_task_tool.py:L1-L40`、`tests/test_planning_selection.py:L247-L351`、`tests/test_long_horizon_controller.py:L171-L190` [新增] 跨 session（含异步绑定）、flat/conflicting/invalid intent 和 wait/no-motion 回归。
- `PhyAgentOS/agent/experience/__init__.py:L31-L36` [整理] 修复既有 import-order lint，使全量 Core Ruff 可通过。

```diff
- active task conflict -> generic exception / misleading missing-tools path
+ structured agent_task_busy(task_id, owner_session_key), no takeover
- flat manipulation fields rejected as missing intent
+ flat or nested semantics normalized to one Coordinator-owned intent
- one-shot CLI closed clients immediately after process_direct()
+ await controller.wait() before stop()/close_mcp()
- node turn resolved active_task from unrelated session key
+ node turn passes explicit active_task_id
```

### 七维验收 / Seven-dimension review

1. 架构集成 / Architecture: PASS — ownership remains in Store/Coordinator; waiting is a thin controller seam; no parallel lifecycle.
2. 失败恢复 / Recovery: PASS — cross-session conflict is actionable, intent conflicts fail before Gateway, and one-shot shutdown joins the runner.
3. 机器人安全 / Robotics safety: PASS — changes are control-plane only; all tests use fake/no-motion records and preserve `motion_authorized=false`.
4. 配置与复现 / Configuration & reproducibility: PASS — Skill version is `2.1.7`; no endpoint, model, or credential hard-coding added.
5. 可维护性 / Maintainability: PASS — compatibility is localized to the trusted-argument builder; no duplicate task store or scheduler.
6. 可观察性 / Observability: PASS — owner session, conflict code, wait boundary, and task identity are explicit in responses/log context.
7. AgentLoop 自主性 / AgentLoop autonomy: PASS — Agent chooses semantic fields; Coordinator owns identity, admission, and motion authority.

### 验证 / Validation

- Core: `425 passed`; Skill: `334 passed`; Adapter: `497 passed`。
- Focused AgentLoop/planning: `97 passed`; focused safety/package: `15 passed`; focused Skill: `74 passed`。
- `python -m compileall -q PhyAgentOS tests examples/forge-skills/pick-place-workflow/tests`: passed.
- Core/changed-file Ruff、`git diff --check` 与 Agent Route Principles Gate: passed.
- No Action, Session, simulator step, physical motion, or live Qwen/Gateway request was started.
