# 2026-09 Changelog — Part 3

## v6.10.6 (2026-09-08 00:10) - codex

### 预期修改 / Planned Changes

- [完成] [agent] [feat] 增加由 AgentTask 状态驱动的 `LongHorizonTaskController`，作为现有 `PlanningLoopAdapter` 的外层控制 seam，支持 checkpoint pause/resume、terminal/blocked 状态透传和持久化 task/revision 恢复；不新增 scheduler、store、Gateway 或 LiteLLM 会话状态。
- [Completed] [Agent] [Feat] Add a task-state-driven `LongHorizonTaskController` as an outer control seam over the existing `PlanningLoopAdapter`, supporting checkpoint pause/resume, terminal/blocked propagation, and persisted task/revision recovery without adding a scheduler, store, Gateway, or LiteLLM session state.
- [完成] [docs] [docs] 记录 PAOS 设计复核、TUI 使用边界和 prompt_toolkit/Textual 选择原则。
- [Completed] [Docs] [Docs] Record the PAOS design review, TUI usage boundary, and prompt_toolkit/Textual selection principles.
- [完成] [eval] [test] 增加 no-motion 的暂停/恢复、终态短路和 awaiting_replan 边界测试。
- [Completed] [Eval] [Test] Add no-motion tests for pause/resume, terminal short-circuit, and awaiting_replan boundaries.

### 预期影响文件 / Planned Files

- `PhyAgentOS/agent/long_horizon.py`
- `tests/test_long_horizon_controller.py`
- `docs/forge/PLANNING_MODULE_DESIGN.md`

### 设计复核补充 / Review Follow-up

- [完成] [model] [fix] 将 pause 请求从 controller 内存集合移入现有 `AgentTaskRecord`，由 Coordinator 原子持久化并在 TUI 重启后恢复；不新增 pause store。
- [Completed] [Model] [Fix] Move pause requests from controller memory into the existing `AgentTaskRecord`, atomically persisted by the Coordinator and recoverable after TUI restart; add no pause store.

### 实际结果 / Actual Results

- [完成] [agent] [feat] `PhyAgentOS/agent/long_horizon.py:L1-L113` 新增 task-state-driven controller；`PhyAgentOS/agent/planning_loop.py:L283-L318,L387-L456` 增加 per-call checkpoint，pause 在节点边界生效。
- [Completed] [Agent] [Feat] `PhyAgentOS/agent/long_horizon.py:L1-L113` adds the task-state-driven controller; `PhyAgentOS/agent/planning_loop.py:L283-L318,L387-L456` adds per-call checkpoints applied at semantic-node boundaries.
- [完成] [model] [feat] `PhyAgentOS/forge/task.py:L296,L1060-L1078` 增加持久化 `pause_requested` 和 Coordinator 原子 pause/resume；`PhyAgentOS/agent/__init__.py:L5-L23` 暴露 lazy exports。
- [Completed] [Model] [Feat] `PhyAgentOS/forge/task.py:L296,L1060-L1078` adds persisted `pause_requested` and Coordinator-atomic pause/resume; `PhyAgentOS/agent/__init__.py:L5-L23` exposes lazy exports.
- [完成] [eval] [test] `tests/test_long_horizon_controller.py:L1-L112` 覆盖 pause/resume、TUI 重启恢复、terminal short-circuit、awaiting_replan；联合专项 suite `45 passed, 1 warning`，Ruff、compileall、`git diff --check` 通过；保持 no-motion。
- [Completed] [Eval] [Test] `tests/test_long_horizon_controller.py:L1-L112` covers pause/resume, TUI restart recovery, terminal short-circuit, and awaiting-replan; combined focused suite `45 passed, 1 warning`, Ruff, compileall, and `git diff --check` pass; no-motion preserved.

### 实际结果 / Actual Results

- [完成] [agent] [feat] `PhyAgentOS/agent/long_horizon.py:L1-L112` 新增 `LongHorizonTaskController`，复用 `PlanningLoopAdapter` 的 per-call checkpoint，支持 task 状态短路、pause/resume、status 与 awaiting-replan 透传；不新增 scheduler 或 execution plane。
- [Completed] [Agent] [Feat] `PhyAgentOS/agent/long_horizon.py:L1-L112` adds `LongHorizonTaskController`, reusing the adapter's per-call checkpoint for task-state short-circuiting, pause/resume, status, and awaiting-replan propagation without a new scheduler or execution plane.
- [完成] [model] [feat] `PhyAgentOS/forge/task.py:L296,L1060-L1077` 增加持久化 `pause_requested` 与 Coordinator 原子 pause/resume API；TUI 重启后仍能恢复暂停状态，in-flight Action 不被伪称为已停止。
- [Completed] [Model] [Feat] `PhyAgentOS/forge/task.py:L296,L1060-L1077` adds persisted `pause_requested` and Coordinator-atomic pause/resume APIs; a restarted TUI observes the pause and no in-flight Action is falsely reported as stopped.
- [完成] [eval] [test] `tests/test_long_horizon_controller.py:L1-L112` 覆盖 TUI controller pause/resume、重启恢复、terminal short-circuit、awaiting_replan；联合专项 suite `45 passed, 1 warning`，Ruff、compileall、`git diff --check` 通过；保持 no-motion。
- [Completed] [Eval] [Test] `tests/test_long_horizon_controller.py:L1-L112` covers controller pause/resume, restart recovery, terminal short-circuit, and awaiting-replan; combined focused suite `45 passed, 1 warning`, Ruff, compileall, and `git diff --check` pass; no-motion preserved.
- [完成] [docs] [docs] `docs/forge/PLANNING_MODULE_DESIGN.md:L456-L525` 与 `docs/forge/MANIPULATION_DAG_DEVELOPER_GUIDE.md:L292-L308` 写入 outer loop、TUI、LiteLLM 无状态和 Textual 后续边界审核结论。
- [Completed] [Docs] [Docs] `docs/forge/PLANNING_MODULE_DESIGN.md:L456-L525` and `docs/forge/MANIPULATION_DAG_DEVELOPER_GUIDE.md:L292-L308` record the outer-loop, TUI, stateless-LiteLLM, and later-Textual boundary review.

### 文件变更详情 / File Change Details

#### [新增] `PhyAgentOS/agent/long_horizon.py:L1-L112`

**新增代码：**
```python
class LongHorizonTaskController:
    """Drive one persisted AgentTask through checkpointed planning turns."""
```

**修改说明**：controller 只编排已有 Coordinator/PlanningLoop，不拥有独立事实存储或 Tool transport。

#### [修改] `PhyAgentOS/forge/task.py:L296,L1060-L1077`

**新增代码：**
```python
pause_requested: bool = False
def request_pause(...): ...
def resume_task(...): ...
```

**修改说明**：把 pause checkpoint 放入既有 AgentTask aggregate。

### Git 提交 / Git Commit
- Commit: `41f8c0a`
- Branch: `feature/planning-loop`

## v6.10.4 (2026-09-07 23:20) - codex

### 预期修改 / Planned Changes

- [完成] [planning] [fix] 在 AgentTask 物化/创建 revision 的边界复用现有纯 DAG 校验，拒绝含环 PlanGraph，避免非法图进入持久化执行状态。
- [Completed] [Planning] [Fix] Reuse the existing pure DAG validator at AgentTask materialization/revision boundaries so cyclic PlanGraphs are rejected before entering persisted execution state.
- [完成] [eval] [test] 增加 coordinator 拒绝循环图的 no-motion 回归测试。
- [Completed] [Eval] [Test] Add a no-motion regression proving the coordinator rejects cyclic graphs.

### 预期影响文件 / Planned Files

- `PhyAgentOS/forge/task.py`
- `tests/test_planning_loop.py`

### 实际结果 / Actual Results

- [完成] [planning] [fix] `PhyAgentOS/forge/task.py:L1950-L1960` 在 graph 输入校验时调用 `validate_graph()`，将循环图转换为 coordinator-owned `AgentTaskError`。
- [Completed] [Planning] [Fix] `PhyAgentOS/forge/task.py:L1950-L1960` calls `validate_graph()` during graph input validation and converts cyclic graphs to a coordinator-owned `AgentTaskError`.
- [完成] [eval] [test] `tests/test_planning_loop.py:L299-L329` 覆盖循环图拒绝；专项 suite `42 passed, 1 warning`，Ruff、compileall、`git diff --check` 通过；保持 no-motion。
- [Completed] [Eval] [Test] `tests/test_planning_loop.py:L299-L329` covers cyclic-graph rejection; focused suite `42 passed, 1 warning`, Ruff, compileall, and `git diff --check` pass; no-motion preserved.

### Git 提交 / Git Commit
- Commit: `f6ac1fc`
- Branch: `feature/planning-loop`

## v6.10.2 (2026-09-07 23:00) - codex

### 预期修改 / Planned Changes

- [完成] [planning] [fix] 在 replan 跨 revision 复用 settlement 前校验节点内容身份，并拒绝执行结果的 task/revision 错绑；移除基于固定 `verify` 节点名的 admission 分支。
- [Completed] [Planning] [Fix] Validate node-content identity before carrying settlements across replan revisions, reject misbound execution results, and remove the admission branch keyed to a fixed `verify` node name.
- [完成] [eval] [test] 增加节点身份变更、错绑结果和任意验证节点命名的回归测试；保持 no-motion。
- [Completed] [Eval] [Test] Add regressions for changed node identity, misbound results, and arbitrary verification-node names while keeping the path no-motion.

### 预期影响文件 / Planned Files

- `PhyAgentOS/forge/task.py`
- `PhyAgentOS/agent/planning_loop.py`
- `PhyAgentOS/agent/planning_dispatch.py`
- `tests/test_planning_loop.py`

### 实际结果 / Actual Results

- [完成] [planning] [fix] `PhyAgentOS/forge/task.py:L1075-L1099` 在跨 revision 保留 settlement 前比较旧/新 `PlanNode` 的完整 digest；节点内容改变时 fail-closed，避免复用过期完成事实。
- [Completed] [Planning] [Fix] `PhyAgentOS/forge/task.py:L1075-L1099` compares complete old/new `PlanNode` digests before carrying settlements across revisions and fails closed when content changes, preventing stale completion facts from being reused.
- [完成] [planning] [fix] `PhyAgentOS/agent/planning_loop.py:L341-L348` 校验 executor 返回的 task/revision/node identity；`PhyAgentOS/agent/planning_dispatch.py:L165-L174` 移除固定 `verify` 节点名限制，验证节点命名完全由 Agent/DAG 决定。
- [Completed] [Planning] [Fix] `PhyAgentOS/agent/planning_loop.py:L341-L348` validates executor task/revision/node identities; `PhyAgentOS/agent/planning_dispatch.py:L165-L174` removes the fixed `verify` node-name restriction so verification naming remains Agent/DAG-owned.
- [完成] [eval] [test] `tests/test_planning_loop.py:L238-L329` 新增 changed-node preserve、错绑结果和 `verify` 节点名兼容回归；专项 suite `41 passed, 1 warning`，Ruff、compileall、`git diff --check` 通过；保持 no-motion。
- [Completed] [Eval] [Test] `tests/test_planning_loop.py:L238-L329` adds regressions for changed-node preservation, misbound results, and `verify`-name compatibility; focused suite `41 passed, 1 warning`, Ruff, compileall, and `git diff --check` pass; no-motion preserved.

### 文件变更详情 / File Change Details

#### [修改] `PhyAgentOS/forge/task.py:L1075-L1099`

**修改前：**
```python
preserved = tuple(
    item.model_copy(update={"revision_id": plan_graph.revision_id})
    for node_id, item in active_settlements.items()
    if node_id in set(delta.preserve_node_ids)
    and node_id in {node.node_id for node in plan_graph.nodes}
)
```

**修改后：**
```python
for node_id in set(delta.preserve_node_ids) & set(active_settlements) & set(replacement_nodes):
    if plan_node_digest(active_nodes[node_id]) != plan_node_digest(replacement_nodes[node_id]):
        raise AgentTaskError(f"cannot preserve node {node_id!r}: replacement content changed")
```

**修改说明**：preserve 现在要求节点完整内容身份不变。

#### [修改] `PhyAgentOS/agent/planning_loop.py:L341-L348`

**修改后：**
```python
if result.task_id != context.task_id or result.revision_id != context.revision_id or result.node_id != context.node_id:
    raise PlanningLoopError("node executor returned a result bound to a different task, revision, or node")
```

**修改说明**：阻止错绑执行结果进入 settlement。

### Git 提交 / Git Commit
- Commit: `5a44b43`
- Branch: `feature/planning-loop`

## v6.10.0 (2026-09-07 21:00) - codex

### 预期修改 / Planned Changes

- [计划] [policy] [feat] 实现 PAOS planning loop 的独立 feature：同一 AgentTask 的 discovery-to-DAG revision 扩展、节点 settlement 持久化、直接前驱上下文注入、ready-node 推进、replay/replan 以及 preserve/invalidate/fresh-evidence 应用；不新增 scheduler、task store 或物理执行协议。
- [Planned] [Policy] [Feat] Implement the PAOS planning-loop feature as an independent extension: same-AgentTask discovery-to-DAG revision expansion, durable node settlements, direct-predecessor context injection, ready-node progression, replay/replan, and preserve/invalidate/fresh-evidence application; add no scheduler, task store, or physical execution protocol.
- [计划] [eval] [test] 增加纯内存/fake execution 的 RGB 属性排序场景测试，覆盖动态库存、DAG 执行、节点上下文、动作失败/unknown、动作后脱落反证、replan 与 reducer replay；保持 no-motion。
- [Planned] [Eval] [Test] Add pure in-memory/fake-execution tests for the RGB attribute-sorting scenario covering dynamic inventory, DAG execution, node context, action failure/unknown, post-action dropped-object counterevidence, replan, and reducer replay; keep the path no-motion.

### 预期影响文件 / Planned Files

- `PhyAgentOS/planning/contracts.py`
- `PhyAgentOS/forge/task.py`
- `tests/test_planning_loop.py`
- `docs/forge/PLANNING_MODULE_DESIGN.md`
- `docs/forge/MANIPULATION_DAG_DEVELOPER_GUIDE.md`

### 实际结果 / Actual Results

- [完成] [docs] [fix] `docs/forge/PLANNING_MODULE_DESIGN.md:L394-L404` 与 `docs/forge/MANIPULATION_DAG_DEVELOPER_GUIDE.md:L287-L298` 明确：`paos.planners` entry point 仅是 PAOS 环境内的开发 seam；生产 Planner/Skill 插件及私有依赖必须在插件自身 managed runtime 环境运行，跨边界只交换 provider-neutral planning projection。
- [Completed] [Docs] [Fix] `docs/forge/PLANNING_MODULE_DESIGN.md:L394-L404` and `docs/forge/MANIPULATION_DAG_DEVELOPER_GUIDE.md:L287-L298` clarify that the `paos.planners` entry point is only an in-PAOS development seam; production Planner/Skill plugins and private dependencies must run in the plugin's managed runtime environment, exchanging only provider-neutral planning projections across the boundary.
- [完成] [eval] [test] 在 `/home/yanxu/miniconda3/envs/paos/bin/python` 下运行 planning/controller no-motion 专项：`47 passed`；Ruff、compileall、`git diff --check` 通过；`python -m PhyAgentOS task --help` 与根 help 正常显示，未知 task 命令返回退出码 1。
- [Completed] [Eval] [Test] Under `/home/yanxu/miniconda3/envs/paos/bin/python`, the planning/controller no-motion focus passes `47 passed`; Ruff, compileall, and `git diff --check` pass; `python -m PhyAgentOS task --help` and root help render successfully, while an unknown task command exits with code 1.

### 六维验收 / Six-Dimension Acceptance

- 架构集成：通过；插件边界与现有 Skill Runtime/Node 安装模型一致，PAOS 不复制 scheduler、store 或 Gateway。
- 失败路径：通过；缺失插件环境不得伪装成 PAOS 内可用插件，未知 task 与 control-only 执行均 fail-closed。
- 权威边界：通过；插件只产生 PlanGraph/ReplanProposal，Coordinator、Tool API、Gateway 和运动权限所有权不变。
- 配置与 provenance：通过；插件环境由 managed runtime 负责，PAOS 只接收 task/revision 绑定的 provider-neutral projection。
- 可维护性：通过；开发 entry point 与生产 runtime 边界写入开发者指南，后续可替换 Textual/TUI 而不改核心。
- Anti-OverDefense：通过；未新增 hash、冻结 contract、重复 gate 或第二事实库。
- Architecture integration: pass; the plugin boundary matches the existing Skill Runtime/Node installation model without duplicating a scheduler, store, or Gateway.
- Failure paths: pass; a missing plugin environment cannot be presented as a PAOS-local plugin, and unknown-task/control-only execution fails closed.
- Authority boundaries: pass; plugins only produce PlanGraph/ReplanProposal while Coordinator, Tool API, Gateway, and motion ownership remain unchanged.
- Configuration/provenance: pass; the managed runtime owns plugin environment resolution, and PAOS consumes only task/revision-bound provider-neutral projections.
- Maintainability: pass; development entry points and production-runtime boundaries are documented, allowing a future Textual/TUI replacement without core changes.
- Anti-OverDefense: pass; no new hash, frozen contract, duplicate gate, or second fact store was added.

### Git 提交 / Git Commit

- Commit: `ae40ce9`
- Branch: `feature/planning-loop`
- `PhyAgentOS/agent/tools/forge_task.py`
- `PhyAgentOS/agent/planning_loop.py`
- `PhyAgentOS/agent/planner_plugin.py`

### 实际结果 / Actual Results

- [完成] [policy] [feat] `PhyAgentOS/agent/planning_loop.py:L1-L431` 新增薄 `PlanningLoopAdapter`、`NodeContextProvider`、`AgentLoopNodeExecutor`、ready-node 推进、纯 reducer replay、postcondition counterevidence recovery；执行和持久化分别委托现有 Agent/Coordinator，不直连 Gateway 或 SQLite。
- [Completed] [Policy] [Feat] `PhyAgentOS/agent/planning_loop.py:L1-L431` adds the thin `PlanningLoopAdapter`, `NodeContextProvider`, `AgentLoopNodeExecutor`, ready-node progression, pure reducer replay, and postcondition counterevidence recovery; execution and persistence remain delegated to the existing Agent/Coordinator without direct Gateway or SQLite access.
- [完成] [policy] [feat] `PhyAgentOS/agent/planner_plugin.py:L1-L113` 增加 task-conditioned `PlanningRequest`、`PlannerPlugin`/`ReplanProposal` 协议和 opt-in `paos.planners` entry-point registry，允许 Agent 根据任务是否充分自行选择观察或直接分解。
- [Completed] [Policy] [Feat] `PhyAgentOS/agent/planner_plugin.py:L1-L113` adds task-conditioned `PlanningRequest`, `PlannerPlugin`/`ReplanProposal` contracts, and an opt-in `paos.planners` entry-point registry, allowing the Agent to choose observation or direct decomposition based on task sufficiency.
- [完成] [model] [feat] `PhyAgentOS/forge/task.py:L167-L233,L850-L1100` 将 `NodeSettlement`、counterevidence、replan metadata 和可选 evidence 纳入现有 `PlanRevision`，新增同一 AgentTask 的通用 plan materialization、settlement/counterevidence persistence、bounded replan request、discovery 不计预算和 preserve settlement 的新 revision 迁移。
- [Completed] [Model] [Feat] `PhyAgentOS/forge/task.py:L167-L233,L850-L1100` adds `NodeSettlement`, counterevidence, replan metadata, and optional evidence to the existing `PlanRevision`, with same-task generic plan materialization, settlement/counterevidence persistence, bounded replan requests, discovery excluded from budget, and preserved-settlement migration into a new revision.
- [完成] [agent] [feat] `PhyAgentOS/agent/loop.py:L447-L470` 增加 `run_node_turn()`，复用现有 AgentLoop、ToolRegistry 和 planning admission guard 作为节点作用域入口。
- [Completed] [Agent] [Feat] `PhyAgentOS/agent/loop.py:L447-L470` adds `run_node_turn()` as a node-scoped entry point reusing the existing AgentLoop, ToolRegistry, and planning admission guard.
- [完成] [eval] [test] `tests/test_planning_loop.py:L1-L285` 覆盖动态发现的 RGB 属性排序 DAG、直接前驱上下文、stale 拒绝、动作后脱落反证、Agent 选择 replan、fresh evidence、unknown 不重放、retry budget、preserve settlement、reducer replay 和插件 registry。
- [Completed] [Eval] [Test] `tests/test_planning_loop.py:L1-L285` covers a dynamically discovered RGB attribute-sorting DAG, direct predecessor context, stale rejection, post-action dropped-object counterevidence, Agent-selected replan, fresh evidence, no replay after unknown, retry budget, preserved settlements, reducer replay, and plugin registry.
- [完成] [docs] [docs] `docs/forge/PLANNING_MODULE_DESIGN.md:L418-L457` 更新实现状态，明确该 slice 已完成及 Action/Session 物理重放仍受 Gateway reconciliation 和 fresh admission 约束。
- [Completed] [Docs] [Docs] `docs/forge/PLANNING_MODULE_DESIGN.md:L418-L457` updates implementation status and states that Action/Session physical reruns remain subject to Gateway reconciliation and fresh admission.
- [完成] [agent] [feat] `PhyAgentOS/agent/tools/forge_task.py:L107-L208` 暴露 `forge_task_materialize_plan`，让 Agent 在任务具体时直接提交 DAG，或自行先选择 observation/scene-understanding Tool；不强制 discovery 阶段。
- [Completed] [Agent] [Feat] `PhyAgentOS/agent/tools/forge_task.py:L107-L208` exposes `forge_task_materialize_plan`, allowing the Agent to submit a DAG directly for sufficiently specified tasks or choose observation/scene-understanding Tools first; no discovery phase is forced.

### 文件变更详情 / File Change Details

#### [新增] `PhyAgentOS/agent/planning_loop.py` L1-L431

**新增代码 / Added:** 节点上下文投影、ready-node loop、settlement 持久化委托、counterevidence recovery 和 reducer replay。

#### [新增] `PhyAgentOS/agent/planner_plugin.py` L1-L113

**新增代码 / Added:** provider-neutral planner protocol 与 opt-in entry-point registry。

#### [修改] `PhyAgentOS/forge/task.py` L167-L232,L848-L1078

**修改前 / Before:** `PlanRevision` 没有 durable `NodeSettlement`，discovery expansion 与 delta 语义没有 coordinator 接入。

**修改后 / After:** settlement 和 revision provenance 写入现有 task aggregate；Coordinator 提供 discovery expansion、settlement、bounded replan 和 preserve migration。

#### [修改] `PhyAgentOS/agent/loop.py` L447-L470

**修改前 / Before:** 没有公开的 semantic-node AgentLoop 入口。

**修改后 / After:** 新增 `run_node_turn()`，复用现有 AgentLoop tool-call 循环。

#### [修改] `PhyAgentOS/agent/tools/forge_task.py` L107-L208

**修改后 / After:** 新增 Agent 可调用的通用 DAG materialization Tool。

### 验证 / Validation

- `python -m ruff check PhyAgentOS/planning PhyAgentOS/agent/planning_loop.py PhyAgentOS/agent/planner_plugin.py PhyAgentOS/forge/task.py tests/test_planning_loop.py`: passed。
- `python -m compileall -q PhyAgentOS tests/test_planning_loop.py`: passed。
- `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=examples/forge-skills/pick-place-workflow/src:. python -m pytest -q tests/test_planning_module.py tests/test_planning_loop.py examples/forge-skills/pick-place-workflow/tests/test_long_horizon.py`: `38 passed`。
- No Gateway, Dora, simulator step, hardware action, or motion authorization was executed。

### Git 提交 / Git Commit

- Commit: `4bc8bb6`
- Branch: `feature/planning-loop`
- 时间: 2026-09-07

## v6.9.1 (2026-09-07 21:05) - codex

### 预期修改 / Planned Changes

- [计划] [eval] [feat] 为独立 RoboTwin simulation probe 增加可选 head-camera 视频证据输出；视频只记录已批准的 simulation-only route，不改变 PAOS/Gateway motion authority，也不把失败转换为成功。
- [Planned] [Eval] [Feat] Add optional head-camera video evidence output to the independent RoboTwin simulation probe; video records only the approved simulation-only route, does not change PAOS/Gateway motion authority, and never converts failure into success.
- [计划] [safety] [test] 每次 probe 仍要求新的 route/approval 绑定；视频文件与 probe result 一起保存，覆盖成功和失败后的 stop/reset 结果，并增加 recorder 的单元测试与可复现验证命令。
- [Planned] [Safety] [Test] Require a fresh route/approval binding for each probe; save the video with the probe result, including stop/reset outcomes for success or failure, and add recorder unit tests plus a reproducible validation command.

### 预期影响文件 / Planned Files

- `examples/forge-adapters/robotwin20/runtime/robotwin_simulation_probe_worker.py`
- `examples/forge-adapters/robotwin20/profiles/robotwin20/simulation-probe.yaml`
- `examples/forge-adapters/robotwin20/tests/test_simulation_probe.py`
- `changelog/2026-09_part3.md`

### 实际完成（等待人工审批） / Completed (Awaiting Human Approval)

- [完成] [eval] [feat] `examples/forge-adapters/robotwin20/runtime/robotwin_simulation_probe_worker.py:L126-L205,L1060-L1160,L1380-L1435,L1470-L1615,L1688-L2040` 增加 provider-owned head-camera MP4 recorder；按每 4 个 simulator steps 采样，success/failure 均写入 `video.mp4` evidence，不改变 PAOS/Gateway motion authority。
- [Completed] [Eval] [Feat] `examples/forge-adapters/robotwin20/runtime/robotwin_simulation_probe_worker.py:L126-L205,L1060-L1160,L1380-L1435,L1470-L1615,L1688-L2040` adds a provider-owned head-camera MP4 recorder; it samples every four simulator steps, records `video.mp4` evidence for both success and failure, and does not change PAOS/Gateway motion authority.
- [完成] [eval] [test] `examples/forge-adapters/robotwin20/tests/test_simulation_probe.py:L61-L110` 增加视频 recorder 的正向 artifact 和无帧失败测试；专项 probe/approval 测试 `42 passed`，Ruff、compileall 和 `git diff --check` 通过。
- [Completed] [Eval] [Test] `examples/forge-adapters/robotwin20/tests/test_simulation_probe.py:L61-L110` adds positive artifact and no-frame failure tests for the video recorder; focused probe/approval tests report `42 passed`, with Ruff, compileall, and `git diff --check` passing.
- [完成] [eval] [test] 使用 pick-place workflow 所需 PYTHONPATH 运行完整 RoboTwin adapter 回归：`303 passed, 1 skipped`。
- [Completed] [Eval] [Test] Ran the complete RoboTwin adapter regression with the pick-place workflow PYTHONPATH: `303 passed, 1 skipped`.
- [完成] [eval] [exp] 重新物化全新 route package `/home/yanxu/robotwin20-runtime/artifacts/paos-route-v6.9.1-20260907T2115Z/`，request `franka-blocks-green0-video-v691-20260907`；route digest `70c6bd03546d67a6ac8e869c2a7b4ddba72a0fa82a68ea759ac4f48164c1204d`，source-manifest digest `be3cf8a2b9a0bb1cb9448e346916e63e32252a228084867f740004032353a618`，worker digest `48193e447a1480faf63da5b26fecbf51f092759fef3f9e068397cf4d20b8bdef`，当前状态 `pending_human_review`。
- [Completed] [Eval] [Exp] Rematerialized a fresh route package at `/home/yanxu/robotwin20-runtime/artifacts/paos-route-v6.9.1-20260907T2115Z/` for request `franka-blocks-green0-video-v691-20260907`; route digest `70c6bd03546d67a6ac8e869c2a7b4ddba72a0fa82a68ea759ac4f48164c1204d`, source-manifest digest `be3cf8a2b9a0bb1cb9448e346916e63e32252a228084867f740004032353a618`, worker digest `48193e447a1480faf63da5b26fecbf51f092759fef3f9e068397cf4d20b8bdef`, currently `pending_human_review`.
- [完成] [safety] [policy] 在新 package 获得精确 simulation-only approval 前未运行 scene.step()；视频 probe 尚未执行，不能提前声称已有视频或路线成功。
- [Completed] [Safety] [Policy] No `scene.step()` was run before exact simulation-only approval for the new package; the video probe has not executed and no video or route success is claimed prematurely.

### 六维验收（实现阶段） / Six-Dimension Acceptance (Implementation Stage)

- 架构集成 / Architecture integration: 通过；视频仅在 RoboTwin provider worker 内生成，PAOS planning、Gateway、Dora、Action 和硬件边界未改变。
- 失败路径 / Failure paths: 通过；无摄像头、无效帧、writer 不可用和 probe failure 均保持不可用/失败语义，failure evidence 仍保存 stop/reset 状态。
- 权威边界 / Authority boundaries: 通过；MP4 只是可视化证据，不授予 motion authority，也不替代 SAPIEN contact 或 Gateway 事实。
- 配置与 provenance / Configuration and provenance: 通过；worker/profile digest 变化触发新 route package，旧 approval 不复用。
- 可维护性 / Maintainability: 通过；复用现有 RoboTwin cameras/OpenCV，不新增独立视频服务或第二套执行状态。
- 防止过度防御编程 / Anti-overdefense: 通过；仅加入用户明确要求的视频输出和必要帧校验，未新增速度、哈希、发布 gate 或重复安全层。

### 实际 probe 结果 / Actual Probe Result

- [完成] [eval] [exp] 在获得精确 approval 后运行唯一一次带视频的 RoboTwin simulation-only probe；结果为 `status=unavailable`，失败阶段 `retreat`，错误 `unexpected simulator contact during attached route`。failure artifact 为 `artifact://simulation-probe/franka-blocks-green0-video-v691-20260907/block-green-1-0/failure`，包含 `6171` 条 contact trace，controller stop=`stopped`、simulation reset=`completed`。
- [Completed] [Eval] [Exp] After exact approval, ran the single video-enabled RoboTwin simulation-only probe; result was `status=unavailable` at `retreat` with `unexpected simulator contact during attached route`. The failure artifact is `artifact://simulation-probe/franka-blocks-green0-video-v691-20260907/block-green-1-0/failure` with `6171` contact-trace records; controller stop=`stopped` and simulation reset=`completed`.
- [完成] [eval] [feat] 已生成可播放 head-camera 视频：[video.mp4](/home/yanxu/robotwin20-runtime/artifacts/paos-route-v6.9.1-20260907T2115Z/simulation-probe/franka-blocks-green0-video-v691-20260907/block-green-1-0/video.mp4)，格式 ISO MP4（MPEG-4），分辨率 `320x240`，`25 FPS`，`293` 帧，时长 `11.72s`。视频只作为 provider evidence，不代表路线成功或 motion authority。
- [Completed] [Eval] [Feat] Generated a playable head-camera video at [video.mp4](/home/yanxu/robotwin20-runtime/artifacts/paos-route-v6.9.1-20260907T2115Z/simulation-probe/franka-blocks-green0-video-v691-20260907/block-green-1-0/video.mp4), ISO MP4 (MPEG-4), `320x240`, `25 FPS`, `293` frames, `11.72s`. The video is provider evidence only and does not indicate route success or motion authority.

### 视频执行验收 / Video Execution Acceptance

- 视频证据 / Video evidence: 通过；文件存在、可由 `ffprobe` 解析并含非零帧数。
- 失败语义 / Failure semantics: 通过；probe 仍返回 `unavailable`，未将视频存在误判为成功。
- 安全边界 / Safety boundary: 通过；未调用 Gateway、Dora、Action 或硬件，stop/reset 已完成。

## v6.8.15 (2026-09-07 20:29) - codex

### 预期修改 / Planned Changes

- [计划] [policy] [docs] 补充 RGB 属性排序场景暴露的 progressive planning 缺口：同一 AgentTask 内的 discovery-to-DAG 扩展、动作后反证/脱落归因、跨 revision 的 preserve/invalidate 与前驱上下文解析；复用 ReplanDelta、PlanRevision、Evidence 和 Verifier，不增加 RGB 专用状态机。
- [Planned] [Policy] [Docs] Document the progressive-planning gaps exposed by the RGB attribute-sorting scenario: discovery-to-DAG expansion within the same AgentTask, post-action counterevidence/drop attribution, and cross-revision preserve/invalidate plus predecessor-context resolution; reuse ReplanDelta, PlanRevision, Evidence, and Verifier without an RGB-specific state machine.

### 预期影响文件 / Planned Files

- `docs/forge/PLANNING_MODULE_DESIGN.md`
- `changelog/2026-09_part3.md`

### 实际结果 / Actual Results

- [完成] [policy] [docs] `docs/forge/PLANNING_MODULE_DESIGN.md:L263-L306,L419-L426` 补充 progressive planning：未知方块库存先通过 observation checkpoint，再在同一 AgentTask 的新 PlanRevision 中扩展 DAG；颜色比较规则不明确时保持 unknown，不从 RGB 名称推断布局。
- [Completed] [Policy] [Docs] `docs/forge/PLANNING_MODULE_DESIGN.md:L263-L306,L419-L426` documents progressive planning: discover the unknown block inventory through an observation checkpoint, then expand the DAG in a new PlanRevision under the same AgentTask; ambiguous color ordering remains unknown instead of inferring layout from RGB names.
- [完成] [policy] [docs] 记录动作成功后由后续 observation/verification 发现方块脱落时的通用 counterevidence 归因，以及 `begin_revision_from_delta()` 尚未应用 preserve/invalidate/fresh-evidence 字段的实现缺口；明确不得覆盖历史成功事实或复用失效分支 evidence。
- [Completed] [Policy] [Docs] Records generic counterevidence attribution when a later observation/verification detects a dropped block after an Action reported success, and the implementation gap that `begin_revision_from_delta()` does not yet apply preserve/invalidate/fresh-evidence fields; historical success facts and invalidated-branch evidence must not be overwritten or reused.

### 验证 / Validation

- `git diff --check` passed.
- No code, runtime, Gateway, Dora, Action, simulation motion, or hardware was executed or changed.

### 文件变更详情 / File Change Details

#### [修改] `docs/forge/PLANNING_MODULE_DESIGN.md` L263-L306,L418-L426

**修改前 / Before:** 未明确未知场景库存如何在同一任务内扩展 DAG，也未明确动作成功后反证、跨 revision preserve/invalidate 和 stale evidence 的处理。

**修改后 / After:** 增加 discovery checkpoint、通用 counterevidence 归因、保留/失效字段应用缺口及场景 revision 刷新要求，并将其纳入当前未完成项。

**修改说明 / Notes:** 仅补充通用架构边界和验收条件，不增加 RGB 专用状态机或执行路径。

### Git 提交 / Git Commit

- Commit: `10433be`
- Branch: `feature/long-horizon-workflow`

## v6.8.13 (2026-09-07 20:29) - codex

### 预期修改 / Planned Changes

- [完成] [policy] [docs] 基于 PAOS 现有 AgentLoop、AgentTaskCoordinator、PlanGraph、NodeSettlement 和 ReplanDelta，分析并记录 RGB 颜色排序场景的节点执行 loop、前驱上下文注入、节点 replay/replan 与可插拔规划器边界；不创建第二套任务状态、DAG 或执行协议。
- [Completed] [Policy] [Docs] Analyzed and recorded the RGB color-sorting scenario's node execution loop, predecessor-context injection, node replay/replan, and pluggable planner boundary using PAOS's existing AgentLoop, AgentTaskCoordinator, PlanGraph, NodeSettlement, and ReplanDelta; no second task state, DAG, or execution protocol was created.

### 预期影响文件 / Planned Files

- `docs/forge/PLANNING_MODULE_DESIGN.md`
- `changelog/2026-09_part3.md`

### 实际结果 / Actual Results

- [完成] [policy] [docs] `docs/forge/PLANNING_MODULE_DESIGN.md:L233-L387` 新增通用属性排序场景分析：明确 RGB 方块只是验证例，数量、颜色集合和位置必须由 observation Skill 发现；定义复用 AgentLoop、AgentComposedDispatch、AgentTaskCoordinator、PlanRevision、Forge Tool、Evidence 和 Verifier 的节点 loop，并明确不引入第二套 scheduler/store/execution path。
- [Completed] [Policy] [Docs] `docs/forge/PLANNING_MODULE_DESIGN.md:L233-L387` adds the generic attribute-sorting analysis: RGB blocks are only a validation case, while count, color set, and locations must be discovered by the observation Skill; it defines a node loop reusing AgentLoop, AgentComposedDispatch, AgentTaskCoordinator, PlanRevision, Forge Tools, Evidence, and Verifier without a second scheduler/store/execution path.
- [完成] [policy] [docs] 同一文档记录最小前驱上下文投影、NodeSettlement 持久化建议、reducer replay 与 Action/Session rerun 的区别、Agent 选择的 ReplanDelta 恢复路径、planner/plugin 边界及六维验收条件；明确当前未完成项和最小下一实现切片。
- [Completed] [Policy] [Docs] The same document records the minimal predecessor-context projection, the NodeSettlement persistence recommendation, the distinction between reducer replay and Action/Session rerun, the Agent-selected ReplanDelta recovery path, the planner/plugin boundary, and six-dimension acceptance conditions; it explicitly lists unfinished pieces and the smallest next implementation slice.

### 文件变更详情 / File Change Details

#### [修改] `docs/forge/PLANNING_MODULE_DESIGN.md` L233-L387

**修改前 / Before:** 文档在 RoboTwin collision-world provider 边界结束，未包含 RGB 属性排序场景的 loop、上下文注入、节点 replay/replan 或插件验收分析。

**修改后 / After:** 新增场景适配、PAOS 权威复用的 `PlanningLoopAdapter`、直接前驱 `NodeContextProvider`、失败/重放/重规划语义、planner/plugin 边界、六维验收表和未完成项清单。

**修改说明 / Notes:** 场景参数保持由观察证据发现；规划层、Coordinator、Gateway、Verifier 和插件的所有权边界不变。

### 验证 / Validation

- `git diff --check` passed.
- No code, runtime, Gateway, Dora, Action, simulation motion, or hardware was executed or changed.

### Git 提交 / Git Commit

- Commit: `c3162f2`
- Branch: `feature/long-horizon-workflow`
- 时间 / Time: 2026-09-07 20:29 (Asia/Shanghai)

## v6.8.12 (2026-09-07 22:35) - codex

### 预期修改 / Planned Changes

- [计划] [eval] [exp] 物化用户对 `paos-route-v6.8.11-20260907T2200Z` 的精确 simulation-only approval，并在批准范围内运行一次完整顺序双臂 RoboTwin probe，覆盖 Curobo peer-sphere projection、attached、lift、transport、descent、release、retreat、hold drift、contact 和 semantic evidence。
- [Planned] [Eval] [Exp] Materialize the user's exact simulation-only approval for `paos-route-v6.8.11-20260907T2200Z` and run one complete sequential dual-arm RoboTwin probe within the approved scope, covering Curobo peer-sphere projection, attached, lift, transport, descent, release, retreat, hold drift, contact, and semantic evidence.
- [计划] [safety] [test] 每次 scene.step 前继续校验 approval、route/source-manifest/worker binding、双臂 capability、controller qualification、collision world、peer projection 和 stop policy；只保存真实成功或失败证据，不将 unavailable/unknown 解释为成功。
- [Planned] [Safety] [Test] Revalidate approval, route/source-manifest/worker bindings, both-arm capabilities, controller qualification, collision world, peer projection, and stop policy before each scene.step; persist only actual success or failure evidence and never interpret unavailable/unknown as success.

### 实际结果 / Actual Results

- [完成] [eval] [exp] 物化了用户批准的 simulation-only approval：`/home/yanxu/robotwin20-runtime/artifacts/paos-route-v6.8.11-20260907T2200Z/probe/approval.json`，approval sha256 为 `249788c6820fed878913a8dfab88e4357d4ff251fb0d811fbd65a4a5257f4ec0`；绑定 route digest `f66bd11a5d1f941ed9c93facd6476e1eee07163ec1df6a2d377a7c3cbb3379c0`、source-manifest digest `542ea4aac6cfbed491e9f8fdf70eff9a06873c69c7cd9d6632776fdad23d8f7e` 和 worker digest `c47601e14f3b632481babdbf8eb22e4c1cd3445aaea46ed9d88bd92a7603df96`。
- [Completed] [Eval] [Exp] Materialized the user-approved simulation-only approval at `/home/yanxu/robotwin20-runtime/artifacts/paos-route-v6.8.11-20260907T2200Z/probe/approval.json`; approval sha256 is `249788c6820fed878913a8dfab88e4357d4ff251fb0d811fbd65a4a5257f4ec0`, bound to route digest `f66bd11a5d1f941ed9c93facd6476e1eee07163ec1df6a2d377a7c3cbb3379c0`, source-manifest digest `542ea4aac6cfbed491e9f8fdf70eff9a06873c69c7cd9d6632776fdad23d8f7e`, and worker digest `c47601e14f3b632481babdbf8eb22e4c1cd3445aaea46ed9d88bd92a7603df96`.
- [完成] [safety] [test] 使用 `/home/yanxu/miniconda3/envs/RoboTwin20/bin/python` 在正确的 RoboTwin checkout 根目录运行独立 probe；实际执行 `1172` 个 simulator steps，完成 controller stop（`stopped`）和 simulation reset（`completed`），未调用 Gateway、Dora、Action 或硬件。第一次启动失败仅因 runtime_root 指向父目录，发生在模型加载前且未消费请求；纠正后才执行唯一一次批准请求。
- [Completed] [Safety] [Test] Ran the independent probe with `/home/yanxu/miniconda3/envs/RoboTwin20/bin/python` and the correct RoboTwin checkout root; it executed `1172` simulator steps and completed controller stop (`stopped`) and simulation reset (`completed`) without Gateway, Dora, Action, or hardware calls. The first startup failure was only a parent-directory runtime_root configuration error before model loading and did not consume the request; the corrected run was the sole approved request.
- [完成] [eval] [fix] probe 返回 `status=unavailable`，失败阶段为 `retreat`，错误为 `unexpected simulator contact during attached route`；失败 evidence 位于 `artifact://simulation-probe/franka-blocks-green0-dual-v6811b-20260907/block-green-1-0/failure`，sha256 为 `d07342034ef3c6b017c0d8932db6a6e5fbfa9c611880874ed8e8ba6ab379788e`，包含 `6157` 条 contact trace。左臂 planner route segment 失败后右臂通过并执行路线；接触发生在 lift/descent/release 等阶段，涉及 `block-red-1`、`block-blue-1`、table 与右臂 link，故不能解释为成功或仅归因于未选中左臂。
- [Completed] [Eval] [Fix] The probe returned `status=unavailable` at `retreat` with `unexpected simulator contact during attached route`; failure evidence is at `artifact://simulation-probe/franka-blocks-green0-dual-v6811b-20260907/block-green-1-0/failure` with sha256 `d07342034ef3c6b017c0d8932db6a6e5fbfa9c611880874ed8e8ba6ab379788e` and `6157` contact-trace records. The left-arm planner route segment failed, the right arm passed and executed the route; contacts occurred in lift/descent/release and involved `block-red-1`, `block-blue-1`, the table, and right-arm links, so the result is not success and cannot be attributed only to the unselected left arm.

### 六维验收 / Six-Dimension Acceptance

- 架构集成 / Architecture integration: 部分通过；审批、worker、Curobo world/peer projection 和 evidence 边界按 provider 路径接入，但当前固定 route 未通过完整 attached collision 证明，不能进入 benchmark 或生产执行。
- 失败路径 / Failure paths: 通过；启动配置错误、planner 单臂失败、仿真接触和 retreat 失败均被显式返回；失败后 stop/reset 完成，`unavailable` 未被解释为成功。
- 权威边界 / Authority boundaries: 通过；审批只允许隔离 simulation probe，结果不授予 PAOS/Gateway motion authority，未接入 Dora、Action 或硬件。
- 配置与 provenance / Configuration and provenance: 通过；approval 精确绑定 route/source-manifest/worker/candidate，before/after snapshot 与 failure artifact 可追溯；runtime_root 修正未改写历史 artifact。
- 可维护性 / Maintainability: 部分通过；probe 生命周期和证据可复现，但 route 仍需显式解决双臂/环境接触后才能宣称完整能力；本轮未修改生产代码。
- 防止过度防御编程 / Anti-overdefense: 通过；没有新增 hash、速度常数、重复 gate 或候选调参，使用已有绑定与实际 contact evidence 判定失败。

### 验证 / Verification

- `approve_simulation_probe.py`：approval materialization passed。
- `run_approved_simulation_probe.py`：返回 `status=unavailable`（预期退出码 `2`），failure、before/after snapshot、contact trace、controller stop 和 simulation reset 均已写入。
- 聚焦模块测试：`59 passed`（dual-arm state、Curobo world port、simulation probe、approval）。

### 代码变更详情 / Code Change Details

- 本轮未修改生产代码；仅新增本节执行结果与验收记录。外部 RoboTwin artifact 位于 `/home/yanxu/robotwin20-runtime/artifacts/paos-route-v6.8.11-20260907T2200Z/`，不纳入本仓库提交。

### 预期影响文件 / Planned Files

- `changelog/2026-09_part3.md`
- `/home/yanxu/robotwin20-runtime/artifacts/paos-route-v6.8.11-20260907T2200Z/probe/approval.json`
- `/home/yanxu/robotwin20-runtime/artifacts/paos-route-v6.8.11-20260907T2200Z/probe/`

## v6.8.11 (2026-09-07 20:55) - codex

### 预期修改 / Planned Changes

- [完成] [policy] [fix] 根据 v6.8.10 双臂 probe 的真实失败证据，评估并在 RoboTwin provider 内复用 Curobo 已有的机器人碰撞模型，为 mesh/convex link 生成固定 hold 姿态的 peer-arm projection；不把 mesh 静默当作 box，也不修改 PAOS Core、route 语义或 collision safety policy。
- [Completed] [Policy] [Fix] Based on the real v6.8.10 dual-arm probe failure, evaluated and reused RoboTwin provider's existing Curobo robot-collision model to generate a fixed-hold-pose peer-arm projection for mesh/convex links; meshes are not silently treated as boxes, and PAOS Core, route semantics, and collision safety policy remain unchanged.
- [完成] [eval] [test] 增加 provider projection 的 mesh/convex、空模型、frame/state drift 和 unavailable 回归；仅在投影证据可验证后重新物化 route 并重新申请审批，未复用 v6.8.10 approval。
- [Completed] [Eval] [Test] Added provider-projection regressions for mesh/convex geometry, empty models, frame/state drift, and unavailable outcomes; rematerialized a route only after projection evidence became verifiable and did not reuse the v6.8.10 approval.

### 预期影响文件 / Planned Files

- `changelog/2026-09_part3.md`
- `examples/forge-adapters/robotwin20/runtime/robotwin_simulation_probe_worker.py`
- `examples/forge-adapters/robotwin20/runtime/robotwin_curobo_world_port.py`
- `examples/forge-adapters/robotwin20/src/robotwin20_adapter/dual_arm_state.py`
- `examples/forge-adapters/robotwin20/tests/`
- `docs/forge/DUAL_ARM_PLANNING_EXECUTION_PLAN.md`

### 实际结果 / Actual Results

- [完成] [policy] [fix] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/dual_arm_state.py:L17,L197-L287` 新增 `peer-arm-projection/v2`，直接表达 Curobo robot collision sphere 的 world-frame center/radius、peer-arm identity 与 state revision；不再从 SAPIEN mesh 猜测单一 box。
- [Completed] [Policy] [Fix] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/dual_arm_state.py:L17,L197-L287` adds `peer-arm-projection/v2`, directly representing world-frame Curobo robot-collision sphere centers/radii, peer-arm identity, and state revision instead of guessing one box from a SAPIEN mesh.
- [完成] [policy] [fix] `examples/forge-adapters/robotwin20/runtime/robotwin_simulation_probe_worker.py:L240-L289` 从未选中臂的实际 hold qpos 调用 `motion_gen.kinematics.get_robot_as_spheres()`，将 61 个有效碰撞球由 peer planner base frame 转换到共享 world frame；模型缺失、调用失败或空模型均返回 unavailable。
- [Completed] [Policy] [Fix] `examples/forge-adapters/robotwin20/runtime/robotwin_simulation_probe_worker.py:L240-L289` calls `motion_gen.kinematics.get_robot_as_spheres()` at the peer arm's actual held qpos and transforms 61 valid collision spheres from the peer planner base frame into the shared world frame; missing, failed, or empty models return unavailable.
- [完成] [policy] [fix] `examples/forge-adapters/robotwin20/runtime/robotwin_curobo_world_port.py:L9-L13,L121-L156,L166-L211` 校验双臂 projection 的 scene/state 一致性，并把每个 Curobo sphere 显式转换为边长 `2r` 的保守包围 OBB。真实 vendored Curobo no-motion 验证确认左右 planner 各加载 64 个 active OBB（table + 2 blocks + 61 peer spheres），两侧均通过 `rebuild_motion_gen`，`motion_authorized=false`。
- [Completed] [Policy] [Fix] `examples/forge-adapters/robotwin20/runtime/robotwin_curobo_world_port.py:L9-L13,L121-L156,L166-L211` validates shared scene/state revisions across both projections and explicitly converts each Curobo sphere to a conservative `2r` enclosing OBB. A real vendored-Curobo no-motion check confirmed 64 active OBBs per planner (table + two blocks + 61 peer spheres), `rebuild_motion_gen` on both sides, and `motion_authorized=false`.
- [完成] [eval] [test] `examples/forge-adapters/robotwin20/tests/test_dual_arm_state.py:L79-L130`、`test_curobo_world_port.py:L187-L243`、`test_simulation_probe.py:L68-L130` 覆盖协议往返、world transform、空/缺失模型、NaN 半径、错误 frame、peer identity、scene/state drift、双臂覆盖和保守 OBB 装载；聚焦 `51 passed`，完整仓库相关回归 `776 passed, 1 skipped`，Ruff、compileall、`git diff --check` 通过。
- [Completed] [Eval] [Test] `examples/forge-adapters/robotwin20/tests/test_dual_arm_state.py:L79-L130`, `test_curobo_world_port.py:L187-L243`, and `test_simulation_probe.py:L68-L130` cover protocol round-trip, world transforms, empty/missing models, NaN radii, invalid frames, peer identity, scene/state drift, both-arm coverage, and conservative OBB loading; focused tests report `51 passed`, the full relevant repository regression reports `776 passed, 1 skipped`, and Ruff, compileall, and `git diff --check` pass.
- [完成] [docs] [docs] `docs/forge/DUAL_ARM_PLANNING_EXECUTION_PLAN.md:L222-L234` 记录 Curobo sphere 来源、base/world frame 流程及当前 checker 只加载 cuboid 的实测限制，明确该投影只支持顺序 hold 模式，不宣称同步双臂 swept-volume。
- [Completed] [Docs] [Docs] `docs/forge/DUAL_ARM_PLANNING_EXECUTION_PLAN.md:L222-L234` records the Curobo-sphere source, base/world-frame flow, and measured limitation that the current checker only loads cuboids; the projection supports sequential hold mode only and does not claim synchronized dual-arm swept-volume planning.
- [完成] [eval] [exp] 生成最终 no-motion route package `/home/yanxu/robotwin20-runtime/artifacts/paos-route-v6.8.11-20260907T2200Z/`，request `franka-blocks-green0-dual-v6811b-20260907`；route digest `f66bd11a5d1f941ed9c93facd6476e1eee07163ec1df6a2d377a7c3cbb3379c0`，source-manifest digest `542ea4aac6cfbed491e9f8fdf70eff9a06873c69c7cd9d6632776fdad23d8f7e`，worker digest `c47601e14f3b632481babdbf8eb22e4c1cd3445aaea46ed9d88bd92a7603df96`；独立 route/collision-world 校验通过，保持 `pending_human_review`、`motion_authorized=false`。
- [Completed] [Eval] [Exp] Created the final no-motion route package at `/home/yanxu/robotwin20-runtime/artifacts/paos-route-v6.8.11-20260907T2200Z/` for request `franka-blocks-green0-dual-v6811b-20260907`; route digest is `f66bd11a5d1f941ed9c93facd6476e1eee07163ec1df6a2d377a7c3cbb3379c0`, source-manifest digest is `542ea4aac6cfbed491e9f8fdf70eff9a06873c69c7cd9d6632776fdad23d8f7e`, and worker digest is `c47601e14f3b632481babdbf8eb22e4c1cd3445aaea46ed9d88bd92a7603df96`; independent route/collision-world validation passes with `pending_human_review` and `motion_authorized=false`.

### 关键代码 Diff / Key Code Diff

**修改前 / Before:**

```python
if len(shapes) != 1 or not callable(getattr(shapes[0], "get_half_size", None)):
    raise SimulationProbeError("peer arm collision geometry is unavailable")
```

**修改后 / After:**

```python
sphere_batches = get_spheres(torch.as_tensor(qpos, device=device), filter_valid=True)
center = base_position + base_rotation @ np.asarray(sphere.pose[:3], dtype=np.float64)
spheres.append({"center_m": [*map(float, center)], "radius_m": float(radius)})
```

**修改说明 / Rationale:** 复用 Curobo 对 Franka 已维护的 collision-sphere 模型；provider port 再用实测可用的 OBB checker 加载保守包围体，避免“协议里有 sphere、碰撞后端实际没加载”的假成功。

### 六维验收 / Six-Dimension Acceptance

- 架构集成 / Architecture integration: 通过；协议在 adapter，Curobo API 与几何转换在 RoboTwin provider，PAOS Core/planning/Gateway 未改变。
- 失败路径 / Failure paths: 通过；缺失/空模型、非有限半径、frame、identity、scene/state drift、双臂 coverage 和 cache rebuild 均有测试或真实 no-motion 证据。
- 权威边界 / Authority boundaries: 通过；projection 与 world receipt 只提供 planner readiness，不能授权 motion；SAPIEN contact 与正式 Gateway 事实未被替代。
- 配置与 provenance / Configuration and provenance: 通过；使用当前 planner 的 yml、qpos、base pose 和 collision-sphere 模型，新 route 精确绑定最新 worker。
- 可维护性 / Maintainability: 通过；没有复制 Curobo 碰撞模型、没有新增 planner service 或第二套任务生命周期，v1 cuboid 协议仍兼容已有调用。
- 防止过度防御编程 / Anti-overdefense: 通过；未新增 hash、baseline、发布 gate 或重复安全层；新增校验均对应本次可复现失败或双臂投影最小语义要求。

### Git 提交 / Git Commit

- Implementation commit: `d89e45c`
- Branch: `feature/long-horizon-workflow`
- 时间 / Time: 2026-09-07 22:20 (Asia/Shanghai)

## v6.8.10 (2026-09-07 20:35) - codex

### 预期修改 / Planned Changes

- [完成] [eval] [exp] 物化用户对 `paos-route-v6.8.9-20260907T2015Z` 的精确 simulation-only approval，并在该批准范围内启动顺序双臂 RoboTwin probe；probe 在 scene initialization fail-closed，因此未进入 attached/lift/transport/descent/release/retreat。
- [Completed] [Eval] [Exp] Materialized the user's exact simulation-only approval for `paos-route-v6.8.9-20260907T2015Z` and started the sequential dual-arm RoboTwin probe within that approved scope; it failed closed during scene initialization and therefore did not enter attached/lift/transport/descent/release/retreat.
- [完成] [safety] [test] 将 probe 失败如实保存到唯一 artifact 目录并进行根因审核；仿真仅绑定该 route/source manifest/worker，未调用 Gateway、Dora、Action 或硬件，也未把 unavailable 解释为成功。
- [Completed] [Safety] [Test] Persisted the probe failure in the unique artifact directory and reviewed its cause; the simulation remained bound to the specified route/source manifest/worker, did not call Gateway, Dora, Action, or hardware, and did not interpret unavailable as success.

### 预期影响文件 / Planned Files

- `changelog/2026-09_part3.md`
- `CHANGELOG.md`
- `/home/yanxu/robotwin20-runtime/artifacts/paos-route-v6.8.9-20260907T2015Z/probe/`

### 实际结果 / Actual Results

- [完成] [eval] [exp] v6.8.9 approval 精确物化后，在文档指定的 `/home/yanxu/miniconda3/envs/RoboTwin20/bin/python3.10` 中执行一次 probe；`world_change_started=true`、`simulator_steps=0`、`status=unavailable`，失败阶段为 `scene_initialization`，错误为 `peer arm collision geometry is unavailable`。
- [Completed] [Eval] [Exp] After exact materialization of the v6.8.9 approval, one probe ran with the documented `/home/yanxu/miniconda3/envs/RoboTwin20/bin/python3.10`; it produced `world_change_started=true`, `simulator_steps=0`, `status=unavailable`, failed during `scene_initialization`, and reported `peer arm collision geometry is unavailable`.
- [完成] [eval] [test] 根因限定为 SAPIEN Franka link 使用 mesh/convex geometry，而旧 `_capture_peer_projection()` 只接受单一 `get_half_size()` box；不是 candidate、速度、TCP、route geometry 或 Curobo OBB cache 失败。失败证据与 reset 结果已保存，未进入机器人控制 step。
- [Completed] [Eval] [Test] The cause is bounded to SAPIEN Franka links using mesh/convex geometry while the old `_capture_peer_projection()` accepted only one `get_half_size()` box; it is not a candidate, speed, TCP, route-geometry, or Curobo OBB-cache failure. Failure evidence and reset outcome were persisted, and no robot-control step ran.

## v6.8.9 (2026-09-07 20:10) - codex

### 预期修改 / Planned Changes

- [完成] [eval] [exp] 按双臂规划执行文档重新物化 `blocks_ranking_rgb` 顺序双臂 route package，绑定最新 simulation worker、双臂 capability、dual-arm state 与 peer-arm projection 约束；使用新的不可覆盖 artifact root，不复用历史 route/approval。
- [Completed] [Eval] [Exp] Re-materialized the `blocks_ranking_rgb` sequential dual-arm route package according to the dual-arm planning execution document, binding the latest simulation worker, both-arm capabilities, dual-arm state, and peer-arm projection constraints; used a new immutable artifact root and did not reuse historical routes or approvals.
- [完成] [safety] [test] 仅执行 no-motion materialization 与独立校验；在获得与新 route/source-manifest/worker digest 精确绑定的 `I_REVIEWED_AND_APPROVE_SIMULATION_ONLY` 前，不运行 `scene.step()`、Gateway、Dora、Action 或硬件。
- [Completed] [Safety] [Test] Ran only no-motion materialization and independent validation; before receiving an `I_REVIEWED_AND_APPROVE_SIMULATION_ONLY` approval bound exactly to the new route/source manifest/worker digest, do not run `scene.step()`, Gateway, Dora, Action, or hardware.

### 预期影响文件 / Planned Files

- `changelog/2026-09_part3.md`
- `/home/yanxu/robotwin20-runtime/artifacts/paos-route-v6.8.9-20260907T*/`

### 实际结果 / Actual Results

- [完成] [eval] [exp] 在 `/home/yanxu/robotwin20-runtime/artifacts/paos-route-v6.8.9-20260907T2015Z/` 生成新的不可覆盖 route package，request `franka-blocks-green0-dual-v689-20260907`；输入绑定了最新 `robotwin_simulation_probe_worker.py`（sha256 `7c7b1bfb6ba2c93419c5a34bec7165415e151bfc81aa65c2fba8fa5271de6d5d`）、左右 MotionCapability、controller qualification 和完整 collision world。
- [Completed] [Eval] [Exp] Created a new immutable route package at `/home/yanxu/robotwin20-runtime/artifacts/paos-route-v6.8.9-20260907T2015Z/` with request `franka-blocks-green0-dual-v689-20260907`; bindings include the latest `robotwin_simulation_probe_worker.py` (sha256 `7c7b1bfb6ba2c93419c5a34bec7165415e151bfc81aa65c2fba8fa5271de6d5d`), both MotionCapability documents, controller qualification, and the complete collision world.
- [完成] [eval] [test] 独立 no-motion 校验通过：`validate_route_request()` 与 `validate_collision_world()` 均通过；collision world 包含 2 个非目标方块障碍，`world_digest=eede7e813b7c0477411e63a7c9cbd9a474ed2c185c88ae0c4d3d3c9878f243b4`，route digest `ba6153412ef675b4a1b7cd750f7ea02316cd10ad989873ccf1ef0c596aa09923`，route request sha256 `052975abab373644ee0f076625df54ddc0165f5f92bb3de91b7dac0c102227eb`，source manifest sha256 `b707f0836d973fb57c3e362e4488d6e93e85bf5d8a3f5c6b8e4c7cb1284815dc`。
- [Completed] [Eval] [Test] Independent no-motion validation passed: `validate_route_request()` and `validate_collision_world()` both passed; the collision world contains two non-target block obstacles with `world_digest=eede7e813b7c0477411e63a7c9cbd9a474ed2c185c88ae0c4d3d3c9878f243b4`, route digest `ba6153412ef675b4a1b7cd750f7ea02316cd10ad989873ccf1ef0c596aa09923`, route-request sha256 `052975abab373644ee0f076625df54ddc0165f5f92bb3de91b7dac0c102227eb`, and source-manifest sha256 `b707f0836d973fb57c3e362e4488d6e93e85bf5d8a3f5c6b8e4c7cb1284815dc`.
- [完成] [safety] [policy] `human_review_request.json` 保持 `pending_human_review`、`simulation_only=true`、`motion_authorized=false`；本阶段未执行 `scene.step()`、Gateway、Dora、Action 或硬件。历史 v6.8.2 route/approval 未复用。
- [Completed] [Safety] [Policy] `human_review_request.json` remains `pending_human_review`, `simulation_only=true`, and `motion_authorized=false`; this stage did not execute `scene.step()`, Gateway, Dora, Action, or hardware. Historical v6.8.2 route/approval was not reused.

### 当前边界 / Current Boundary

新的 package 现在等待人工提供与以下字段精确绑定的审批：`route_digest=ba6153412ef675b4a1b7cd750f7ea02316cd10ad989873ccf1ef0c596aa09923`、`source_manifest_digest=b707f0836d973fb57c3e362e4488d6e93e85bf5d8a3f5c6b8e4c7cb1284815dc`、`simulation_probe_worker_sha256=7c7b1bfb6ba2c93419c5a34bec7165415e151bfc81aa65c2fba8fa5271de6d5d`。收到新审批后，下一步才是完整双臂 attached/lift/transport/descent/release/retreat/semantic probe。

The new package now awaits a human approval bound exactly to `route_digest=ba6153412ef675b4a1b7cd750f7ea02316cd10ad989873ccf1ef0c596aa09923`, `source_manifest_digest=b707f0836d973fb57c3e362e4488d6e93e85bf5d8a3f5c6b8e4c7cb1284815dc`, and `simulation_probe_worker_sha256=7c7b1bfb6ba2c93419c5a34bec7165415e151bfc81aa65c2fba8fa5271de6d5d`. Only after that approval may the complete dual-arm attached/lift/transport/descent/release/retreat/semantic probe run.

### 文件变更详情 / File Change Details

- [修改] `changelog/2026-09_part3.md:L3-L56`：将 v6.8.9 从计划状态补全为实际 no-motion route package、校验结果与人工审批边界。
- [Modified] `changelog/2026-09_part3.md:L3-L56`: completed the v6.8.9 plan with the actual no-motion route package, validation results, and human-approval boundary.
- [修改] `CHANGELOG.md:L5-L13`：将 v6.8.9 加入最近版本入口，记录 artifact root、route/source-manifest/worker 绑定和 49 项聚焦回归结果。
- [Modified] `CHANGELOG.md:L5-L13`: added v6.8.9 to the recent-version index with artifact-root, route/source-manifest/worker bindings, and the 49-test focused regression result.

**修改前 / Before:** 最新入口为 v6.8.8，尚无绑定最新双臂 worker 的 route package。

**修改后 / After:** 最新入口为 v6.8.9，并记录新 package 仍处于 `pending_human_review`；没有把 no-motion 物化解释为仿真任务成功。

### 六维阶段验收 / Six-Dimension Stage Acceptance

- 架构集成 / Architecture integration: 通过；route package 继续使用既有 adapter/materializer，双臂状态和 peer projection 由 provider worker 在 reset 后采集，没有新增第二套 planner 或执行面。
- 失败路径 / Failure paths: 通过；旧 approval、worker 漂移和缺少精确人工批准均不能进入 probe。
- 权威边界 / Authority boundaries: 通过；物化与校验不授予 motion authority，Gateway/Verifier 职责未改变。
- 配置与 provenance / Configuration and provenance: 通过；新 package 绑定左右 capability、controller qualification、collision world、route、manifest 和最新 worker。
- 可维护性 / Maintainability: 通过；没有为本次测试增加生产代码或 provider-specific Agent Tool，复用现有 materializer/validator。
- 防止过度防御编程 / Anti-overdefense: 通过；未新增 hash、gate、baseline 或防御层，只使用现有正式 simulation approval 边界所必需的 package binding。

### 验证 / Verification

- 聚焦回归：`49 passed`。
- `validate_route_request()`、`validate_collision_world()`、`git diff --check`：通过。
- simulator steps：`0`；Gateway/Dora/Action/hardware：未调用。

### Git 提交 / Git Commit

- Commit: `af27378`
- Branch: `feature/long-horizon-workflow`
- 时间 / Time: 2026-09-07 20:25 (Asia/Shanghai)

## v6.8.8 (2026-09-07 19:05) - codex

### 预期修改 / Planned Changes

- [计划] [docs] [docs] 记录跨 benchmark 单臂 Franka 的代码复用边界：复用 PAOS Core/planning/Skill 协议，通过 adapter topology 分支接入单臂 provider，不把当前双臂实现误称为即插即用。
- [Planned] [Docs] [Docs] Record the code-reuse boundary for single-arm Franka benchmarks: reuse PAOS Core/planning/Skill contracts and connect a single-arm provider through an adapter topology branch, without calling the current dual-arm implementation plug-and-play.

### 实际结果 / Actual Results

- [完成] [docs] [docs] 在 `docs/forge/DUAL_ARM_PLANNING_EXECUTION_PLAN.md:L420-L458` 记录跨 benchmark 单臂复用边界：PAOS Core/planning/Skill、CapabilitySnapshot、ArmAssignment 和 Gateway/Verifier/Experience 可复用；RoboTwin runtime、route readiness、probe、collision-world 仍需 topology 分支改造。
- [Completed] [Docs] [Docs] Documented the cross-benchmark single-arm reuse boundary at `docs/forge/DUAL_ARM_PLANNING_EXECUTION_PLAN.md:L420-L458`: PAOS Core/planning/Skill, CapabilitySnapshot, ArmAssignment, and Gateway/Verifier/Experience are reusable, while the RoboTwin runtime, route readiness, probe, and collision-world still require topology branches.

### 六维验收 / Six-Dimension Acceptance

- 架构集成 / Architecture integration: 通过；单臂复用被定义为 provider topology 分支，不创建第二套 PAOS 生命周期。
- 失败路径 / Failure paths: 通过；明确当前双臂专用路径不能伪装成单臂即插即用。
- 权威边界 / Authority boundaries: 通过；benchmark/provider 替换不改变 Gateway、Verifier 和物理事实权威。
- 配置与 provenance / Configuration and provenance: 通过；要求新 benchmark 在新任务/revision 中重新绑定 profile、capability 和 evidence。
- 可维护性 / Maintainability: 通过；单臂和双臂共享语义接口，provider 细节保持隔离。
- 防止过度防御编程 / Anti-overdefense: 通过；本轮仅补充文档，没有增加代码 gate、hash 或额外运行时检查。

### 验证 / Verification

- `git diff --check` 通过；本轮为文档与架构边界澄清，没有运行仿真或动作。

### Git 提交 / Git Commit

- Commit: `09388c3`
- Branch: `feature/long-horizon-workflow`
- 时间 / Time: 2026-09-07 19:05 (Asia/Shanghai)

## v6.8.7 (2026-09-07 18:35) - codex

### 预期修改 / Planned Changes

- [计划] [docs] [docs] 在双臂规划主文档中显式标注当前仅支持顺序执行，禁止将两个独立 Curobo planner 的结果解释为双臂同时行动或同步联合规划。
- [Planned] [Docs] [Docs] Explicitly mark in the dual-arm planning document that only sequential execution is currently supported; do not interpret two independent Curobo planner results as simultaneous bimanual action or synchronized joint planning.

### 实际结果 / Actual Results

- [完成] [docs] [docs] 在 `docs/forge/DUAL_ARM_PLANNING_EXECUTION_PLAN.md:L61-L63` 增加醒目的当前实现范围说明：一次只驱动一只选中臂，另一臂采用 hold/park，并以静态投影参与规划；同步双臂、统一时间轴、swept-volume、共同停止和原子执行仍未实现。
- [Completed] [Docs] [Docs] Added a prominent current-scope statement at `docs/forge/DUAL_ARM_PLANNING_EXECUTION_PLAN.md:L61-L63`: only one selected arm is driven at a time while the peer arm uses hold/park and static projection; synchronized bimanual motion, a shared timeline, swept-volume checks, common stop, and atomic execution remain unimplemented.

### 六维验收 / Six-Dimension Acceptance

- 架构集成 / Architecture integration: 通过；文档与 provider 实现边界一致。
- 失败路径 / Failure paths: 通过；同步模式保持未实现/不可宣称，不会被独立 planner 成功结果掩盖。
- 权威边界 / Authority boundaries: 通过；没有新增执行权限或动作接口。
- 配置与 provenance / Configuration and provenance: 通过；仅更新文档，不改变 route/artifact。
- 可维护性 / Maintainability: 通过；限制集中在主规划文档醒目标记。
- 防止过度防御编程 / Anti-overdefense: 通过；未增加代码 gate、hash 或运行时检查。

### 验证 / Verification

- `git diff --check` 通过；本轮没有运行仿真或动作。

### Git 提交 / Git Commit

- Commit: `b0bed6d`
- Branch: `feature/long-horizon-workflow`
- 时间 / Time: 2026-09-07 18:35 (Asia/Shanghai)

## v6.8.6 (2026-09-07 18:10) - codex

### 预期修改 / Planned Changes

- [计划] [policy] [feat] 在 RoboTwin adapter 增加 provider-owned 双臂规划状态与 qualified link/contact 归因，表达 reset/stabilization 后的双臂初始状态、hold/park 语义和状态漂移。
- [Planned] [Policy] [Feat] Add a provider-owned dual-arm planning state and qualified link/contact attribution in the RoboTwin adapter, representing the post-reset/stabilization state, hold/park semantics, and state drift.
- [计划] [policy] [feat] 扩展碰撞世界投影，使选中机械臂规划时可携带另一只机械臂的静态几何投影；保持 Curobo 仅在 provider port 内使用，不新增 PAOS Core planner 或同步 atomic 执行。
- [Planned] [Policy] [Feat] Extend collision-world projection so planning for the selected arm can include a static geometric projection of the peer arm; keep Curobo inside the provider port without adding a PAOS Core planner or synchronized atomic execution.
- [计划] [eval] [test] 为双臂状态、peer-arm projection、contact identity、hold drift 和失败路径增加模块测试，并在实现后执行六维验收（含防止过度防御编程）。
- [Planned] [Eval] [Test] Add module tests for dual-arm state, peer-arm projection, contact identity, hold drift, and failure paths, followed by six-dimension acceptance including anti-overdefense.

### 预期影响文件 / Planned Files

- `examples/forge-adapters/robotwin20/src/robotwin20_adapter/dual_arm_state.py`
- `examples/forge-adapters/robotwin20/src/robotwin20_adapter/collision_world.py`
- `examples/forge-adapters/robotwin20/src/robotwin20_adapter/__init__.py`
- `examples/forge-adapters/robotwin20/runtime/robotwin_curobo_world_port.py`
- `examples/forge-adapters/robotwin20/runtime/robotwin_simulation_probe_worker.py`
- `examples/forge-adapters/robotwin20/tests/test_dual_arm_state.py`
- `examples/forge-adapters/robotwin20/tests/test_collision_world.py`
- `examples/forge-adapters/robotwin20/tests/test_curobo_world_port.py`
- `examples/forge-adapters/robotwin20/tests/test_simulation_probe.py`

### 实际结果 / Actual Results

- [完成] [policy] [feat] 新增 `examples/forge-adapters/robotwin20/src/robotwin20_adapter/dual_arm_state.py:L1-L200`，实现 provider-owned 双臂状态、qualified link identity、hold drift 和 peer-arm projection；所有模型保持 `motion_authorized=false`。
- [Completed] [Policy] [Feat] Added `examples/forge-adapters/robotwin20/src/robotwin20_adapter/dual_arm_state.py:L1-L200` implementing provider-owned dual-arm state, qualified link identity, hold drift, and peer-arm projection; all models keep `motion_authorized=false`.
- [完成] [policy] [feat] 修改 `robotwin_curobo_world_port.py:L82-L238`，将 peer-arm 几何转换到每个 planner 的 base frame，复用 Curobo WorldConfig/update 或已有容量不足重建路径；未执行 scene step。
- [Completed] [Policy] [Feat] Modified `robotwin_curobo_world_port.py:L82-L238` to transform peer-arm geometry into each planner base frame and reuse Curobo WorldConfig/update or the existing rebuild path when capacity is insufficient; no scene step is executed.
- [完成] [safety] [fix] 修改 `robotwin_simulation_probe_worker.py:L426-L500,L503-L542,L853-L910,L1692-L1740`，在 reset 后绑定双臂状态、每步检查 hold drive-target 漂移，并将重复 link 名归一化为 qualified contact identity；无法唯一归因时保持 ambiguous/fail-closed。
- [Completed] [Safety] [Fix] Modified `robotwin_simulation_probe_worker.py:L426-L500,L503-L542,L853-L910,L1692-L1740` to bind dual-arm state after reset, check held-arm drive-target drift each step, and normalize duplicate link names into qualified contact identities; ambiguous attribution remains fail-closed.
- [完成] [eval] [test] 新增和更新双臂状态、peer projection、Curobo port、contact identity 测试；RoboTwin adapter 全量 `289 passed, 1 skipped`，双臂/route/probe 聚焦测试 `40 passed`，Ruff、compileall、`git diff --check` 均通过。
- [Completed] [Eval] [Test] Added and updated dual-arm state, peer projection, Curobo-port, and contact-identity tests; the full RoboTwin adapter suite passed `289 passed, 1 skipped`, focused dual-arm/route/probe tests passed `40`, and Ruff, compileall, and `git diff --check` passed.

### 六维验收 / Six-Dimension Acceptance

- 架构集成 / Architecture integration: 通过；状态与 peer projection 位于 RoboTwin adapter/provider，planning、Task/Revision 和 Gateway 所有权未改变。当前仅实现顺序 + hold/static projection，不宣称同步联合轨迹。
- 失败路径 / Failure paths: 通过；缺失 peer projection、错误 planner arm、无可投影几何、hold 漂移、重复/歧义 contact、provider update 失败均保持明确错误或 unavailable。
- 权威边界 / Authority boundaries: 通过；Curobo 仍只在 provider port，projection/readiness 不授予 motion authority，simulation probe 不接入 Gateway/Dora/Action/hardware。
- 配置与 provenance / Configuration and provenance: 通过；状态绑定 scene/state revision、frame 和 artifact ref；未复用旧 approval。新 worker 变化要求重新物化 route package 和 simulation-only approval。
- 可维护性 / Maintainability: 通过；纯状态模型、provider bridge、probe 运行时和测试分层；未创建第二套 scheduler、生命周期或 planner 实现。
- 防止过度防御编程 / Anti-overdefense: 通过；新增检查均对应此前真实失败（另一臂碰撞、link 归因、状态漂移），未加入硬编码速度、额外哈希或无证据 gate。

### 验证限制 / Verification Boundary

本轮没有运行新的真实 simulation probe，因为 worker 源码已变化，既有 route/approval 不能复用；下一步需重新物化 route package、取得新的 simulation-only approval，再验证完整 attached/lift/transport/descent/release/retreat 与语义 evidence。

### 文件变更详情 / File Change Details

#### [新增] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/dual_arm_state.py` L1-L200

**新增代码 / Added:** provider-owned `DualArmPlanningState`、qualified link identity、hold drift 和 peer-arm projection contracts。

#### [修改] `examples/forge-adapters/robotwin20/runtime/robotwin_curobo_world_port.py` L82-L238

**修改前 / Before:** Curobo world 只包含 table 和非目标物体。

**修改后 / After:** 接收选中臂对应的 peer projection，将 peer cuboid 转换到 planner base frame，并复用已有 update/rebuild 路径。

#### [修改] `examples/forge-adapters/robotwin20/runtime/robotwin_simulation_probe_worker.py` L426-L542,L853-L910,L1692-L1740

**修改前 / Before:** contact 记录使用未限定的 link/entity 名称，路线没有双臂状态与 hold drift 证据。

**修改后 / After:** reset 后捕获双臂状态，逐步检查 held-arm drive target，contact 记录使用 `left:<link>` / `right:<link>` 或 `ambiguous:*`，并将 peer projection 交给 provider port。

其余测试文件新增对应纯函数、provider bridge 与 contact 归因回归用例；未修改 PAOS Core、Gateway、Dora、Action 或硬件执行面。

### Git 提交 / Git Commit

- Commit: `3bfd2b3`
- Branch: `feature/long-horizon-workflow`
- 时间 / Time: 2026-09-07 18:10 (Asia/Shanghai)


## v6.8.5 (2026-09-07 16:45) - codex

### 预期修改 / Planned Changes

- [计划] [docs] [docs] 强化双臂规划文档的联合规划与防碰撞主线，区分单臂带另一臂障碍、顺序双臂和真正同步双臂三种 provider 能力，不把独立 Curobo 规划误称为联合规划。
- [Planned] [Docs] [Docs] Strengthen the dual-arm planning document around joint planning and collision prevention, distinguishing single-arm planning with the other arm as an obstacle, sequential bimanual execution, and true synchronized bimanual capability; do not call independent Curobo planning joint planning.
- [计划] [policy] [docs] 补充联合状态、inter-arm swept-volume、时间轴、资源/取消语义和 PAOS admission 边界，明确当前 RoboTwin/Curobo 路径的可证明范围与阻塞项。
- [Planned] [Policy] [Docs] Add joint-state, inter-arm swept-volume, timeline, resource/cancellation semantics, and PAOS admission boundaries, explicitly stating what the current RoboTwin/Curobo path can prove and what remains blocked.

### 预期影响文件 / Planned Files

- `docs/forge/DUAL_ARM_PLANNING_EXECUTION_PLAN.md`
- `changelog/2026-09_part3.md`

### 实际结果 / Actual Results

- [完成] [docs] [docs] 在 `docs/forge/DUAL_ARM_PLANNING_EXECUTION_PLAN.md:L61-L121` 明确联合规划不是两个单臂结果拼接，区分静态另一臂投影、顺序双臂和同步双臂三种能力，并记录当前独立 Curobo planner 的边界。
- [Completed] [Docs] [Docs] In `docs/forge/DUAL_ARM_PLANNING_EXECUTION_PLAN.md:L61-L121`, clarified that joint planning is not a concatenation of two single-arm results, distinguished static other-arm projection, sequential bimanual, and synchronized bimanual modes, and recorded the limits of the current independent Curobo planners.
- [完成] [policy] [docs] 增加联合状态、三层防碰撞约束（几何、轨迹、执行）、inter-arm swept-volume、统一时间轴、取消/停止和 route admission 语义；当前只允许顺序路线加另一臂 hold/park，atomic synchronized 模式保持阻塞。
- [Completed] [Policy] [Docs] Added joint-state, three-layer collision prevention (geometry, trajectory, execution), inter-arm swept-volume, shared timeline, cancellation/stop, and route-admission semantics; only sequential routes with explicit hold/park are currently allowed, while atomic synchronized mode remains blocked.
- [完成] [eval] [test] `git diff --check` 通过；本轮未修改运行时代码、未执行仿真或动作，因此没有新的 motion evidence。
- [Completed] [Eval] [Test] `git diff --check` passed; no runtime code was changed and no simulation or motion was executed in this documentation revision, so no new motion evidence was produced.
- [完成] [policy] [docs] 在 `docs/forge/DUAL_ARM_PLANNING_EXECUTION_PLAN.md:L123-L228` 映射 Curobo 已有的 `plan_single/plan_batch/check_start_state/update_world/attach/detach/self-collision` 能力与双臂联合规划缺口，并给出 MoveIt 2、Drake、Tesseract 和 SAPIEN/FCL 等 provider 的适用边界；明确 PAOS 只扩展 provider port 和 capability artifact，不重复实现 planner。
- [Completed] [Policy] [Docs] In `docs/forge/DUAL_ARM_PLANNING_EXECUTION_PLAN.md:L123-L228`, mapped Curobo's existing `plan_single/plan_batch/check_start_state/update_world/attach/detach/self-collision` capabilities and its bimanual gaps, and defined the boundaries for MoveIt 2, Drake, Tesseract, and SAPIEN/FCL providers; PAOS extends only provider ports and capability artifacts rather than reimplementing planners.
- [完成] [docs] [review] 按 `docs/zh/03-developer-manual.md` 第 12 节及 Manipulation/benchmark 扩展边界复核 `DUAL_ARM_PLANNING_EXECUTION_PLAN.md:L289-L324`：双臂 planner 保持 adapter/provider port，不新增 `curobo.plan` Agent Tool，不创建第二套执行协议或生命周期，正式动作仍归 Gateway。
- [Completed] [Docs] [Review] Re-reviewed `DUAL_ARM_PLANNING_EXECUTION_PLAN.md:L289-L324` against section 12 and the manipulation/benchmark extension boundary in `docs/zh/03-developer-manual.md`: the dual-arm planner remains an adapter/provider port, with no `curobo.plan` Agent Tool, second execution protocol, or second lifecycle; formal actions remain Gateway-owned.

## v6.8.4 (2026-09-07 16:20) - codex

### 预期修改 / Planned Changes

- [计划] [docs] [docs] 审核“双臂初始姿态、未选中机械臂保持和 inter-arm collision”诊断，纠正无法由当前 contact evidence 证明的 arm 归因，并定义 PAOS-compatible provider-owned 双臂规划协议。
- [Planned] [Docs] [Docs] Review the dual-arm initial-pose, unselected-arm hold, and inter-arm-collision diagnosis; correct arm attribution claims not provable by current contact evidence and define a PAOS-compatible provider-owned dual-arm planning protocol.
- [计划] [policy] [docs] 写入从 reset/stabilization、状态快照、能力/碰撞投影、单臂或双臂 route admission、执行、接触归因到语义验收和重规划的具体流程；明确 PAOS Core、planning、adapter/provider、Gateway、Verifier 和 Experience 的所有权。
- [Planned] [Policy] [Docs] Record the concrete flow from reset/stabilization, state snapshots, capability/collision projection, single- or bimanual route admission, execution, contact attribution, semantic verification, and replanning, with ownership across PAOS Core, planning, adapter/provider, Gateway, Verifier, and Experience.

### 预期影响文件 / Planned Files

- `docs/forge/DUAL_ARM_PLANNING_EXECUTION_PLAN.md`
- `changelog/2026-09_part3.md`

### 实际结果 / Actual Results

- [完成] [docs] [docs] 完成上一轮双臂初始状态、未选中臂保持和 contact arm 归因审核；确认现有 evidence 不能证明碰撞来自左臂，并将后续协议写入 `DUAL_ARM_PLANNING_EXECUTION_PLAN.md`。
- [Completed] [Docs] Completed the previous review of dual-arm initial state, unselected-arm hold, and contact arm attribution; confirmed that existing evidence cannot prove the contact came from the left arm and recorded the follow-up protocol in `DUAL_ARM_PLANNING_EXECUTION_PLAN.md`.

## v6.8.3 (2026-09-07 15:40) - codex

### 预期修改 / Planned Changes

- [计划] [eval] [exp] 物化用户批准的 `franka-blocks-green0-collision-v682-20260907` simulation-only approval，并运行一次独立 RoboTwin probe；仅允许该 route/candidate/source-manifest，覆盖 attached-object、lift、transport、descent、release、retreat 和 after-semantic evidence。
- [Planned] [Eval] [Exp] Materialize the user-approved `franka-blocks-green0-collision-v682-20260907` simulation-only approval and run one independent RoboTwin probe; permit only this route/candidate/source-manifest and cover attached-object, lift, transport, descent, release, retreat, and after-semantic evidence.
- [计划] [safety] [test] 保存成功或失败 evidence、接触对、stop/reset 和语义验收结果；不运行 Gateway、Dora、Action 或 hardware，不把失败解释为成功。
- [Planned] [Safety] [Test] Persist success or failure evidence, contact pairs, stop/reset, and semantic-verification results; do not run Gateway, Dora, Action, or hardware, and do not interpret failure as success.

### 预期影响文件 / Planned Files

- `changelog/2026-09_part3.md`
- `/home/yanxu/robotwin20-runtime/artifacts/paos-route-v6.8.2-20260907T1515Z/route/approval.json`
- `/home/yanxu/robotwin20-runtime/artifacts/paos-route-v6.8.2-20260907T1515Z/probe/`

### 实际结果 / Actual Results

- [完成] [eval] [exp] 物化了与人工确认完全匹配的 simulation-only approval：`/home/yanxu/robotwin20-runtime/artifacts/paos-route-v6.8.2-20260907T1515Z/route/probe/approval.json`，approval sha256 为 `a1529ddd1286863f2be390a8ccf192931515df7aefcf3a3654a997b0c00e5fdf`。
- [Completed] [Eval] [Exp] Materialized the exactly matching simulation-only approval at `/home/yanxu/robotwin20-runtime/artifacts/paos-route-v6.8.2-20260907T1515Z/route/probe/approval.json`; approval sha256 is `a1529ddd1286863f2be390a8ccf192931515df7aefcf3a3654a997b0c00e5fdf`.
- [完成] [safety] [test] 使用 RoboTwin20 环境运行一次独立 probe；provider 可用并实际执行了 `1174` 个 simulator steps，随后完成 stop/reset（`controller_stop_status=stopped`、`simulation_reset_status=completed`），未调用 Gateway、Dora、Action 或 hardware。
- [Completed] [Safety] [Test] Ran one independent probe in the RoboTwin20 environment; the provider was available and executed `1174` simulator steps, then completed stop/reset (`controller_stop_status=stopped`, `simulation_reset_status=completed`), without Gateway, Dora, Action, or hardware calls.
- [完成] [eval] [fix] probe 结果为 `unavailable`，失败阶段为 `retreat`，原因是 `unexpected simulator contact during attached route`；失败 evidence 已保存至 `artifact://simulation-probe/franka-blocks-green0-collision-v682-20260907/block-green-1-0/failure`，并记录 `6167` 个 contact samples。该失败不能解释为成功。
- [Completed] [Eval] [Fix] The probe returned `unavailable` at `retreat` because of `unexpected simulator contact during attached route`; failure evidence is stored at `artifact://simulation-probe/franka-blocks-green0-collision-v682-20260907/block-green-1-0/failure` with `6167` contact samples. The failure is not interpreted as success.

### 诊断 / Diagnosis

- [完成] [policy] [fix] 左臂规划失败后右臂路线被选中，但未选中的左臂在仿真 retreat/descent 期间产生 `panda_leftfinger ↔ table` 接触（最大冲量约 `2.41 N·s`）；因此当前 route 不能证明完整双臂环境安全。该证据指向“未选中机械臂缺少 park/back-to-origin 子任务或其初始状态未纳入 route admission”，不是继续调 candidate 或速度参数的理由。
- [Completed] [Policy] [Fix] After left-arm planning failed, the right-arm route was selected, but the unselected left arm contacted the table during the simulated descent/retreat (maximum impulse about `2.41 N·s`); the current route therefore cannot prove complete dual-arm environmental safety. The evidence points to a missing park/back-to-origin subtask or unadmitted initial state for the unselected arm, not to more candidate or speed tuning.

### 六维验收状态 / Six-Dimension Acceptance Status

- 架构集成 / Architecture: 部分通过；approval、provider probe 和 evidence 边界正确，但完整双臂 route 仍未通过。
- 失败路径 / Failure paths: 通过；错误以 `unavailable` 返回并完成 stop/reset，未伪造成功。
- 权威边界 / Authority: 通过；仅 simulation-only provider 执行，无 Gateway/Dora/Action/hardware 权限。
- 配置与 provenance / Configuration and provenance: 通过；route/source-manifest/approval/candidate 精确绑定，failure artifact 可追溯。
- 可维护性 / Maintainability: 部分通过；证据结构完整，但需要后续显式 park/未选中臂状态建模。
- 防止过度防御编程 / Anti-overdefense: 通过；本轮未新增 hash/gate/速度调参，使用已有绑定和实际接触证据。

### 验证 / Verification

- `approve_simulation_probe.py`：approval materialization passed。
- `run_approved_simulation_probe.py`：返回 `status=unavailable`（预期失败码 `2`），failure artifact、before/after snapshot、contact trace、stop/reset 均已写入。
- 本轮未修改生产代码；因此无代码 Diff。后续若增加 park 子任务，必须生成新的 route package 并重新取得人工 simulation-only approval。

## v6.8.2 (2026-09-07 15:15) - codex

### 预期修改 / Planned Changes

- [计划] [eval] [exp] 使用当前 route-request/v7、collision-world provider 和已资格化的双 Franka controller 输入，物化一个唯一且不可覆盖的 `blocks_ranking_rgb` simulation-only route package；本阶段只生成和独立校验 artifact，不执行 `scene.step()`。
- [Planned] [Eval] [Exp] Materialize one unique, non-overwriting `blocks_ranking_rgb` simulation-only route package using route-request/v7, the collision-world provider, and qualified dual-Franka controller inputs; this stage only generates and independently validates artifacts and does not execute `scene.step()`.
- [计划] [docs] [docs] 记录新 route/source-manifest digest、输入来源、验证结果和待人工审核范围；收到与该包精确绑定的批准后，才运行 attached-object/transport/descent/release/retreat/semantic simulation probe。
- [Planned] [Docs] [Docs] Record the new route/source-manifest digests, input sources, validation results, and human-review scope; only after receiving approval bound to this exact package may the attached-object/transport/descent/release/retreat/semantic simulation probe run.

### 预期影响文件 / Planned Files

- `changelog/2026-09_part3.md`
- `/home/yanxu/robotwin20-runtime/artifacts/paos-route-v6.8.2-*/`

### 实际结果 / Actual Results

- [完成] [eval] [exp] 重新运行 RoboTwin route-input worker，生成带 `coverage=complete` 的新 scene-facts；同 seed=0 下三方块位姿与历史观测一致，且 `robot_control_steps=0`。
- [Completed] [Eval] [Exp] Re-ran the RoboTwin route-input worker to produce new scene facts with `coverage=complete`; all three block poses match the historical observation for seed 0 and `robot_control_steps=0`.
- [完成] [eval] [exp] 物化唯一 package `/home/yanxu/robotwin20-runtime/artifacts/paos-route-v6.8.2-20260907T1515Z/route/`，request `franka-blocks-green0-collision-v682-20260907`，使用真实 `candidate://block-green-1/0`、q4 controller qualification、双臂 MotionCapability 和当前 provider worker。
- [Completed] [Eval] [Exp] Materialized the unique package at `/home/yanxu/robotwin20-runtime/artifacts/paos-route-v6.8.2-20260907T1515Z/route/` with request `franka-blocks-green0-collision-v682-20260907`, using real `candidate://block-green-1/0`, q4 controller qualification, both-arm MotionCapability, and the current provider worker.
- [完成] [policy] [fix] 新 package 使用 route-request/v7，并包含 table+red+blue collision world；collision-world `coverage=complete`、`obstacle_count=2`、`world_digest=bd5a8588011d67d0c1adc4b41338923bc3e15b89ee4b7422dec4776b4460eb2b`。
- [Completed] [Policy] [Fix] The new package uses route-request/v7 and contains a table+red+blue collision world; collision-world `coverage=complete`, `obstacle_count=2`, and `world_digest=bd5a8588011d67d0c1adc4b41338923bc3e15b89ee4b7422dec4776b4460eb2b`.

### 独立校验 / Independent Validation

- `route_geometry_digest=ada0e733dbbe2e59145ef00c89bb33f3841bf379ab15dee94cb3ef31551455d1`
- `route_request_sha256=6911e14243c35e1d4dddf1b05a1b321ea8fb9e0ea3c66b4f6699a4d2b56d2c1e`
- `source_manifest_sha256=96f1e24087f76f8ced9cf1ab68c4fba4704f659683274882a5a7e513b85fc308`
- `human_review_request.json` 当前为 `pending_human_review`，`motion_authorized=false`。
- `validate_route_request()`、`validate_collision_world()`：通过；未运行 `scene.step()`、benchmark、Gateway、Dora、Action 或 hardware。

### 待人工审核 / Awaiting Human Review

只有对上述 `route_digest` 和 `source_manifest_sha256` 精确绑定的人工批准，才允许下一步
运行完整 attached-object/transport/descent/release/retreat/semantic simulation probe；旧
route 的批准不能复用。

## v6.8.1 (2026-09-07 13:10) - codex

### 预期修改 / Planned Changes

- [完成] [fix] 修复 RoboTwin Curobo collision-world 的真实 OBB cache 容量不足：容量足够时沿用 `update_world()`，不足时由 provider 使用原有机器人配置重建带完整世界的 MotionGen，并在两个机械臂实例准备完成后切换；不修改 PAOS Core、不放宽碰撞规则、不执行动作。
- [Completed] [Fix] Fixed the real RoboTwin Curobo collision-world OBB cache shortage: keep `update_world()` when capacity is sufficient, otherwise let the provider rebuild MotionGen from the existing robot configuration with the complete world and swap only after both arm instances are ready; do not change PAOS Core, relax collision rules, or execute motion.
- [完成] [eval] 增加容量检测、重建成功/失败、双臂原子切换和 no-motion provider 验证，并完成六维验收（架构集成、失败路径、权威边界、配置与 provenance、可维护性、防止过度防御编程）。
- [Completed] [Eval] Added capacity detection, rebuild success/failure, dual-arm atomic swap, and no-motion provider validation, followed by six-dimension acceptance (architecture, failure paths, authority boundaries, configuration/provenance, maintainability, and anti-overdefense).

### 预期影响文件 / Planned Files

- `examples/forge-adapters/robotwin20/runtime/robotwin_curobo_world_port.py`
- `examples/forge-adapters/robotwin20/tests/test_curobo_world_port.py`
- `docs/forge/ROBOTWIN_ADAPTER_REFACTOR_DIAGNOSIS.md`
- `docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md`
- `docs/forge/PLANNING_MODULE_DESIGN.md`

### 实际结果 / Actual Results

- [完成] [feat] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/collision_world.py:L1-L227` 新增 provider-owned `SceneCollisionWorld` 投影；scene facts 的 coverage 必须为 complete，非目标实体生成 cuboid obstacle，保持 `motion_authorized=false`。
- [Completed] [Feat] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/collision_world.py:L1-L227` added the provider-owned `SceneCollisionWorld` projection; scene-facts coverage must be complete, non-target entities become cuboid obstacles, and `motion_authorized=false` is preserved.
- [完成] [feat] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/route_readiness.py:L190-L330`、`route_generation.py:L180-L245`、`scripts/materialize_complete_route.py:L480-L580` 接入 route-request/v7 collision-world binding、artifact 物化和 scene/world/target/obstacle 校验。
- [Completed] [Feat] Wired route-request/v7 collision-world binding, artifact materialization, and scene/world/target/obstacle validation in `route_readiness.py:L190-L330`, `route_generation.py:L180-L245`, and `materialize_complete_route.py:L480-L580`.
- [完成] [safety] `examples/forge-adapters/robotwin20/runtime/robotwin_curobo_world_port.py:L1-L195` 与 `robotwin_simulation_probe_worker.py:L1530-L1555` 将同一 provider world 投影到左右 `motion_gen`/batch；容量不足时按 RoboTwin YAML 重建并 warmup，仍不执行 `scene.step()`。
- [Completed] [Safety] `robotwin_curobo_world_port.py:L1-L195` and `robotwin_simulation_probe_worker.py:L1530-L1555` apply the same provider world to both arms' `motion_gen`/batch; insufficient capacity rebuilds and warms up from the RoboTwin YAML without `scene.step()`.
- [完成] [eval] `tests/test_collision_world.py:L1-L76`、`tests/test_curobo_world_port.py:L1-L140` 及受影响 route 测试覆盖 target exclusion、coverage/provenance、容量分支、重复 world revision、失败回滚和双臂原子替换。
- [Completed] [Eval] Added coverage for target exclusion, coverage/provenance, capacity branches, repeated world revisions, rollback, and dual-arm atomic replacement in `test_collision_world.py:L1-L76`, `test_curobo_world_port.py:L1-L140`, and affected route tests.

### 验证 / Verification

- 受影响 collision-world suite: `8 passed`。
- Full module suite: `759 passed, 1 skipped`。
- Real RoboTwin20/Curobo no-motion probe: `blocks_ranking_rgb` 双 Franka，table+red+blue world；两臂均 `operation=rebuild_motion_gen`，provider capacity 3，`scene.step`/控制命令 0。
- Ruff、compileall、`git diff --check`: passed。

### 六维验收 / Six-Dimension Review

- 架构集成、失败路径、权威边界、配置与 provenance、可维护性：通过，无 Blocker/Major。
- Architecture, failure paths, authority boundaries, configuration/provenance, and maintainability: pass, no Blocker/Major.
- 防止过度防御编程：通过；cache capacity 从固定 8 改为由 obstacle 数量推导，仅保留与已复现 Curobo overflow、跨进程 artifact drift 和 scene/world stale 直接对应的检查。
- Anti-overdefense: pass; cache capacity is derived from obstacle count instead of fixed 8, and only checks justified by the reproduced Curobo overflow, cross-process artifact drift, and scene/world staleness remain.

### Git 提交 / Git Commit

- Commit: `2e2c07b`
- Documentation closeout: `1830517`
- Branch: `feature/long-horizon-workflow`
- 时间 / Time: 2026-09-07 14:55 (Asia/Shanghai)

## v6.8.0 (2026-09-07 12:30) - codex

### 预期修改 / Planned Changes

- [完成] [policy] [feat] 在 RoboTwin adapter 增加 provider-owned `SceneCollisionWorld` 纯计算投影，从已验证 scene facts 生成非目标实体的 cuboid 障碍描述，并保持 `motion_authorized=false`。
- [Completed] [Policy] [Feat] Added a provider-owned, pure-computation `SceneCollisionWorld` projection to the RoboTwin adapter, deriving cuboid obstacles for non-target entities from validated scene facts while keeping `motion_authorized=false`.
- [完成] [safety] [feat] 在 RoboTwin runtime 增加 Curobo world port，将同一 collision world 更新到左右 `MotionGen`/`motion_gen_batch`，并记录 provider update receipt；不接入 PAOS Core、Gateway 或动作授权。
- [Completed] [Safety] [Feat] Added a RoboTwin runtime Curobo world port that applies the same collision world to both arms' `MotionGen`/`motion_gen_batch` and records provider update receipts; do not connect it to PAOS Core, Gateway, or motion authorization.
- [完成] [eval] [test] 扩展 route-readiness 对 collision-world 引用、场景/世界版本、覆盖状态和目标排除的校验；增加纯 builder 与 runtime port 单元测试，默认不执行 scene.step。
- [Completed] [Eval] [Test] Extended route-readiness validation for collision-world references, scene/world revisions, coverage, and target exclusion; added pure builder and runtime-port unit tests without executing `scene.step` by default.

### 预期影响文件 / Planned Files

- `examples/forge-adapters/robotwin20/src/robotwin20_adapter/collision_world.py`
- `examples/forge-adapters/robotwin20/src/robotwin20_adapter/route_readiness.py`
- `examples/forge-adapters/robotwin20/src/robotwin20_adapter/__init__.py`
- `examples/forge-adapters/robotwin20/runtime/robotwin_curobo_world_port.py`
- `examples/forge-adapters/robotwin20/tests/test_collision_world.py`
- `examples/forge-adapters/robotwin20/tests/test_curobo_world_port.py`
- `examples/forge-adapters/robotwin20/tests/test_route_readiness.py`

## v6.7.11 (2026-09-07 12:05) - codex

### 变更摘要 / Change Summary

- [完成] [chore] 回写 v6.7.10 Anti-OverDefense 审查提交信息；不修改功能代码。
- [Completed] [Chore] Recorded the v6.7.10 Anti-OverDefense review commit; no functional code changes.

### Git 提交 / Git Commit

- Commit: `c60e082`
- Branch: `feature/planning-loop`

## v6.7.10 (2026-09-07 10:45) - codex

### 预期修改 / Planned Changes

- [计划] [policy] [review] 按全局 Anti-OverDefense 规则审查最近两周新增代码；仅在能指出具体失败场景且证明 Git、版本号、主键、事务、唯一约束、类型和普通测试不足时删除额外 hash/SHA、冻结 contract、baseline 或 gate；既有机器人安全、权限、跨系统和正式发布门禁保持不变。
- [Planned] [Policy] [Review] Audit code added in the last two weeks under the global Anti-OverDefense rule; remove extra hashes/SHA, frozen contracts, baselines, or gates only when a concrete failure and insufficiency of Git, versioning, primary keys, transactions, uniqueness, types, and ordinary tests are demonstrated; retain existing robotics-safety, authorization, cross-system, and release gates.
- [计划] [eval] [test] 以模块级静态审查和受影响测试为证据；不为审查新增防御性测试，不运行 Gateway、Dora、Action、硬件或未经批准的仿真动作。
- [Planned] [Eval] [Test] Use module-level static review and affected tests as evidence; add no defensive tests solely for this audit and do not run Gateway, Dora, Action, hardware, or unapproved simulation motion.

### 预期影响文件 / Planned Files

- `PhyAgentOS/forge/`
- `PhyAgentOS/planning/`
- `examples/forge-adapters/robotwin20/`
- `changelog/2026-09_part3.md`

### 实际结果 / Actual Results

- [完成] [policy] 审查 2026-08-24 至 2026-09-07 新增的 planning、Forge task/evidence、Skill runtime、RoboTwin adapter/runtime、审批与资格代码；仅确认两类本地派生标识属于过度防御：Skill reducer 内部 `node_digest/dag_digest` 与单次候选枚举的 `option_id` 内容哈希。跨进程、任务 revision、artifact、审批、控制器资格、Gateway 幂等和正式发布完整性摘要均有可达失败场景，予以保留。
- [Completed] [Policy] Audited planning, Forge task/evidence, Skill runtime, RoboTwin adapter/runtime, approval, and qualification code added from 2026-08-24 through 2026-09-07; identified only two local derived identifiers as over-defensive: Skill reducer `node_digest/dag_digest` and the per-enumeration candidate `option_id` content hash. Cross-process, task-revision, artifact, approval, controller-qualification, Gateway idempotency, and release-integrity digests remain because reachable failure scenarios justify them.
- [完成] [refactor] `examples/forge-skills/pick-place-workflow/src/pick_place_workflow/long_horizon.py:L5-L507` 删除本地 `node_digest`、`dag_digest` 及状态/快照绑定；DAG 仍由 `WORKFLOW_DAG_VERSION`、节点字段校验、依赖校验和普通测试保证一致性。
- [Completed] [Refactor] `examples/forge-skills/pick-place-workflow/src/pick_place_workflow/long_horizon.py:L5-L507` removed local `node_digest`, `dag_digest`, and state/snapshot bindings; DAG consistency remains covered by `WORKFLOW_DAG_VERSION`, node-field validation, dependency validation, and ordinary tests.
- [完成] [refactor] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/arm_candidates.py:L5-L285` 删除候选枚举 `option_id` 的 SHA-256，改为当前枚举内的 `option-{candidate_index}-{arm_ids}`；候选、task/revision、node 和 arm 绑定字段继续提供归因。
- [Completed] [Refactor] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/arm_candidates.py:L5-L285` removed the candidate-enumeration SHA-256 `option_id` and replaced it with local `option-{candidate_index}-{arm_ids}`; candidate, task/revision, node, and arm binding fields still provide attribution.
- [完成] [eval] 更新 `examples/forge-skills/pick-place-workflow/tests/test_long_horizon.py:L35-L130`，验证仅保留 DAG version、移除 digest 快照，并覆盖 stale version；候选枚举回归测试保持通过。
- [Completed] [Eval] Updated `examples/forge-skills/pick-place-workflow/tests/test_long_horizon.py:L35-L130` to verify DAG version only, remove digest snapshot assertions, and cover stale version; candidate-enumeration regression tests remain passing.

### 验证 / Verification

- `test_long_horizon.py`: 20 passed
- `test_arm_candidates.py`: 13 passed
- Full module suite: 751 passed, 1 skipped
- Ruff, compileall, and `git diff --check`: passed

### Git 提交 / Git Commit

- Commit: `ab10a61`
- Branch: `feature/long-horizon-workflow`
- 时间 / Time: 2026-09-07 12:00 (Asia/Shanghai)

## v6.7.9 (2026-09-07 10:35) - codex

### 预期修改 / Planned Changes

- [计划] [chore] 将用户批准的 Anti-OverDefense 规则写入全局 `~/.codex/AGENTS.md`：默认不新增 hash/SHA、冻结 contract、baseline 或 gate；只有具体失败场景和不足理由充分时才允许加入；不删除已有安全措施，门禁限于不可逆/跨系统/安全/正式发布边界。
- [Planned] [Chore] Add the user-approved Anti-OverDefense rule to global `~/.codex/AGENTS.md`: do not add hashes/SHA, frozen contracts, baselines, or gates by default; add them only with a concrete failure scenario and a demonstrated insufficiency; never remove existing safety measures, and limit gates to irreversible, cross-system, safety, or release boundaries.
- [计划] [chore] 将 v6.7.7 未提交的过度防御实现草稿标记为撤回并保持删除；保留此前已提交的真实安全措施和诊断文档。
- [Planned] [Chore] Mark the uncommitted over-defensive v6.7.7 draft as withdrawn and keep it deleted; retain previously committed real safety measures and diagnosis documents.

### 预期影响文件 / Planned Files

- `/home/yanxu/.codex/AGENTS.md`
- `examples/forge-adapters/robotwin20/src/robotwin20_adapter/__init__.py`（仅确认草稿导出已撤回）
- `changelog/2026-09_part3.md`

### v6.7.9 实际结果 / Actual Results

- [完成] [chore] `/home/yanxu/.codex/AGENTS.md:L3-L25` 新增中英文 Anti-OverDefense 全局规则；明确默认不新增 hash/SHA、冻结 contract、baseline 或 gate，只有具体失败场景且现有机制不足时才允许加入，并保留既有安全措施。
- [Completed] [Chore] Added the bilingual Anti-OverDefense global rule at `/home/yanxu/.codex/AGENTS.md:L3-L25`; it prohibits hashes/SHA, frozen contracts, baselines, and gates by default, requires a concrete failure plus insufficiency evidence, and preserves existing safety measures.
- [完成] [chore] 撤回并删除 v6.7.7 未提交的 `collision_world.py` 与 `test_collision_world.py` 草稿，移除 `__init__.py` 的临时导出；未修改已提交的安全门禁或诊断文档。
- [Completed] [Chore] Withdrawn and deleted the uncommitted v6.7.7 `collision_world.py` and `test_collision_world.py` drafts and removed their temporary `__init__.py` exports; previously committed safety gates and diagnosis documents were not changed.
- [完成] [eval] [test] 只读验证通过：草稿文件不存在、Anti-OverDefense 中英文规则可检索、`git diff --check` 通过；未执行仿真、Gateway、Dora、Action 或硬件。
- [Completed] [Eval] [Test] Read-only validation passed: draft files are absent, the bilingual Anti-OverDefense rule is present, and `git diff --check` passes; no simulation, Gateway, Dora, Action, or hardware was run.

### 文件变更详情 / File Change Details

#### [新增] `/home/yanxu/.codex/AGENTS.md` L3-L25

**新增内容：** Anti-OverDefense 全局范围规则及英文等价规则。

#### [删除] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/collision_world.py`（未提交草稿）

**删除原因：** 该实现加入了未经具体失败场景证明的额外 hash、schema、容量和防御性校验。

#### [删除] `examples/forge-adapters/robotwin20/tests/test_collision_world.py`（未提交草稿）

**删除原因：** 与撤回的过度防御实现绑定，避免测试固化未经批准的 contract。

## v6.7.8 (2026-09-07 10:25) - codex

### 预期修改 / Planned Changes

- [计划] [chore] 按 Stop That Shit 范围闸门撤回本轮未提交的 `SceneCollisionWorld` 防御性实现草稿和测试；不保留未经用户要求、未经真实 provider seam 证明的额外 schema/哈希/容量逻辑。
- [Planned] [Chore] Apply the Stop That Shit scope gate by removing this turn's uncommitted defensive `SceneCollisionWorld` draft and tests; retain no extra schema/hash/capacity logic that was neither requested nor proven by a real provider seam.
- [计划] [docs] [docs] 将 Stop That Shit 闸门加入 PAOS 开发工作流：先确认请求、必要性和可达证据，再决定是否修改；review/answer 默认只读，禁止 speculative hardening、重复审计和无必要抽象。
- [Planned] [Docs] [Docs] Add the Stop That Shit gate to the PAOS development workflow: confirm request, necessity, and reachable evidence before editing; review/answer remain read-only by default, with speculative hardening, repeated audits, and unnecessary abstractions prohibited.

### 预期影响文件 / Planned Files

- `examples/forge-adapters/robotwin20/src/robotwin20_adapter/collision_world.py`（删除草稿）
- `examples/forge-adapters/robotwin20/tests/test_collision_world.py`（删除草稿）
- `examples/forge-adapters/robotwin20/src/robotwin20_adapter/__init__.py`
- `docs/user_development_guide/README.md`
- `docs/user_development_guide/README_en.md`
- `changelog/2026-09_part3.md`

## v6.7.7 (2026-09-07 10:20) - codex

### 预期修改 / Planned Changes

- [计划] [policy] [feat] 在 RoboTwin adapter 新增纯计算 `SceneCollisionWorld` artifact builder；从已校验 scene-facts 投影非目标实体，绑定 coverage、scene/world revision、frame、calibration、geometry provenance、cache capacity 和不可授权标志。
- [Planned] [Policy] [Feat] Add a pure-computation `SceneCollisionWorld` artifact builder to the RoboTwin adapter; project non-target entities from validated scene-facts while binding coverage, scene/world revisions, frame, calibration, geometry provenance, cache capacity, and the non-authoritative flag.
- [计划] [eval] [test] 增加 builder/schema 的成功与 fail-closed 测试，覆盖未知空间、缺失几何、目标重复注册、digest/frame/revision 漂移、非有限尺寸和 cache capacity；不导入 Curobo，不执行 scene.step。
- [Planned] [Eval] [Test] Add success and fail-closed builder/schema tests covering unknown space, missing geometry, duplicate target registration, digest/frame/revision drift, non-finite dimensions, and cache capacity; do not import Curobo or execute scene.step.

### 预期影响文件 / Planned Files

- `examples/forge-adapters/robotwin20/src/robotwin20_adapter/collision_world.py`
- `examples/forge-adapters/robotwin20/src/robotwin20_adapter/__init__.py`
- `examples/forge-adapters/robotwin20/tests/test_collision_world.py`
- `changelog/2026-09_part3.md`

## v6.7.6 (2026-09-07 10:10) - codex

### 预期修改 / Planned Changes

- [完成] [chore] 将 v6.7.5 的实际提交哈希写回月度日志；不修改功能或运行代码。
- [Completed] [Chore] Write the v6.7.5 commit hash back to the monthly log; no functionality or runtime code changed.

### Git 提交 / Git Commit

- Commit: `c60e082`
- Branch: `feature/planning-loop`

## v6.7.5 (2026-09-07 10:09) - codex

### 预期修改 / Planned Changes

- [计划] [chore] 补充 v6.7.4 文档复核提交的 Git commit 元数据，不修改功能、协议或运行代码。
- [Planned] [Chore] Add Git commit metadata for the v6.7.4 documentation review without changing functionality, protocols, or runtime code.

### 预期影响文件 / Planned Files

- `changelog/2026-09_part3.md`

### v6.7.5 实际结果 / Actual Results

- [完成] [chore] 记录 v6.7.4 文档复核提交 `7f85ce1`，分支为 `feature/long-horizon-workflow`；未修改功能、协议或运行代码。
- [Completed] [Chore] Recorded documentation-review commit `7f85ce1` on `feature/long-horizon-workflow`; no functionality, protocol, or runtime code was changed.

### Git 提交 / Git Commit

- Commit: `6fc1f2e`
- Branch: `feature/long-horizon-workflow`

## v6.7.4 (2026-09-07 10:06) - codex

### 预期修改 / Planned Changes

- [完成] [policy] [diagnosis] 从架构集成、失败路径、权威边界、配置与 provenance、可维护性五个维度审查“将已观测物体纳入 Curobo 碰撞世界”的诊断；补充未知空间、阶段化目标排除、双 MotionGen 实例和 world-revision 语义，避免把候选替换误报为根因修复。
- [Completed] [Policy] Reviewed the diagnosis for projecting observed objects into the Curobo collision world across architecture integration, failure paths, authority boundaries, configuration/provenance, and maintainability; added unknown-space, phase-scoped target exclusion, dual-MotionGen, and world-revision semantics so candidate replacement is not misreported as a root-cause fix.
- [完成] [docs] [docs] 将五维审核结论和 provider-owned `SceneCollisionWorld` 协议写入 PAOS 状态诊断、RoboTwin adapter 重构诊断与 planning module 设计文档；未修改运行代码，未启动 scene.step、Gateway、Dora、Action 或硬件。
- [Completed] [Docs] [Docs] Recorded the five-dimension review and provider-owned `SceneCollisionWorld` contract in the PAOS state diagnosis, RoboTwin adapter refactor diagnosis, and planning-module design documents; no runtime code was changed and no scene.step, Gateway, Dora, Action, or hardware was started.
- [完成] [eval] [test] 执行文档一致性、`git diff --check` 与只读结构检查；记录仍需新审批和真实/独立 evidence 的门禁。
- [Completed] [Eval] [Test] Ran documentation consistency, `git diff --check`, and read-only structural checks; recorded the remaining gates for fresh approval and real/independent evidence.

### 预期影响文件 / Planned Files

- `docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md`
- `docs/forge/ROBOTWIN_ADAPTER_REFACTOR_DIAGNOSIS.md`
- `docs/forge/PLANNING_MODULE_DESIGN.md`
- `changelog/2026-09_part3.md`

### v6.7.4 实际结果 / Actual Results

- [完成] [policy] [diagnosis] 五维复核确认 32.23 的根因成立：`planner.py:L58-L91` 的 Curobo `world_config` 仅含 table，probe `L790-L829` 仅 attach 当前目标；candidate 替换不构成修复。补充未知空间覆盖、阶段化目标排除、双 `MotionGen` 同步和 Coordinator 负责重规划的语义门禁。
- [Completed] [Policy] [Diagnosis] The five-dimension review confirms the root cause in 32.23: Curobo `world_config` in `planner.py:L58-L91` contains only the table, while the probe `L790-L829` attaches only the current target; candidate replacement is not a fix. Added explicit gates for unknown-space coverage, phase-scoped target exclusion, synchronized dual-`MotionGen` updates, and Coordinator-owned replanning.
- [完成] [docs] [docs] `docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md:L1373-L1431` 写入五维审核、`SceneCollisionWorld` 归属和后续门禁；`docs/forge/ROBOTWIN_ADAPTER_REFACTOR_DIAGNOSIS.md:L574-L649` 写入 provider artifact、字段约束和 adapter seam；`docs/forge/PLANNING_MODULE_DESIGN.md:L211-L224` 明确 planning 只做 admission，不构建 Curobo world 或创建 revision。
- [Completed] [Docs] [Docs] Recorded the five-dimension review, `SceneCollisionWorld` ownership, and follow-up gates in `docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md:L1373-L1431`; provider artifact fields and adapter seams in `docs/forge/ROBOTWIN_ADAPTER_REFACTOR_DIAGNOSIS.md:L574-L649`; and the planning boundary (admission only, no Curobo world or revision creation) in `docs/forge/PLANNING_MODULE_DESIGN.md:L211-L224`.
- [完成] [eval] [test] 只读验证通过：`git diff --check` 无输出；文档关键字段、五维标题和 no-motion 门禁均可检索。未修改运行代码，未执行 `scene.step`、benchmark、Gateway、Dora、Action 或硬件。
- [Completed] [Eval] [Test] Read-only validation passed: `git diff --check` is clean, and the document's key fields, five-dimension headings, and no-motion gates are present. No runtime code was changed and no `scene.step`, benchmark, Gateway, Dora, Action, or hardware was run.

### 文件变更详情 / File Change Details

#### [新增] `docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md` L1373-L1431

**新增内容：** 五维复核、未知空间与阶段化目标排除修订、planning/Coordinator 权威边界，以及新的 SceneCollisionWorld 实施门禁。

#### [新增] `docs/forge/ROBOTWIN_ADAPTER_REFACTOR_DIAGNOSIS.md` L574-L649

**新增内容：** provider-owned collision-world artifact schema、字段校验、双 MotionGen 更新要求和 adapter 分层。

#### [新增] `docs/forge/PLANNING_MODULE_DESIGN.md` L211-L224

**新增内容：** collision-world admission boundary，明确 planning 模块不得调用 Curobo、scene.step 或创建 PlanRevision。

### 验证 / Validation

- `git diff --check`：通过 / passed
- 关键文档字段检索：通过 / passed
- 运行代码与动作执行：未触碰 / not run

## v6.7.3 (2026-09-07 02:05) - codex

### 预期修改 / Planned Changes

- [完成] [eval] [exp] 基于 v6.7.2 attached-object/environment 接触证据，使用同一 scene、calibration、qualification 与 capability 绑定生成新的 GraspGen candidate-0 simulation-only route；不复用已消费的 v6.7.2 approval，不修改速度或碰撞门禁。
- [Completed] [Eval] [Exp] Generated a new GraspGen candidate-0 simulation-only route from the v6.7.2 attached-object/environment contact evidence while retaining the same scene, calibration, qualification, and capability bindings; did not reuse the consumed v6.7.2 approval or alter speed/collision gates.
- [完成] [safety] [review] 对 candidate-0 route 仅执行只读 geometry/provenance 校验并生成新的人工审批请求；在新的 `I_REVIEWED_AND_APPROVE_SIMULATION_ONLY` 之前不运行 scene.step、benchmark、Gateway、Dora、Action 或硬件。
- [Completed] [Safety] [Review] Ran only read-only geometry/provenance validation for the candidate-0 route and emitted a fresh human-review request; no scene.step, benchmark, Gateway, Dora, Action, or hardware ran before a new `I_REVIEWED_AND_APPROVE_SIMULATION_ONLY` approval.

### v6.7.3 实际结果 / Actual Results

- [完成] [eval] [exp] 使用真实 GraspGen `candidate://block-green-1/0` 生成不可覆盖 package `/home/yanxu/robotwin20-runtime/artifacts/paos-route-v6.7.3-candidate0-20260907T015812591206071/`；request `franka-blocks-green0-qualified-q4-v673-20260907`，route digest `8a63ba68a9387e80ef8b3b3b67fbb0b469425139b09b621e6a4a0bccefb8f07a`，source-manifest digest `0c70ba501db1a0e2962a5dff3e839fc58b8b1c0dbe0f6cf05d255ad8baee1b8`。
- [Completed] [Eval] [Exp] Generated an immutable package from the real GraspGen `candidate://block-green-1/0` at `/home/yanxu/robotwin20-runtime/artifacts/paos-route-v6.7.3-candidate0-20260907T015812591206071/`; request `franka-blocks-green0-qualified-q4-v673-20260907`, route digest `8a63ba68a9387e80ef8b3b3b67fbb0b469425139b09b621e6a4a0bccefb8f07a`, source-manifest digest `0c70ba501db1a0e2962a5dff3e839fc58b8b1c0dbe0f6cf05d255ad8baee1b8`.
- [完成] [safety] [review] 只读校验确认 package 绑定同一 scene/calibration/q4 qualification/双臂 capability；candidate-0 release 点到静态 blue block 的水平距离约 `0.1189 m`，candidate-1 约 `0.0565 m`。package 保持 `pending_human_review` 与 `motion_authorized=false`，未执行 scene.step、benchmark、Gateway、Dora、Action 或硬件。
- [Completed] [Safety] [Review] Read-only validation confirms the same scene/calibration/q4 qualification/both-arm capability bindings; candidate-0 release point is approximately `0.1189 m` horizontally from the static blue block versus `0.0565 m` for candidate-1. The package remains `pending_human_review` and `motion_authorized=false`; no scene.step, benchmark, Gateway, Dora, Action, or hardware ran.
- [完成] [policy] [diagnosis] 发现 Curobo provider `world_config` 仅注册 table，未注册其它方块；因此 planner pass 不能替代 attached-object/environment clearance 证据。未放宽碰撞门禁、未调整速度参数、未复用 v6.7.2 approval。
- [Completed] [Policy] [Diagnosis] Confirmed that the Curobo provider `world_config` registers only the table and omits other blocks; planner pass therefore cannot substitute for attached-object/environment clearance evidence. No collision gate was relaxed, no speed parameter was tuned, and the v6.7.2 approval was not reused.

### 五维复核 / Five-Dimension Review

- 架构集成：通过；仅新增 provider-owned route artifact 与诊断文档，不创建第二套 PAOS 生命周期或执行面。
- 失败路径：通过；旧 approval 不复用，candidate/scene/provenance digest 绑定失败时保持 fail-closed。
- 权威边界：通过；geometry 筛选不授予 motion authority，真实接触仍由独立 probe 判定。
- 配置与 provenance：通过；scene、calibration、GraspGen 候选、qualification、capability、worker digest 全部冻结。
- 可维护性：通过；候选替换通过 materializer 参数完成，未修改公共 Gateway/Action 接口。
- Architecture integration: pass; only a provider-owned route artifact and diagnosis were added, with no second PAOS lifecycle or execution plane.
- Failure paths: pass; the old approval is not reused, and candidate/scene/provenance digest drift remains fail-closed.
- Authority boundaries: pass; geometry screening grants no motion authority, and real contacts remain adjudicated by the independent probe.
- Configuration and provenance: pass; scene, calibration, GraspGen candidate, qualification, capability, and worker digests are frozen.
- Maintainability: pass; candidate replacement is parameterized through the materializer without changing public Gateway/Action interfaces.

### Git 提交 / Git Commit

- Commit: `8af4b74`
- Branch: `feature/long-horizon-workflow`
- 时间 / Time: 2026-09-07 02:06 (Asia/Shanghai)


## v6.7.2 (2026-09-07 00:35) - codex

### 预期修改 / Planned Changes

- [计划] [eval] [exp] 使用已确认的 v6.7.1 route/source-manifest digest 生成一次性 simulation-only probe approval，并严格绑定 reviewer、runtime、worker、route 和输入 artifact digest。
- [Planned] [Eval] [Exp] Generate a single-use simulation-only probe approval from the confirmed v6.7.1 route/source-manifest digests, strictly binding reviewer, runtime, worker, route, and input-artifact digests.
- [计划] [safety] [test] 运行已批准的单对象 RoboTwin probe，保存完整 route phase、controller step、contact/attached-object、before/after semantic 和 stop/failure evidence；不运行 benchmark、Gateway、Dora、Action 或硬件。
- [Planned] [Safety] [Test] Run the approved single-object RoboTwin probe and persist complete route-phase, controller-step, contact/attached-object, before/after semantic, and stop/failure evidence; do not run benchmark, Gateway, Dora, Action, or hardware.

### 预期影响文件 / Planned Files

- `/home/yanxu/robotwin20-runtime/artifacts/paos-route-v6.7.1-20260907T0020Z/probe/`
- `changelog/2026-09_part3.md`

### 实际结果 / Actual Results

- [完成] [policy] [fix] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/controller_qualification.py:L109-L119` 兼容 Python 3.10 的 RFC3339 `Z` 时间戳；此前 worker 在 world change 前错误拒绝 q4 qualification package。
- [Completed] [Policy] [Fix] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/controller_qualification.py:L109-L119` normalizes RFC3339 `Z` timestamps for Python 3.10; before this fix the worker incorrectly rejected the q4 qualification package before world change.
- [完成] [eval] [test] 新增 timestamp portability regression；qualification/probe 专项 `41 passed`。
- [Completed] [Eval] [Test] Added a timestamp portability regression; qualification/probe focus passes `41 tests`.
- [完成] [eval] [exp] v6.7.2 route 已重新物化并取得一次性 approval：route digest `28cf77aeca2f511401fd621e62790d77b380fcc00e77cf0f1ffe47d8217d6383`，source-manifest digest `0daf43b67d4fcdd077e17cad8fcdacfb152bbfa440feed731487d96500a11784`，approval sha256 `efa23da2f76c9ade2f2cc5ac9be77fe96fe0a50febc2bcdec845228b8600dc50`。
- [Completed] [Eval] [Exp] Rematerialized v6.7.2 and obtained a single-use approval with route digest `28cf77aeca2f511401fd621e62790d77b380fcc00e77cf0f1ffe47d8217d6383`, source-manifest digest `0daf43b67d4fcdd077e17cad8fcdacfb152bbfa440feed731487d96500a11784`, and approval SHA-256 `efa23da2f76c9ade2f2cc5ac9be77fe96fe0a50febc2bcdec845228b8600dc50`.
- [完成] [safety] [eval] 单对象 probe 执行 `970` 个 simulator steps；左臂 planner 失败后右臂执行。descent 阶段 `panda_hand` 与非目标 `block-blue-1` 接触，release 阶段 `panda_rightfinger` 与 table 接触；worker stop/reset 后返回 `status=unavailable`，`world_change_completed=false`。
- [Completed] [Safety] [Eval] The single-object probe executed `970` simulator steps; the left planner failed and the right arm executed. During descent `panda_hand` contacted non-target `block-blue-1`, and during release `panda_rightfinger` contacted the table; after controller stop/reset the worker returned `status=unavailable` with `world_change_completed=false`.
- [完成] [policy] [docs] 该失败是 attached-object/environment collision 的真实证据，不是速度调参问题；未放宽 collision gate，未生成成功 route，未进入 benchmark/Gateway/Dora/Action/硬件。
- [Completed] [Policy] [Docs] This failure is real attached-object/environment collision evidence, not speed tuning; no collision gate was relaxed, no successful route was produced, and benchmark/Gateway/Dora/Action/hardware remained disconnected.

### 五维复核 / Five-Dimension Review

- 架构集成、失败路径、权威边界、配置与 provenance、可维护性：均通过。兼容性修复位于 provider contract，probe 仍受 bounded controller、审批和 route digest 约束；碰撞失败保持 fail-closed。
- Architecture integration, failure paths, authority boundaries, configuration/provenance, and maintainability: all pass. The compatibility fix remains in the provider contract, while bounded controller, approval, and route digest gates remain enforced; collision failure stays fail-closed.

### Git 提交 / Git Commit

- Commit: `53858b8`
- Branch: `feature/long-horizon-workflow`
- 时间 / Time: 2026-09-07 00:50 (Asia/Shanghai)

## v6.7.1 (2026-09-07 00:20) - codex

### 预期修改 / Planned Changes

- [计划] [eval] [exp] 使用 v6.7 当前源码重新实体化不可覆盖的 simulation-only route package，绑定 q4 ControllerQualification、双臂 MotionCapability、runtime/calibration/placement/candidate provenance 以及当前 worker/controller source digest；不执行 scene.step、benchmark、Gateway、Dora、Action 或硬件动作。
- [Planned] [Eval] [Exp] Rematerialize an immutable simulation-only route package from the current v6.7 sources, binding the q4 ControllerQualification, both-arm MotionCapability, runtime/calibration/placement/candidate provenance, and current worker/controller source digests; do not execute scene.step, benchmark, Gateway, Dora, Action, or hardware motion.
- [计划] [safety] [test] 对新 package 运行 no-motion route/schema/digest 校验，输出新的 route digest、source-manifest digest 和人工审批请求；在 fresh `I_REVIEWED_AND_APPROVE_SIMULATION_ONLY` 之前保持 fail-closed。
- [Planned] [Safety] [Test] Run no-motion route/schema/digest validation for the fresh package and emit new route/source-manifest digests plus a human-review request; remain fail-closed until a fresh `I_REVIEWED_AND_APPROVE_SIMULATION_ONLY` is provided.

### 预期影响文件 / Planned Files

- `/home/yanxu/robotwin20-runtime/artifacts/paos-route-v6.7.1-*/`
- `changelog/2026-09_part3.md`

### 实际结果 / Actual Results

- [完成] [eval] [exp] 在 `/home/yanxu/robotwin20-runtime/artifacts/paos-route-v6.7.1-20260907T0020Z/` 生成新的不可覆盖 route package；route digest `5dc2abfb6b23c322f12816c86e1762d36dde5da47dfd17b996e7eb112a6a0808`，source-manifest digest `73aed005d1e07c3a11ba50785ee29cb0bb356217bae6c99b55d087f8abcedd2e`，route-request digest `61cae3e2a19f1af4fece88e61ce9835a9a6449332a942660369ef1e643f78a9d`。
- [Completed] [Eval] [Exp] Created a fresh immutable route package under `/home/yanxu/robotwin20-runtime/artifacts/paos-route-v6.7.1-20260907T0020Z/`; route digest `5dc2abfb6b23c322f12816c86e1762d36dde5da47dfd17b996e7eb112a6a0808`, source-manifest digest `73aed005d1e07c3a11ba50785ee29cb0bb356217bae6c99b55d087f8abcedd2e`, and route-request digest `61cae3e2a19f1af4fece88e61ce9835a9a6449332a942660369ef1e643f78a9d`.
- [完成] [safety] [test] 只读 no-motion 校验通过 route schema、几何 digest、scene/calibration/candidate geometry/object transform/placement/attestation/route artifact digest；结果为 `pending_human_review`、`motion_authorized=false`、`robot_control_steps=0`、`simulator_steps=0`。
- [Completed] [Safety] [Test] Read-only no-motion validation passed route schema, geometry digest, scene/calibration/candidate geometry/object transform/placement/attestation/route artifact digests; result is `pending_human_review`, `motion_authorized=false`, `robot_control_steps=0`, and `simulator_steps=0`.
- [完成] [policy] [docs] q4 qualification、双臂 capability、runtime、当前 worker digest `0460e10b...` 和 bounded controller source digest `6a1e9cdc...` 均已绑定；新的人工审批尚未提供，未运行 simulation probe、scene.step、benchmark、Gateway、Dora、Action 或硬件动作。
- [Completed] [Policy] [Docs] The q4 qualification, both-arm capabilities, runtime, current worker digest `0460e10b...`, and bounded-controller source digest `6a1e9cdc...` are bound; fresh human approval is not yet provided, and no simulation probe, scene.step, benchmark, Gateway, Dora, Action, or hardware motion was run.
- [完成] [eval] [test] route/approval/probe 专项回归 `64 passed`，PAOS/core/Skill/adapter 全量回归 `750 passed, 1 skipped`；均未执行仿真动作。
- [Completed] [Eval] [Test] Route/approval/probe focused regression `64 passed`; full PAOS/core/Skill/adapter regression `750 passed, 1 skipped`; no simulation motion was executed.

### 文件变更详情 / File Change Details

- [修改] `docs/forge/ROBOTWIN_CONTROLLER_QUALIFICATION_EXECUTION_PLAN.md:L365-L387`：记录 fresh route、digest、no-motion 校验和五维门禁。
- [Modified] `docs/forge/ROBOTWIN_CONTROLLER_QUALIFICATION_EXECUTION_PLAN.md:L365-L387`: recorded the fresh route, digests, no-motion validation, and five-dimension gate.
- [修改] `docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md:L1334-L1353`：记录 v6.7.1 route provenance 与审批边界。
- [Modified] `docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md:L1334-L1353`: recorded v6.7.1 route provenance and approval boundary.

### 五维验收 / Five-Dimension Review

- 架构集成：通过；本步只物化 provider-owned artifact，不新增 PAOS lifecycle 或执行平面。
- 失败路径：通过；materializer 要求空 output root，并在输入 schema/digest/qualification/capability/source drift 时拒绝；审批前保持 fail-closed。
- 权威边界：通过；q4 qualification 不等于 route 或 benchmark authority，且新 package 仍固定 `motion_authorized=false`。
- 配置与 provenance：通过；route、manifest、worker/controller source、runtime、双臂 capability、GraspGen 和场景输入均可追溯。
- 可维护性：通过；生成、校验、审批、执行分层，旧 route/approval 不复用。
- Architecture integration: pass; this step only materializes provider-owned artifacts and adds no PAOS lifecycle or execution plane.
- Failure paths: pass; the materializer requires an empty output root and rejects schema/digest/qualification/capability/source drift; it remains fail-closed before approval.
- Authority boundaries: pass; q4 qualification is not route or benchmark authority, and the fresh package fixes `motion_authorized=false`.
- Configuration and provenance: pass; route, manifest, worker/controller source, runtime, both-arm capabilities, GraspGen, and scene inputs are traceable.
- Maintainability: pass; generation, validation, approval, and execution remain layered, and old routes/approvals are not reused.

### Git 提交 / Git Commit

- Commit: `c264255`
- Branch: `feature/long-horizon-workflow`
- 时间 / Time: 2026-09-07 00:25 (Asia/Shanghai)

## v6.3.0 (2026-09-06 18:25) - codex

### 预期修改 / Planned Changes

- [计划] [safety] [feat] 根据真实 qualification negative evidence，在 RoboTwin adapter 内新增 capability-driven drive controller：逐命令校验 joint order、position/velocity limits、finite values、状态机和 step acknowledgement；越限、NaN/Inf、fault/stop 状态命令必须在到达 SAPIEN 前拒绝。
- [Planned] [Safety] [Feat] Add a capability-driven drive controller inside the RoboTwin adapter based on real negative qualification evidence: validate joint order, position/velocity limits, finite values, controller state, and step acknowledgement for every command; reject over-limit, NaN/Inf, fault/stop-state commands before they reach SAPIEN.
- [计划] [policy] [refactor] 将 controller source/identity 纳入 MotionCapability 的 provider source binding，生成全新的 capability 和 qualification plan；旧 drive-target approval 不得用于新 controller identity。
- [Planned] [Policy] [Refactor] Bind the controller source/identity into MotionCapability provider provenance and generate a new capability and qualification plan; the old drive-target approval must not be reused for the new controller identity.
- [计划] [eval] [test] 修正 qualification 测试语义，使预期拒绝、丢步检测、stop/error/reset 和 contact fixture 能被独立验证；修正文档中过时/重复的大步顺序，完成专项、全量回归和五维代码审查后停在新人工审批门禁。
- [Planned] [Eval] [Test] Correct qualification-test semantics so expected rejection, dropped-step detection, stop/error/reset, and contact fixtures are independently verifiable; repair stale/duplicated milestone ordering in the docs, run focused/full regression and five-dimension review, then stop at the new human-approval gate.

### 预期影响文件 / Planned Files

- `examples/forge-adapters/robotwin20/runtime/robotwin_controller_qualification_worker.py`
- `examples/forge-adapters/robotwin20/src/robotwin20_adapter/motion_capabilities.py`
- `examples/forge-adapters/robotwin20/scripts/materialize_motion_capability.py`
- `examples/forge-adapters/robotwin20/tests/test_controller_qualification_worker.py`
- `examples/forge-adapters/robotwin20/tests/test_motion_capabilities.py`
- `docs/forge/ROBOTWIN_CONTROLLER_QUALIFICATION_EXECUTION_PLAN.md`
- `docs/forge/REAL_SPEED_LIMITS_ARCHITECTURE.md`

### 实际结果 / Actual Results

- [完成] [safety] [feat] `examples/forge-adapters/robotwin20/runtime/robotwin_capability_controller.py:L1-L195` 新增 provider-owned `CapabilityBoundedDriveController`，从 MotionCapability 读取逐关节 position/velocity bounds，在进入 SAPIEN 前拒绝越限、NaN/Inf、错误状态和未结算 step，并跟踪 accepted/rejected/settled 状态。
- [Completed] [Safety] [Feat] `examples/forge-adapters/robotwin20/runtime/robotwin_capability_controller.py:L1-L195` adds the provider-owned `CapabilityBoundedDriveController`, reads per-joint position/velocity bounds from MotionCapability, rejects over-limit, NaN/Inf, fault-state, and unsettled-step commands before SAPIEN, and tracks accepted/rejected/settled state.
- [完成] [policy] [refactor] `motion_capabilities.py:L91-L105,L441-L575`、`materialize_motion_capability.py:L20-L45`、`verify_motion_capability.py:L18-L55` 支持 provider controller identity 和外部 adapter controller source digest；新 controller 使用 `paos-robotwin-capability-bounded-drive-target`，旧 `robotwin-sapien-drive-target` capability/approval 不复用。
- [Completed] [Policy] [Refactor] `motion_capabilities.py:L91-L105,L441-L575`, `materialize_motion_capability.py:L20-L45`, and `verify_motion_capability.py:L18-L55` support provider controller identity and external adapter-controller source digests; the new controller uses `paos-robotwin-capability-bounded-drive-target`, and the old `robotwin-sapien-drive-target` capability/approval is not reused.
- [完成] [safety] [fix] `robotwin_controller_qualification_worker.py:L75-L88,L350-L540,L628-L829` 将 over-limit、stop、dropped-step、error-path 改为按“预期拒绝/故障”语义验收，并把 SAPIEN runtime 接入 capability-bounded controller；Franka DOF 数量显式校验为 7 arm + 2 gripper。
- [Completed] [Safety] [Fix] `robotwin_controller_qualification_worker.py:L75-L88,L350-L540,L628-L829` updates over-limit, stop, dropped-step, and error-path tests to validate expected rejection/fault semantics and connects the SAPIEN runtime to the capability-bounded controller; Franka DOF count is explicitly checked as 7 arm plus 2 gripper joints.
- [完成] [eval] [test] 新增 `test_robotwin_capability_controller.py:L1-L86`；专项 `18 passed`（controller、worker、motion capability），全量回归待本大步完成后重跑。
- [Completed] [Eval] [Test] Added `test_robotwin_capability_controller.py:L1-L86`; focused suite `18 passed` (controller, worker, motion capability), with the full regression to be rerun at milestone closeout.
- [完成] [eval] [exp] 生成新 capability/plan 包 `/home/yanxu/robotwin20-runtime/artifacts/paos-controller-qualification-plan-20260906T1845Z/`；no-motion validation `validated_no_motion_plan`，plan digest `4f0cf0ddf7e20faad11729edd30220e1421689197bf869a0ef0f541ecaca4c9c`，当前为 `pending_human_review`，未执行 qualification motion。
- [Completed] [Eval] [Exp] Generated the new capability/plan package at `/home/yanxu/robotwin20-runtime/artifacts/paos-controller-qualification-plan-20260906T1845Z/`; no-motion validation is `validated_no_motion_plan`, plan digest `4f0cf0ddf7e20faad11729edd30220e1421689197bf869a0ef0f541ecaca4c9c`, and it remains `pending_human_review` with no qualification motion executed.

### 五维验收 / Five-Dimension Review

- 架构集成：通过；controller 位于 RoboTwin provider adapter，通过 runtime port 接入 qualification worker，不创建 PAOS 第二套执行生命周期。
- 失败路径：通过；越限、NaN/Inf、stop、fault、未结算 step、provider write failure、DOF/joint-order drift 均 fail-closed；新 plan 在新 identity 下重新生成。
- 权威边界：通过；controller 只负责 provider command admission，不授予 PAOS motion authority；新 plan 尚未获人工审批，旧 approval 不可复用。
- 配置与 provenance：通过；limits 来自 capability artifact，controller source digest 和 identity 进入 capability/plan 绑定，没有硬编码速度常量。
- 可维护性：通过；控制器、qualification worker、materializer、validator 和测试分层，支持未来替换真实 SDK/controller provider。

### 文件变更详情 / Detailed Changes

- [新增] `examples/forge-adapters/robotwin20/runtime/robotwin_capability_controller.py:L1-L206`：新增 capability-driven provider controller。
- [修改] `examples/forge-adapters/robotwin20/runtime/robotwin_controller_qualification_worker.py:L75-L88,L350-L500,L570-L700`：接入 controller 并修正预期拒绝测试语义。
- [修改] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/motion_capabilities.py:L91-L105,L441-L575`、`examples/forge-adapters/robotwin20/scripts/materialize_motion_capability.py:L20-L45`、`verify_motion_capability.py:L18-L55`：支持新 controller identity/source binding。
- [新增] `examples/forge-adapters/robotwin20/tests/test_robotwin_capability_controller.py:L1-L86`：新增 controller 状态和失败路径测试。
- [修改] `docs/forge/ROBOTWIN_CONTROLLER_QUALIFICATION_EXECUTION_PLAN.md:L102-L220`、`docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md:L1317-L1332`、`docs/forge/REAL_SPEED_LIMITS_ARCHITECTURE.md:L338-L351`：记录新 provider controller、plan digest 和审批门禁。

### Git 提交 / Git Commit

- Commit: `b104e9e`
- Branch: `feature/long-horizon-workflow`
- 时间: 2026-09-06 18:55 (Asia/Shanghai)

## v6.3.1 (2026-09-06 19:00) - codex

### 变更摘要 / Change Summary

- [完成] [docs] 修正 qualification 执行计划的里程碑顺序和过时的 SAPIEN unavailable 描述，补充新 bounded-controller q3 plan 为 pending human review。
- [Completed] [Docs] Corrected qualification milestone ordering and stale SAPIEN-unavailable wording, and recorded the new bounded-controller q3 plan as pending human review.

## v6.4.0 (2026-09-06 20:30) - codex

### 预期修改 / Planned Changes

- [计划] [safety] [feat] 新增 evidence-result approval CLI，将独立 validator 的 `validated_pass` 转换为最终不可变 `ControllerQualification(approved_pass)`；结果审批使用独立短语，不能由 plan approval 自动推导。
- [Planned] [Safety] [Feat] Add an evidence-result approval CLI that converts an independent validator `validated_pass` into an immutable final `ControllerQualification(approved_pass)`; result approval uses a separate phrase and cannot be inferred from plan approval.
- [计划] [policy] [feat] 严格绑定 plan/evidence/validation digest、provider identity、全部测试通过状态和 controller_enforced=true；最终记录继续保持 benchmark/hardware/PAOS motion unauthorized。
- [Planned] [Policy] [Feat] Strictly bind plan/evidence/validation digests, provider identity, all-test pass status, and `controller_enforced=true`; the final record continues to keep benchmark/hardware/PAOS motion unauthorized.
- [计划] [eval] [test] 增加错误短语、失败 evidence、validated_failure、digest drift、identity drift 和重复输出拒绝测试；运行全量回归并按五个维度复审。
- [Planned] [Eval] [Test] Add rejection tests for wrong phrase, failed evidence, `validated_failure`, digest drift, identity drift, and divergent output; run full regression and five-dimension review.

### 预期影响文件 / Planned Files

- `examples/forge-adapters/robotwin20/scripts/approve_controller_qualification_evidence.py`
- `examples/forge-adapters/robotwin20/tests/test_approve_controller_qualification_evidence.py`
- `docs/forge/ROBOTWIN_CONTROLLER_QUALIFICATION_EXECUTION_PLAN.md`
- `docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md`

### 实际结果 / Actual Results

- [完成] [safety] [feat] `examples/forge-adapters/robotwin20/runtime/robotwin_controller_qualification_worker.py:L75-L88,L350-L540,L628-L875` 修正双臂执行语义：qualification runtime 只要求有 pending command 的 arm 完成 step acknowledgement，空闲 arm 保持当前 drive target；nominal/contact/reset 改为双臂命令先提交、共享 step，stop/error/drop 按预期拒绝或 fault 验收。
- [Completed] [Safety] [Feat] `examples/forge-adapters/robotwin20/runtime/robotwin_controller_qualification_worker.py:L75-L88,L350-L540,L628-L875` corrects dual-arm execution semantics: only arms with pending commands require step acknowledgement, idle arms hold their current drive target, nominal/contact/reset submit both commands before a shared step, and stop/error/drop validate expected rejection or fault.
- [完成] [safety] [feat] `examples/forge-adapters/robotwin20/runtime/robotwin_capability_controller.py:L1-L200` 增加 `has_pending_step` provider 状态查询；`examples/forge-adapters/robotwin20/tests/test_robotwin_capability_controller.py:L1-L96` 增加单臂 command/空闲臂 step 回归。
- [Completed] [Safety] [Feat] `examples/forge-adapters/robotwin20/runtime/robotwin_capability_controller.py:L1-L200` adds the `has_pending_step` provider state query; `examples/forge-adapters/robotwin20/tests/test_robotwin_capability_controller.py:L1-L96` adds a single-arm command/idle-arm step regression.
- [完成] [policy] [feat] `examples/forge-adapters/robotwin20/scripts/approve_controller_qualification_evidence.py:L1-L131` 新增独立结果审批 CLI，只有 `validated_pass`、`controller_enforced=true`、全部 digest/identity 绑定和新的结果审批短语才能生成最终 `approved_pass`；继续保持 `motion_authorized=false`。
- [Completed] [Policy] [Feat] `examples/forge-adapters/robotwin20/scripts/approve_controller_qualification_evidence.py:L1-L131` adds an independent result-approval CLI; only `validated_pass`, `controller_enforced=true`, complete digest/identity bindings, and a new result-approval phrase can produce final `approved_pass`, while `motion_authorized=false` remains fixed.
- [完成] [eval] [test] `examples/forge-adapters/robotwin20/tests/test_approve_controller_qualification_evidence.py:L1-L97` 覆盖最终审批成功、错误短语和无输出失败；q3 实际 qualification 为 `passed`，独立验证为 `validated_pass`，证据 digest `deda609a888526a6c2808f6bc0137407fd3a97f9f5d8f983c21bce8c9e93e4fd`，validation digest `0f4d9dfdb63d362351bb8eb7091eee0d85c950bef12816bb4363fc50613efac3`。
- [Completed] [Eval] [Test] `examples/forge-adapters/robotwin20/tests/test_approve_controller_qualification_evidence.py:L1-L97` covers final approval success, wrong phrase, and no-output rejection; q3 qualification was `passed`, independently `validated_pass`, with evidence digest `deda609a888526a6c2808f6bc0137407fd3a97f9f5d8f983c21bce8c9e93e4fd` and validation digest `0f4d9dfdb63d362351bb8eb7091eee0d85c950bef12816bb4363fc50613efac3`.
- [完成] [eval] [exp] q4 单臂调度修正版已生成新 capability/plan：`/home/yanxu/robotwin20-runtime/artifacts/paos-controller-qualification-plan-20260906T2145Z/`，no-motion validation `validated_no_motion_plan`，plan digest `99233d0ce8c885936277894e4265cc255c3ea561bc6edfec91239234e13dea98`，当前等待新的人工审批，未执行 q4 motion。
- [Completed] [Eval] [Exp] The q4 single-arm scheduling revision generated a new capability/plan at `/home/yanxu/robotwin20-runtime/artifacts/paos-controller-qualification-plan-20260906T2145Z/`; no-motion validation is `validated_no_motion_plan`, plan digest `99233d0ce8c885936277894e4265cc255c3ea561bc6edfec91239234e13dea98`, pending fresh human approval with no q4 motion executed.

### 五维验收 / Five-Dimension Review

- 架构集成：通过；单臂/双臂调度语义留在 RoboTwin provider runtime，不把 arm scheduling 逻辑写入 PAOS Core 或 Gateway。
- 失败路径：通过；共享 step 的错误被定位并修复；stop、fault、dropped-step、越限、digest drift 和 stale q3 approval 均保持 fail-closed。
- 权威边界：通过；q3 `validated_pass` 仅属于旧 controller source digest；q4 source 改变后重新生成 capability/plan，旧 approval/evidence 不可覆盖新代码。
- 配置与 provenance：通过；controller source digest、runtime identity、capability 和 plan 全部重新绑定；没有新增硬编码速度事实。
- 可维护性：通过；最终 evidence approval 与计划 approval 分离，单臂 idle semantics 通过 `has_pending_step` provider port 表达，可测试且可替换。

### 文件变更详情 / Detailed Changes

- [新增] `examples/forge-adapters/robotwin20/scripts/approve_controller_qualification_evidence.py:L1-L131`：新增最终结果人工审批与 `ControllerQualification` 生成。
- [新增] `examples/forge-adapters/robotwin20/tests/test_approve_controller_qualification_evidence.py:L1-L97`：新增最终结果审批契约测试。
- [修改] `examples/forge-adapters/robotwin20/runtime/robotwin_capability_controller.py:L1-L200`、`robotwin_controller_qualification_worker.py:L75-L88,L350-L540,L628-L875`：支持单臂 pending-step 语义并修正双臂 qualification 编排。
- [修改] `docs/forge/ROBOTWIN_CONTROLLER_QUALIFICATION_EXECUTION_PLAN.md:L102-L270`、`docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md:L1317-L1344`、`docs/forge/REAL_SPEED_LIMITS_ARCHITECTURE.md:L338-L360`：记录 q3 通过、q4 重绑和结果审批门禁。

### Git 提交 / Git Commit

- Commit: `0bb37ea`
- Branch: `feature/long-horizon-workflow`
- 时间: 2026-09-06 21:25 (Asia/Shanghai)

## v6.4.1 (2026-09-06 21:30) - codex

### 变更摘要 / Change Summary

- [完成] [docs] 记录 q3 qualification 实际通过、q4 重绑和最终结果审批实现的提交信息。
- [Completed] [Docs] Recorded the commit for the q3 qualification pass, q4 rebinding, and final result-approval implementation.

## v6.5.0 (2026-09-06 22:55) - codex

### 预期修改 / Planned Changes

- [计划] [eval] [exp] 执行新 q4 单臂调度 qualification plan，使用独立 artifact root 保存八项测试 evidence 和独立 validation；不启动 benchmark、route、Gateway、Dora、Action 或硬件。
- [Planned] [Eval] [Exp] Execute the new q4 single-arm scheduling qualification plan with an isolated artifact root for all eight test evidence and independent validation; do not start benchmark, route, Gateway, Dora, Action, or hardware.
- [计划] [docs] [review] 更新执行计划、诊断和速度架构文档，记录 q4 通过结果、证据摘要、五维验收和最终结果审批门禁。
- [Planned] [Docs] [Review] Update the execution plan, diagnosis, and speed architecture docs with the q4 pass result, evidence summary, five-dimension acceptance, and final result-approval gate.

### 实际结果 / Actual Results

- [完成] [eval] [exp] q4 plan 获得新的隔离 simulation-only approval，approval digest `5796e83200818f27ce5c1b5c002bb4016356bdc8a610e96fded87241316dbcf9`；在全新 artifact root `/home/yanxu/robotwin20-runtime/artifacts/qualification-run-20260906T2245Z/` 运行，八项测试全部通过。
- [Completed] [Eval] [Exp] The q4 plan received a fresh isolated simulation-only approval with approval digest `5796e83200818f27ce5c1b5c002bb4016356bdc8a610e96fded87241316dbcf9`; it ran in the fresh artifact root `/home/yanxu/robotwin20-runtime/artifacts/qualification-run-20260906T2245Z/` with all eight tests passing.
- [完成] [eval] [feat] q4 evidence digest `e8fd8a72003f1df9a5dc3418c36f666e1b0e179ec7c17e3601836887e03841f8`，独立 validation digest `da0a02604e59c92a60abf219b1778f70768ec53e052b6b363710671e428bc5aa`，状态分别为 `passed` / `validated_pass`；仍保持 `motion_authorized=false`。
- [Completed] [Eval] [Feat] q4 evidence digest is `e8fd8a72003f1df9a5dc3418c36f666e1b0e179ec7c17e3601836887e03841f8`, independent validation digest is `da0a02604e59c92a60abf219b1778f70768ec53e052b6b363710671e428bc5aa`, statuses are `passed` / `validated_pass`, and `motion_authorized=false` remains fixed.
- [完成] [docs] [review] 更新执行计划、诊断和速度架构文档，明确 q4 是当前 controller source/单臂 idle-arm semantics 的有效 qualification；最终 `ControllerQualification(approved_pass)` 仍需独立结果审批。
- [Completed] [Docs] [Review] Updated execution, diagnosis, and speed-architecture docs to state that q4 is the valid qualification for the current controller source and idle-arm semantics; final `ControllerQualification(approved_pass)` still requires independent result approval.

### 五维验收 / Five-Dimension Review

- 架构集成：通过；q4 只在 RoboTwin provider adapter 内运行，单臂/双臂 step 语义没有进入 PAOS Core 或 Gateway。
- 失败路径：通过；八项 qualification 测试均执行并留下 trace，失败仍会生成不可用/失败证据，不会自动晋升。
- 权威边界：通过；q4 `validated_pass` 不是 benchmark、route、Gateway 或硬件授权，`motion_authorized=false`。
- 配置与 provenance：通过；q4 plan、approval、capability、controller source、runtime identity 和 evidence digest 完整绑定。
- 可维护性：通过；新增最终结果审批 CLI，计划审批与结果审批分离；空闲 arm 语义由 provider port 明确表达。

### 文件变更详情 / Detailed Changes

- [修改] `docs/forge/ROBOTWIN_CONTROLLER_QUALIFICATION_EXECUTION_PLAN.md:L183-L230`、`docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md:L1330-L1348`、`docs/forge/REAL_SPEED_LIMITS_ARCHITECTURE.md:L354-L365`：记录 q4 运行 evidence、validation 和最终审批门禁。

### Git 提交 / Git Commit

- Commit: `2356fc1`
- Branch: `feature/long-horizon-workflow`
- 时间: 2026-09-06 23:20 (Asia/Shanghai)

## v6.5.1 (2026-09-06 23:25) - codex

### 变更摘要 / Change Summary

- [完成] [docs] 记录 q4 qualification 实际通过与独立验证提交信息。
- [Completed] [Docs] Recorded the commit for the q4 qualification pass and independent validation.

## v6.6.0 (2026-09-06 23:45) - codex

### 预期修改 / Planned Changes

- [计划] [eval] [feat] 使用独立结果审批短语生成 q4 最终 `ControllerQualification(approved_pass)`，绑定 plan/approval/evidence/validation digest 和 provider identity。
- [Planned] [Eval] [Feat] Generate the final q4 `ControllerQualification(approved_pass)` using the independent result-approval phrase, binding plan/approval/evidence/validation digests and provider identity.
- [计划] [docs] [review] 更新执行计划、诊断和速度架构文档，记录最终 qualification artifact、authority flags、五维验收和 route 重绑定前门禁。
- [Planned] [Docs] [Review] Update execution, diagnosis, and speed architecture docs with the final qualification artifact, authority flags, five-dimension acceptance, and the gate before route rebinding.

### v6.6.0 实际结果 / Actual Results

- [完成] [eval] [feat] 使用 `I_REVIEWED_AND_APPROVE_CONTROLLER_QUALIFICATION_EVIDENCE` 生成 q4 最终 `ControllerQualification(approved_pass)`，digest `50ac70982b1dcdeb67ae65cfbd0e3ff3fcc31ebca5b7dd99baa7dcb03f3dc8e6`，并通过 `validate_controller_qualification.py --kind qualification`。
- [Completed] [Eval] [Feat] Used `I_REVIEWED_AND_APPROVE_CONTROLLER_QUALIFICATION_EVIDENCE` to generate the final q4 `ControllerQualification(approved_pass)`, digest `50ac70982b1dcdeb67ae65cfbd0e3ff3fcc31ebca5b7dd99baa7dcb03f3dc8e6`, and passed `validate_controller_qualification.py --kind qualification`.
- [完成] [policy] [fix] 最终记录固定 `motion_authorized=false`、`benchmark_motion_authorized=false`、`hardware_motion_authorized=false`；仅关闭 controller qualification 门禁，route/Gateway/benchmark 门禁保持开启。
- [Completed] [Policy] [Fix] The final record fixes `motion_authorized=false`, `benchmark_motion_authorized=false`, and `hardware_motion_authorized=false`; only the controller-qualification gate is closed while route/Gateway/benchmark gates remain open.

### 五维验收 / Five-Dimension Review

- 架构集成：通过；最终 qualification 是 RoboTwin provider artifact，不创建 PAOS 第二生命周期或执行事实源。
- 失败路径：通过；最终审批拒绝错误短语、失败 evidence、非独立 validation、digest/identity drift 和重复输出。
- 权威边界：通过；`approved_pass` 不等于 benchmark 或硬件授权，不改变 Gateway/Verifier 权威。
- 配置与 provenance：通过；最终记录完整绑定 plan、approval、evidence、validation、controller identity 和 digest。
- 可维护性：通过；plan approval、evidence validation 和 result approval 三层分离，便于后续 provider 替换。

### 文件变更详情 / Detailed Changes

- [修改] `docs/forge/ROBOTWIN_CONTROLLER_QUALIFICATION_EXECUTION_PLAN.md:L207-L245`、`docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md:L1340-L1355`、`docs/forge/REAL_SPEED_LIMITS_ARCHITECTURE.md:L360-L372`：记录最终 qualification artifact 和大步 C 前置门禁。

## v6.7.0 (2026-09-07 00:05) - codex

### 预期修改 / Planned Changes

- [计划] [policy] [feat] 将最终 `ControllerQualification(approved_pass)` 作为不可变 provider-owned binding 接入 route v6、source manifest v4、review request v3 和 simulation approval v4；旧 route/approval 不得复用。
- [Planned] [Policy] [Feat] Bind the final provider-owned `ControllerQualification(approved_pass)` immutably into route v6, source manifest v4, review request v3, and simulation approval v4; legacy routes and approvals must not be reused.
- [计划] [eval] [fix] 在 route materializer 和 simulation worker 中交叉验证 qualification、plan、evidence、validation、双臂 MotionCapability 与 runtime/controller identity；任何缺失、失败或 digest drift 均在 world change 前拒绝。
- [Planned] [Eval] [Fix] Cross-validate qualification, plan, evidence, validation, dual-arm MotionCapability, and runtime/controller identity in the route materializer and simulation worker; reject missing, failed, or digest-drifted inputs before world change.
- [计划] [eval] [test] 增加 qualification binding 的正向、篡改、失败状态、provider identity 和 capability drift 测试，运行专项与全量回归并完成五维验收；随后只生成新的 no-motion route/review package，不执行 benchmark motion。
- [Planned] [Eval] [Test] Add positive, tamper, failed-status, provider-identity, and capability-drift tests for qualification binding, run focused and full regression with five-dimension review, then generate only a fresh no-motion route/review package without benchmark motion.

### 预期影响文件 / Planned Files

- `examples/forge-adapters/robotwin20/src/robotwin20_adapter/controller_qualification.py`
- `examples/forge-adapters/robotwin20/src/robotwin20_adapter/route_readiness.py`
- `examples/forge-adapters/robotwin20/scripts/materialize_complete_route.py`
- `examples/forge-adapters/robotwin20/scripts/approve_simulation_probe.py`
- `examples/forge-adapters/robotwin20/runtime/robotwin_simulation_probe_worker.py`
- `examples/forge-adapters/robotwin20/tests/`
- `docs/forge/ROBOTWIN_CONTROLLER_QUALIFICATION_EXECUTION_PLAN.md`

### 实际完成 / Completed

- [完成] [policy] `examples/forge-adapters/robotwin20/runtime/robotwin_simulation_probe_worker.py:L620-L704,L820-L910,L1420-L1510`：将当前导入的 bounded controller 源码摘要和版本绑定到 MotionCapability，并在构造 controller、每个 simulator step、夹爪 arm-hold 前重新校验；native controller、源码漂移、版本漂移、越界夹爪命令均 fail-closed。
- [Completed] [Policy] `examples/forge-adapters/robotwin20/runtime/robotwin_simulation_probe_worker.py:L620-L704,L820-L910,L1420-L1510`: bind the imported bounded-controller source digest/version to MotionCapability and revalidate before controller construction, every simulator step, and gripper arm-hold; native controllers, source/version drift, and out-of-range gripper commands fail closed.
- [完成] [eval] [test] `examples/forge-adapters/robotwin20/tests/test_simulation_probe.py:L390-L640,L690-L800`：新增审批 worker 摘要漂移、native/qualified controller source binding、版本漂移、bounded trajectory/gripper/reset 负向回归。
- [Completed] [Eval] [Test] `examples/forge-adapters/robotwin20/tests/test_simulation_probe.py:L390-L640,L690-L800`: added approval-worker digest drift, native/qualified controller source binding, version drift, bounded trajectory/gripper/reset regressions.
- [完成] [docs] `docs/forge/ROBOTWIN_CONTROLLER_QUALIFICATION_EXECUTION_PLAN.md`、`docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md`、`examples/forge-adapters/robotwin20/README.md`：记录执行路径 Blocker、修复边界和五维验收结论。
- [Completed] [Docs] Updated the controller qualification execution plan, PAOS diagnosis, and RoboTwin README with the execution-path blocker, fix boundary, and five-dimension acceptance.

### 验证 / Validation

- Focused probe/qualification suite: `67 passed`。
- Full core/Skill/adapter regression: `750 passed, 1 skipped`。
- Ruff、compileall、`git diff --check`：通过。
- No benchmark route, Gateway, Dora, Action, hardware, or new simulation motion was executed; existing v6 route artifacts remain stale and diagnostic-only until a fresh materialization and human simulation-only approval.

## v6.2.0 (2026-09-06 17:10) - codex

### 预期修改 / Planned Changes

- [计划] [safety] [feat] 在已批准的隔离 qualification scope 内实现 RoboTwin/SAPIEN provider worker；只运行空场景 controller qualification，不接入 benchmark、Gateway、Dora、Action 或硬件。
- [Planned] [Safety] [Feat] Implement the RoboTwin/SAPIEN provider worker within the approved isolated qualification scope; run controller qualification only in an empty scene without benchmark, Gateway, Dora, Action, or hardware integration.
- [计划] [policy] [feat] 在每次 `scene.step()` 前重新校验 approval、plan/source-manifest、双臂 MotionCapability、runtime/controller identity、命令族和 stop/reset policy；执行证据固定为 provider-owned artifact，不能授予 PAOS motion authority。
- [Planned] [Policy] [Feat] Revalidate approval, plan/source-manifest, both MotionCapability artifacts, runtime/controller identity, command families, and stop/reset policy before every `scene.step()`; keep execution evidence provider-owned and non-authoritative for PAOS motion.
- [计划] [eval] [test] 增加 qualification worker 的 dry-run、批准绑定、identity/digest drift、stop/timeout、NaN/Inf、证据缺失和失败收敛测试；完成后执行五维验收。
- [Planned] [Eval] [Test] Add qualification-worker tests for dry-run, approval binding, identity/digest drift, stop/timeout, NaN/Inf, missing evidence, and failure reconciliation, followed by five-dimension acceptance.

### 预期影响文件 / Planned Files

- `examples/forge-adapters/robotwin20/runtime/robotwin_controller_qualification_worker.py`
- `examples/forge-adapters/robotwin20/scripts/run_controller_qualification.py`
- `examples/forge-adapters/robotwin20/scripts/validate_controller_qualification_evidence.py`
- `examples/forge-adapters/robotwin20/tests/test_controller_qualification_worker.py`
- `docs/forge/ROBOTWIN_CONTROLLER_QUALIFICATION_EXECUTION_PLAN.md`
- `docs/forge/REAL_SPEED_LIMITS_ARCHITECTURE.md`
- `docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md`

### 实际结果 / Actual Results

- [完成] [safety] [feat] `examples/forge-adapters/robotwin20/runtime/robotwin_controller_qualification_worker.py:L1-L684` 新增 provider-owned qualification runtime port、审批/digest 每步门禁、空双 Panda SAPIEN runtime、八项测试执行、commanded/observed joint/TCP/contact/step/status/stop-error-reset trace，以及越限命令必须由 controller 明确 `rejected`/`limited`/`fault` 才能通过的约束。
- [Completed] [Safety] [Feat] `examples/forge-adapters/robotwin20/runtime/robotwin_controller_qualification_worker.py:L1-L684` adds the provider-owned qualification runtime port, approval/digest gates before every step, an empty two-Panda SAPIEN runtime, the eight-test execution matrix, commanded/observed joint/TCP/contact/step/status/stop-error-reset traces, and requires explicit controller `rejected`/`limited`/`fault` status for an over-limit command to pass.
- [完成] [eval] [feat] `examples/forge-adapters/robotwin20/scripts/run_controller_qualification.py:L1-L115` 在 provider 无法启动时持久化完整 `unavailable` evidence；`validate_controller_qualification_evidence.py:L1-L93` 独立验证模型、绑定、trace digest、双臂信号、有限值和失败原因，并保持 `motion_authorized=false`。
- [Completed] [Eval] [Feat] `examples/forge-adapters/robotwin20/scripts/run_controller_qualification.py:L1-L115` persists complete `unavailable` evidence when the provider cannot start; `validate_controller_qualification_evidence.py:L1-L93` independently validates the model, bindings, trace digests, both-arm signals, finite values, and failure reasons while keeping `motion_authorized=false`.
- [完成] [eval] [test] `examples/forge-adapters/robotwin20/tests/test_controller_qualification_worker.py:L1-L150` 覆盖 digest drift、越限误报、fixture unavailable、缺失信号和失败原因；全量回归 `733 passed, 1 skipped`，Ruff、compileall、`git diff --check` 通过。
- [Completed] [Eval] [Test] `examples/forge-adapters/robotwin20/tests/test_controller_qualification_worker.py:L1-L150` covers digest drift, over-limit false-positive prevention, unavailable fixtures, missing signals, and failure reasons; full regression `733 passed, 1 skipped`, with Ruff, compileall, and `git diff --check` passing.
- [完成] [eval] [exp] 审批 `I_REVIEWED_AND_APPROVE_CONTROLLER_QUALIFICATION_SIMULATION_ONLY` 生成 approval digest `6b56a2e5be8adfc47917490c16ba512066de99a51172f7686c9ddaaf998be38b`。安装 `sapien==3.0.0b1` 后运行 `/home/yanxu/robotwin20-runtime/artifacts/qualification-run-20260906T1630Z-v6/`：nominal/reset/stop 通过，over-limit 未被 controller 拒绝，contact-load、dropped-step、error-path unavailable；总结果 `unavailable`，独立 validator `validated_failure`。
- [Completed] [Eval] [Exp] Approval `I_REVIEWED_AND_APPROVE_CONTROLLER_QUALIFICATION_SIMULATION_ONLY` produced approval digest `6b56a2e5be8adfc47917490c16ba512066de99a51172f7686c9ddaaf998be38b`. After installing `sapien==3.0.0b1`, `/home/yanxu/robotwin20-runtime/artifacts/qualification-run-20260906T1630Z-v6/` ran with nominal/reset/stop passing, over-limit not rejected by the controller, and contact-load/dropped-step/error-path unavailable; overall `unavailable`, independently `validated_failure`.

### 五维验收 / Five-Dimension Review

- 架构集成：通过；实现只在 RoboTwin adapter，runtime 通过 provider port 注入，不创建 PAOS lifecycle、SQLite 或 Gateway 执行面。
- 失败路径：通过；真实运行暴露了 SAPIEN drive-target 不提供越限拒绝、contact fixture 和 error/drop instrumentation 缺失，均保留为 failed/unavailable evidence，没有调参绕过。
- 权威边界：通过；qualification approval 只允许隔离场景，evidence/validation 始终 `motion_authorized=false`，不能授权 benchmark、route、Gateway、Dora、Action 或硬件。
- 配置与 provenance：通过；限制来自 capability artifact，plan/approval/source/capability 文件摘要每步复核；未添加硬编码速度上限。
- 可维护性：通过；provider runtime、worker、CLI、独立 validator 与测试分层；SAPIEN 依赖缺失时生成可审计 unavailable 产物。

### 文件变更详情 / Detailed Changes

- [新增] `examples/forge-adapters/robotwin20/runtime/robotwin_controller_qualification_worker.py:L1-L684`：新增隔离 qualification provider port、SAPIEN runtime、八项测试、trace 和 fail-closed 处理。
- [新增] `examples/forge-adapters/robotwin20/scripts/run_controller_qualification.py:L1-L115`：新增审批绑定执行 CLI 和 unavailable evidence 持久化。
- [新增] `examples/forge-adapters/robotwin20/scripts/validate_controller_qualification_evidence.py:L1-L93`：新增独立证据验证 CLI。
- [新增] `examples/forge-adapters/robotwin20/tests/test_controller_qualification_worker.py:L1-L150`：新增 worker/validator 契约测试。
- [修改] `docs/forge/ROBOTWIN_CONTROLLER_QUALIFICATION_EXECUTION_PLAN.md:L80-L151`、`docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md:L1301-L1315`、`docs/forge/REAL_SPEED_LIMITS_ARCHITECTURE.md:L338-L343`、`examples/forge-adapters/robotwin20/README.md:L458-L484`：记录执行边界、实际 unavailable 结果和下一步门禁。

### Git 提交 / Git Commit

- Commit: `ec5afce`
- Branch: `feature/long-horizon-workflow`
- 时间: 2026-09-06 18:05 (Asia/Shanghai)

## v6.2.1 (2026-09-06 18:10) - codex

### 变更摘要 / Change Summary

- [完成] [docs] 补充 v6.2.0 实际提交、provider qualification 运行结果和五维验收索引。
- [Completed] [Docs] Added the v6.2.0 implementation commit, provider qualification run result, and five-dimension acceptance index.

## v6.1.0 (2026-09-06 16:03) - codex

### 实际结果 / Actual Results

- [完成] [safety] [feat] 新增 RoboTwin provider-owned controller qualification contracts：plan、source manifest、review request、evidence、独立 validation 和 final qualification；所有记录严格绑定双臂、provider/runtime identity、command family、证据范围和 digest，禁止把 planner constraint 或测量值升级为 controller authority。
- [Completed] [Safety] [Feat] Added RoboTwin provider-owned controller qualification contracts for plan, source manifest, review request, evidence, independent validation, and final qualification; all records bind both arms, provider/runtime identity, command families, evidence scope, and digests, and cannot promote planner constraints or measurements to controller authority.
- [完成] [safety] [feat] 增加无动作 materializer、跨 artifact verifier 和人工审批 CLI；staging 目录原子发布，错误短语、identity/digest drift、不完整矩阵均拒绝，benchmark/hardware/PAOS motion 始终保持未授权。
- [Completed] [Safety] [Feat] Added a no-motion materializer, cross-artifact verifier, and human-approval CLI; staging is atomically published, wrong phrases, identity/digest drift, and incomplete matrices are rejected, and benchmark/hardware/PAOS motion remains unauthorized.
- [完成] [eval] [test] 专项 `10 passed`；组合回归 `728 passed, 1 skipped`；Ruff、compileall、`git diff --check` 通过。真实包 `/home/yanxu/robotwin20-runtime/artifacts/paos-controller-qualification-plan-20260906T1630Z/` 的 no-motion validation digest 为 `2d977c2ec9ae179fa7ad9ae37b82367e2ddf84b9d3daded96ea2841f69af497e`。
- [Completed] [Eval] [Test] Focused suite `10 passed`; combined regression `728 passed, 1 skipped`; Ruff, compileall, and `git diff --check` passed. The real package at `/home/yanxu/robotwin20-runtime/artifacts/paos-controller-qualification-plan-20260906T1630Z/` has no-motion validation digest `2d977c2ec9ae179fa7ad9ae37b82367e2ddf84b9d3daded96ea2841f69af497e`.

### 五维验收 / Five-Dimension Review

- 架构集成：通过；qualification remains in the RoboTwin adapter and does not create a second PAOS lifecycle, store, or execution plane.
- 失败路径：通过；missing/duplicate tests, digest mismatch, provider identity drift, wrong approval phrase, and incomplete bindings fail closed.
- 权威边界：通过；qualification approval is scoped only to isolated qualification motion; benchmark, hardware, and PAOS motion remain unauthorized.
- 配置与 provenance：通过；双臂 capability/validation、manifest、plan 和 review request cross-bind all provider identities and digests without hard-coded speed facts.
- 可维护性：通过；contracts, materializer, verifier, approval CLI, and tests are separated for provider replacement.

### 文件变更详情 / Detailed Changes

- [新增] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/controller_qualification.py:L1-L700`：新增严格 qualification contract 和跨 artifact validator。
- [Added] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/controller_qualification.py:L1-L700`: added strict qualification contracts and cross-artifact validation.
- [新增] `examples/forge-adapters/robotwin20/scripts/materialize_controller_qualification_plan.py:L1-L230`、`verify_controller_qualification_plan.py:L1-L145`、`approve_controller_qualification_plan.py:L1-L135`：新增 no-motion package 生成、验证和隔离审批流程。
- [Added] `examples/forge-adapters/robotwin20/scripts/materialize_controller_qualification_plan.py:L1-L230`, `verify_controller_qualification_plan.py:L1-L145`, and `approve_controller_qualification_plan.py:L1-L135`: added no-motion package materialization, verification, and scoped approval flow.
- [新增] `examples/forge-adapters/robotwin20/tests/test_controller_qualification.py:L1-L430`：覆盖 schema、矩阵、digest、identity drift、权限隔离和 CLI 失败路径。
- [Added] `examples/forge-adapters/robotwin20/tests/test_controller_qualification.py:L1-L430`: covered schema, matrix, digest, identity drift, authority isolation, and CLI failure paths.
- [新增] `docs/forge/ROBOTWIN_CONTROLLER_QUALIFICATION_EXECUTION_PLAN.md:L1-L150`：记录阶段门禁、实际 artifact 和大步 A 五维验收。
- [Added] `docs/forge/ROBOTWIN_CONTROLLER_QUALIFICATION_EXECUTION_PLAN.md:L1-L150`: documented phase gates, actual artifacts, and the five-dimension review for milestone A.

### Git 提交 / Git Commit

- Commit: `17bef6f`
- Branch: `feature/long-horizon-workflow`

## v6.1.0 (2026-09-06 16:03) - codex

### 预期修改 / Planned Changes

- [计划] [safety] [feat] 固化 RoboTwin/SAPIEN controller qualification 的分阶段执行路线；先实现 provider-owned qualification plan/evidence contract 与无动作 dry-run 校验，不启动 benchmark、Gateway、Dora 或硬件动作。
- [Planned] [Safety] [Feat] Record the phased RoboTwin/SAPIEN controller-qualification route; first implement the provider-owned qualification plan/evidence contract and no-motion dry-run validation without starting benchmark, Gateway, Dora, or hardware motion.
- [计划] [policy] [feat] 增加独立 qualification artifact 的身份、命令族、测试覆盖、证据引用和审核状态绑定；未完成独立资格时继续 fail-closed，禁止把 planner capability 或观测值升级为 controller enforcement。
- [Planned] [Policy] [Feat] Add identity, command-family, test-coverage, evidence-reference, and review-state bindings for an independent qualification artifact; remain fail-closed until qualification is complete and never promote planner capability or measurements to controller enforcement.
- [计划] [eval] [test] 为 schema、digest、identity drift、缺失/不完整测试矩阵和错误授权状态增加回归；完成该大步后执行五维代码审查。
- [Planned] [Eval] [Test] Add regressions for schema, digests, identity drift, incomplete test matrices, and invalid authorization states, followed by a five-dimension code review for this milestone.

### 预期影响文件 / Planned Files

- `examples/forge-adapters/robotwin20/src/robotwin20_adapter/controller_qualification.py`
- `examples/forge-adapters/robotwin20/src/robotwin20_adapter/__init__.py`
- `examples/forge-adapters/robotwin20/scripts/materialize_controller_qualification_plan.py`
- `examples/forge-adapters/robotwin20/scripts/validate_controller_qualification.py`
- `examples/forge-adapters/robotwin20/scripts/verify_controller_qualification_plan.py`
- `examples/forge-adapters/robotwin20/scripts/approve_controller_qualification_plan.py`
- `examples/forge-adapters/robotwin20/tests/test_controller_qualification.py`
- `docs/forge/ROBOTWIN_CONTROLLER_QUALIFICATION_EXECUTION_PLAN.md`
- `docs/forge/REAL_SPEED_LIMITS_ARCHITECTURE.md`
- `docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md`

## v5.10.1 (2026-09-06 14:26) - codex

### 预期修改 / Planned Changes

- [取消] [safety] [feat] 原计划在缺少真实 provider 的情况下实现独立 speed-bounded simulator controller；经预审确认不能用命令截断替代物理控制器保证。
- [Cancelled] [Safety] [Feat] The planned simulator-controller implementation was cancelled after review confirmed that command clipping cannot replace a physical controller guarantee.
- [取消] [safety] [feat] 原计划增加缺少 SDK 实体的 Franka adapter；在没有 libfranka/frankx provider identity 和硬件验证时不创建伪执行 seam。
- [Cancelled] [Safety] [Feat] The planned Franka adapter was cancelled because no libfranka/frankx provider identity or hardware qualification is available; no fake execution seam was created.
- [取消] [eval] [test] 原计划新增控制器测试未执行；继续使用现有 controller qualification 和 fail-closed 回归。
- [Cancelled] [Eval] [Test] The planned controller tests were not added; existing controller-qualification and fail-closed regressions remain authoritative.
- [完成] [docs] [docs] 已更新本条记录及五维审查结论，明确真实 provider 环境是下一阶段前置条件。
- [Completed] [Docs] [Docs] This record and the five-dimension review now state that a real provider environment is a prerequisite for the next stage.

### 预期影响文件 / Planned Files

- `examples/forge-adapters/robotwin20/runtime/trajectory_controller.py`
- `examples/forge-adapters/robotwin20/runtime/franka_sdk_controller.py`
- `examples/forge-adapters/robotwin20/runtime/controller_qualification.py`
- `examples/forge-adapters/robotwin20/tests/test_trajectory_controller.py`
- `examples/forge-adapters/robotwin20/tests/test_franka_sdk_controller.py`
- `examples/forge-adapters/robotwin20/tests/test_controller_qualification.py`
- `docs/forge/PLANNING_MODULE_DESIGN.md`
- `docs/forge/MANIPULATION_DAG_DEVELOPER_GUIDE.md`
- `docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md`

### 实际结果 / Actual Results

- [完成] [safety] [review] 预审发现仅通过 `v·dt` 截断目标或包装缺失的 SDK 导入，无法证明 SAPIEN drive-target 或 Franka controller 的硬速度边界；该方案会把命令整形误报为控制器保证，因此未保留新增实现。
- [Completed] [Safety] [Review] Pre-review found that `v·dt` target clipping or a wrapper around an unavailable SDK cannot prove a hard speed bound for SAPIEN drive-target or a Franka controller; retaining it would misrepresent command shaping as controller enforcement, so no new implementation was kept.
- [完成] [eval] [test] 只读环境核验显示 PAOS 解释器缺少 `sapien`、`mplib`、`curobo`、`frankx`、`libfranka` 和 `franka_ros`；现有回归保持 `734 passed, 1 skipped`，未启动仿真动作、Gateway、Dora 或硬件。
- [Completed] [Eval] [Test] Read-only environment checks show that the PAOS interpreter lacks `sapien`, `mplib`, `curobo`, `frankx`, `libfranka`, and `franka_ros`; the existing regression remains `734 passed, 1 skipped`, with no simulation motion, Gateway, Dora, or hardware started.
- [完成] [docs] [docs] 当前下一步收口为：提供真实 RoboTwin 依赖环境或部署专用 Franka SDK adapter，再生成独立且可审计的 `ControllerQualification` evidence；在此之前保持 `hard_bounded` fail-closed。
- [Completed] [Docs] [Docs] The next gate is now explicit: provide a real RoboTwin dependency environment or a deployment-specific Franka SDK adapter, then generate independently auditable `ControllerQualification` evidence; keep `hard_bounded` fail-closed until then.

### 五维审查 / Five-Dimension Review

- 架构集成：通过；没有把不具备物理保证的代码接入 PAOS 执行面。
- 失败路径：通过；缺失 provider 依赖和无法证明硬边界均保持 unavailable/fail-closed。
- 权威边界：通过；未将 adapter 诊断测量升级为 controller authority。
- 配置与 provenance：通过；未新增无法绑定真实 provider identity 的伪配置。
- 可维护性：通过；删除了会造成误导的浅层实现，保留现有 controller seam。
- Architecture integration: pass; no code without physical guarantees was wired into the PAOS execution plane.
- Failure paths: pass; missing provider dependencies and unproven hard bounds remain unavailable/fail-closed.
- Authority boundaries: pass; adapter diagnostics were not promoted to controller authority.
- Configuration and provenance: pass; no fake configuration lacking a real provider identity was added.
- Maintainability: pass; the misleading shallow implementation was removed and the existing controller seam was preserved.

## v5.9.3 (2026-09-06 10:20) - codex

### 预期修改 / Planned Changes

- [计划] [docs] [fix] 同步两份 PAOS 诊断文档中的 controller qualification 门禁，修正 changelog 对文档状态的引用；不修改运行时代码，不启动任何 probe 或动作。
- [Planned] [Docs] [Fix] Synchronize the controller-qualification gate in both PAOS diagnosis documents and correct the changelog's documentation-state references; no runtime code or probe/action execution will be changed.

### 实际结果 / Actual Results

- [完成] [docs] [fix] 两份 PAOS 诊断文档新增 controller qualification evidence gate，明确 capability flag、diagnostic evidence 与 hard-limit qualification 的边界；运行时代码和动作执行未改变。
- [Completed] [Docs] [Fix] Added the controller-qualification evidence gate to both PAOS diagnosis documents, clarifying the boundary between capability flags, diagnostic evidence, and hard-limit qualification; runtime code and action execution were unchanged.

### 文件变更详情 / Detailed Changes

- [修改] `docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md:L1196-L1208`、`docs/forge/STATE_FILE_IMPLEMENTATION_REVIEW_20260903.md:L1191-L1203`：补齐 qualification evidence 门禁说明。
- [Modified] `docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md:L1196-L1208` and `docs/forge/STATE_FILE_IMPLEMENTATION_REVIEW_20260903.md:L1191-L1203`: completed the qualification-evidence gate documentation.

### 五维审查 / Five-Dimension Review

架构集成、失败路径、权威边界、配置/provenance 和可维护性均通过；文档与实现状态一致。Architecture integration, failure paths, authority boundaries, configuration/provenance, and maintainability pass; documentation now matches implementation state.

### 验证 / Validation

`git diff --check`：通过；未运行新的 simulation probe、Gateway、Dora、Action 或硬件。

## v5.9.2 (2026-09-06 10:00) - codex

### 实际结果 / Actual Results

- [完成] [safety] [feat] 为 adapter-local controller 增加 `ControllerQualification` 独立资格协议，绑定 controller/simulator identity、速度上限、实测最大速度、样本数、artifact ref、independent 标记和审核状态。
- [Completed] [Safety] [Feat] Added the adapter-local `ControllerQualification` independent qualification contract binding controller/simulator identity, speed limit, observed maximum speed, sample count, artifact reference, independence, and review status.
- [完成] [policy] [fix] `hard_bounded` 现在必须通过已批准且 identity/limit 匹配的 qualification evidence；缺失、待审核、超限或 identity 漂移均 fail-closed。当前 RoboTwin drive-target backend 仍在能力预检阶段拒绝 hard mode。
- [Completed] [Policy] [Fix] `hard_bounded` now requires approved qualification evidence matching controller identity and limit; missing, pending, over-limit, or drifted evidence fails closed. The current RoboTwin drive-target backend still rejects hard mode during capability preflight.
- [完成] [eval] [test] 新增 qualification schema、缺失证据、identity drift 和 pending review 回归；专项 `40 passed`，组合回归 `725 passed, 1 skipped`，未启动新的 simulation probe、Gateway、Dora、Action 或硬件。
- [Completed] [Eval] [Test] Added qualification-schema, missing-evidence, identity-drift, and pending-review regressions; focused suite `40 passed`, combined regression `725 passed, 1 skipped`, with no new simulation probe, Gateway, Dora, Action, or hardware started.

### 文件变更详情 / Detailed Changes

- [修改] `examples/forge-adapters/robotwin20/runtime/trajectory_controller.py:L18-L151`：新增独立 qualification contract，并让 hard mode 强制验证 qualification。
- [Modified] `examples/forge-adapters/robotwin20/runtime/trajectory_controller.py:L18-L151`: added the independent qualification contract and required qualification validation for hard mode.
- [新增] `examples/forge-adapters/robotwin20/tests/test_trajectory_controller.py:L13-L73`：覆盖 qualification 缺失、批准通过、identity drift 和 pending review。
- [Added] `examples/forge-adapters/robotwin20/tests/test_trajectory_controller.py:L13-L73`: covered missing, approved, identity-drift, and pending-review qualification paths.
- [修改] `docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md:L1194-L1204`、`docs/forge/STATE_FILE_IMPLEMENTATION_REVIEW_20260903.md:L1189-L1199`：记录 qualification 门禁及当前 backend 未获 hard-limit 资格。
- [Modified] `docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md:L1194-L1204` and `docs/forge/STATE_FILE_IMPLEMENTATION_REVIEW_20260903.md:L1189-L1199`: recorded the qualification gate and the current backend's lack of hard-limit qualification.

### 五维审查 / Five-Dimension Review

- 架构集成：通过；qualification 仍在 adapter-local seam，不接管 PAOS lifecycle 或 Gateway。
- 失败路径：通过；缺失/漂移/待审核/超限 qualification 均拒绝 hard mode。
- 权威边界：通过；qualification 不授予 motion authority，必须由 Gateway/Runtime 继续授权。
- 配置与 provenance：通过；资格记录绑定 controller、simulator、limit 和 artifact ref；未来 route approval 可通过 joint-limit digest 继承绑定。
- 可维护性：通过；controller 能力、资格证据和执行模式分离，便于替换后重新独立验证。
- Architecture integration: pass; qualification remains an adapter-local seam without taking over PAOS lifecycle or Gateway.
- Failure paths: pass; missing, drifted, pending, or over-limit qualification rejects hard mode.
- Authority boundaries: pass; qualification grants no motion authority, which remains with Gateway/Runtime.
- Configuration and provenance: pass; qualification binds controller, simulator, limit, and artifact reference; future route approval can inherit it through the joint-limit digest.
- Maintainability: pass; controller capabilities, qualification evidence, and execution mode are separated for independent requalification after replacement.

### 验证 / Validation

- controller + simulation probe 专项：`40 passed`。
- Combined core/Skill/adapter regression：`725 passed, 1 skipped`。
- Ruff、compileall、`git diff --check`：通过。

## v5.9.2 (2026-09-06 10:00) - codex

### 预期修改 / Planned Changes

- [计划] [safety] [feat] 为 adapter-local controller 增加独立 qualification record 协议，要求 hard-bounded 模式绑定 controller/simulator identity、速度上限、实测最大速度、样本数、artifact provenance 和审核状态。
- [Planned] [Safety] [Feat] Add an adapter-local independent qualification-record contract requiring hard-bounded mode to bind controller/simulator identity, speed limit, observed maximum speed, sample count, artifact provenance, and review status.
- [计划] [policy] [fix] 让 `hard_bounded` 在缺少或不匹配 qualification evidence 时 fail-closed；诊断模式继续只提供 measured-speed guard，不改变 motion authority。
- [Planned] [Policy] [Fix] Make `hard_bounded` fail closed when qualification evidence is missing or mismatched; diagnostic mode remains a measured-speed guard and does not change motion authority.
- [计划] [eval] [test] 增加 qualification schema、controller/simulator drift、超限和空证据测试；不启动新的 simulation probe、Gateway、Dora、Action 或硬件。
- [Planned] [Eval] [Test] Add qualification-schema, controller/simulator-drift, over-limit, and empty-evidence tests without starting a new simulation probe, Gateway, Dora, Action, or hardware.

## v5.9.0 (2026-09-06 09:00) - codex

### 实际结果 / Actual Results

- [完成] [policy] [feat] 在 RoboTwin adapter 内新增 provider-local `SpeedBoundedExecutionController` 协议与能力声明，区分 `hard_bounded` 与 `diagnostic_measured_guard`；PAOS Planning、Skill、Gateway 和 Experience 不承载仿真器实现。
- [Completed] [Policy] [Feat] Added a provider-local `SpeedBoundedExecutionController` contract and capability declaration in the RoboTwin adapter, distinguishing `hard_bounded` from `diagnostic_measured_guard`; PAOS Planning, Skill, Gateway, and Experience carry no simulator implementation.
- [完成] [safety] [fix] 当前 `set_arm_joints` drive-target 后端声明无 hard Cartesian 限速，选择 `hard_bounded` 在 world change 前 fail-closed；diagnostic 模式复用实测速度 gate，不把 command scaling 或 post-step measurement 宣称为硬限速。
- [Completed] [Safety] [Fix] The current `set_arm_joints` drive-target backend declares no hard Cartesian limiter, so `hard_bounded` fails closed before world change; diagnostic mode reuses the measured-speed gate without representing command scaling or post-step measurement as hard limiting.
- [完成] [eval] [test] 新增 controller capability、unsupported backend、输入校验和 violation evidence 测试；专项 `37 passed`，组合回归 `722 passed, 1 skipped`，未启动新的 simulation probe、Gateway、Dora、Action 或硬件。
- [Completed] [Eval] [Test] Added controller capability, unsupported-backend, input-validation, and violation-evidence tests; focused suite `37 passed`, combined regression `722 passed, 1 skipped`, with no new simulation probe, Gateway, Dora, Action, or hardware started.
- [完成] [config] [fix] 修复 controller identity/version 未绑定到 route policy 的 provenance 缺口；`execution_controller` 现由 profile/materializer/probe policy 共同绑定，trajectory/failure evidence 记录 controller identity 与 mode，并拒绝 backend/policy 漂移。
- [Completed] [Config] [Fix] Fixed the provenance gap where controller identity/version were not bound to the route policy; `execution_controller` is now bound across profile/materializer/probe policy, trajectory/failure evidence records controller identity and mode, and backend/policy drift is rejected.

### 文件变更详情 / Detailed Changes

- [新增] `examples/forge-adapters/robotwin20/runtime/trajectory_controller.py:L1-L125`：新增 adapter-local controller capability、模式、预检、policy binding 和实测速度 violation contract。
- [Added] `examples/forge-adapters/robotwin20/runtime/trajectory_controller.py:L1-L125`: added the adapter-local controller capability, mode, preflight, policy binding, and measured-speed violation contract.
- [修改] `examples/forge-adapters/robotwin20/runtime/robotwin_simulation_probe_worker.py:L29-L39,L546-L635,L782-L824,L1018-L1032`：将 measured-speed gate 接入 controller seam，校验 execution controller provenance 并保留失败 artifact。
- [Modified] `examples/forge-adapters/robotwin20/runtime/robotwin_simulation_probe_worker.py:L29-L39,L546-L635,L782-L824,L1018-L1032`: routed the measured-speed gate through the controller seam, validated execution-controller provenance, and preserved failure artifacts.
- [新增] `examples/forge-adapters/robotwin20/tests/test_trajectory_controller.py:L1-L87`、`test_simulation_probe.py:L389-L417`：覆盖 hard mode 拒绝、实测速度、超限、身份、输入、policy 漂移和 evidence 失败。
- [Added] `examples/forge-adapters/robotwin20/tests/test_trajectory_controller.py:L1-L87` and `test_simulation_probe.py:L389-L417`: covered hard-mode rejection, measured speed, violations, identity, invalid input, policy drift, and evidence failures.
- [修改] `docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md:L1176-L1190`、`docs/forge/STATE_FILE_IMPLEMENTATION_REVIEW_20260903.md:L1173-L1187`：记录层级归属、五维审查和后续 qualification gate。
- [Modified] `docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md:L1176-L1190` and `docs/forge/STATE_FILE_IMPLEMENTATION_REVIEW_20260903.md:L1173-L1187`: recorded layer ownership, five-dimension review, and the qualification gate.
- [修改] `examples/forge-adapters/robotwin20/profiles/robotwin20/route-inputs.yaml:L47-L57`、`.../scripts/materialize_complete_route.py:L105-L118`、`.../runtime/robotwin_simulation_probe_worker.py:L546-L635,L782-L824,L1018-L1032`：增加并校验 `execution_controller` provenance binding。
- [Modified] `examples/forge-adapters/robotwin20/profiles/robotwin20/route-inputs.yaml:L47-L57`, `.../scripts/materialize_complete_route.py:L105-L118`, and `.../runtime/robotwin_simulation_probe_worker.py:L546-L635,L782-L824,L1018-L1032`: added and validated `execution_controller` provenance binding.

### 五维审查 / Five-Dimension Review

- 架构集成：通过；controller 仅在 adapter runtime，PAOS Planning/Skill/Gateway/Experience 所有权不变。
- 失败路径：通过；未具备硬限速能力时 preflight 拒绝，实测超限产生结构化 failure evidence。
- 权威边界：通过；controller 不授予 motion authority，Gateway 仍是生产执行唯一授权面。
- 配置：通过；controller identity/version 属于 adapter seam，未引入第二份 PAOS 配置源。
- 可维护性：通过；硬限速与诊断 guard 明确分模式，未来 backend 可独立 qualification 和替换。
- Architecture integration: pass; the controller is confined to adapter runtime and PAOS Planning/Skill/Gateway/Experience ownership is unchanged.
- Failure paths: pass; preflight rejects backends without hard limiting and measured violations produce structured failure evidence.
- Authority boundaries: pass; the controller grants no motion authority and Gateway remains the sole production execution authority.
- Configuration: pass; controller identity/version stay within the adapter seam without adding a second PAOS configuration source.
- Maintainability: pass; hard limiting and diagnostic guard are explicit modes, allowing independently qualified backend replacement.

### 验证 / Validation

- `test_trajectory_controller.py` + `test_simulation_probe.py`：`38 passed`。
- Combined core/Skill/adapter regression：`723 passed, 1 skipped`。
- Ruff、compileall、`git diff --check`：通过。

## v5.8.0 (2026-09-06 08:00) - codex

### 预期修改 / Planned Changes

- [完成] [policy] [refactor] 根据 v7-v10 仿真证据撤回不能证明 Cartesian 速度受控的 `execution_position_subdivision` 实验性执行调参；`execution_velocity_scale` 恢复到首次实验前的 `0.25`，并明确仅为 advisory，保留不可变 `0.20 m/s` 观测门禁。
- [Completed] [Policy] [Refactor] Based on v7-v10 simulation evidence, removed the experimental `execution_position_subdivision` tuning that cannot prove bounded Cartesian speed; restored `execution_velocity_scale` to the pre-sequence `0.25` advisory value and retained the immutable `0.20 m/s` observed-speed gate.
- [完成] [eval] [fix] 同步 profile、materializer、simulation worker 与测试，恢复单一 planner sample 执行语义；uniform retiming 的 `max_samples` 继续独立限制轨迹样本，不再依赖 subdivision。
- [Completed] [Eval] [Fix] Synchronized the profile, materializer, simulation worker, and tests to restore single planner-sample execution semantics; uniform retiming `max_samples` independently bounds trajectory samples without subdivision.
- [完成] [docs] [docs] 保留 v7-v10 失败证据和诊断文档，记录“停止无意义调参”的原因；未生成新 route，未启动新的 simulation probe、Gateway、Dora、Action 或硬件。
- [Completed] [Docs] [Docs] Retained v7-v10 negative evidence and diagnosis documents and recorded why unsupported tuning stopped; no new route was generated and no new simulation probe, Gateway, Dora, Action, or hardware was started.

### 文件变更详情 / Detailed Changes

- [修改] `examples/forge-adapters/robotwin20/profiles/robotwin20/route-inputs.yaml:L47-L49`：移除 `execution_position_subdivision`，将 scale 恢复为 `0.25` 并改为 advisory 说明。
- [Modified] `examples/forge-adapters/robotwin20/profiles/robotwin20/route-inputs.yaml:L47-L49`: removed `execution_position_subdivision`, restored scale to `0.25`, and documented it as advisory.
- [修改] `examples/forge-adapters/robotwin20/runtime/robotwin_simulation_probe_worker.py:L546-L633,L750-L811,L939-L950`：删除 subdivision 校验、插值和有效样本乘法，仅保留原始 planner sample 执行、命令缩放和 measured-speed fail-closed gate。
- [Modified] `examples/forge-adapters/robotwin20/runtime/robotwin_simulation_probe_worker.py:L546-L633,L750-L811,L939-L950`: removed subdivision validation, interpolation, and multiplied effective-sample logic, retaining original planner-sample execution, advisory command scaling, and measured-speed fail-closed gating.
- [修改] `examples/forge-adapters/robotwin20/scripts/materialize_complete_route.py:L105-L112`：撤销 subdivision profile 字段及其校验。
- [Modified] `examples/forge-adapters/robotwin20/scripts/materialize_complete_route.py:L105-L112`: removed the subdivision profile field and validation.
- [修改] `examples/forge-adapters/robotwin20/tests/test_simulation_probe.py:L220-L469`：删除 subdivision 专项 fixture、插值和预算测试，保留 scale、retiming、速度违规和失败恢复回归。
- [Modified] `examples/forge-adapters/robotwin20/tests/test_simulation_probe.py:L220-L469`: removed subdivision fixtures, interpolation, and budget tests while retaining scale, retiming, speed-violation, and failure-recovery regressions.

### 五维审查 / Five-Dimension Review

- 架构集成：通过；回滚限于 adapter simulation worker/profile，不改变 PAOS lifecycle、Gateway 或 Action 权威。
- 失败路径：通过；measured-speed 超限仍立即 fail-closed，历史 v10 attach/detach/reset 失败证据保留。
- 权威边界：通过；未放宽 `0.20 m/s`、joint/workspace/stop policy，未把 scale 当作安全授权。
- 配置：通过；profile 与 materializer schema 同步，旧含 subdivision 的 route 不会被当前 profile 重新生成。
- 可维护性：通过；删除无证据的调参分支，保留根因说明和可复现负证据，避免继续 v11/v12 试错。
- Architecture integration: pass; rollback is confined to the adapter simulation worker/profile and does not alter PAOS lifecycle, Gateway, or Action authority.
- Failure paths: pass; measured-speed violations still fail closed immediately, while historical v10 attach/detach/reset evidence remains preserved.
- Authority boundaries: pass; no `0.20 m/s`, joint, workspace, or stop-policy limit was relaxed, and scale is not treated as safety authorization.
- Configuration: pass; profile and materializer schemas are synchronized, and the current profile no longer regenerates subdivision routes.
- Maintainability: pass; unsupported tuning branches are removed while root-cause notes and reproducible negative evidence remain available, preventing v11/v12 trial-and-error.

### 验证 / Validation

- `test_simulation_probe.py`：`31 passed`。
- Combined core/Skill/adapter regression：`716 passed, 1 skipped`。
- Ruff、compileall、`git diff --check`：通过。


## v5.7.19 (2026-09-06 07:20) - codex

### 预期修改 / Planned Changes

- [计划] [eval] [exp] 使用 reviewer `yanxu` 批准的 v10 route/source-manifest digest，在全新 artifact root 创建不可变 simulation-only approval，并运行一次 single-use RoboTwin20 probe。
- [Planned] [Eval] [Exp] Using reviewer `yanxu`'s approved v10 route/source-manifest digests, create an immutable simulation-only approval under a fresh artifact root and run one single-use RoboTwin20 probe.
- [计划] [docs] [fix] 保存 v10 probe response、before/after snapshot、contact trace、trajectory 或 failure evidence，并完成五维审查；不接入 Gateway、Dora、Action 或硬件。
- [Planned] [Docs] [Fix] Persist v10 probe response, before/after snapshots, contact trace, trajectory or failure evidence, and complete the five-dimension review without connecting Gateway, Dora, Action, or hardware.

### 实际结果 / Actual Results

- [完成] [eval] [exp] v10 approval artifact 写入 `/home/yanxu/robotwin20-sim-probe-20260906T072000Z/probe/approval.json`，SHA-256 `a8fad1605248346dca3a8f2304168e0a7792191a3c2f8b9b7ba42868c361dfff`；single-use probe 返回 `unavailable`。
- [Completed] [Eval] [Exp] The v10 approval artifact was written to `/home/yanxu/robotwin20-sim-probe-20260906T072000Z/probe/approval.json` with SHA-256 `a8fad1605248346dca3a8f2304168e0a7792191a3c2f8b9b7ba42868c361dfff`; the single-use probe returned `unavailable`.
- [完成] [eval] [exp] v10 probe 失败证据保存在 `/home/yanxu/robotwin20-sim-probe-20260906T072000Z/simulation-probe/franka-blocks-green1-candidate1-20260906-v10-subdivided/block-green-1-1/failure.json`；失败阶段为 transport，第 4293 步观测 `0.2030478779191653 m/s`，对象已 attach，detach/reset 完成。
- [Completed] [Eval] [Exp] v10 failure evidence is stored at `/home/yanxu/robotwin20-sim-probe-20260906T072000Z/simulation-probe/franka-blocks-green1-candidate1-20260906-v10-subdivided/block-green-1-1/failure.json`; failure occurred in transport at step 4293 with `0.2030478779191653 m/s`, after attachment, with detach/reset completed.
- [完成] [eval] [exp] v10 通过 contact/lift 并进入 transport，第 4293 步观测末端线速度 `0.2030478779191653 m/s`，超过不可变 `0.20 m/s`；对象已 attach，失败恢复 detach 成功、simulation reset 完成，完整 route/semantic 成功未证明。
- [Completed] [Eval] [Exp] v10 passed contact/lift and reached transport, but observed end-effector speed `0.2030478779191653 m/s` at step 4293 exceeded the immutable `0.20 m/s` limit; the object was attached, detach succeeded during recovery, simulation reset completed, and complete route/semantic success remains unproven.
- [完成] [diagnosis] [fix] 根据 RoboTwin `set_arm_joints()` 同时设置 drive position/velocity target 的实现，确认 velocity target 不是 SAPIEN 的硬速度上限；位置子步化和 scale 不能单调保证 Cartesian speed，当前标记为 simulator-dynamics blocker，停止无证据的继续调参重试。
- [Completed] [Diagnosis] [Fix] Based on RoboTwin `set_arm_joints()` setting both drive position and velocity targets, confirmed that velocity target is not a hard SAPIEN speed limit; position subdivision and scaling cannot monotonically guarantee Cartesian speed, so this is recorded as a simulator-dynamics blocker and unsupported tuning retries stop.

### 五维审查 / Five-Dimension Review

- 架构集成：通过；v10 仍是 adapter-owned simulation evidence，未接管 PAOS lifecycle 或 Gateway。
- 失败路径：通过；contact/lift/transport 阶段、attached object、detach、reset、before/after 和不可变 failure artifact 均有记录。
- 权威边界：通过；没有放宽 `0.20 m/s`，没有将 simulator velocity target 误当作安全授权。
- 配置：通过；v10 使用独立 approval/digest；不再生成无证据的 v11 调参 route。
- 可维护性：通过；将“drive target 非硬限速”的根因写入文档，避免重复非单调实验。
- Architecture integration: pass; v10 remains adapter-owned simulation evidence without taking over PAOS lifecycle or Gateway ownership.
- Failure paths: pass; contact/lift/transport phases, attached object, detach, reset, before/after, and immutable failure artifacts are recorded.
- Authority boundaries: pass; the `0.20 m/s` limit was not relaxed and simulator velocity targets were not misrepresented as safety authorization.
- Configuration: pass; v10 uses independent approval/digests and no unsupported v11 tuning route is generated.
- Maintainability: pass; the drive-target non-hard-limit root cause is documented to prevent repeated non-monotonic experiments.

## v5.7.18 (2026-09-06 06:10) - codex

### 预期修改 / Planned Changes

- [计划] [policy] [fix] 为 profile-owned trajectory position subdivision 增加有效采样总预算校验，拒绝 planner sample 数与 subdivision 相乘后超过上限的执行请求，避免子步化引入无界仿真时长。
- [Planned] [Policy] [Fix] Add an effective-sample budget check for profile-owned trajectory position subdivision, rejecting execution requests whose planner-sample count multiplied by subdivision exceeds the cap and preventing unbounded simulation duration.
- [计划] [eval] [test] 增加总预算超限和边界值回归测试，并重新执行 simulation probe/core/Skill/adapter 组合测试、Ruff、compileall 与 diff check；不运行未审批的 v10 probe。
- [Planned] [Eval] [Test] Add effective-budget overflow and boundary regressions and rerun the simulation-probe/core/Skill/adapter suites, Ruff, compileall, and diff check; do not run the unapproved v10 probe.

### 实际结果 / Actual Results

- [完成] [policy] [fix] `examples/forge-adapters/robotwin20/runtime/robotwin_simulation_probe_worker.py:L762-L808,L974-L983` 增加有效采样预算校验：`len(planner_samples) × execution_position_subdivision` 不得超过 `trajectory_retiming.max_samples`，超限 fail-closed。
- [Completed] [Policy] [Fix] `examples/forge-adapters/robotwin20/runtime/robotwin_simulation_probe_worker.py:L762-L808,L974-L983` adds an effective sample-budget check: `len(planner_samples) × execution_position_subdivision` must not exceed `trajectory_retiming.max_samples`, otherwise execution fails closed.
- [完成] [eval] [test] `examples/forge-adapters/robotwin20/tests/test_simulation_probe.py:L505-L533` 增加预算溢出回归；全量组合回归 `724 passed, 1 skipped`，Ruff、compileall、`git diff --check` 通过；未运行未审批的 v10 probe。
- [Completed] [Eval] [Test] `examples/forge-adapters/robotwin20/tests/test_simulation_probe.py:L505-L533` adds effective-budget overflow regression; full combined regression passes `724 passed, 1 skipped`, with Ruff, compileall, and `git diff --check` passing; the unapproved v10 probe was not run.

### 五维审查 / Five-Dimension Review

- 架构集成：通过；预算校验仅位于 adapter simulation worker，不接管 PAOS lifecycle 或 Gateway。
- 失败路径：通过；子步化导致的预算溢出明确 fail-closed，且不会启动额外仿真步。
- 权威边界：通过；未修改任何物理/安全上限，未授予生产动作权限。
- 配置：通过；预算继承 materialized trajectory-retiming policy，request/manifest digest 继续绑定。
- 可维护性：通过；位置子步化、速度缩放和总预算均有独立测试与文档说明。
- Architecture integration: pass; the budget check is confined to the adapter simulation worker and does not take over PAOS lifecycle or Gateway ownership.
- Failure paths: pass; subdivision-induced budget overflow fails closed without starting additional simulator steps.
- Authority boundaries: pass; no physical or safety limit changed and no production motion authority granted.
- Configuration: pass; the budget derives from the materialized trajectory-retiming policy with request/manifest digests still bound.
- Maintainability: pass; position subdivision, velocity scaling, and total budget have isolated tests and documentation.

## v5.7.16 (2026-09-06 04:55) - codex

### 预期修改 / Planned Changes

- [计划] [policy] [fix] 修复 v9 证明的 drive-target 加速度主导问题：新增 profile-owned `execution_position_subdivision`，在每个规划采样间插值关节目标并按子步缩放速度，降低单步目标跳变；保持 `0.20 m/s` 线速度门禁不可变。
- [Planned] [Policy] [Fix] Address the drive-target acceleration behavior evidenced by v9 by adding profile-owned `execution_position_subdivision`; interpolate joint targets between planner samples and scale velocity per substep to reduce per-step target jumps while keeping the immutable `0.20 m/s` gate.
- [计划] [config] [feat] 将 position subdivision 写入 joint-limit policy 并由 materializer、approval binding 和 worker 严格校验；不修改 URDF、workspace、joint limits 或 stop policy。
- [Planned] [Config] [Feat] Record position subdivision in the joint-limit policy and validate it strictly through materialization, approval binding, and the worker without changing URDF, workspace, joint limits, or stop policy.
- [计划] [eval] [test] 增加插值端点、子步速度、非法配置和超限 evidence 回归；生成全新 v10 route，v9 approval 不复用。
- [Planned] [Eval] [Test] Add interpolation endpoint, substep velocity, invalid-configuration, and violation-evidence regressions; generate a fresh v10 route without reusing v9 approval.

### 实际结果 / Actual Results

- [完成] [policy] [fix] `examples/forge-adapters/robotwin20/runtime/robotwin_simulation_probe_worker.py:L543-L640,L749-L830,L946-L960` 增加 profile-owned `execution_position_subdivision`，在 planner sample 间插值 joint targets，并按子步缩放速度；`0.20 m/s` measured gate 保持不变。
- [Completed] [Policy] [Fix] `examples/forge-adapters/robotwin20/runtime/robotwin_simulation_probe_worker.py:L543-L640,L749-L830,L946-L960` adds profile-owned `execution_position_subdivision`, interpolates joint targets between planner samples, and scales velocity per substep while keeping the measured `0.20 m/s` gate unchanged.
- [完成] [config] [feat] `examples/forge-adapters/robotwin20/profiles/robotwin20/route-inputs.yaml:L49-L50` 写入 `execution_velocity_scale=0.10` 与 `execution_position_subdivision=4`；materializer `.../scripts/materialize_complete_route.py:L105-L112` 对 subdivision 执行 `(1..16)` 严格校验。
- [Completed] [Config] [Feat] `examples/forge-adapters/robotwin20/profiles/robotwin20/route-inputs.yaml:L49-L50` records `execution_velocity_scale=0.10` and `execution_position_subdivision=4`; materializer `.../scripts/materialize_complete_route.py:L105-L112` strictly validates subdivision in `(1..16)`.
- [完成] [eval] [test] `examples/forge-adapters/robotwin20/tests/test_simulation_probe.py:L303-L340,L430-L503,L530-L566` 覆盖非法 subdivision、插值端点、子步速度与违规 evidence；全量组合回归 `723 passed, 1 skipped`，Ruff、compileall、`git diff --check` 通过。
- [Completed] [Eval] [Test] `examples/forge-adapters/robotwin20/tests/test_simulation_probe.py:L303-L340,L430-L503,L530-L566` covers invalid subdivision, interpolation endpoints, substep velocity, and violation evidence; full combined regression passes `723 passed, 1 skipped`, with Ruff, compileall, and `git diff --check` passing.
- [完成] [eval] [exp] v9 负证据与 position-subdivision 设计已写入两份诊断文档；尚未生成 v10 route 或运行 probe，需新的 route/source-manifest digest 与人工审批。
- [Completed] [Eval] [Exp] The v9 negative evidence and position-subdivision design are recorded in both diagnosis documents; no v10 route or probe has run yet, and fresh route/source-manifest digests plus human approval are required.
- [完成] [eval] [exp] 已生成 v10 route `/home/yanxu/robotwin20-route-inputs-20260906T050000Z/`，request `franka-blocks-green1-candidate1-20260906-v10-subdivided`，route digest `29450e78db41de37ad4e2dc570508e2de37069f35ad4cb32ec443ec173e1bc4a`，source manifest digest `b476aeacf282ce3091943f4f8ba365e5b744446fa7e46040505a05a18db3f583`；状态 `pending_human_review`，未运行 probe。
- [Completed] [Eval] [Exp] Generated v10 route `/home/yanxu/robotwin20-route-inputs-20260906T050000Z/` with request `franka-blocks-green1-candidate1-20260906-v10-subdivided`, route digest `29450e78db41de37ad4e2dc570508e2de37069f35ad4cb32ec443ec173e1bc4a`, and source-manifest digest `b476aeacf282ce3091943f4f8ba365e5b744446fa7e46040505a05a18db3f583`; status is `pending_human_review` and the probe has not run.

## v5.7.15 (2026-09-06 03:45) - codex

### 预期修改 / Planned Changes

- [计划] [eval] [exp] 使用 reviewer `yanxu` 批准的 v9 route/source-manifest digest，在全新 artifact root 创建不可变 simulation-only approval，并运行一次 single-use RoboTwin20 probe。
- [Planned] [Eval] [Exp] Using reviewer `yanxu`'s approved v9 route/source-manifest digests, create an immutable simulation-only approval under a fresh artifact root and run one single-use RoboTwin20 probe.
- [计划] [docs] [fix] 保存 v9 probe response、before/after snapshot、contact trace、trajectory 或 failure evidence，并完成五维审查；不接入 Gateway、Dora、Action 或硬件。
- [Planned] [Docs] [Fix] Persist v9 probe response, before/after snapshots, contact trace, trajectory or failure evidence, and complete the five-dimension review without connecting Gateway, Dora, Action, or hardware.

## v5.7.14 (2026-09-06 02:55) - codex

### 预期修改 / Planned Changes

- [计划] [eval] [fix] 记录 v8 single-use simulation probe 的负证据：`execution_velocity_scale=0.20` 后 contact 第 876 步仍观测 `0.20113678709101113 m/s`，仿真 reset 完成且对象尚未 attach。
- [Planned] [Eval] [Fix] Record the v8 single-use simulation-probe negative evidence: with `execution_velocity_scale=0.20`, contact step 876 still observed `0.20113678709101113 m/s`; simulation reset completed before object attachment.
- [计划] [config] [tune] 根据 v7→v8 的单调改善将 scale 保守降低到 `0.10`；保持 `0.20 m/s` 线速度上限和全部物理/安全边界不变。
- [Planned] [Config] [Tune] Conservatively lower the scale to `0.10` based on monotonic v7→v8 improvement while preserving the `0.20 m/s` linear-speed limit and all physical/safety boundaries.
- [计划] [eval] [exp] 生成全新 v9 route/source manifest；v8 approval 不复用，需新的 simulation-only 人工审批。
- [Planned] [Eval] [Exp] Generate a fresh v9 route/source manifest; do not reuse v8 approval and require fresh simulation-only human approval.

### 实际结果 / Actual Results

- [完成] [eval] [exp] v9 approval artifact 写入 `/home/yanxu/robotwin20-sim-probe-20260906T040000Z/probe/approval.json`，SHA-256 `5aaa23258ffa209694028c7092a0e97d4987f6a127e9c1a4d99d087c0d3e9a9a`；single-use probe 返回 `unavailable`。
- [Completed] [Eval] [Exp] The v9 approval artifact was written to `/home/yanxu/robotwin20-sim-probe-20260906T040000Z/probe/approval.json` with SHA-256 `5aaa23258ffa209694028c7092a0e97d4987f6a127e9c1a4d99d087c0d3e9a9a`; the single-use probe returned `unavailable`.
- [完成] [eval] [exp] v9 通过 contact 并进入 lift，但第 1038 步观测 `0.2034626107917902 m/s`，仍超过 `0.20 m/s`；对象已 attach，失败恢复时 detach 成功、simulation reset 完成，完整 route/semantic 成功未证明。
- [Completed] [Eval] [Exp] v9 passed contact and reached lift, but observed `0.2034626107917902 m/s` at step 1038, still above `0.20 m/s`; the object was attached, detach succeeded during failure recovery, simulation reset completed, and complete route/semantic success remains unproven.
- [完成] [diagnosis] [fix] 证实单纯降低 execution velocity scale 不是可靠末端速度控制；后续改为 profile-owned 轨迹位置子步插值，避免继续进行非单调 scale 调参。
- [Completed] [Diagnosis] [Fix] Confirmed that lowering execution velocity scale alone is not a reliable end-effector speed control; the next fix is profile-owned trajectory-position subdivision rather than further non-monotonic scale tuning.

### 实际结果 / Actual Results

- [完成] [eval] [exp] v8 approval artifact 写入 `/home/yanxu/robotwin20-sim-probe-20260906T020000Z/probe/approval.json`，SHA-256 `b73f6718d763472011da3be1a5204646d6c1dae3da911be619033aaccade82a9`；single-use probe 返回 `unavailable`。
- [Completed] [Eval] [Exp] The v8 approval artifact was written to `/home/yanxu/robotwin20-sim-probe-20260906T020000Z/probe/approval.json` with SHA-256 `b73f6718d763472011da3be1a5204646d6c1dae3da911be619033aaccade82a9`; the single-use probe returned `unavailable`.
- [完成] [eval] [exp] v8 在 `contact` 第 876 步观测 `0.20113678709101113 m/s`，仍超过 `0.20 m/s`；左臂规划失败、右臂规划通过、对象未 attach、simulation reset 完成，未证明完整抓取放置。
- [Completed] [Eval] [Exp] v8 observed `0.20113678709101113 m/s` at contact step 876, still above `0.20 m/s`; left-arm planning failed, right-arm planning passed, the object was not attached, simulation reset completed, and complete pick-place remains unproven.
- [完成] [config] [tune] `examples/forge-adapters/robotwin20/profiles/robotwin20/route-inputs.yaml:L49` 将 scale 从 `0.20` 降为 `0.10`，未修改任何物理或安全上限。
- [Completed] [Config] [Tune] `examples/forge-adapters/robotwin20/profiles/robotwin20/route-inputs.yaml:L49` lowers the scale from `0.20` to `0.10` without changing physical or safety limits.
- [完成] [eval] [exp] v9 route 已生成于 `/home/yanxu/robotwin20-route-inputs-20260906T030000Z/`，route digest `438f4ddaba73b32f10586d7a96e9a87b39e5b850de489ced88a854129fbae46f`，source manifest digest `14e686c14e09dd7731c7449397883e16d18841f841d267a3d2e6d5bed4f4c2f9`，状态 `pending_human_review`。
- [Completed] [Eval] [Exp] The v9 route was generated under `/home/yanxu/robotwin20-route-inputs-20260906T030000Z/` with route digest `438f4ddaba73b32f10586d7a96e9a87b39e5b850de489ced88a854129fbae46f` and source-manifest digest `14e686c14e09dd7731c7449397883e16d18841f841d267a3d2e6d5bed4f4c2f9`; status is `pending_human_review`.

### 五维审查 / Five-Dimension Review

- 架构集成：通过；v8 probe 和 v9 route 仍属于 adapter-owned simulation evidence，不接管 PAOS 生命周期或 Gateway。
- 失败路径：通过；超限、左右臂规划差异、未 attach、reset 和不可变 failure artifact 均记录。
- 权威边界：通过；`0.20 m/s` gate 未放宽，simulation approval 不授予生产动作权限。
- 配置：通过；v9 route/source manifest/request digest 独立生成，未复用 v8 approval。
- 可维护性：通过；每次 probe 使用独立 artifact root，失败证据与后续 route 分离保存。
- Architecture integration: pass; v8 probing and v9 routing remain adapter-owned simulation evidence without taking over PAOS lifecycle or Gateway ownership.
- Failure paths: pass; speed violation, arm-planning divergence, non-attachment, reset, and immutable failure artifact are recorded.
- Authority boundaries: pass; the `0.20 m/s` gate was not relaxed, and simulation approval grants no production motion authority.
- Configuration: pass; v9 route/source-manifest/request digests are independently generated and v8 approval is not reused.
- Maintainability: pass; each probe uses a separate artifact root and keeps failure evidence separate from subsequent routes.

## v5.7.13 (2026-09-06 02:05) - codex

### 预期修改 / Planned Changes

- [计划] [eval] [exp] 使用 reviewer `yanxu` 批准的 v8 route/source-manifest digest，在全新 artifact root 创建不可变 simulation-only approval，并运行一次 single-use RoboTwin20 probe。
- [Planned] [Eval] [Exp] Using reviewer `yanxu`'s approved v8 route/source-manifest digests, create an immutable simulation-only approval under a fresh artifact root and run one single-use RoboTwin20 probe.
- [计划] [docs] [fix] 保存 v8 probe response、before/after snapshot、contact trace、trajectory 或 failure evidence，并完成五维审查；不接入 Gateway、Dora、Action 或硬件。
- [Planned] [Docs] [Fix] Persist v8 probe response, before/after snapshots, contact trace, trajectory or failure evidence, and complete the five-dimension review without connecting Gateway, Dora, Action, or hardware.

## v5.7.12 (2026-09-06 01:20) - codex

### 预期修改 / Planned Changes

- [计划] [eval] [fix] 记录 v7 single-use simulation probe 的新负证据：`contact` 第 872 步观测末端线速度 `0.202505748 m/s`，超过不可变 `0.20 m/s` 上限；仿真已 reset。
- [Planned] [Eval] [Fix] Record the new v7 single-use simulation-probe negative evidence: observed end-effector speed `0.202505748 m/s` at contact step 872 exceeded the immutable `0.20 m/s` limit; simulation reset completed.
- [计划] [config] [tune] 根据实测超限保守降低 profile-owned `execution_velocity_scale` 从 `0.25` 到 `0.20`；不放宽速度上限、不修改 joint limits、workspace、URDF 或 motion authority。
- [Planned] [Config] [Tune] Conservatively lower profile-owned `execution_velocity_scale` from `0.25` to `0.20` based on measured violation; do not relax the speed limit or modify joint limits, workspace, URDF, or motion authority.
- [计划] [eval] [exp] 生成全新 v8 route/source manifest；v7 approval 不得复用，新的 simulation-only probe 需重新人工审批。
- [Planned] [Eval] [Exp] Generate a fresh v8 route/source manifest; v7 approval must not be reused, and a new simulation-only probe requires fresh human approval.

### 实际结果 / Actual Results

- [完成] [eval] [exp] v7 reviewer approval 已验证并消费一次；probe 在 `/home/yanxu/robotwin20-sim-probe-20260906T005500Z/` 返回 `unavailable`，contact 第 872 步观测 `0.2025057481391883 m/s`，超过不可变 `0.20 m/s`；仿真 reset 完成，未宣称完整抓取放置成功。
- [Completed] [Eval] [Exp] The v7 reviewer approval was validated and consumed once; the probe under `/home/yanxu/robotwin20-sim-probe-20260906T005500Z/` returned `unavailable` with observed `0.2025057481391883 m/s` at contact step 872 against the immutable `0.20 m/s` limit; simulation reset completed and no complete pick-place success is claimed.
- [完成] [config] [tune] `examples/forge-adapters/robotwin20/profiles/robotwin20/route-inputs.yaml:L49` 将 profile-owned execution scale 从 `0.25` 降为 `0.20`；没有放宽任何安全上限或修改物理模型。
- [Completed] [Config] [Tune] `examples/forge-adapters/robotwin20/profiles/robotwin20/route-inputs.yaml:L49` lowers the profile-owned execution scale from `0.25` to `0.20`; no safety limit or physical model was relaxed or changed.
- [完成] [eval] [exp] v8 route 已在 `/home/yanxu/robotwin20-route-inputs-20260906T013000Z/` 生成，request `franka-blocks-green1-candidate1-20260906-v8-scaled`，route digest `6315cb3e4cc83e13738876aa32628d86b420e2aa6f80b785832d01db74b9fed3`，source manifest digest `a62f4fbe5849445c821877d000ca8945c80ec94fcdc7ac15724bd43a89427506`；状态为 `pending_human_review`，尚未运行 v8 probe。
- [Completed] [Eval] [Exp] The v8 route was generated under `/home/yanxu/robotwin20-route-inputs-20260906T013000Z/` with request `franka-blocks-green1-candidate1-20260906-v8-scaled`, route digest `6315cb3e4cc83e13738876aa32628d86b420e2aa6f80b785832d01db74b9fed3`, and source-manifest digest `a62f4fbe5849445c821877d000ca8945c80ec94fcdc7ac15724bd43a89427506`; it remains `pending_human_review` and the v8 probe has not run.

### 五维审查 / Five-Dimension Review

- 架构集成：通过；v7 probe 仍是 adapter-owned simulation evidence，v8 仅更新 profile policy 与 route artifact，不改变 PAOS 生命周期或 Gateway ownership。
- 失败路径：通过；速度超限 fail-closed、reset、before/after snapshot 和不可变 failure artifact 均已保存；v8 需重新审批后才能运行。
- 权威边界：通过；未放宽 `0.20 m/s`，未修改 joint limits、workspace、URDF、stop policy 或 motion authority。
- 配置：通过；scale 来自 route-input profile，v8 route/source-manifest/request digest 独立绑定，未复用 v7 approval。
- 可维护性：通过；v7 负证据和 v8 新 route 分离保存，失败原因与下一门禁已写入诊断文档。
- Architecture integration: pass; v7 remains adapter-owned simulation evidence, while v8 only updates profile policy and route artifacts without changing PAOS lifecycle or Gateway ownership.
- Failure paths: pass; speed violations fail closed with reset, before/after snapshots, and immutable failure artifacts; v8 requires fresh approval before execution.
- Authority boundaries: pass; `0.20 m/s` was not relaxed, and joint limits, workspace, URDF, stop policy, and motion authority were unchanged.
- Configuration: pass; the scale comes from the route-input profile, v8 route/source-manifest/request digests are independently bound, and v7 approval is not reused.
- Maintainability: pass; v7 negative evidence and v8 route artifacts are stored separately, with cause and next gate documented.

## v5.7.11 (2026-09-06 00:50) - codex

### 预期修改 / Planned Changes

- [计划] [eval] [exp] 使用 reviewer `yanxu` 已批准的 v7 route/source-manifest digest，在全新 artifact root 生成不可变 simulation-only approval，并运行一次 single-use RoboTwin20 probe。
- [Planned] [Eval] [Exp] Using reviewer `yanxu`'s approved v7 route/source-manifest digests, create an immutable simulation-only approval under a fresh artifact root and run one single-use RoboTwin20 probe.
- [计划] [docs] [fix] 保存 probe response、before/after snapshot、contact trace、failure 或成功 evidence，并更新诊断与五维验收边界；不接入 Gateway、Dora、Action 或硬件。
- [Planned] [Docs] [Fix] Persist probe response, before/after snapshots, contact trace, failure or success evidence, and update diagnosis and five-dimension boundaries without connecting Gateway, Dora, Action, or hardware.

### 实际结果 / Actual Results

- [完成] [eval] [exp] v7 approval artifact 写入 `/home/yanxu/robotwin20-sim-probe-20260906T005500Z/probe/approval.json`；approval SHA-256 为 `ff32ecc109f12ab2a23fc0d543a839523e063c7aa1a20d1aef741beda6cd31cf`。
- [Completed] [Eval] [Exp] The v7 approval artifact was written to `/home/yanxu/robotwin20-sim-probe-20260906T005500Z/probe/approval.json`; approval SHA-256 is `ff32ecc109f12ab2a23fc0d543a839523e063c7aa1a20d1aef741beda6cd31cf`.
- [完成] [eval] [exp] single-use probe 返回 `unavailable`：`failed_phase=contact`、`simulator_steps=872`、`observed_mps=0.2025057481391883`、`simulation_reset_status=completed`、`world_change_completed=false`；完整 lift/transport/release/retreat/contact/semantic 成功未证明。
- [Completed] [Eval] [Exp] The single-use probe returned `unavailable`: `failed_phase=contact`, `simulator_steps=872`, `observed_mps=0.2025057481391883`, `simulation_reset_status=completed`, and `world_change_completed=false`; complete lift/transport/release/retreat/contact/semantic success remains unproven.
- [完成] [docs] [fix] 更新两份诊断文档，记录 v7 负证据、v8 route 生成和新的人工审批门。
- [Completed] [Docs] [Fix] Updated both diagnosis documents with v7 negative evidence, v8 route generation, and the new human-approval gate.

## v5.7.10 (2026-09-06 00:35) - codex

### 预期修改 / Planned Changes

- [计划] [chore] [docs] 回写 v5.7.9 simulation probe evidence 修复的提交哈希到月度日志与入口索引；不修改实现或安全边界。
- [Planned] [Chore] [Docs] Record the v5.7.9 simulation-probe evidence-fix commit hash in the monthly log and index without changing implementation or safety boundaries.

### 实际结果 / Actual Results

- [完成] [chore] [docs] v5.7.9 已提交为 `f4222d1` 并保留 `.codegraph/`、`.cursor/` 未跟踪且未提交。
- [Completed] [Chore] [Docs] v5.7.9 was committed as `f4222d1`; `.codegraph/` and `.cursor/` remain untracked and uncommitted.

### Git 提交 / Git Commit

- Commit: `f4222d1`
- Branch: `feature/long-horizon-workflow`

## v5.10.3 (2026-09-06 15:05) - codex

- [完成] [chore] 回写 v5.10.2 Git 提交 `891dfea`；不改变代码、配置、文档或验证结果。
- [Completed] [Chore] Recorded commit `891dfea` for v5.10.2; no code, configuration, documentation, or validation results changed.

## v6.0.0 (2026-09-06 15:20) - codex

### 预期修改 / Planned Changes

- [计划] [policy] [feat] 在 RoboTwin adapter 内实现 provider-owned `MotionCapability` v2：从实际 embodiment URDF、CuRobo profile、RoboTwin planner/controller source 和独立 runtime interpreter 导出逐关节限制、timing、provider identity 与 enforcement 语义。
- [Planned] [Policy] [Feat] Implement provider-owned `MotionCapability` v2 in the RoboTwin adapter, deriving per-joint limits, timing, provider identity, and enforcement semantics from the selected embodiment URDF, CuRobo profile, RoboTwin planner/controller sources, and an independent runtime interpreter.
- [计划] [eval] [feat] 实现独立、无动作的 capability qualification 验证，严格重算来源 digest 和语义；当前 SAPIEN drive-target 只能获得 `planner_constrained` 结论，不生成伪 `controller_enforced` 资格。
- [Planned] [Eval] [Feat] Implement independent no-motion capability qualification that recomputes source digests and semantics; the current SAPIEN drive-target backend may qualify only as `planner_constrained`, never as pseudo `controller_enforced`.
- [计划] [policy] [fix] 将 route v5、Plan/Arm capability snapshot 和 materializer 绑定到 motion-capability artifact/digest；缺失、漂移、旧 controller-only 引用或非 hard-controller qualification 时保持 pre-motion fail-closed。
- [Planned] [Policy] [Fix] Bind route v5, Plan/Arm capability snapshots, and materialization to the motion-capability artifact/digest; keep pre-motion fail-closed on missing/drifted/legacy controller-only references or absent hard-controller qualification.
- [计划] [eval] [test] 覆盖逐关节解析、source tamper、joint order、timing、runtime identity、enforcement 越权、route binding 和 pre-motion failure，并执行组合回归与五维审查。
- [Planned] [Eval] [Test] Cover per-joint parsing, source tampering, joint order, timing, runtime identity, enforcement escalation, route binding, and pre-motion failure, then run combined regression and the five-dimension review.

### 预期影响文件 / Planned Files

- `PhyAgentOS/forge/manipulation.py`
- `examples/forge-adapters/robotwin20/src/robotwin20_adapter/motion_capabilities.py`
- `examples/forge-adapters/robotwin20/scripts/materialize_motion_capability.py`
- `examples/forge-adapters/robotwin20/scripts/verify_motion_capability.py`
- `examples/forge-adapters/robotwin20/src/robotwin20_adapter/arm_candidates.py`
- `examples/forge-adapters/robotwin20/src/robotwin20_adapter/route_readiness.py`
- `examples/forge-adapters/robotwin20/scripts/materialize_complete_route.py`
- `examples/forge-adapters/robotwin20/runtime/robotwin_simulation_probe_worker.py`
- `examples/forge-adapters/robotwin20/profiles/robotwin20/`
- `examples/forge-adapters/robotwin20/tests/`
- `docs/forge/REAL_SPEED_LIMITS_ARCHITECTURE.md`

### 实际结果 / Actual Results

- [完成] [policy] [feat] 新增 `motion_capabilities.py` provider-owned v2 artifact：从真实 RoboTwin Franka checkout 的 URDF、embodiment profile、CuRobo profile、planner/simulator/drive-target source 和独立 runtime interpreter 导出逐关节 limits、timing、identity、source digest 和 enforcement semantics；controller period 无来源时保持 `null`。
- [Completed] [Policy] [Feat] Added the provider-owned v2 artifact in `motion_capabilities.py`: derived per-joint limits, timing, identity, source digests, and enforcement semantics from the real RoboTwin Franka checkout's URDF, embodiment/CuRobo profiles, planner/simulator/drive-target sources, and independent runtime interpreter; controller period remains `null` when unproven.
- [完成] [eval] [feat] 新增 `materialize_motion_capability.py` 和 `verify_motion_capability.py`，左右臂均通过 source revalidation，状态为 `validated_planner_constraints`；两份 validation 固定 `independent_execution_qualification=false`、`controller_enforced=false`、`motion_authorized=false`。
- [Completed] [Eval] [Feat] Added materialization and revalidation CLIs; both arms pass source revalidation with status `validated_planner_constraints`, while both validation records fix `independent_execution_qualification=false`, `controller_enforced=false`, and `motion_authorized=false`.
- [完成] [policy] [fix] route-request 升级为 v5、source-manifest 升级为 v3；materializer 交叉验证 runtime/arm planning embodiment identity，并绑定左右 capability 与 validation digest；simulation worker 在 world change 前拒绝缺少独立 controller qualification 的请求。
- [Completed] [Policy] [Fix] Upgraded route-request to v5 and source-manifest to v3; the materializer cross-binds runtime and arm-planning embodiment identity and records both capability/validation digests; the simulation worker rejects before world change without independent controller qualification.
- [完成] [refactor] [fix] 将 PAOS `ArmCapability` opaque 字段统一为 `motion_capabilities_ref`，删除旧 `controller_capabilities.py` 并同步文档，避免第二份速度事实源。
- [Completed] [Refactor] [Fix] Renamed the PAOS `ArmCapability` opaque field to `motion_capabilities_ref`, removed the legacy `controller_capabilities.py`, and synchronized documentation to avoid a second speed fact source.
- [完成] [eval] [test] 新增 capability、tamper、joint order、timing ambiguity、force-limit escalation、route binding 和 fail-closed 测试；组合回归 `718 passed, 1 skipped`，Ruff、compileall、`git diff --check` 全部通过。
- [Completed] [Eval] [Test] Added capability, tamper, joint-order, timing-ambiguity, force-limit-escalation, route-binding, and fail-closed tests; combined regression `718 passed, 1 skipped`, with Ruff, compileall, and `git diff --check` all passing.
- [完成] [eval] [exp] 真实 RoboTwin Franka no-motion artifact 位于 `/home/yanxu/robotwin20-runtime/artifacts/paos-motion-capability-v2-20260906TntyqoT/`；左臂 capability digest `8694c5593ca43abc764af3c1a933430a02924f819a1c07d0131af4fa1ad8d221`，右臂 `99575eb6c8aba0a6e43667b82360bf1dce5890abbfae63ce4e3920fff6826f6f`。
- [Completed] [Eval] [Exp] Real RoboTwin Franka no-motion artifacts are under `/home/yanxu/robotwin20-runtime/artifacts/paos-motion-capability-v2-20260906TntyqoT/`; left capability digest `8694c5593ca43abc764af3c1a933430a02924f819a1c07d0131af4fa1ad8d221`, right `99575eb6c8aba0a6e43667b82360bf1dce5890abbfae63ce4e3920fff6826f6f`.
- [完成] [eval] [exp] 基于 `blocks_ranking_rgb-0-1` 生成新 route package `/home/yanxu/robotwin20-runtime/artifacts/paos-route-v5-motion-capability-20260906T2ssZ1U/`；route digest `bf52b56e3d258789cb58f7cfc2fa7b7ec382771dedf3665a0762f0aba017ecae`，source manifest digest `5d62573d7d70649020ff6bcb8421b572d6d5a080c42787ff909942a23d45888a`，仍为 `pending_human_review` 且 `motion_authorized=false`。
- [Completed] [Eval] [Exp] Generated a new route package from `blocks_ranking_rgb-0-1` at `/home/yanxu/robotwin20-runtime/artifacts/paos-route-v5-motion-capability-20260906T2ssZ1U/`; route digest `bf52b56e3d258789cb58f7cfc2fa7b7ec382771dedf3665a0762f0aba017ecae`, source manifest digest `5d62573d7d70649020ff6bcb8421b572d6d5a080c42787ff909942a23d45888a`, still `pending_human_review` with `motion_authorized=false`.

### 五维验收 / Five-Dimension Review

- 架构集成：通过；MotionCapability 属于 RoboTwin adapter，PAOS Core/Planning 仅保存 opaque reference，未新增 lifecycle、Gateway 或 SQLite 事实源。
- Architecture integration: pass; MotionCapability belongs to the RoboTwin adapter, while PAOS Core/Planning stores only an opaque reference and no lifecycle, Gateway, or SQLite fact source was added.
- 失败路径：通过；source tamper、runtime/embodiment mismatch、joint-order/timing ambiguity、digest drift、force-limit claim 和缺 qualification 均 fail-closed。
- Failure paths: pass; source tampering, runtime/embodiment mismatch, joint-order/timing ambiguity, digest drift, force-limit claims, and missing qualification all fail closed.
- 权威边界：通过；当前 SAPIEN 仅 `planner_constrained`，Cartesian/effort 为 `unknown`；validation 和 route 均不授予 motion authority。
- Authority boundaries: pass; current SAPIEN is only `planner_constrained`, Cartesian/effort are `unknown`, and neither validation nor route grants motion authority.
- 配置与 provenance：通过；RoboTwin git revision、runtime versions、source paths/digests、左右 arm binding 和 route manifest 均可追溯，旧 approval 未复用。
- Configuration and provenance: pass; RoboTwin git revision, runtime versions, source paths/digests, both arm bindings, and route manifest are traceable, and no old approval was reused.
- 可维护性：通过；提取、验证、物化、route gate 分层，未来硬件 controller 仍须独立 provider qualification；未发现 Blocker/Major。
- Maintainability: pass; extraction, validation, materialization, and route gating are layered, future hardware controllers still require independent provider qualification, and no Blocker/Major was found.

### 验证边界 / Validation Boundary

- 未启动 Gateway、Dora、Action、真实仿真动作或硬件；未生成新的 motion approval。该阶段完成 provider artifact，不等同于 controller-enforced qualification 或完整抓取放置闭环。
- No Gateway, Dora, Action, real simulation motion, or hardware was started; no new motion approval was generated. This stage completes the provider artifact, not controller-enforced qualification or the full pick-place loop.

### Git 提交 / Git Commit

- Commit: `992d0de`
- Branch: `feature/long-horizon-workflow`

### 文件变更详情 / Detailed File Changes

- [新增] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/motion_capabilities.py:L1-L619`：实现 MotionCapability v2 的 provider extraction、canonical digest、source/runtime/joint/timing/drive 语义校验和 planner-only validation record。
- [Added] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/motion_capabilities.py:L1-L619`: implemented MotionCapability v2 provider extraction, canonical digesting, source/runtime/joint/timing/drive-semantics validation, and planner-only validation records.
- [新增] `examples/forge-adapters/robotwin20/scripts/materialize_motion_capability.py:L1-L39`、`verify_motion_capability.py:L1-L49`、`tests/test_motion_capabilities.py:L1-L285`：提供两阶段 CLI 和完整失败路径测试。
- [Added] `examples/forge-adapters/robotwin20/scripts/materialize_motion_capability.py:L1-L39`, `verify_motion_capability.py:L1-L49`, and `tests/test_motion_capabilities.py:L1-L285`: added the two-stage CLIs and complete failure-path tests.
- [修改] `PhyAgentOS/forge/manipulation.py:L90-L121`、`examples/forge-adapters/robotwin20/src/robotwin20_adapter/arm_candidates.py:L98-L170`、`profiles/robotwin20/manipulation-planning.yaml:L1-L25`：将 arm opaque capability reference 统一为 `motion_capabilities_ref`。
- [Modified] `PhyAgentOS/forge/manipulation.py:L90-L121`, `arm_candidates.py:L98-L170`, and `manipulation-planning.yaml:L1-L25`: unified the arm opaque capability reference as `motion_capabilities_ref`.
- [修改] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/route_readiness.py:L25-L250`、`route_generation.py:L190-L205`、`scripts/materialize_complete_route.py:L1-L590`：升级 route v5、绑定左右 capability/validation artifact，并交叉验证 runtime/arm profile identity。
- [Modified] `route_readiness.py:L25-L250`, `route_generation.py:L190-L205`, and `materialize_complete_route.py:L1-L590`: upgraded route v5, bound both capability/validation artifacts, and cross-validated runtime/arm-profile identity.
- [修改] `examples/forge-adapters/robotwin20/runtime/robotwin_simulation_probe_worker.py:L31-L40,L257-L275,L453-L516,L1210-L1220`、`tests/test_route_readiness.py:L50-L78`、`tests/test_simulation_probe.py:L27-L180,L285-L400`：校验 capability digest/arm coverage，并在 world change 前拒绝未获 controller qualification 的请求。
- [Modified] `robotwin_simulation_probe_worker.py:L31-L40,L257-L275,L453-L516,L1210-L1220`, `test_route_readiness.py:L50-L78`, and `test_simulation_probe.py:L27-L180,L285-L400`: validated capability digests/arm coverage and rejected requests without controller qualification before world change.
- [删除] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/controller_capabilities.py:L1-L147`、`tests/test_controller_capabilities.py:L1-L99`：移除与 MotionCapability 并行的旧速度事实源。
- [Deleted] `controller_capabilities.py:L1-L147` and `test_controller_capabilities.py:L1-L99`: removed the legacy parallel speed fact source.

## v5.7.9 (2026-09-06 00:10) - codex

### 预期修改 / Planned Changes

- [计划] [eval] [test] 为 simulation probe 的 `_execute_segment` 增加命令级 execution velocity scale 回归，验证传入机器人 API 的速度确实按 profile 缩放且非法值 fail-closed。
- [Planned] [Eval] [Test] Add command-level execution velocity scale regression coverage for simulation probe `_execute_segment`, proving robot API velocities are scaled by the profile and invalid values fail closed.
- [计划] [eval] [test] 增加线速度超限 failure artifact 的字段断言，确保 observed speed、limit、phase、step 与 scale 可审计保存。
- [Planned] [Eval] [Test] Assert linear-speed violation failure artifacts persist auditable observed speed, limit, phase, step, and scale fields.
- [计划] [policy] [fix] 修正 `_execute_segment` 在 `scene.step()` 后才计数导致的超限步数低报；每个已执行的仿真步（包括触发失败的步）必须先计入 evidence state。
- [Planned] [Policy] [Fix] Correct `_execute_segment` undercounting when a violation occurs after `scene.step()`; every executed simulator step, including the failing step, must be counted before evidence is emitted.

### 预期影响文件 / Planned Files

- `examples/forge-adapters/robotwin20/tests/test_simulation_probe.py`
- `examples/forge-adapters/robotwin20/runtime/robotwin_simulation_probe_worker.py`
- `docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md`
- `docs/forge/STATE_FILE_IMPLEMENTATION_REVIEW_20260903.md`

## v6.10.9 (2026-09-08 09:00) - codex

### 预期修改 / Planned Changes

- [计划] [agent] [feat] 为长程控制器增加 control-only 构造入口，并将 prompt_toolkit 交互层接入持久化 task status/pause/resume 命令；不新增 scheduler、store 或 Gateway 路径。
- [Planned] [Agent] [Feat] Add a control-only construction seam for the long-horizon controller and wire prompt_toolkit interaction to persisted task status/pause/resume commands without adding a scheduler, store, or Gateway path.
- [计划] [eval] [test] 增加 control-only、未知任务和交互命令解析测试；运行 no-motion planning/controller 回归、Ruff、compileall 与 diff check。
- [Planned] [Eval] [Test] Add control-only, unknown-task, and interactive-command parsing coverage; run no-motion planning/controller regression, Ruff, compileall, and diff checks.

### 预期影响文件 / Planned Files

- `PhyAgentOS/agent/long_horizon.py`
- `PhyAgentOS/cli/commands.py`
- `tests/test_long_horizon_controller.py`

### 实际结果 / Actual Results

- [完成] [agent] [feat] `PhyAgentOS/agent/long_horizon.py:L38-L82` 增加 control-only controller facade；`PhyAgentOS/cli/commands.py:L48-L270,L940-L1000` 增加 `paos task status|pause|resume` 与交互式 `/task ...`，全部通过 Coordinator 持久化状态，不创建第二套执行平面。
- [Completed] [Agent] [Feat] `PhyAgentOS/agent/long_horizon.py:L38-L82` adds a control-only controller facade; `PhyAgentOS/cli/commands.py:L48-L270,L940-L1000` adds `paos task status|pause|resume` and interactive `/task ...`, all using Coordinator-persisted state without a second execution plane.
- [完成] [eval] [test] 新增 control-only 与交互命令解析回归；planning/controller no-motion 专项 `47 passed, 2 warnings`，Ruff、compileall、`git diff --check` 通过。CLI help 在当前 Typer 0.9/Click 8.2 环境仍受既有 `Parameter.make_metavar(ctx)` 兼容错误影响，未归因于本 feature。
- [Completed] [Eval] [Test] Added control-only and interactive-command regressions; the no-motion planning/controller focus passes `47 passed, 2 warnings`, with Ruff, compileall, and `git diff --check` passing. CLI help remains affected by the pre-existing `Parameter.make_metavar(ctx)` incompatibility in the current Typer 0.9/Click 8.2 environment and is not attributed to this feature.
- [补充] [eval] [test] 根目录全量收集在当前环境因缺少既有依赖 `loguru` 于 `tests/test_state_file_authority_boundaries.py` 停止；未将依赖环境错误伪报为 feature 通过。
- [Supplement] [Eval] [Test] Full root-suite collection stopped in the current environment because the pre-existing `loguru` dependency is missing at `tests/test_state_file_authority_boundaries.py`; this environment issue is not reported as a feature pass.

### 六维验收 / Six-Dimension Acceptance

- 架构集成：通过；CLI/TUI 仅调用 controller，Coordinator 仍是 task/revision authority，PlanningLoopAdapter 仍是唯一执行 loop。
- 失败路径：通过本 feature 范围；未知 task、terminal、pause、resume、awaiting_replan 和 control-only 禁止执行均有覆盖；Gateway/Action 状态仍由既有 Coordinator 规则处理。
- 权威边界：通过；控制命令不调用 Tool、LiteLLM、Gateway，不把自然语言当生命周期事实。
- 配置与 provenance：通过；workspace/config 只用于定位既有 SQLite，结果仍绑定 task/revision/node。
- 可维护性：通过；无第二 scheduler/store，交互解析是薄适配层，保留 Textual 后续替换空间。
- Anti-OverDefense：通过；仅持久化 pause_requested 和复用现有事务，没有新增 hash、gate 或 UI 专用事实库。
- Architecture integration: pass; CLI/TUI calls only the controller, while Coordinator remains task/revision authority and PlanningLoopAdapter remains the sole execution loop.
- Failure paths: pass for this feature; unknown task, terminal, pause, resume, awaiting-replan, and control-only execution refusal are covered; existing Coordinator rules still own Gateway/Action state.
- Authority boundaries: pass; controls invoke no Tool, LiteLLM, or Gateway and do not treat natural language as lifecycle facts.
- Configuration/provenance: pass; workspace/config only locate the existing SQLite store, with results still bound to task/revision/node.
- Maintainability: pass; no second scheduler/store, the command parser is a thin adapter, and Textual can replace the surface later.
- Anti-OverDefense: pass; only the existing transactional `pause_requested` state is used, with no new hash, gate, or UI-specific fact store.

### Git 提交 / Git Commit

- Commit: `c60e082` (implementation), `649c846` (log)
- Branch: `feature/planning-loop`

## v6.10.10 (2026-09-08 10:30) - codex

### 预期修改 / Planned Changes

- [计划] [docs] [fix] 明确 Planner/Skill 插件的环境边界：PAOS 核心只消费 provider-neutral 结果，插件代码与依赖在插件自身 managed runtime 环境运行；不把插件依赖安装进 PAOS 环境。
- [Planned] [Docs] [Fix] Clarify Planner/Skill plugin environment boundaries: PAOS core consumes only provider-neutral results, while plugin code and dependencies run in the plugin's managed runtime environment; plugin dependencies are not installed into the PAOS environment.
- [计划] [eval] [test] 在 paos 环境重新运行 controller/planning no-motion 回归，并验证 CLI task help 与未知 task 的 fail-closed 行为。
- [Planned] [Eval] [Test] Re-run controller/planning no-motion regression in the paos environment and verify CLI task help plus fail-closed handling for unknown tasks.

### 预期影响文件 / Planned Files

- `docs/forge/PLANNING_MODULE_DESIGN.md`
- `docs/forge/MANIPULATION_DAG_DEVELOPER_GUIDE.md`

### 实际结果 / Actual Results

- [完成] [docs] [fix] 明确生产 Planner/Skill 插件运行在自身 managed runtime 环境，`paos.planners` entry point 仅作为 PAOS 内开发 seam；PAOS 核心只消费 provider-neutral planning projection。
- [Completed] [Docs] [Fix] Clarified that production Planner/Skill plugins run in their own managed runtime environment, while `paos.planners` is only an in-PAOS development seam; PAOS core consumes only provider-neutral planning projections.
- [完成] [eval] [test] 在 `/home/yanxu/miniconda3/envs/paos/bin/python` 下 planning/controller no-motion 专项 `47 passed`；Ruff、compileall、`git diff --check`、CLI task/root help 与未知 task 退出码 1 均通过。
- [Completed] [Eval] [Test] Under `/home/yanxu/miniconda3/envs/paos/bin/python`, the planning/controller no-motion focus passes `47 passed`; Ruff, compileall, `git diff --check`, CLI task/root help, and unknown-task exit code 1 all pass.

### 六维验收 / Six-Dimension Acceptance

- 架构集成：通过；插件环境边界与 Skill Runtime/Node 安装模型一致，不新增 scheduler/store/Gateway。
- 失败路径：通过；插件环境缺失、未知 task、control-only 执行均保持 fail-closed。
- 权威边界：通过；插件只返回规划投影，Coordinator、Tool API、Gateway 与运动权限所有权不变。
- 配置与 provenance：通过；插件私有依赖不进入 PAOS，结果仍绑定 task/revision。
- 可维护性：通过；开发 seam 与生产 runtime seam 明确分离，TUI 可独立替换。
- Anti-OverDefense：通过；未增加额外 hash、gate、冻结契约或事实库。
- Architecture integration: pass; the plugin boundary follows Skill Runtime/Node installation without adding a scheduler, store, or Gateway.
- Failure paths: pass; missing plugin environments, unknown tasks, and control-only execution remain fail-closed.
- Authority boundaries: pass; plugins return planning projections only, while Coordinator, Tool API, Gateway, and motion ownership remain unchanged.
- Configuration/provenance: pass; private plugin dependencies stay out of PAOS and results remain task/revision-bound.
- Maintainability: pass; development and production runtime seams are explicit and the TUI remains replaceable.
- Anti-OverDefense: pass; no extra hash, gate, frozen contract, or fact store was added.

### Git 提交 / Git Commit

- Commit: pending
- Branch: `feature/planning-loop`


## v5.10.2 (2026-09-06 14:45) - codex

### 预期修改 / Planned Changes

- [完成] [policy] [rollback] 撤销 RoboTwin 路线中无 provider 来源的统一 `0.20 m/s`、`1.0 rad/s` 执行缩放、轨迹重定时和 measured-speed 强失败门禁；保留历史 evidence 作为负证据，不删除历史记录。
- [Completed] [Policy] [Rollback] Remove provider-unbacked global `0.20 m/s`/`1.0 rad/s` execution scaling, trajectory retiming, and measured-speed hard-failure gates from the RoboTwin route; retain historical evidence as negative evidence without deleting history.
- [完成] [docs] [feat] 新增真正速度限制架构文档，基于 RoboTwin URDF/CuRobo/SAPIEN 与 Franka libfranka/frankx 的 provider-owned limits，明确 PAOS ownership、artifact、planner、controller、runtime evidence 和机械臂替换边界。
- [Completed] [Docs] [Feat] Add the real speed-limit architecture document based on RoboTwin URDF/CuRobo/SAPIEN and Franka libfranka/frankx provider-owned limits, defining PAOS ownership, artifacts, planner/controller/runtime evidence, and robot replacement boundaries.

### 预期影响文件 / Planned Files

- `examples/forge-adapters/robotwin20/runtime/robotwin_simulation_probe_worker.py`
- `examples/forge-adapters/robotwin20/runtime/trajectory_controller.py`
- `examples/forge-adapters/robotwin20/src/robotwin20_adapter/grasp_adaptation.py`
- `examples/forge-adapters/robotwin20/src/robotwin20_adapter/route_generation.py`
- `examples/forge-adapters/robotwin20/src/robotwin20_adapter/route_readiness.py`
- `examples/forge-adapters/robotwin20/src/robotwin20_adapter/route_inputs.py`
- `examples/forge-adapters/robotwin20/scripts/materialize_complete_route.py`
- `examples/forge-adapters/robotwin20/profiles/robotwin20/route-inputs.yaml`
- `docs/forge/REAL_SPEED_LIMITS_ARCHITECTURE.md`

### 实际结果 / Actual Results

- [完成] [policy] [rollback] route request 升级为 v4、route-input profile 升级为 v3、joint-limit policy 升级为 v2；从 pose、grasp adaptation、route generation、materializer 和 simulation worker 中删除无来源的 `0.20 m/s`、统一 `1.0 rad/s`、execution velocity scale、uniform retiming 和 measured-speed 强失败逻辑。
- [Completed] [Policy] [Rollback] Upgraded the route request to v4, route-input profile to v3, and joint-limit policy to v2; removed unbacked `0.20 m/s`, uniform `1.0 rad/s`, execution-velocity scaling, uniform retiming, and measured-speed hard-failure logic from poses, grasp adaptation, route generation, materialization, and the simulation worker.
- [完成] [safety] [fix] 删除伪 `SpeedBoundedExecutionController`；当前 simulation probe 在任何 world change 前因缺少 provider-owned motion-speed capability 明确 fail-closed，末端速度只保留为 finite diagnostic evidence。
- [Completed] [Safety] [Fix] Removed the pseudo `SpeedBoundedExecutionController`; the simulation probe now explicitly fails closed before any world change while provider-owned motion-speed capability is unavailable, retaining end-effector speed only as finite diagnostic evidence.
- [完成] [docs] [feat] 新增 `REAL_SPEED_LIMITS_ARCHITECTURE.md`，记录 RoboTwin URDF/CuRobo/SAPIEN、libfranka/frankx、PAOS artifact/ownership、仿真/硬件差异、机械臂替换、迁移顺序和五维审查。
- [Completed] [Docs] [Feat] Added `REAL_SPEED_LIMITS_ARCHITECTURE.md` covering RoboTwin URDF/CuRobo/SAPIEN, libfranka/frankx, PAOS artifact ownership, simulation/hardware differences, robot substitution, migration order, and the five-dimension review.

### 文件变更详情 / Detailed File Changes

- [修改] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/route_readiness.py:L25-L84`、`route_generation.py:L31-L56,L143-L148`、`grasp_adaptation.py:L201-L208,L309-L333`、`route_inputs.py:L16-L20,L205-L216,L312-L316`：删除 route pose 的硬编码速度字段并严格拒绝旧字段；route schema 升级为 v4。
- [Modified] `route_readiness.py:L25-L84`, `route_generation.py:L31-L56,L143-L148`, `grasp_adaptation.py:L201-L208,L309-L333`, and `route_inputs.py:L16-L20,L205-L216,L312-L316`: removed hard-coded speed fields from route poses, strictly reject legacy fields, and upgraded the route schema to v4.
- [修改] `examples/forge-adapters/robotwin20/profiles/robotwin20/route-inputs.yaml:L1-L52`、`scripts/materialize_complete_route.py:L82-L112,L308-L323,L352-L362`：删除 route/profile 的速度数字、scale、controller 伪绑定与自制 retiming；profile/joint policy schema 进位。
- [Modified] `route-inputs.yaml:L1-L52` and `materialize_complete_route.py:L82-L112,L308-L323,L352-L362`: removed route/profile speed numbers, scaling, pseudo-controller binding, and custom retiming, with profile/joint-policy schema revisions.
- [修改] `examples/forge-adapters/robotwin20/runtime/robotwin_simulation_probe_worker.py:L422-L482,L598-L645,L745-L822`：保留轨迹结构、position limits、finite state 与诊断测量，删除速度补偿/阈值，并在缺少 provider capability 时于 world change 前拒绝。
- [Modified] `robotwin_simulation_probe_worker.py:L422-L482,L598-L645,L745-L822`: retained trajectory structure, position limits, finite-state checks, and diagnostic measurement; removed speed compensation/thresholds and reject before world change when provider capability is absent.
- [删除] `examples/forge-adapters/robotwin20/runtime/trajectory_controller.py:L1-L179`、`examples/forge-adapters/robotwin20/tests/test_trajectory_controller.py:L1-L126`：删除不能证明 SAPIEN/Franka controller enforcement 的伪硬限速层及其自洽测试。
- [Deleted] `trajectory_controller.py:L1-L179` and `test_trajectory_controller.py:L1-L126`: removed the pseudo hard-speed layer that could not prove SAPIEN/Franka controller enforcement and its self-referential tests.
- [修改] `examples/forge-adapters/robotwin20/tests/test_grasp_adaptation.py:L45-L82`、`test_route_inputs.py:L51-L125`、`test_route_readiness.py:L24-L125`、`test_simulation_probe.py:L209-L373`：迁移 v4/v3/v2 契约，验证旧速度字段拒绝、缺 provider capability fail-closed 和 planner velocity 不被私自缩放。
- [Modified] Adapter tests migrate the v4/v3/v2 contracts and verify legacy-speed-field rejection, fail-closed behavior without provider capability, and absence of private planner-velocity scaling.
- [新增] `docs/forge/REAL_SPEED_LIMITS_ARCHITECTURE.md:L1-L301`；[修改] `docs/forge/PLANNING_MODULE_DESIGN.md:L42-L61`、`MANIPULATION_DAG_DEVELOPER_GUIDE.md:L106-L120`、`examples/forge-adapters/robotwin20/README.md:L418-L455`：建立新规范入口并标记旧 v3 evidence 为只读历史负证据。
- [Added] `REAL_SPEED_LIMITS_ARCHITECTURE.md:L1-L301`; [Modified] planning/developer/adapter documentation to establish the normative entry and mark old v3 evidence as read-only historical negative evidence.

### 关键 Diff / Key Diff

```text
Before: Route poses and profiles carried 0.20 m/s, uniform 1.0 rad/s,
        execution_velocity_scale, custom retiming, and a measured-speed gate
        presented as an execution-controller path.
After:  Route v4 contains geometry only; the unqualified controller layer is
        deleted; simulation fails closed before world change until an immutable,
        provider-derived MotionCapability artifact is implemented and bound.
```

### 五维审查 / Five-Dimension Review

- 架构集成：通过；速度真值回到 adapter/provider/controller，PAOS Core 只持有 capability artifact reference。
- 失败路径：通过；旧 schema/旧字段被拒绝，缺 provider capability 在 world change 前 fail-closed。
- 权威边界：通过；measurement 不再冒充 enforcement，Agent/Experience 不可修改物理限制。
- 配置：通过；当前 route/profile 不含无来源速度常量，历史 evidence 不覆盖、不复用。
- 可维护性：通过；删除平行 controller 伪实现，保留 provider-neutral contract 和后续 v2 artifact 迁移点。
- Architecture integration, failure paths, authority boundaries, configuration/provenance, and maintainability pass under the same boundaries.

### 验证 / Validation

- Adapter regression: `241 passed, 1 skipped`.
- Combined root/Skill/adapter regression: `717 passed, 1 skipped`.
- Ruff, compileall, and `git diff --check`: passed.
- 未启动 simulation probe、Gateway、Dora、Action 或硬件；没有生成或复用审批包。
- No simulation probe, Gateway, Dora, Action, or hardware was started; no approval package was generated or reused.

### Git 提交 / Git Commit

- Commit: `891dfea`
- Branch: `feature/long-horizon-workflow`

## v5.10.0 (2026-09-06 18:30) - codex

### 预期修改 / Planned Changes

- [完成] [policy] [feat] 复用现有 `CapabilitySnapshot/ArmCapability`，增加可选但严格校验的 adapter-owned `controller_capabilities_ref`；新增 RoboTwin controller capability 文档模型，区分 SDK、仿真 controller、benchmark policy 与 diagnostic threshold 的来源和执行语义，不新增第二套 PAOS 能力事实源。
- [Completed] [Policy] [Feat] Reused `CapabilitySnapshot/ArmCapability` with an optional but strictly validated adapter-owned `controller_capabilities_ref`; added the RoboTwin controller-capability document model with explicit SDK, simulator-controller, benchmark-policy, and diagnostic-threshold provenance without creating a second PAOS capability source of truth.
- [完成] [docs] [docs] 记录 RoboTwin 未接入 Franka SDK 的本地依赖/源码证据、PAOS 分层方案、双臂 per-arm capability 绑定及当前 `0.20 m/s` 的诊断属性，并引用 libfranka Robot API 的 rate-limiting/velocity-limit 接口。
- [Completed] [Docs] [Docs] Recorded local dependency/source evidence that RoboTwin does not use the Franka SDK, the PAOS layering plan, per-arm capability binding, and the diagnostic classification of `0.20 m/s`, with the libfranka Robot API rate-limiting and velocity-limit interfaces cited.
- [完成] [eval] [test] 增加 profile 字段、快照投影和 controller capability 文档的缺失/非法/未资格化 hard-bound 回归；保持 no-motion、`motion_authorized=false`，未启动 Gateway、Dora、仿真动作或硬件。
- [Completed] [Eval] [Test] Added regressions for profile fields, snapshot projection, and missing/invalid/unqualified hard-bound controller capability documents; preserved no-motion and `motion_authorized=false` without starting Gateway, Dora, simulation motion, or hardware.

### 预期影响文件 / Planned Files

- `PhyAgentOS/forge/manipulation.py`
- `examples/forge-adapters/robotwin20/src/robotwin20_adapter/arm_candidates.py`
- `examples/forge-adapters/robotwin20/profiles/robotwin20/manipulation-planning.yaml`
- `examples/forge-adapters/robotwin20/tests/test_arm_candidates.py`
- `docs/forge/PLANNING_MODULE_DESIGN.md`
- `docs/forge/MANIPULATION_DAG_DEVELOPER_GUIDE.md`
- `docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md`
- `docs/forge/STATE_FILE_IMPLEMENTATION_REVIEW_20260903.md`
- `changelog/2026-09_part3.md`

### 文件变更详情 / File Change Details

- [修改] `PhyAgentOS/forge/manipulation.py:L90-L121`：在既有 `ArmCapability` 中增加可选 `controller_capabilities_ref`，并严格限定为 `artifact://` 引用；不改变旧快照兼容性。
- [Modified] `PhyAgentOS/forge/manipulation.py:L90-L121`: added an optional `controller_capabilities_ref` to the existing `ArmCapability`, restricted to `artifact://` references, while preserving legacy snapshot compatibility.
- [修改] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/arm_candidates.py:L98-L167`：要求新的 adapter profile 明确每个 arm 的 controller capability artifact，并在 snapshot 投影中保留绑定。
- [Modified] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/arm_candidates.py:L98-L167`: required each new adapter profile to declare a controller-capability artifact and preserved the binding in snapshot projection.
- [新增] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/controller_capabilities.py:L1-L147`：新增严格的 controller capability schema、来源/执行语义校验、硬限制 qualification 引用和 canonical digest。
- [Added] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/controller_capabilities.py:L1-L147`: added the strict controller-capability schema, provenance/enforcement validation, hard-bound qualification reference, and canonical digest.
- [新增] `examples/forge-adapters/robotwin20/tests/test_controller_capabilities.py:L1-L99`：覆盖 diagnostic threshold、hard-bound qualification、摘要绑定、非法文档和 loader 失败路径。
- [Added] `examples/forge-adapters/robotwin20/tests/test_controller_capabilities.py:L1-L99`: covered diagnostic thresholds, hard-bound qualification, digest binding, invalid documents, and loader failure paths.
- [修改] `examples/forge-adapters/robotwin20/profiles/robotwin20/manipulation-planning.yaml:L6-L25`、`examples/forge-adapters/robotwin20/tests/test_arm_candidates.py:L43-L44`：为左右臂增加独立 controller capability artifact 引用。
- [Modified] `examples/forge-adapters/robotwin20/profiles/robotwin20/manipulation-planning.yaml:L6-L25` and `examples/forge-adapters/robotwin20/tests/test_arm_candidates.py:L43-L44`: added independent controller-capability artifact references for both arms.
- [修改] `docs/forge/PLANNING_MODULE_DESIGN.md:L27-L55`、`docs/forge/MANIPULATION_DAG_DEVELOPER_GUIDE.md:L101-L123`、`docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md:L1209-L1237`、`docs/forge/STATE_FILE_IMPLEMENTATION_REVIEW_20260903.md:L1204-L1230`：写入 RoboTwin/Franka SDK 证据和 PAOS capability/controller 分层。
- [Modified] `docs/forge/PLANNING_MODULE_DESIGN.md:L27-L55`, `docs/forge/MANIPULATION_DAG_DEVELOPER_GUIDE.md:L101-L123`, `docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md:L1209-L1237`, and `docs/forge/STATE_FILE_IMPLEMENTATION_REVIEW_20260903.md:L1204-L1230`: documented RoboTwin/Franka SDK evidence and the PAOS capability/controller layering.

### 关键代码 Diff / Key Diff

**修改前：** `ArmCapability` 只绑定 planner、workspace 和 joint-limit artifact。

**修改后：** `ArmCapability.controller_capabilities_ref` 绑定 adapter-owned controller capability artifact；详细 SDK/仿真限制仍不进入 PAOS Core。

**修改前：** RoboTwin arm profile 没有 controller capability provenance。

**修改后：** 左右臂 profile 分别声明 `artifact://robotwin/franka/{left,right}-controller-capabilities`，并由 snapshot digest 继承绑定。

### 五维审查 / Five-Dimension Review

- 架构集成：通过；复用既有 capability snapshot，没有新增生命周期、Planner service、锁或执行路径。
- 失败路径：通过；缺失引用、非法 source/enforcement、未资格化 hard bound 和 loader 输入均 fail-closed。
- 权威边界：通过；详细限制仍由 adapter/controller artifact 所有，Planning 只消费引用，Experience 不可修改。
- 配置与 provenance：通过；每个 arm 有独立 artifact ref，来源类型和 controller enforcement 显式区分；当前 `0.20 m/s` 明确为 diagnostic threshold。
- 可维护性：通过；RoboTwin controller 文档模型独立于 PAOS Core，未来真实 Franka adapter 可复用相同投影；没有重复 `MotionLimit` 事实源。
- Architecture integration: pass; existing capability snapshots are reused without a new lifecycle, Planner service, lock, or execution path.
- Failure paths: pass; missing refs, invalid source/enforcement, unqualified hard bounds, and invalid loader inputs fail closed.
- Authority boundaries: pass; detailed limits remain adapter/controller-owned, Planning only consumes references, and Experience cannot modify them.
- Configuration/provenance: pass; each arm has an independent artifact reference with explicit source and enforcement semantics; `0.20 m/s` is classified as a diagnostic threshold.
- Maintainability: pass; the RoboTwin controller document is independent from PAOS Core, future Franka adapters can reuse the projection, and no duplicate `MotionLimit` source of truth was introduced.

### 验证 / Validation

- `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=.:examples/forge-adapters/robotwin20/src:examples/forge-adapters/robotwin20/runtime:examples/forge-skills/pick-place-workflow/src python -m pytest -p pytest_asyncio.plugin -q tests examples/forge-skills/pick-place-workflow/tests examples/forge-adapters/robotwin20/tests`：`732 passed, 1 skipped`。
- `python -m ruff check ...`：通过。
- `python -m compileall -q ...`：通过。
- `git diff --check`：通过。
- RoboTwin20 Python capability check：`sapien=True`, `mplib=True`, `curobo=True`, `frankx=False`, `libfranka=False`, `franka_ros=False`。
- libfranka upstream header check：`Robot API` exposes `limit_rate` controls and `getUpperJointVelocityLimits/getLowerJointVelocityLimits` APIs。

### Git 提交 / Git Commit

- Commit: pending (to be recorded after verification)
- Branch: `feature/long-horizon-workflow`

### 实际结果 / Actual Results

- [完成] [eval] [test] `examples/forge-adapters/robotwin20/tests/test_simulation_probe.py:L263-L297,L357-L468,L470-L535` 增加非法 scale、命令速度缩放、线速度违规字段和失败 artifact 持久化回归；专项 `31 passed`。
- [Completed] [Eval] [Test] `examples/forge-adapters/robotwin20/tests/test_simulation_probe.py:L263-L297,L357-L468,L470-L535` adds invalid-scale, command-velocity scaling, linear-speed violation fields, and failure-artifact persistence regressions; focused suite passes `31 passed`.
- [完成] [policy] [fix] `examples/forge-adapters/robotwin20/runtime/robotwin_simulation_probe_worker.py:L779-L799` 在 `scene.step()` 后立即计入 `simulator_steps`，避免触发超限的已执行步被低报；线速度门限和 fail-closed 行为不变。
- [Completed] [Policy] [Fix] `examples/forge-adapters/robotwin20/runtime/robotwin_simulation_probe_worker.py:L779-L799` increments `simulator_steps` immediately after `scene.step()` so an executed violating step is not under-counted; the linear-speed gate and fail-closed behavior are unchanged.
- [完成] [docs] [fix] `docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md:L1047-L1051`、`docs/forge/STATE_FILE_IMPLEMENTATION_REVIEW_20260903.md:L1063-L1066` 记录 step evidence 计数修复及其非放宽安全门禁的边界。
- [Completed] [Docs] [Fix] `docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md:L1047-L1051` and `docs/forge/STATE_FILE_IMPLEMENTATION_REVIEW_20260903.md:L1063-L1066` document the step-evidence accounting fix and its unchanged safety boundary.
- [完成] [eval] [test] 全量组合回归 `716 passed, 1 skipped`；Ruff、compileall、`git diff --check` 均通过。尚未运行 v7 simulation probe，因为新的人工 simulation-only approval 尚未提供。
- [Completed] [Eval] [Test] Full combined regression passes `716 passed, 1 skipped`; Ruff, compileall, and `git diff --check` pass. The v7 simulation probe has not run because fresh human simulation-only approval is not yet provided.

### 文件变更详情 / File Change Details

#### [修改] `examples/forge-adapters/robotwin20/runtime/robotwin_simulation_probe_worker.py:L779-L799`

**修改前：** `scene.step()` 后先计算速度，只有通过速度检查后才递增 `simulator_steps`。

**修改后：** `scene.step()` 后立即递增 `simulator_steps`，随后计算并审计速度；失败步的 `step` 与总计数均包含该已执行仿真步。

**修改说明：** 修正 failure evidence 的计数准确性，不改变速度限制、workspace、joint limit 或 motion authority。

#### [新增测试] `examples/forge-adapters/robotwin20/tests/test_simulation_probe.py:L357-L468,L470-L535`

**新增内容：** 验证 profile scale 传入 `set_arm_joints()` 前真实缩放；验证速度违规保存 phase/step/observed/limit/scale；验证 failure artifact 原样保留这些字段。

### 五维审查 / Five-Dimension Review

- 架构集成：通过；修复仅位于独立 simulation probe worker 的 evidence bookkeeping，不接管 PAOS AgentTask、PlanRevision、Gateway、Dora 或 Verifier。
- 失败路径：通过；非法 scale、超限步、失败恢复和 immutable failure artifact 均有回归覆盖；超限仍返回 `unavailable`。
- 权威边界：通过；`execution_velocity_scale` 仍是 adapter/profile-owned policy，`0.20 m/s` 仍为不可变运行时 gate，未获得生产 motion authority。
- 配置：通过；scale 从 materialized joint-limit policy 读取并严格绑定 request；没有新增 Markdown 配置源。
- 可维护性：通过；单元测试隔离命令、仿真计数和 artifact 持久化，文档明确计数修复不等于安全门禁放宽。
- Architecture integration: pass; the fix is limited to evidence bookkeeping in the independent simulation probe worker and does not take ownership of PAOS AgentTask, PlanRevision, Gateway, Dora, or Verifier.
- Failure paths: pass; invalid scale, violating step, recovery, and immutable failure-artifact paths are covered; violations still return `unavailable`.
- Authority boundaries: pass; `execution_velocity_scale` remains adapter/profile-owned policy, `0.20 m/s` remains the immutable runtime gate, and no production motion authority is granted.
- Configuration: pass; scale is read from and bound to the materialized joint-limit policy without adding a Markdown configuration source.
- Maintainability: pass; command, simulator-count, and artifact-persistence tests are isolated, and docs state that accounting correction is not a safety-limit relaxation.

### Git 提交 / Git Commit

- Commit: pending (to be recorded after verification)
- Branch: `feature/long-horizon-workflow`

### 实际结果 / Actual Results

- [完成] [policy] [fix] `examples/forge-adapters/robotwin20/runtime/robotwin_simulation_probe_worker.py:L587-L631,L749-L799` 接入并校验 profile-owned `execution_velocity_scale=0.25`，在机器人命令前缩放 drive velocity；保留 `max_linear_speed_mps=0.20` 不可变门禁，并在超限时保存 phase/step/observed/limit/scale。
- [Completed] [Policy] [Fix] `examples/forge-adapters/robotwin20/runtime/robotwin_simulation_probe_worker.py:L587-L631,L749-L799` validates the profile-owned `execution_velocity_scale=0.25` and scales drive velocity before robot commands; the immutable `max_linear_speed_mps=0.20` gate remains, with phase/step/observed/limit/scale saved on violations.
- [完成] [config] [feat] `examples/forge-adapters/robotwin20/profiles/robotwin20/route-inputs.yaml:L40-L51` 与 materializer `.../scripts/materialize_complete_route.py:L310-L335` 严格绑定并验证 scale；未修改 joint limits、workspace、URDF 或 motion authority。
- [Completed] [Config] [Feat] `examples/forge-adapters/robotwin20/profiles/robotwin20/route-inputs.yaml:L40-L51` and `.../scripts/materialize_complete_route.py:L310-L335` bind and validate the scale strictly; joint limits, workspace, URDF, and motion authority are unchanged.
- [完成] [eval] [test] `examples/forge-adapters/robotwin20/tests/test_simulation_probe.py:L263-L297,L357-L468,L470-L535` 覆盖非法 scale、命令缩放和 failure artifact；全量组合回归 `716 passed, 1 skipped`，Ruff、compileall、`git diff --check` 通过。
- [Completed] [Eval] [Test] `examples/forge-adapters/robotwin20/tests/test_simulation_probe.py:L263-L297,L357-L468,L470-L535` covers invalid scale, command scaling, and failure artifacts; full combined regression passes `716 passed, 1 skipped`, with Ruff, compileall, and `git diff --check` passing.
- [完成] [eval] [exp] 新 v7 route 已 materialize，但仍为 `pending_human_review`；在收到新的 route/source-manifest 审批前未运行 single-use simulation probe，未接入 Gateway、Dora、Action 或硬件。
- [Completed] [Eval] [Exp] The fresh v7 route is materialized but remains `pending_human_review`; no single-use simulation probe ran before fresh route/source-manifest approval, and Gateway, Dora, Action, and hardware remain disconnected.

### 五维审查 / Five-Dimension Review

- 架构集成：通过；速度缩放和计数修复仅位于 adapter-owned independent simulation probe，不迁移 PAOS 生命周期或执行权威。
- 失败路径：通过；非法配置、速度超限、失败恢复和 artifact 持久化均 fail-closed 且有测试。
- 权威边界：通过；速度上限仍由 route/runtime gate 执行，profile scale 不能授权生产动作。
- 配置：通过；参数来自实体化 policy artifact，request/manifest digest 绑定保持不变。
- 可维护性：通过；新增单元测试和文档说明了 observed evidence 与安全门禁的区别。
- Architecture integration: pass; scaling and step-accounting fixes stay in the adapter-owned independent simulation probe and do not move PAOS lifecycle or execution authority.
- Failure paths: pass; invalid configuration, speed violation, recovery, and artifact persistence fail closed with test coverage.
- Authority boundaries: pass; the route/runtime gate still owns the speed limit, and the profile scale cannot authorize production motion.
- Configuration: pass; parameters come from the materialized policy artifact with request/manifest digest binding preserved.
- Maintainability: pass; tests and docs distinguish observed evidence from safety-gate authorization.

## v5.7.8 (2026-09-05 23:35) - codex

### 预期修改 / Planned Changes

- [计划] [eval] [fix] 分析并修复 simulation probe 在 contact 阶段的实际末端线速度超限：现有 retiming 只约束关节速度，SAPIEN 驱动执行仍可能产生超过 `0.2 m/s` 的末端速度；增加 profile-owned execution velocity scale，保持线速度上限不可变。
- [Planned] [Eval] [Fix] Diagnose and fix the contact-phase simulator end-effector speed violation: current retiming constrains joint speed only, while SAPIEN drive execution can still exceed `0.2 m/s`; add a profile-owned execution velocity scale while keeping the linear-speed limit immutable.
- [计划] [eval] [test] 为 execution velocity scale、非法配置、命令绑定和实际线速度记录增加回归测试；使用全新 route/artifact root 重新 materialize 和 no-motion preflight，不复用已消费 v3 审批。
- [Planned] [Eval] [Test] Add regression coverage for execution velocity scale, invalid configuration, command binding, and observed linear-speed recording; rematerialize under a fresh route/artifact root and rerun no-motion preflight without reusing the consumed v3 approval.

### 预期影响文件 / Planned Files

- `examples/forge-adapters/robotwin20/profiles/robotwin20/route-inputs.yaml`
- `examples/forge-adapters/robotwin20/runtime/robotwin_simulation_probe_worker.py`
- `examples/forge-adapters/robotwin20/scripts/materialize_complete_route.py`
- `examples/forge-adapters/robotwin20/tests/`
- `docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md`
- `docs/forge/STATE_FILE_IMPLEMENTATION_REVIEW_20260903.md`
- `changelog/2026-09_part3.md`

### 实际结果 / Actual Results

- [完成] [policy] [fix] 已完成 profile-owned execution velocity scale、严格 materializer 校验、命令级缩放和超限 evidence 记录；线速度上限仍不可变。
- [Completed] [Policy] [Fix] Implemented profile-owned execution velocity scaling, strict materializer validation, command-level scaling, and violation evidence recording; the linear-speed limit remains immutable.
- [完成] [eval] [test] 新增并通过非法配置、命令缩放、线速度违规和 artifact 持久化回归；全量组合回归 `716 passed, 1 skipped`，Ruff、compileall、`git diff --check` 均通过。
- [Completed] [Eval] [Test] Added and passed invalid-configuration, command-scaling, linear-speed-violation, and artifact-persistence regressions; full combined regression `716 passed, 1 skipped`, with Ruff, compileall, and `git diff --check` passing.
- [完成] [eval] [exp] v7 route 已重新 materialize，但尚未取得新的人工 simulation-only approval，因此未运行 probe；Gateway/Dora/Action/硬件均未启用。
- [Completed] [Eval] [Exp] The v7 route was rematerialized but has no fresh human simulation-only approval, so the probe was not run; Gateway/Dora/Action/hardware remain disabled.

### Git 提交 / Git Commit

- Commit: `f4222d1`
- Branch: `feature/long-horizon-workflow`

## v5.7.7 (2026-09-05 23:15) - codex

### 预期修改 / Planned Changes

- [计划] [chore] [docs] 回写 v5.7.6 simulation-only probe 记录的提交哈希；不修改实现、证据或安全边界。
- [Planned] [Chore] [Docs] Record the v5.7.6 simulation-only probe commit hash; do not change implementation, evidence, or safety boundaries.

### 预期影响文件 / Planned Files

- `CHANGELOG.md`
- `changelog/2026-09_part3.md`

### 实际结果 / Actual Results

- [完成] [chore] [docs] v5.7.6 已提交为 `d5aa0de` 并推送到 `feature/long-horizon-workflow`。
- [Completed] [Chore] [Docs] v5.7.6 was committed as `d5aa0de` and pushed to `feature/long-horizon-workflow`.

### Git 提交 / Git Commit

- Commit: pending
- Branch: `feature/long-horizon-workflow`

## v5.7.6 (2026-09-05 23:00) - codex

### 预期修改 / Planned Changes

- [计划] [eval] [exp] 使用 reviewer 已批准的 v3 route package，运行一次独立 simulation-only probe；严格绑定 route/source-manifest digest，保存不可变 probe response 与 evidence manifest，不启用 Gateway、Dora 或硬件动作。
- [Planned] [Eval] [Exp] Run one independent simulation-only probe using the reviewer-approved v3 route package; bind route/source-manifest digests strictly and persist immutable probe response and evidence manifest without enabling Gateway, Dora, or hardware motion.
- [计划] [docs] [fix] 更新当前诊断与执行文档，记录审批、probe 结果及仍未关闭的物理/语义证据门禁。
- [Planned] [Docs] [Fix] Update the current diagnosis and execution documents with the approval, probe result, and any remaining physical/semantic evidence gates.

### 预期影响文件 / Planned Files

- `/home/yanxu/robotwin20-runtime/artifacts/paos-simulation-probe-20260905T*/`
- `docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md`
- `docs/forge/STATE_FILE_IMPLEMENTATION_REVIEW_20260903.md`
- `changelog/2026-09_part3.md`

### Git 提交 / Git Commit

- Commit: `d5aa0de`
- Branch: `feature/long-horizon-workflow`

### 实际结果 / Actual Results

- [完成] [eval] [exp] reviewer `yanxu` 已批准 v3 route package 的 simulation-only probe；审批记录写入 `/home/yanxu/robotwin20-sim-probe-20260905T230500Z/probe/approval.json`，绑定 route digest `b253fdc0f58a683ca6c73b33853a95f5655af3f0d7af78f8416c03096ed8e85e`、source manifest digest `9d4f599d703170fb2a4b24b0b4b7bbb68588d39b8f24c5e0ba2bc4e640fd9b93`。
- [Completed] [Eval] [Exp] Reviewer `yanxu` approved one simulation-only probe for the v3 route package; the approval was written to `/home/yanxu/robotwin20-sim-probe-20260905T230500Z/probe/approval.json`, bound to route digest `b253fdc0f58a683ca6c73b33853a95f5655af3f0d7af78f8416c03096ed8e85e` and source-manifest digest `9d4f599d703170fb2a4b24b0b4b7bbb68588d39b8f24c5e0ba2bc4e640fd9b93`.
- [完成] [eval] [exp] 独立 RoboTwin20 Python 3.10 worker 已启动隔离仿真并执行一次候选；结果为 `unavailable`，`failed_phase=contact`，`error_detail=simulator motion exceeds waypoint linear-speed limit`，`simulator_steps=834`，`world_change_started=true`、`world_change_completed=false`、`simulation_reset_status=completed`、`reconciliation_required=false`。probe response、before/after snapshot、contact trace 和 failure artifact 均已保存在同一新 artifact root；未接入 Gateway、Dora、Action 或硬件。
- [Completed] [Eval] [Exp] The independent RoboTwin20 Python 3.10 worker started the isolated simulation and attempted the candidate once; the result was `unavailable` with `failed_phase=contact`, `error_detail=simulator motion exceeds waypoint linear-speed limit`, `simulator_steps=834`, `world_change_started=true`, `world_change_completed=false`, `simulation_reset_status=completed`, and `reconciliation_required=false`. The probe response, before/after snapshots, contact trace, and failure artifact were preserved under the new artifact root; Gateway, Dora, Action, and hardware were not connected.
- [完成] [docs] [fix] `docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md:L980-L1026` 修正规划模块历史章节的残留未完成描述，并新增 v3 probe 结果、失败原因、证据路径和下一门禁；`docs/forge/STATE_FILE_IMPLEMENTATION_REVIEW_20260903.md:L984-L1045` 新增审批、负结果及五维边界审查。
- [Completed] [Docs] [Fix] `docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md:L980-L1026` corrected the stale outstanding-status wording in the planning historical section and added the v3 probe result, failure cause, evidence paths, and next gate; `docs/forge/STATE_FILE_IMPLEMENTATION_REVIEW_20260903.md:L984-L1045` added the approval, negative result, and five-dimension boundary review.
- [完成] [eval] [test] 配置错误（worker Python 不存在、runtime root 指向上级目录）均在启动前 fail-closed；修正为 `/home/yanxu/miniconda3/envs/RoboTwin20/bin/python3.10` 与 `/home/yanxu/robotwin20-runtime/RoboTwin` 后成功启动并得到可审计负证据。`git diff --check` 与相关 probe/approval 测试已在本次提交前复跑通过。
- [Completed] [Eval] [Test] Configuration errors (missing worker Python and parent-directory runtime root) failed closed before startup; after binding `/home/yanxu/miniconda3/envs/RoboTwin20/bin/python3.10` and `/home/yanxu/robotwin20-runtime/RoboTwin`, the worker started and produced auditable negative evidence. `git diff --check` passed; focused probe/approval tests were rerun before commit.

## v5.7.5 (2026-09-05 22:30) - codex

### 预期修改 / Planned Changes

- [计划] [chore] [docs] 回写 v5.7.4 日志提交哈希；不修改实现、测试或安全边界。
- [Planned] [Chore] [Docs] Record the v5.7.4 log commit hash; do not change implementation, tests, or safety boundaries.

### 预期影响文件 / Planned Files

- `changelog/2026-09_part3.md`

### 实际结果 / Actual Results

- [完成] [chore] [docs] v5.7.4 的提交哈希已回写为 `2f1f31f`。
- [Completed] [Chore] [Docs] Recorded `2f1f31f` as the v5.7.4 commit hash.

### Git 提交 / Git Commit

- Commit: pending
- Branch: `feature/long-horizon-workflow`

## v5.7.4 (2026-09-05 22:25) - codex

### 预期修改 / Planned Changes

- [计划] [chore] [docs] 回写 v5.7.3 规划模块集成验收的实际提交哈希并验证远端分支；不修改实现、测试或安全边界。
- [Planned] [Chore] [Docs] Record the actual commit hash for the v5.7.3 planning-module integration acceptance and verify the remote branch; do not change implementation, tests, or safety boundaries.

### 预期影响文件 / Planned Files

- `changelog/2026-09_part3.md`
- `CHANGELOG.md`

### 实际结果 / Actual Results

- [完成] [chore] [docs] `changelog/2026-09_part3.md:L3-L48`、`CHANGELOG.md:L5-L25` 将 v5.7.3 的 `Commit: pending` 更新为 `f244751`；实现提交已推送到 `feature/long-horizon-workflow`，工具目录 `.codegraph/`、`.cursor/` 未跟踪且未提交。
- [Completed] [Chore] [Docs] Updated v5.7.3 `Commit: pending` to `f244751` in `changelog/2026-09_part3.md:L3-L48` and `CHANGELOG.md:L5-L25`; the implementation commit was pushed to `feature/long-horizon-workflow`, while `.codegraph/` and `.cursor/` remain untracked and uncommitted.

### Git 提交 / Git Commit

- Commit: `2f1f31f`
- Branch: `feature/long-horizon-workflow`

## v5.7.3 (2026-09-05 22:15) - codex

### 预期修改 / Planned Changes

- [计划] [docs] [fix] 修正规划模块历史验收章节对 live ToolSpec projection、AgentLoop dispatch 和 Experience policy candidate 状态的过时描述，明确当前有效门禁为独立 readiness/simulation evidence；不修改代码、运行时或动作权限。
- [Planned] [Docs] [Fix] Correct stale status wording in the planning-module historical acceptance section for live ToolSpec projection, AgentLoop dispatch, and Experience policy candidates, and state that the current gate is independent readiness/simulation evidence; do not change code, runtime, or motion authority.
- [计划] [eval] [test] 增加一个 no-motion 集成验收，串联 Agent-composed PlanGraph、Coordinator 持久化、AgentLoop dispatch、动态 Tool admission、结算/重规划和 Experience candidate 审核晋升；不调用真实 Gateway、Dora 或硬件。
- [Planned] [Eval] [Test] Add a no-motion integration acceptance that exercises Agent-composed PlanGraph, Coordinator persistence, AgentLoop dispatch, dynamic Tool admission, settlement/replanning, and Experience candidate review/promotion without calling a real Gateway, Dora, or hardware.

### 预期影响文件 / Planned Files

- `docs/forge/STATE_FILE_IMPLEMENTATION_REVIEW_20260903.md`
- `docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md`
- `CHANGELOG.md`
- `tests/test_planning_end_to_end.py`

### 实际结果 / Actual Results

- [完成] [docs] [fix] `docs/forge/STATE_FILE_IMPLEMENTATION_REVIEW_20260903.md:L958-L986` 将历史 32.5 节的过时“待完成”列表改为状态纠正，明确 live ToolSpec projection、AgentLoop dispatch 和 Experience policy candidate 已在 v5.7.0/v5.7.1 完成；`docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md:L966-L968` 增加历史章节说明，当前门禁仍以独立 readiness/simulation evidence 和人工审核为准。
- [Completed] [Docs] [Fix] `docs/forge/STATE_FILE_IMPLEMENTATION_REVIEW_20260903.md:L958-L986` corrects the stale “remaining work” list in historical section 32.5, confirming that live ToolSpec projection, AgentLoop dispatch, and Experience policy candidates were completed in v5.7.0/v5.7.1; `docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md:L966-L968` adds a historical-section note, with independent readiness/simulation evidence and human review remaining the current gate.
- [完成] [eval] [test] `tests/test_planning_end_to_end.py:L1-L246` 新增 no-motion 有效接入验收：真实 `PlanGraph` 经 Coordinator 持久化并激活 dispatch，验证 ready set、动态 Query/Action admission、失败 settlement、`ReplanDelta` revision 适配，以及 Experience 独立 replay、人工审核和 callback promotion；专项规划集成 `30 passed`，组合回归 `709 passed, 1 skipped`，无 Gateway/Dora/硬件调用。
- [Completed] [Eval] [Test] `tests/test_planning_end_to_end.py:L1-L246` adds a no-motion integration acceptance: a real `PlanGraph` is persisted by the Coordinator and activated through dispatch, validating ready sets, dynamic Query/Action admission, failed settlement, `ReplanDelta` revision adaptation, and Experience independent replay, human review, and callback promotion; planning integration focus `30 passed`, combined regression `709 passed, 1 skipped`, with no Gateway/Dora/hardware calls.

### 五维验收 / Five-Dimension Acceptance

- 架构集成：通过；测试走现有 Coordinator/AgentLoop/Experience 边界，planning 不接管生命周期或执行。
- 失败路径：通过；缺失绑定、失败结算、重规划和候选审核门禁均被验证。
- 权威边界：通过；全链路保持 `motion_authorized=false`，未调用 transport。
- 配置与 provenance：通过；graph、node、ToolSpec、trace、replay 和 promotion 均有 digest/artifact 绑定。
- 可维护性：通过；新增测试为独立 no-motion 集成夹具，不作为生产 provider 或第二状态源。
- Architecture integration: pass; the test uses existing Coordinator/AgentLoop/Experience boundaries, while planning owns no lifecycle or execution.
- Failure paths: pass; missing bindings, failed settlement, replanning, and candidate review gates are exercised.
- Authority boundaries: pass; the whole flow keeps `motion_authorized=false` and performs no transport call.
- Configuration and provenance: pass; graph, node, ToolSpec, trace, replay, and promotion use digest/artifact bindings.
- Maintainability: pass; the test is an isolated no-motion integration fixture, not a production provider or second state source.

### 关键 Diff / Key Diff

```text
Before: historical planner status could be read as if dispatch/policy-candidate work were still pending; no single integration test exercised all planner seams.
After: historical status is explicitly corrected, and tests/test_planning_end_to_end.py validates the complete no-motion planner integration and evolution gates.
```

### Git 提交 / Git Commit

- Commit: `f244751`
- Branch: `feature/long-horizon-workflow`

## v5.7.2 (2026-09-05 22:05) - codex

### 预期修改 / Planned Changes

- [计划] [docs] [docs] 为 v5.7.1 规划模块收口记录补充精确行号和关键 diff，满足项目日志审计格式；不修改实现、测试或安全边界。
- [Planned] [Docs] [Docs] Add exact line ranges and key diffs to the v5.7.1 planning-module closeout record to satisfy the project audit format; do not change implementation, tests, or safety boundaries.

### 预期影响文件 / Planned Files

- `changelog/2026-09_part3.md`
- `CHANGELOG.md`

### 实际结果 / Actual Results

- [完成] [docs] [docs] `changelog/2026-09_part3.md:L3-L14` 新增本 patch 的计划记录；`changelog/2026-09_part3.md:L15-L47` 和 `CHANGELOG.md:L5-L32` 补充收口验收的精确行号、测试结果与关键 diff，未修改实现或安全边界。
- [Completed] [Docs] [Docs] `changelog/2026-09_part3.md:L3-L14` adds this patch's plan record; `changelog/2026-09_part3.md:L15-L47` and `CHANGELOG.md:L5-L32` add exact line ranges, test results, and key diffs for the closeout acceptance without changing implementation or safety boundaries.

### 关键 Diff / Key Diff

```text
Before: v5.7.1 closeout entries described the review and tests without exact line ranges.
After:  v5.7.1 entries identify the precise changelog ranges and preserve implementation commit aadc833.
```

### Git 提交 / Git Commit

- Previous implementation commit: `aadc833`
- Branch: `feature/long-horizon-workflow`

## v5.7.1 (2026-09-05 22:00) - codex

### 预期修改 / Planned Changes

- [计划] [eval] [review] 对规划模块文档、实现和全量回归进行最终收口审查，确认未完成项仅限于文档明确后置的真实 Gateway/Dora/Action/仿真运动与 benchmark 证据；回写本轮实现提交哈希。
- [Planned] [Eval] [Review] Perform final closeout review of the planning-module docs, implementation, and full regression, confirming that only the explicitly deferred real Gateway/Dora/Action/simulation-motion and benchmark evidence remain; record the implementation commit hash.
- [计划] [chore] [docs] 将 `CHANGELOG.md` 与本月日志中 v5.7.0 的 `Commit: pending` 更新为实际提交，并保持 `.codegraph/`、`.cursor/` 等工具目录不进入提交。
- [Planned] [Chore] [Docs] Replace v5.7.0 `Commit: pending` markers in `CHANGELOG.md` and the monthly log with the actual commit while keeping tool directories such as `.codegraph/` and `.cursor/` out of the commit.

### 预期影响文件 / Planned Files

- `changelog/2026-09_part3.md`
- `CHANGELOG.md`

### 实际结果 / Actual Results

- [完成] [eval] [review] `changelog/2026-09_part3.md:L32-L69`、`CHANGELOG.md:L16-L32` 记录并复核规划模块文档、实现与入口：未发现 Blocker/Major；`PhyAgentOS/planning` 仍为纯协议/计算库，`PlanRevision`/SQLite 仍是生命周期事实源，Registry guard 只做执行前准入，Experience promotion 仍需独立 replay、人工审核和 Skill Runtime callback。真实 Gateway/Dora/Action、仿真运动和 benchmark evidence 继续按设计后置。
- [Completed] [Eval] [Review] Reviewed the planning-module docs, implementation, and entry points item by item: no Blocker/Major found; `PhyAgentOS/planning` remains a pure protocol/calculation library, `PlanRevision`/SQLite remain the lifecycle authority, the registry guard only performs pre-execution admission, and Experience promotion still requires independent replay, human review, and a Skill Runtime callback. Real Gateway/Dora/Action, simulation motion, and benchmark evidence remain deferred by design.
- [完成] [eval] [test] 全量组合回归 `708 passed, 1 skipped`；核心/Skill/adapter 回归 `205 passed`；Ruff、compileall、`git diff --check` 均通过。`.codegraph/` 与 `.cursor/` 保持未跟踪且未进入提交。
- [Completed] [Eval] [Test] Full combined regression: `708 passed, 1 skipped`; core/Skill/adapter regression: `205 passed`; Ruff, compileall, and `git diff --check` all passed. `.codegraph/` and `.cursor/` remain untracked and were not included in the commit.

### 五维验收 / Five-Dimension Acceptance

- 架构集成：通过；规划桥接不接管任务、SQLite、Gateway 或执行事实。
- 失败路径：通过；缺上下文、绑定漂移、未 ready、循环、资源冲突、replay/审核/callback 缺失均 fail-closed。
- 权威边界：通过；规划输出不产生 readiness、verdict 或 motion authority。
- 配置与 provenance：通过；graph、ToolSpec projection、replay 和 promotion 均显式 digest/artifact 绑定，无第二配置源。
- 可维护性：通过；planning、dispatch、Skill、Coordinator、Experience 分层清晰，回归命令可复现。
- Architecture integration: pass; the planning bridge does not own task lifecycle, SQLite, Gateway, or execution facts.
- Failure paths: pass; missing context, binding drift, unready nodes, cycles, resource conflicts, and missing replay/review/callback fail closed.
- Authority boundaries: pass; planning emits no readiness, verdict, or motion authority.
- Configuration and provenance: pass; graph, ToolSpec projections, replay, and promotion use explicit digest/artifact bindings with no second configuration source.
- Maintainability: pass; planning, dispatch, Skill, Coordinator, and Experience remain layered with reproducible regression commands.

### Git 提交 / Git Commit

- Commit: `aadc833`
- Branch: `feature/long-horizon-workflow`


## v5.6.1 (2026-09-05 23:40) - codex

### 预期修改 / Planned Changes

- [计划] [policy] [feat] 新增显式 live ToolSpec → `ToolSpecPolicy` 投影器；只消费 ToolSpec 中版本化的 `planning` 扩展，不根据 endpoint、implementation 或 tool_id 猜测能力。
- [Planned] [Policy] [Feat] Add an explicit live ToolSpec to `ToolSpecPolicy` projector; consume only the versioned `planning` extension in a ToolSpec and never infer capabilities from endpoint, implementation, or tool_id.
- [计划] [policy] [fix] 严格校验 planning capabilities、preconditions、evidence、effects、resource claims、scene-write、failure classes 和 idempotency；缺失或非法扩展 fail-closed，保持旧 ToolSpec 可被正常执行但不可被 agent-composed admission 使用。
- [Planned] [Policy] [Fix] Strictly validate planning capabilities, preconditions, evidence, effects, resource claims, scene-write, failure classes, and idempotency; reject missing or invalid extensions while keeping legacy ToolSpecs executable but unavailable to agent-composed admission.
- [计划] [eval] [test] 覆盖合法投影、spec digest、资源声明、重复值、未知字段、provider-specific payload 和缺失扩展；同步设计文档和五维审查记录。
- [Planned] [Eval] [Test] Cover valid projection, spec digests, resource claims, duplicate values, unknown fields, provider-specific payloads, and missing extensions; update design documentation and five-dimension review records.

### 预期影响文件 / Planned Files

- `PhyAgentOS/planning/projection.py`
- `PhyAgentOS/planning/__init__.py`
- `tests/test_tool_spec_projection.py`
- `docs/forge/PLANNING_MODULE_DESIGN.md`
- `docs/forge/MANIPULATION_DAG_DEVELOPER_GUIDE.md`

## v5.6.0 (2026-09-05 23:10) - codex

### 预期修改 / Planned Changes

- [完成] [policy] [feat] 将具体 `PlanGraph` 通过显式 graph artifact/digest 参数绑定到 `AgentTaskCoordinator` 创建和重规划的 `PlanRevision`，保持 SQLite 为唯一生命周期事实源。
- [Completed] [Policy] [Feat] Bind a concrete `PlanGraph` to `PlanRevision` through explicit graph-artifact/digest parameters in `AgentTaskCoordinator` task creation and replanning while keeping SQLite as the sole lifecycle authority.
- [完成] [policy] [refactor] 为规划驱动的 Query/Action/Session 调用增加可选 node/obligation/input-binding/DecisionTrace 归因；未提供完整规划绑定时保持 legacy execution compatibility，提供部分绑定时 fail-closed。
- [Completed] [Policy] [Refactor] Add optional node/obligation/input-binding/DecisionTrace attribution to planning-driven Query/Action/Session calls; preserve legacy execution compatibility when no planning binding is supplied and fail closed on partial bindings.
- [完成] [policy] [feat] 增加 `ReplanDelta` 到 coordinator 的显式适配入口，只允许在 `awaiting_replan` 状态创建新的 revision，不在 planning 模块内写库或调度。
- [Completed] [Policy] [Feat] Add an explicit `ReplanDelta` adapter entrypoint on the coordinator; only create a new revision in `awaiting_replan` state, with no store writes or scheduling inside the planning module.
- [完成] [eval] [test] 覆盖 graph identity/digest、规划绑定字段完整性、三类 Tool 归因、partial binding 拒绝、replan delta revision 适配和 legacy compatibility，并完成五维验收。
- [Completed] [Eval] [Test] Cover graph identity/digests, complete planning-binding fields, all three Tool semantics, partial-binding rejection, replan-delta revision adaptation, and legacy compatibility, followed by five-dimension acceptance.

### 预期影响文件 / Planned Files

- `PhyAgentOS/forge/task.py`
- `PhyAgentOS/agent/tools/forge_tool_api.py`
- `PhyAgentOS/agent/tools/forge_task.py`
- `tests/test_planning_module.py`
- `examples/forge-skills/pick-place-workflow/tests/test_planning_task_integration.py`
- `docs/forge/PLANNING_MODULE_DESIGN.md`
- `docs/forge/MANIPULATION_DAG_DEVELOPER_GUIDE.md`

### 实际结果 / Actual Results

- [完成] [policy] [feat] `PhyAgentOS/forge/task.py:L685-L905` 让 `create_task()`、`begin_revision()` 接受具体 `PlanGraph` 与 `artifact://` 引用，并将 graph/planner/policy digest 持久化到 coordinator-owned `PlanRevision`；新增 `begin_revision_from_delta()` 适配纯 `ReplanDelta`，planning 模块不写库、不创建 revision。
- [Completed] [Policy] [Feat] `PhyAgentOS/forge/task.py:L685-L905` makes `create_task()` and `begin_revision()` accept a concrete `PlanGraph` with an `artifact://` reference and persist graph/planner/policy digests in the coordinator-owned `PlanRevision`; adds `begin_revision_from_delta()` for pure `ReplanDelta` adaptation without store writes or revision creation in planning.
- [完成] [policy] [refactor] `PhyAgentOS/planning/contracts.py:L75-L142`、`__init__.py` 新增 `plan_node_digest()` 与不可变 `PlanningExecutionBinding`；`_append_execution()` 为 Query/Action/Session 统一写入 node/obligation/input-binding/DecisionTrace 归因，部分绑定 fail-closed，旧无绑定调用保持兼容。
- [Completed] [Policy] [Refactor] `PhyAgentOS/planning/contracts.py:L75-L142` and `__init__.py` add `plan_node_digest()` and immutable `PlanningExecutionBinding`; `_append_execution()` records node/obligation/input-binding/DecisionTrace attribution uniformly for Query/Action/Session, rejects partial bindings, and preserves legacy calls without planning metadata.
- [完成] [policy] [feat] `PhyAgentOS/agent/tools/forge_task.py:L49-L158`、`forge_tool_api.py:L90-L279,L373-L407` 暴露 graph/ref 与 planning binding 参数；`PhyAgentOS/agent/experience/contracts.py:L139-L160`、`source.py:L254-L265` 将 trace 引用以 opaque evidence ref 投影进现有 experience outcome。
- [Completed] [Policy] [Feat] `PhyAgentOS/agent/tools/forge_task.py:L49-L158` and `forge_tool_api.py:L90-L279,L373-L407` expose graph/ref and planning-binding parameters; `PhyAgentOS/agent/experience/contracts.py:L139-L160` and `source.py:L254-L265` project trace references into the existing experience outcome as opaque evidence refs.
- [完成] [eval] [test] `tests/test_planning_task_integration.py:L1-L233` 覆盖 graph identity/digest、完整/部分 execution binding、三类 Tool semantics、legacy compatibility、replan delta revision 适配与 experience trace 投影；planning/core + pick-place Skill 回归 `457 passed`，planning 专项 `15 passed`，RoboTwin adapter `233 passed, 1 skipped`，Ruff、compileall、`git diff --check` 通过。
- [Completed] [Eval] [Test] `tests/test_planning_task_integration.py:L1-L233` covers graph identity/digests, complete/partial execution bindings, all three Tool semantics, legacy compatibility, replan-delta revision adaptation, and experience trace projection; planning/core plus pick-place Skill regression `457 passed`, planning focus `15 passed`, RoboTwin adapter `233 passed, 1 skipped`, with Ruff, compileall, and `git diff --check` passing.

### 五维验收 / Five-Dimension Acceptance

- 架构集成：通过；planning 仍是纯库，AgentTask/PlanRevision/SQLite 是唯一生命周期事实源，Gateway 仍是执行面。
- 失败路径：通过；身份漂移、缺失 graph/ref、部分 binding、stale delta、重复 active revision 均 fail-closed；legacy 无规划元数据任务保持兼容。
- 权威边界：通过；规划绑定和 trace 不授予 readiness、资源锁、Gateway admission 或 motion authority。
- 配置：通过；graph/ref/trace 为显式输入，没有新增 Markdown、endpoint、credential 或 provider 配置源。
- 可维护性：通过；三类 Tool 共用一条归因路径，node digest 和 immutable binding 防止字段拼接错误，Experience 复用现有 outcome projection。
- Architecture integration: pass; planning remains a pure library, AgentTask/PlanRevision/SQLite remain the sole lifecycle authority, and Gateway remains the execution plane.
- Failure paths: pass; identity drift, missing graph/ref, partial binding, stale delta, and duplicate active revisions fail closed while legacy tasks without planning metadata remain compatible.
- Authority boundaries: pass; planning metadata and traces grant no readiness, resource lock, Gateway admission, or motion authority.
- Configuration: pass; graph/ref/trace are explicit inputs with no new Markdown, endpoint, credential, or provider configuration source.
- Maintainability: pass; all three Tool semantics share one attribution path, node digests and immutable bindings prevent ad-hoc field combinations, and Experience reuses the existing outcome projection.

### 剩余未完成 / Remaining documented work

Live Gateway ToolSpec-to-`ToolSpecPolicy` projection, AgentLoop production dispatch of `agent_composed`, and Experience policy-candidate aggregation, independent replay, human review, and promotion remain separate follow-up stages. No Gateway, Dora, Action, simulation motion, or hardware execution was started.

### Git 提交 / Git Commit

- Commit: `619c1b2`
- Branch: `feature/long-horizon-workflow`

## v5.5.5 (2026-09-05 22:20) - codex

### 预期修改 / Planned Changes

- [计划] [policy] [feat] 为抓取放置 Skill 增加 `baseline` 与 `agent_composed` 双模式；保留旧 `LongHorizonWorkflow` 作为兼容 replay projection，新增由 Agent 输入实体/依赖生成的语义 PlanGraph 投影。
- [Planned] [Policy] [Feat] Add `baseline` and `agent_composed` modes to the pick-place Skill; retain the existing `LongHorizonWorkflow` as a compatibility replay projection and add an Agent-input semantic PlanGraph projection.
- [计划] [policy] [refactor] 增加纯 Skill planning bridge：按语义 capability 选择候选 Tool，通过 PAOS planning admission 检查证据、scene revision、资源和 ToolSpec digest；bridge 不执行 Tool、不写 SQLite、不创建 revision。
- [Planned] [Policy] [Refactor] Add a pure Skill planning bridge that selects candidate Tools by semantic capability and delegates evidence, scene, resource, and ToolSpec-digest checks to PAOS planning admission; the bridge executes no Tool, writes no SQLite, and creates no revision.
- [计划] [eval] [test] 覆盖多实体子任务 DAG、部分序/并行 ready set、动态 Tool 替代、缺证据/stale/resource/capability 失败，以及 baseline 回归兼容；完成后按五维复审。
- [Planned] [Eval] [Test] Cover multi-entity subtask DAGs, partial-order/parallel ready sets, dynamic Tool substitution, missing-evidence/stale/resource/capability failures, and baseline compatibility, followed by five-dimension review.

### 预期影响文件 / Planned Files

- `examples/forge-skills/pick-place-workflow/src/pick_place_workflow/agent_planning.py`
- `examples/forge-skills/pick-place-workflow/src/pick_place_workflow/__init__.py`
- `examples/forge-skills/pick-place-workflow/tests/test_agent_planning.py`
- `examples/forge-skills/pick-place-workflow/README.md`
- `examples/forge-skills/pick-place-workflow/SKILL.md`
- `docs/forge/PLANNING_MODULE_DESIGN.md`

### 实际结果 / Actual Results

- [完成] [policy] [feat] `examples/forge-skills/pick-place-workflow/src/pick_place_workflow/agent_planning.py:L1-L216` 新增 Skill bridge：`AgentSubtaskSpec`、`compose_agent_plan`、`AgentComposedPlan`、`DynamicToolPlanner` 和显式 `baseline/agent_composed` 模式；多实体子任务编译为带 `verify` join 的 PAOS PlanGraph，Tool 只按 semantic capability 动态候选，不写死单一 Tool。
- [Completed] [Policy] [Feat] `examples/forge-skills/pick-place-workflow/src/pick_place_workflow/agent_planning.py:L1-L216` adds the Skill bridge: `AgentSubtaskSpec`, `compose_agent_plan`, `AgentComposedPlan`, `DynamicToolPlanner`, and explicit `baseline/agent_composed` modes; multi-entity subtasks compile into a PAOS PlanGraph with a `verify` join, and Tools are candidate-selected by semantic capability instead of being fixed one-to-one.
- [完成] [policy] [refactor] `examples/forge-skills/pick-place-workflow/src/pick_place_workflow/__init__.py:L1-L111` 导出 bridge；`README.md:L43-L89`、`SKILL.md:L128-L143`、`docs/forge/PLANNING_MODULE_DESIGN.md:L104-L124` 明确 baseline 与 agent-composed 的职责和动态调用边界。
- [Completed] [Policy] [Refactor] `examples/forge-skills/pick-place-workflow/src/pick_place_workflow/__init__.py:L1-L111` exports the bridge; `README.md:L43-L89`, `SKILL.md:L128-L143`, and `docs/forge/PLANNING_MODULE_DESIGN.md:L104-L124` document the baseline/agent-composed responsibilities and dynamic-call boundary.
- [完成] [eval] [test] `examples/forge-skills/pick-place-workflow/tests/test_agent_planning.py:L1-L126` 覆盖多实体并行 ready set、verify join、未知依赖/重复绑定/cycle、动态 Tool 替代、admission 拒绝和 mode switch；专项 `4 passed`，Skill 回归 `270 passed`，Ruff、compileall、`git diff --check` 通过。
- [Completed] [Eval] [Test] `examples/forge-skills/pick-place-workflow/tests/test_agent_planning.py:L1-L126` covers multi-entity parallel ready sets, the verify join, unknown dependencies/duplicate bindings/cycles, dynamic Tool substitution, admission rejection, and mode switching; focused suite `4 passed`, Skill regression `270 passed`, and Ruff, compileall, and `git diff --check` pass.

### 五维验收 / Five-Dimension Acceptance

- **架构集成 / Architecture integration：通过。** bridge 只编译 PlanGraph/调用纯 admission；AgentTask/PlanRevision/SQLite、ForgeToolClient/Gateway、adapter/readiness 和 Verifier owner 未迁移；legacy reducer 仍是 baseline。
- **失败路径 / Failure paths：通过。** 空/重复/未知子任务、非法 entity、无候选 Tool、Tool 非声明候选和 admission 失败均显式拒绝；失败后仍由 PAOS coordinator 创建新 revision。
- **权威边界 / Authority boundaries：通过。** Agent 提供语义分解并选择候选；planning/bridge 仅校验；执行事实由 Gateway；物理事实由 adapter/readiness；经验系统只能产生 review-gated policy candidate；无 motion authority。
- **配置 / Configuration：通过。** ToolSpec candidate 来自冻结 binding projection；没有新增 Markdown 或第二份 Tool 配置源；plan/policy digests 继续绑定到 PlanRevision。
- **可维护性 / Maintainability：通过。** Skill bridge、PAOS pure planning、legacy reducer 和测试独立分层；每个小功能均有 focused coverage，无 Gateway/SQLite/动作副作用。

### Git 提交 / Git Commit

- Commit: `f6d1b7d`
- Branch: `feature/long-horizon-workflow`

## v5.2.0 (2026-09-05 10:39) - codex — continuation

本文件承接 `changelog/2026-09_part2.md` 的 v5.2.0 详细记录。由于 part2 已超过 1500 行/300KB，当前验收收口写入本分卷；part2 中原计划与实现内容保持历史可追溯。

This file continues the detailed v5.2.0 record from `changelog/2026-09_part2.md`. Because part2 exceeds the 1500-line/300KB split threshold, the acceptance closeout is recorded here while the original plan and implementation remain traceable in part2.

### 补充计划 / Additional Plan

- [完成] [robotics] [fix] 修复 selector→evaluation adapter→单参数 readiness client 的 request identity 双重追加，并恢复 client 对逐 candidate/entity/scene/frame/calibration/digest/no-motion evidence 的完整校验；增加端到端 unavailable→replan conformance。
- [Completed] [Robotics] [Fix] Fixed duplicate request-identity suffixing across selector→evaluation adapter→one-argument readiness client, restored complete per-candidate/entity/scene/frame/calibration/digest/no-motion evidence validation, and added end-to-end unavailable→replan conformance.
- [完成] [robotics] [fix] 修复同一 grasp candidate 分别评估左右臂时的 evidence 文件名冲突：artifact identity 同时绑定 option-specific `request_id` 与 `candidate_ref`，保持不可变且不互相覆盖。
- [Completed] [Robotics] [Fix] Fixed evidence filename collisions when the same grasp candidate is evaluated for left and right arms: artifact identity binds both the option-specific `request_id` and `candidate_ref`, preserving immutability without cross-option overwrite.
- [完成] [robotics] [safety] 删除 evaluation adapter 的虚构 fallback evidence ref，严格校验 check/status/evidence 一致性，并要求 worker/client 顶层显式 `world_change_started=false`。
- [Completed] [Robotics] [Safety] Removed the evaluation adapter's fabricated fallback evidence ref, strictly validated check/status/evidence consistency, and required explicit top-level `world_change_started=false` across worker/client.

### 文件变更详情 / File Change Details

- [修改] `PhyAgentOS/forge/manipulation.py:L170-L314`：`ReplanSignal` 绑定 `node_digest` 并自校验内容摘要；不写 SQLite、不创建 Gateway invocation。
- [Modified] `PhyAgentOS/forge/manipulation.py:L170-L314`: binds `node_digest` and self-validates content digest; does not write SQLite or create Gateway invocations.
- [修改] `examples/forge-skills/pick-place-workflow/src/pick_place_workflow/long_horizon.py:L281-L639`：恢复状态校验 task/revision identity、immutable steps、DAG readiness 和 blocked reason 一致性。
- [Modified] `examples/forge-skills/pick-place-workflow/src/pick_place_workflow/long_horizon.py:L281-L639`: validates restored task/revision identity, immutable steps, DAG readiness, and blocked-reason consistency.
- [修改] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/route_readiness.py:L95-L591`：验证 release TCP 变换、八阶段夹爪语义和关键 phase 边界；新增 `RouteReadinessEvaluationAdapter`，将单参数 worker response 投影到 `route-evaluation/v1`，完整校验逐 candidate response binding，并拒绝缺失/虚构 evidence ref 与顶层 world change。
- [Modified] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/route_readiness.py:L95-L591`: validates the release-TCP transform, eight phase gripper semantics, and key phase boundaries; adds `RouteReadinessEvaluationAdapter` projecting the one-argument worker response into `route-evaluation/v1`, fully validates each candidate response binding, and rejects missing/fabricated evidence refs or top-level world change.
- [修改] `examples/forge-adapters/robotwin20/runtime/robotwin_route_readiness_worker.py:L27-L102`：artifact identity 绑定 request/candidate，防止左右臂 option 互相覆盖，并在顶层响应显式声明 `world_change_started=false`。
- [Modified] `examples/forge-adapters/robotwin20/runtime/robotwin_route_readiness_worker.py:L27-L102`: binds artifact identity to request/candidate so left/right options cannot overwrite each other and explicitly declares `world_change_started=false` in the top-level response.
- [修改] `examples/forge-adapters/robotwin20/tests/test_route_readiness.py:L1-L287`、`examples/forge-adapters/robotwin20/tests/test_arm_candidates.py:L1-L339`、`tests/test_manipulation.py:L1-L112`、`examples/forge-skills/pick-place-workflow/tests/test_long_horizon.py:L1-L277`：增加失败路径、双臂跨进程 wiring、evidence 绑定和 world-change 边界回归。
- [Modified] The listed tests add failure-path, dual-arm cross-process wiring, evidence-binding, and world-change boundary regressions.
- [文档] `docs/forge/STATE_FILE_IMPLEMENTATION_REVIEW_20260903.md:L843-L886`、`docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md:L829-L842`、`CHANGELOG.md:L5-L39`：记录最终十一个 Major 修复、五维结论和执行门禁。
- [Docs] The listed review, diagnosis, and root changelog sections record the final eleven Major fixes, five-dimension conclusion, and execution gate.

### 关键 Diff / Key Diff

```text
Before: evidence_refs = item.get("evidence") or ["artifact://route-readiness/unavailable"]
After:  require a non-empty, worker-returned artifact:// evidence list; otherwise fail closed.

Before: the worker response had no top-level world-change assertion and the client only checked item-level flags.
After:  worker emits world_change_started=false and the client rejects every other or missing top-level value.

Before: artifact identity hashed candidate_ref only, so left/right evaluations of one grasp shared a path.
After:  artifact identity hashes request_id + candidate_ref, making each option-specific artifact immutable.
```

### 五维代码审查 / Five-Dimension Review

- 架构集成 / Architecture integration：通过；Skill DAG 是 projection，PAOS lifecycle/SQLite/Verifier/Gateway ownership 未迁移，route-readiness 与 selector 通过明确 adapter projection 对接。
- 失败路径 / Failure paths：通过；stale identity、非法恢复、重复 YAML、worker unavailable、候选耗尽、transform/gripper 语义错误和 evaluator 异常均 fail-closed。
- 权威边界 / Authority boundaries：通过；DAG、replan、selector、readiness、evidence 均不创建 invocation 或授权运动，`ForgeTaskVerifier` 是用户级 verdict 唯一 owner。
- 配置 / Configuration：通过；本体、路线、limits、worker、artifact root 和环境占位符保留在严格版本化 profile。
- 可维护性 / Maintainability：通过；独立 schema、digest、strict loader、failure taxonomy 和文档同步，无隐藏 Action wiring/fake success。

### 验证 / Validation

- 组合 `tests + adapter tests + Skill tests`（排除缺少 NumPy 的 GraspGen collection）：`616 passed, 2 skipped`。
- Skill：`261 passed`；core/adapter：`355 passed, 2 skipped`；根仓库：`168 passed`；route generation/readiness/selection 专项：`36 passed`；开发者指南完整 DAG/route/evidence 专项：`69 passed`。
- `ruff check`、`compileall`、`git diff --check`：通过。
- GraspGen provider collection 仍受 PAOS 环境缺少 `numpy` 阻塞；NumPy 隔离环境缺少 `pydantic_settings`，未伪装为通过。
- 未启动 Gateway、Dora、Action、硬件或 simulation motion；真实 readiness、人工批准和抓取放置闭环仍未完成。

### Git 提交 / Git Commit

- Implementation commit: `514e044`
- Branch: `feature/long-horizon-workflow`

## v5.2.1 (2026-09-05 13:19) - codex

### 预期修改 / Planned Changes

- [完成] [chore] [docs] 回写 v5.2.0 实现提交 `514e044` 到月度日志和根 CHANGELOG，滚动维护最近五条完整记录，并确认当前分支远端一致；不修改实现、测试或安全边界。
- [Completed] [Chore] [Docs] Recorded v5.2.0 implementation commit `514e044` in the monthly and root changelogs, rolled the latest-five detailed entries, and confirmed the current branch matches its remote; implementation, tests, and safety boundaries are unchanged.

### 预期影响文件 / Expected Files

- `changelog/2026-09_part3.md`
- `CHANGELOG.md`

### 实际结果 / Actual Results

- [完成] [chore] [docs] `changelog/2026-09_part2.md:L2996-L3037` 与 `changelog/2026-09_part3.md:L62-L65` 回写 v5.2.0 实现提交 `514e044` 并校正最终实现行号；实现、测试计数和 no-motion 结论未改变。
- [Completed] [Chore] [Docs] `changelog/2026-09_part2.md:L2996-L3037` and `changelog/2026-09_part3.md:L62-L65` record v5.2.0 implementation commit `514e044` and correct final implementation line ranges; implementation, test counts, and the no-motion conclusion are unchanged.
- [完成] [docs] [docs] `CHANGELOG.md:L5-L60,L143-L147` 新增 v5.2.1 双语详细记录、回写 v5.2.0 commit 并前移 Archive 边界，使最近五条固定为 v5.2.1、v5.2.0、撤销的 v5.1.0、v5.0.0、v4.12.2。
- [Completed] [Docs] [Docs] `CHANGELOG.md:L5-L60,L143-L147` adds the detailed bilingual v5.2.1 record, records the v5.2.0 commit, and moves the Archive boundary so the latest five remain v5.2.1, v5.2.0, withdrawn v5.1.0, v5.0.0, and v4.12.2.

### 关键 Diff / Key Diff

```text
Before: v5.2.0 implementation and validation were recorded with Commit: pending.
After:  implementation commit 514e044 and the pushed branch are recorded; code and evidence are unchanged.
```

### 验证 / Validation

- `514e044` 同时为实现提交和远端 `origin/feature/long-horizon-workflow` 当前提交；日志 UTF-8 显示正常。
- `git diff --check` 通过；本次只暂存两份 changelog，`.codegraph/`、`.cursor/` 仍未跟踪且未暂存。

### Git 提交 / Git Commit

- Implementation commit: `514e044`
- Branch: `feature/long-horizon-workflow`
## v5.4.0 (2026-09-05 15:05) - codex

### 变更摘要 / Change summary
- [policy] [feat] [计划] 新增 RoboTwin/Franka 真实 route-input materializer，从固定 seed 场景生成带 provenance 的 placement target、对象 geometry、经验证的 `object_T_tcp`、joint/stop policy 与 v2 complete-route request。
- [policy] [feat] [planned] Add a RoboTwin/Franka real route-input materializer that derives a provenance-bound placement target, object geometry, validated `object_T_tcp`, joint/stop policies, and a v2 complete-route request from the fixed-seed scene.
- [eval] [exp] [计划] 生成唯一真实运行目录和人工审核包；审批前保持无动作，审批后运行单次独立 simulation probe，保存成功或失败的全部中间 evidence。
- [eval] [exp] [planned] Produce a unique real-run directory and human-review package; remain no-motion before approval, then run one single-use independent simulation probe and preserve all success or failure evidence.
- [docs] [docs] [计划] 更新完整抓取放置边界、运行证据和最终五维代码审查，不将 preliminary 或失败证据表述为 readiness pass。
- [docs] [docs] [planned] Update the complete pick-place boundary, runtime evidence, and final five-dimension code review without presenting preliminary or failed evidence as a readiness pass.

### 预期影响文件 / Planned files
- `examples/forge-adapters/robotwin20/src/robotwin20_adapter/route_inputs.py`
- `examples/forge-adapters/robotwin20/runtime/robotwin_route_input_worker.py`
- `examples/forge-adapters/robotwin20/scripts/materialize_complete_route.py`
- `examples/forge-adapters/robotwin20/tests/test_route_inputs.py`
- `examples/forge-adapters/robotwin20/tests/test_route_input_worker.py`
- `examples/forge-adapters/robotwin20/README.md`
- `docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md`
- `docs/forge/STATE_FILE_IMPLEMENTATION_REVIEW_20260903.md`

## v5.7.0 (2026-09-05 23:40) - codex

### 预期修改 / Planned Changes

- [完成] [policy] [feat] 为 AgentLoop 增加显式 `agent_composed` 规划调度桥接：只向 Agent 暴露当前 PlanGraph ready 节点和经过冻结 ToolSpec projection 的候选，不绕过 Coordinator、Gateway 或 Action admission。
- [Completed] [Policy] [Feat] Added an explicit `agent_composed` planning-dispatch bridge to AgentLoop: it exposes only ready PlanGraph nodes and frozen ToolSpec-projection candidates without bypassing the Coordinator, Gateway, or Action admission.
- [完成] [policy] [feat] 为 planning `WorkflowPolicyCandidate` 增加 Experience 侧持久化、独立 replay receipt 与人工审核状态迁移；候选不得由单个 episode 自动晋升，也不得修改当前任务。
- [Completed] [Policy] [Feat] Added Experience persistence, independent replay receipts, and human-review transitions for `WorkflowPolicyCandidate`; a single episode cannot auto-promote a candidate or mutate the active task.
- [完成] [eval] [test] 增加 AgentLoop/dispatch 的 ready-set、缺失 projection、stale graph、Action fail-closed 测试，以及 candidate 去重、replay 绑定、审核/晋升拒绝路径测试；运行 core、Skill、adapter、Ruff、compileall 与 diff check。
- [Completed] [Eval] [Test] Added AgentLoop/dispatch ready-set, missing-projection, stale-graph, and Action fail-closed tests, plus candidate deduplication, replay binding, and review/promotion rejection tests; core, Skill, adapter, Ruff, compileall, and diff checks passed.

### v5.7.0 实际结果 / Actual Results

- [完成] [policy] [feat] `PhyAgentOS/agent/planning_dispatch.py:L1-L224` 新增只读 `AgentComposedDispatch`；`PhyAgentOS/agent/tools/planning.py:L17-L99` 提供 `forge_plan_activate`/`forge_plan_ready`，并要求可信 `AdmissionContext` provider，拒绝 Agent 自带 evidence/settlement/condition 伪造。
- [Completed] [Policy] [Feat] `PhyAgentOS/agent/planning_dispatch.py:L1-L224` adds the read-only `AgentComposedDispatch`; `PhyAgentOS/agent/tools/planning.py:L17-L99` provides `forge_plan_activate`/`forge_plan_ready` and requires a trusted `AdmissionContext` provider, rejecting Agent-supplied evidence/settlement/condition forgery.
- [完成] [policy] [fix] `PhyAgentOS/agent/loop.py:L145-L208,L260-L314` 与 `ToolRegistry:L17-L90` 接入可选 admission guard；它只在现有 Forge Query/Action/Session wrapper 前执行纯校验，完整 node digest、obligation、trace 和 ToolSpec projection 漂移均 fail-closed，执行仍归 Coordinator/Gateway。
- [Completed] [Policy] [Fix] `PhyAgentOS/agent/loop.py:L145-L208,L260-L314` and `ToolRegistry:L17-L90` integrate an optional admission guard; it performs pure checks before existing Forge Query/Action/Session wrappers, fails closed on node-digest, obligation, trace, or ToolSpec-projection drift, and leaves execution with Coordinator/Gateway.
- [完成] [policy] [feat] `PhyAgentOS/planning/contracts.py:L327-L397` 增加 review-gated `WorkflowPolicyReplayReceipt` 与候选审核字段；`PhyAgentOS/agent/experience/store.py:L125-L143,L767-L903` 持久化候选/独立 replay；`policy_candidates.py:L1-L129` 聚合不同 episode、校验独立 pass、人工审核并要求 Skill Runtime callback 才能 promotion。
- [Completed] [Policy] [Feat] `PhyAgentOS/planning/contracts.py:L327-L397` adds review-gated `WorkflowPolicyReplayReceipt` and candidate review fields; `PhyAgentOS/agent/experience/store.py:L125-L143,L767-L903` persists candidates/replays; `policy_candidates.py:L1-L129` aggregates distinct episodes, requires independent passes, supports human review, and requires a Skill Runtime callback for promotion.
- [完成] [policy] [fix] replay receipt 绑定 candidate 的 base/proposed policy digest；候选 policy 变更或跨版本重放会被拒绝。
- [Completed] [Policy] [Fix] Bind replay receipts to the candidate base/proposed policy digests; policy changes or cross-version replays are rejected.
- [完成] [eval] [test] 新增 `tests/test_planning_dispatch.py`、`tests/test_workflow_policy_candidates.py`，覆盖 ready-set、上下文刷新、缺失/漂移 binding、激活失败清理、候选聚合、replay、审核和 promotion fail-closed；组合回归 `707 passed, 1 skipped`，Ruff、compileall、`git diff --check` 通过。
- [Completed] [Eval] [Test] Added `tests/test_planning_dispatch.py` and `tests/test_workflow_policy_candidates.py` covering ready sets, context refresh, missing/drifted bindings, activation cleanup, candidate aggregation, replay, review, and fail-closed promotion; combined regression `707 passed, 1 skipped`, with Ruff, compileall, and `git diff --check` passing.

### 五维验收 / Five-Dimension Acceptance

- **架构集成：通过。** planning dispatch 是 AgentLoop 的薄适配层；PlanRevision/SQLite、Coordinator/Gateway、Evidence/Verifier、Experience ledger ownership 未被转移。
- **失败路径：通过。** 缺可信上下文、旧/漂移 graph、缺 projection、节点/义务/trace 不匹配、循环/未 ready、replay 缺失、审核门槛不足和 callback 缺失均 fail-closed；激活失败清除旧 dispatch。
- **权威边界：通过。** Agent 只能选择 Tool 和提交候选；planning 只计算；Coordinator/Gateway 保留执行事实；Evidence/Verifier 提供可信结果；Experience/Skill Runtime 控制演化晋升；无 motion authority。
- **配置与 provenance：通过。** graph、ToolSpec projection、replay receipt 和 promotion ref 均显式 digest/artifact 绑定；Agent 不可提交 evidence/settlement 事实；未增加 Markdown 或第二 Tool registry 事实源。
- **可维护性：通过。** dispatch、read-only tools、registry guard、Experience candidate manager 分层；循环依赖改为 TYPE_CHECKING；新增公共 API 与测试/文档同步。
- **Architecture integration: pass.** The dispatch is a thin AgentLoop adapter; PlanRevision/SQLite, Coordinator/Gateway, Evidence/Verifier, and Experience ownership remain unchanged.
- **Failure paths: pass.** Missing trusted context, stale/drifted graph, missing projection, node/obligation/trace mismatch, cycles/unready nodes, missing replay, insufficient review support, and missing callback fail closed; activation failure clears the previous dispatch.
- **Authority boundaries: pass.** The Agent selects Tools and submits candidates only; planning computes; Coordinator/Gateway retain execution facts; Evidence/Verifier provide trusted outcomes; Experience/Skill Runtime control evolution promotion; no motion authority is added.
- **Configuration and provenance: pass.** Graphs, ToolSpec projections, replay receipts, and promotion references use explicit digest/artifact bindings; the Agent cannot submit evidence/settlement facts; no Markdown or second Tool registry fact source was added.
- **Maintainability: pass.** Dispatch, read-only tools, registry guard, and Experience candidate manager are layered; runtime imports use TYPE_CHECKING where appropriate; public APIs, tests, and docs are synchronized.

## v5.5.4 (2026-09-05 21:00) - codex

### 预期修改 / Planned Changes

- [计划] [policy] [feat] 新增 `PhyAgentOS/planning` 纯规划协议模块：任务级 PlanGraph/PlanNode、动态 Tool admission、节点结算、重规划 delta、决策追踪与可演化 WorkflowPolicy；不接管 AgentTask、SQLite、Gateway、动作执行或物理真值。
- [Planned] [Policy] [Feat] Add the pure `PhyAgentOS/planning` protocol module for task-level PlanGraph/PlanNode, dynamic Tool admission, node settlement, replan deltas, decision tracing, and evolvable WorkflowPolicy; it must not own AgentTask, SQLite, Gateway, motion execution, or physical truth.
- [计划] [docs] [docs] 保存设计审核结论、所有权边界、协议版本、演化约束、失败语义和迁移阶段，作为实现与后续 review 的基准。
- [Planned] [Docs] [Docs] Record the design review, ownership boundaries, protocol versions, evolution constraints, failure semantics, and migration phases as the implementation and review baseline.
- [计划] [eval] [test] 为模块内每一个纯计算小功能增加单元测试和失败路径测试，并验证其不调用 Gateway、不写 SQLite、不产生 motion authority。
- [Planned] [Eval] [Test] Add unit and failure-path tests for every pure-computation feature and verify that the module does not call Gateway, write SQLite, or produce motion authority.

### 预期影响文件 / Planned Files

- `docs/forge/PLANNING_MODULE_DESIGN.md`
- `PhyAgentOS/planning/__init__.py`
- `PhyAgentOS/planning/contracts.py`
- `PhyAgentOS/planning/dag.py`
- `PhyAgentOS/planning/admission.py`
- `PhyAgentOS/planning/settlement.py`
- `PhyAgentOS/planning/replan.py`
- `PhyAgentOS/planning/trace.py`
- `PhyAgentOS/planning/policy.py`
- `tests/test_planning_*.py`

### 实际结果 / Actual Results

- [完成] [policy] [feat] `PhyAgentOS/planning/__init__.py:L1-L56`、`contracts.py:L1-L316`、`dag.py:L1-L75`、`admission.py:L1-L97`、`settlement.py:L1-L24`、`replan.py:L1-L43`、`trace.py:L1-L37`、`policy.py:L1-L40` 新增纯规划协议库：PlanGraph/PlanNode、ToolSpecPolicy、ToolCall/Result、NodeSettlement、ReplanDelta、DecisionTrace、WorkflowPolicy 和 review-gated PolicyCandidate；实现 DAG 校验、ready set、条件/evidence/scene/resource/capability admission、unknown/stale/cancelled 结算、传递失效重规划、决策摘要与 policy 边校验。模块无 Gateway、SQLite、锁或 motion authority。
- [Completed] [Policy] [Feat] `PhyAgentOS/planning/__init__.py:L1-L56`, `contracts.py:L1-L316`, `dag.py:L1-L75`, `admission.py:L1-L97`, `settlement.py:L1-L24`, `replan.py:L1-L43`, `trace.py:L1-L37`, and `policy.py:L1-L40` add the pure planning protocol library: PlanGraph/PlanNode, ToolSpecPolicy, ToolCall/Result, NodeSettlement, ReplanDelta, DecisionTrace, WorkflowPolicy, and a review-gated PolicyCandidate; they implement DAG validation, ready sets, condition/evidence/scene/resource/capability admission, unknown/stale/cancelled settlement, transitive replan invalidation, decision summaries, and policy-edge validation. The module has no Gateway, SQLite, locks, or motion authority.
- [完成] [policy] [refactor] `PhyAgentOS/forge/task.py:L83-L197` 为 PlanRevision 增加 artifact graph ref 与 graph/planner/policy digest binding，为 ToolExecutionRecord 增加 node/obligation/input-binding/decision-trace binding；规划字段 legacy 可选，但一旦出现即全量校验，SQLite 仍是唯一生命周期事实源。`PhyAgentOS/forge/__init__.py:L38-L95` 导出公共 planning seam。
- [Completed] [Policy] [Refactor] `PhyAgentOS/forge/task.py:L83-L197` adds an artifact graph reference plus graph/planner/policy digest binding to PlanRevision and node/obligation/input-binding/decision-trace binding to ToolExecutionRecord; planning fields remain optional for legacy records but become all-or-nothing when present, while SQLite remains the sole lifecycle authority. `PhyAgentOS/forge/__init__.py:L38-L95` exports the public planning seam.
- [完成] [docs] [docs] `docs/forge/PLANNING_MODULE_DESIGN.md:L1-L118` 保存架构审核结论、所有权、协议版本、动态 Tool 合法性、自我进化边界、失败语义和迁移阶段；`docs/forge/MANIPULATION_DAG_DEVELOPER_GUIDE.md:L10-L216` 将 semantic DAG 权威从 Skill 固定流水线调整为 planning PlanGraph + PlanRevision 绑定，并保留旧 WorkflowDag 为 baseline projection。
- [Completed] [Docs] [Docs] `docs/forge/PLANNING_MODULE_DESIGN.md:L1-L118` records the architecture review, ownership, protocol versions, dynamic Tool legality, self-evolution boundaries, failure semantics, and migration stages; `docs/forge/MANIPULATION_DAG_DEVELOPER_GUIDE.md:L10-L216` moves semantic-DAG authority from the fixed Skill pipeline to planning PlanGraph + PlanRevision binding while retaining the old WorkflowDag as a baseline projection.
- [完成] [eval] [test] `tests/test_planning_module.py:L1-L267` 覆盖每个纯计算小功能、cycle/unknown evidence/stale scene/resource/capability/precondition/unknown/cancelled/stale/failed、传递重规划、trace/policy review gate、生命周期绑定和纯模块边界；专项 `8 passed`，根仓库（加载 pytest-asyncio）`180 passed`，Ruff、compileall、`git diff --check` 通过。
- [Completed] [Eval] [Test] `tests/test_planning_module.py:L1-L267` covers each pure-computation feature, cycle/unknown evidence/stale scene/resource/capability/precondition/unknown/cancelled/stale/failed paths, transitive replanning, trace/policy review gates, lifecycle bindings, and the pure-module boundary; focused suite `8 passed`, root suite (with pytest-asyncio loaded) `180 passed`, and Ruff, compileall, and `git diff --check` passed.

### 五维验收 / Five-Dimension Acceptance

- **架构集成 / Architecture integration：通过。** planning 是纯库；PlanRevision/SQLite 仍权威；Skill WorkflowDag 降级为 baseline；Gateway/adapter/Verifier/experience ownership 未被侵占。
- **失败路径 / Failure paths：通过。** cycle、未知依赖、缺 evidence、stale scene、Tool/capability/resource/precondition mismatch、unknown/cancelled/stale settlement、传递失效和 policy cycle 均 fail-closed；新 revision 仍由 coordinator 创建。
- **权威边界 / Authority boundaries：通过。** Agent 提议、planning 校验、Gateway 执行、adapter/readiness 提供物理事实、Verifier 判定语义成功、experience 仅产生 review-gated candidate；planning 输出 `motion_authorized=false`。
- **配置 / Configuration：通过。** ToolSpec/policy/graph 以 digest 绑定；无 Markdown 第二事实源；legacy optional binding 不改变旧 SQLite records。
- **可维护性 / Maintainability：通过。** 协议、纯计算、生命周期适配和测试分层；无 TODO/pass/fake-success；可复现专项与全量命令已写入 developer guide。

### Git 提交 / Git Commit

- Commit: `f95c53d`
- Branch: `feature/long-horizon-workflow`

## v5.5.0 (2026-09-05 22:00) - codex

### 预期修改 / Planned Changes

- [计划] [policy] [feat] 按 PAOS 扩展原则新增 provider-neutral 的机械臂能力快照、资源需求、协调组和 assignment 协议；保持 AgentTask/PlanRevision/SQLite、Gateway、Evidence、Verifier 的既有权威边界。
- [Planned] [Policy] [Feat] Add provider-neutral arm capability snapshot, resource requirement, coordination-group, and assignment contracts under PAOS extension principles while preserving AgentTask/PlanRevision/SQLite, Gateway, Evidence, and Verifier authority boundaries.
- [计划] [policy] [refactor] 扩展 Skill 侧语义 DAG 与失败重规划接口，支持单 Agent 多执行资源、替代臂、顺序协同和未来原子双臂 route；不把 benchmark 规则、URDF、坐标或执行锁写入 PAOS core。
- [Planned] [Policy] [Refactor] Extend the Skill semantic DAG and bounded replan interfaces for one Agent with multiple execution resources, alternative arms, sequential coordination, and future atomic bimanual routes without leaking benchmark rules, URDFs, coordinates, or execution locks into PAOS core.
- [计划] [eval] [test] 为 schema 严格校验、绑定漂移、能力不可用、assignment 证据缺失、双臂未配置和跨资源失败路径增加可复现测试；完成后按架构集成、失败路径、权威边界、配置、可维护性五维验收。
- [Planned] [Eval] [Test] Add reproducible coverage for strict schemas, binding drift, unavailable capabilities, missing assignment evidence, unsupported bimanual coordination, and cross-resource failures, followed by the five-dimension acceptance review.

- [计划补充] [eval] [feat] 增加单次 approved simulation probe 运行入口，独占写入顶层 response，使真实成功或失败证据可被后续 verifier 原样消费。
- [Planned supplement] [Eval] [Feat] Add a single-use approved simulation-probe runner that exclusively persists the top-level response so the verifier can consume the real success or failure evidence unchanged.

## v5.5.1 (2026-09-05 23:10) - codex

### 预期修改 / Planned Changes

- [计划] [policy] [fix] 修复语义 DAG 未实际消费 capability snapshot、arm assignment 与 coordination group 的准入断点；将这些引用加入 acquire/place 的强绑定，并从 terminal Tool response 中只提取受约束的 opaque references。
- [Planned] [Policy] [Fix] Close the semantic-DAG admission gap where capability snapshots, arm assignments, and coordination groups were declared but not consumed; require those bindings for acquire/place and extract only constrained opaque references from terminal Tool responses.
- [计划] [policy] [feat] 将 `manipulation.capabilities` 作为 pick-place Skill 的必需 Query Tool 接入 manifest、contract 与 DAG observe/understand 前置链路；保持 provider-neutral、no-motion、无 AgentTask/Gateway side effect。
- [Planned] [Policy] [Feat] Register `manipulation.capabilities` as a required pick-place Query Tool in the manifest, contract, and DAG pre-action path while preserving provider-neutral, no-motion behavior and no AgentTask/Gateway side effects.
- [计划] [eval] [test] 增加 capability/assignment/coordination binding drift、缺失响应字段、manifest discovery 与失败重规划回归；完成后执行五维代码审查。
- [Planned] [Eval] [Test] Add regression coverage for capability/assignment/coordination binding drift, missing response fields, manifest discovery, and failure replanning, followed by the five-dimension code review.

### 预期影响文件 / Planned Files

- `examples/forge-skills/pick-place-workflow/src/pick_place_workflow/long_horizon.py`
- `examples/forge-skills/pick-place-workflow/contracts/object.acquire.tool.yaml`
- `examples/forge-skills/pick-place-workflow/contracts/object.place.tool.yaml`
- `examples/forge-skills/pick-place-workflow/skill.yaml`
- `examples/forge-skills/pick-place-workflow/tests/test_long_horizon.py`
- `PhyAgentOS/forge/capability_runtime/manipulation_capabilities.py`
- `docs/forge/MANIPULATION_DAG_DEVELOPER_GUIDE.md`
- `docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md`

## v5.5.2 (2026-09-06 00:20) - codex

### 预期修改 / Planned Changes

- [计划] [policy] [fix] 将 `manipulation.capabilities` 从仅有 manifest/Action 引用提升为 canonical Skill DAG 的显式 no-motion Query 节点，强制 `observe → capabilities → understand`，并保持 capability snapshot 与后续 assignment 的 scene/observation 绑定。
- [Planned] [Policy] [Fix] Promote `manipulation.capabilities` from manifest/Action-only binding to an explicit no-motion Query node in the canonical Skill DAG, enforcing `observe → capabilities → understand` while preserving scene/observation binding into later assignments.
- [计划] [eval] [test] 更新 DAG terminal response、恢复和失败路径测试；重新运行 core、Skill、adapter 组合回归及五维审查。
- [Planned] [Eval] [Test] Update DAG terminal-response, recovery, and failure-path coverage; rerun the core, Skill, and adapter regression plus the five-dimension review.
- [计划补充] [docs] [fix] 修正当前开发者指南、Skill README/SKILL 与七节点 canonical DAG 不一致的问题；同步 `manipulation.capabilities` 节点、固定执行顺序和七 Tool 描述。
- [Planned supplement] [Docs] [Fix] Correct the mismatch between the current developer guide/Skill README/SKILL and the seven-node canonical DAG; document the `manipulation.capabilities` node, fixed order, and seven-tool description consistently.
- [计划补充] [chore] [fix] 修正 capabilities 回归测试的 Ruff import 顺序，并将静态检查纳入验收门槛。
- [Planned supplement] [Chore] [Fix] Fix the Ruff import ordering in the capabilities regression test and include static checks in the acceptance gate.
- [计划补充] [policy] [fix] 提升七节点 DAG、workflow reducer、Skill manifest 与 Python 包的协议版本，避免把新增 `manipulation.capabilities` 误识别为旧六节点状态。
- [Planned supplement] [Policy] [Fix] Bump the protocol versions for the seven-node DAG, workflow reducer, Skill manifest, and Python package so the new `manipulation.capabilities` node cannot be mistaken for the legacy six-node state.
- [计划补充] [eval] [fix] 更新 binding、runtime discovery、activation 和 bundle packaging 测试 fixture 到 Skill `0.10.0`，验证旧 Runtime 版本会 fail-closed。
- [Planned supplement] [Eval] [Fix] Update binding, runtime-discovery, activation, and bundle-packaging test fixtures to Skill `0.10.0`, verifying that an old Runtime version fails closed.

## v5.5.3 (2026-09-05 19:06) - codex

### 预期修改 / Planned Changes

- [计划] [policy] [refactor] 将 canonical Skill DAG 从线性 `observe → capabilities → understand` 重构为 `observe` 后 `capabilities` 与 `understand` 两个独立 ready Query，`propose` 通过显式 join 等待两者完成；Agent 通过 `next_tools()` 选择或并行调用，不由 reducer 固定调用顺序。
- [Planned] [Policy] [Refactor] Refactor the canonical Skill DAG from linear `observe → capabilities → understand` into two independent ready Queries, `capabilities` and `understand`, after `observe`; make `propose` an explicit join, and let the Agent choose or parallelize calls through `next_tools()` instead of fixing an execution order in the reducer.
- [计划] [docs] [fix] 更新 Skill、开发者指南、架构诊断和五维验收记录，明确 DAG 只表达语义依赖，Tool 调用由 Agent/PAOS Tool API 负责；同步版本与旧状态拒绝边界。
- [Planned] [Docs] [Fix] Update the Skill, developer guides, architecture diagnosis, and five-dimension review to state that the DAG expresses semantic dependencies only while Agent/PAOS Tool API performs Tool calls; synchronize versions and stale-state rejection boundaries.
- [计划] [eval] [test] 增加 ready-set、任意 Query 顺序、join、恢复和版本漂移回归；重新执行 core/Skill/adapter 组合测试、Ruff、compileall 与 diff check。
- [Planned] [Eval] [Test] Add ready-set, either-Query-order, join, recovery, and version-drift regression coverage; rerun the core/Skill/adapter suite, Ruff, compileall, and diff check.

### v5.5.2 实际结果 / Actual Results

- [完成] [policy] [fix] `examples/forge-skills/pick-place-workflow/src/pick_place_workflow/long_horizon.py:L15-L17,L173-L236,L560-L585` 将 `manipulation.capabilities` 固定为 `observe` 后的 canonical no-motion Query，并让 `understand/propose/prepare/acquire/place` 强制复用同一 `capability_snapshot_ref`；旧引用、缺失引用和跨阶段漂移均 fail-closed。
- [Completed] [Policy] [Fix] `examples/forge-skills/pick-place-workflow/src/pick_place_workflow/long_horizon.py:L15-L17,L173-L236,L560-L585` makes `manipulation.capabilities` the canonical no-motion Query after `observe` and requires `understand/propose/prepare/acquire/place` to reuse one `capability_snapshot_ref`; missing, legacy, and cross-stage drift fail closed.
- [完成] [docs] [fix] 同步 `README.md`、`SKILL.md`、`MANIPULATION_DAG_DEVELOPER_GUIDE.md`、用户开发指南和架构诊断，明确七节点顺序、Query 无动作边界与双臂 atomic route 尚未实现。
- [Completed] [Docs] [Fix] Synchronized the Skill README/SKILL, manipulation developer guide, user development guides, and architecture diagnosis with the seven-node order, no-motion Query boundary, and the unimplemented atomic bimanual route.
- [完成] [policy] [fix] 将 DAG/reducer 升级为 `pick_and_place_semantic_dag_v2`/`pick_and_place_workflow_v3`，Skill manifest/Python 包升级为 `0.10.0`，并更新旧 Runtime 版本 fail-closed 的 packaging/activation/binding fixtures。
- [Completed] [Policy] [Fix] Bumped the DAG/reducer to `pick_and_place_semantic_dag_v2`/`pick_and_place_workflow_v3`, the Skill manifest/Python package to `0.10.0`, and updated packaging/activation/binding fixtures so old Runtime versions fail closed.
- [完成] [eval] [test] capabilities import 顺序、组合回归和静态门禁均通过：`670 passed, 1 skipped`；Ruff、compileall、`git diff --check` 全部通过。
- [Completed] [Eval] [Test] Capability import ordering, combined regression, and static gates pass: `670 passed, 1 skipped`; Ruff, compileall, and `git diff --check` all pass.

### 五维代码审查 / Five-Dimension Review

- 架构集成：通过；DAG/Query/Action 仍为 projection，AgentTask/PlanRevision/SQLite、Gateway、Evidence、Verifier owner 未改变。
- 失败路径：通过；provider unavailable、invalid snapshot、binding drift、旧 terminal response、旧版本 Runtime 和恢复路径均 fail-closed。
- 权威边界：通过；capability snapshot 不授予 lease、readiness、Action 或 motion；atomic 双臂 route、真实 readiness 和完整语义闭环仍未实现。
- 配置：通过；七 Tool 与版本在 manifest、package、Skill 文档和开发者指南一致，机器人/benchmark/planner 细节仍由 adapter/profile 所有。
- 可维护性：通过；版本/digest 防止旧状态误接入，测试覆盖 manifest discovery、package freeze、terminal response、恢复和引用漂移。
- Architecture integration: pass; DAG/Query/Action remain projections and AgentTask/PlanRevision/SQLite, Gateway, Evidence, and Verifier ownership is unchanged.
- Failure paths: pass; provider-unavailable, invalid snapshots, binding drift, legacy terminal responses, old Runtime versions, and recovery fail closed.
- Authority boundaries: pass; capability snapshots grant no lease, readiness, Action, or motion authority; atomic bimanual routes, real readiness, and the complete semantic loop remain unimplemented.
- Configuration: pass; seven Tools and versions agree across manifest, package, Skill docs, and developer guides; robot/benchmark/planner details remain adapter/profile-owned.
- Maintainability: pass; version/digest checks reject stale states, with coverage for discovery, package freeze, terminal responses, recovery, and reference drift.

### 验证边界 / Validation Boundary

- 未启动 Gateway、Dora、Action、仿真运动或硬件；本轮不宣称真实 IK/collision/lift、人工 readiness approval、atomic bimanual executor 或完整抓取放置语义成功。
- Gateway, Dora, Action, simulation motion, and hardware were not started; this review does not claim real IK/collision/lift, human readiness approval, an atomic bimanual executor, or complete pick-place semantic success.

### v5.5.1 实际结果 / Actual Results

- [完成] [policy] [fix] `examples/forge-skills/pick-place-workflow/src/pick_place_workflow/long_horizon.py:L191-L225,L440-L473,L552-L610` 将 `capability_snapshot_ref` 与 `assignment_ref` 加入 acquire/place DAG 强绑定，并从 terminal response 仅提取受约束 opaque refs；缺失、非法 scheme 或漂移均 fail-closed。
- [Completed] [Policy] [Fix] `examples/forge-skills/pick-place-workflow/src/pick_place_workflow/long_horizon.py:L191-L225,L440-L473,L552-L610` makes `capability_snapshot_ref` and `assignment_ref` mandatory for acquire/place DAG admission and extracts only constrained opaque refs from terminal responses; missing, invalid-scheme, or drifted bindings fail closed.
- [完成] [policy] [feat] `PhyAgentOS/forge/capability_runtime/manipulation_capabilities.py:L23-L105`、`examples/forge-skills/pick-place-workflow/contracts/manipulation.capabilities.tool.yaml:L1-L15`、`skill.yaml:L7-L16` 与 `fake_gateway.py:L14-L24,L285-L430` 接入只读 capability Query；provider failure、snapshot binding drift 返回 unavailable，始终 `motion_authorized=false`。
- [Completed] [Policy] [Feat] `PhyAgentOS/forge/capability_runtime/manipulation_capabilities.py:L23-L105`, `examples/forge-skills/pick-place-workflow/contracts/manipulation.capabilities.tool.yaml:L1-L15`, `skill.yaml:L7-L16`, and `fake_gateway.py:L14-L24,L285-L430` integrate the read-only capability Query; provider failures and snapshot binding drift return unavailable and always keep `motion_authorized=false`.
- [完成] [policy] [fix] `object_acquire.py:L95-L225,L300-L425` 与 `object_place.py:L40-L270,L340-L501` 的公共 Action input/terminal schema 和校验同步要求 capability/assignment refs；没有新增资源锁、生命周期事实或执行授权。
- [Completed] [Policy] [Fix] `object_acquire.py:L95-L225,L300-L425` and `object_place.py:L40-L270,L340-L501` synchronize public Action input/terminal schemas and validation to require capability/assignment refs without adding resource locks, lifecycle facts, or execution authority.
- [完成] [eval] [test] 新增 capability contract/query/failure 测试并更新绑定 fixture；Skill/core/adapter 组合回归 `669 passed, 1 skipped`，Ruff、compileall、`git diff --check` 通过。
- [Completed] [Eval] [Test] Added capability contract/query/failure coverage and updated binding fixtures; combined Skill/core/adapter regression is `669 passed, 1 skipped`, with Ruff, compileall, and `git diff --check` passing.

### 五维代码审查 / Five-Dimension Review

- 架构集成：通过；DAG、Query、Action schema 均为 projection，AgentTask/PlanRevision/SQLite、Gateway concurrency、Evidence/Verifier ownership 未迁移。
- 失败路径：通过；provider unavailable、invalid snapshot、binding drift、缺失 assignment、旧 terminal response 均 fail-closed；Replan 仍走既有 revision 流程。
- 权威边界：通过；capability/assignment/readiness 不能授权运动，Fake Gateway 仅 replay no-motion，atomic bimanual 仍无执行器。
- 配置：通过；公共 Query contract 与 Skill manifest 同步，机器人细节继续留在 adapter/profile。
- 可维护性：通过；冻结模型、digest、严格 schema、contract parity 测试和开发者指南保持同步。
- Architecture integration: pass; DAG, Query, and Action schemas remain projections, with AgentTask/PlanRevision/SQLite, Gateway concurrency, and Evidence/Verifier ownership unchanged.
- Failure paths: pass; provider unavailable, invalid snapshots, binding drift, missing assignments, and legacy terminal responses fail closed; replanning still uses the existing revision path.
- Authority boundaries: pass; capability/assignment/readiness cannot authorize motion, the Fake Gateway is no-motion replay only, and atomic bimanual execution remains unimplemented.
- Configuration: pass; Query contract and Skill manifest are synchronized, while robot details remain adapter/profile-owned.
- Maintainability: pass; frozen models, digests, strict schemas, parity tests, and the developer guide are synchronized.

### 验证边界 / Validation Boundary

- 未启动 Gateway、Dora、Action、仿真运动或硬件；真实 readiness、人工审批、atomic 双臂 route 与完整抓取放置语义证据仍是后续门槛。
- Gateway, Dora, Action, simulation motion, and hardware were not started; real readiness, human approval, atomic bimanual routes, and complete pick-place semantic evidence remain future gates.

## v5.4.2 (2026-09-05 17:40) - codex

### 预期修改 / Planned Changes

- [完成] [policy] [fix] 根据 v4 真实 preflight 负证据修正 GraspGen `gripper_base_link` 到 RoboTwin Curobo `panda_hand` 的 planner-frame 语义；夹爪 depth 只作为接触范围验证依据，不再错误平移 planner TCP。
- [Completed] [Policy] [Fix] Corrected the GraspGen `gripper_base_link` to RoboTwin Curobo `panda_hand` planner-frame semantics from the real v4 preflight negative evidence; gripper depth is used only to validate contact reach instead of incorrectly translating the planner TCP.
- [完成] [eval] [test] 保存 v4 单次失败 evidence，增加 planner TCP/夹爪接触区间回归测试，并生成新的不可覆盖 route/review package。
- [Completed] [Eval] [Test] Preserved the single-use v4 failure evidence, added planner-TCP/gripper-contact-range regression tests, and generated a new non-overwriting route/review package.
- [计划补充] [policy] [refactor] 将 GraspGen canonical contact pose 与 RoboTwin 标准 gripper target 分离；由 profile 声明 canonical→RoboTwin 轴映射、抓取目标原点偏移和 endlink 偏移，route 使用后者，接触壳校验使用前者。
- [Planned supplement] [Policy] [Refactor] Separate the GraspGen canonical contact pose from the RoboTwin standard gripper target; bind the canonical→RoboTwin axis map, target-origin offset, and endlink offset in the profile, use the latter for routes, and the former for contact-shell validation.
- [计划补充] [eval] [test] 将 route request 升级为 v3，拒绝旧的混合 frame 契约，并增加 canonical/contact、RoboTwin target、endlink round-trip 回归测试。
- [Planned supplement] [Eval] [Test] Upgrade the route request to v3 so the old mixed-frame contract is rejected, and add regression tests for canonical/contact, RoboTwin target, and endlink round trips.

- `CHANGELOG.md`

### v5.4.2 实际结果 / Actual Results

- [完成] [policy] [refactor] `grasp_adaptation.py:L193-L340` 分离 canonical contact、RoboTwin standard target 和 planner/endlink round-trip；provider depth 字段升级为 `provider_T_contact_center`，route target 使用 profile 声明的轴映射、0.12 m reference distance 与 0.08 m gripper bias。
- [Completed] [Policy] [Refactor] `grasp_adaptation.py:L193-L340` separates canonical contact, RoboTwin standard target, and planner/endlink round-trip; the provider depth field is now `provider_T_contact_center`, while route targets use the profile-declared axis map, 0.12 m reference distance, and 0.08 m gripper bias.
- [完成] [policy] [fix] `route_generation.py:L194-L359`、`route_readiness.py:L241-L352`、`route_inputs.py:L1-L390` 和 `robotwin_simulation_probe_worker.py:L37-L270,L477-L730` 全面迁移 v3 `robot_target_pose`/`object_T_robot_target` 契约，旧 TCP 混合字段 fail-closed。
- [Completed] [Policy] [Fix] `route_generation.py:L194-L359`, `route_readiness.py:L241-L352`, `route_inputs.py:L1-L390`, and `robotwin_simulation_probe_worker.py:L37-L270,L477-L730` fully migrate the v3 `robot_target_pose`/`object_T_robot_target` contract and fail closed on mixed legacy TCP fields.
- [完成] [eval] [test] `tests/test_grasp_adaptation.py`、`test_route_inputs.py`、`test_route_generation.py`、`test_route_readiness.py`、`test_simulation_probe.py`、`test_approve_simulation_probe.py`：专项 `69 passed`；完整 adapter（加载 pick-place 与 pytest-asyncio，排除 NumPy provider collection）`212 passed, 2 skipped`。
- [Completed] [Eval] [Test] `tests/test_grasp_adaptation.py`, `test_route_inputs.py`, `test_route_generation.py`, `test_route_readiness.py`, `test_simulation_probe.py`, and `test_approve_simulation_probe.py`: focused suite `69 passed`; full adapter suite (with pick-place and pytest-asyncio loaded, excluding the NumPy provider collection) `212 passed, 2 skipped`.
- [完成] [eval] [exp] `/home/yanxu/robotwin20-runtime/artifacts/paos-route-inputs-20260905T182500Z/materialized/` 生成新的 v3 route/review package；route digest `6c528b54...`、source manifest digest `b88543f...`，保持 `pending_human_review` 与 `motion_authorized=false`。RoboTwin20 Python 3.10 no-motion preflight 返回 preliminary-only（`prepared_candidates=[]`、`collision=unavailable`），没有 simulator step。
- [Completed] [Eval] [Exp] Generated a new v3 route/review package under `/home/yanxu/robotwin20-runtime/artifacts/paos-route-inputs-20260905T182500Z/materialized/`; route digest `6c528b54...`, source-manifest digest `b88543f...`, still `pending_human_review` with `motion_authorized=false`. RoboTwin20 Python 3.10 no-motion preflight returned preliminary-only (`prepared_candidates=[]`, `collision=unavailable`) with zero simulator steps.

### 关键 Diff / Key Diff

```text
Before: GraspGen provider depth was treated as the RoboTwin planner TCP and v4 route artifacts used contact_tcp_pose/object_T_tcp/release_tcp_pose.
After:  provider_T_contact_center -> canonical contact -> robot_target_pose is explicit; v3 route/probe/approval schemas bind object_T_robot_target and reject legacy mixed fields.
```

### 验证 / Validation

- `python -m ruff check ...`, `python -m compileall ...`, `git diff --check`: passed.
- No Gateway, Dora, Action, hardware, or simulation probe was started; a new human approval is still required.

## v5.4.3 (2026-09-05 18:20) - codex

### 预期修改 / Planned Changes

- [计划] [policy] [fix] 完成 GraspGen canonical contact、RoboTwin standard gripper target 与 `panda_hand` planner 输入的统一契约；同步 attestation 目标帧、materializer、route generation、simulation probe 和审批摘要，拒绝旧 v4/v2/v1 混合产物。
- [Planned] [Policy] [Fix] Complete the contract separating GraspGen canonical contact, the RoboTwin standard gripper target, and the `panda_hand` planner input; synchronize attestation, materializer, route generation, simulation probe, and approval summaries while rejecting consumed v4/v2/v1 mixed artifacts.
- [计划] [eval] [test] 运行专项与完整 adapter 回归，使用唯一 artifact root 生成新的 no-motion v3 route/review package；在新人工审批之前不运行 simulation probe、Gateway、Dora 或硬件动作。
- [Planned] [Eval] [Test] Run focused and full adapter regression and create a new no-motion v3 route/review package under a unique artifact root; do not run the simulation probe, Gateway, Dora, or hardware motion before new human approval.

### 预期影响文件 / Planned Files

- `examples/forge-adapters/robotwin20/profiles/robotwin20/graspgen-tool-transform.json`
- `examples/forge-adapters/robotwin20/scripts/materialize_complete_route.py`
- `examples/forge-adapters/robotwin20/src/robotwin20_adapter/route_generation.py`
- `examples/forge-adapters/robotwin20/runtime/robotwin_simulation_probe_worker.py`
- `examples/forge-adapters/robotwin20/scripts/approve_simulation_probe.py`
- `examples/forge-adapters/robotwin20/tests/`
- `docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md`

### Git 提交 / Git Commit
- Commit: `c60f58c`
- Branch: `feature/long-horizon-workflow`

## v5.4.4 (2026-09-05 20:10) - codex

### 预期修改 / Planned Changes

- [计划] [policy] [fix] 根据 RoboTwin/Curobo 的 250 Hz 轨迹语义，为 PAOS profile-owned 的 1.0 rad/s 策略增加可审计的 trajectory retiming；不提升策略上限、不修改物理 URDF 限幅，并保持原始 planner 轨迹与 retimed evidence 可区分。
- [Planned] [Policy] [Fix] Add auditable trajectory retiming for the PAOS profile-owned 1.0 rad/s policy under RoboTwin/Curobo's 250 Hz trajectory semantics; do not raise the policy limit or alter physical URDF limits, and keep raw planner output distinct from retimed evidence.
- [计划] [eval] [test] 增加 retiming 的单位、终点、速度、线速度和失败路径回归；重新运行全部候选的连续 no-motion preflight，只有完整 route 通过才生成新的 review package。
- [Planned] [Eval] [Test] Add regression coverage for retiming units, endpoint preservation, velocity, linear-speed, and failure paths; rerun continuous no-motion preflight for all candidates and generate a new review package only if a complete route passes.

### 预期影响文件 / Planned Files

- `examples/forge-adapters/robotwin20/runtime/robotwin_simulation_probe_worker.py`
- `examples/forge-adapters/robotwin20/profiles/robotwin20/route-inputs.yaml`
- `examples/forge-adapters/robotwin20/tests/test_simulation_probe.py`
- `examples/forge-adapters/robotwin20/README.md`
- `docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md`

### Git 提交 / Git Commit

- Commit: pending (implementation commit to be recorded after verification)
- Branch: `feature/long-horizon-workflow`

### v5.4.4 实际结果 / Actual Results

- [完成] [policy] [fix] `examples/forge-adapters/robotwin20/runtime/robotwin_simulation_probe_worker.py:L422-L535,L542-L603,L829-L891` 增加 profile-owned `uniform_time_dilation` retiming；以 Curobo/RoboTwin `1/250 s` 采样语义计算速度，保留 endpoint、原始 dtype 和 retiming sidecar，并继续执行 joint-limit/finite-value 校验。
- [Completed] [Policy] [Fix] `examples/forge-adapters/robotwin20/runtime/robotwin_simulation_probe_worker.py:L422-L535,L542-L603,L829-L891` adds profile-owned `uniform_time_dilation` retiming; it computes speed under Curobo/RoboTwin's `1/250 s` sampling semantics, preserves endpoints, source dtype, and a retiming sidecar, while retaining joint-limit and finite-value checks.
- [完成] [config] [feat] `examples/forge-adapters/robotwin20/profiles/robotwin20/route-inputs.yaml:L40-L51` 将 retiming 参数写入实体化 joint-limit policy：`0.004 s`、`0.95` safety margin、20,000 sample 上限；未提升 1.0 rad/s 策略，也未修改 Franka URDF 物理上限。
- [Completed] [Config] [Feat] `examples/forge-adapters/robotwin20/profiles/robotwin20/route-inputs.yaml:L40-L51` records retiming in the materialized joint-limit policy: `0.004 s`, `0.95` safety margin, and a 20,000-sample bound; it does not raise the 1.0 rad/s policy or alter Franka URDF physical limits.
- [完成] [eval] [test] PAOS 环境安装 NumPy `2.5.2`；adapter 回归 `228 passed, 1 skipped`，根仓库 `168 passed`，retiming/route 专项 `62 passed`，Ruff、compileall、`git diff --check` 全部通过。
- [Completed] [Eval] [Test] Installed NumPy `2.5.2` in the PAOS environment; adapter regression `228 passed, 1 skipped`, root regression `168 passed`, retiming/route focus `62 passed`, and Ruff, compileall, and `git diff --check` all passed.
- [完成] [eval] [exp] 新建不可覆盖 v6 package `/home/yanxu/robotwin20-runtime/artifacts/paos-route-inputs-20260905T204500Z/materialized/`；route digest `b253fdc0f58a683ca6c73b33853a95f5655af3f0d7af78f8416c03096ed8e85e`，source manifest digest `9d4f599d703170fb2a4b24b0b4b7bbb68588d39b8f24c5e0ba2bc4e640fd9b93`。连续 no-motion preflight 右臂八阶段通过、左臂 unavailable，`robot_control_steps=0`、`simulator_steps=0`、`motion_authorized=false`。
- [Completed] [Eval] [Exp] Created immutable v6 package `/home/yanxu/robotwin20-runtime/artifacts/paos-route-inputs-20260905T204500Z/materialized/`; route digest `b253fdc0f58a683ca6c73b33853a95f5655af3f0d7af78f8416c03096ed8e85e`, source-manifest digest `9d4f599d703170fb2a4b24b0b4b7bbb68588d39b8f24c5e0ba2bc4e640fd9b93`. Continuous no-motion preflight passes all eight right-arm phases and leaves the left arm unavailable, with `robot_control_steps=0`, `simulator_steps=0`, and `motion_authorized=false`.
- [完成] [docs] [review] 按架构集成、失败路径、权威边界、配置、可维护性五个维度复核：未发现新的 Blocker/Major；仍明确阻塞于 fresh human approval 以及 attached-object collision、真实 lift、接触动力学、语义放置 evidence，未接入 Gateway/Dora/Action/硬件。
- [Completed] [Docs] [Review] Re-reviewed architecture integration, failure paths, authority boundaries, configuration, and maintainability: no new Blocker/Major found; fresh human approval and attached-object collision, physical lift, contact-dynamics, and semantic-placement evidence remain blocking, with no Gateway/Dora/Action/hardware wiring.

## v5.4.1 (2026-09-05 16:40) - codex

### 预期修改 / Planned Changes

- [计划] [eval] [fix] 修复 simulation probe 成功路径的 observed-outcome v1/v2 契约断裂，并强化人工审批脚本的严格 schema、route digest、profile 和 artifact 绑定。
- [Planned] [Eval] [Fix] Fix the simulation-probe success-path observed-outcome v1/v2 contract break and strengthen strict schema, route-digest, profile, and artifact binding in the human-approval script.
- [计划] [eval] [test] 增加审批脚本、篡改输入、语义失败和 legacy outcome 拒绝测试；保持 motion/approval fail-closed。
- [Planned] [Eval] [Test] Add approval-script, tampered-input, semantic-failure, and legacy-outcome rejection tests while keeping motion and approval fail-closed.
- [计划] [docs] [docs] 更新 route package 验证结果、人工审核边界和后续真实 simulation probe 入口。
- [Planned] [Docs] [Docs] Update route-package validation results, the human-review boundary, and the next real simulation-probe entry point.

### 预期影响文件 / Planned Files

- `examples/forge-adapters/robotwin20/runtime/robotwin_simulation_probe_worker.py`
- `examples/forge-adapters/robotwin20/scripts/approve_simulation_probe.py`
- `examples/forge-adapters/robotwin20/tests/test_simulation_probe.py`
- `examples/forge-adapters/robotwin20/tests/test_approve_simulation_probe.py`
- `examples/forge-adapters/robotwin20/README.md`
- `docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md`
- `docs/forge/STATE_FILE_IMPLEMENTATION_REVIEW_20260903.md`

## v6.10.8 (2026-09-08 10:20) - codex

### 预期修改 / Planned Changes

- [计划] [policy] [fix] 修正 RoboTwin/Curobo provider 的原生 table 碰撞世界投影：复用仿真 scene 的真实 table pose 转换到各 arm base frame，避免沿用 `planner.py` 中与双臂 base 不一致的硬编码 table pose；不修改 GraspGen 位姿、不增加固定 z 偏移或速度参数。
- [Planned] [Policy] [Fix] Correct the RoboTwin/Curobo provider's native table collision projection by reusing the simulator scene table pose transformed into each arm base frame, instead of retaining the dual-arm-inconsistent hard-coded pose from `planner.py`; do not modify GraspGen poses or add fixed z offsets or speed parameters.
- [计划] [eval] [test] 增加 provider table-pose projection 回归，验证真实 scene table 与 planner frame 一致、缺失 table pose 时保持现有 provider 行为，并运行 collision-world/Curobo adapter 专项测试；保持 no-motion。
- [Planned] [Eval] [Test] Add provider table-pose projection regressions proving scene-table/planner-frame consistency, preserve existing behavior when no scene table pose is bound, and run focused collision-world/Curobo adapter tests with no motion.

### 预期影响文件 / Planned Files

- `examples/forge-adapters/robotwin20/runtime/robotwin_curobo_world_port.py`
- `examples/forge-adapters/robotwin20/runtime/robotwin_simulation_probe_worker.py`
- `examples/forge-adapters/robotwin20/tests/test_curobo_world_port.py`
- `examples/forge-adapters/robotwin20/tests/test_simulation_probe.py`
