## v0.4.0 (2026-09-01 13:07) - codex

### 变更摘要
- [完成] [sense] [feat] 实现 provider-neutral `manipulation.prepare` Query：对 `grasp.propose` 候选集做非侵入式 workspace/kinematic/collision 准备评估，返回带 provenance 的准备证据与绑定 observation/scene/frame/candidate-set 的确定性 preparation 引用；`prepared` 不等于动作授权，`motion_authorized` 恒为 `false`，不产生任何物理运动 (local)。

### Change Summary
- [Completed] [sense] [feat] Implement the provider-neutral `manipulation.prepare` Query: run non-mutating workspace/kinematic/collision readiness evaluation over one `grasp.propose` candidate set and return provenance-backed evidence plus a deterministic preparation reference bound to the observation, scene, frame, and candidate set; `prepared` never implies action admission, `motion_authorized` stays `false`, and no physical motion is produced (local).

### 预期影响文件
- `examples/forge-skills/scene-observe/contracts/manipulation.prepare.tool.yaml` [新增]
- `examples/forge-skills/scene-observe/src/scene_observe/manipulation_prepare.py` [新增]
- `examples/forge-skills/scene-observe/src/scene_observe/fake_gateway.py` [修改]
- `examples/forge-skills/scene-observe/tests/test_manipulation_prepare.py` [新增]
- `examples/forge-skills/scene-observe/tests/test_grasp_propose.py` [修改：/tools 发现列表与版本断言]
- `examples/forge-skills/scene-observe/skill.yaml`、`pyproject.toml`（0.3.0 → 0.4.0）、`SKILL.md`、`README.md`、`CHANGELOG.md` [修改]
- `examples/forge-skills/scene-observe/src/scene_observe/__init__.py` [修改：导出 manipulation.prepare 公共接口]
- 分支：`feature/manipulation-prepare`（自 `feature/grasp-propose` `f646aab` 创建）

### 验证
- `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=src:/home/yanxu/PhyAgentOS-forge/PhyAgentOS python -m pytest -p pytest_asyncio.plugin -q` → 121 passed。
- `ruff check .`、`python -m compileall -q src tests`、`git diff --check` → passed。
- `python scripts/package_skill.py examples/forge-skills/scene-observe --output-dir <tmp> --force` → `scene-observe-0.4.0.tar.gz`，SHA-256 `96e4d0dcf50b274fe3708b521de810a85ec12ee43786deb3c756955620f3c365`，26,966 bytes。

### 文件变更详情

#### [新增] `examples/forge-skills/scene-observe/src/scene_observe/manipulation_prepare.py` L1-L463

**新增代码摘要：**
```python
class PreparationProvider(Protocol):
    def prepare(self, request: dict[str, Any]) -> PreparationSnapshot | None: ...

class ManipulationPreparationEndpoint:
    """Read-only readiness evaluation; never authorizes or produces motion."""
```

**修改说明**：新增 provider-neutral `manipulation.prepare` Query；在 provider 调用前
拒绝过期观测、缺失标定和非法候选；校验候选/实体/frame/unit/provenance 绑定及三项
readiness checks；仅全为 `pass` 的候选可标记 `prepared`，所有输出保持
`motion_authorized: false`。

#### [新增] `examples/forge-skills/scene-observe/contracts/manipulation.prepare.tool.yaml` L1-L114

**新增代码摘要：**
```yaml
tool_id: manipulation.prepare
semantics: query
output_schema:
  properties:
    motion_authorized: {const: false}
```

**修改说明**：发布严格 ToolSpec，输入/输出禁止未知字段，契约与 Python
`MANIPULATION_TOOL_SPEC` 逐字段一致。

#### [修改] `examples/forge-skills/scene-observe/src/scene_observe/fake_gateway.py` L18-L26, L242-L389

**修改前：** Fake Gateway 只注册 `scene.observe`、`scene.understand`、`grasp.propose`。

**修改后：** 注册 `manipulation.prepare` 的 ToolSpec、context 和
`/tools/manipulation_preparation/prepare:invoke` Query 路由；未配置 provider 返回
503，未创建 Action/Session/运动路由。

#### [新增] `examples/forge-skills/scene-observe/tests/test_manipulation_prepare.py` L1-L521

**新增代码摘要：**
```python
async with ForgeToolClient("http://fake", transport=transport) as client:
    result = await client.invoke_query_tool("manipulation.prepare", arguments)
```

**修改说明**：覆盖 discovery、严格 schema、成功/部分/空结果、stale、缺失
calibration、provider 不可用/异常、非法候选和 malformed snapshot，以及无
Action/Session/运动准入证明。

#### [修改] Bundle 文档、版本和公共导出

- `skill.yaml` L1-L15：版本 `0.3.0` → `0.4.0`，增加 `manipulation.prepare` 并同步描述。
- `pyproject.toml` L1-L6：Python 包版本 `0.3.0` → `0.4.0`。
- `SKILL.md` L46-L58、`README.md` L3-L24：记录固定四阶段只读工作流、准备检查边界和运动授权边界。
- `src/scene_observe/__init__.py` L9-L33：导出 `MANIPULATION_TOOL_SPEC`、Endpoint、Provider 和 Snapshot。
- `CHANGELOG.md` L3-L32：增加 v0.4.0 双语 Bundle 变更与验证记录。
- `tests/test_grasp_propose.py` L170-L173, L218-L222：同步 Tool discovery 和版本断言。

**关键版本 Diff：**
```text
version: "0.3.0"  ->  version: "0.4.0"
required_tools: [scene.observe, scene.understand, grasp.propose]
                -> [scene.observe, scene.understand, grasp.propose, manipulation.prepare]
```

### Git 提交
- Commit: `3d686da`
- Branch: `feature/manipulation-prepare`
- 时间: 2026-09-01 15:05 (Asia/Shanghai)

## v0.3.1 (2026-09-01 13:10) - codex

### 变更摘要
- [完成] [sense] [fix] 让 grasp.propose provider 异常和 malformed snapshot 以契约化错误 fail-closed 返回 (local)。
- [完成] [sense] [fix] 将包含三个 Query 能力的 Skill Bundle 与 Python 包版本统一升级到 0.3.0，避免 PAOS Skill/ToolSpec binding 漂移 (local)。

### Change Summary
- [Completed] [sense] [fix] Make grasp.propose fail closed with contract errors when a provider raises or returns a malformed snapshot (local).
- [Completed] [sense] [fix] Bump the three-Query Skill Bundle and Python package to 0.3.0 so PAOS Skill/ToolSpec bindings cannot alias older content (local).

### 影响文件
- `examples/forge-skills/scene-observe/src/scene_observe/grasp_proposal.py`
- `examples/forge-skills/scene-observe/tests/test_grasp_propose.py`
- `examples/forge-skills/scene-observe/skill.yaml`
- `examples/forge-skills/scene-observe/pyproject.toml`

### Affected Files
- `examples/forge-skills/scene-observe/src/scene_observe/grasp_proposal.py`
- `examples/forge-skills/scene-observe/tests/test_grasp_propose.py`
- `examples/forge-skills/scene-observe/skill.yaml`
- `examples/forge-skills/scene-observe/pyproject.toml`

### 文件变更详情

#### [修改] `examples/forge-skills/scene-observe/src/scene_observe/grasp_proposal.py` L423-L434, L519-L551

**修改前：** provider 结果直接迭代，provider 异常向 Gateway 传播。

**修改后：** 校验 snapshot 类型及 candidates/ambiguities 容器；捕获 provider 异常并返回 `grasp_proposal_provider_error`。

**修改说明**：保持 Query 失败关闭，避免未定义 HTTP 500。

#### [新增] `examples/forge-skills/scene-observe/tests/test_grasp_propose.py` L143-L146, L319-L342, L213-L221

**新增代码**：provider 异常与 malformed snapshot 的 fail-closed 回归测试。

#### [修改] `examples/forge-skills/scene-observe/skill.yaml` L3；`pyproject.toml` L3

**修改前：** `0.1.0`

**修改后：** `0.3.0`

**修改说明**：使 Bundle manifest、Python 包和三个 Query 能力的冻结版本一致。

### 验证

- `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=src python -m pytest -p pytest_asyncio.plugin -q` → 65 passed。
- `ruff check .`、`python -m compileall -q src tests`、`git diff --check` → passed。
- `python scripts/package_skill.py examples/forge-skills/scene-observe --output-dir <tmp> --force` → `scene-observe-0.3.0.tar.gz`, SHA-256 `210163687b68af88e9d48cd873fb4c5f18b16062171032eae417c71f74f27c45`, 21266 bytes。

### Verification

- `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=src python -m pytest -p pytest_asyncio.plugin -q` -> 65 passed.
- `ruff check .`, `python -m compileall -q src tests`, and `git diff --check` -> passed.
- `python scripts/package_skill.py examples/forge-skills/scene-observe --output-dir <tmp> --force` -> `scene-observe-0.3.0.tar.gz`, SHA-256 `210163687b68af88e9d48cd873fb4c5f18b16062171032eae417c71f74f27c45`, 21266 bytes.

### Git 提交
- Commit: `e97682d`
- Branch: `feature/grasp-propose`
- 时间: 2026-09-01 13:00 (Asia/Shanghai)

### Git Commit
- Commit: `e97682d`
- Branch: `feature/grasp-propose`
- Time: 2026-09-01 13:00 (Asia/Shanghai)
## v0.5.0 (2026-09-01 15:20) - codex

### 变更摘要
- [完成] [policy] [feat] 实现 provider-neutral `object.acquire` bounded Action：消费 `manipulation.prepare` 的 preparation/candidate 引用，通过标准 Action admission 与 invocation status/result/cancel 生命周期执行有界 acquire；内部 approach/contact/close/lift/hold 阶段不暴露为 Agent 工具，终态返回版本化 `capability_outcome_summary_v1`，并保留 unknown/cancellation/并发拒绝语义 (local)。

### Change Summary
- [Completed] [policy] [feat] Implement the provider-neutral `object.acquire` bounded Action: consume `manipulation.prepare` preparation/candidate references through standard Action admission and invocation status/result/cancel lifecycle; keep approach/contact/close/lift/hold internal, return a versioned `capability_outcome_summary_v1` at terminal state, and preserve unknown, cancellation, and concurrency-rejection semantics (local).

### 影响文件 / Affected Files
- `examples/forge-skills/scene-observe/contracts/object.acquire.tool.yaml` [新增 / added]
- `examples/forge-skills/scene-observe/src/scene_observe/object_acquire.py` [新增 / added]
- `examples/forge-skills/scene-observe/src/scene_observe/fake_gateway.py` [修改 / modified]
- `examples/forge-skills/scene-observe/src/scene_observe/__init__.py` [修改 / modified]
- `examples/forge-skills/scene-observe/tests/test_object_acquire.py` [新增 / added]
- `examples/forge-skills/scene-observe/tests/test_grasp_propose.py`、`tests/test_manipulation_prepare.py` [修改 / modified]
- `examples/forge-skills/scene-observe/skill.yaml`、`pyproject.toml`、`SKILL.md`、`README.md`、`CHANGELOG.md` [修改 / modified]

### 文件变更详情 / File Change Details

#### [新增 / Added] `examples/forge-skills/scene-observe/src/scene_observe/object_acquire.py` L1-L473

**关键代码 / Key code:**
```python
class ObjectAcquireEndpoint:
    """Validate and admit one bounded Action without owning Gateway lifecycle state."""

    def admit(self, arguments: Any) -> AcquireAdmission | AcquireRejection:
        # strict reference, freshness, provider, and terminal-summary checks
```

**修改说明 / Description:** 对 Action 输入、准备引用、provider 快照和终态摘要执行
fail-closed 校验；内部阶段不进入 Tool 输入，终态只投影 provider-neutral、版本化摘要。
Validates Action inputs, preparation bindings, provider snapshots, and terminal summaries
fail-closed; internal phases stay out of Tool inputs and terminal results expose only the
versioned provider-neutral summary.

#### [新增 / Added] `examples/forge-skills/scene-observe/contracts/object.acquire.tool.yaml` L1-L76

**关键代码 / Key code:**
```yaml
tool_id: object.acquire
semantics: action
max_concurrency: 1
cancellation: supported_via_common_cancel_route
unknown_semantics: terminal_for_accounting_not_physical_stop
```

**修改说明 / Description:** 发布严格 Action ToolSpec，绑定 observation/frame/calibration/
candidate/preparation 引用，并禁止 provider、仿真器和低层阶段字段。
Publishes a strict Action ToolSpec bound to observation/frame/calibration/candidate/preparation
references, with no provider, simulator, or low-level phase fields.

#### [修改 / Modified] `examples/forge-skills/scene-observe/src/scene_observe/fake_gateway.py` L31-L38, L244-L541

**修改前 / Before:** Fake Gateway 只提供三个 Query 路由。

**修改后 / After:** 注册第五个 Tool，增加 `/tools/object.acquire:invoke` 及通用
`/invocations/{id}` status/result/cancel 生命周期，支持 pending、terminal、cancel 和 unknown。
Registered the fifth Tool and added the standard Action admission plus invocation status/result/
cancel lifecycle with pending, terminal, cancellation, and unknown states.

#### [新增 / Added] `examples/forge-skills/scene-observe/tests/test_object_acquire.py` L1-L303

覆盖 discovery、严格 schema、绑定/新鲜度/标定拒绝、provider 错误、并发、pending、cancel、
unknown、malformed summary、Tool API 路由和不拆分内部阶段等验收项。
Coverage includes discovery, strict schema, binding/freshness/calibration rejection, provider
errors, concurrency, pending, cancel, unknown, malformed summaries, routing, and internal-phase
non-exposure.

#### [修改 / Modified] Bundle metadata and docs

- `skill.yaml` L1-L22、`pyproject.toml` L1-L6：版本 `0.4.0` → `0.5.0`，加入 `object.acquire`。
- `SKILL.md` L25-L74、`README.md` L3-L43：记录 Query → Action 工作流、物理效果边界和对账规则。
- `src/scene_observe/__init__.py` L15-L46：导出 Action snapshot/admission/provider 接口。
- `tests/test_grasp_propose.py` L170-L174、`tests/test_manipulation_prepare.py` L170-L174：同步五 Tool discovery 与版本断言。
- `CHANGELOG.md` L3-L22：增加 v0.5.0 双语 Bundle 记录。

**关键版本 diff / Key version diff:**
```text
version: "0.4.0"  ->  version: "0.5.0"
required_tools: [..., manipulation.prepare]
                -> [..., manipulation.prepare, object.acquire]
```

### 验证 / Verification
- `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=src:/home/yanxu/PhyAgentOS-forge/PhyAgentOS python -m pytest -p pytest_asyncio.plugin -q examples/forge-skills/scene-observe/tests` → 143 passed。
- `ruff check examples/forge-skills/scene-observe` → passed。
- `python -m compileall -q examples/forge-skills/scene-observe/src examples/forge-skills/scene-observe/tests` → passed。
- `git diff --check` → passed。
- Bundle：`scene-observe-0.5.0.tar.gz`，SHA-256 `8d4bc6b99113a17b4c76c4f40d5c5366db037bc177d22f571245b32c45f8924c`，35000 bytes。

### Git 提交
- Commit: `292457e`
- Branch: `feature/object-acquire`
- 时间: 2026-09-01 (Asia/Shanghai)

## v0.6.0 (2026-09-01 19:10) - codex

### 变更摘要 / Change Summary
- [完成] [policy] [feat] 新增 provider-neutral `object.place` bounded Action，消费已成功 acquire invocation 与准备引用，通过标准 Forge Action admission 和 invocation 生命周期执行 transport/descent/release/retreat；阶段保持 Gateway 内部，并保留取消、unknown 和释放后证据语义。(local)
- [Completed] [policy] [feat] Added the provider-neutral `object.place` bounded Action. It consumes a successful acquire invocation and preparation references through the standard Forge Action admission and invocation lifecycle; transport/descent/release/retreat remain Gateway-internal while cancellation, unknown, and post-release evidence semantics are preserved. (local)

### 预期影响文件 / Expected Files
- `examples/forge-skills/scene-observe/contracts/object.place.tool.yaml` [新增 / added]
- `examples/forge-skills/scene-observe/src/scene_observe/object_place.py` [新增 / added]
- `examples/forge-skills/scene-observe/src/scene_observe/fake_gateway.py` [修改 / modified]
- `examples/forge-skills/scene-observe/src/scene_observe/__init__.py` [修改 / modified]
- `examples/forge-skills/scene-observe/tests/test_object_place.py` [新增 / added]
- `examples/forge-skills/scene-observe/tests/test_object_acquire.py` [修改：Tool discovery / modified: Tool discovery]
- `examples/forge-skills/scene-observe/skill.yaml`、`pyproject.toml`、`SKILL.md`、`README.md`、`CHANGELOG.md` [修改 / modified]

### 验收目标 / Acceptance Targets
- ToolSpec 使用 `semantics: action` 和标准 `/tools/object.place:invoke`、`/invocations` 路由；不暴露 RoboTwin、SAPIEN、provider、task 或低层 phase 字段。
- Admission 严格绑定 observation、scene revision、frame、calibration、candidate-set、preparation、candidate、entity 和 provider-neutral destination reference；stale、missing calibration、非法绑定、provider 不可用和并发超限均 fail-closed。
- 终态只返回版本化 `capability_outcome_summary_v1`，其中 `capability_phase` 限定为 `transport|descent|release|retreat|none`，并包含 `world_change_started`、`outcome_known`、`evidence_availability`、`artifact_refs` 和 `bounded_metric_names`。
- Fake Gateway 支持 pending、terminal、cancel accepted、unknown 和 post-release evidence；不新增独立执行协议、Session 或运动绕过路由。
- 测试使用真实 `ForgeToolClient` 覆盖 discovery、契约严格性、引用绑定、生命周期、失败归因、provider-neutral 和内部 phase 不暴露；PAOS 核心不修改。
- 实际实现：`object_place.py` L1-L550 新增严格输入/快照校验、成功 acquire 绑定和版本化终态摘要；`object.place.tool.yaml` L1-L91 新增与 `PLACE_TOOL_SPEC` 完全一致的严格契约。
- Fake Gateway 修改 `fake_gateway.py` L39-L49、L258-L289、L297-L429、L469-L583，注册 place Tool、标准 Action 路由、按 Tool 隔离并发 lease，并在 provider 前拒绝未找到/未终态/未成功的 acquire invocation。
- 测试新增 `test_object_place.py` L1-L340 共 15 个测试场景；同步 `test_grasp_propose.py` L174-L225、`test_manipulation_prepare.py` L153-L157、`test_object_acquire.py` L120-L127 的 discovery/version 断言。
- Bundle 元数据与文档：`skill.yaml` L1-L16、`pyproject.toml` L1-L6、`__init__.py` L1-L63、`SKILL.md` L25-L91、`README.md` L3-L55、Bundle `CHANGELOG.md` L3-L31。
- 关键 Diff：`required_tools` 增加 `object.place`；版本 `0.5.0 -> 0.6.0`；新增 `acquire_invocation_ref` 与 opaque `destination_ref`；摘要新增 `post_release_evidence`；Fake Gateway invocation 记录新增 `tool_id`/`arguments` 并改为按工具并发检查。
- 验证：`PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=examples/forge-skills/scene-observe/src:PhyAgentOS python -m pytest -p pytest_asyncio.plugin -q examples/forge-skills/scene-observe/tests` -> `158 passed`；`ruff check examples/forge-skills/scene-observe`、`compileall`、`git diff --check` 均通过。
- Bundle：`scene-observe-0.6.0.tar.gz`，SHA-256 `9f3da60106984100bde2e3f19773ccc24d27d51713af280c3ede65e376673455`，39,855 bytes。
- Git 提交：`e51dbc9`，分支 `feature/object-place`，时间 2026-09-01 (Asia/Shanghai)。

## v0.7.0 (2026-09-02 00:00) - codex

### 变更摘要 / Change Summary
- [完成] [policy] [feat] 新增长程 pick-and-place workflow 编排投影：在一个 AgentTask 语义下固定 observe → understand → propose → prepare → acquire → place 的阶段顺序，记录每阶段的引用与终态，拒绝跳步、陈旧引用、unknown 盲重试，并为 recovery 保留可追加的下一阶段状态；实际执行继续经过现有 ForgeToolClient/AgentTask 路径，不新增执行协议。(local)
- [Completed] [policy] [feat] Added a long-horizon pick-and-place workflow orchestration projection. Under one AgentTask semantic, enforce observe → understand → propose → prepare → acquire → place ordering, record per-step references and terminal states, reject skipped steps, stale bindings, and blind retries of unknown outcomes, and preserve an appendable next-step state for recovery. Execution continues through the existing ForgeToolClient/AgentTask path; no second execution protocol is introduced. (local)

### 预期影响文件 / Expected Files
- `examples/forge-skills/scene-observe/src/scene_observe/long_horizon.py` [新增 / added]
- `examples/forge-skills/scene-observe/tests/test_long_horizon.py` [新增 / added]
- `examples/forge-skills/scene-observe/src/scene_observe/__init__.py` [修改 / modified]
- `examples/forge-skills/scene-observe/skill.yaml`、`pyproject.toml`、`SKILL.md`、`README.md`、`CHANGELOG.md` [修改 / modified]

### 验收目标 / Acceptance Targets
- 编排器只描述阶段顺序、opaque 引用和状态，不复制 Tool API 或 provider payload；不增加新的 Gateway route、Action、Session 或运动授权字段。
- 只有前一阶段 terminal success 才能推进下一阶段；`unknown`、`cancelled`、`failed` 停止自动推进并要求显式 recovery/reconciliation。
- 所有引用必须与同一 AgentTask/workflow revision 绑定；新鲜度与 scene/candidate/acquire/place 关联不一致时 fail-closed。
- 输出可序列化、可重放且不包含坐标、RoboTwin、SAPIEN、provider、task 或低层 phase 字段；PAOS 核心不修改。
- 测试覆盖完整成功链、跳步、非法状态迁移、unknown/取消/失败、引用漂移、恢复追加和 provider-neutral 约束，并继续通过真实 Forge Tool API 现有测试集。
- 实际实现：`long_horizon.py` L1-L275 新增 `LongHorizonWorkflow`、`WorkflowState`/`WorkflowStep` 和严格引用/状态 reducer；`test_long_horizon.py` L1-L133 新增 11 个编排与恢复测试。
- Bundle 文档与导出：`__init__.py` L1-L73、`skill.yaml` L1-L16、`pyproject.toml` L1-L6、`SKILL.md` L77-L108、`README.md` L33-L47、Bundle `CHANGELOG.md` L3-L31。
- 关键 Diff：新增固定六阶段 `WORKFLOW_VERSION=pick_and_place_workflow_v1`；新增 `record()`/`begin_recovery()`/`snapshot()`，仅接受终态和 opaque 引用；不新增 Gateway route、Session、Action 或运动授权。
- 验证：`PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=examples/forge-skills/scene-observe/src:PhyAgentOS python -m pytest -p pytest_asyncio.plugin -q examples/forge-skills/scene-observe/tests` -> `169 passed`；`ruff check examples/forge-skills/scene-observe`、`compileall`、`git diff --check` 均通过。
- Bundle：`scene-observe-0.7.0.tar.gz`，SHA-256 `f285ee78ee7dbf3374f3a1e86b025ad6860a4fb065ca4fc62f9542bda1eb0357`，46,093 bytes。
- Git 提交：`4e3c57e`，分支 `feature/long-horizon-workflow`，时间 2026-09-02 (Asia/Shanghai)。

## v0.8.0 (2026-09-02 00:00) - codex

### 变更摘要 / Change Summary
- [计划] [policy] [feat] 在 PAOS 通用 verification 层增加 capability outcome projection：从终态 ToolExecutionRecord 的版本化 `capability_outcome_summary_v1` 生成仅用于任务级验证的 execution facts，保留 unknown/失败/释放后证据语义，不把 Gateway artifact 自动升级为验证证据，也不允许投影直接判定任务成功。(local)
- [Planned] [policy] [feat] Add a capability outcome projection in the generic PAOS verification layer. Convert versioned `capability_outcome_summary_v1` values from terminal ToolExecutionRecords into task-level execution facts while preserving unknown/failure/post-release evidence semantics; do not promote Gateway artifacts into verified evidence or let the projection decide task success. (local)

### 预期影响文件 / Expected Files
- `PhyAgentOS/verification/outcome_projection.py` [新增 / added]
- `PhyAgentOS/verification/request_builder.py` [修改 / modified]
- `PhyAgentOS/verification/__init__.py`、`docs/en/03-developer-manual.md`、`docs/zh/03-developer-manual.md` [修改 / modified]
- `examples/forge-skills/scene-observe/tests/test_outcome_projection.py` [新增 / added]
- `CHANGELOG.md` [修改 / modified]

### 验收目标 / Acceptance Targets
- 只读取终态 Action 记录和标准 `capability_outcome_summary_v1`；未知版本、字段、枚举、引用或状态不一致时产生 bounded projection error，不抛出未定义异常。
- Projection 明确标记 `execution_fact_only`、`task_success_authorized=false`；summary artifact refs 保持 opaque，不加入 `valid_evidence_refs`。
- `VerificationRequestBuilder.build_agent_task` 将 projection 与错误放入 verifier context，同时保留既有 Gateway terminal results、evidence 和 AgentTask verdict 流程。
- 测试覆盖 acquire/place 成功、unknown、失败、释放后证据、malformed summary、非 Action/非终态跳过、状态不一致、provider-neutral 和 no-motion/no-new-route 约束。

### 实际修改 / Actual Changes
- [完成] 新增 `outcome_projection.py` L1-L264：校验并投影终态 Action 的 `capability_outcome_summary_v1`，保留失败/unknown/释放后证据语义，artifact 引用保持 opaque，并固定 `execution_fact_only` 与 `task_success_authorized=false`。(local)
- [Completed] Added `outcome_projection.py` L1-L264 to validate and project terminal Action `capability_outcome_summary_v1` values, preserve failure/unknown/post-release semantics, keep artifact references opaque, and force `execution_fact_only` with `task_success_authorized=false`. (local)
- [完成] 修改 `request_builder.py` L14-L18、L123-L185 与 `verification/__init__.py` L21-L50，将 projection 及有界错误注入 AgentTask verifier context，但不扩展 `valid_evidence_refs`。(local)
- [Completed] Updated `request_builder.py` L14-L18, L123-L185 and `verification/__init__.py` L21-L50 to add projections and bounded errors to AgentTask verifier context without extending `valid_evidence_refs`. (local)
- [完成] 更新双语 Developer Manual `docs/en/03-developer-manual.md` L118-L129、`docs/zh/03-developer-manual.md` L115-L125，明确投影的权威边界和禁止的执行副作用。(local)
- [Completed] Updated the bilingual Developer Manual at `docs/en/03-developer-manual.md` L118-L129 and `docs/zh/03-developer-manual.md` L115-L125 to document projection authority and prohibited execution side effects. (local)
- [完成] 新增 `test_outcome_projection.py` L1-L180 共 14 个测试，覆盖成功、失败、unknown、状态/版本/字段错误、释放后证据、缺失摘要和 AgentTask 集成。(local)
- [Completed] Added 14 tests in `test_outcome_projection.py` L1-L180 covering success, failure, unknown, status/version/field errors, post-release evidence, missing summaries, and AgentTask integration. (local)
- [完成] 验证：scene-observe 测试 `183 passed`；`ruff check`、`compileall`、`git diff --check` 均通过。(local)
- [Completed] Validation: scene-observe tests `183 passed`; `ruff check`, `compileall`, and `git diff --check` all passed. (local)

### Git 提交 / Git Commit

## v5.3.0 (2026-09-05 14:33) - codex

### 变更摘要 / Change summary
- [policy] [feat] [计划] 新增 calibration-bound grasp adaptation，将 observation-frame GraspGen pose 严格转换到 profile-owned route frame，并显式应用 provider-to-TCP transform。
- [policy] [feat] [planned] Add calibration-bound grasp adaptation that transforms observation-frame GraspGen poses into the profile-owned route frame and explicitly applies the provider-to-TCP transform.

### 预期影响文件 / Expected files
- `examples/forge-adapters/robotwin20/src/robotwin20_adapter/grasp_adaptation.py`
- `examples/forge-adapters/robotwin20/src/robotwin20_adapter/__init__.py`
- `examples/forge-adapters/robotwin20/tests/test_grasp_adaptation.py`

- Commit: `de9a479`
- Branch: `feature/long-horizon-workflow`
- 时间 / Time: 2026-09-02 (Asia/Shanghai)

### 额外同步 / Additional Sync
- [完成] 更新根 `CHANGELOG.md`，加入 v1.3.0 Lesson activation scope 记录。(local)
- [Completed] Updated root `CHANGELOG.md` with the v1.3.0 Lesson activation scope entry. (local)

### Git 提交 / Git Commit
- Commit: `e12dbcb`
- Branch: `feature/long-horizon-workflow`
- 时间 / Time: 2026-09-02 (Asia/Shanghai)

## v1.4.0 (2026-09-02 00:00) - codex

### 变更摘要 / Change Summary
- [完成] [policy] [feat] 为 active Lesson counterexample 增加 capability owner scope 校验：成功 episode 只有在与 Lesson scope 一致时才计入反证支持；scope 不一致或 scoped/legacy 缺失时只记录 bounded 诊断，不退休或削弱 Lesson。(local)
- [Completed] [policy] [feat] Add capability owner-scope validation to active-Lesson counterexamples: count a successful episode as counterevidence only when its scope matches the Lesson; mismatched or scoped/legacy-missing scopes record bounded diagnostics without retiring or weakening the Lesson. (local)

### 预期影响文件 / Expected Files
- `PhyAgentOS/agent/experience/contracts.py` [修改 / modified]
- `PhyAgentOS/agent/experience/attribution.py` [修改 / modified]
- `PhyAgentOS/agent/experience/evolution.py` [修改 / modified]
- `docs/en/05-agent-experience-and-skill-evolution.md`、`docs/zh/05-agent-experience-and-skill-evolution.md` [修改 / modified]
- `examples/forge-skills/scene-observe/tests/test_experience_attribution.py` [修改 / modified]

### 验收目标 / Acceptance Targets
- `ScopedLesson` 持久化 bounded capability owner scope；counterexample 只接受精确 scope 匹配或双方都为空的 legacy 情况。
- 不一致 scope 不增加 `counterexample_episode_ids`，不触发 `lesson_retired` 或 cluster reopen；只记录幂等 bounded event。
- 不修改 AgentTask verdict、Tool API、运动权限、Skill candidate 支持和 promotion threshold。
- 测试覆盖匹配、不同 owner、scoped/legacy 缺失和成功 episode 其他演化路径。

### 实际修改 / Actual Changes
- [完成] `ScopedLesson` 在 `contracts.py` L387-L410 持久化 bounded `capability_failure_owners`；构建 Lesson 时继承 cluster scope。(local)
- [Completed] `ScopedLesson` persists bounded `capability_failure_owners` in `contracts.py` L387-L410; Lesson construction inherits the cluster scope.
- [完成] `attribution.py` L153-L178 新增 `validate_counterexample_scope`；`evolution.py` L426-L454 在反证前执行精确 scope 匹配，不一致时仅记录幂等诊断。(local)
- [Completed] Added `validate_counterexample_scope` in `attribution.py` L153-L178; `evolution.py` L426-L454 now requires exact scope matching before counterevidence and records only an idempotent diagnostic on mismatch.
- [完成] 新增 `test_experience_attribution.py` L181-L243 共 3 个测试，覆盖匹配、不同 owner、legacy 空 scope 和“不退休”行为；全量测试 `200 passed`，ruff、compileall、git diff --check 均通过。(local)
- [Completed] Added 3 tests in `test_experience_attribution.py` L181-L243 covering matching, owner mismatch, legacy-empty scope, and no-retirement behavior; full tests `200 passed`, with ruff, compileall, and git diff checks passing. (local)

### Git 提交 / Git Commit
- Commit: `614551d`
- Branch: `feature/long-horizon-workflow`
- 时间 / Time: 2026-09-02 (Asia/Shanghai)

### 额外同步 / Additional Sync
- [完成] 更新根 `CHANGELOG.md`，加入 v1.4.0 Lesson counterexample scope 记录。(local)
- [Completed] Updated root `CHANGELOG.md` with the v1.4.0 Lesson counterexample scope entry. (local)

### Git 提交 / Git Commit
- Commit: `10361b4`
- Branch: `feature/long-horizon-workflow`
- 时间 / Time: 2026-09-02 (Asia/Shanghai)

### 追加修正 / Follow-up Correction
- [完成] 补齐 `experience.__init__` 对 `validate_counterexample_scope` 的导出。(local)
- [Completed] Completed the `experience.__init__` export for `validate_counterexample_scope`. (local)

## v1.5.0 (2026-09-02 00:00) - codex

### 变更摘要 / Change Summary
- [完成] [policy] [feat] 为 SkillCandidate 增加 capability failure-owner scope：成功 episode 只有在相同 Skill/workflow 与相同 scope 下才累计 candidate support；不同 scope 创建独立 candidate revision，保持 promotion 门槛与 AgentTask verdict 不变。(local)
- [Completed] [policy] [feat] Add capability failure-owner scope to `SkillCandidate`: successful episodes accumulate support only within the same Skill/workflow and scope; different scopes create independent candidate revisions while preserving promotion thresholds and AgentTask verdicts. (local)

### 预期影响文件 / Expected Files
- `PhyAgentOS/agent/experience/contracts.py` [修改 / modified]
- `PhyAgentOS/agent/experience/evolution.py` [修改 / modified]
- `docs/en/05-agent-experience-and-skill-evolution.md`、`docs/zh/05-agent-experience-and-skill-evolution.md` [修改 / modified]
- `examples/forge-skills/scene-observe/tests/test_experience_attribution.py` [修改 / modified]

### 验收目标 / Acceptance Targets
- Candidate scope 只使用已持久化的 bounded owner 列表；artifact、failure code、provider 字段不进入 candidate。
- 同 scope 支持继续累计；不同 scope 不得覆盖 proposal、合并 support episode 或共享 promotion 计数。
- legacy 空 scope 与空 scope 保持兼容；空/非空 scope 不合并。
- 不新增 Tool/Gateway/运动接口；测试覆盖 scope 分桶和全量回归。

### 实际修改 / Actual Changes
- [完成] `SkillCandidate` 在 `contracts.py` L411-L430 增加 bounded `capability_failure_owners` 字段，候选 contract 不含 artifact/provider 数据。(local)
- [Completed] Added bounded `capability_failure_owners` to `SkillCandidate` in `contracts.py` L411-L430; candidate contracts contain no artifact/provider data.
- [完成] `evolution.py` L473-L519 将 candidate identity 与 support 按 owner scope 分桶；不同 scope 创建独立 candidate，不共享 promotion 计数。(local)
- [Completed] Partitioned candidate identity and support by owner scope in `evolution.py` L473-L519; different scopes create independent candidates and do not share promotion counts.
- [完成] 双语 Experience 文档更新；`test_experience_attribution.py` L235-L285 新增 scope 分桶测试；全量测试 `201 passed`，ruff、compileall、git diff --check 均通过。(local)
- [Completed] Updated bilingual Experience docs; added scope partition tests in `test_experience_attribution.py` L235-L285; full tests `201 passed`, with ruff, compileall, and git diff checks passing.

### Git 提交 / Git Commit
- Commit: `fa31be7`
- Branch: `feature/long-horizon-workflow`
- 时间 / Time: 2026-09-02 (Asia/Shanghai)

### 额外同步 / Additional Sync
- [完成] 更新根 `CHANGELOG.md`，加入 v1.5.0 Skill candidate scope 记录。(local)
- [Completed] Updated root `CHANGELOG.md` with the v1.5.0 Skill candidate scope entry. (local)

### Git 提交 / Git Commit
- Commit: `81b6bbf`
- Branch: `feature/long-horizon-workflow`
- 时间 / Time: 2026-09-02 (Asia/Shanghai)

## v0.9.0 (2026-09-02 00:00) - codex

### 变更摘要 / Change Summary
- [完成] [policy] [feat] 将已验证的 capability outcome 以 provider-neutral、去敏的 execution facts 接入 AgentTaskOutcomeSource 和 TaskEpisode，为 ExperienceAnalyzer/Skill evolution 提供阶段、状态、失败归因和证据可用性上下文；不改变任务 verdict、Tool API、晋升门槛或运动权限。(local)
- [Completed] [policy] [feat] Feed verified capability outcomes into `AgentTaskOutcomeSource` and `TaskEpisode` as provider-neutral, redacted execution facts for ExperienceAnalyzer and Skill evolution, without changing task verdicts, the Tool API, promotion thresholds, or motion authority. (local)

### 预期影响文件 / Expected Files
- `PhyAgentOS/agent/experience/contracts.py` [修改 / modified]
- `PhyAgentOS/agent/experience/source.py` [修改 / modified]
- `PhyAgentOS/agent/experience/__init__.py` [修改 / modified]
- `PhyAgentOS/agent/experience/analyzer.py` [修改 / modified]
- `docs/en/05-agent-experience-and-skill-evolution.md`、`docs/zh/05-agent-experience-and-skill-evolution.md` [修改 / modified]
- `examples/forge-skills/scene-observe/tests/test_experience_capability_facts.py` [新增 / added]

### 验收目标 / Acceptance Targets
- 仅从现有 `project_terminal_outcomes` 消费合法终态 Action 摘要；artifact URI 不进入学习 contract，错误保持 bounded 且不伪造事实。
- `TaskOutcomeEnvelope`/`TaskEpisode` 能序列化 capability facts，且 facts 不含 provider、RoboTwin、坐标、原始输入、端点或可执行权限字段。
- AgentTask outcome source 保持原 verdict/learnable 语义；projection 错误只作为诊断上下文，不授予成功或晋升资格。
- 测试覆盖成功/失败/unknown、projection error、去敏和与现有 ExperienceCoordinator 数据流兼容性。

### 实际修改 / Actual Changes
- [完成] `contracts.py` L53-L124 新增 `CapabilityOutcomeFact` 与 `CapabilityOutcomeErrorFact`，并将其作为可选字段加入 `TaskOutcomeEnvelope`；仅保存有界状态、phase、failure owner、世界变化和证据可用性。(local)
- [Completed] Added `CapabilityOutcomeFact` and `CapabilityOutcomeErrorFact` in `contracts.py` L53-L124 and optional fields on `TaskOutcomeEnvelope`, retaining only bounded status, phase, failure owner, world-change, and evidence-availability facts. (local)
- [完成] `source.py` L15-L31、L179-L226`AgentTaskOutcomeSource` 接入 `project_terminal_outcomes`，对 capability 名称做 provider-private marker 过滤；artifact URI 与 failure code 不进入 episode，错误只保存为 bounded diagnostic code。(local)
- [Completed] `source.py` L15-L31 and L179-L226 integrate `project_terminal_outcomes` into `AgentTaskOutcomeSource`; provider-private capability markers are filtered, artifact URIs and failure codes stay out of episodes, and errors are retained only as bounded diagnostic codes. (local)
- [完成] `analyzer.py` L30-L34、双语 evolution 文档 L60-L66 增加 execution-fact-only 约束，明确 facts/errors 不能授权 verdict、learnability 或 promotion。(local)
- [Completed] Added execution-fact-only constraints in `analyzer.py` L30-L34 and the bilingual evolution docs L60-L66, stating that facts/errors cannot authorize verdicts, learnability, or promotion. (local)
- [完成] 新增 `test_experience_capability_facts.py` L1-L110 共 4 个测试，覆盖去敏 fact、unknown/failed 状态、非法 projection 诊断和 provider-private Tool ID 过滤。(local)
- [Completed] Added 4 tests in `test_experience_capability_facts.py` L1-L110 covering redacted facts, unknown/failed states, invalid-projection diagnostics, and provider-private Tool ID filtering. (local)
- [完成] 验证：scene-observe 测试 `188 passed`；experience ruff、compileall、git diff --check 均通过。(local)
- [Completed] Validation: scene-observe tests `188 passed`; experience ruff, compileall, and git diff checks passed. (local)

### 额外同步 / Additional Sync
- [完成] 更新根 `CHANGELOG.md`，加入 v0.9.0 experience capability facts 记录。(local)
- [Completed] Updated root `CHANGELOG.md` with the v0.9.0 experience capability facts entry. (local)

### Git 提交 / Git Commit
- Commit: `103a7c8`
- Branch: `feature/long-horizon-workflow`
- 时间 / Time: 2026-09-02 (Asia/Shanghai)

### 后续日志提交 / Follow-up Log Commits
- Commit: `5704cd1`（记录 v0.9.0 功能提交 hash / recorded the v0.9.0 feature hash）
- Commit: `bb2e9c4`（同步根 CHANGELOG / synchronized root CHANGELOG）
- Commit: `80fbca0`（完成 v0.9.0 元数据 / finalized v0.9.0 metadata）
- Commit: `a7768a1`（补充 TaskEpisode 往返测试 / added TaskEpisode round-trip test）

## v0.10.0 (2026-09-02 00:00) - codex

### 变更摘要 / Change Summary
- [完成] [policy] [feat] 为 TaskOutcomeEnvelope 增加 capability execution-fact summary，按终态、failure owner、证据可用性、world-change 与 projection error 做确定性聚合，供 ExperienceAnalyzer 归因使用；不改变 verdict、Lesson eligibility、Skill promotion 或运动权限。(local)
- [Completed] [policy] [feat] Add a deterministic capability execution-fact summary to `TaskOutcomeEnvelope`, aggregating terminal status, failure owner, evidence availability, world-change state, and projection errors for ExperienceAnalyzer attribution without changing verdicts, Lesson eligibility, Skill promotion, or motion authority. (local)

### 预期影响文件 / Expected Files
- `PhyAgentOS/agent/experience/contracts.py` [修改 / modified]
- `PhyAgentOS/agent/experience/source.py` [修改 / modified]
- `PhyAgentOS/agent/experience/analyzer.py` [修改 / modified]
- `docs/en/05-agent-experience-and-skill-evolution.md`、`docs/zh/05-agent-experience-and-skill-evolution.md` [修改 / modified]
- `examples/forge-skills/scene-observe/tests/test_experience_capability_facts.py` [修改 / modified]

### 验收目标 / Acceptance Targets
- 摘要只由已验证的 `CapabilityOutcomeFact` 和 bounded error code 计算，字段固定、可序列化、可重放。
- 明确区分 failed/cancelled/stopped/unknown、failure owner、evidence gap 和 world-change；不把统计结果当作任务成功或晋升证据。
- Analyzer prompt 能看到该摘要并被明确约束为归因上下文；现有 ExperienceCoordinator 数据流和 promotion 门槛保持不变。
- 测试覆盖成功、失败、unknown、证据缺口、projection error、provider-neutral 和 TaskEpisode round-trip。

### 实际修改 / Actual Changes
- [完成] `contracts.py` L93-L156 新增 `CapabilityOutcomeSummary`，固定 status/failure owner/evidence 计数、world-change 与 unknown 计数、projection error code；所有计数非负且 error code 去重。(local)
- [Completed] Added `CapabilityOutcomeSummary` in `contracts.py` L93-L156 with fixed status/failure-owner/evidence counts, world-change/unknown counts, and deduplicated bounded projection error codes. (local)
- [完成] `source.py` L179-L239 在构建 AgentTask outcome 时确定性汇总 facts/errors，保持原 verdict、learnable 和 promotion 语义不变；`analyzer.py` L30-L35 明确 summary 仅为 attribution aid。(local)
- [Completed] `source.py` L179-L239 now deterministically summarizes facts/errors while preserving verdict, learnability, and promotion semantics; `analyzer.py` L30-L35 states the summary is attribution-only. (local)
- [完成] 新增并扩展 `test_experience_capability_facts.py` L58-L127，覆盖状态/owner/unknown/error 统计与 TaskEpisode 往返。(local)
- [Completed] Extended `test_experience_capability_facts.py` L58-L127 to cover status/owner/unknown/error aggregation and TaskEpisode round-trip. (local)
- [完成] 验证：scene-observe 测试 `188 passed`；ruff、compileall、git diff --check 均通过。(local)
- [Completed] Validation: scene-observe tests `188 passed`; ruff, compileall, and git diff checks passed. (local)

### Git 提交 / Git Commit
- Commit: `2460309`
- Branch: `feature/long-horizon-workflow`
- 时间 / Time: 2026-09-02 (Asia/Shanghai)

## v0.11.0 (2026-09-02 00:00) - codex

### 变更摘要 / Change Summary
- [完成] [policy] [feat] 将 capability fact summary 用作 Skill evolution 的确定性归因 guard：存在 unknown、projection error 或未决终态时阻止 Lesson/Candidate 写入并记录 bounded event；已知且可归因的失败继续由现有 analyzer、LessonEligibility 与 promotion 门槛处理。(local)
- [Completed] [policy] [feat] Use the capability fact summary as a deterministic attribution guard for Skill evolution: block Lesson/Candidate writes and record a bounded event when unknown outcomes, projection errors, or unsettled terminal states exist; known attributable failures continue through the existing analyzer, LessonEligibility, and promotion gates. (local)

### 预期影响文件 / Expected Files
- `PhyAgentOS/agent/experience/attribution.py` [新增 / added]
- `PhyAgentOS/agent/experience/evolution.py` [修改 / modified]
- `PhyAgentOS/agent/experience/__init__.py` [修改 / modified]
- `docs/en/05-agent-experience-and-skill-evolution.md`、`docs/zh/05-agent-experience-and-skill-evolution.md` [修改 / modified]
- `examples/forge-skills/scene-observe/tests/test_experience_attribution.py` [新增 / added]

### 验收目标 / Acceptance Targets
- guard 只读取 `CapabilityOutcomeSummary`，输出固定 reason 或允许；不调用 Gateway，不修改 AgentTask/verdict，不改变 existing Tool API。
- unknown、cancelled/stopped 未决状态和 projection errors fail-closed；guard 触发时不支持 Lesson observation、Skill candidate 或 promotion。
- 已知 succeeded/failed 且无 guard 条件的 episode 保持原有 evolution 行为。
- 测试覆盖允许路径、unknown、projection error、未决状态、幂等事件和 provider-neutral/no-motion 约束。

### 实际修改 / Actual Changes
- [完成] 新增 `attribution.py` L1-L45 的 `EvolutionAttributionDecision` 与 `assess_evolution_attribution`，对 unknown/cancelled/stopped、projection error fail-closed，输出固定 bounded payload。(local)
- [Completed] Added `EvolutionAttributionDecision` and `assess_evolution_attribution` in `attribution.py` L1-L45; unknown/cancelled/stopped and projection-error states fail closed with a fixed bounded payload. (local)
- [完成] 修改 `evolution.py` L85-L97，在 assessment 写入前执行 guard；修改 `store.py` L748-L767，新增 `record_event_once` 保证诊断事件幂等。(local)
- [Completed] Updated `evolution.py` L85-L97 to run the guard before assessment writes and `store.py` L748-L767 with idempotent `record_event_once` diagnostics. (local)
- [完成] 更新 `experience/__init__.py`、`analyzer.py` 与双语 Experience 文档，明确 unknown/未决状态不能生成可复用 Lesson/Candidate。(local)
- [Completed] Updated `experience/__init__.py`, `analyzer.py`, and bilingual Experience docs to state that unknown/unsettled states cannot produce reusable Lessons/Candidates. (local)
- [完成] 新增 `test_experience_attribution.py` L1-L87 共 4 个测试，覆盖允许路径、未决状态、projection error 优先级与幂等 event。(local)
- [Completed] Added 4 tests in `test_experience_attribution.py` L1-L87 covering allowed paths, unsettled states, projection-error precedence, and idempotent events. (local)
- [完成] 验证：scene-observe 测试 `192 passed`；ruff、compileall、git diff --check 均通过。(local)
- [Completed] Validation: scene-observe tests `192 passed`; ruff, compileall, and git diff checks passed. (local)

### Git 提交 / Git Commit
- Commit: `f375976`
- Branch: `feature/long-horizon-workflow`
- 时间 / Time: 2026-09-02 (Asia/Shanghai)

## v1.1.0 (2026-09-02 00:00) - codex

### 变更摘要 / Change Summary
- [完成] [policy] [feat] 增加 capability-to-LessonEligibility 归因上下文与一致性校验：为 analyzer 提供 provider-neutral 的 failure owner/evidence hints，并在 evolution 写入前拒绝将明确 infrastructure 或 evidence-limit 事实误标为 workflow-related；planner/execution/readiness 保持模型判断，不直接等价为 Skill 缺陷。(local)
- [Completed] [policy] [feat] Add capability-to-LessonEligibility attribution context and consistency validation: provide provider-neutral failure-owner/evidence hints to the analyzer and reject evolution writes that label explicit infrastructure or evidence-limit facts as workflow-related; planner/execution/readiness remain model judgments rather than automatic Skill defects. (local)

### 预期影响文件 / Expected Files
- `PhyAgentOS/agent/experience/attribution.py` [修改 / modified]
- `PhyAgentOS/agent/experience/analyzer.py` [修改 / modified]
- `PhyAgentOS/agent/experience/evolution.py` [修改 / modified]
- `docs/en/05-agent-experience-and-skill-evolution.md`、`docs/zh/05-agent-experience-and-skill-evolution.md` [修改 / modified]
- `examples/forge-skills/scene-observe/tests/test_experience_attribution.py` [修改 / modified]

### 验收目标 / Acceptance Targets
- analyzer payload 增加确定性、只读、provider-neutral attribution context，不复制 artifact、failure code、坐标或原始 input。
- infrastructure-only 失败不能生成 workflow-related observation；仅有 unknown evidence 且无其他可归因事实时不能生成 workflow-related observation。
- planner/execution/readiness 只标记为 `requires_semantic_attribution`，仍须通过现有 LessonEligibility 与多 episode 门槛。
- 不修改 `ExperienceAssessment` 公共 schema、AgentTask verdict、Tool API、Gateway 或 promotion threshold；测试覆盖映射与拒绝路径。

### 实际修改 / Actual Changes
- [完成] `attribution.py` L45-L112 新增 analyzer attribution context 与 assessment consistency validator：输出 bounded owner/evidence hints，并拒绝 infrastructure/evidence-only 与 workflow_related 冲突。(local)
- [Completed] Added analyzer attribution context and assessment consistency validation in `attribution.py` L45-L112, with bounded owner/evidence hints and rejection of infrastructure/evidence-only conflicts with workflow-related claims. (local)
- [完成] `analyzer.py` L126-L137 注入只读 attribution context；`evolution.py` L98-L108 在写入前执行一致性检查并记录幂等 bounded event。(local)
- [Completed] Injected read-only attribution context in `analyzer.py` L126-L137; `evolution.py` L98-L108 now validates consistency before writes and records an idempotent bounded event. (local)
- [完成] 新增 `test_experience_attribution.py` L47-L137 共 6 个测试，覆盖 owner 映射、infrastructure/evidence-limit 拒绝、analyzer payload、未决 guard 与事件幂等。(local)
- [Completed] Added 6 tests in `test_experience_attribution.py` L47-L137 covering owner mapping, infrastructure/evidence-limit rejection, analyzer payload, unsettled guards, and event idempotency. (local)
- [完成] 验证：scene-observe 测试 `195 passed`；ruff、compileall、git diff --check 均通过。(local)
- [Completed] Validation: scene-observe tests `195 passed`; ruff, compileall, and git diff checks passed. (local)

### 文档补充 / Documentation Follow-up
- [完成] 更新双语 Experience 文档，说明 analyzer attribution context、semantic attribution owner hints 及冲突拒绝边界。(local)
- [Completed] Updated bilingual Experience docs with analyzer attribution context, semantic-owner hints, and conflict-rejection boundaries. (local)

### Git 提交 / Git Commit
- Commit: `05bbf76`
- Branch: `feature/long-horizon-workflow`
- 时间 / Time: 2026-09-02 (Asia/Shanghai)
- Follow-up Commit: `7381730`（补充 analyzer attribution 文档 / clarified analyzer attribution docs）

## v1.2.0 (2026-09-02 00:00) - codex

### 变更摘要 / Change Summary
- [完成] [policy] [feat] 将 capability failure-owner 作为跨 episode LessonCluster 的有界作用域，拒绝不同 owner 根因的 cluster 合并；保持现有 root-task 唯一支持、LessonEligibility 与 promotion 门槛。(local)
- [Completed] [policy] [feat] Use capability failure-owner as a bounded cross-episode LessonCluster scope and reject merges with different root causes while preserving unique root-task support, LessonEligibility, and promotion thresholds. (local)

### 预期影响文件 / Expected Files
- `PhyAgentOS/agent/experience/contracts.py` [修改 / modified]
- `PhyAgentOS/agent/experience/evolution.py` [修改 / modified]
- `PhyAgentOS/agent/experience/store.py` [修改 / modified]
- `docs/en/05-agent-experience-and-skill-evolution.md`、`docs/zh/05-agent-experience-and-skill-evolution.md` [修改 / modified]
- `examples/forge-skills/scene-observe/tests/test_experience_attribution.py` [修改 / modified]

### 验收目标 / Acceptance Targets
- observation/cluster 持久化 bounded capability failure-owner scope，匹配 cluster 时 scope 不一致 fail-closed。
- 不复制 provider、RoboTwin、SAPIEN、artifact URI、failure code、坐标或原始输入；不新增 Tool/Gateway/运动接口。
- 跨 episode 相同 owner 可继续聚合，不同 owner 不得混合；既有独立 root-task 计数保持不变。
- 测试覆盖新 cluster owner scope、mismatched cluster rejection、序列化和全量回归。

### 实际修改 / Actual Changes
- [完成] `attribution.py` L116-L151 新增 `validate_cluster_owner_scope`，在 Lesson synthesis 前拒绝不同 owner 或 scoped/legacy 混合的 observations。(local)
- [Completed] Added `validate_cluster_owner_scope` in `attribution.py` L116-L151 to reject different owner scopes or scoped/legacy mixes before Lesson synthesis.
- [完成] `coordinator.py` L318-L337 与 `evolution.py` L366-L374 接入激活前校验；冲突 cluster 进入 blocked 并记录幂等 `lesson_cluster_attribution_blocked` event。(local)
- [Completed] Wired pre-activation validation into `coordinator.py` L318-L337 and `evolution.py` L366-L374; conflicting clusters become blocked with an idempotent `lesson_cluster_attribution_blocked` event.
- [完成] `test_experience_attribution.py` L124-L180 新增同 owner、不同 owner 和 scoped/legacy 混合测试；全量测试 `198 passed`，ruff、compileall、git diff --check 均通过。(local)
- [Completed] Added same-owner, different-owner, and scoped/legacy-mix tests in `test_experience_attribution.py` L124-L180; full tests `198 passed`, with ruff, compileall, and git diff checks passing. (local)

### 实际修改 / Actual Changes
- [完成] `contracts.py` L300-L340 为 `FailureObservation`、`LessonCluster` 增加 `capability_failure_owners`。(local)
- [Completed] Added `capability_failure_owners` to `FailureObservation` and `LessonCluster` in `contracts.py` L300-L340. (local)
- [完成] `evolution.py` L199-L260 从 episode summary 绑定 owner scope，匹配时不一致 fail-closed；`store.py` L535-L548 合并同 cluster 的 owner scope。(local)
- [Completed] Bound owner scope from the episode summary in `evolution.py` L199-L260, rejected mismatches, and merged scope on cluster persistence in `store.py` L535-L548. (local)
- [完成] 双语 Experience 文档与 `test_experience_attribution.py` L105-L170 同步更新；全量测试 `196 passed`，ruff、compileall、git diff --check 均通过。(local)
- [Completed] Updated bilingual Experience docs and `test_experience_attribution.py` L105-L170; full tests `196 passed`, with ruff, compileall, and git diff checks passing. (local)

### Git 提交 / Git Commit
- Commit: `4c70a5a`
- Branch: `feature/long-horizon-workflow`
- 时间 / Time: 2026-09-02 (Asia/Shanghai)

### 额外同步 / Additional Sync
- [完成] 更新根 `CHANGELOG.md`，加入 v1.2.0 Lesson owner scope 记录。(local)
- [Completed] Updated root `CHANGELOG.md` with the v1.2.0 Lesson owner-scope entry. (local)

## v1.3.0 (2026-09-02 00:00) - codex

### 变更摘要 / Change Summary
- [完成] [policy] [feat] 在 Lesson 激活前验证跨 episode capability owner scope：同 owner 可聚合，不同 owner 或 scoped/legacy 混合保持 blocked；不改变 Tool API、任务 verdict 或 promotion 门槛。(local)
- [Completed] [policy] [feat] Validate cross-episode capability owner scope before Lesson activation: same-owner observations may aggregate, while different-owner or scoped/legacy mixes remain blocked without changing the Tool API, task verdicts, or promotion thresholds. (local)

### 预期影响文件 / Expected Files
- `PhyAgentOS/agent/experience/attribution.py`、`coordinator.py`、`evolution.py`、`__init__.py` [修改 / modified]
- `docs/en/05-agent-experience-and-skill-evolution.md`、`docs/zh/05-agent-experience-and-skill-evolution.md` [修改 / modified]
- `examples/forge-skills/scene-observe/tests/test_experience_attribution.py` [修改 / modified]

### 验收目标 / Acceptance Targets
- 激活前 scope 校验只读取规范化 observation owner 列表，不调用 Gateway，不产生运动权限。
- 旧的无 owner generic observations 保持兼容；与有 owner observation 混合时 fail-closed。
- 测试覆盖同 owner、不同 owner、scoped/legacy 混合和完整回归。

### 实际修改 / Actual Changes
- [完成] `attribution.py` L116-L151 新增 `validate_cluster_owner_scope`；`coordinator.py` L318-L337 在 synthesis 前阻止冲突 cluster，`evolution.py` L366-L374 在直接激活路径重复校验。(local)
- [Completed] Added `validate_cluster_owner_scope` in `attribution.py` L116-L151; `coordinator.py` L318-L337 blocks conflicts before synthesis and `evolution.py` L366-L374 repeats validation for direct activation.
- [完成] `FailureObservation`、`LessonCluster` owner scope 继续持久化并在 store 合并；双语 Experience 文档补充激活边界。(local)
- [Completed] Persisted and merged owner scope on `FailureObservation` and `LessonCluster`; documented activation boundaries in bilingual Experience docs.
- [完成] 新增 2 个 scope 测试，scene-observe 全量测试 `198 passed`；ruff、compileall、git diff --check 均通过。(local)
- [Completed] Added 2 scope tests; full scene-observe tests `198 passed`, with ruff, compileall, and git diff checks passing.

### Git 提交 / Git Commit
- Commit: `c0babf8`
- Branch: `feature/long-horizon-workflow`
- 时间 / Time: 2026-09-02 (Asia/Shanghai)

### v1.3.0 Git 提交 / v1.3.0 Git Commit
- Commit: `3791073`
- Branch: `feature/long-horizon-workflow`
- 时间 / Time: 2026-09-02 (Asia/Shanghai)
## v1.6.0 (2026-09-02 00:00) - codex

### 变更摘要 / Change Summary
- [完成] [policy] [feat] 增加 Fake Gateway 上的完整 PAOS AgentTask 工作流集成测试：通过现有 ForgeToolClient 和 AgentTaskCoordinator 串联六个 provider-neutral Tool，并验证 ExperienceCoordinator 生成 TaskEpisode；保持 no-motion、单一执行协议和 fail-closed 恢复语义。(local)
- [Completed] [policy] [feat] Add a full PAOS AgentTask workflow integration test on the Fake Gateway: use the existing ForgeToolClient and AgentTaskCoordinator to compose six provider-neutral Tools and verify ExperienceCoordinator TaskEpisode creation while preserving no-motion, one execution protocol, and fail-closed recovery semantics. (local)

### 预期影响文件 / Expected Files
- `examples/forge-skills/scene-observe/tests/test_full_workflow.py` [新增 / added]
- `changelog/2026-09.md` [修改 / modified]
- `CHANGELOG.md` [修改 / modified]

### 验收目标 / Acceptance Targets
- 成功路径真实经过 AgentTaskCoordinator -> ForgeToolClient -> FakeGatewayTransport，六阶段绑定同一 task/revision，并在终态生成可学习 TaskEpisode。
- 覆盖 non-terminal Action 禁止 finalize、unknown Action 禁止盲重试、取消 fail-closed 分支；不调用真实机器人、不引入 RoboTwin/SAPIEN。
- 验证 Query/Action execution records、invocation/attempt 引用、capability outcome projection 与 ExperienceCoordinator 的同步 episode 持久化；异步 analyzer 使用确定性 fake。

### 实际修改 / Actual Changes
- [完成] `examples/forge-skills/scene-observe/tests/test_full_workflow.py` L1-L410 新增确定性 provider fixture 与 3 个集成测试：成功路径执行 observe → understand → propose → prepare → acquire → place 六阶段；验证单一 AgentTask/revision、6 条 execution records、Action invocation/attempt 引用、capability outcome projection 和 ExperienceCoordinator 持久化 TaskEpisode；另覆盖 non-terminal finalize 拒绝、unknown Action 禁止相同参数重发，以及 cancel 后必须先观察终态。(local)
- [Completed] Added deterministic provider fixtures and 3 integration tests in `examples/forge-skills/scene-observe/tests/test_full_workflow.py` L1-L410: execute the six-stage observe → understand → propose → prepare → acquire → place workflow; verify one AgentTask/revision, six execution records, Action invocation/attempt references, capability outcome projection, and ExperienceCoordinator TaskEpisode persistence; also cover non-terminal finalize rejection, unknown Action resend blocking, and cancellation requiring terminal observation.
- [完成] 集成测试只使用 FakeGatewayTransport 与真实 ForgeToolClient/AgentTaskCoordinator；Fake Gateway invocation 仅为 no-motion bounded Action 记录，不新增路由、执行协议或 RoboTwin 依赖。(local)
- [Completed] Integration tests use only FakeGatewayTransport with the real ForgeToolClient/AgentTaskCoordinator; Fake Gateway invocations remain no-motion bounded Action records, with no new routes, execution protocol, or RoboTwin dependency.
- [完成] 验证：scene-observe 全量 `204 passed`；`ruff check PhyAgentOS examples/forge-skills/scene-observe`、`compileall`、`git diff --check` 均通过。(local)
- [Completed] Validation: full scene-observe suite `204 passed`; `ruff check PhyAgentOS examples/forge-skills/scene-observe`, `compileall`, and `git diff --check` all passed.

### Git 提交 / Git Commit
- Commit: `f174f75`
- Branch: `feature/long-horizon-workflow`
- 时间 / Time: 2026-09-02 (Asia/Shanghai)
## v1.7.0 (2026-09-02 00:00) - codex

### 变更摘要 / Change Summary
- [完成] [runtime] [feat] 增加 scene-observe Bundle 的 manifest-v2 安装与 Runtime discovery 集成测试：验证归档安装、Catalog manifest 读取、required_tools 完整性及健康运行时发现；不启动 Dora、不连接真实机器人、不新增执行协议。(local)
- [Completed] [runtime] [feat] Add manifest-v2 installation and Runtime discovery integration tests for the scene-observe Bundle: verify archive installation, Catalog manifest loading, required-tool completeness, and healthy-runtime discovery without starting Dora, connecting to real robots, or adding another execution protocol. (local)

### 预期影响文件 / Expected Files
- `examples/forge-skills/scene-observe/tests/test_runtime_install_discovery.py` [新增 / added]
- `CHANGELOG.md`、`changelog/2026-09.md` [修改 / modified]
- `examples/forge-skills/scene-observe/skill.yaml` [修改 / modified]

### 验收目标 / Acceptance Targets
- 使用真实 `SkillInstaller` 将当前 Bundle 安装到隔离目录，并由 `SkillCatalog` 重新解析 manifest-v2。
- 使用 RuntimeStateStore 与确定性 fake manager 验证 `discover_active_runtime` 只发布 running、flow/gateway/context 全 ready 的 runtime；未就绪或多 runtime fail-closed。
- 测试不得调用 Dora、RoboTwin、SAPIEN、硬件或运动路由。

### 实际修改 / Actual Changes
- [完成] `examples/forge-skills/scene-observe/skill.yaml` L15-L16 将无 Node 依赖的 fake Bundle `artifacts.resolver` 从 `registry` 修正为 `local`，满足 manifest-v2 对空 `artifacts.nodes` 的校验约束；不放宽 Runtime 校验、不伪造 Node lock。(local)
- [Completed] Updated `examples/forge-skills/scene-observe/skill.yaml` L15-L16 to set the no-Node fake Bundle `artifacts.resolver` from `registry` to `local`, satisfying manifest-v2 validation for an empty `artifacts.nodes`; Runtime validation remains strict and no Node lock is fabricated.
- [完成] `examples/forge-skills/scene-observe/tests/test_runtime_install_discovery.py` L1-L129 新增 3 个测试：通过 `scripts/package_skill.py` 生成带 archive manifest 的归档，使用真实 `SkillInstaller`/`SkillCatalog` 安装并重载；使用 `RuntimeStateStore` 与 fake manager 验证单一健康 runtime 发布，以及 starting/flow/context 未就绪时返回 `None`。(local)
- [Completed] Added 3 tests in `examples/forge-skills/scene-observe/tests/test_runtime_install_discovery.py` L1-L129: package an archive with the embedded archive manifest, install and reload it through real `SkillInstaller`/`SkillCatalog`, and verify single healthy-runtime publication plus fail-closed `None` for starting or unhealthy flow/context states using `RuntimeStateStore` and a fake manager.
- [完成] 验证：scene-observe 全量 `207 passed`；`ruff check examples/forge-skills/scene-observe PhyAgentOS`、`compileall`、`git diff --check` 均通过；测试不启动 Dora、不调用硬件或运动路由。(local)
- [Completed] Validation: full scene-observe suite `207 passed`; `ruff check examples/forge-skills/scene-observe PhyAgentOS`, `compileall`, and `git diff --check` all passed; tests start no Dora process and call no hardware or motion routes.

### Git 提交 / Git Commit
- Commit: `5154998`
- Branch: `feature/long-horizon-workflow`
- 时间 / Time: 2026-09-02 (Asia/Shanghai)
## v1.8.0 (2026-09-02 00:00) - codex

### 变更摘要 / Change Summary
- [完成] [runtime] [test] 增加真实 `RuntimeManager.status` HTTP health contract 测试：使用本地一次性 HTTP server 验证 `/tools` 与 required Tool `/context` 的 ready/binding_error 语义，并验证缺失 context 时持久化 Runtime 状态 fail-closed；不启动 Dora、不调用硬件。(local)
- [Completed] [runtime] [test] Add a real `RuntimeManager.status` HTTP health-contract test: use a one-shot local HTTP server to verify `/tools` and required Tool `/context` ready/binding_error semantics, and verify persisted Runtime state fails closed when a context is missing; no Dora or hardware calls. (local)

### 预期影响文件 / Expected Files
- `examples/forge-skills/scene-observe/tests/test_runtime_install_discovery.py` [修改 / modified]
- `CHANGELOG.md`、`changelog/2026-09.md` [修改 / modified]
- `examples/forge-skills/scene-observe/skill.yaml` [未修改 / unchanged]

### 验收目标 / Acceptance Targets
- RuntimeManager 真实 urllib health 读取只接受 JSON object、HTTP 200、`ok=true`、context `ready=true` 且 `binding_error=null`。
- Healthy state remains running; missing required context transitions to failed with a bounded diagnostic and `discover_active_runtime` does not publish it.
- Test server binds localhost only and is shut down deterministically; no runtime process, robot, simulator, or motion route is started.

### 实际修改 / Actual Changes
- [完成] `examples/forge-skills/scene-observe/tests/test_runtime_install_discovery.py` L1-L206 新增本地 `ThreadingHTTPServer` health fixture 与测试：通过真实 `RuntimeManager.status()` 读取 `/tools` 和六个 required Tool context，并在 `object.place` 返回 `ready=false/binding_error` 时将持久化 Runtime 状态转为 `failed`；server 仅绑定 `127.0.0.1` 并确定性关闭。(local)
- [Completed] Added a localhost `ThreadingHTTPServer` health fixture and test in `examples/forge-skills/scene-observe/tests/test_runtime_install_discovery.py` L1-L206: exercise real `RuntimeManager.status()` against `/tools` and all six required Tool contexts, persist a `failed` Runtime state when `object.place` reports `ready=false/binding_error`, and deterministically shut down the server bound only to `127.0.0.1`.
- [完成] 验证：scene-observe 全量 `208 passed`；`ruff check examples/forge-skills/scene-observe PhyAgentOS`、`compileall`、`git diff --check` 均通过；没有启动 Dora、机器人或运动路由。(local)
- [Completed] Validation: full scene-observe suite `208 passed`; `ruff check examples/forge-skills/scene-observe PhyAgentOS`, `compileall`, and `git diff --check` all passed; no Dora process, robot, or motion route was started.

### Git 提交 / Git Commit
- Commit: `9b306cc`
- Branch: `feature/long-horizon-workflow`
- 时间 / Time: 2026-09-02 (Asia/Shanghai)
## v1.9.0 (2026-09-02 00:00) - codex

### 变更摘要 / Change Summary
- [完成] [runtime] [test] 增加 SkillRuntimeController 切换与回滚保护测试：验证非终态 AgentTask 禁止切换、目标 Runtime 启动失败时旧 Runtime 恢复，以及健康目标切换后的 active registry 原子替换；不启动 Dora、不连接硬件。(local)
- [Completed] [runtime] [test] Add SkillRuntimeController switch and rollback protection tests: verify switching is blocked by a non-terminal AgentTask, a failed target Runtime restores the previous Runtime, and a healthy target atomically replaces the active registry; no Dora or hardware connections are started. (local)

### 预期影响文件 / Expected Files
- `examples/forge-skills/scene-observe/tests/test_runtime_controller.py` [新增 / added]
- `CHANGELOG.md`、`changelog/2026-09.md` [修改 / modified]

### 验收目标 / Acceptance Targets
- Controller 只在目标 `RuntimeManager.status().ready` 且有 state 时发布 active runtime。
- 切换期间存在 active AgentTask 时 fail-closed；目标启动或健康检查失败时旧 Runtime 恢复，恢复失败则 registry 清空。
- 测试仅使用 fake catalog/manager 与临时 state store，不启动 Dora、HTTP Gateway、RoboTwin、SAPIEN 或运动路由。

### 实际修改 / Actual Changes
- [完成] `examples/forge-skills/scene-observe/tests/test_runtime_controller.py` L1-L140 新增确定性 manifest/catalog/manager/task-store fixture 与 3 个 controller 测试：非终态 AgentTask 在任何 start/stop 前阻断切换；同 Gateway 目标启动失败后重新启动旧 Runtime 并保持 registry 指向旧 Skill；健康目标通过 status ready 后替换 registry 并停止旧 Runtime。(local)
- [Completed] Added deterministic manifest/catalog/manager/task-store fixtures and 3 controller tests in `examples/forge-skills/scene-observe/tests/test_runtime_controller.py` L1-L140: block switching before any start/stop when an AgentTask is non-terminal; restart the previous Runtime and keep the registry on the old Skill after same-Gateway target startup failure; and replace the registry only after the healthy target reports ready, then stop the old Runtime.
- [完成] 验证：scene-observe 全量 `211 passed`；`ruff check examples/forge-skills/scene-observe PhyAgentOS`、`compileall`、`git diff --check` 均通过；没有启动 Dora、HTTP Gateway、仿真器、硬件或运动路由。(local)
- [Completed] Validation: full scene-observe suite `211 passed`; `ruff check examples/forge-skills/scene-observe PhyAgentOS`, `compileall`, and `git diff --check` all passed; no Dora, HTTP Gateway, simulator, hardware, or motion route was started.

### Git 提交 / Git Commit
- Commit: `4058b86`
- Branch: `feature/long-horizon-workflow`
- 时间 / Time: 2026-09-02 (Asia/Shanghai)
## v2.0.0 (2026-09-02 00:00) - codex

### 变更摘要 / Change Summary
- [完成] [runtime] [test] 增加 ForgeSkillBindingResolver preview/freeze/validate 集成测试：验证 manifest、SKILL 文档、required ToolSpec 哈希与 Runtime 身份被冻结到 binding，并在 active Runtime 更换后 fail-closed；不执行 Tool Action。(local)
- [Completed] [runtime] [test] Add ForgeSkillBindingResolver preview/freeze/validate integration tests: verify manifest, SKILL document, required ToolSpec hashes, and Runtime identity are frozen into a binding, then fail closed after the active Runtime changes; no Tool Actions are executed. (local)

### 预期影响文件 / Expected Files
- `examples/forge-skills/scene-observe/tests/test_binding_freeze.py` [新增 / added]
- `CHANGELOG.md`、`changelog/2026-09.md` [修改 / modified]

### 验收目标 / Acceptance Targets
- Preview 通过真实 `ForgeToolClient` 读取六个 ToolSpec/context，并生成可冻结 candidate。
- Freeze 产出 immutable `ForgeSkillBinding`，保留 manifest/SKILL/ToolSpec SHA-256、Runtime instance/profile/gateway identity。
- Runtime instance 或 Gateway identity 变化后，旧 binding 的 `validate_tool` 被拒绝；无 Action、Session 或运动调用。

### 实际修改 / Actual Changes
- [完成] `examples/forge-skills/scene-observe/tests/test_binding_freeze.py` L1-L157 新增 3 个测试：通过真实 `ForgeSkillBindingResolver.preview()` 读取六个 ToolSpec/context，断言 manifest/SKILL SHA-256、ToolSpec 哈希、Runtime instance/profile/gateway identity 和 ready 标志；验证 `freeze()` 生成 immutable binding；Runtime identity 更换或候选 ToolSpec 哈希篡改时均 fail-closed。(local)
- [Completed] Added 3 tests in `examples/forge-skills/scene-observe/tests/test_binding_freeze.py` L1-L157: exercise real `ForgeSkillBindingResolver.preview()` across six ToolSpecs/contexts, assert manifest/SKILL SHA-256, ToolSpec hashes, Runtime instance/profile/gateway identity, and readiness; verify immutable `freeze()` output; and fail closed when Runtime identity changes or candidate ToolSpec hashes are tampered.
- [完成] 验证：scene-observe 全量 `214 passed`；`ruff check examples/forge-skills/scene-observe PhyAgentOS`、`compileall`、`git diff --check` 均通过；没有执行 Action、Session、Dora 或运动路由。(local)
- [Completed] Validation: full scene-observe suite `214 passed`; `ruff check examples/forge-skills/scene-observe PhyAgentOS`, `compileall`, and `git diff --check` all passed; no Action, Session, Dora, or motion route was executed.

### Git 提交 / Git Commit
- Commit: `a56cd0b`
- Branch: `feature/long-horizon-workflow`
- 时间 / Time: 2026-09-02 (Asia/Shanghai)
## v2.1.0 (2026-09-02 00:00) - codex

### 变更摘要 / Change Summary
- [完成] [runtime] [test] 增加 Skill activation → Forge binding freeze → AgentTask immutable binding 集成测试：验证当前 turn 的 primary Skill 激活候选被任务冻结，并在 Runtime identity 漂移后阻止 governed Tool 调用；不执行 Action。(local)
- [Completed] [runtime] [test] Add Skill activation → Forge binding freeze → AgentTask immutable binding integration tests: verify the current turn's primary Skill activation candidate is frozen into the task and governed Tool calls are blocked after Runtime identity drift; no Actions are executed. (local)

### 预期影响文件 / Expected Files
- `examples/forge-skills/scene-observe/tests/test_task_binding_activation.py` [新增 / added]
- `CHANGELOG.md`、`changelog/2026-09.md` [修改 / modified]

### 验收目标 / Acceptance Targets
- 使用真实 `SkillActivationManager`、`ForgeSkillBindingResolver` 与 `AgentTaskCoordinator` 完成 activation/preview/freeze。
- AgentTask 保存 immutable binding、revision binding ID、Runtime snapshot ref，并绑定 Experience 记录。
- Runtime identity 变化后，`invoke_query` 在网络调用前 fail-closed；不执行 Action、Session、Dora 或硬件。

### 实际修改 / Actual Changes
- [完成] `examples/forge-skills/scene-observe/tests/test_task_binding_activation.py` L1-L173 新增 2 个集成测试：使用真实 `SkillActivationManager` 激活 primary Skill，通过 `ForgeSkillBindingResolver.preview/freeze` 和 `AgentTaskCoordinator.create_task` 绑定同一个 immutable binding；断言 activation content digest、Skill document digest、revision binding ID、Runtime snapshot ref 与 Experience binding 一致，并验证 Runtime identity 漂移时 `invoke_query` 在 Gateway 请求前拒绝。(local)
- [Completed] Added 2 integration tests in `examples/forge-skills/scene-observe/tests/test_task_binding_activation.py` L1-L173: use the real `SkillActivationManager` to activate a primary Skill, freeze one immutable binding through `ForgeSkillBindingResolver.preview/freeze` and `AgentTaskCoordinator.create_task`, assert activation/content digest, Skill document digest, revision binding ID, Runtime snapshot ref, and Experience binding consistency, and reject `invoke_query` before any Gateway request after Runtime identity drift.
- [完成] 验证：scene-observe 全量 `216 passed`；`ruff check examples/forge-skills/scene-observe PhyAgentOS`、`compileall`、`git diff --check` 均通过；未执行 Action、Session、Dora 或硬件。(local)
- [Completed] Validation: full scene-observe suite `216 passed`; `ruff check examples/forge-skills/scene-observe PhyAgentOS`, `compileall`, and `git diff --check` all passed; no Action, Session, Dora, or hardware execution occurred.

### Git 提交 / Git Commit
- Commit: `b3fdaa9`
- Branch: `feature/long-horizon-workflow`
- 时间 / Time: 2026-09-02 (Asia/Shanghai)
## v2.2.0 (2026-09-02 00:00) - codex

### 变更摘要 / Change Summary
- [完成] [runtime] [test] 增加冻结 binding 后的 AgentTask governed execution 集成测试：通过真实 `AgentTaskCoordinator` 执行 bound Query 与 bounded Action，验证每条记录的 binding/revision/ToolSpec 身份和 invocation/attempt 引用；保持 Fake Gateway no-motion。(local)
- [Completed] [runtime] [test] Add governed AgentTask execution integration tests after binding freeze: execute a bound Query and bounded Action through the real `AgentTaskCoordinator`, verify binding/revision/ToolSpec identity and invocation/attempt references on every record, and keep the Fake Gateway no-motion. (local)

### 预期影响文件 / Expected Files
- `examples/forge-skills/scene-observe/tests/test_bound_execution_records.py` [新增 / added]
- `CHANGELOG.md`、`changelog/2026-09.md` [修改 / modified]

### 验收目标 / Acceptance Targets
- 激活 primary Skill、创建 immutable AgentTask 后，Query/Action 均经过 `_require_binding_tool` 与标准 ForgeToolClient 路径。
- Query record 与 Action record 绑定同一 task/revision/binding；Action terminal response 保留 invocation/attempt 和 capability summary。
- Runtime drift 或 ToolSpec mismatch 仍在网络调用前 fail-closed；测试不执行真实运动。

### 实际修改 / Actual Changes
- [完成] `examples/forge-skills/scene-observe/tests/test_bound_execution_records.py` L1-L170 新增 bound execution 集成测试：从 primary Skill activation 和 immutable binding 创建 AgentTask，真实执行 `scene.observe` Query 与 `object.acquire` bounded Action，观察终态后检查两条记录共享 task binding/revision，ToolSpec SHA-256 与冻结 binding 一致，并保留 invocation/attempt 与 capability outcome summary。(local)
- [Completed] Added a bound execution integration test in `examples/forge-skills/scene-observe/tests/test_bound_execution_records.py` L1-L170: create an AgentTask from primary Skill activation and immutable binding, execute `scene.observe` Query and `object.acquire` bounded Action, reconcile the terminal result, and verify both records share the task binding/revision, match frozen ToolSpec SHA-256 values, and retain invocation/attempt plus capability outcome summary.
- [完成] 验证：scene-observe 全量 `217 passed`；`ruff check examples/forge-skills/scene-observe PhyAgentOS`、`compileall`、`git diff --check` 均通过；Fake Gateway 仅执行 no-motion 合约路径。(local)
- [Completed] Validation: full scene-observe suite `217 passed`; `ruff check examples/forge-skills/scene-observe PhyAgentOS`, `compileall`, and `git diff --check` all passed; the Fake Gateway only exercised no-motion contract paths.

### Git 提交 / Git Commit
- Commit: `3e2ed16`
- Branch: `feature/long-horizon-workflow`
- 时间 / Time: 2026-09-02 (Asia/Shanghai)
## v2.3.0 (2026-09-02 00:00) - codex

### 变更摘要 / Change Summary
- [完成] [policy] [test] 增加 AgentTask generic verification/recovery 集成测试：验证 fake verifier 返回 `replan_required` 时任务进入 `awaiting_replan`，同一 task 追加 PlanRevision 后恢复执行，并最终保留失败与成功 lineage；不新增执行协议或运动权限。(local)
- [Completed] [policy] [test] Add AgentTask generic verification/recovery integration tests: verify a fake verifier's `replan_required` moves the task to `awaiting_replan`, the same task appends a PlanRevision and resumes execution, and final history preserves failed and successful lineage without adding another execution protocol or motion authority. (local)

### 预期影响文件 / Expected Files
- `examples/forge-skills/scene-observe/tests/test_task_verification_recovery.py` [新增 / added]
- `CHANGELOG.md`、`changelog/2026-09.md` [修改 / modified]

### 验收目标 / Acceptance Targets
- 使用真实 AgentTaskCoordinator finalize/recovery 生命周期和标准 ForgeToolClient Query 路径。
- `replan_required` 必须携带 recovery context；`begin_revision` 保持同一 task ID、受限 replan budget 和 append-only revision。
- 最终成功 episode 的 lineage 同时包含早期 failure 与后续 success；不执行 Action、Session、Dora 或硬件。

### 实际修改 / Actual Changes
- [完成] `examples/forge-skills/scene-observe/tests/test_task_verification_recovery.py` L1-L116 新增确定性 fake verifier 与恢复测试：首次 finalize 返回带 recovery context 的 `replan_required`，任务进入 `awaiting_replan`；`begin_revision` 在同一 task 上追加 revision，第二次 Query 后 verifier 返回 success；`AgentTaskOutcomeSource` 保留两条 revision lineage（replan_required、success）。(local)
- [Completed] Added a deterministic fake verifier and recovery test in `examples/forge-skills/scene-observe/tests/test_task_verification_recovery.py` L1-L116: first finalize returns `replan_required` with recovery context and moves the task to `awaiting_replan`; `begin_revision` appends a revision on the same task, a second Query is verified as success, and `AgentTaskOutcomeSource` preserves both revision lineage entries (`replan_required`, `success`).
- [完成] 验证：scene-observe 全量 `218 passed`；`ruff check examples/forge-skills/scene-observe PhyAgentOS`、`compileall`、`git diff --check` 均通过；仅执行 Fake Gateway Query，无 Action、Session、Dora 或硬件。(local)
- [Completed] Validation: full scene-observe suite `218 passed`; `ruff check examples/forge-skills/scene-observe PhyAgentOS`, `compileall`, and `git diff --check` all passed; only Fake Gateway Queries ran, with no Action, Session, Dora, or hardware execution.

### Git 提交 / Git Commit
- Commit: `b9d4000`
- Branch: `feature/long-horizon-workflow`
- 时间 / Time: 2026-09-02 (Asia/Shanghai)
## v2.4.0 (2026-09-02 00:00) - codex

### 变更摘要 / Change Summary
- [完成] [policy] [test] 增加 ExperienceCoordinator recovery episode 集成测试：验证同一 AgentTask 的 `replan_required → success` 多 revision 被持久化为 mixed TaskEpisode 并完整传入 analyzer；确认 capability facts 仅作 attribution context，不自动创建 Skill/Lesson；不执行真实动作。(local)
- [Completed] [policy] [test] Add an ExperienceCoordinator recovery-episode integration test: verify a single AgentTask's multi-revision `replan_required → success` history is persisted as a mixed TaskEpisode and passed intact to the analyzer; capability facts remain attribution context only and do not auto-create Skills/Lessons; no real actions execute. (local)

### 预期影响文件 / Expected Files
- `examples/forge-skills/scene-observe/tests/test_experience_recovery_episode.py` [新增 / added]
- `CHANGELOG.md`、`changelog/2026-09.md` [修改 / modified]

### 验收目标 / Acceptance Targets
- 使用真实 AgentTaskOutcomeSource 与 ExperienceCoordinator episode 持久化路径，而非直接构造 analyzer 输入。
- analyzer 收到 mixed outcome、两个 revision lineage 和 capability summary；episode 仅创建一次且最终 processing 可完成。
- 不因 capability outcome facts 自动触发 candidate/Lesson；无 Action、Session、Dora、硬件或运动路由。

### 实际修改 / Actual Changes
- [完成] `examples/forge-skills/scene-observe/tests/test_experience_recovery_episode.py` L1-L139 新增 deterministic verifier/analyzer 集成测试：真实 finalize/replan/revision 路径生成单一 TaskEpisode，analyzer 收到 `replan_required → success` 两条 lineage，episode processing 完成；同时断言 capability summary 不会单独创建 candidate 或 Lesson cluster。(local)
- [Completed] Added a deterministic verifier/analyzer integration test in `examples/forge-skills/scene-observe/tests/test_experience_recovery_episode.py` L1-L139: the real finalize/replan/revision path creates one TaskEpisode, the analyzer receives both `replan_required → success` lineage entries, processing completes, and capability summaries alone create neither a Skill candidate nor a Lesson cluster.
- [完成] 验证：scene-observe 全量 `219 passed`；`ruff check examples/forge-skills/scene-observe PhyAgentOS`、`compileall`、`git diff --check` 均通过；无真实 Action、Session、Dora 或硬件。(local)
- [Completed] Validation: full scene-observe suite `219 passed`; `ruff check examples/forge-skills/scene-observe PhyAgentOS`, `compileall`, and `git diff --check` all passed; no real Action, Session, Dora, or hardware execution.

### Git 提交 / Git Commit
- Commit: `187cec0`
- Branch: `feature/long-horizon-workflow`
- 时间 / Time: 2026-09-02 (Asia/Shanghai)

## v2.5.0 (2026-09-02 15:19) - codex

### 变更摘要 / Change Summary
- [完成] [policy] [test] 验证冻结 Skill binding 的 AgentTask execution facts 经过通用 `VerificationRequestBuilder` 进入 verifier context；确认 capability projection 只作 execution fact、opaque artifact URI 不进入证据白名单，最终 verdict 仍由 `TaskVerificationContract`/verifier 产生；Fake Gateway 保持 no-motion。(local)
- [Completed] [policy] [test] Verified that bound AgentTask execution facts flow through the generic `VerificationRequestBuilder` into verifier context; capability projections remain execution facts, opaque artifact URIs stay outside the evidence allowlist, and the final verdict is produced only by the `TaskVerificationContract`/verifier; the Fake Gateway remains no-motion. (local)

### 预期影响文件 / Expected Files
- `examples/forge-skills/scene-observe/tests/test_task_verification_context.py` [新增 / added]
- `CHANGELOG.md`、`changelog/2026-09.md` [修改 / modified]

### 实际修改 / Actual Changes
- [完成] `examples/forge-skills/scene-observe/tests/test_task_verification_context.py` L1-L262 新增 bound AgentTask verification-context 集成测试：通过真实 `SkillActivationManager`、`ForgeSkillBindingResolver`、`AgentTaskCoordinator` 和 `VerificationRequestBuilder`，执行 bound `scene.observe` Query 与 `object.acquire` bounded Action；断言 binding/revision/invocation 身份、capability projection 的 `execution_fact_only`/`task_success_authorized=false`、opaque artifact URI 与证据白名单隔离，以及 verifier context 不含运动授权字段。(local)
- [Completed] Added `examples/forge-skills/scene-observe/tests/test_task_verification_context.py` L1-L262: an integration test using the real `SkillActivationManager`, `ForgeSkillBindingResolver`, `AgentTaskCoordinator`, and `VerificationRequestBuilder` to execute a bound `scene.observe` Query and bounded `object.acquire` Action; it asserts binding/revision/invocation identity, `execution_fact_only` and `task_success_authorized=false` capability projection semantics, opaque artifact versus evidence-allowlist separation, and absence of motion-authorization fields from verifier context.
- [完成] 验证：scene-observe 全量 `220 passed`；`ruff check PhyAgentOS examples/forge-skills/scene-observe`、`python -m compileall -q PhyAgentOS examples/forge-skills/scene-observe`、`git diff --check` 均通过；仅使用 Fake Gateway no-motion 路径，未启动 Dora、仿真器、硬件或运动路由。(local)
- [Completed] Validation: full scene-observe suite `220 passed`; `ruff check PhyAgentOS examples/forge-skills/scene-observe`, `python -m compileall -q PhyAgentOS examples/forge-skills/scene-observe`, and `git diff --check` all passed; only the Fake Gateway no-motion path was used, with no Dora, simulator, hardware, or motion route started.

### Git 提交 / Git Commit
- Commit: `d6f6a74`
- Branch: `feature/long-horizon-workflow`
- 时间 / Time: 2026-09-02 (Asia/Shanghai)

## v2.5.1 (2026-09-02 15:24) - codex

### 变更摘要 / Change Summary
- [完成] [docs] 回填 v2.5.0 verification-context 集成测试提交哈希并同步根 Changelog 索引。(local)
- [Completed] [docs] Backfilled the v2.5.0 verification-context integration-test commit hash and synchronized the root Changelog index. (local)

### 预期影响文件 / Expected Files
- `changelog/2026-09.md` [修改 / modified]
- `CHANGELOG.md` [修改 / modified]

### 实际修改 / Actual Changes
- [完成] `changelog/2026-09.md` L909-L928 回填 v2.5.0 的提交 `d6f6a74`，并记录本次文档收尾变更。(local)
- [Completed] Backfilled commit `d6f6a74` for v2.5.0 and recorded this documentation closeout in `changelog/2026-09.md` L909-L928.
- [完成] `CHANGELOG.md` L1-L18 新增 v2.5.0 根索引条目，保持最近版本可发现。(local)
- [Completed] Added the v2.5.0 root index entry in `CHANGELOG.md` L1-L18 so the latest version remains discoverable.

### 验证 / Verification
- [完成] `git diff --check` 通过；仅修改双语日志索引，未触及运行时代码或执行路径。(local)
- [Completed] `git diff --check` passed; only bilingual changelog/index files changed, with no runtime code or execution path touched.

## v2.5.2 (2026-09-02 15:28) - codex

### 变更摘要 / Change Summary
- [完成] [docs] 校正 v2.5.1 文档收尾条目中的实际日志行号引用。(local)
- [Completed] Corrected the actual monthly-log line references in the v2.5.1 documentation closeout entry. (local)

### 预期影响文件 / Expected Files
- `changelog/2026-09.md` [修改 / modified]

### 实际修改 / Actual Changes
- [完成] `changelog/2026-09.md` L941-L944 的引用更新为实际 v2.5.0/v2.5.1 条目位置；未修改代码。(local)
- [Completed] Updated `changelog/2026-09.md` L941-L944 to the actual v2.5.0/v2.5.1 entry locations; no code changed.

## v2.5.3 (2026-09-02 16:14) - codex

### 变更摘要
- [完成] [docs] 新增 v1.0 功能引用卡方法，规定规范文档、扩展点、代码模块、验收测试和 PR 追踪关系。(local)

### Change Summary
- [Completed] [docs] Add a v1.0 feature-reference-card method defining the relationship between normative documents, extension points, code modules, acceptance tests, and PR traceability. (local)

### 预期影响文件 / Expected Files
- `docs/forge/FEATURE_REFERENCE_CARDS.md` [新增 / new]
- `docs/forge/README.md` [修改 / modified]
- `docs/README.md` [修改 / modified]
- `CHANGELOG.md` [修改 / modified]

### 实际修改 / Actual Changes
- [完成] `docs/forge/FEATURE_REFERENCE_CARDS.md` L1-L146 新增功能引用卡模板、规范优先级、功能类型映射、代码追踪表、实施顺序、自我进化边界和 PR 交付要求。
- [Completed] Added the feature-card template, reference precedence, capability mapping, code traceability table, implementation sequence, evolution boundaries, and PR delivery requirements in `docs/forge/FEATURE_REFERENCE_CARDS.md` L1-L146.
- [完成] `docs/forge/README.md` L217-L223、`docs/README.md` L20-L72 增加中英文索引链接，使引用卡可从 Forge 契约和文档首页发现。
- [Completed] Added Chinese and English index links in `docs/forge/README.md` L217-L223 and `docs/README.md` L20-L72 so the feature cards are discoverable from the Forge contract and documentation index.
- [完成] `CHANGELOG.md` L5-L15 新增 v2.5.3 根索引条目。
- [Completed] Added the v2.5.3 root index entry in `CHANGELOG.md` L5-L15.

### 关键 Diff / Key Diff

#### [新增 / Added] `docs/forge/FEATURE_REFERENCE_CARDS.md` L1-L146

**新增 / Added:**
```markdown
## 2. 功能引用卡模板
## 3. 文档到代码的追踪
## 5. 推荐实施顺序
## 7. 自我进化边界
```

**说明 / Rationale:** 每个功能先锁定 v1.0 规范来源、扩展点、所有权、失败语义和验收证据，避免把示例文档复制成第二套契约。

### 验证 / Verification
- [完成] `git diff --check` 通过；Markdown 内部链接目标存在；未修改运行时代码、Gateway、Runtime、仿真器、硬件或运动路径。
- [Completed] `git diff --check` passed; Markdown internal link targets exist; no runtime code, Gateway, Runtime, simulator, hardware, or motion path was changed.
## v2.6.0 (2026-09-02 00:00) - codex

### 变更摘要 / Change Summary
- [docs] [refactor] [完成] 澄清 v1.0 文档中的所有权边界与执行顺序：Skill 仅承载 provider-neutral ToolSpec/工作流，RoboTwin 2.0 作为独立 Gateway→ToolEndpoint→Dora→SAPIEN runtime 接入；删除“Skill 与仿真环境绑定”的歧义。(local)
- [docs] [refactor] [Completed] Clarified v1.0 ownership and execution order: a Skill carries only provider-neutral ToolSpecs/workflow, while RoboTwin 2.0 is an independent Gateway→ToolEndpoint→Dora→SAPIEN runtime; removed the ambiguity that a Skill is bound to a simulator environment. (local)

### 预期影响文件 / Expected Files
- `docs/forge/FEATURE_REFERENCE_CARDS.md` [修改 / modified]
- `docs/user_development_guide/README.md`、`README_en.md` [修改 / modified]
- `docs/en/01-framework-introduction.md`、`03-developer-manual.md` [修改 / modified]
- `docs/zh/01-framework-introduction.md`、`03-developer-manual.md` [修改 / modified]
- `CHANGELOG.md` [修改 / modified：更新最近版本索引 / update recent-version index]
- `examples/forge-skills/robotwin2-scene-observe/` [删除残留未跟踪草案 / remove stale ignored prototype]

### 实际修改 / Actual Changes

- [完成] `docs/forge/FEATURE_REFERENCE_CARDS.md` L99-L144：将 RoboTwin 扩展点改为 Gateway/ToolEndpoint adapter + Dora profile；新增 RoboTwin 2.0 独立 runtime 边界、固定接入顺序和 `required_tools` readiness 约束；明确 Skill 不得绑定仿真器名称。(local)
- [Completed] `docs/forge/FEATURE_REFERENCE_CARDS.md` L99-L144: changed the RoboTwin extension point to a Gateway/ToolEndpoint adapter plus Dora profile; added the independent-runtime boundary, fixed onboarding order, and `required_tools` readiness constraint; made simulator-bound Skill names invalid.
- [完成] `docs/user_development_guide/README.md` L12-L43、`README_en.md` L12-L46：补充中英文接入表和 RoboTwin 执行顺序，规定 task/SAPIEN/embodiment/benchmark 仅在 adapter/profile，Agent 只走统一 Tool API。(local)
- [Completed] `docs/user_development_guide/README.md` L12-L43 and `README_en.md` L12-L46: added bilingual integration-table entries and RoboTwin execution order; task/SAPIEN/embodiment/benchmark remain in adapter/profile and Agent uses only the governed Tool API.
- [完成] `docs/en/01-framework-introduction.md` L9-L47、`docs/zh/01-framework-introduction.md` L9-L47：将 RoboTwin 明确定位为执行链末端 simulator runtime，并禁止 PAOS import RoboTwin/SAPIEN 或将仿真器名写入 Skill/ToolSpec。(local)
- [Completed] `docs/en/01-framework-introduction.md` L9-L47 and `docs/zh/01-framework-introduction.md` L9-L47: positioned RoboTwin as the terminal simulator runtime and prohibited PAOS imports or simulator names in Skills/ToolSpecs.
- [完成] `docs/en/03-developer-manual.md` L9-L23、L208-L226 与 `docs/zh/03-developer-manual.md` L9-L23、L197-L220：将物理链补全为 `ForgeToolClient → Gateway Tool API → ToolEndpoint → Dora → robot/simulator`，并按 ToolSpec → Fake Gateway → adapter → Bundle/Runtime → simulation acceptance 重排扩展流程。(local)
- [Completed] `docs/en/03-developer-manual.md` L9-L23, L208-L226 and `docs/zh/03-developer-manual.md` L9-L23, L197-L220: completed the physical path and reordered extension work as ToolSpec → Fake Gateway → adapter → Bundle/Runtime → simulation acceptance.
- [完成] 移除被忽略的旧草案目录 `examples/forge-skills/robotwin2-scene-observe/`；为可恢复性将其移至 `/tmp/robotwin2-scene-observe-withdrawn-20260902`，未加入 Git。(local)
- [Completed] Removed the ignored stale prototype directory `examples/forge-skills/robotwin2-scene-observe/`; moved it to `/tmp/robotwin2-scene-observe-withdrawn-20260902` for recoverability and did not add it to Git.
- [完成] `CHANGELOG.md` L5-L19 新增 v2.6.0 根索引条目；`.codegraph/` 与 `.cursor/` 保持未跟踪且未纳入提交。(local)
- [Completed] Added the v2.6.0 root index entry in `CHANGELOG.md` L5-L19; `.codegraph/` and `.cursor/` remain untracked and are excluded from the commit.

### 关键 Diff / Key Diff

```text
RoboTwin/仿真器适配 → Gateway/ToolEndpoint adapter + Dora profile；Bundle 仅冻结 wiring 与制品
Skill + adapter（仿真器语义易泄漏） → provider-neutral Skill + independent RoboTwin runtime
ToolEndpoint → Dora → robot/simulator → ForgeToolClient → Gateway Tool API → ToolEndpoint → Dora → robot/simulator
```

### 验证 / Verification

- [完成] `git diff --check` 通过；Markdown 文件 UTF-8 无替换字符；所有修改仅为文档、日志和残留草案清理，未修改 PAOS runtime、Gateway 实现、Dora、仿真器、硬件或运动路径。(local)
- [Completed] `git diff --check` passed; Markdown files are UTF-8 without replacement characters; changes are limited to documentation, logs, and stale-prototype cleanup, with no PAOS runtime, Gateway implementation, Dora, simulator, hardware, or motion-path changes.

### Git 提交 / Git Commit

- Commit: `720f1f1` (amended after changelog metadata update)
- Branch: `feature/long-horizon-workflow`
- 时间 / Time: 2026-09-02 (Asia/Shanghai)

## v2.6.1 (2026-09-02 00:00) - codex

### 变更摘要 / Change Summary
- [docs] [完成] 保存并审核“通用能力运行时与环境适配器分离”诊断：纠正将六个 PAOS ToolEndpoint 全部放入 RoboTwin adapter workspace 的深绑定方案，明确 capability runtime、provider ports、EnvironmentAdapter、Skill/Bundle 和 Gateway 的职责边界。(local)
- [docs] [Completed] Saved and reviewed the diagnosis for separating the generic capability runtime from environment adapters: rejected the deeply RoboTwin-bound layout that puts all six PAOS ToolEndpoints in one adapter workspace, and clarified responsibilities across capability runtime, provider ports, EnvironmentAdapter, Skill/Bundle, and Gateway. (local)

### 预期影响文件 / Expected Files
- `docs/forge/ROBOTWIN_ADAPTER_REFACTOR_DIAGNOSIS.md` [新增 / new]
- `docs/forge/README.md`、`docs/forge/README_zh.md`、`docs/README.md` [修改 / modified：增加诊断索引 / add diagnosis links]
- `changelog/2026-09.md` [修改 / modified]

### 实际修改 / Actual Changes

- [完成] `docs/forge/ROBOTWIN_ADAPTER_REFACTOR_DIAGNOSIS.md` L1-L278：保存架构诊断，定义 PAOS、generic capability runtime、provider port、EnvironmentAdapter、Gateway、Skill/Bundle 的所有权边界；记录六个 Tool 的重构 seam、Hephaestus clean-room 使用边界、profile 策略、实施顺序和验收门禁。(local)
- [Completed] Added `docs/forge/ROBOTWIN_ADAPTER_REFACTOR_DIAGNOSIS.md` L1-L278: defined ownership across PAOS, the generic capability runtime, provider ports, EnvironmentAdapter, Gateway, and Skill/Bundle; documented six-Tool refactor seams, clean-room Hephaestus usage rules, profile strategy, implementation order, and acceptance gates.
- [完成] `docs/forge/README.md` L218-L224、`docs/forge/README_zh.md` L205-L210、`docs/README.md` L21-L73：增加中英文诊断索引链接。(local)
- [Completed] Added bilingual diagnosis index links in `docs/forge/README.md` L218-L224, `docs/forge/README_zh.md` L205-L210, and `docs/README.md` L21-L73.
- [完成] `CHANGELOG.md` L5-L20：新增 v2.6.1 根索引；未修改 Hephaestus，`.codegraph/` 与 `.cursor/` 未纳入提交。(local)
- [Completed] Added the v2.6.1 root index in `CHANGELOG.md` L5-L20; Hephaestus was not modified, and `.codegraph/` plus `.cursor/` were excluded from the commit.

### 审核结论 / Review Outcome

- [完成] 无 Blocker：目标分层符合 PAOS v1.0 execution/evidence/verdict ownership，且没有新增第二套执行协议或环境绑定 Skill 名称。(local)
- [Completed] No Blocker: the target layering follows PAOS v1.0 execution/evidence/verdict ownership, with no second execution protocol or environment-bound Skill name.
- [完成] Major 风险已记录为后续实施门禁：当前尚无 generic capability runtime、RoboTwin20 EnvironmentAdapter、真实 Gateway/ToolEndpoint server、Dora flow/Node artifacts 或六个 Tool 的真实端到端验收。(local)
- [Completed] Major risks are recorded as future implementation gates: there is still no generic capability runtime, RoboTwin20 EnvironmentAdapter, production Gateway/ToolEndpoint server, Dora flow/Node artifacts, or real end-to-end acceptance for the six Tools.

### 验证 / Verification

- [完成] `git diff --check` 通过；新增 Markdown 相对链接目标检查通过；未执行运行时代码测试，因为本次仅新增诊断文档和索引。(local)
- [Completed] `git diff --check` passed; new Markdown relative-link targets passed; runtime tests were not run because this change only adds diagnosis documentation and index links.

### Git 提交 / Git Commit

- Commit: `ea60822`
- Branch: `feature/long-horizon-workflow`
- 时间 / Time: 2026-09-02 (Asia/Shanghai)
## v2.6.2 (2026-09-02 18:20) - codex

### 变更摘要 / Change Summary
- [完成] [docs] [refactor] 将包含六个公共 Tool 的 Skill 从 `scene-observe` 重命名为 `pick-place-workflow`，并修正其单一职责描述；明确 v1.0 仍采用独立 generic capability runtime。(local)
- [Completed] [docs] [refactor] Rename the Skill containing the six public Tools from `scene-observe` to `pick-place-workflow`, correct its single-workflow responsibility, and reaffirm the independent generic capability runtime required by v1.0. (local)
- [完成] [docs] [fix] 纠正 RoboTwin adapter 的感知边界：actor/entity、segmentation、object metadata 和内部几何只能作为仿真辅助/验收事实，不能冒充真实物理观察感知；真实感知必须通过传感器与可替换 perception provider 接入。(local)
- [Completed] [docs] [fix] Correct the RoboTwin adapter perception boundary: actor/entity data, segmentation, object metadata, and internal geometry are simulation aids/acceptance facts only, never physical-world perception; real perception must enter through sensors and replaceable perception providers. (local)

### 预期影响文件 / Expected Files
- `examples/forge-skills/scene-observe/` → `examples/forge-skills/pick-place-workflow/` [重命名 / rename]
- `docs/forge/ROBOTWIN_ADAPTER_REFACTOR_DIAGNOSIS.md` [修改 / modify]
- `docs/forge/FEATURE_REFERENCE_CARDS.md` [修改 / modify]
- `docs/user_development_guide/README.md`、`README_en.md`、`docs/en/03-developer-manual.md`、`docs/zh/03-developer-manual.md` [修改 / modify]
- `CHANGELOG.md` [修改 / modify：更新最近版本索引 / update recent-version index]

### 实际修改 / Actual Changes

- [完成] `examples/forge-skills/scene-observe/` 重命名为 `examples/forge-skills/pick-place-workflow/`；同步 Python 包目录、测试导入、manifest、Runtime discovery 和 workflow 文本中的 Skill identity。六个 Tool ID 保持不变，Skill 名称不再暗示只有 scene observation。(local)
- [Completed] Renamed `examples/forge-skills/scene-observe/` to `examples/forge-skills/pick-place-workflow/`; synchronized the Python package directory, test imports, manifest, Runtime discovery, and workflow text. All six Tool IDs remain unchanged, while the Skill name no longer implies scene observation only. (local)
- [完成] `examples/forge-skills/pick-place-workflow/SKILL.md` L3-L14、`README.md` L1-L16、`skill.yaml` L1-L4、`pyproject.toml` L1-L4：明确该 Skill 是完整六 Tool pick-and-place workflow，而不是 scene-observe Skill；Skill 不实现 provider、simulator、camera driver、robot SDK 或 task-specific verdict。(local)
- [Completed] Updated `examples/forge-skills/pick-place-workflow/SKILL.md` L3-L14, `README.md` L1-L16, `skill.yaml` L1-L4, and `pyproject.toml` L1-L4 to define one complete six-Tool pick-and-place workflow, not a scene-observe Skill; the Skill implements no provider, simulator, camera driver, robot SDK, or task-specific verdict. (local)
- [完成] `docs/forge/ROBOTWIN_ADAPTER_REFACTOR_DIAGNOSIS.md` L25-L30、L50-L56、L140-L143、L180-L209、L260-L292：确认 v1.0 仍采用独立 generic capability runtime；将 RoboTwin actor/entity、segmentation、object metadata、内部 pose 和 `check_success()` 限定为仿真辅助/对照/验收事实，禁止冒充真实物理感知；补充真实传感器与 perception provider 门禁，并更新当前审核结论。(local)
- [Completed] Updated `docs/forge/ROBOTWIN_ADAPTER_REFACTOR_DIAGNOSIS.md` L25-L30, L50-L56, L140-L143, L180-L209, and L260-L292 to reaffirm the independent generic capability runtime required by v1.0; classify RoboTwin actor/entity data, segmentation, object metadata, internal poses, and `check_success()` as simulation aid/comparison/acceptance facts only; prohibit presenting them as physical-world perception; and add sensor/perception-provider gates plus the corrected review conclusion. (local)
- [完成] `docs/forge/FEATURE_REFERENCE_CARDS.md` L103-L110、`docs/user_development_guide/README.md` L26-L43、`README_en.md` L26-L45、`docs/en/03-developer-manual.md` L216-L221、`docs/zh/03-developer-manual.md` L205-L211：同步单一 Skill 命名、generic runtime 依据和 sim-ground-truth/真实感知边界。(local)
- [Completed] Synchronized the single-Skill naming, generic-runtime basis, and simulation-ground-truth versus real-perception boundary in `docs/forge/FEATURE_REFERENCE_CARDS.md` L103-L110, `docs/user_development_guide/README.md` L26-L43, `README_en.md` L26-L45, `docs/en/03-developer-manual.md` L216-L221, and `docs/zh/03-developer-manual.md` L205-L211. (local)

### 关键 Diff / Key Diff

```text
scene-observe Skill → pick-place-workflow Skill
“RoboTwin actor/entity 可作为 observation” → “仅仿真对照/验收事实，真实 observation 必须来自传感器”
六个 Tool 绑定在 Skill 名称上 → 一个 workflow Skill 编排六个稳定 ToolSpec，环境由 profile 选择
```

### 验证 / Verification

- [完成] `/home/yanxu/miniconda3/envs/paos/bin/python -m pytest -q examples/forge-skills/pick-place-workflow/tests` → `220 passed in 2.60s`。(local)
- [Completed] `/home/yanxu/miniconda3/envs/paos/bin/python -m pytest -q examples/forge-skills/pick-place-workflow/tests` → `220 passed in 2.60s`. (local)
- [完成] `ruff check PhyAgentOS examples/forge-skills/pick-place-workflow`、`python -m compileall -q PhyAgentOS examples/forge-skills/pick-place-workflow/src examples/forge-skills/pick-place-workflow/tests`、`git diff --check` 均通过；未启动 Dora、Gateway server、RoboTwin、硬件或运动路由。(local)
- [Completed] `ruff check PhyAgentOS examples/forge-skills/pick-place-workflow`, `python -m compileall -q PhyAgentOS examples/forge-skills/pick-place-workflow/src examples/forge-skills/pick-place-workflow/tests`, and `git diff --check` all passed; no Dora, Gateway server, RoboTwin, hardware, or motion route was started. (local)

### Git 提交 / Git Commit

- Commit: `51135d7`
- Branch: `feature/long-horizon-workflow`
- 时间 / Time: 2026-09-02 18:20 (Asia/Shanghai)

## v2.6.3 (2026-09-02 18:35) - codex

### 变更摘要 / Change Summary
- [完成] [docs] [fix] 在中英文用户开发指南中补齐“独立 generic capability runtime”阶段，并将 RoboTwin 接入明确为 `EnvironmentAdapter`/provider ports，确保通用 runtime 与 Fake Gateway 契约通过后才接入环境。(local)
- [Completed] [docs] [fix] Added the independent generic capability runtime phase to the bilingual user development guides and made RoboTwin integration explicitly an `EnvironmentAdapter`/provider-port concern, so the environment is introduced only after generic runtime and Fake Gateway conformance.(local)

### 预期影响文件 / Expected Files
- `docs/user_development_guide/README.md` [修改 / modified]
- `docs/user_development_guide/README_en.md` [修改 / modified]
- `CHANGELOG.md` [修改 / modified：更新最近版本索引 / update recent-version index]

### 文件变更详情 / File Change Details

#### [修改 / Modified] `docs/user_development_guide/README.md` L36-L46

**修改前 / Before:**
```text
2. Fake Gateway；3. RoboTwin Gateway/ToolEndpoint adapter；4. Bundle/Dora。
```

**修改后 / After:**
```text
2. Fake Gateway；3. 独立 generic capability runtime；4. RoboTwin EnvironmentAdapter/provider ports；
5. Bundle/Dora；6. readiness；7. ForgeToolClient route。
```

**修改说明 / Notes:** 将通用 ToolEndpoint 生命周期与 provider ports 放在仿真器之前，并禁止把
Gateway/ToolEndpoint 实现归属 RoboTwin；仿真真值仍只作对照事实。

#### [修改 / Modified] `docs/user_development_guide/README_en.md` L37-L49

**修改前 / Before:**
```text
2. Fake Gateway; 3. RoboTwin Gateway/ToolEndpoint adapter; 4. Bundle/Dora.
```

**修改后 / After:**
```text
2. Fake Gateway; 3. simulator-free generic capability runtime; 4. RoboTwin EnvironmentAdapter/provider ports;
5. Bundle/Dora; 6. readiness; 7. ForgeToolClient route.
```

**修改说明 / Notes:** Keeps generic Endpoint semantics independent and makes the adapter implement only
provider ports and environment lifecycle.

#### [修改 / Modified] `docs/en/03-developer-manual.md` L216-L224；`docs/zh/03-developer-manual.md` L205-L213

**修改说明 / Notes:** Replaced “Gateway/ToolEndpoint adapter” wording with `EnvironmentAdapter` and provider
ports consumed by the generic runtime/Gateway; retained simulator-ground-truth and real-sensor boundaries.

### 验证 / Verification

- [完成] `PYTHONPATH=examples/forge-skills/pick-place-workflow/src:/home/yanxu/PhyAgentOS-forge /home/yanxu/miniconda3/envs/paos/bin/python -m pytest -q examples/forge-skills/pick-place-workflow/tests` → `220 passed in 2.48s`。(local)
- [Completed] The pick-place-workflow suite passed with `220 passed in 2.48s` in the isolated `paos` environment. (local)
- [完成] `ruff check PhyAgentOS examples/forge-skills/pick-place-workflow`、`python -m compileall -q PhyAgentOS examples/forge-skills/pick-place-workflow/src examples/forge-skills/pick-place-workflow/tests`、`git diff --cached --check` 均通过；未启动 Dora、Gateway server、RoboTwin、硬件或运动路由。(local)
- [Completed] Ruff, compileall, and staged diff checks passed; no Dora, Gateway server, RoboTwin, hardware, or motion route was started. (local)

### 审核结论 / Review Outcome

- [完成] 无 Blocker/Major：Skill 名称为 provider-neutral `pick-place-workflow`；v1.0 的 generic capability runtime 独立于 RoboTwin；仿真 actor/entity、segmentation、metadata、内部 pose 和 `check_success()` 未被定义为真实感知。(local)
- [Completed] No Blocker/Major findings remain: the Skill is provider-neutral `pick-place-workflow`; the v1.0 generic capability runtime is independent of RoboTwin; simulator actor/entity, segmentation, metadata, internal poses, and `check_success()` are not defined as real perception. (local)

### Git 提交 / Git Commit

- Commit: `51135d7`
- Branch: `feature/long-horizon-workflow`
- 时间 / Time: 2026-09-02 18:35 (Asia/Shanghai)

## v2.7.0 (2026-09-02 19:05) - codex

### 变更摘要 / Change Summary
- [完成] [comm] [feat] 实现不依赖仿真器、YOLO、机器人 SDK 或硬件的 generic capability runtime 基础：ToolEndpoint 注册、发现/context、Query 分发、Action 生命周期和 provider port 协议。(local)
- [Completed] [comm] [feat] Implemented the simulator-, YOLO-, robot-SDK-, and hardware-free generic capability runtime foundation: ToolEndpoint registration, discovery/context, Query dispatch, Action lifecycle, and provider-port protocols. (local)

### 预期影响文件 / Expected Files
- `PhyAgentOS/forge/capability_runtime/ports.py` [新增 / new]
- `PhyAgentOS/forge/capability_runtime/runtime.py` [新增 / new]
- `PhyAgentOS/forge/capability_runtime/__init__.py` [新增 / new]
- `PhyAgentOS/forge/__init__.py` [修改 / modified：导出公共 runtime API / export public runtime API]
- `examples/forge-skills/pick-place-workflow/tests/test_generic_capability_runtime.py` [新增 / new]
- `docs/forge/ROBOTWIN_ADAPTER_REFACTOR_DIAGNOSIS.md`、`docs/forge/FEATURE_REFERENCE_CARDS.md` [修改 / modified]
- `docs/user_development_guide/README.md`、`README_en.md`、`docs/en/03-developer-manual.md`、`docs/zh/03-developer-manual.md` [修改 / modified：统一 EnvironmentAdapter 术语 / normalize EnvironmentAdapter terminology]

### 文件变更详情 / File Change Details

#### [新增 / Added] `PhyAgentOS/forge/capability_runtime/ports.py` L1-L84

**新增代码 / Added:**
```python
class ObservationSource(Protocol): ...
class SceneUnderstandingProvider(Protocol): ...
class GraspProposalProvider(Protocol): ...
class ReadinessEvaluator(Protocol): ...
class ManipulationExecutor(Protocol): ...
```

**修改说明 / Notes:** 定义环境可替换 provider ports 和通用 ActionAdmission；不导入 RoboTwin、YOLO、Dora 或硬件。

#### [新增 / Added] `PhyAgentOS/forge/capability_runtime/runtime.py` L1-L295

**新增代码 / Added:**
```python
runtime.register_tool(spec, endpoint, context=...)
runtime.invoke_query(endpoint_id, operation, arguments)
runtime.start_action(tool_id, arguments, caller_id=..., timeout_ms=...)
runtime.invocation_status(invocation_id)
runtime.invocation_result(invocation_id)
runtime.cancel_invocation(invocation_id)
```

**修改说明 / Notes:** 实现 provider-neutral ToolSpec 校验、发现/context、Query 分发、max_concurrency、
Action invocation/attempt bookkeeping、pending/terminal/unknown 和 cancel-requested 语义；运行时不执行
物理运动，取消请求不冒充已停止。

#### [新增 / Added] `PhyAgentOS/forge/capability_runtime/__init__.py` L1-L43；[修改 / Modified] `PhyAgentOS/forge/__init__.py` L3-L28

**修改说明 / Notes:** 暴露 generic runtime 与 provider-port 公共 API。

#### [新增 / Added] `examples/forge-skills/pick-place-workflow/tests/test_generic_capability_runtime.py` L1-L119

**修改说明 / Notes:** 覆盖 ToolSpec/provider-neutral 校验、发现/context、Query readiness、Action 并发、
caller/timeout 传递、pending→running→succeeded、cancel-requested 和非法 admission；全部 no-motion。

#### [修改 / Modified] `docs/forge/ROBOTWIN_ADAPTER_REFACTOR_DIAGNOSIS.md` L260-L280；`docs/forge/FEATURE_REFERENCE_CARDS.md` L118-L145；`examples/forge-skills/pick-place-workflow/README.md` L13-L19

**修改说明 / Notes:** 将 generic runtime foundation 标记为已实现，同时明确没有 YOLO/Ultralytics、真实相机、
抓取模型或机器人执行器接入；Fake/provider-neutral `grasp.propose` 结果不等于识别或抓取完成。

#### [修改 / Modified] `docs/user_development_guide/README.md` L15-L46；`README_en.md` L15-L49；`docs/en/03-developer-manual.md` L216-L224；`docs/zh/03-developer-manual.md` L205-L213

**修改说明 / Notes:** 统一 generic runtime → EnvironmentAdapter/provider ports 的术语和执行顺序。

### 验证 / Verification

- [完成] `PYTHONPATH=examples/forge-skills/pick-place-workflow/src:/home/yanxu/PhyAgentOS-forge /home/yanxu/miniconda3/envs/paos/bin/python -m pytest -q examples/forge-skills/pick-place-workflow/tests` → `225 passed in 2.52s`。(local)
- [Completed] The complete pick-place-workflow suite passed with `225 passed in 2.52s` in the isolated `paos` environment. (local)
- [完成] `ruff check PhyAgentOS examples/forge-skills/pick-place-workflow`、`python -m compileall -q PhyAgentOS examples/forge-skills/pick-place-workflow/src examples/forge-skills/pick-place-workflow/tests`、`git diff --cached --check` 均通过；未启动 Dora、Gateway server、RoboTwin、硬件或运动路由。(local)
- [Completed] Ruff, compileall, and staged diff checks passed; no Dora, Gateway server, RoboTwin, hardware, or motion route was started. (local)

### 审核结论 / Review Outcome

- [完成] 无 Blocker/Major：runtime 只提供通用 registration/dispatch/lifecycle foundation；provider-specific implementation、YOLO、真实传感器和 physical execution 仍在后续 EnvironmentAdapter/Gateway 阶段。(local)
- [Completed] No Blocker/Major findings: the runtime only provides generic registration/dispatch/lifecycle foundations; provider-specific implementations, YOLO, real sensors, and physical execution remain later EnvironmentAdapter/Gateway work. (local)

### Git 提交 / Git Commit

- Commit: `148b5eb`
- Branch: `feature/long-horizon-workflow`
- 时间 / Time: 2026-09-02 19:05 (Asia/Shanghai)

## v2.8.0 (2026-09-02 19:40) - codex

### 变更摘要 / Change Summary
- [完成] [sense] [feat] 在独立 adapter 包中实现 RoboTwin20 环境生命周期 seam 与 camera/depth/state 传感器 observation source；不依赖 RoboTwin、SAPIEN、YOLO、Dora、机器人 SDK 或硬件。(local)
- [Completed] [sense] [feat] Implemented the RoboTwin20 environment lifecycle seam and camera/depth/state sensor observation source in an independent adapter package, with no RoboTwin, SAPIEN, YOLO, Dora, robot-SDK, or hardware dependency. (local)

### 预期影响文件 / Expected Files
- `examples/forge-adapters/robotwin20/pyproject.toml` [新增 / new]
- `examples/forge-adapters/robotwin20/README.md` [新增 / new]
- `examples/forge-adapters/robotwin20/src/robotwin20_adapter/adapter.py` [新增 / new]
- `examples/forge-adapters/robotwin20/src/robotwin20_adapter/__init__.py` [新增 / new]
- `examples/forge-adapters/robotwin20/tests/test_adapter.py` [新增 / new]
- `docs/forge/ROBOTWIN_ADAPTER_REFACTOR_DIAGNOSIS.md`、`docs/forge/FEATURE_REFERENCE_CARDS.md` [修改 / modified]
- `examples/forge-adapters/robotwin20/README.md`、`docs/user_development_guide/README.md`、`README_en.md` [修改 / modified：记录依赖/资产隔离 / document dependency and asset isolation]

### 文件变更详情 / File Change Details

#### [新增 / Added] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/adapter.py` L1-L219

**新增代码 / Added:**
```python
class RoboTwin20Adapter:
    def reset(self, *, seed: int | None = None) -> None: ...
    def snapshot(self) -> dict[str, Any]: ...
    def capture(self, sensor_ref: str) -> SensorCapture: ...

class RoboTwinObservationSource:
    def capture(self, request: Mapping[str, Any]) -> dict[str, Any] | None: ...
```

**修改说明 / Notes:** 通过注入的 `RoboTwinSensorBackend` 提供环境 reset、sanitized snapshot 和传感器捕获；
强制 RGB/depth/state、frame、calibration、scene revision、时区时间戳和 artifact ref，拒绝缺失数据与
actor/ground-truth 风格引用，不读取或导出 simulator truth。

#### [新增 / Added] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/__init__.py` L1-L22；`pyproject.toml` L1-L22；`README.md` L1-L27

**修改说明 / Notes:** 建立独立 adapter 包；依赖为空，RoboTwin/SAPIEN/Torch/YOLO 和仿真资产由外部 runtime/profile
提供，不进入 PAOS wheel 或 control-plane 环境。

#### [新增 / Added] `examples/forge-adapters/robotwin20/tests/test_adapter.py` L1-L105

**修改说明 / Notes:** 覆盖 reset/snapshot 隔离、RGB/depth/state observation、缺失 calibration/required kind、
非法 artifact、typed capture 和 invalid profile；全部 no-motion。

#### [修改 / Modified] `docs/forge/ROBOTWIN_ADAPTER_REFACTOR_DIAGNOSIS.md` L268-L284；`docs/forge/FEATURE_REFERENCE_CARDS.md` L144-L148；`docs/user_development_guide/README.md` L40-L44；`README_en.md` L41-L49

**修改说明 / Notes:** 记录 adapter foundation 已完成、真实 RoboTwin backend 尚未可运行，并明确依赖与资产隔离边界。

### 验证 / Verification

- [完成] `PYTHONPATH=examples/forge-skills/pick-place-workflow/src:examples/forge-adapters/robotwin20/src:/home/yanxu/PhyAgentOS-forge /home/yanxu/miniconda3/envs/paos/bin/python -m pytest -q examples/forge-skills/pick-place-workflow/tests examples/forge-adapters/robotwin20/tests` → `232 passed in 2.52s`。(local)
- [Completed] The workflow and adapter suites passed with `232 passed in 2.52s` in the isolated `paos` interpreter. (local)
- [完成] `ruff check PhyAgentOS examples/forge-skills/pick-place-workflow examples/forge-adapters/robotwin20`、`compileall`、`git diff --cached --check` 通过；PAOS 与 adapter 依赖隔离检查通过；未启动 RoboTwin、SAPIEN、Dora、YOLO、硬件或运动路由。(local)
- [Completed] Ruff, compileall, staged diff, and dependency-isolation checks passed; no RoboTwin, SAPIEN, Dora, YOLO, hardware, or motion route was started. (local)

### 审核结论 / Review Outcome

- [完成] 无 Blocker/Major：adapter 仅实现环境 seam 和传感器 observation contract；PAOS 不新增仿真/模型依赖，仿真 ground truth 不进入公共感知结果。(local)
- [Completed] No Blocker/Major findings: the adapter implements only the environment seam and sensor observation contract; PAOS gains no simulator/model dependency, and simulator ground truth cannot enter public perception results. (local)

### Git 提交 / Git Commit

- Commit: `327f3ee`
- Branch: `feature/long-horizon-workflow`
- 时间 / Time: 2026-09-02 19:40 (Asia/Shanghai)

## v2.8.1 (2026-09-02 20:05) - codex

### 变更摘要 / Change Summary
- [完成] [env] [chore] 核对 `RoboTwin20` 隔离 conda 环境，递归 checkout 官方 RoboTwin 2.0 源码，并确认官方资产由仓库脚本从 `TianxingChen/RoboTwin2.0` 下载；仅在 PAOS/Hephaestus 之外准备外部 runtime，未修改 PAOS 依赖或公共 ToolSpec。
- [Completed] [env] [chore] Verified the isolated `RoboTwin20` conda environment, recursively checked out the official RoboTwin 2.0 source, and confirmed that the repository script downloads assets from `TianxingChen/RoboTwin2.0`; prepared the external runtime outside PAOS/Hephaestus without changing PAOS dependencies or public ToolSpecs.

### 预期影响文件 / Expected Files
- `changelog/2026-09.md` [修改 / modified]
- `CHANGELOG.md` [修改 / modified]
- 外部目录（不属于本仓库）：`/home/yanxu/robotwin20-runtime/` [计划 / planned]

### 文件变更详情 / File Change Details

#### [完成 / Completed] 外部 RoboTwin runtime 与部分 assets

**已执行 / Executed:**
```text
git clone --recurse-submodules https://github.com/RoboTwin-Platform/RoboTwin.git /home/yanxu/robotwin20-runtime/RoboTwin
```

源码 commit 为 `30954692d06ba7e89f7a6b76064f4062c488fa81`，`XPolicyLab` 子模块为
`c37109c500be67d0dea6b36bf7337bbd26e763cd`。官方资产下载脚本为
`/home/yanxu/robotwin20-runtime/RoboTwin/assets/_download.py`，来源为
`https://huggingface.co/datasets/TianxingChen/RoboTwin2.0`。

已下载并校验：`assets/embodiments.zip`，大小 `219859313` bytes，SHA-256
`6b87d7d55e106d8ff25917e0538eb1e177fc549280e8a742a8cec3cb9f953fc6`。
未下载：`background_texture.zip`（`10970687027` bytes）和 `objects.zip`
（`3737778549` bytes）。

**用户自行下载命令 / User-run download commands:**
```bash
cd /home/yanxu/robotwin20-runtime/RoboTwin
conda activate RoboTwin20
python assets/_download.py
cd assets
unzip -f background_texture.zip
unzip -f embodiments.zip
unzip -f objects.zip
cd ..
python scripts/update_embodiment_config_path.py
```

**边界 / Boundary:** RoboTwin/SAPIEN/Torch/YOLO 依赖、task/embodiment 配置与 simulator assets remain outside the PAOS wheel, PAOS conda environment, and Hephaestus source tree.

#### [修改 / Modified] `CHANGELOG.md` L4-L22

**修改前 / Before:** 最新条目为 v2.8.0，未记录独立 RoboTwin runtime checkout 或资产下载状态。

**修改后 / After:** 增加 v2.8.1 双语摘要、官方源码/子模块 commit、资产来源、已校验 archive digest、未下载 archive 大小以及未跟踪目录边界。

**修改说明 / Notes:** 让仓库入口 changelog 与月度日志同步，明确“环境已建立”不等于“完整 RoboTwin 资产已安装”。

### 验证 / Verification
- [完成] `RoboTwin20` 环境存在（Python 3.10.21）；已验证 `sapien==3.0.0b1`、`torch==2.4.1`、`torchvision==0.19.1`、OpenCV、Gymnasium、Open3D；未安装 `robotwin`/`RoboTwin` Python 包和 `ultralytics`。(local)
- [Completed] The `RoboTwin20` environment exists (Python 3.10.21) with SAPIEN, Torch, TorchVision, OpenCV, Gymnasium, and Open3D verified; `robotwin`/`RoboTwin` Python packages and `ultralytics` are not installed. (local)
- [完成] 官方资产 API 清单显示三项 zip 总计约 14.9 GB；本次仅完成 219 MB embodiments，未将部分下载误报为完整资产。(local)
- [Completed] The official asset API lists three zip files totaling about 14.9 GB; only the 219 MB embodiments archive was completed, and the partial state was not reported as a complete asset set. (local)
## v2.8.2 (2026-09-02 20:30) - codex

### 变更摘要 / Change Summary
- [完成] [env] [chore] 验收官方 RoboTwin 2.0 assets，并按官方安装顺序补齐独立 `RoboTwin20` conda 环境的 runtime 依赖；随后执行无动作的 Vulkan/SAPIEN/import/config 集成验证，不污染 PAOS 环境。(local)
- [Completed] [env] [chore] Validated the official RoboTwin 2.0 assets, completed the isolated `RoboTwin20` conda runtime dependencies in the official order, and ran no-action Vulkan/SAPIEN/import/config integration checks without modifying the PAOS environment. (local)

### 验证 / Verification
- [完成] `robotwin20_adapter.preflight` 从 PAOS 根目录执行返回 `ready=true`；16 项检查全部通过，包含 11,000 background-texture files、229 embodiment files、9,368 object files、Python 3.10.21、Torch `2.9.1+cu128`、CUDA capability `[12, 0]`、CuRobo/PyTorch3D/Warp、SAPIEN、Vulkan 和 task import。(local)
- [Completed] `robotwin20_adapter.preflight` returned `ready=true` from the PAOS root; all 16 checks passed, including the three asset families, Python 3.10.21, Torch `2.9.1+cu128`, CUDA capability `[12, 0]`, CuRobo/PyTorch3D/Warp, SAPIEN, Vulkan, and task import. (local)

### 文件变更详情 / File Change Details
- [新增 / Added] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/preflight.py` L1-L284：标准库实现的 fail-closed 外部 runtime 检查；不在 PAOS 进程导入 RoboTwin/SAPIEN/Torch。
- [新增 / Added] `examples/forge-adapters/robotwin20/tests/test_preflight.py` L1-L75：布局、资产缺失和 runtime probe 失败分支测试。
- [修改 / Modified] `examples/forge-adapters/robotwin20/README.md` L29-L71、`pyproject.toml` L1-L13：记录 preflight 命令与 console entry point。

## v2.8.3 (2026-09-02 21:40) - codex

### 变更摘要 / Change Summary
- [完成] [sense] [feat] 在独立 RoboTwin runtime 中实现真实 camera RGB/depth/state artifact backend，并通过现有 `RoboTwin20Adapter` 注入；严格禁止 simulator truth、动作执行和 PAOS 依赖污染。
- [Completed] [sense] [feat] Implemented a real camera RGB/depth/state artifact backend in the isolated RoboTwin runtime and injected it through the existing `RoboTwin20Adapter`; simulator truth, action execution, and PAOS dependency contamination remain prohibited.

### 预期影响文件 / Expected Files
- `examples/forge-adapters/robotwin20/runtime/robotwin_backend.py` [新增 / new; runtime-only source, not PAOS wheel]
- `examples/forge-adapters/robotwin20/tests/test_robotwin_backend_contract.py` [新增 / new]
- `examples/forge-adapters/robotwin20/README.md` [修改 / modified]
- `examples/forge-adapters/robotwin20/src/robotwin20_adapter/adapter.py` [修改 / modified if contract requires]
- `CHANGELOG.md`、`changelog/2026-09.md` [修改 / modified]

### 计划验证 / Planned Verification
- fake runtime contract tests and static dependency-boundary checks;
- real RoboTwin runtime reset + one camera RGB/depth/state capture with no `play_once`/`check_success`;
- artifact bytes, timestamp, frame, calibration, and scene revision validation;
- PAOS test suite, Ruff, compileall, and `git diff --check`.

### 实际结果 / Actual Result
- [完成] 从 PAOS 根目录运行 `robotwin_backend.py` 成功完成 `beat_block_hammer/demo_clean`、seed `0`、`camera/head` 的无动作 capture；生成 `240x320` RGB PNG、`240x320` float32 depth NPY、state JSON（`joint_action`/`endpose`）和 calibration JSON（`intrinsic_cv`/`extrinsic_cv`/`cam2world_gl`）。
- [Completed] A no-motion capture succeeded from the PAOS root for `beat_block_hammer/demo_clean`, seed `0`, and `camera/head`, producing a `240x320` RGB PNG, `240x320` float32 depth NPY, state JSON (`joint_action`/`endpose`), and calibration JSON (`intrinsic_cv`/`extrinsic_cv`/`cam2world_gl`).
- [完成] PAOS adapter/workflow tests `241 passed in 2.56s`；Ruff、compileall、`git diff --check` 通过。运行期间 SAPIEN 输出 OIDN CUDA denoiser warning/error，但 artifact 成功持久化，不能据此宣称真实感知质量已验证。(local)
- [Completed] PAOS adapter/workflow tests passed with `241 passed in 2.56s`; Ruff, compileall, and `git diff --check` passed. SAPIEN emitted OIDN CUDA denoiser warnings/errors during the run, but artifacts persisted; this does not establish real-perception quality. (local)

### 文件变更详情 / File Change Details
- [新增 / Added] `examples/forge-adapters/robotwin20/runtime/robotwin_backend.py` L1-L338：runtime-only `RoboTwinSensorBackend`，动态导入官方 task，输出 RGB/depth/state/calibration artifact，并拒绝 segmentation/actor truth、task evaluator 和动作调用。
- [新增 / Added] `examples/forge-adapters/robotwin20/tests/test_robotwin_backend_contract.py` L1-L77：profile 边界、公共 camera ref、禁止 PAOS artifact root 与 runtime cwd 恢复测试。
- [修改 / Modified] `examples/forge-adapters/robotwin20/README.md` L46-L72：补充真实 capture 命令、禁止根目录、产物和 OIDN 风险。

### 预期影响文件 / Expected Files
- `changelog/2026-09.md`、`CHANGELOG.md` [修改 / modified]
- 外部 runtime（不属于本仓库）：`/home/yanxu/robotwin20-runtime/RoboTwin/` 与 `/home/yanxu/miniconda3/envs/RoboTwin20/` [安装/验证 / install and validate]

### 计划验证 / Planned Verification
- 三类官方 assets 目录、文件数量与配置路径；
- `vulkaninfo`、SAPIEN renderer/scene；
- 官方 requirements、PyTorch3D、CuRobo、Warp 与 XPolicyLab import/install；
- RoboTwin task/config import preflight，仅验证构造入口与配置，不调用 `setup_demo`、`play_once`、`check_success` 或任何动作执行。

### 实际结果 / Actual Result
- [完成] 以上 preflight 与依赖隔离验证已完成；未启动 Dora、Gateway、YOLO、真实机器人或动作路由。`setup_demo` 仅在后续 observation smoke 中用于建立传感器场景，未调用 `play_once`、`check_success` 或动作 API。
- [Completed] The preflight and dependency-isolation checks completed; no Dora, Gateway, YOLO, real robot, or action route was started. `setup_demo` is used only by the later observation smoke to initialize a sensor scene; `play_once`, `check_success`, and action APIs were not called.

## v2.8.4 (2026-09-02 22:15) - codex

### 变更摘要 / Change Summary
- [完成] [sense] [fix] 修复 runtime-only backend 从 PAOS 根目录启动时的工作目录边界：所有 RoboTwin 官方导入、场景初始化、观测读取和关闭调用均在外部 runtime root 上下文执行；不改变 PAOS 依赖、ToolSpec 或动作权限。(local)
- [Completed] [sense] [fix] Fixed the runtime-only backend working-directory boundary when launched from the PAOS root: all official RoboTwin imports, scene setup, observation reads, and close calls execute under the external runtime root context without changing PAOS dependencies, ToolSpecs, or motion authority. (local)

### 计划验证 / Planned Verification
- 从 PAOS 根目录执行真实 `RoboTwin20` RGB/depth/state capture；
- 验证生成 artifact 与 adapter contract；
- 重跑 adapter/workflow tests、Ruff、compileall 和 diff check；保留 OIDN 警告为已知风险。
- Run a real `RoboTwin20` RGB/depth/state capture from the PAOS root; validate artifacts and the adapter contract; rerun tests, Ruff, compileall, and diff checks while retaining OIDN warnings as a known risk.

### 文件变更详情 / File Change Details
- [修改 / Modified] `examples/forge-adapters/robotwin20/runtime/robotwin_backend.py` L15、L18、L49-L76、L88-L101、L119-L120、L179-L184、L208-L209、L223-L224、L308-L326：新增 `_runtime_cwd()` 受控上下文和 `forbidden_roots` 校验，包裹官方 import、`setup_demo`、`get_obs`、`close_env` 并防止 artifact 写入 PAOS/Hephaestus。
- [修改 / Modified] `examples/forge-adapters/robotwin20/tests/test_robotwin_backend_contract.py` L45-L54、L67-L77：验证禁止根目录及 runtime root 切换/cwd 恢复，防止从 PAOS 根目录启动回归。

### 验证 / Verification
- [完成] 从 `/home/yanxu/PhyAgentOS-forge` 根目录真实执行 backend 成功；此前的 `assets/objects/objaverse/list.json` 相对路径错误已消除，产物仍写入外部 `/home/yanxu/robotwin20-runtime/artifacts`。(local)
- [Completed] The backend succeeded when launched from `/home/yanxu/PhyAgentOS-forge`; the prior relative-path failure for `assets/objects/objaverse/list.json` was eliminated, with artifacts still written outside the repository under `/home/yanxu/robotwin20-runtime/artifacts`. (local)

### Git 提交 / Git Commit
- Commit: `321606a`
- Branch: `feature/long-horizon-workflow`
- 时间 / Time: 2026-09-02 22:20 (Asia/Shanghai)

## v2.8.5 (2026-09-02 22:40) - codex

### 变更摘要 / Change Summary
- [完成] [sense] [feat] 将 RoboTwin sensor capture 投影为与 Fake Gateway 相同的 provider-neutral `scene.observe` snapshot，并通过 `ForgeToolClient.invoke_query_tool` 验证两条路径返回一致结果；PAOS 不导入 RoboTwin、SAPIEN、Torch 或模型依赖。(local)
- [Completed] [sense] [feat] Projected RoboTwin sensor captures into the same provider-neutral `scene.observe` snapshot used by the Fake Gateway, and verified both paths through `ForgeToolClient.invoke_query_tool`; PAOS imports none of RoboTwin, SAPIEN, Torch, or model dependencies. (local)

### 预期影响文件 / Expected Files
- `examples/forge-adapters/robotwin20/runtime/robotwin_backend.py` [修改 / modified]
- `examples/forge-adapters/robotwin20/tests/test_scene_observe_integration.py` [新增 / new]
- `examples/forge-adapters/robotwin20/pyproject.toml`、`examples/forge-adapters/robotwin20/README.md` [修改 / modified]
- `examples/forge-skills/pick-place-workflow/src/pick_place_workflow/fake_gateway.py`、`docs/forge/ROBOTWIN_ADAPTER_REFACTOR_DIAGNOSIS.md`、`CHANGELOG.md` [修改 / modified]

### 实际结果 / Actual Result
- Fake Gateway and RoboTwin provider snapshots through the real `ForgeToolClient`;
- exact output equality for observation identity, frame, calibration, freshness, and artifact kinds;
- no simulator/model imports in the PAOS interpreter; tests, Ruff, compileall, and diff checks.

### 文件变更详情 / File Change Details

#### [修改 / Modified] `examples/forge-adapters/robotwin20/runtime/robotwin_backend.py` L49-L104、L347-L404

**修改前 / Before:** runtime capture only exposed raw `SensorCapture` metadata and the provider accepted only
`capture_sensors` backends.

**修改后 / After:** added `RoboTwinObservationSnapshot` and `RoboTwinObservationProvider`, accepting either the
runtime `capture_sensors(sensor_ref)` seam or the PAOS adapter `capture(sensor_ref)` seam and projecting
observation identity, timestamp, scene revision, frame, calibration and typed artifact references.

**修改说明 / Notes:** this is a provider-neutral boundary; RoboTwin/SAPIEN/Torch remain in the external Python
3.10 runtime process and are not imported by PAOS. `--format scene_observe` emits the same JSON shape consumed by
the Gateway endpoint and performs one sensor capture per invocation.

#### [修改 / Modified] `examples/forge-skills/pick-place-workflow/src/pick_place_workflow/fake_gateway.py` L59

**修改前 / Before:** `^artifact://[^/]+/[^/]+$`

**修改后 / After:** `^artifact://[^/]+/.+$`

**修改说明 / Notes:** accepts valid artifact references with capture subpaths such as
`artifact://scene/capture/rgb`, matching the public ToolSpec and RoboTwin runtime artifacts.

#### [新增 / Added] `examples/forge-adapters/robotwin20/tests/test_scene_observe_integration.py` L1-L126

**新增代码 / Added:** invokes `scene.observe` through `ForgeToolClient` against both a Fake provider and the
RoboTwin adapter provider, asserting byte-for-byte equal provider-neutral results and JSON serializability.

#### [修改 / Modified] `docs/forge/ROBOTWIN_ADAPTER_REFACTOR_DIAGNOSIS.md` L269-L292

**修改说明 / Notes:** corrected the status section to record that the runtime-only RoboTwin backend,
provider-neutral `scene.observe` projection, and external smoke are complete; only production HTTP/Dora wiring,
cross-process provider transport, and the remaining five real providers are still pending.

### 验证 / Verification

- [完成] PAOS adapter/workflow tests: `244 passed in 2.53s`；Ruff、compileall、`git diff --check` 全部通过。
- [Completed] PAOS adapter/workflow tests: `244 passed in 2.53s`; Ruff, compileall, and `git diff --check` passed.
- [完成] 在外部 `RoboTwin20` Python 3.10 环境从 PAOS 根目录执行 `--format scene_observe`，返回
  `observation://beat_block_hammer-0-1/head_camera` 以及 RGB/depth/state artifacts；OIDN CUDA warnings
  仍为已知运行时风险，不构成感知质量证明。(local)
- [Completed] Ran `--format scene_observe` in the external RoboTwin20 Python 3.10 environment from the PAOS
  root; it returned `observation://beat_block_hammer-0-1/head_camera` with RGB/depth/state artifacts. OIDN
  CUDA warnings remain a known runtime risk and do not prove perception quality. (local)
- [完成] 在 PAOS Python 3.11+ 解释器导入 generic capability runtime 后，`robotwin`/`sapien`/`torch`/
  `ultralytics`/`dora` 均未进入 `sys.modules`；PAOS 侧保持零仿真/模型依赖。(local)
- [Completed] Importing the generic capability runtime under the PAOS Python 3.11+ interpreter left
  `robotwin`/`sapien`/`torch`/`ultralytics`/`dora` absent from `sys.modules`; PAOS retains zero simulator/model
  dependencies. (local)

### Git 提交 / Git Commit
- Commit: `146d837`
- Branch: `feature/long-horizon-workflow`
- 时间 / Time: 2026-09-02 22:20 (Asia/Shanghai)
## v2.8.6 (2026-09-02 22:30) - codex

### 变更摘要 / Change Summary
- [完成] [sense] [feat] 按 v1.0 扩展文档，在现有 provider-neutral `scene.understand` ToolSpec 之上接入独立 adapter/provider seam；输入仅绑定 `scene.observe` artifacts，输出保留 entity/relation/confidence/provenance，不读取 RoboTwin 仿真真值或模型专有字段。(local)
- [Completed] [sense] [feat] Following the v1.0 extension guide, added an independent adapter/provider seam behind the existing provider-neutral `scene.understand` ToolSpec; it binds only to `scene.observe` artifacts and preserves entity/relation/confidence/provenance without RoboTwin simulator truth or model-specific fields. (local)

### 预期影响文件 / Expected Files
- `examples/forge-adapters/robotwin20/src/robotwin20_adapter/understanding.py` [新增 / new]
- `examples/forge-adapters/robotwin20/tests/test_scene_understand_provider.py` [新增 / new]
- `examples/forge-adapters/robotwin20/README.md`、`examples/forge-skills/pick-place-workflow/src/pick_place_workflow/understanding.py`、`docs/forge/FEATURE_REFERENCE_CARDS.md`、`CHANGELOG.md` [修改 / modified]

### 实际结果 / Actual Result
- provider contract tests through the existing `scene.understand` Fake Gateway and `ForgeToolClient`;
- reject missing/stale observation bindings and malformed provider output;
- confirm the adapter package remains free of RoboTwin/SAPIEN/Torch/YOLO imports and no motion route is called.

### 文件变更详情 / File Change Details

#### [新增 / Added] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/understanding.py` L1-L99

**修改说明 / Notes:** added an injected `SceneUnderstandingInference` seam and
`RoboTwinSceneUnderstandingProvider`. It forwards only observation identity and artifact references,
normalizes neutral entity/relation/envelope/ambiguity collections, and rejects provider-specific fields.

#### [修改 / Modified] `examples/forge-skills/pick-place-workflow/src/pick_place_workflow/understanding.py` L222-L234

**修改说明 / Notes:** provider exceptions now become the explicit `understanding_provider_error` ToolResult;
they do not escape as an unstructured Gateway failure.

#### [新增 / Added] `examples/forge-adapters/robotwin20/tests/test_scene_understand_provider.py` L1-L95

**新增代码 / Added:** verifies ForgeToolClient/Fake Gateway invocation, observation-only request projection,
stale rejection before inference, provider-specific field rejection, and the PAOS import boundary.

#### [修改 / Modified] `examples/forge-adapters/robotwin20/README.md` L85-L90；`docs/forge/FEATURE_REFERENCE_CARDS.md` L144-L149

**修改说明 / Notes:** documents the injected provider seam and updates the implementation status without
claiming a real VLM/detector or YOLO integration.

### 验证 / Verification

- [完成] `248 passed in 2.65s`；Ruff、compileall、`git diff --check` 全部通过。
- [Completed] `248 passed in 2.65s`; Ruff, compileall, and `git diff --check` passed.
- [完成] provider-specific output is converted to `understanding_provider_error`; no simulator/model import is
  added to PAOS, and no Action/Session/motion route is invoked. (local)
- [Completed] Provider-specific output is converted to `understanding_provider_error`; no simulator/model import
  was added to PAOS, and no Action/Session/motion route was invoked. (local)

### Git 提交 / Git Commit
- Commit: `d6fed7e`
- Branch: `feature/long-horizon-workflow`
- 时间 / Time: 2026-09-02 22:30 (Asia/Shanghai)
## v2.8.7 (2026-09-02 22:45) - codex

### 变更摘要 / Change Summary
- [完成] [sense] [refactor] 将 `scene.understand` 公共 ToolSpec、snapshot 校验、结果投影和 fail-closed 语义归位到 PAOS generic capability runtime；Skill 包仅保留兼容导出，避免把通用能力实现放在 RoboTwin/Skill workspace。(local)
- [Completed] [sense] [refactor] Moved the public `scene.understand` ToolSpec, snapshot validation, result projection, and fail-closed semantics into the PAOS generic capability runtime; the Skill package now keeps only compatibility exports so generic capability code is not owned by a RoboTwin/Skill workspace. (local)

### 预期影响文件 / Expected Files
- `PhyAgentOS/forge/capability_runtime/understanding.py` [新增 / new]
- `PhyAgentOS/forge/capability_runtime/__init__.py` [修改 / modified]
- `examples/forge-skills/pick-place-workflow/src/pick_place_workflow/understanding.py` [重构 / refactor]
- `CHANGELOG.md` [修改 / modified]

### 实际结果 / Actual Result
- existing workflow and adapter suites through the PAOS-owned implementation;
- strict ToolSpec and provider failure behavior unchanged;
- no RoboTwin/SAPIEN/Torch/YOLO/Dora imports and no motion route.

### 文件变更详情 / File Change Details

#### [新增 / Added] `PhyAgentOS/forge/capability_runtime/understanding.py` L1-L238

**修改说明 / Notes:** added the PAOS-owned `scene.understand` ToolSpec, neutral snapshot type, strict
observation/artifact/entity/relation/envelope validation, stale binding rejection, provider error projection,
and Query result projection. The module contains no environment or model imports.

#### [重构 / Refactor] `examples/forge-skills/pick-place-workflow/src/pick_place_workflow/understanding.py` L1-L33

**修改前 / Before:** the Skill example owned the full `scene.understand` contract and endpoint implementation.

**修改后 / After:** the module re-exports the PAOS generic runtime implementation and preserves the existing
Skill import names for compatibility.

#### [修改 / Modified] `PhyAgentOS/forge/capability_runtime/__init__.py` L23-L34

**修改说明 / Notes:** exported the PAOS-owned endpoint, snapshot, and ToolSpec alias from the generic runtime.

### 验证 / Verification

- [完成] adapter/workflow tests: `248 passed in 2.64s`；Ruff、compileall、`git diff --check` 通过。
- [Completed] Adapter/workflow tests: `248 passed in 2.64s`; Ruff, compileall, and `git diff --check` passed.
- [完成] Skill 侧现有 imports 继续工作，但实现解析到 `PhyAgentOS.forge.capability_runtime.understanding`；未新增 RoboTwin、SAPIEN、Torch、YOLO、Dora 或动作依赖。(local)
- [Completed] Existing Skill imports remain compatible while resolving to `PhyAgentOS.forge.capability_runtime.understanding`; no RoboTwin, SAPIEN, Torch, YOLO, Dora, or action dependency was added. (local)

### Git 提交 / Git Commit
- Commit: `5b7cdfc`
- Branch: `feature/long-horizon-workflow`
- 时间 / Time: 2026-09-02 22:45 (Asia/Shanghai)
## v2.8.8 (2026-09-02 23:10) - codex

### 变更摘要 / Change Summary
- [完成] [sense] [refactor] 将 `grasp.propose` 的 provider-neutral ToolSpec、候选/候选集校验、funnel 与 fail-closed Query 投影迁移到 PAOS generic capability runtime；Skill 侧仅保留兼容导出，模型依赖继续留在 provider/adapter。(local)
- [Completed] [sense] [refactor] Moved the provider-neutral `grasp.propose` ToolSpec, candidate/candidate-set validation, funnel accounting, and fail-closed Query projection into the PAOS generic capability runtime; kept only compatibility exports in the Skill package and left model dependencies in providers/adapters. (local)

### 预期影响文件 / Expected Files
- `PhyAgentOS/forge/capability_runtime/grasp_proposal.py` [新增 / new]
- `PhyAgentOS/forge/capability_runtime/__init__.py` [修改 / modified]
- `examples/forge-skills/pick-place-workflow/src/pick_place_workflow/grasp_proposal.py` [重构为兼容导出 / compatibility wrapper]
- `examples/forge-skills/pick-place-workflow/src/pick_place_workflow/manipulation_prepare.py` [修改导入 / import]
- `examples/forge-skills/pick-place-workflow/tests/test_grasp_propose.py` [回归验证 / regression]
- `CHANGELOG.md` [更新最近版本 / update recent versions]

### 文件变更详情 / File Change Details

#### [新增 / Added] `PhyAgentOS/forge/capability_runtime/grasp_proposal.py` L1-L586

**修改说明 / Description:** PAOS now owns the strict `grasp.propose` ToolSpec, argument/target validation,
candidate and candidate-set identity, score/confidence/provenance checks, funnel reconciliation, stale/empty/
unavailable/invalid states, provider exception fail-closed behavior, and provider-neutral Query projection.

#### [修改 / Modified] `examples/forge-skills/pick-place-workflow/src/pick_place_workflow/grasp_proposal.py` L1-L32

**修改前 / Before:** the Skill module contained the complete contract and endpoint implementation.

**修改后 / After:** the module re-exports the PAOS generic implementation, preserving existing import names;
`_validate_candidate` remains available only as a compatibility seam for the preparation module.

#### [修改 / Modified] `PhyAgentOS/forge/capability_runtime/__init__.py` L3-L57

**修改说明 / Description:** exported the generic grasp endpoint, snapshot, and ToolSpec alias from the PAOS
capability runtime package.

#### [修改 / Modified] `examples/forge-skills/pick-place-workflow/src/pick_place_workflow/manipulation_prepare.py` L9

**修改说明 / Description:** candidate validation now imports directly from the PAOS-owned generic runtime.

#### [新增 / Added] `examples/forge-skills/pick-place-workflow/tests/test_grasp_propose.py` L209-L213

**新增代码 / Added:** regression assertion that the Skill compatibility imports resolve to the PAOS generic
runtime module, preventing accidental reintroduction of Skill-owned public semantics.

### 验证 / Verification
- `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=examples/forge-skills/pick-place-workflow/src:examples/forge-adapters/robotwin20/src:PhyAgentOS /home/yanxu/miniconda3/envs/paos/bin/python -m pytest -p pytest_asyncio.plugin -q examples/forge-skills/pick-place-workflow/tests examples/forge-adapters/robotwin20/tests` → 249 passed。
- `/home/yanxu/miniconda3/envs/paos/bin/ruff check ...`、`python -m compileall -q ...`、`git diff --check` → passed。
- Generic runtime source contains no RoboTwin/SAPIEN/Torch/YOLO/Dora/Hephaestus imports; no provider/model was added in this change.
- `grasp.propose` remains Query-only and does not create Action/Session or motion authorization.

### 关键 Diff / Key Diff
```text
Skill grasp_proposal.py: 585-line implementation
                       -> 32-line compatibility export
PAOS capability_runtime: no grasp module
                       -> 586-line provider-neutral contract/endpoint implementation
```

### Git 提交 / Git Commit
- Commit: `cc8d324`
- Branch: feature/long-horizon-workflow
- 时间 / Time: 2026-09-02 (Asia/Shanghai)

## v2.8.9 (2026-09-02 23:20) - codex

### 变更摘要 / Change Summary
- [完成] [sense] [refactor] 将 `manipulation.prepare` 的 provider-neutral ToolSpec、候选绑定、readiness 检查、preparation identity 与只读 fail-closed 投影迁移到 PAOS generic capability runtime；Skill 侧保留兼容导出，继续禁止运动授权。(local)
- [Completed] [sense] [refactor] Moved the provider-neutral `manipulation.prepare` ToolSpec, candidate binding, readiness checks, preparation identity, and read-only fail-closed projection into the PAOS generic capability runtime; kept a compatibility export in the Skill package and continued to prohibit motion authorization. (local)

### 预期影响文件 / Expected Files
- `PhyAgentOS/forge/capability_runtime/manipulation_prepare.py` [新增 / new]
- `PhyAgentOS/forge/capability_runtime/__init__.py` [修改 / modified]
- `examples/forge-skills/pick-place-workflow/src/pick_place_workflow/manipulation_prepare.py` [重构为兼容导出 / compatibility wrapper]
- `examples/forge-skills/pick-place-workflow/tests/test_manipulation_prepare.py` [回归验证 / regression]
- `CHANGELOG.md` [更新最近版本 / update recent versions]

### 文件变更详情 / File Change Details

#### [新增 / Added] `PhyAgentOS/forge/capability_runtime/manipulation_prepare.py` L1-L463

**修改说明 / Description:** PAOS now owns the strict preparation ToolSpec, candidate/observation/frame/
calibration binding, preparation identity, kinematic/collision/workspace check validation, evidence projection,
stale/empty/unavailable/invalid states, provider failure projection, and the fixed `motion_authorized: false`
boundary.

#### [修改 / Modified] `examples/forge-skills/pick-place-workflow/src/pick_place_workflow/manipulation_prepare.py` L1-L29

**修改前 / Before:** the Skill module contained the full preparation contract and endpoint implementation.

**修改后 / After:** it re-exports the PAOS generic implementation while preserving existing Skill import names.

#### [修改 / Modified] `PhyAgentOS/forge/capability_runtime/__init__.py` L8-L65

**修改说明 / Description:** exported the PAOS-owned preparation ToolSpec, endpoint, and snapshot.

#### [新增 / Added] `examples/forge-skills/pick-place-workflow/tests/test_manipulation_prepare.py` L196-L202

**新增代码 / Added:** regression assertion that compatibility imports resolve to the PAOS generic runtime.

### 验证 / Verification
- `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=examples/forge-skills/pick-place-workflow/src:examples/forge-adapters/robotwin20/src:PhyAgentOS /home/yanxu/miniconda3/envs/paos/bin/python -m pytest -p pytest_asyncio.plugin -q examples/forge-skills/pick-place-workflow/tests examples/forge-adapters/robotwin20/tests` → 250 passed。
- `/home/yanxu/miniconda3/envs/paos/bin/ruff check ...`、`python -m compileall -q ...`、`git diff --check` → passed。
- Generic runtime has no RoboTwin/SAPIEN/Torch/YOLO/Dora/Hephaestus imports; no provider/model implementation was added.
- `manipulation.prepare` remains read-only and never creates Action/Session or motion authorization.

### 关键 Diff / Key Diff
```text
Skill manipulation_prepare.py: 463-line implementation
                            -> 29-line compatibility export
PAOS capability_runtime: no preparation module
                       -> 463-line provider-neutral read-only runtime
```

### Git 提交 / Git Commit
- Commit: `bc34823`
- Branch: feature/long-horizon-workflow
- 时间 / Time: 2026-09-02 (Asia/Shanghai)

## v2.8.10 (2026-09-02 23:35) - codex

### 变更摘要 / Change Summary
- [完成] [sense] [refactor] 消除 RoboTwin `scene.understand` adapter 中重复的 provider-neutral snapshot 定义，改为复用 PAOS generic runtime 的 `UnderstandingSnapshot`；adapter 仅保留 inference 输入/输出转换，不复制公共能力语义。(local)
- [Completed] [sense] [refactor] Removed the duplicate provider-neutral snapshot definition from the RoboTwin `scene.understand` adapter and reused the PAOS generic runtime `UnderstandingSnapshot`; the adapter now only translates inference input/output without copying public capability semantics. (local)

### 预期影响文件 / Expected Files
- `examples/forge-adapters/robotwin20/src/robotwin20_adapter/understanding.py` [修改 / modified]
- `examples/forge-adapters/robotwin20/tests/test_scene_understand_provider.py` [回归验证 / regression]
- `CHANGELOG.md` [更新最近版本 / update recent versions]

### 文件变更详情 / File Change Details

#### [修改 / Modified] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/understanding.py` L9-L31, L60-L84

**修改前 / Before:** the adapter declared a second `RoboTwinUnderstandingSnapshot` dataclass with the same
provider-neutral fields as PAOS.

**修改后 / After:** `RoboTwinUnderstandingSnapshot` is a compatibility alias of PAOS `UnderstandingSnapshot`,
and inference results instantiate the PAOS-owned type. The adapter still filters request/result fields and never
reads simulator truth.

#### [新增 / Added] `examples/forge-adapters/robotwin20/tests/test_scene_understand_provider.py` L100-L101

**新增代码 / Added:** regression assertion that the adapter compatibility name is identical to the PAOS
generic snapshot type.

### 验证 / Verification
- Adapter/workflow tests: `251 passed in 2.63s`。
- Ruff, compileall, and `git diff --check` passed。
- ForgeToolClient/Fake Gateway behavior remains unchanged; provider-specific fields still fail closed.

### Git 提交 / Git Commit
- Commit: `f341924`
- Branch: feature/long-horizon-workflow
- 时间 / Time: 2026-09-02 (Asia/Shanghai)

## v2.8.11 (2026-09-02 23:55) - codex

### 变更摘要 / Change Summary
- [完成] [sense] [feat] 在独立 RoboTwin adapter 中接入可配置的 GPT-5.6-sol Responses API inference provider；通过注入式 artifact resolver 与 client factory 将图像输入转换为 provider-neutral scene-understanding 结果，PAOS generic runtime 不增加模型依赖。(local)
- [Completed] [sense] [feat] Added a configurable GPT-5.6-sol Responses API inference provider in the independent RoboTwin adapter; injected artifact resolution and client factories convert image inputs to provider-neutral scene-understanding results without adding model dependencies to the PAOS generic runtime. (local)

### 预期影响文件 / Expected Files
- `examples/forge-adapters/robotwin20/src/robotwin20_adapter/openai_scene_understanding.py` [新增 / new]
- `examples/forge-adapters/robotwin20/src/robotwin20_adapter/__init__.py` [修改 / modified]
- `examples/forge-adapters/robotwin20/tests/test_openai_scene_understanding.py` [新增 / new]
- `PhyAgentOS/forge/capability_runtime/understanding.py` [修改 / modified]
- `examples/forge-adapters/robotwin20/src/robotwin20_adapter/understanding.py` [修改 / modified]
- `examples/forge-adapters/robotwin20/tests/test_scene_understand_provider.py` [修改 / modified]
- `examples/forge-adapters/robotwin20/README.md` [修改 / modified]
- `docs/forge/ROBOTWIN_ADAPTER_REFACTOR_DIAGNOSIS.md` [修改 / modified]
- `changelog/2026-09.md` [修改 / modified]
- `CHANGELOG.md` [更新最近版本 / update recent versions]

### 执行计划 / Execution Plan
1. 参考 Hephaestus 的 Responses API 结构化输出格式，但重新实现 adapter-side provider，不 import Hephaestus。
2. 通过 `RoboTwinSceneUnderstandingProvider` 进入 PAOS generic `SceneUnderstandingEndpoint`，拒绝 provider-specific 字段。
3. 使用 fake client 做 payload/解析回归；真实 API 仅在显式提供 `HEPHAESTUS_RELAY_API_KEY` 或等价 key 后运行。

### 追加审核计划 / Additional Review Plan
- [完成] [sense] [fix] 收紧 Responses JSON schema 与 provider 输出边界，确保模型只能返回 PAOS scene-understand 契约字段，并补充 malformed/unknown-field 回归验证。(local)
- [Completed] [sense] [fix] Tightened the Responses JSON schema and provider output boundary so the model can return only PAOS scene-understand contract fields, and added malformed/unknown-field regression coverage. (local)
- [完成] [docs] [fix] 更新 RoboTwin adapter 诊断中的当前状态，明确 GPT provider 已实现但真实调用仍需外部凭据，避免文档继续声称没有真实场景理解 provider。(local)
- [Completed] [docs] [fix] Updated the RoboTwin adapter diagnosis with the current status: the GPT provider is implemented, while live calls still require external credentials, so the document no longer claims that no real scene-understanding provider exists. (local)

### 文件变更详情 / File Change Details

#### [新增 / Added] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/openai_scene_understanding.py` L1-L321

**新增代码 / Added:** adapter-owned `OpenAIResponsesConfig`, `ArtifactPayload`, injected `ArtifactResolver`,
lazy OpenAI SDK factory, strict Responses JSON schema, data-URL image conversion, GPT-5.6-sol request payload,
structured response parsing, and fail-closed credential/artifact/provider errors. The module does not import
Hephaestus, RoboTwin, SAPIEN, Torch, or PAOS.

#### [修改 / Modified] `PhyAgentOS/forge/capability_runtime/understanding.py` L15-L286

**修改前 / Before:** the endpoint accepted only a PAOS `UnderstandingSnapshot` and validated provenance format.

**修改后 / After:** the endpoint accepts a provider-neutral Mapping across the Python 3.10/3.11 boundary,
normalizes it to the PAOS-owned snapshot, and binds every claim provenance reference to the request artifact list.

#### [修改 / Modified] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/understanding.py` L14-L86

**修改前 / Before:** adapter snapshot type imported PAOS runtime classes, breaking the independent Python 3.10
RoboTwin environment.

**修改后 / After:** the provider returns plain mappings and PAOS normalizes them only at the Gateway edge; the
adapter remains interpreter- and simulator-independent.

#### [修改 / Modified] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/__init__.py` L12-L42

**修改说明 / Description:** exported the adapter-owned GPT provider types and schema without importing the
optional OpenAI SDK at module import time.

#### [修改 / Modified] `examples/forge-adapters/robotwin20/pyproject.toml` L8-L9

**修改说明 / Description:** added the optional `openai` extra; the base adapter package remains dependency-free.

#### [新增 / Added] `examples/forge-adapters/robotwin20/tests/test_openai_scene_understanding.py` L1-L158

**新增代码 / Added:** fake Responses client tests for model/config, data URL, closed schema, provider composition,
missing credentials/artifacts, and provider-specific response rejection.

#### [修改 / Modified] `examples/forge-adapters/robotwin20/tests/test_scene_understand_provider.py` L85-L116

**新增代码 / Added:** regression that unbound provenance is rejected by the generic endpoint as `invalid_entity_claim`.

#### [修改 / Modified] `examples/forge-adapters/robotwin20/README.md` L100-L112

**修改说明 / Description:** documented optional adapter-only OpenAI installation, external credential handling,
and provenance binding through the same Gateway endpoint as the Fake path.

#### [修改 / Modified] `docs/forge/ROBOTWIN_ADAPTER_REFACTOR_DIAGNOSIS.md` L268-L298

**修改说明 / Description:** corrected current-status claims: the clean-room GPT provider is implemented, but a
live model result is not claimed without the externally supplied API key; Gateway/Dora and the remaining five
providers remain unfinished.

### 验证 / Verification
- `258 passed in 2.60s` for the adapter/workflow suites.
- Ruff, compileall, and `git diff --check` passed.
- RoboTwin20 Python `3.10.21` imported `robotwin20_adapter` successfully; PAOS imports no RoboTwin/SAPIEN/Torch/model dependency.
- A real desktop-tidy invocation failed closed before network transmission because `HEPHAESTUS_RELAY_API_KEY` was absent; no GPT result is claimed.
- `.codegraph/` and `.cursor/` remain untracked and are excluded from the commit.

### Git 提交 / Git Commit
- Commit: `b9d8e6f`
- Branch: `feature/long-horizon-workflow`
- 时间 / Time: 2026-09-02 (Asia/Shanghai)

## v2.8.12 (2026-09-03 00:00) - codex

### 变更摘要 / Change Summary
- [完成] [sense] [feat] 为独立 RoboTwin adapter 增加安全的文件系统 artifact resolver，使外部 runtime 产生的 `artifact://scene/capture/rgb` 可被 GPT scene-understanding provider 实际读取；PAOS 仍不接触路径、资产或模型依赖。(local)
- [Completed] [sense] [feat] Added a safe filesystem artifact resolver to the independent RoboTwin adapter so `artifact://scene/capture/rgb` emitted by the external runtime can be consumed by the GPT scene-understanding provider; PAOS still sees no paths, assets, or model dependency. (local)

### 预期影响文件 / Expected Files
- `examples/forge-adapters/robotwin20/src/robotwin20_adapter/openai_scene_understanding.py` [修改 / modified]
- `examples/forge-adapters/robotwin20/src/robotwin20_adapter/__init__.py` [修改 / modified]
- `examples/forge-adapters/robotwin20/tests/test_openai_scene_understanding.py` [修改 / modified]
- `examples/forge-adapters/robotwin20/README.md` [修改 / modified]
- `CHANGELOG.md` [更新 / update]

### 文件变更详情 / File Change Details

#### [修改 / Modified] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/openai_scene_understanding.py` L11-L74, L342-L361

**新增代码 / Added:** `FilesystemArtifactResolver` validates an absolute external root, parses only
`artifact://` references, rejects traversal/non-image leaves, resolves RoboTwin RGB capture files, and returns
bytes as `ArtifactPayload`; no PAOS or simulator path crosses the boundary.

#### [修改 / Modified] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/__init__.py` L12-L43

**修改说明 / Description:** exported the filesystem resolver as an adapter-side integration seam.

#### [修改 / Modified] `examples/forge-adapters/robotwin20/tests/test_openai_scene_understanding.py` L1-L195

**新增代码 / Added:** verified the complete `ForgeToolClient -> Fake Gateway -> generic endpoint -> RoboTwin
provider -> GPT client` route and resolver path/traversal/non-image rejection.

#### [修改 / Modified] `examples/forge-adapters/robotwin20/README.md` L100-L111

**修改说明 / Description:** documented the concrete external artifact-root resolver construction.

### 验证 / Verification
- `260 passed in 2.62s` for the adapter/workflow suites.
- Ruff, compileall, and `git diff --check` passed.
- RoboTwin20 Python `3.10.21` imported the adapter and resolver successfully.
- No live API call was attempted because `HEPHAESTUS_RELAY_API_KEY` remains absent; provider remains fail-closed.
- `.codegraph/` and `.cursor/` remain untracked and are excluded from the commit.

### Git 提交 / Git Commit
- Commit: `83bfe1b`
- Branch: `feature/long-horizon-workflow`
- 时间 / Time: 2026-09-03 (Asia/Shanghai)

## v2.8.17 (2026-09-03 15:49) - codex

### 变更摘要 / Change Summary
- [sense] [feat] 扩展 provider-neutral `grasp.propose` target 以携带 observation/revision/frame/calibration 绑定的 geometry artifacts；generic runtime 接受 adapter 返回的 plain mapping，并校验单位四元数与 approach vector。 (local)
- [Sense] [Feat] Extend provider-neutral `grasp.propose` targets with observation/revision/frame/calibration-bound geometry artifacts; generic runtime accepts adapter plain mappings and validates unit quaternions and approach vectors. (local)
- [sense] [feat] 新增独立 adapter-side GraspGen-compatible JSONL worker/client、profile、点云解析、矩阵 canonicalization、确定性 SE(3) NMS、provenance/funnel 投影和 cleanup fail-closed；不执行 IK、碰撞准入或动作。 (local)
- [Sense] [Feat] Add an isolated adapter-side GraspGen-compatible JSONL worker/client, profile, point-cloud resolution, matrix canonicalization, deterministic SE(3) NMS, provenance/funnel projection, and fail-closed cleanup; no IK, collision admission, or action is performed. (local)

### 影响文件 / Affected Files
- `PhyAgentOS/forge/capability_runtime/grasp_proposal.py` [修改 / modified]
- `PhyAgentOS/forge/capability_runtime/__init__.py` [修改 / modified]
- `examples/forge-skills/pick-place-workflow/contracts/grasp.propose.tool.yaml` [修改 / modified]
- `examples/forge-adapters/robotwin20/src/robotwin20_adapter/grasp_proposal.py`、`grasp_profile.py`、`single_view_perception.py`、`__init__.py` [新增/修改 / added/modified]
- `examples/forge-adapters/robotwin20/runtime/graspgen_worker.py`、`worker_protocol.py`、`profiles/robotwin20/graspgen.yaml` [新增/修改 / added/modified]
- `examples/forge-adapters/robotwin20/tests/test_grasp_proposal.py`、`test_grasp_profile.py` [新增 / added]
- `examples/forge-adapters/robotwin20/README.md`、`docs/forge/FEATURE_REFERENCE_CARDS.md`、`docs/forge/ROBOTWIN_ADAPTER_REFACTOR_DIAGNOSIS.md` [修改 / modified]

### 文件变更详情 / File Change Details
#### [修改 / Modified] `PhyAgentOS/forge/capability_runtime/grasp_proposal.py` L34-L678
**修改前 / Before:** target 只接受 `spatial_envelope`，provider 必须返回 PAOS dataclass，候选 pose 只做有限数值检查。
**修改后 / After:** target 可携带严格绑定的 `geometry_artifacts`；provider mapping 在 Gateway 边界归一化；四元数和 approach vector 必须为有限单位向量；所有非法结果 fail-closed。

#### [新增 / Added] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/grasp_proposal.py` L1-L383
**新增代码 / Added:** adapter 解析 object/fused point-cloud artifact，发送 versioned worker request，校验 4x4 matrix/funnel，生成中性 candidate、provenance 和确定性 NMS 结果，并在每次 Query 后释放 worker。

#### [新增 / Added] `examples/forge-adapters/robotwin20/runtime/graspgen_worker.py` L1-L129; `grasp_profile.py` L1-L74; `profiles/robotwin20/graspgen.yaml` L1-L29
**新增代码 / Added:** 独立解释器 worker 和 profile 注入 checkpoint/source/python/timeout；缺失模型、错误协议和空候选只返回 unavailable/empty，不伪造成功。

#### [修改 / Modified] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/single_view_perception.py` L250-L276; `runtime/worker_protocol.py` L12-L72
**修改后 / After:** 增加受 artifact-root 约束的派生点云加载校验，并允许不同隔离 worker 使用显式 schema version；既有 perception worker 默认协议保持不变。

#### [新增 / Added] `examples/forge-adapters/robotwin20/tests/test_grasp_proposal.py` L1-L115; `test_grasp_profile.py` L1-L70
**新增代码 / Added:** 点云绑定、NMS、worker cleanup、错误绑定和 profile 环境变量测试。

### 验证 / Verification
- PAOS generic grasp tests: `57 passed`。
- Isolated adapter grasp/profile tests: `7 passed`。
- Ruff、compileall、`git diff --check` 通过 / passed。
- 未找到可验证的本地 GraspGen checkpoint/source environment，未声称 live inference；worker 会在外部 profile 未提供模型时 fail-closed。
- `.codegraph/`、`.cursor/` 保持未跟踪且未加入提交 / remain untracked and unstaged。

### Git 提交 / Git Commit
- Commit: `f6417d2`
- Branch: `feature/long-horizon-workflow`
- 时间 / Time: 2026-09-03 (Asia/Shanghai)

## v2.8.13 (2026-09-03 11:10) - codex

### 变更摘要 / Change Summary
- [sense] [fix] 修正 GPT Responses strict JSON schema 中 `spatial_envelopes.unit` 缺失 `type` 字段的问题，并增加递归 strict-schema 回归校验；同步记录识别、分割、定位与抓取位姿估计在 PAOS provider-neutral 能力边界中的模块归属。 (local)
- [sense] [fix] Fix the missing `type` keyword on `spatial_envelopes.unit` in the GPT Responses strict JSON schema and add recursive strict-schema regression checks; document the PAOS provider-neutral module ownership for recognition, segmentation, localization, and grasp-pose estimation. (local)

### 预期影响文件 / Expected Files
- `examples/forge-adapters/robotwin20/src/robotwin20_adapter/openai_scene_understanding.py` [修改 / modified]
- `examples/forge-adapters/robotwin20/tests/test_openai_scene_understanding.py` [修改 / modified]
- `examples/forge-adapters/robotwin20/README.md` [修改 / modified]
- `docs/forge/ROBOTWIN_ADAPTER_REFACTOR_DIAGNOSIS.md` [修改 / modified]
- `CHANGELOG.md` [更新 / update]

### 验证 / Verification
- `261 passed` for the adapter/workflow suites.
- Ruff, compileall, and `git diff --check` passed.
- Real GPT path reached `ForgeToolClient -> Fake Gateway -> generic endpoint -> RoboTwin provider -> Responses`; the latest RGB-only run returned `status=available`, 3 entities, 1 relation, 0 spatial envelopes, and 2 ambiguity entries without fabricating metric geometry.
- `.codegraph/` and `.cursor/` remain untracked and are excluded from the commit.

### 文件变更详情 / File Change Details

#### [修改 / Modified] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/openai_scene_understanding.py` L161, L192-L223, L257

**修改前 / Before:** `unit` used only `{const: "m"}` and fake clients did not exercise Responses schema validation.

**修改后 / After:** `unit` is `{type: "string", const: "m"}`; `_validate_strict_schema` recursively checks typed nodes, closed objects, complete `required` lists, and array items before constructing a provider.

#### [修改 / Modified] `examples/forge-adapters/robotwin20/tests/test_openai_scene_understanding.py` L198-L223

**新增 / Added:** recursive strict-schema conformance regression and exact `unit` schema assertion.

#### [修改 / Modified] `examples/forge-adapters/robotwin20/README.md` L97, L113-L128

**修改说明 / Description:** retained the user-selected `CUSTOM_API_KEY` and documented the module ownership of recognition, segmentation, localization, grasp pose, readiness, and execution.

#### [修改 / Modified] `docs/forge/ROBOTWIN_ADAPTER_REFACTOR_DIAGNOSIS.md` L270-L326

**修改说明 / Description:** corrected live-provider status and added the PAOS use-case mapping for perception and grasp-pose capabilities.

### Git 提交 / Git Commit
- Commit: `530f5b3`
- Branch: `feature/long-horizon-workflow`
- 时间 / Time: 2026-09-03 11:15 (Asia/Shanghai)

## v2.8.14 (2026-09-03 11:25) - codex

### 变更摘要 / Change Summary
- [docs] [docs] 整理单视角 LocateAnything→SAM2→RGB-D 定位方案与多视角 ObservationSet→几何融合方案，明确它们共享 PAOS ToolSpec、分别由 provider/profile 实现，不新增 RoboTwin Skill 或直接暴露模型工具；多视角同时覆盖跨视角分割、实体身份确认和融合几何，Global SceneGeometry 保持独立可选输出。 (local)
- [Docs] [Docs] Consolidated the single-view LocateAnything→SAM2→RGB-D localization path and the multi-view ObservationSet→geometry-fusion path, clarifying shared PAOS ToolSpecs, provider/profile ownership, no RoboTwin Skill or direct model tool exposure; multi-view now explicitly covers cross-view segmentation, entity identity confirmation, and fused geometry, while Global SceneGeometry remains a separate optional output. (local)

### 影响文件 / Affected Files
- `docs/forge/ROBOTWIN_ADAPTER_REFACTOR_DIAGNOSIS.md` L228-L383、L470 [修改 / modified]

### 文件变更详情 / File Change Details

#### [修改 / Modified] `docs/forge/ROBOTWIN_ADAPTER_REFACTOR_DIAGNOSIS.md` L228-L383、L470

**修改前 / Before:** 多视角方案主要描述为全局 SceneGeometry 融合，未充分说明其可用于跨视角分割和实体几何生成。

**修改后 / After:** 方案 B 明确执行 per-view proposal/mask、cross-view identity/mask correspondence、融合实体 mask/RGB-D 几何，并规划由 `FusedEntityPerceptionArtifact` 供 `scene.understand`/`grasp.propose` 消费；同时记录当前 generic Tool 尚无 derived-artifact、融合几何或全局场景几何输入字段，必须先扩展 contract，不能私自增加请求字段。Global SceneGeometry 仅作为独立可选 provider 输出。新增按 PAOS 架构、开发者指南、ToolSpec 缺口和安全门禁的审核结论。

**English:** Plan B now specifies per-view proposal/mask, cross-view identity/mask correspondence, and fused entity RGB-D geometry, while recording that current generic Tools lack derived-artifact, fused-geometry, and global-scene inputs. The contract must be extended before these artifacts become public Tool inputs; Global SceneGeometry remains an independent optional provider output.

### 验证 / Verification
- `git diff --check` passed.
- 文档审核确认没有新增 `multiview` Skill 或 RoboTwin 专属 ToolSpec；主动移动视角仍需单独 Action/Session safety admission。
- `.codegraph/` and `.cursor/` remain untracked and are excluded from the commit.

### Git 提交 / Git Commit
- Commit: `027bb51`
- Branch: `feature/long-horizon-workflow`
- 时间 / Time: 2026-09-03 (Asia/Shanghai)

## v2.8.15 (2026-09-03 13:08) - codex

### 变更摘要 / Change Summary
- [sense] [feat] 扩展 provider-neutral `scene.understand` contract，增加可审计的派生感知资产，首批支持实例 mask、目标点云和度量定位结果；所有资产绑定 observation、scene revision、entity、frame、calibration、source artifacts 和 provenance，且不授权运动。 (local)
- [Sense] [Feat] Extended the provider-neutral `scene.understand` contract with auditable derived perception artifacts for instance masks, object point clouds, and metric localization; every artifact binds observation, scene revision, entity, frame, calibration, source artifacts, and provenance, and never authorizes motion. (local)

### 影响文件 / Affected Files
- `PhyAgentOS/forge/capability_runtime/understanding.py` [修改 / modified]
- `examples/forge-skills/pick-place-workflow/contracts/scene.understand.tool.yaml` [修改 / modified]
- `examples/forge-skills/pick-place-workflow/tests/test_scene_understand.py` [修改 / modified]
- `examples/forge-adapters/robotwin20/src/robotwin20_adapter/understanding.py` [修改 / modified]
- `examples/forge-adapters/robotwin20/tests/test_scene_understand_provider.py` [修改 / modified]
- `examples/forge-skills/pick-place-workflow/SKILL.md`、`CHANGELOG.md` [修改 / modified]
- `examples/forge-skills/pick-place-workflow/skill.yaml`、`pyproject.toml` 与版本断言测试 [修改 / modified]
- `docs/forge/ROBOTWIN_ADAPTER_REFACTOR_DIAGNOSIS.md` [修改 / modified]

### 验证 / Verification
- Fake Gateway 覆盖合法派生资产及未知类型、未绑定 source/provenance、实体/frame/calibration 不一致、退化 bbox 和 motion 字段等 fail-closed 路径。
- workflow/adapter pytest：`268 passed`；Ruff、compileall、ToolSpec YAML equality、`git diff --check` 均通过。
- Bundle：`pick-place-workflow-0.8.0.tar.gz`，SHA-256 `46a009563442c658cbbec92b63dee47c1bb84fa6318b50f94ca15bb5a31eeb8d`，63,927 bytes。
- 保持 `.codegraph/`、`.cursor/` 未跟踪且不进入提交。

### 文件变更详情 / File Change Details

#### [修改 / Modified] `PhyAgentOS/forge/capability_runtime/understanding.py` L29-L601

**修改前 / Before:** `UnderstandingSnapshot` 只包含实体、关系、空间包络和 ambiguity，derived artifacts 未进入公共 schema。

**修改后 / After:** 增加 `derived_artifacts` 类型、严格 schema、descriptor 校验、派生 lineage/root provenance 校验，并在 Query 投影中返回该字段；不增加任何 Action 或 motion authorization。

#### [修改 / Modified] `examples/forge-skills/pick-place-workflow/contracts/scene.understand.tool.yaml` L25-L133

**修改前 / Before:** output schema 没有 `derived_artifacts`。

**修改后 / After:** 与 generic ToolSpec 同步，发布 instance mask、object point cloud、metric localization 三类派生资产及其绑定字段。

#### [修改 / Modified] `examples/forge-skills/pick-place-workflow/tests/test_scene_understand.py` L1-L245

**修改前 / Before:** 只覆盖实体/关系/空间包络的 Query 校验。

**修改后 / After:** 增加合法派生资产、YAML binding、descriptor/lineage/binding/kind fail-closed 测试。

#### [修改 / Modified] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/understanding.py` L45-L88

**修改前 / Before:** adapter 结果白名单不包含 derived artifacts。

**修改后 / After:** adapter 仅转发 plain mappings 中的 `derived_artifacts`，仍不导入 PAOS、Torch、模型或仿真类型。

#### [修改 / Modified] `examples/forge-adapters/robotwin20/tests/test_scene_understand_provider.py` L119-L153

**新增 / Added:** 验证 adapter 可传递中性 derived artifact 且经过 Fake Gateway generic validation。

#### [修改 / Modified] `docs/forge/ROBOTWIN_ADAPTER_REFACTOR_DIAGNOSIS.md` L360-L376, L424-L452

**修改说明 / Description:** 更新 contract 缺口、当前已完成的 derived-artifacts 能力和后续多视角/抓取 provider 边界。

#### [修改 / Modified] Bundle docs/version and related assertions

**修改说明 / Description:** Bundle/package 升级至 `0.8.0`，同步 Skill/README、版本断言和 adapter boundary 说明。

### Git 提交 / Git Commit
- Commit: `436b722`
- Branch: `feature/long-horizon-workflow`
- 时间 / Time: 2026-09-03 13:38 (Asia/Shanghai)

## v2.8.16 (2026-09-03 13:46) - codex

### 预期修改 / Planned Changes
- [完成] [sense] [feat] 在独立 RoboTwin adapter 中 clean-room 实现单视角感知 composition：语义实体绑定后依次调用 proposal provider、segmentation provider 和 deterministic RGB-D localization provider，物化 instance mask、object point cloud 与 metric localization 派生资产，并经现有 `scene.understand` Gateway contract 投影。 (local)
- [Completed] [Sense] [Feat] Clean-room implement the single-view perception composition in the independent RoboTwin adapter: after semantic entity binding, invoke proposal, segmentation, and deterministic RGB-D localization providers, materialize instance-mask, object-point-cloud, and metric-localization artifacts, and project them through the existing `scene.understand` Gateway contract. (local)

### 预期影响文件 / Expected Files
- `PhyAgentOS/forge/capability_runtime/understanding.py` [修改 / modified]
- `examples/forge-skills/pick-place-workflow/tests/test_scene_understand.py` [修改 / modified]
- `examples/forge-adapters/robotwin20/src/robotwin20_adapter/single_view_perception.py` [新增 / added]
- `examples/forge-adapters/robotwin20/src/robotwin20_adapter/process_worker.py` [新增 / added]
- `examples/forge-adapters/robotwin20/src/robotwin20_adapter/perception_profile.py` [新增 / added]
- `examples/forge-adapters/robotwin20/runtime/locateanything_worker.py` [新增 / added]
- `examples/forge-adapters/robotwin20/runtime/sam2_worker.py` [新增 / added]
- `examples/forge-adapters/robotwin20/runtime/worker_protocol.py` [新增 / added]
- `examples/forge-adapters/robotwin20/profiles/robotwin20/perception.yaml` [新增 / added]
- `examples/forge-adapters/robotwin20/src/robotwin20_adapter/__init__.py` [修改 / modified]
- `examples/forge-adapters/robotwin20/pyproject.toml`、`README.md` [修改 / modified]
- `examples/forge-adapters/robotwin20/tests/test_single_view_perception.py` [新增 / added]
- `examples/forge-adapters/robotwin20/tests/test_process_worker.py`、`tests/fixtures/jsonl_worker.py` [新增 / added]
- `examples/forge-adapters/robotwin20/tests/test_perception_profile.py` [新增 / added]
- `examples/forge-adapters/robotwin20/tests/test_perception_workers.py` [新增 / added]
- `docs/forge/ROBOTWIN_ADAPTER_REFACTOR_DIAGNOSIS.md` [修改 / modified]

### 预期验证 / Planned Validation
- 覆盖唯一 proposal 成功链路、多个 proposal ambiguity、空 mask、RGB/depth shape mismatch、非法内外参、无有限正深度、artifact 越界、worker/cleanup failure 和 no-motion Gateway 投影。
- 运行 workflow/adapter pytest、Ruff、compileall、`git diff --check`；真实模型未实际运行时不得声称 LocateAnything/SAM2 已完成 live inference。
- 保持 PAOS wheel 无 NumPy/Torch/SAM2/LocateAnything 依赖，`.codegraph/`、`.cursor/` 不进入提交。

### 实际结果 / Actual Results
- [完成] [sense] [feat] 新增 adapter-owned JSONL process worker client、LocateAnything proposal worker、SAM2 box worker、profile 加载与事务式派生 artifact rollback；模型环境仍分别是 `hephaestus-locateanything` 和 `seg`，不混入 PAOS/RoboTwin20。 (local)
- [Completed] [Sense] [Feat] Added the adapter-owned JSONL process client, LocateAnything proposal worker, SAM2 box worker, profile loader, and transactional derived-artifact rollback; the model environments remain `hephaestus-locateanything` and `seg`, isolated from PAOS/RoboTwin20. (local)

### 文件变更详情 / File Change Details

#### [新增 / Added] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/process_worker.py` L1-L212

**新增代码 / Added:**
```python
class JsonlProcessWorkerClient:
    def request(self, payload): ...
    def release(self): ...
```
**说明 / Description:** 使用绝对解释器路径、无 shell 启动、startup/request/shutdown timeout、request identity、JSONL 大小和 stderr tail 门禁。

#### [新增 / Added] `examples/forge-adapters/robotwin20/runtime/locateanything_worker.py` L1-L242; `sam2_worker.py` L1-L186; `worker_protocol.py` L1-L60

**新增代码 / Added:**
```text
LocateAnything: RGB + semantic query -> normalized model box -> pixel proposal
SAM2: exact RGB + proposal bbox -> same-size instance mask artifact
```
**说明 / Description:** 两个入口都是 adapter clean-room worker；不导入 Hephaestus 类，不读取 simulator truth，不向 PAOS 传递本地模型路径。LocateAnything 没有可信 score 时返回 `confidence: null`。

#### [新增 / Added] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/single_view_perception.py` L93-L707

**修改后 / After:** proposal 完成后先 release，再进入 SAM2；对 mask/depth/calibration 做 shape、frame、内外参和正深度校验，并在后续写入失败时回滚 mask/points/localization 文件。

#### [新增 / Added] `examples/forge-adapters/robotwin20/profiles/robotwin20/perception.yaml` L1-L56

**说明 / Description:** 由 profile 注入两个解释器、模型 revision、checkpoint、CUDA device、cache、artifact root 和 timeout；PAOS 不保存这些设置。

#### [修改 / Modified] `PhyAgentOS/forge/capability_runtime/understanding.py` L461-L601; `examples/forge-skills/pick-place-workflow/tests/test_scene_understand.py` L238-L262

**修改前 / Before:** derived artifact 可以仅凭 snapshot 内部字段选择 binding。

**修改后 / After:** `SceneUnderstandingEndpoint.invoke` 将请求的 observation/revision/frame/calibration 传入 validator，并拒绝与当前 request 不一致的 artifact。

### 验证 / Verification
- PAOS/workflow/adapter suite：`281 passed, 2 skipped`。
- 含 NumPy/Pillow 的 adapter numerical/worker suite：`27 passed`。
- 真实无动作 composition：LocateAnything 返回 `[133, 60, 163, 96]`；SAM2 mask `240x320`，788 个前景像素；point cloud `788x3 float32`；Gateway `available`、`motion_authorized=false`，无 worker 残留。
- Ruff、compileall、`git diff --check` 通过。使用系统 Python 跑全套测试未采纳：该解释器缺少 PAOS `loguru` 和 asyncio 测试依赖。
- 根 `CHANGELOG.md` 滚动窗口更新为最新 v2.8.16–v2.8.12；v2.8.11 完整记录保留在本月归档。
- Root `CHANGELOG.md` rolling window now contains v2.8.16–v2.8.12; the complete v2.8.11 record remains in the monthly archive.

### Git 提交 / Git Commit
- Commit: `3d4dba3a3f1f8660ba667a7c893416a2a3862580`
- Branch: `feature/long-horizon-workflow`
- 时间 / Time: 2026-09-03 (Asia/Shanghai)

## v5.2.2 (2026-09-05  - codex)

### 变更摘要 / Change summary
- [policy] [refactor] [计划] 将完整路线 readiness 五项检查接入 object.acquire/place admission，同时保留旧 replay conformance 的三项检查兼容。
- [policy] [refactor] [planned] Require five complete-route readiness scopes for object.acquire/place admission while preserving legacy replay conformance compatibility.

### 预期影响文件 / Expected files
- `examples/forge-adapters/robotwin20/src/robotwin20_adapter/action_readiness.py`
- `examples/forge-adapters/robotwin20/src/robotwin20_adapter/route_readiness.py`
- `examples/forge-skills/pick-place-workflow/src/pick_place_workflow/object_acquire.py`

### 完成记录 / Completion
- [policy] [refactor] [完成] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/action_readiness.py:L27-L34,L98-L117,L199-L218,L277-L302` 新增五项 complete-route checks、可选严格门禁参数及 `build_complete_route_readiness_gate`；默认旧 gate 仍只接受三项 replay checks，严格 gate 对缺失/混合 schema fail-closed。
- [policy] [refactor] [done] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/__init__.py:L3-L10,L164-L169` 导出 complete-route gate 构造器。

**关键 Diff / Key diff:**
```text
旧 / Before: checks 必须包含 kinematic, collision, workspace
新 / After: require_complete_route=True 时必须恰好包含 attached_object_collision,
complete_transport_descent_retreat, contact_dynamics, workspace_and_joint_limits, stop_control；
否则拒绝 admission。
```

### 验证 / Verification
- `PYTHONPATH=examples/forge-adapters/robotwin20/src:examples/forge-skills/pick-place-workflow/src PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python -m pytest -p pytest_asyncio.plugin -q examples/forge-adapters/robotwin20/tests/test_action_readiness_gate.py` -> `14 passed`。
- Route/readiness focused suite -> `25 passed`。

### Git 提交 / Git Commit

## v5.2.3 (2026-09-05  - codex)

### 变更摘要 / Change summary
- [chore] [exp] [计划] 按 PAOS 分层原则准备独立 RoboTwin 仿真环境与 readiness worker 环境；不向 PAOS 核心环境写入仿真/规划依赖。
- [chore] [exp] [planned] Prepare isolated RoboTwin simulation and readiness-worker environments per PAOS layering; do not install simulator/planner dependencies into the PAOS core environment.

### 预期操作 / Planned operations
- 检查 RoboTwin requirements、现有解释器和虚拟环境；创建隔离环境（如缺失）。
- Inspect RoboTwin requirements and interpreters; create isolated environments when absent.

### 环境验证 / Environment verification
- [chore] [exp] [完成] 发现并验证既有隔离环境 `/home/yanxu/miniconda3/envs/RoboTwin20`（Python 3.10.21），可导入 `numpy 1.26.4`、`sapien 3.0.0b1`、`mplib 0.2.1`；PAOS 核心环境未修改。
- [chore] [exp] [done] Verified existing isolated `/home/yanxu/miniconda3/envs/RoboTwin20` (Python 3.10.21) with `numpy 1.26.4`, `sapien 3.0.0b1`, and `mplib 0.2.1`; PAOS core environment was unchanged.

**结果 / Result:** RoboTwin/Franka readiness 应使用该解释器；临时 Python 3.12 venv 因 `scipy==1.10.1` 不兼容而未作为运行环境。

### Git 提交 / Git Commit

### 运行边界发现 / Runtime boundary finding
- [chore] [fix] [完成] 使用 RoboTwin20 Python 3.10 验证真实 route request 时发现 adapter 当前导入 PAOS `datetime.UTC`（要求 Python 3.11+），导致 worker 无法直接加载 PAOS 模块；未绕过该边界或启动动作。
- [chore] [fix] [done] Running the real route request with RoboTwin20 Python 3.10 exposed that the adapter imports PAOS `datetime.UTC` (Python 3.11+), so the worker cannot directly load PAOS modules; no boundary bypass or motion was started.

**结论 / Conclusion:** 需要为 worker 提供纯协议/序列化依赖或使用兼容 Python 3.12 的仿真环境；当前 RoboTwin20 环境虽具备仿真库，但不能直接运行 PAOS-coupled worker。

### Git 提交 / Git Commit

## v5.2.5 (2026-09-05  - codex)

### 变更摘要 / Change summary
- [chore] [fix] [计划] 将 PAOS 的 UTC 时间实现改为 Python 3.10 兼容的 `timezone.utc`，解除 RoboTwin worker 加载协议模块的版本阻塞。
- [chore] [fix] [planned] Replace PAOS UTC usage with Python 3.10-compatible `timezone.utc` to unblock RoboTwin worker protocol imports.

### 完成记录 / Completion
- [chore] [fix] [完成] `PhyAgentOS/state_io/protocol.py:L1,L168`; `PhyAgentOS/skill_runtime/state.py:L1,L18`; `PhyAgentOS/skill_runtime/installer.py:L1,L63` 使用 `timezone.utc` 替代 Python 3.11 专有 `datetime.UTC`。
- [chore] [fix] [done] Replaced Python 3.11-only `datetime.UTC` with `timezone.utc` in the three PAOS modules.

### 验证 / Verification
- RoboTwin20 Python 3.10 成功导入 `robotwin20_adapter.route_readiness`。
- PAOS state/skill_runtime `compileall` 通过。

### Git 提交 / Git Commit

## v5.2.6 (2026-09-05  - codex)

### 变更摘要 / Change summary
- [chore] [fix] [计划] 为 PAOS manipulation 模块增加 Python 3.10 的 `StrEnum` 兼容 fallback，保持现有字符串枚举协议不变。
- [chore] [fix] [planned] Add a Python 3.10 `StrEnum` compatibility fallback in PAOS manipulation while preserving string enum protocol values.

### 完成记录 / Completion
- [chore] [fix] [完成] `PhyAgentOS/forge/manipulation.py:L12-L22` 增加 Python 3.10 `StrEnum` fallback。
- [chore] [fix] [done] Added Python 3.10 `StrEnum` fallback in `PhyAgentOS/forge/manipulation.py`.

### 验证 / Verification
- RoboTwin20 Python 3.10 成功导入 `robotwin20_adapter.route_readiness`。

### Git 提交 / Git Commit

## v5.2.7 (2026-09-05  - codex)

### 变更摘要 / Change summary
- [chore] [refactor] [完成] 将 `arm_candidates` 改为 adapter package 惰性导出，解除 Python 3.10 worker 对 Python 3.11+ PAOS Core 的隐式导入；撤销 v5.2.5/v5.2.6 对核心运行时的兼容修改。
- [chore] [refactor] [done] Lazily export `arm_candidates` so Python 3.10 workers do not implicitly import Python 3.11+ PAOS Core; reverted the v5.2.5/v5.2.6 core-runtime compatibility changes.

### 文件变更详情 / File changes
- [修改 / Modified] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/__init__.py:L3-L4,L141-L159` 删除 eager `arm_candidates` 导入，增加 `__getattr__` 惰性解析；公共 `__all__` 不变。
- [恢复 / Restored] `PhyAgentOS/state_io/protocol.py:L13,L231`; `PhyAgentOS/skill_runtime/state.py:L9,L43`; `PhyAgentOS/skill_runtime/installer.py:L11,L121`; `PhyAgentOS/forge/manipulation.py:L13` 恢复 PAOS 声明的 Python 3.11+ 实现。

**关键 Diff / Key diff:**
```text
修改前 / Before: import robotwin20_adapter.route_readiness -> package __init__ eagerly imports arm_candidates -> imports PAOS Core
修改后 / After:  route worker imports route_readiness directly; arm_candidates loads only when its public symbols are requested
```

### 验证 / Verification
- RoboTwin20 Python 3.10：`from robotwin20_adapter.route_readiness import validate_route_request` 通过。
- PAOS Python 3.12：`from robotwin20_adapter import CompleteRouteSelector` 通过。
- arm candidates、route readiness、simulation probe 专项：`48 passed`。
- `git diff --check` 通过。

### Git 提交 / Git Commit

### 本轮完成记录 / This turn completion
- [adapter] [feat] [完成] 新增 `grasp_adaptation.py:L1-L285`，校验 calibration digest 和 `world_to_camera_cv` 语义，求逆外参并应用 provider-to-TCP transform；新增 `test_grasp_adaptation.py:L1-L112`。
- [adapter] [feat] [done] Added calibration-bound grasp adaptation with inverse extrinsic and provider-to-TCP transform, plus focused numeric and fail-closed tests.
- [adapter] [fix] [完成] `robotwin_readiness_worker.py:L23-L165` 复用同一 camera-to-world 转换；修正后真实 71 候选 preliminary probe 结果为 42 个通过、29 个 planner fail，`motion_authorized=false`。
- [adapter] [fix] [done] Reused the same camera-to-world conversion in the worker; corrected live preliminary probe yielded 42/71 candidates with no motion authorization.
- [docs] [docs] [完成] 更新 `docs/forge/PAOS_STATE_FILE_ARCHITECTURE_DIAGNOSIS.md` 与 `docs/forge/STATE_FILE_IMPLEMENTATION_REVIEW_20260903.md`，记录旧 evidence 失效原因、运行证据和五维复审。
- [docs] [docs] [done] Updated diagnosis and five-dimension review with the invalidated legacy evidence, runtime evidence, and boundary review.

### Git 提交 / Git Commit

## v5.3.1 (2026-09-05 14:50) - codex

### 变更摘要 / Change summary
- [policy] [fix] [计划] 修正 preliminary readiness worker：仅有 IK/workspace 结果时不得投影为 `prepared_candidates` 或 `collision=pass`；新增明确的 `preliminary_candidates`，collision 保持 unavailable。
- [policy] [fix] [planned] Fix preliminary readiness worker so IK/workspace-only results cannot be projected as `prepared_candidates` or `collision=pass`; expose explicit `preliminary_candidates` with collision unavailable.

### 完成记录 / Completion
- [policy] [fix] [完成] `examples/forge-adapters/robotwin20/runtime/robotwin_readiness_worker.py:L190-L260` 将 preliminary IK/workspace 结果从 `prepared_candidates` 移至 `preliminary_candidates`，collision 固定为 `unavailable`。
- [policy] [fix] [done] Moved preliminary IK/workspace results from `prepared_candidates` to `preliminary_candidates` and fixed collision status to `unavailable`.
- [docs] [docs] [完成] 更新诊断与五维 review，明确 42/71 仅为 preliminary，不可触发旧 readiness gate。
- [docs] [docs] [done] Updated diagnosis and five-dimension review to state that 42/71 are preliminary only and cannot trigger the legacy readiness gate.

### 验证 / Verification
- Grasp adaptation/route/simulation probe focused suite: `45 passed`。
- Ruff、compileall、git diff --check 通过。

### Git 提交 / Git Commit

### Git 提交 / Git Commit
- Commit: `8bc983d`
- Branch: `feature/long-horizon-workflow`
- 时间 / Time: 2026-09-05 (Asia/Shanghai)

## v10.1.11 (2026-09-14 20:20) - codex

### 变更摘要 / Change summary
- [env] [chore] [完成] 填充 operator-owned RoboTwin persistent Runtime 环境文件，使用本机已验证的解释器、模型、感知、GraspGen、校准与能力凭据路径；保留模型密钥在进程环境中，不写入文件。
- [env] [chore] [completed] Populated the operator-owned RoboTwin persistent Runtime environment file with locally verified interpreter, model, perception, GraspGen, qualification, and capability paths; kept the model secret in the process environment rather than the file.

### 影响文件 / Affected files
- `/home/yanxu/.PhyAgentOS/deployments/robotwin-persistent/runtime.env`（operator-owned deployment file）

### 失败场景与边界 / Failure scenario and boundary
- [env] [chore] [完成] 避免 2.0.1 Skill 启动时因缺少 required environment 在 Dora admission 前逐项失败；不填入 `ROBOTWIN20_MODEL_API_KEY`，防止 secret 进入部署文件、日志或 trace。
- [env] [chore] [completed] Prevented incremental pre-admission failures caused by missing required environment values; omitted `ROBOTWIN20_MODEL_API_KEY` so the secret does not enter the deployment file, logs, or traces.

### 文件变更详情 / File changes
- [新增 / Added] `/home/yanxu/.PhyAgentOS/deployments/robotwin-persistent/runtime.env:L1-L36`：写入 26 个非秘密部署变量；模型 API key 保持进程环境注入。
- [新增 / Added] `/home/yanxu/.PhyAgentOS/deployments/robotwin-persistent/runtime.env:L1-L36`: wrote 26 non-secret deployment variables; the model API key remains process-injected.

**关键 Diff / Key diff:**
```text
修改前 / Before: deployment file absent; all required values empty in runtime.env.example
修改后 / After: absolute local paths for PAOS/RoboTwin/model workers/GraspGen/qualification/capability artifacts; ROBOTWIN20_MODEL_API_KEY omitted by design
```

### 验证 / Verification
- Skill manifest is `2.0.1`; PAOS environment parser reports 26/27 required values supplied, with only `ROBOTWIN20_MODEL_API_KEY` intentionally missing.
- All 26 configured paths exist; file mode is `0600`; artifact root mode is `0700`.
- No Runtime, Gateway Query, Action, simulator, hardware, or motion was started.

### Git 提交 / Git Commit
- Pending repository receipt commit; operator-owned environment file is outside the repository and is not committed.
## v10.3.2 (2026-09-15 18:45) - codex

### 变更摘要 / Change summary [完成]
- [comm] [tune] 将 `scene.understand` 的 Agent 查询超时提高到 180 秒，返回带原因的明确 `timeout` 错误；同步将持久 Runtime profile 切换到 GPT-Sol High 并延长 provider 超时。
- [Comm] [Tune] Raise the Agent query timeout for `scene.understand`, return an explicit reasoned `timeout` error, and align the persistent Runtime profile with GPT-Sol High and a longer provider timeout.

### 预期影响文件 / Planned files
- `PhyAgentOS/agent/tools/forge_tool_api.py`
- `PhyAgentOS/forge/tool_client.py`
- `tests/test_forge_tool_api.py`
- `examples/forge-skills/pick-place-workflow/profiles/robotwin-persistent/persistent-host.yaml`
- `/home/yanxu/.PhyAgentOS/skills/pick-place-workflow/profiles/robotwin-persistent/persistent-host.yaml`（operator-owned Runtime profile）

### 文件变更详情 / File changes

#### [修改] `PhyAgentOS/agent/tools/forge_tool_api.py:L18-L32,L146-L160`

**修改前 / Before:**
```python
# caller-provided timeout was forwarded unchanged
timeout_ms=timeout_ms
```

**修改后 / After:**
```python
_SCENE_UNDERSTAND_TIMEOUT_MS = 180_000
effective_timeout_ms = _effective_query_timeout_ms(tool_id, timeout_ms)
```

**修改说明 / Description:** `scene.understand` now has a 180-second minimum
Agent-side Gateway wait; other Query timeouts remain unchanged.

#### [修改] `PhyAgentOS/forge/tool_client.py:L30-L35,L217-L221`

**修改前 / Before:**
```python
raise ForgeToolAPITimeoutError("... timed out; remote state is unknown")
```

**修改后 / After:**
```python
raise ForgeToolAPITimeoutError(
    "... timed out; remote outcome was not confirmed",
    timeout_s=read_timeout_s,
)
```

**修改说明 / Description:** Timeout exceptions retain the effective budget for
diagnostics without changing internal no-success accounting.

#### [修改] `examples/forge-skills/pick-place-workflow/profiles/robotwin-persistent/persistent-host.yaml:L21-L22`
#### [修改] `/home/yanxu/.PhyAgentOS/skills/pick-place-workflow/profiles/robotwin-persistent/persistent-host.yaml:L21-L22`

**修改前 / Before:** `reasoning_effort: low`, `timeout_seconds: 90`

**修改后 / After:** `reasoning_effort: high`, `timeout_seconds: 150`

**修改说明 / Description:** Both source and operator-owned Runtime profiles
now select GPT-Sol High and allow a longer provider request.

#### [新增] `tests/test_forge_tool_api.py:L1-L31`

Added focused regression tests for the scene-understanding timeout floor,
unchanged non-understanding Query deadlines, and explicit reasoned timeout
responses.

### 验证 / Verification

- Focused timeout tests: `3 passed`。
- Core PAOS suite: `397 passed`。
- Ruff、compileall、`git diff --check`：通过。
- Both YAML profiles parse with `model=${ROBOTWIN20_MODEL}`, `reasoning_effort=high`, and `timeout_seconds=150`。
- Existing Runtime process was not restarted because the invoking shell does not contain `ROBOTWIN20_MODEL_API_KEY`; the new profile applies on the next controlled Runtime restart.

### 设计边界 / Design boundary

- User-facing Tool errors now expose `status=timeout`, `code=gateway_timeout`,
  and a reason; Coordinator persistence continues to retain an unconfirmed
  terminal outcome and never infers success or motion.
- No Action/Session, simulator step, hardware IO, or motion was executed.

### Git 提交 / Git Commit

- Commit: `53c63b9`
- Branch: `feature/planning-loop`
- 时间 / Time: 2026-09-15 (Asia/Shanghai)

## v10.3.3 (2026-09-15 19:35) - codex

### 预期修改 / Planned Changes [完成]
- [sense] [fix] 在 RoboTwin adapter 边界归一化 GPT-Sol High 的 ambiguity 别名，并在已有 RGB-D metric evidence 时执行实体级 reconciliation；保持 PAOS Core provider-neutral。
- [sense] [fix] Normalize GPT-Sol High ambiguity aliases at the RoboTwin adapter boundary and reconcile entity-scoped metric evidence when valid RGB-D artifacts exist; keep PAOS Core provider-neutral.
- [policy] [fix] 修复 Agent-composed PlanNode 将描述性 `produced_evidence` 当作 opaque evidence ref 导致 `missing_produced_evidence` 的契约映射；在 materialization/settlement 边界 fail-closed 并提供可诊断错误。
- [policy] [fix] Fix the contract mapping where Agent-composed PlanNodes use descriptive `produced_evidence` labels instead of opaque evidence refs, causing `missing_produced_evidence`; fail closed with a diagnostic error at materialization/settlement boundaries.

### 影响文件 / Planned Files
- `examples/forge-adapters/robotwin20/src/robotwin20_adapter/single_view_perception.py`
- `examples/forge-adapters/robotwin20/src/robotwin20_adapter/grounding.py`
- `PhyAgentOS/agent/plan_proposal.py`
- `PhyAgentOS/planning/settlement.py`
- `examples/forge-adapters/robotwin20/tests/test_single_view_perception.py`
- `tests/test_planning_task_integration.py`
- `docs/forge/MANIPULATION_DAG_DEVELOPER_GUIDE.md`

### 实际修改 / Implemented Changes

- [sense] [fix] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/single_view_perception.py:L27-L54,L482-L483,L520-L525,L640-L663`：新增 provider ambiguity alias normalization；`NO_METRIC_3D_ENVELOPES` 归一化为 `metric_3d_unavailable`，`BLOCK_GEOMETRY_UNCERTAIN` 归一化为 `object_shape_uncertain`；metric evidence 有效时按实体 reconciliation，并在 reconciliation 中保留 `provider_code`，未知 code 继续 fail-closed。
- [Sense] [Fix] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/single_view_perception.py:L27-L54,L482-L483,L520-L525,L640-L663`: Added provider ambiguity alias normalization; mapped `NO_METRIC_3D_ENVELOPES` to `metric_3d_unavailable` and `BLOCK_GEOMETRY_UNCERTAIN` to `object_shape_uncertain`; reconciled valid metric evidence per entity while retaining `provider_code` in reconciliation and keeping unknown codes fail-closed.
- [policy] [fix] `PhyAgentOS/agent/plan_proposal.py:L25-L69`、`PhyAgentOS/agent/tools/forge_task.py:L190-L201`：root discovery node 在 Tool terminal result 之前不得声明 `produced_evidence`；materialization 现在给出明确错误，并指导后续节点使用精确 `paos_record.evidence_refs`，避免 `missing_produced_evidence` 运行时失败。
- [Policy] [Fix] `PhyAgentOS/agent/plan_proposal.py:L25-L69`, `PhyAgentOS/agent/tools/forge_task.py:L190-L201`: Root discovery nodes may not declare `produced_evidence` before a Tool terminal result; materialization now reports a precise error and directs later nodes to exact `paos_record.evidence_refs`, preventing runtime `missing_produced_evidence` failures.
- [docs] [docs] `docs/forge/MANIPULATION_DAG_DEVELOPER_GUIDE.md:L292-L304`：明确 root discovery evidence 与 Coordinator opaque evidence ref 的映射规则。
- [Docs] [Docs] `docs/forge/MANIPULATION_DAG_DEVELOPER_GUIDE.md:L292-L304`: Documented the mapping rule between root discovery outputs and Coordinator-owned opaque evidence refs.

### 测试 / Verification

- Adapter single-view focused: `15 passed`。
- Planning/Coordinator focused: `65 passed`；planning module/recovery/end-to-end: `57 passed`。
- Grounding and Skill planning: `34 passed`。
- Core PAOS with `pytest_asyncio`: `398 passed`。
- RoboTwin20 Adapter with runtime/scripts PYTHONPATH and `pytest_asyncio`: `466 passed`。
- Ruff、compileall、`git diff --check`：通过。
- Existing `task_efb37a49dbfe431e` capture was used for a no-motion settlement replay: corrected empty root `produced_evidence` settles `completed`; persisted records contain zero Action/Session and `motion_authorized=false`。
- 未启动 Runtime，未调用 Gateway Action/Session，未执行仿真或硬件运动。

### 六维 Code Review / Six-Dimension Review

1. **架构集成 / Architecture integration：PASS** — provider aliases stay in the RoboTwin adapter; graph validation stays in Agent plan compilation; PAOS planning remains pure and Gateway/Runtime ownership is unchanged.
2. **失败与恢复 / Failure and recovery：PASS** — unknown ambiguity remains blocking; missing RGB-D lineage still fails closed; invalid root evidence is rejected before execution; no retry or graph mutation is hidden in settlement.
3. **权限与机器人安全 / Authority and robotics safety：PASS** — normalization and validation are Query-only; no Action/Session, simulator step, hardware IO, or motion authority is introduced.
4. **配置与复现 / Configuration and reproducibility：PASS** — no new model path, credential, or Runtime setting; tests use deterministic injected providers and the existing capture ledger.
5. **可维护性 / Maintainability：PASS** — aliases are centralized, canonical codes remain provider-neutral, and the root evidence rule is documented at the materialization seam.
6. **可观察性 / Observability：PASS** — reconciliation retains the original provider code and evidence refs; materialization errors name the offending node and labels; existing task/revision records remain authoritative.

### 关键 Diff / Key Diff

```diff
- ambiguities = list(base.get("ambiguities", []))
+ambiguities = _normalize_ambiguities(base.get("ambiguities", []))

- missing = set(node.produced_evidence) - set(result.evidence_refs)
+ root semantic nodes with unresolved produced_evidence are rejected during
+ plan compilation, before a Tool record can be created.
```

### Git 提交 / Git Commit

- Functional commit: `d182781`; branch: `feature/planning-loop`.

## v10.3.4 (2026-09-15 19:55) - codex

### 预期修改 / Planned Changes [计划]
- [env] [chore] 将包含 ambiguity normalization 修复的 RoboTwin20 Node 升级为 `0.1.5`，同步 Skill manifest 到 `pick-place-workflow 2.0.3`，构建并安装本地锁定 Node，然后受控重启 `robotwin-persistent` Runtime。
- [Env] [Chore] Upgrade the RoboTwin20 Node containing ambiguity normalization to `0.1.5`, update the Skill manifest to `pick-place-workflow 2.0.3`, build and install the locally locked Node, then perform a controlled `robotwin-persistent` Runtime restart.

### 实际部署 / Deployment Receipt [完成]
- [env] [chore] `examples/forge-skills/pick-place-workflow/skill.yaml:L3,L64-L70` 升级到 Skill `2.0.3` 与 Node `0.1.5`；构建产物 `/tmp/paos-robotwin20-release/rebuild-2.0.3/robotwin20_persistent_host-0.1.5-linux-x86_64.tar.gz`，SHA-256 `544e661d968763b5842bec07b1360e5e6d7eb5cfa190f2df7e86faaa99898096`。
- [Env] [Chore] Upgraded `examples/forge-skills/pick-place-workflow/skill.yaml:L3,L64-L70` to Skill `2.0.3` and Node `0.1.5`; built `/tmp/paos-robotwin20-release/rebuild-2.0.3/robotwin20_persistent_host-0.1.5-linux-x86_64.tar.gz` with SHA-256 `544e661d968763b5842bec07b1360e5e6d7eb5cfa190f2df7e86faaa99898096`.
- [env] [chore] 已停止旧 `pick-place-workflow 2.0.2` Runtime，安装本地 Skill `2.0.3` 与 Node `robotwin20_persistent_host-0.1.5-linux-x86_64`，并通过 `paos forge-node verify` 验签。
- [Env] [Chore] Stopped the old `pick-place-workflow 2.0.2` Runtime, installed local Skill `2.0.3` and Node `robotwin20_persistent_host-0.1.5-linux-x86_64`, and passed `paos forge-node verify`.
- [env] [chore] 新 Runtime 尚未启动：当前进程缺少必需的 `ROBOTWIN20_MODEL_API_KEY`；secret 未写入文件、日志或提交，Runtime 当前保持 `stopped`。
- [Env] [Chore] The new Runtime has not started because the current process lacks required `ROBOTWIN20_MODEL_API_KEY`; the secret was not written to files, logs, or commits, and the Runtime remains `stopped`.

### 验证 / Verification
- `paos skill inspect pick-place-workflow`：`2.0.3`。
- `paos forge-node verify pick-place-workflow robotwin20_persistent_host`：SHA-256 verified。
- `paos skill stop pick-place-workflow`：stopped successfully。
- Runtime start is pending only on operator-provided API-key injection; no Query, Action, Session, simulator, hardware, or motion was executed after the stop.
## v10.3.5 (2026-09-15 20:00) - codex

### 预期修改 / Planned Changes [计划]
- [sense] [fix] 将 GPT-Sol 场景理解 provider 的 ambiguity contract 收紧到 RGB 可见语义范围，禁止模型声明 metric scale、3-D extents、精确尺寸、位姿或碰撞几何能力；保留 RGB-D composition adapter 作为 metric/geometry 证据唯一 owner。
- [Sense] [Fix] Restrict the GPT-Sol scene-understanding provider ambiguity contract to RGB-visible semantics, forbidding model claims about metric scale, 3-D extents, exact dimensions, pose, or collision geometry; keep the RGB-D composition adapter as the sole owner of metric/geometry evidence.
- [sense] [fix] 扩展 adapter 对已观测 provider metric ambiguity 别名的兼容归一化，并按每个 entity 独立 reconciliation，避免多实体 ambiguity 被首个实体处理后错误丢失；未知代码继续 fail-closed。
- [Sense] [Fix] Extend adapter compatibility normalization for observed provider metric-ambiguity aliases and reconcile each entity independently so multi-entity ambiguities are not lost after the first entity; keep unknown codes fail-closed.
- [eval] [test] 增加 provider schema/prompt、metric alias、多实体 reconciliation、未知 ambiguity 和 RGB-D 缺失证据回归测试，并完成六维 code review。
- [Eval] [Test] Add provider schema/prompt, metric-alias, multi-entity reconciliation, unknown-ambiguity, and missing RGB-D evidence regressions, followed by a six-dimension code review.

### 实际修改 / Implemented Changes [完成]
- [sense] [fix] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/openai_scene_understanding.py:L108-L115,L183-L200,L357-L370,L389-L407`：新增 `SCENE_SEMANTIC_AMBIGUITY_CODES`，将 Responses JSON schema 的 ambiguity code 限制为六类可见语义歧义；system prompt 明确 RGB-only provider 不负责 metric scale、depth、3-D extents、exact dimensions、pose、point cloud 或 collision geometry；解析层对绕过 schema 的 metric/provider-specific code fail-closed。
- [Sense] [Fix] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/openai_scene_understanding.py:L108-L115,L183-L200,L357-L370,L389-L407`: Added the canonical six-code semantic ambiguity vocabulary, restricted the Responses schema accordingly, explicitly assigned metric/depth/3-D/pose/point-cloud/collision evidence to RGB-D composition, and fail-closed on metric or provider-specific codes that bypass the schema.
- [sense] [fix] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/single_view_perception.py:L29-L35,L537-L538,L632-L676`：兼容 `NO_METRIC_3D_EXTENTS`、`METRIC_EXTENTS_NOT_ESTIMATED` 等历史 provider code；使用 entity-indexed reconciliation 对多实体逐一消费 metric evidence，仅在 localization/geometry 成功时移除对应 ambiguity，未知代码和无证据路径保持 active。
- [Sense] [Fix] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/single_view_perception.py:L29-L35,L537-L538,L632-L676`: Added compatibility aliases for observed legacy metric codes and entity-indexed reconciliation; each ambiguity is consumed only for entities with successful localization/geometry, while unknown codes and missing-evidence paths remain active.
- [eval] [test] `examples/forge-adapters/robotwin20/tests/test_openai_scene_understanding.py:L184-L271`、`examples/forge-adapters/robotwin20/tests/test_single_view_perception.py:L101-L140,L329-L367`：新增 provider metric code rejection/prompt/schema、canonical semantic ambiguity、双实体 alias reconciliation 与 unknown ambiguity 保留测试。
- [Eval] [Test] `examples/forge-adapters/robotwin20/tests/test_openai_scene_understanding.py:L184-L271`, `examples/forge-adapters/robotwin20/tests/test_single_view_perception.py:L101-L140,L329-L367`: Added provider metric-code rejection/prompt/schema, canonical semantic ambiguity, two-entity alias reconciliation, and unknown-ambiguity retention coverage.

### 验证 / Verification
- Adapter full suite: `471 passed`。
- Core planning focused suite: `39 passed`。
- Ruff、compileall、`git diff --check`：通过。
- No Gateway Action/Session, simulator step, hardware IO, or motion authority was introduced or executed.

### 六维 Code Review / Six-Dimension Review
1. **架构集成 / Architecture integration：PASS** — provider contract owns only RGB semantics; RGB-D composition owns metric evidence; PAOS Core, Grounding, readiness, and Gateway boundaries are unchanged.
2. **失败与恢复 / Failure and recovery：PASS** — schema-bypassing metric codes fail closed at the provider; legacy aliases reconcile only with valid per-entity evidence; unknown codes and missing depth/calibration remain blocking.
3. **权限与机器人安全 / Authority and robotics safety：PASS** — all changes are Query-side perception validation; no action/session route, motion authorization, simulator mutation, or hardware IO is added.
4. **配置与复现 / Configuration and reproducibility：PASS** — no credentials, model paths, runtime settings, or nondeterministic external state changed; tests use injected deterministic providers and temporary artifacts.
5. **可维护性 / Maintainability：PASS** — canonical vocabulary is centralized, provider-specific aliases stay adapter-local, and multi-entity reconciliation is explicit and covered by regression tests.
6. **可观察性 / Observability：PASS** — raw provider wording is retained only in reconciliation receipts, canonical active ambiguities remain visible, and provider contract failures expose a diagnostic class without leaking secrets.
## v10.3.6 (2026-09-15 20:10) - codex

### 预期修改 / Planned Changes [计划]
- [env] [chore] 在已安全取消 AgentTask 并停止旧 Runtime 后，将包含 `ffe4266` 的 RoboTwin20 Node 升级到 `0.1.6`，同步 Skill 到 `2.0.4`，构建、验签并安装；Runtime 启动等待 operator 进程注入 API key，未写入 secret。
- [Env] [Chore] After safely cancelling the AgentTask and stopping the old Runtime, upgrade the RoboTwin20 Node containing `ffe4266` to `0.1.6`, synchronize the Skill to `2.0.4`, build, verify, and install it; Runtime start awaits operator API-key injection and no secret was written.

### 实际部署 / Deployment Receipt [部分完成]
- 任务 `task_dacf700c775a467c` 已由 Coordinator 安全取消：无 Action、Session 或世界变化。
- 旧 `pick-place-workflow 2.0.3` Runtime 已停止。
- Node archive：`/tmp/paos-robotwin20-release/rebuild-2.0.4-TuI0UI/robotwin20_persistent_host-0.1.6-linux-x86_64.tar.gz`；SHA-256：`c07fa90eb526470c48f94c23d16ffaba40a33cb1ffb12e8daf13bd8c17f8f486`。
- Skill bundle：`/tmp/paos-robotwin20-release/rebuild-2.0.4-package-Bto8BK/pick-place-workflow-2.0.4.tar.gz`；SHA-256：`b5cb23a9e36b9b1ccc86c2c453763af6a7e60b77275659f66fc8b18966b51d34`。
- 本地 Skill `2.0.4` 与 Node `0.1.6` 已安装，`paos forge-node verify` 已通过。
- Runtime 尚未启动：当前进程缺少 `ROBOTWIN20_MODEL_API_KEY`；按安全边界未把 key 写入文件、Skill、日志或 traces。

## v10.3.7 (2026-09-15 21:45) - codex

### 预期修改 / Planned Changes [计划]
- [comm] [fix] 将 LLM 请求超时从通用 transient 重试路径中排除，避免一次 180 秒超时被重复放大为约 12 分钟；保留 HTTP 限流、5xx 与连接故障的有限重试。
- [Comm] [Fix] Exclude LLM request timeouts from generic transient retries so one 180-second timeout is not amplified to roughly 12 minutes; retain bounded retries for rate limits, 5xx, and connection failures.
- [sense] [fix] 为 `scene.understand` provider 异常返回脱敏、稳定的失败原因类别，保留 fail-closed 状态并区分 timeout、provider contract 与其他 provider failure。
- [Sense] [Fix] Return redacted, stable failure-reason categories for `scene.understand` provider exceptions while preserving fail-closed status and distinguishing timeout, provider-contract, and other provider failures.
- [eval] [test] 增加 timeout 不重试和理解 provider 失败分类回归测试，执行相关核心与适配器测试、静态检查及六维 code review。
- [Eval] [Test] Add regressions for no-retry-on-timeout and understanding-provider failure classification, then run focused core/adapter tests, static checks, and a six-dimension code review.

### 实际修改 / Implemented Changes [完成]
- [comm] [fix] `PhyAgentOS/providers/base.py:L86-L96,L198-L203,L259-L265`：将 timeout 从 transient marker 中移除并在 `chat_with_retry` 中显式 fail-fast；一次 provider timeout 只执行一次 bounded attempt，不再触发 1/2/4 秒退避及第四次请求。
- [Comm] [Fix] `PhyAgentOS/providers/base.py:L86-L96,L198-L203,L259-L265`: Removed timeout from transient markers and added explicit fail-fast handling in `chat_with_retry`; one provider timeout now consumes one bounded attempt without 1/2/4-second backoff or a fourth request.
- [sense] [fix] `PhyAgentOS/forge/capability_runtime/understanding.py:L256-L316,L669-L715` 与 `examples/forge-skills/pick-place-workflow/contracts/scene.understand.tool.yaml:L163-L172`：为 Query error 增加脱敏 `reason`、`failure_stage`、`retryable` 字段；provider exception 分类为 `timeout`、`contract` 或 `provider_failure`，保留原始错误不出边界、状态继续 fail-closed。
- [Sense] [Fix] `PhyAgentOS/forge/capability_runtime/understanding.py:L256-L316,L669-L715` and `examples/forge-skills/pick-place-workflow/contracts/scene.understand.tool.yaml:L163-L172`: Added redacted `reason`, `failure_stage`, and `retryable` fields to Query errors; provider exceptions are classified as `timeout`, `contract`, or `provider_failure` while raw details stay inside the boundary and fail-closed status is preserved.
- [eval] [test] `tests/test_turn_timeouts.py:L22-L60`、`examples/forge-skills/pick-place-workflow/tests/test_scene_understand.py:L130-L142`、`examples/forge-adapters/robotwin20/tests/test_scene_understand_provider.py:L86-L95`：新增 timeout 单次调用、理解 timeout 分类和 provider contract 分类回归测试。
- [Eval] [Test] `tests/test_turn_timeouts.py:L22-L60`, `examples/forge-skills/pick-place-workflow/tests/test_scene_understand.py:L130-L142`, and `examples/forge-adapters/robotwin20/tests/test_scene_understand_provider.py:L86-L95`: Added regressions for one-shot timeout calls, understanding timeout classification, and provider-contract classification.

### 关键 Diff / Key Diff
```diff
- if not self._is_transient_error(response.content):
+ if self._is_timeout_error(response.content):
+     return response
+ if not self._is_transient_error(response.content):

- except Exception:
-     return _error("understanding_provider_error", "scene understanding provider failed", ...)
+ except Exception as exc:
+     reason, retryable = _provider_failure(exc)
+     return _error("understanding_provider_error", "scene understanding provider failed", reason=reason, ...)
```

### 验证 / Verification
- `PYTHONPATH=. python -m pytest -q tests/test_turn_timeouts.py tests/test_prompt_context.py tests/test_agent_foundation.py tests/test_forge_tool_api.py` → 51 passed。
- `PYTHONPATH=.:examples/forge-skills/pick-place-workflow/src:examples/forge-adapters/robotwin20/src python -m pytest -q examples/forge-skills/pick-place-workflow/tests/test_scene_understand.py examples/forge-adapters/robotwin20/tests/test_scene_understand_provider.py -k 'not import_boundary'` → 28 passed, 1 deselected。
- `PYTHONPATH=.:examples/forge-skills/pick-place-workflow/src:examples/forge-adapters/robotwin20/src:examples/forge-adapters/robotwin20/runtime:examples/forge-adapters/robotwin20/scripts python -m pytest -q examples/forge-adapters/robotwin20/tests` → 471 passed。
- `ruff check ...`、`python -m compileall -q ...`、`git diff --check` → passed。未修改或执行任何 Action、Session、模拟器状态、硬件 IO 或 motion authorization。

### 六维 Code Review / Six-Dimension Review
1. **架构集成 / Architecture integration：PASS** — retry policy remains in the generic provider; diagnostic projection remains in the PAOS Query endpoint; adapter and Gateway ownership are unchanged.
2. **失败与恢复 / Failure and recovery：PASS** — timeout is explicit and bounded; retryable classification is advisory only; unavailable/contract failures remain fail-closed and cannot authorize a tool or action.
3. **权限与机器人安全 / Authority and robotics safety：PASS** — changes affect LLM/Query error handling only; no action/session route, readiness bypass, motion authority, simulator mutation, or hardware IO was added.
4. **配置与复现 / Configuration and reproducibility：PASS** — no model, API endpoint, credential, runtime profile, or opaque evidence reference was changed; tests use deterministic injected providers.
5. **可维护性 / Maintainability：PASS** — timeout classification is centralized, error fields have one contract, and tests cover both PAOS core and adapter projection.
6. **可观察性 / Observability：PASS** — logs identify fail-fast timeout behavior and Query results expose stable redacted reason/stage/retryability without provider secrets or raw exception text.

## v10.3.8 (2026-09-15 22:06) - codex

### 预期修改 / Planned Changes [计划]
- [env] [chore] 将包含 `scene.understand` 错误字段 contract 的 Skill 从 `2.0.4` 升级到 `2.0.5`，打包并安装新的本地 bundle；复用已验证的 Node `0.1.6`，不重新构建 Node。
- [Env] [Chore] Upgrade the Skill carrying the `scene.understand` error-field contract from `2.0.4` to `2.0.5`, package and install the new local bundle; reuse the verified Node `0.1.6` without rebuilding it.
- [env] [chore] 安全停止当前 `robotwin-persistent` Runtime，校验 Skill/Node 安装与 archive manifest，然后在 operator 当前进程注入 API key 后重启并验证所有 Tool context ready；不执行 Action/Session。
- [Env] [Chore] Safely stop the current `robotwin-persistent` Runtime, verify Skill/Node installation and archive manifest, then restart with the API key injected only in the operator process and verify all Tool contexts are ready; execute no Action/Session.

### 实际部署 / Deployment Receipt [部分完成]
- [env] [chore] `examples/forge-skills/pick-place-workflow/skill.yaml:L3` 升级 Skill `2.0.4` → `2.0.5`；复用已验证 Node `robotwin20_persistent_host-0.1.6-linux-x86_64`，未重复构建 Node。
- [Env] [Chore] Upgraded `examples/forge-skills/pick-place-workflow/skill.yaml:L3` from Skill `2.0.4` to `2.0.5`; reused the verified `robotwin20_persistent_host-0.1.6-linux-x86_64` Node without rebuilding it.
- [env] [chore] 新 Bundle：`/tmp/paos-robotwin20-release/rebuild-2.0.5-A5GNfQ/pick-place-workflow-2.0.5.tar.gz`；SHA-256 `ddf9eb8afaab0bbf65642aee121a0810fedf139cdee6ea0e512ac3528a345300`；已本地安装。
- [Env] [Chore] New bundle: `/tmp/paos-robotwin20-release/rebuild-2.0.5-A5GNfQ/pick-place-workflow-2.0.5.tar.gz`; SHA-256 `ddf9eb8afaab0bbf65642aee121a0810fedf139cdee6ea0e512ac3528a345300`; installed locally.
- [env] [chore] 已安全取消遗留执行任务并停止旧 Runtime；`paos skill inspect` 显示 `2.0.5`，`paos forge-node verify` 对 Node `0.1.6` 通过；Runtime 启动等待 operator 当前进程注入 `ROBOTWIN20_MODEL_API_KEY`，未写入 secret。
- [Env] [Chore] Safely cancelled the lingering executing task and stopped the old Runtime; `paos skill inspect` reports `2.0.5` and `paos forge-node verify` passes for Node `0.1.6`; Runtime start awaits operator-process injection of `ROBOTWIN20_MODEL_API_KEY`, with no secret written.

## v10.3.9 (2026-09-15 23:55) - codex

### 预期修改 / Planned Changes [计划]
- [policy] [refactor] 修正 AgentLoop discovery/planning 工具阶段：没有完成必要的 task-bound discovery evidence 时隐藏 `forge_task_materialize_plan`，避免先物化只含 observation 的冻结 revision；仅依据 Coordinator 持久 terminal records 开放物化。
- [Policy] [Refactor] Correct the AgentLoop discovery/planning tool phases: hide `forge_task_materialize_plan` until required task-bound discovery evidence exists, preventing a frozen observation-only revision; expose materialization only from Coordinator-persisted terminal records.
- [policy] [fix] 在 Coordinator finalize 前检查 active PlanGraph 的未结算节点和非终态执行记录，返回结构化 `task_not_ready_for_finalization`，保持任务 executing 并避免提前触发 Verifier。
- [Policy] [Fix] Check unsettled PlanGraph nodes and non-terminal execution records before Coordinator finalization; return structured `task_not_ready_for_finalization`, keep the task executing, and avoid premature Verifier invocation.
- [eval] [test] 增加 discovery 阶段工具暴露和 premature finalize 回归测试，并执行 AgentLoop、planning、task 与 Skill 集成测试。
- [Eval] [Test] Add regressions for discovery-phase tool exposure and premature finalization, then run AgentLoop, planning, task, and Skill integration tests.

### 实际修改 / Implemented Changes [完成]
- [policy] [refactor] `PhyAgentOS/agent/prompt_context.py:L21-L34,L222-L225`：发现阶段仅暴露上下文与查询；只有 Coordinator 持久化的 `scene.observe` 和 `scene.understand` 成功记录存在时才暴露计划物化。
- [Policy] [Refactor] `PhyAgentOS/agent/prompt_context.py:L21-L34,L222-L225`: Discovery exposes only context/query; plan materialization is visible only after Coordinator-persisted successful `scene.observe` and `scene.understand` records.
- [policy] [fix] `PhyAgentOS/agent/tools/forge_task.py:L240-L266`：无场景上下文时拒绝包含 discovery capability 的图并返回 `discovery_required`，保持 motion unauthorized。
- [Policy] [Fix] `PhyAgentOS/agent/tools/forge_task.py:L240-L266`: Reject discovery-capability graphs without scene context with `discovery_required`, preserving motion unauthorized.
- [eval] [test] `tests/test_prompt_context.py:L158-L176,L430-L440`：覆盖 discovery 阶段隐藏物化及完成观察/理解后开放物化；`37 passed`。
- [Eval] [Test] `tests/test_prompt_context.py:L158-L176,L430-L440`: Cover hidden materialization during discovery and exposure after completed observation/understanding; `37 passed`.

### 关键 Diff / Key Diff
```diff
- "forge_task_materialize_plan" in _DISCOVERY
+ _DISCOVERY = {"forge_tool_context", "forge_tool_query"}
+ if _discovery_complete(task): allowed.add("forge_task_materialize_plan")
+ if context is None and contains_discovery_capability(nodes): return discovery_required
```

### 验证 / Verification
- `PYTHONPATH=. python -m pytest -q tests/test_prompt_context.py tests/test_planning_loop.py` → 37 passed。
- `git diff --check` → passed。
- 未执行 Action、Session、Runtime 或硬件 IO。

### 六维 Code Review / Six-Dimension Review
1. 架构集成：PASS；阶段投影位于 AgentLoop，入口防护位于 Tool/Coordinator 边界。
2. 失败恢复：PASS；非法物化返回 discovery_required，不创建 revision 或触发 Verifier。
3. 权限与安全：PASS；未改变 Gateway、PlanGraph、binding 或 motion gate。
4. 配置与复现：PASS；无模型、凭据或 Runtime 配置变更。
5. 可维护性：PASS；发现条件集中且测试覆盖正反路径。
6. 可观察性：PASS；返回稳定错误码与原因，便于 Agent 继续 discovery。

## v10.3.10 (2026-09-16 08:00) - codex

### 预期修改 / Planned Changes [计划]
- [env] [chore] 将 pick-place-workflow Skill 升级至 2.0.6，重新打包、安装并验证；重启 Agent/Runtime 后仅执行只读 discovery 验证，不执行 Action/Session。
- [Env] [Chore] Upgrade pick-place-workflow Skill to 2.0.6, repackage, install, and verify; restart Agent/Runtime and run read-only discovery validation only, with no Action/Session.

### 实际部署 / Deployment Receipt [完成]
- [env] [chore] `examples/forge-skills/pick-place-workflow/skill.yaml:L3` 升级至 `2.0.6`；bundle `/tmp/paos-skill-2.0.6-fgNK/pick-place-workflow-2.0.6.tar.gz`，SHA-256 `28833d40c8303f4824e2d00ec274df6bfb0fae84a2b557c056999b857d082f2f`。
- [Env] [Chore] Upgraded `examples/forge-skills/pick-place-workflow/skill.yaml:L3` to `2.0.6`; bundle `/tmp/paos-skill-2.0.6-fgNK/pick-place-workflow-2.0.6.tar.gz`, SHA-256 `28833d40c8303f4824e2d00ec274df6bfb0fae84a2b557c056999b857d082f2f`.
- [env] [chore] 已停止旧 Skill/Runtime 并安装 Skill `2.0.6`；`paos skill inspect` 验证通过，Runtime 当前为 stopped，等待 operator 注入 API key 后启动。
- [Env] [Chore] Stopped the old Skill/Runtime and installed Skill `2.0.6`; `paos skill inspect` passes, Runtime is stopped pending operator-process API-key injection.

## v10.3.11 (2026-09-16 08:15) - codex

### 预期修改 / Planned Changes [计划]
- [policy] [refactor] 将 AgentLoop discovery 完成判断限定到 active revision 的权威执行记录，避免历史 revision 污染当前规划阶段。
- [Policy] [Refactor] Scope AgentLoop discovery completion to authoritative execution records from the active revision, preventing historical revisions from unlocking current planning.
- [policy] [fix] 在 Coordinator 物化入口拒绝当前 revision discovery prerequisites 不完整的 PlanGraph，并为 finalize 返回结构化 `task_not_ready_for_finalization`，保持任务 executing。
- [Policy] [Fix] Reject PlanGraphs with incomplete current-revision discovery prerequisites at the Coordinator materialization boundary, and return structured `task_not_ready_for_finalization` from finalize while keeping the task executing.
- [eval] [test] 增加跨 revision、直接物化和 premature finalize 回归测试；执行 AgentLoop、planning、task 与 Skill 集成测试及六维 review。
- [Eval] [Test] Add regressions for cross-revision history, direct materialization, and premature finalization; run AgentLoop, planning, task, Skill integration tests, and six-dimension review.

### 实际修改 / Implemented Changes [完成]
- [policy] [refactor] `PhyAgentOS/agent/prompt_context.py:L27-L36,L222-L227`：discovery 完成判断改为只读取 active revision execution records；历史 revision 记录不再解锁当前物化。
- [Policy] [Refactor] `PhyAgentOS/agent/prompt_context.py:L27-L36,L222-L227`: Discovery completion now reads only active-revision execution records; historical revisions cannot unlock current materialization.
- [policy] [fix] `PhyAgentOS/forge/task.py:L1157-L1170` 与 `PhyAgentOS/agent/tools/forge_task.py:L242-L258,L276-L306`：Coordinator 拒绝仅包含已完成 Query 的冻结 discovery 图，并将 `discovery_required` 结构化返回；未改变通用执行图和模型可用查询能力。
- [Policy] [Fix] `PhyAgentOS/forge/task.py:L1157-L1170` and `PhyAgentOS/agent/tools/forge_task.py:L242-L258,L276-L306`: Coordinator rejects frozen discovery-only graphs containing only completed Queries and projects `discovery_required` structurally; generic execution graphs and model query capabilities remain available.
- [policy] [fix] `PhyAgentOS/agent/tools/forge_task.py:L327-L358`：finalize lifecycle rejection 返回 `task_not_ready_for_finalization`，区分 incomplete plan 与 active invocation，保持 motion unauthorized。
- [Policy] [Fix] `PhyAgentOS/agent/tools/forge_task.py:L327-L358`: Finalize lifecycle rejections return `task_not_ready_for_finalization`, distinguishing incomplete plans from active invocations while keeping motion unauthorized.
- [eval] [test] `tests/test_prompt_context.py:L163-L224`、`tests/test_agent_foundation.py:L205-L223`：新增跨 revision、结构化 finalize、completed-query-only materialization 回归；核心与 Skill 集成测试共 `409 passed`。
- [Eval] [Test] `tests/test_prompt_context.py:L163-L224` and `tests/test_agent_foundation.py:L205-L223`: Added cross-revision, structured-finalize, and completed-query-only materialization regressions; core and Skill integration tests total `409 passed`.

### 关键 Diff / Key Diff
```diff
- for record in task.execution_records
+ for record in task.active_revision.execution_records

+ if completed_queries and all(node.capability in completed_queries for node in plan_graph.nodes):
+     return discovery_required

- {"ok": True, "data": await coordinator.finalize_task(task_id)}
+ {"ok": False, "error": {"code": "task_not_ready_for_finalization", ...}}
```

### 六维 Code Review / Six-Dimension Review
1. 架构集成：PASS；AgentLoop 仅投影阶段工具，Coordinator 保持 revision/admission 所有权。
2. 失败恢复：PASS；非法 discovery 物化与 premature finalize 均返回可恢复错误，不触发 Verifier。
3. 权限与机器人安全：PASS；未增加动作路径，`motion_authorized=false` 保持 fail-closed。
4. 配置与复现：PASS；无模型、凭据、Runtime 或 Node 配置变更。
5. 可维护性：PASS；active revision 事实与 Coordinator 入口分别负责，未扩大 Core capability vocabulary；模型仍可自由选择 discovery Query。
6. 可观察性：PASS；结构化错误码区分 discovery prerequisite、incomplete plan 与 active invocation。
