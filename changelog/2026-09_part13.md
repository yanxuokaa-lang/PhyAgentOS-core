## v11.4.11 (2026-09-23 22:13) - codex

### 变更计划 / Planned changes

- [agent] [fix] [计划] 在 `forge_plan_ready` 的每个候选 Tool 上明确当前冻结 ToolSpec 的来源模式；`forge_plan_select` 对无 projection Tool 误带 `projection_source` 返回可纠正的输入诊断，允许在现有 bounded node turn 内使用 `argument_sources` 重新选择，不自动猜测候选路径、不触发 Gateway。(local)
- [Agent] [Fix] [Planned] Expose each candidate Tool's source mode from its frozen ToolSpec in `forge_plan_ready`; treat `projection_source` on a non-projection Tool as a correctable input diagnostic so the existing bounded node turn can select again with `argument_sources`, without guessing candidate paths or invoking Gateway. (local)
- [tests] [feat] [计划] 用 projection/non-projection、错误来源模式、不同前驱候选数组、缺失或歧义来源和持久化 selection 交接做多组无运动回归；运行 Core、Skill/Adapter 相关测试与静态检查，并根据真实结果记录这一部分验收边界。(local)
- [Tests] [Feat] [Planned] Run varied no-motion regressions for projection/non-projection modes, wrong source mode, predecessor candidate arrays, missing or ambiguous sources, and durable selection handoff; run relevant Core, Skill/Adapter, and static checks and record the actual acceptance boundary. (local)

### 失败场景与 Anti-OverDefense 依据 / Failure scenario and rationale

`task_509ab4935dc34d7a` 的 `grasp.propose` 已成功，但 `manipulation.prepare` 的冻结 ToolSpec 没有 argument projection。Agent 沿用上一节点的 `projection_source`，Coordinator 在 Gateway 前正确拒绝；当前代码却标记 `requires_replan`，把尚未消费 selection、尚未执行 Tool 的字段模式错误升级为 `awaiting_replan`。图、场景和前驱证据均未变化，现有节点迭代/超时和 Coordinator 参数校验足以约束一次修正；本次不新增 hash、gate、状态机或 Action 重试。

For `task_509ab4935dc34d7a`, `grasp.propose` succeeded, but the frozen `manipulation.prepare` ToolSpec has no argument projection. The Agent reused `projection_source` from the previous node and the Coordinator correctly rejected it before Gateway; the current code then marked `requires_replan`, escalating an unconsumed, unexecuted input-mode error to `awaiting_replan`. The graph, scene, and predecessor evidence were unchanged, and the existing node iteration/timeout and Coordinator argument checks bound one correction. No hash, gate, state machine, or Action retry is added.

### 预期影响文件 / Expected files

- [tests] [feat] [完成] 补充 `tests/test_agent_foundation.py` 同一节点回合内错误来源模式后再次选择的 AgentLoop 控制流回归。(local)
- [Tests] [Feat] [Completed] Add an AgentLoop control-flow regression in `tests/test_agent_foundation.py` for correcting a wrong source mode within one node turn. (local)

- `PhyAgentOS/agent/planning_dispatch.py`
- `PhyAgentOS/agent/tools/planning.py`
- `tests/test_planning_dispatch.py`
- `tests/test_planning_selection.py`
- `docs/forge/AGENT_TOOL_INPUT_SELECTION_DESIGN.md`
- `CHANGELOG.md`

### 实际修改 / Completed changes [完成]

- [agent] [fix] [完成] `PhyAgentOS/agent/planning_dispatch.py:L223-L229,L260-L269` 在 ready/diagnostic 的候选 Tool 上暴露冻结来源模式；不改变 ToolSpec、PlanGraph 或动作授权。(local)
- [Agent] [Fix] [Completed] `PhyAgentOS/agent/planning_dispatch.py:L223-L229,L260-L269` exposes each candidate's frozen source mode in ready/diagnostic views without changing ToolSpecs, the PlanGraph, or motion authority. (local)
- [agent] [fix] [完成] `PhyAgentOS/agent/tools/planning.py:L167-L174,L327-L355,L399-L413` 将无 projection Tool 的模式误用和无效授权路径归类为可纠正输入；节点上下文失效仍要求 plan recovery，最终参数继续经现有 Coordinator/schema/Gateway 校验。(local)
- [Agent] [Fix] [Completed] `PhyAgentOS/agent/tools/planning.py:L167-L174,L327-L355,L399-L413` classifies a wrong source mode or invalid authorized path as correctable input while a broken node context still requires plan recovery; final arguments retain existing Coordinator/schema/Gateway validation. (local)
- [tests] [feat] [完成] `tests/test_planning_dispatch.py:L85-L98`、`tests/test_planning_selection.py:L104-L105,L180-L299,L391-L397,L596-L645`、`tests/test_planning_end_to_end.py:L219-L225` 覆盖两种来源模式、三种候选数组、错误模式后正确选择、不可见来源、真实 Coordinator 状态和失效上下文。(local)
- [Tests] [Feat] [Completed] `tests/test_planning_dispatch.py:L85-L98`, `tests/test_planning_selection.py:L104-L105,L180-L299,L391-L397,L596-L645`, and `tests/test_planning_end_to_end.py:L219-L225` cover both source modes, three candidate arrays, correction after a wrong mode, hidden sources, real Coordinator state, and invalid context. (local)
- [tests] [feat] [完成] `tests/test_agent_foundation.py:L251-L289` 证明 AgentLoop 在同一节点回合将可纠正错误交给下一次模型请求并再次选择，未进入 replan 交接。(local)
- [Tests] [Feat] [Completed] `tests/test_agent_foundation.py:L251-L289` proves AgentLoop passes the correctable error to the next model request and selects again in the same node turn without a replan handoff. (local)
- [docs] [docs] [完成] `docs/forge/AGENT_TOOL_INPUT_SELECTION_DESIGN.md:L62-L69,L164-L176` 记录来源模式、可纠正 selection 与失效上下文之间的边界。(local)
- [Docs] [Docs] [Completed] `docs/forge/AGENT_TOOL_INPUT_SELECTION_DESIGN.md:L62-L69,L164-L176` documents source modes and the boundary between correctable selection input and invalid context. (local)

### 关键 Diff / Key diff

```diff
- if projection_source:
-     raise PlanningDispatchError(..., retryable_in_revision=False, requires_replan=True)
+ if projection_source is not None:
+     raise PlanningDispatchError(..., retryable_in_revision=True,
+                                 recommended_action="use_argument_sources_for_this_consumer")
+ "selection_source_modes": {
+     policy.tool_id: (
+         "projection_source" if policy.argument_projection is not None
+         else "arguments_or_argument_sources"
+     )
+ }
+ except PlanningLoopError from resolve_node_argument_sources:
+     report invalid_argument_source without consuming a selection
+ except PlanningLoopError from NodeContextProvider.build:
+     report source_context_invalid and require plan recovery
+ # AgentLoop test: wrong-mode result -> second selection in the same node turn
```

## v11.5.0 (2026-09-24 10:10) - codex

### 变更计划 / Planned changes

- [agent] [fix] [完成] 将 Action 对账轮询预算从固定 `max_action_polls=100` 改为由已有 `ForgeConfig.execution_timeout_s` 与 `poll_interval_s` 推导；避免配置允许 300 秒执行却只等待约 50 秒后错误进入 `outcome_unknown`。(local)
- [Agent] [Fix] [Completed] Derive the Action reconciliation poll budget from the existing `ForgeConfig.execution_timeout_s` and `poll_interval_s` instead of the fixed `max_action_polls=100`; prevent a configuration that allows 300 seconds of execution from being marked `outcome_unknown` after only about 50 seconds. (local)
- [tests] [feat] [完成] 增加配置预算回归，验证默认 Forge 配置得到至少执行超时覆盖的轮询次数，显式调用方仍可覆盖预算，且 unknown fail-closed 语义不变。(local)
- [Tests] [Feat] [Completed] Add configuration-budget regressions proving the default Forge configuration covers the execution timeout, explicit callers can still override the budget, and unknown remains fail-closed. (local)

### 失败场景与架构依据 / Failure scenario and architecture rationale

此前修复了轮询间隔，但生产 `AgentLoop` 仍使用固定 100 次预算；配置中的 `execution_timeout_s=300` 与 `poll_interval_s=0.5` 实际要求约 600 次轮询。若 Action 在 50 秒到 300 秒之间完成，现有代码会在 Gateway 仍可查询时提前写入 `action_poll_budget_exhausted/outcome_unknown`，阻断三方块任务。该失败由已有配置参数传递不完整造成，使用现有超时与间隔推导预算即可修复，不新增状态、hash 或重试机制。

The previous fix wired the poll interval, but production `AgentLoop` still used a fixed budget of 100 polls; the configured `execution_timeout_s=300` and `poll_interval_s=0.5` require roughly 600 polls. If an Action completes between 50 and 300 seconds, the current code records `action_poll_budget_exhausted/outcome_unknown` while the Gateway is still queryable, blocking the three-block task. This is an incomplete handoff of existing configuration; deriving the budget from the existing timeout and interval fixes it without new state, hashes, or retry behavior.

### 既有验收记录（本次预算修复不涉及） / Prior acceptance records (not this budget fix)

- [eval] [test] [完成] 专项无运动回归以不同输入连续三轮均 `42 passed`；Core `551 passed`、Skill `346 passed`、RoboTwin Adapter `639 passed, 1 skipped`；Ruff、compileall、`git diff --check` 通过。(local)
- [Eval] [Test] [Completed] Varied no-motion focused tests passed `42` in each of three consecutive runs; Core passed `551`, Skill passed `346`, RoboTwin Adapter passed `639, 1 skipped`; Ruff, compileall, and `git diff --check` passed. (local)
- [eval] [test] [完成] 加入 AgentLoop 控制流测试后，扩大专项回归连续三轮均 `91 passed`；Core 全量 `552 passed`，Ruff 和 diff 检查通过。(local)
- [Eval] [Test] [Completed] After adding the AgentLoop control-flow test, the expanded focused suite passed `91` in each of three runs; full Core passed `552`, and Ruff and diff checks passed. (local)
- [eval] [exp] [完成] 对 `task_509ab4935dc34d7a` 的冻结记录只读重放得到 `red_manipulation_prepare -> arguments_or_argument_sources`；该任务仅含 discovery 与 `grasp.propose` Query、零 Action，之后通过 `paos task stop` 正常取消。此验收不等同于三方块物理排列、Verifier、benchmark 或累计视频成功。(local)
- [Eval] [Exp] [Completed] Read-only replay of frozen `task_509ab4935dc34d7a` records produced `red_manipulation_prepare -> arguments_or_argument_sources`; the task contained only discovery and `grasp.propose` Queries and zero Actions, then was cancelled through `paos task stop`. This acceptance does not establish physical three-block placement, Verifier, benchmark, or cumulative-video success. (local)

### 实际修改与验收 / Completed changes and acceptance

- [agent] [fix] [完成] `PhyAgentOS/agent/planning_loop.py:L599-L640` 在未显式指定预算时按 `ceil(execution_timeout_s / poll_interval_s)` 推导默认轮询次数；生产默认配置为 `600` 次，显式 `max_action_polls` 仍优先。(local)
- [Agent] [Fix] [Completed] `PhyAgentOS/agent/planning_loop.py:L599-L640` derives the default poll count as `ceil(execution_timeout_s / poll_interval_s)` when no explicit budget is supplied; the production default is `600` polls and an explicit `max_action_polls` still wins. (local)
- [tests] [feat] [完成] `tests/test_planning_effect_recovery.py:L396-L414` 覆盖默认预算推导与显式覆盖；原有 unknown、同 invocation 和无重发回归保持通过。(local)
- [Tests] [Feat] [Completed] `tests/test_planning_effect_recovery.py:L396-L414` covers default budget derivation and explicit override; existing unknown, same-invocation, and no-resend regressions remain passing. (local)
- [eval] [test] [完成] Planning/Prompt 专项 `103 passed`；Core 全量在显式加载 `pytest_asyncio` 并隔离 ROS 外部插件后 `557 passed`；Ruff、compileall、`git diff --check` 通过。(local)
- [Eval] [Test] [Completed] Planning/Prompt focused tests passed `103`; the full Core suite passed `557` with `pytest_asyncio` explicitly loaded while ROS external plugins were isolated; Ruff, compileall, and `git diff --check` passed. (local)

### 代码 Diff / Code diff

```diff
# PhyAgentOS/agent/planning_loop.py:L599-L640
- max_action_polls: int = 100,
+ max_action_polls: int | None = None,
+ max_action_polls = math.ceil(execution_timeout_s / action_poll_interval_s)

# tests/test_planning_effect_recovery.py:L396-L414
+ assert executor.max_action_polls == 600
+ assert explicit_executor.max_action_polls == 1
```

### Git 提交 / Git commit

- Commit: pending
- Branch: `feature/planning-loop`

## v11.5.1 (2026-09-24 10:30) - codex

### 变更计划 / Planned changes

- [agent] [fix] [完成] 将所有返回完整 AgentTask 的 `forge_task_*` 最新结果纳入已有有界投影，修复 `forge_task_create` 在 discovery 下一轮重复注入 ToolSpec schema、造成 prompt 膨胀并触发 provider timeout 的问题。(local)
- [Agent] [Fix] [Completed] Route latest `forge_task_*` results that return a full AgentTask through the existing bounded projection, fixing `forge_task_create` reinjecting ToolSpec schemas into discovery and causing prompt inflation and provider timeouts. (local)
- [tests] [feat] [完成] 扩展 `forge_task_create`/`forge_task_get` 压缩回归，明确验证 `task_id`、`status`、revision/plan/invocation 引用保留以及大型 ToolSpec/SkillUse 字段被裁减。(local)
- [Tests] [Feat] [Completed] Extend the `forge_task_create`/`forge_task_get` compaction regressions to verify retention of `task_id`, `status`, revision/plan/invocation references, and removal of oversized ToolSpec/SkillUse fields. (local)

### 失败场景与架构依据 / Failure scenario and architecture rationale

`AgentTask` 的完整持久化记录由 Coordinator 负责，AgentLoop 已经注入了独立的 bounded task projection；保留最新 `forge_task_create` 原文会重复携带 ToolSpec schema、SkillUse 和 revision 内容，使模型无法稳定进入 discovery。统一压缩 task lifecycle 响应只改变 prompt 投影，不改变 Coordinator 状态、Tool 参数、Runtime ownership 或 Action 对账，不新增 hash 或状态门禁。

The Coordinator owns the complete persisted `AgentTask`, while AgentLoop already injects a bounded task projection. Keeping the latest `forge_task_create` response raw duplicates ToolSpec schemas, SkillUse, and revision data, destabilizing discovery. Compacting task lifecycle responses changes only the prompt projection; it does not change Coordinator state, Tool arguments, Runtime ownership, or Action reconciliation, and adds no hash or state gate.

### 实际修改与验收 / Completed changes and acceptance

- [agent] [fix] [完成] `PhyAgentOS/agent/prompt_context.py:L719-L734` 对所有 `forge_task_*` 最新和历史结果使用既有 bounded reference projection。(local)
- [Agent] [Fix] [Completed] `PhyAgentOS/agent/prompt_context.py:L719-L734` applies the existing bounded reference projection to latest and historical `forge_task_*` results. (local)
- [tests] [feat] [完成] `tests/test_prompt_context.py:L136-L169` 覆盖 task/revision/plan/invocation/destination 身份字段保留，并验证 ToolSpec schema 与大型 SkillUse 内容被裁减。(local)
- [Tests] [Feat] [Completed] `tests/test_prompt_context.py:L136-L169` verifies retention of task/revision/plan/invocation/destination identities and removal of ToolSpec schemas and oversized SkillUse content. (local)
- [eval] [test] [完成] `tests/test_prompt_context.py` `26 passed`；Core `558 passed`（`PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 pytest -p pytest_asyncio.plugin -q tests`）；Ruff、compileall、`git diff --check` 通过。(local)
- [Eval] [Test] [Completed] `tests/test_prompt_context.py` passed `26`; Core passed `558` (`PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 pytest -p pytest_asyncio.plugin -q tests`); Ruff, compileall, and `git diff --check` passed. (local)

### 代码 Diff / Code diff

```diff
# PhyAgentOS/agent/prompt_context.py:L724-L734
- name == "forge_task_get"
+ name.startswith("forge_task_")

# tests/test_prompt_context.py:L141-L169
+ assert result["task_id"] == "task-rgb"
+ assert result["status"] == "executing"
+ assert result["plan_graph_ref"] == "artifact://plan/rgb-2"
+ assert result["invocation_id"] == "invocation-place-red-1"
+ assert "input_schema" not in content
```

### Git 提交 / Git commit

- Commit: `eee2a2a`
- Branch: `feature/planning-loop`

## v11.4.12 (2026-09-23 23:05) - codex

### 变更计划 / Planned changes

- [agent] [fix] [计划] 修正 `manipulation_intent_v2` 选择参数合并：当节点已冻结完整 `intent`、Agent 仅补充来源得到的 `allowed_arms` 等语义字段时，按字段合并；相同字段冲突仍拒绝，Coordinator 身份字段仍禁止由 Agent 提供。(local)
- [Agent] [Fix] [Planned] Fix `manipulation_intent_v2` selection merging so a node-frozen complete `intent` can be supplemented by source-derived fields such as `allowed_arms`; conflicting fields remain rejected and Coordinator identity fields remain Agent-owned forbidden. (local)
- [tests] [feat] [计划] 增加完整冻结 intent、部分补充、冲突字段和 Coordinator-owned 字段的多输入无运动回归，并复跑 Core 与相关静态检查。(local)
- [Tests] [Feat] [Planned] Add varied no-motion regressions for complete frozen intent, partial supplementation, conflicting fields, and Coordinator-owned fields, then rerun Core and static checks. (local)

### 失败场景与 Anti-OverDefense 依据 / Failure scenario and rationale

最新任务日志显示 `allowed_arms` 已由当前 capability 快照提供，但 Agent 反复在选择层被拒绝。现有实现把部分 flat intent 当作完整字典与冻结节点 intent 做全量相等比较，导致合法补充无法合并；这不是需要新 gate/hash 的问题，普通字段合并与冲突校验即可保持现有 Coordinator/ToolSpec 约束。

The latest task log shows `allowed_arms` available from the current capability snapshot while selection repeatedly fails. The implementation treats a partial flat intent as a complete dictionary and compares it for full equality with the frozen node intent, preventing valid supplementation. No new gate or hash is needed; field merge with conflict checks preserves existing Coordinator and ToolSpec constraints.

## v11.4.13 (2026-09-24 00:20) - codex

### 变更计划 / Planned changes

- [agent] [fix] [计划] 修正 `PlanningLoopAdapter` 在 Action `outcome_unknown` 后只返回 blocked 但不持久化任务恢复状态的问题；复用既有 `record_planning_node_blocked` 将任务收敛到 `awaiting_replan`，不重发 Action、不伪造终态。(local)
- [Agent] [Fix] [Planned] Fix `PlanningLoopAdapter` returning blocked after Action `outcome_unknown` without persisting recovery state; reuse existing `record_planning_node_blocked` to converge the task to `awaiting_replan`, without resending the Action or fabricating a terminal outcome. (local)
- [tests] [feat] [计划] 增加 unknown settlement 生命周期回归，验证任务不再保持 executing、Action invocation 只保留一次，并保持现有 Core/AgentLoop 测试通过。(local)
- [Tests] [Feat] [Planned] Add an unknown-settlement lifecycle regression proving the task does not remain executing, the Action invocation is retained once, and existing Core/AgentLoop tests remain passing. (local)

### 失败场景与架构依据 / Failure scenario and architecture rationale

真实任务 `task_de08ab12b38b4afa` 的 `red_place` 已记录 `outcome_unknown/action_poll_budget_exhausted`，但任务仍为 `executing`。这违反 PAOS 中 Coordinator 负责生命周期权威、AgentLoop 失败必须可恢复或安全停止的原则；已有 `record_planning_node_blocked` 正是该边界的持久化机制，因此不新增状态、重试或安全 gate。

The real task `task_de08ab12b38b4afa` recorded `outcome_unknown/action_poll_budget_exhausted` for `red_place` while the task remained `executing`. That violates PAOS ownership of lifecycle state and the AgentLoop requirement that failures converge to recovery or safe stop. The existing `record_planning_node_blocked` is the proper persistence boundary, so no new state, retry, or safety gate is added.

### 实际修改与验收 / Completed changes and acceptance

- [agent] [fix] [完成] `PhyAgentOS/agent/planning_dispatch.py:L696-L732` 增加字段级 intent 合并；冻结节点提供完整语义时，Agent 只补充 `allowed_arms` 等缺失字段，冲突字段仍拒绝，Coordinator-owned 字段仍由后续白名单拒绝。(local)
- [Agent] [Fix] [Completed] `PhyAgentOS/agent/planning_dispatch.py:L696-L732` adds field-level intent merging; a frozen node supplies complete semantics while the Agent may supplement missing fields such as `allowed_arms`, conflicting fields remain rejected, and Coordinator-owned fields remain rejected by the existing allowlist. (local)
- [tests] [feat] [完成] `tests/test_planning_selection.py:L1118-L1185` 覆盖完整冻结 intent + 部分 `allowed_arms` 输入；原有缺失参数、冲突语义和身份字段回归保持通过。(local)
- [Tests] [Feat] [Completed] `tests/test_planning_selection.py:L1118-L1185` covers complete frozen intent plus partial `allowed_arms` input; existing missing-argument, semantic-conflict, and identity-field regressions remain passing. (local)
- [eval] [test] [完成] 相关专项连续三轮均 `91 passed`；Core 全量 `552 passed`；Ruff 与 `git diff --check` 通过，未调用 Gateway 或产生 Action。(local)
- [Eval] [Test] [Completed] The focused suite passed `91` in each of three consecutive runs; Core passed `552`; Ruff and `git diff --check` passed, with no Gateway call or Action produced. (local)

### 实际修改与验收 / Completed changes and acceptance

- [agent] [fix] [完成] `PhyAgentOS/agent/planning_loop.py:L1316-L1324` 在 unknown recovery 分支持久化 `planning_node_blocked`，复用 Coordinator 既有 `awaiting_replan` 生命周期；不重发 invocation。(local)
- [Agent] [Fix] [Completed] `PhyAgentOS/agent/planning_loop.py:L1316-L1324` persists `planning_node_blocked` in the unknown recovery branch and reuses the Coordinator's existing `awaiting_replan` lifecycle without resending the invocation. (local)
- [tests] [feat] [完成] `tests/test_planning_loop.py:L1080-L1110` 覆盖 unknown settlement、任务状态收敛和无 Action 重试；原有规划与 AgentLoop 回归保持通过。(local)
- [Tests] [Feat] [Completed] `tests/test_planning_loop.py:L1080-L1110` covers unknown settlement, task-state convergence, and no Action retry; existing planning and AgentLoop regressions remain passing. (local)
- [eval] [test] [完成] PlanningLoop `53 passed`，扩大 Core/AgentLoop 套件 `144 passed`，Ruff 与 diff 检查通过。(local)
- [Eval] [Test] [Completed] PlanningLoop passed `53`, the expanded Core/AgentLoop suite passed `144`, and Ruff plus diff checks passed. (local)
- [eval] [exp] [完成] 三轮真实 CLI AgentLoop 验收：第一轮旧 owner 任务安全取消；第二轮新任务完成 discovery、PlanGraph、grasp/prepare/acquire，并在真实 `object.place` 后按 invocation 记录 `outcome_unknown`，无重发 Action；第三轮 provider timeout 在建任务前停止，数据库无新任务、无 Action。(local)
- [Eval] [Exp] [Completed] Three live CLI AgentLoop runs: the first stale owner task was safely cancelled; the second new task completed discovery, PlanGraph, grasp/prepare/acquire, then recorded `outcome_unknown` after a real `object.place` with no Action resend; the third provider timeout stopped before task creation with no new task or Action in the database. (local)
- [eval] [exp] [完成] 第二轮任务最终通过 Coordinator `task stop` 收敛为 `failed`，保留 `red_place` `outcome_unknown/action_poll_budget_exhausted` settlement；未宣称三方块排列或 Verifier 成功。(local)
- [Eval] [Exp] [Completed] The second task converged to `failed` through Coordinator `task stop`, retaining the `red_place` `outcome_unknown/action_poll_budget_exhausted` settlement; no three-block arrangement or Verifier success is claimed. (local)
## v11.4.14 (2026-09-24 08:35) - codex

### 变更计划 / Planned changes

- [agent] [fix] [完成] 将已有 `ForgeConfig.poll_interval_s` 接入 `AgentLoopNodeExecutor` 的 Action invocation 对账轮询，避免零间隔消耗有限轮询预算后错误产生 `outcome_unknown`；继续由 Coordinator 保存 invocation 生命周期，不重发 Action、不伪造成功。(local)
- [Agent] [Fix] [Completed] Wire the existing `ForgeConfig.poll_interval_s` into `AgentLoopNodeExecutor` Action invocation reconciliation so a zero-delay loop cannot exhaust the bounded poll budget and produce a false `outcome_unknown`; keep invocation lifecycle persistence in the Coordinator without resending Actions or fabricating success. (local)
- [tests] [feat] [完成] 增加配置接线和 pending 到 terminal result 的回归，验证同一 invocation 完成 settlement，unknown 仍保持 fail-closed。(local)
- [Tests] [Feat] [Completed] Add regressions for configuration wiring and pending-to-terminal reconciliation, proving settlement uses the same invocation and unknown remains fail-closed. (local)

### 失败场景与架构依据 / Failure scenario and architecture rationale

真实 `object.place` invocation 在 Gateway 仍未返回 terminal result 时，以零秒间隔连续读取 100 次，随后被本地轮询预算标记为 `action_poll_budget_exhausted` 和 `outcome_unknown`。现有 Forge 配置已经规定了 0.5 秒轮询间隔，但 AgentLoop 没有消费它；这属于参数传递缺失，不需要新增 hash、状态或重试机制。修复只把已有配置传递到现有对账路径，保留 unknown 的安全收敛语义。

When a real `object.place` invocation had not produced a terminal Gateway result, the loop issued 100 reads with zero delay and then recorded `action_poll_budget_exhausted` and `outcome_unknown`. The existing Forge configuration already specifies a 0.5 second poll interval, but the AgentLoop did not consume it. This is a missing parameter handoff, not a need for a new hash, state, or retry mechanism. The fix wires the existing value into the current reconciliation path while preserving fail-closed unknown handling.

### 实际修改与验收 / Completed changes and acceptance

- [agent] [fix] [完成] `PhyAgentOS/agent/planning_loop.py:L592-L625` 将 `action_poll_interval_s` 改为可选；未显式覆盖时读取 `coordinator.config.poll_interval_s`，没有 Forge 配置的测试替身保持零延迟兼容，现有对账循环仍只使用原 invocation。(local)
- [Agent] [Fix] [Completed] `PhyAgentOS/agent/planning_loop.py:L592-L625` makes `action_poll_interval_s` optional; when no override is supplied it reads `coordinator.config.poll_interval_s`, while test doubles without Forge config retain zero-delay compatibility and the reconciliation loop continues using only the original invocation. (local)
- [tests] [feat] [完成] `tests/test_planning_effect_recovery.py:L368-L393` 增加配置接线回归：pending 结果后返回 terminal succeeded，验证同一 invocation 两次 status/result、只启动一次 Action、最终 settlement 成功。(local)
- [Tests] [Feat] [Completed] `tests/test_planning_effect_recovery.py:L368-L393` adds the configuration wiring regression: a pending result followed by terminal success uses two status/result reads for the same invocation, starts one Action, and settles successfully. (local)
- [eval] [test] [完成] `tests/test_planning_effect_recovery.py tests/test_planning_loop.py` 共 `76 passed`；Core 全量在显式加载 `pytest_asyncio` 并隔离 ROS 外部插件后 `554 passed`；Ruff、compileall、`git diff --check` 通过。(local)
- [Eval] [Test] [Completed] `tests/test_planning_effect_recovery.py tests/test_planning_loop.py` passed `76`; the full Core suite passed `554` with `pytest_asyncio` explicitly loaded while ROS external plugins were isolated; Ruff, compileall, and `git diff --check` passed. (local)

### 代码 Diff / Code diff

```diff
# PhyAgentOS/agent/planning_loop.py:L599-L625
- action_poll_interval_s: float = 0.0,
+ action_poll_interval_s: float | None = None,
+ if action_poll_interval_s is None:
+     action_poll_interval_s = getattr(
+         getattr(coordinator, "config", None), "poll_interval_s", 0.0
+     )

# tests/test_planning_effect_recovery.py:L368-L393
+ coordinator.config = SimpleNamespace(poll_interval_s=0.01)
+ executor = AgentLoopNodeExecutor(agent, coordinator)
+ assert executor.action_poll_interval_s == 0.01
+ assert projected.status == "succeeded"
+ assert coordinator.started == 1
+ assert client.status_calls == client.result_calls == 2
```

## v11.4.15 (2026-09-24 09:35) - codex

### 变更计划 / Planned changes

- [agent] [fix] [完成] 压缩重复 discovery 中的 `forge_task_get` 原始全量任务结果；当前 AgentTask 投影已提供权威状态摘要，避免每次只读状态查询把全部 revisions、Tool records 和 SkillUse 重新注入 prompt，导致上下文膨胀和 provider timeout。(local)
- [Agent] [Fix] [Completed] Compact raw `forge_task_get` results during repeated discovery; the existing AgentTask projection already provides the authoritative state summary, so full revisions, Tool records, and SkillUse data are not re-injected on every read-only status query, avoiding context inflation and provider timeouts. (local)
- [tests] [feat] [完成] 增加 latest `forge_task_get` 结果也必须使用有界投影的回归，同时保持完整 Core 与 prompt assembly 测试通过。(local)
- [Tests] [Feat] [Completed] Add a regression proving the latest `forge_task_get` result also uses a bounded projection, while preserving full Core and prompt assembly coverage. (local)

### 失败场景与架构依据 / Failure scenario and architecture rationale

真实 discovery 会话在没有产生新执行事实时连续调用 `forge_task_get`；该工具的最新原始响应绕过了历史 Forge 结果压缩，重复携带完整任务记录，使模型请求从约 18k 增长到约 57k token 并最终超时。Coordinator 的任务投影已经是 AgentLoop 的只读事实边界，压缩 `forge_task_get` 不丢失执行权威，也不改变任务状态、参数或动作对账；普通字段投影足以解决该具体失败场景，无需新增 hash 或状态门禁。

During a real discovery session with no new execution facts, the Agent repeatedly called `forge_task_get`; its latest raw response bypassed historical Forge-result compaction and repeatedly carried the full task record, growing model requests from roughly 18k to 57k tokens before timeout. The Coordinator task projection is already the AgentLoop's read-only fact boundary. Compacting `forge_task_get` does not alter lifecycle state, arguments, or Action reconciliation; the existing bounded projection addresses this concrete failure without a new hash or state gate.

### 实际修改与验收 / Completed changes and acceptance

- [agent] [fix] [完成] `PhyAgentOS/agent/prompt_context.py:L706-L732` 对 `forge_task_get` 的每次结果强制使用现有 bounded reference projection；其余 Tool 的 latest-result 保留策略不变。(local)
- [Agent] [Fix] [Completed] `PhyAgentOS/agent/prompt_context.py:L706-L732` forces every `forge_task_get` result through the existing bounded reference projection; latest-result retention for other Tools is unchanged. (local)
- [tests] [feat] [完成] `tests/test_prompt_context.py:L136-L155` 验证最新 `forge_task_get` 结果会移除大 `revisions/skill_uses` 内容并保持摘要版本标识。(local)
- [Tests] [Feat] [Completed] `tests/test_prompt_context.py:L136-L155` verifies the latest `forge_task_get` result removes oversized `revisions/skill_uses` content while retaining the summary version marker. (local)
- [eval] [test] [完成] Prompt/planning 专项 `117 passed`；Core 全量在显式加载 `pytest_asyncio` 并隔离 ROS 外部插件后 `555 passed`；Ruff、compileall、`git diff --check` 通过。(local)
- [Eval] [Test] [Completed] Prompt/planning focused tests passed `117`; the full Core suite passed `555` with `pytest_asyncio` explicitly loaded while ROS external plugins were isolated; Ruff, compileall, and `git diff --check` passed. (local)

### 代码 Diff / Code diff

```diff
# PhyAgentOS/agent/prompt_context.py:L724-L732
- and (aggressive or latest_by_name.get(name) != index)
+ and (
+     name == "forge_task_get"
+     or aggressive
+     or latest_by_name.get(name) != index
+ )

# tests/test_prompt_context.py:L136-L155
+ projected = _compact_forge_results(messages, aggressive=False)
+ assert len(projected[0]["content"]) < len(raw)
+ assert json.loads(projected[0]["content"])["version"] == "agent_tool_result_summary_v1"
```
## v11.5.2 (2026-09-24 02:38) - codex

### 变更计划 / Planned changes

- [agent] [fix] [完成] 将已有精确 `argument_sources` 解析复用于 PlanGraph 前的 task-bound `forge_tool_query`，允许后续 Query 从同一 AgentTask、当前 revision 的成功 Query 记录复制字段，避免 Agent 手工重组 scene.observe 参数；保留 Gateway/schema 校验与 Coordinator 审计。(local)
- [Agent] [Fix] [Completed] Reuse exact `argument_sources` resolution for task-bound `forge_tool_query` before PlanGraph materialization, allowing follow-up Queries to copy fields from successful Query records in the same AgentTask and active revision instead of manually rebuilding scene observation arguments; retain Gateway/schema validation and Coordinator audit. (local)
- [tests] [feat] [完成] 覆盖三种 scene/frame/artifact 输入形状、成功与失败来源筛选、literal/source 冲突、artifact `map_field` 提取及 Action Tool schema 隔离。(local)
- [Tests] [Feat] [Completed] Cover three scene/frame/artifact input shapes, successful and failed source filtering, literal/source conflicts, artifact `map_field` extraction, and isolation from the Action Tool schema. (local)

### 失败场景与架构依据 / Failure scenario and architecture rationale

真实任务 `task_f391f6a0149b43b7` 的 `scene.observe` 成功记录已含 `scene_revision`、frame、calibration、freshness 和 artifacts；Agent 的 `scene.understand` 仅传了 `observation_ref`，Gateway 因 `scene_revision` 缺失返回 `invalid_scene_revision`，之后观测源又不可用。现有 `argument_sources` 解析只挂在 PlanGraph node selection 上，而 discovery Query 必须在 PlanGraph 物化前完成，所以 Agent 被迫手工复制上游字段。复用同任务成功 Query 记录的精确路径解析可避免字段值重组；不增加 hash、状态门禁或物理重试。

In real task `task_f391f6a0149b43b7`, a successful `scene.observe` record contained `scene_revision`, frame, calibration, freshness, and artifacts; the Agent sent only `observation_ref` to `scene.understand`, Gateway returned `invalid_scene_revision`, and the observation source later became unavailable. Existing `argument_sources` resolution is available only during PlanGraph node selection, while discovery Queries must finish before graph materialization, forcing the Agent to rebuild upstream values manually. Reusing exact paths from successful same-task Query records removes this field-copy failure without adding hashes, state gates, or physical retries.

### 实际修改与验收 / Completed changes and acceptance

- [agent] [refactor] [完成] `PhyAgentOS/agent/argument_sources.py:L1-L143` 提取路径校验、记录读取、嵌套目标写入和数组 `map_field` 投影；`planning_loop.py:L379-L388,L422-L446` 复用该实现。(local)
- [Agent] [Refactor] [Completed] `PhyAgentOS/agent/argument_sources.py:L1-L143` extracts path validation, record reads, nested target writes, and array `map_field` projection; `planning_loop.py:L379-L388,L422-L446` reuses it. (local)
- [agent] [fix] [完成] `PhyAgentOS/agent/tools/forge_tool_api.py:L123-L136,L147-L214,L582-L602` 为 task-bound `forge_tool_query` 增加来源选择；只暴露当前 revision 中每个 Tool 最新、成功且 domain status 可用的 Query 记录，复制后仍由 Coordinator/Gateway 正常校验和留档。(local)
- [Agent] [Fix] [Completed] `PhyAgentOS/agent/tools/forge_tool_api.py:L123-L136,L147-L214,L582-L602` adds source selection to task-bound `forge_tool_query`; it exposes only the latest successful Query per Tool in the active revision with an available domain status, then retains normal Coordinator/Gateway validation and recording. (local)
- [agent] [fix] [完成] `PhyAgentOS/agent/tools/planning.py:L86-L87,L184-L200` 复用路径 schema，并允许数组 source 通过显式 `map_field` 提取 `ref`，Action 参数 schema 不开放该能力。(local)
- [Agent] [Fix] [Completed] `PhyAgentOS/agent/tools/planning.py:L86-L87,L184-L200` reuses the path schema and permits explicit `map_field` extraction such as artifact `ref`; the Action argument schema does not expose this capability. (local)
- [tests] [feat] [完成] `tests/test_forge_tool_api.py:L43-L257` 覆盖 1/2/3 个 artifact 的三种输入、active revision/latest query 过滤、invalid latest 不回退、来源不可见、冲突拒绝、task owner 必需及只读 Query/Action schema 边界。(local)
- [Tests] [Feat] [Completed] `tests/test_forge_tool_api.py:L43-L257` covers three inputs with one/two/three artifacts, active-revision/latest-query filtering, no fallback from an invalid latest result, invisible sources, conflict rejection, required task ownership, and the read-only Query/Action schema boundary. (local)
- [eval] [test] [完成] 相关 AgentLoop/参数来源专项连续三轮均 `100 passed`；Core `567 passed`；Ruff、compileall、`git diff --check` 通过。(local)
- [Eval] [Test] [Completed] The focused AgentLoop/argument-source suite passed `100` in each of three consecutive runs; Core passed `567`; Ruff, compileall, and `git diff --check` passed. (local)
- [eval] [exp] [完成] 前置复现任务 `task_f391f6a0149b43b7` 在 `scene.observe` 成功后因漏传 `scene_revision` 被 Gateway 拒绝并安全取消；零 Action、零 PlanGraph、未运行 Verifier，不能计为 RGB 任务成功。(local)
- [Eval] [Exp] [Completed] Pre-fix reproduction `task_f391f6a0149b43b7` was safely cancelled after Gateway rejected `scene.understand` for missing `scene_revision`; zero Actions and no PlanGraph or Verifier ran, so this is not an RGB task success. (local)

### 代码 Diff / Code diff

```diff
# PhyAgentOS/agent/tools/forge_tool_api.py:L129-L136
+ use argument_sources to copy exact fields from a successful same-task Query
+ scene.understand artifact refs use map_field="ref"

# PhyAgentOS/agent/tools/forge_tool_api.py:L188-L198
+ resolved_arguments = resolve_argument_sources(
+     _task_query_source_records(task), arguments, argument_sources
+ )

# tests/test_forge_tool_api.py:L116-L130
+ "scene_revision": {"record_id": ..., "path": ["response", "scene_revision"]}
+ "artifacts": {"record_id": ..., "path": ["response", "artifacts"], "map_field": "ref"}
```

### Git 提交 / Git commit

- Implementation commit: `0932ca5`
- Branch: `feature/planning-loop`

## v11.5.3 (2026-09-24 02:52) - codex

### 变更计划 / Planned changes

- [agent] [fix] [完成] `persistent_host.py:L249-L273,L613-L619` 将 profile 的 `sensor_ref`/`max_observation_age_ms` 发布为 `scene.observe` schema defaults；`persistent_runtime.py:L165-L170,L294-L299` 保留这些 defaults；`forge/task.py:L2165-L2194` 仅补 Query 缺失字段并持久化 effective arguments。(local)
- [Agent] [Fix] [Completed] `persistent_host.py:L249-L273,L613-L619` publishes profile `sensor_ref`/`max_observation_age_ms` as `scene.observe` schema defaults; `persistent_runtime.py:L165-L170,L294-L299` preserves them; `forge/task.py:L2165-L2194` fills only missing Query fields and persists effective arguments. (local)
- [agent] [fix] [完成] `prompt_context.py:L83-L92,L244-L249` 只为带 invocation ID 的 unresolved Action 开放 terminal-task status/result；`forge/task.py:L2312-L2368,L3220-L3223` 接受迟到 Gateway 观察、保留任务 terminal 状态，并在所有 Action/Session 均明确终结且不含 unknown 时释放 Runtime binding。(local)
- [Agent] [Fix] [Completed] `prompt_context.py:L83-L92,L244-L249` exposes terminal-task status/result only for unresolved Actions with an invocation ID; `forge/task.py:L2312-L2368,L3220-L3223` persists late Gateway observations, preserves task terminality, and releases the Runtime binding only after every Action/Session is known terminal with no unknown outcomes. (local)
- [tests] [feat] [完成] profile 默认值/ToolSpec 投影、Coordinator 缺失字段填充/显式覆盖/来源值优先/Query-only 与无默认值不猜值均有回归；AgentLoop/Core 专项连续三轮每轮 `105 passed`，Core `572 passed`、Skill `347 passed`、Adapter `643 passed, 1 skipped`。(local)
- [Tests] [Feat] [Completed] Regressions cover profile defaults/ToolSpec projection, Coordinator missing-field filling/explicit override/source precedence/Query-only behavior, and no inference without defaults; AgentLoop/Core focused tests passed `105` in each of three runs, Core `572`, Skill `347`, and Adapter `643 passed, 1 skipped`. (local)
- [tests] [chore] [完成] 整理扩展后 `test_prepared_routes.py:L1-L10` 的 Ruff import 分组，不改变测试行为。(local)
- [Tests] [Chore] [Completed] Normalize Ruff import grouping in the extended `test_prepared_routes.py:L1-L10` without changing test behavior. (local)

### 失败场景与 Anti-OverDefense 依据 / Failure scenario and Anti-OverDefense rationale

新任务 `task_8422730f808f44bf` 的 Oracle Runtime 及 Gateway 均 ready，但当前 Runtime context 只声明 readiness，不含 `scene.observe` 必填 `sensor_ref` 与 `max_age_ms`。`camera/head` 已由本任务所绑定的 RobotWin profile 声明；年龄阈值此前仅散见于旧调用/测试，未由运行配置所有。Agent 因无权猜测而请求用户补值，任务进入 `waiting_for_user`，零 Query、零 PlanGraph、零 Action。现有 Runtime ToolSpec、Task binding 和 Gateway schema 未传递实际 profile 参数；把 profile 值写入冻结 ToolSpec 并由 Coordinator 补缺能修复来源断层，不新增 hash、状态 gate 或重试。

For new task `task_8422730f808f44bf`, the Oracle Runtime and Gateway were ready, but live context exposed readiness only and omitted the required `scene.observe` `sensor_ref` and `max_age_ms`. The bound RobotWin profile declares `camera/head`; the age threshold existed only in prior calls/tests and had no runtime configuration owner. Since the Agent could not invent either value, it requested user input and the task entered `waiting_for_user` with zero Queries, PlanGraph, or Actions. Existing Runtime ToolSpec, task binding, and Gateway schema did not carry the configured profile values. Freezing profile defaults into ToolSpec and letting the Coordinator fill omissions closes that source gap without adding hashes, state gates, or retries.

此前的 `object.place` invocation 在有限轮询预算后被记录为 `unknown`，AgentTask 随后进入 terminal `failed`。status/result Agent tools 虽已注册，`visible_tool_names` 却对 terminal task 隐藏了对账工具，`AgentTaskCoordinator.observe_action` 也会直接丢弃 terminal task 的后续观察，因此即使 Gateway result 仍可读取，原 invocation 与 Runtime binding 仍无法对账。只允许针对同一任务已记录的 invocation 做只读对账；任务保持 terminal，绝不重发 Action。

The previous `object.place` invocation was recorded as `unknown` after its bounded poll budget, and the AgentTask then became terminal `failed`. Although the status/result Agent tools are registered, `visible_tool_names` hid them for terminal tasks and `AgentTaskCoordinator.observe_action` discarded later observations, leaving the original invocation and Runtime binding unreconciled even when Gateway result remained readable. Permit read-only reconciliation only for an invocation already recorded by the same task; preserve terminal task status and never resend the Action.

### 预期影响文件 / Expected files

- `PhyAgentOS/forge/task.py`
- `examples/forge-skills/pick-place-workflow/src/pick_place_workflow/persistent_runtime.py`
- `examples/forge-adapters/robotwin20/src/robotwin20_adapter/persistent_deployment.py`
- `examples/forge-adapters/robotwin20/src/robotwin20_adapter/persistent_host.py`
- `examples/forge-adapters/robotwin20/runtime/robotwin_backend.py`
- `examples/forge-adapters/robotwin20/profiles/robotwin20/franka-blocks-ranking.yaml`
- `examples/forge-skills/pick-place-workflow/skill.yaml`, `pyproject.toml`, and package changelog
- Core and RoboTwin tests for frozen Query defaults
- `examples/forge-skills/pick-place-workflow/tests/test_grasp_propose.py` for the v2.6.18 bundle/package version assertion
- `PhyAgentOS/agent/prompt_context.py` and `tests/test_prompt_context.py` for terminal-task reconciliation visibility
- `PhyAgentOS/forge/task.py` and `tests/test_agent_foundation.py` for late result persistence and invocation ownership

### 实际修改 / Completed changes

- [adapter] [fix] [完成] `franka-blocks-ranking.yaml:L6-L7` 配置正整数观察年龄；`robotwin_backend.py:L104-L125` 加载并校验 profile 值；`persistent_deployment.py:L104-L128` 将当前 profile 传入 Host；`persistent_host.py:L249-L273,L613-L619` 生成并发布 `scene.observe` defaults；`prepared_routes.py:L69-L76` 保持来源 provenance 校验，并允许 place 执行 scene revision 前进。(local)
- [Adapter] [Fix] [Completed] `franka-blocks-ranking.yaml:L6-L7` configures a positive observation age; `robotwin_backend.py:L104-L125` loads and validates profile values; `persistent_deployment.py:L104-L128` passes the profile into the Host; `persistent_host.py:L249-L273,L613-L619` publishes `scene.observe` defaults; `prepared_routes.py:L69-L76` preserves source provenance checks while allowing the place execution scene revision to advance. (local)
- [skill] [fix] [完成] `persistent_runtime.py:L165-L170,L294-L299` 保留 schema defaults；`object_place.py:L40-L46,L325-L354` 将当前 scene revision 与 acquire provenance 分开校验；`README.md:L71-L77`、`SKILL.md:L256-L262` 更新运行约束；包/manifest 升至 `2.6.18`，Node 锁升级至 `0.6.15`；`test_grasp_propose.py:L260-L270` 修正版本断言。(local)
- [Skill] [Fix] [Completed] `persistent_runtime.py:L165-L170,L294-L299` preserves schema defaults; `object_place.py:L40-L46,L325-L354` validates the current scene revision separately from acquire provenance; `README.md:L71-L77` and `SKILL.md:L256-L262` document the runtime contract; package/manifest is `2.6.18` with Node lock `0.6.15`; `test_grasp_propose.py:L260-L270` aligns the version assertion. (local)
- [agent] [fix] [完成] `prompt_context.py:L83-L92,L244-L249` terminal task 只暴露已记录 unresolved Action 的 status/result；`forge/task.py:L2312-L2368` 保存同一 invocation 的迟到响应，只在 Gateway 确定终态时释放 invocation；`L2349-L2368,L3220-L3223` 仅当所有 Action/Session 明确终结后释放 Runtime task binding，任务状态不变。(local)
- [Agent] [Fix] [Completed] `prompt_context.py:L83-L92,L244-L249` exposes status/result only for a recorded unresolved Action on a terminal task; `forge/task.py:L2312-L2368` persists a late response for that invocation and releases it only on a known Gateway terminal result; `L2349-L2368,L3220-L3223` releases the Runtime task binding only after all Actions/Sessions are known terminal, without changing task status. (local)
- [tests] [feat] [完成] `test_persistent_host.py:L35-L50,L309-L315`、`test_persistent_deployment.py:L149-L162`、`test_robotwin_backend_contract.py:L96-L119`、`test_prepared_routes.py:L48-L52` 覆盖 profile defaults 与 route 输入；`test_object_place.py:L140-L153`、`test_persistent_runtime.py:L81-L114` 覆盖 Skill contract；`test_planning_task_integration.py:L205-L281` 覆盖 Query defaults/覆盖；`test_prompt_context.py:L390-L416`、`test_agent_foundation.py:L1046-L1090` 覆盖 terminal Action 对账。(local)
- [Tests] [Feat] [Completed] `test_persistent_host.py:L35-L50,L309-L315`, `test_persistent_deployment.py:L149-L162`, `test_robotwin_backend_contract.py:L96-L119`, and `test_prepared_routes.py:L48-L52` cover profile defaults and route inputs; `test_object_place.py:L140-L153` and `test_persistent_runtime.py:L81-L114` cover the Skill contract; `test_planning_task_integration.py:L205-L281` covers Query defaults/overrides; `test_prompt_context.py:L390-L416` and `test_agent_foundation.py:L1046-L1090` cover terminal Action reconciliation. (local)

### 代码 Diff / Code Diff

```diff
# AgentLoop terminal-task projection
-terminal task -> task create/get/context only
+terminal task with unresolved owned Action -> status/result for that invocation

# Coordinator late invocation observation
-if task.terminal: return
+persist status/result against the existing task-owned invocation
+release invocation and Runtime binding only after known Gateway terminal result

# Coordinator Query argument completion
+copy frozen ToolSpec defaults into missing Query fields
+persist and invoke with those effective arguments; explicit values remain unchanged

# object.place scene/provenance contract
-require acquire observation scene == current place execution scene
+validate provenance references against their source scene and validate the current execution scene separately
```

### 验证边界 / Verification boundary

- AgentLoop/Core focused suites: three runs, each `105 passed`.
- Core: `572 passed`; Skill: `347 passed`; RobotWin Adapter: `643 passed, 1 skipped`.
- Ruff, compileall, and `git diff --check` passed. No Gateway Action, Dora, or robot was started. This verifies local control-plane and contract behavior; it does not count as physical RGB task acceptance.

### Git 提交 / Git Commit

- Commit: `ab6a711`
- Branch: `feature/planning-loop`
- Time: `2026-09-24 03:29` Asia/Shanghai
