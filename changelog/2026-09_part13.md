## v11.4.11 (2026-09-23 22:13) - codex

### 变更计划 / Planned changes

- [agent] [fix] [计划] 在 `forge_plan_ready` 的每个候选 Tool 上明确当前冻结 ToolSpec 的来源模式；`forge_plan_select` 对无 projection Tool 误带 `projection_source` 返回可纠正的输入诊断，允许在现有 bounded node turn 内使用 `argument_sources` 重新选择，不自动猜测候选路径、不触发 Gateway。(local)
- [Agent] [Fix] [Planned] Expose each candidate Tool's source mode from its frozen ToolSpec in `forge_plan_ready`; treat `projection_source` on a non-projection Tool as a correctable input diagnostic so the existing bounded node turn can select again with `argument_sources`, without guessing candidate paths or invoking Gateway. (local)
- [tests] [feat] [计划] 用 projection/non-projection、错误来源模式、不同前驱候选数组、缺失或歧义来源和持久化 selection 交接做多组无运动回归；运行 Core、Skill/Adapter 相关测试与静态检查，并根据真实结果记录这一部分验收边界。(local)
- [Tests] [Feat] [Planned] Run varied no-motion regressions for projection/non-projection modes, wrong source mode, predecessor candidate arrays, missing or ambiguous sources, and durable selection handoff; run relevant Core, Skill/Adapter, and static checks and record the actual acceptance boundary. (local)

### 失败场景与 Anti-OverDefense 依据 / Failure scenario and rationale

`task_509ab4935dc34d7a` 的 `grasp.propose` 已成功，但 `manipulation.prepare` 的冻结 ToolSpec 没有 argument projection。Agent 沿用上一节点的 `projection_source`，Coordinator 在 Gateway 前正确拒绝；当前代码却标记 `requires_replan`，把尚未消费 selection、尚未执行 Tool 的字段模式错误升级为 `awaiting_replan`。图、场景和前驱证据均未变化，现有节点迭代/超时和 Coordinator 参数校验足以约束一次修正；本次不新增 hash、gate、状态机或 Action 重试。

For `task_509ab4935dc34d7a`, `grasp.propose` succeeded, but the frozen `manipulation.prepare` ToolSpec has no argument projection. The Agent reused `projection_source` from the previous node and the Coordinator correctly rejected it before Gateway; the current code then marked `requires_replan`, escalating an unconsumed, unexecuted input-mode error to `awaiting_replan`. The graph, scene, and predecessor evidence were unchanged, and the existing node iteration/timeout and Coordinator argument checks bound one correction. No hash, gate, state machine, or Action retry is added.

### 预期影响文件 / Expected files

- [tests] [feat] [完成] 补充 `tests/test_agent_foundation.py` 同一节点回合内错误来源模式后再次选择的 AgentLoop 控制流回归。(local)
- [Tests] [Feat] [Completed] Add an AgentLoop control-flow regression in `tests/test_agent_foundation.py` for correcting a wrong source mode within one node turn. (local)

- `PhyAgentOS/agent/planning_dispatch.py`
- `PhyAgentOS/agent/tools/planning.py`
- `tests/test_planning_dispatch.py`
- `tests/test_planning_selection.py`
- `docs/forge/AGENT_TOOL_INPUT_SELECTION_DESIGN.md`
- `CHANGELOG.md`

### 实际修改 / Completed changes [完成]

- [agent] [fix] [完成] `PhyAgentOS/agent/planning_dispatch.py:L223-L229,L260-L269` 在 ready/diagnostic 的候选 Tool 上暴露冻结来源模式；不改变 ToolSpec、PlanGraph 或动作授权。(local)
- [Agent] [Fix] [Completed] `PhyAgentOS/agent/planning_dispatch.py:L223-L229,L260-L269` exposes each candidate's frozen source mode in ready/diagnostic views without changing ToolSpecs, the PlanGraph, or motion authority. (local)
- [agent] [fix] [完成] `PhyAgentOS/agent/tools/planning.py:L167-L174,L327-L355,L399-L413` 将无 projection Tool 的模式误用和无效授权路径归类为可纠正输入；节点上下文失效仍要求 plan recovery，最终参数继续经现有 Coordinator/schema/Gateway 校验。(local)
- [Agent] [Fix] [Completed] `PhyAgentOS/agent/tools/planning.py:L167-L174,L327-L355,L399-L413` classifies a wrong source mode or invalid authorized path as correctable input while a broken node context still requires plan recovery; final arguments retain existing Coordinator/schema/Gateway validation. (local)
- [tests] [feat] [完成] `tests/test_planning_dispatch.py:L85-L98`、`tests/test_planning_selection.py:L104-L105,L180-L299,L391-L397,L596-L645`、`tests/test_planning_end_to_end.py:L219-L225` 覆盖两种来源模式、三种候选数组、错误模式后正确选择、不可见来源、真实 Coordinator 状态和失效上下文。(local)
- [Tests] [Feat] [Completed] `tests/test_planning_dispatch.py:L85-L98`, `tests/test_planning_selection.py:L104-L105,L180-L299,L391-L397,L596-L645`, and `tests/test_planning_end_to_end.py:L219-L225` cover both source modes, three candidate arrays, correction after a wrong mode, hidden sources, real Coordinator state, and invalid context. (local)
- [tests] [feat] [完成] `tests/test_agent_foundation.py:L251-L289` 证明 AgentLoop 在同一节点回合将可纠正错误交给下一次模型请求并再次选择，未进入 replan 交接。(local)
- [Tests] [Feat] [Completed] `tests/test_agent_foundation.py:L251-L289` proves AgentLoop passes the correctable error to the next model request and selects again in the same node turn without a replan handoff. (local)
- [docs] [docs] [完成] `docs/forge/AGENT_TOOL_INPUT_SELECTION_DESIGN.md:L62-L69,L164-L176` 记录来源模式、可纠正 selection 与失效上下文之间的边界。(local)
- [Docs] [Docs] [Completed] `docs/forge/AGENT_TOOL_INPUT_SELECTION_DESIGN.md:L62-L69,L164-L176` documents source modes and the boundary between correctable selection input and invalid context. (local)

### 关键 Diff / Key diff

```diff
- if projection_source:
-     raise PlanningDispatchError(..., retryable_in_revision=False, requires_replan=True)
+ if projection_source is not None:
+     raise PlanningDispatchError(..., retryable_in_revision=True,
+                                 recommended_action="use_argument_sources_for_this_consumer")
+ "selection_source_modes": {
+     policy.tool_id: (
+         "projection_source" if policy.argument_projection is not None
+         else "arguments_or_argument_sources"
+     )
+ }
+ except PlanningLoopError from resolve_node_argument_sources:
+     report invalid_argument_source without consuming a selection
+ except PlanningLoopError from NodeContextProvider.build:
+     report source_context_invalid and require plan recovery
+ # AgentLoop test: wrong-mode result -> second selection in the same node turn
```

## v11.5.0 (2026-09-24 10:10) - codex

### 变更计划 / Planned changes

- [agent] [fix] [完成] 将 Action 对账轮询预算从固定 `max_action_polls=100` 改为由已有 `ForgeConfig.execution_timeout_s` 与 `poll_interval_s` 推导；避免配置允许 300 秒执行却只等待约 50 秒后错误进入 `outcome_unknown`。(local)
- [Agent] [Fix] [Completed] Derive the Action reconciliation poll budget from the existing `ForgeConfig.execution_timeout_s` and `poll_interval_s` instead of the fixed `max_action_polls=100`; prevent a configuration that allows 300 seconds of execution from being marked `outcome_unknown` after only about 50 seconds. (local)
- [tests] [feat] [完成] 增加配置预算回归，验证默认 Forge 配置得到至少执行超时覆盖的轮询次数，显式调用方仍可覆盖预算，且 unknown fail-closed 语义不变。(local)
- [Tests] [Feat] [Completed] Add configuration-budget regressions proving the default Forge configuration covers the execution timeout, explicit callers can still override the budget, and unknown remains fail-closed. (local)

### 失败场景与架构依据 / Failure scenario and architecture rationale

此前修复了轮询间隔，但生产 `AgentLoop` 仍使用固定 100 次预算；配置中的 `execution_timeout_s=300` 与 `poll_interval_s=0.5` 实际要求约 600 次轮询。若 Action 在 50 秒到 300 秒之间完成，现有代码会在 Gateway 仍可查询时提前写入 `action_poll_budget_exhausted/outcome_unknown`，阻断三方块任务。该失败由已有配置参数传递不完整造成，使用现有超时与间隔推导预算即可修复，不新增状态、hash 或重试机制。

The previous fix wired the poll interval, but production `AgentLoop` still used a fixed budget of 100 polls; the configured `execution_timeout_s=300` and `poll_interval_s=0.5` require roughly 600 polls. If an Action completes between 50 and 300 seconds, the current code records `action_poll_budget_exhausted/outcome_unknown` while the Gateway is still queryable, blocking the three-block task. This is an incomplete handoff of existing configuration; deriving the budget from the existing timeout and interval fixes it without new state, hashes, or retry behavior.

### 既有验收记录（本次预算修复不涉及） / Prior acceptance records (not this budget fix)

- [eval] [test] [完成] 专项无运动回归以不同输入连续三轮均 `42 passed`；Core `551 passed`、Skill `346 passed`、RoboTwin Adapter `639 passed, 1 skipped`；Ruff、compileall、`git diff --check` 通过。(local)
- [Eval] [Test] [Completed] Varied no-motion focused tests passed `42` in each of three consecutive runs; Core passed `551`, Skill passed `346`, RoboTwin Adapter passed `639, 1 skipped`; Ruff, compileall, and `git diff --check` passed. (local)
- [eval] [test] [完成] 加入 AgentLoop 控制流测试后，扩大专项回归连续三轮均 `91 passed`；Core 全量 `552 passed`，Ruff 和 diff 检查通过。(local)
- [Eval] [Test] [Completed] After adding the AgentLoop control-flow test, the expanded focused suite passed `91` in each of three runs; full Core passed `552`, and Ruff and diff checks passed. (local)
- [eval] [exp] [完成] 对 `task_509ab4935dc34d7a` 的冻结记录只读重放得到 `red_manipulation_prepare -> arguments_or_argument_sources`；该任务仅含 discovery 与 `grasp.propose` Query、零 Action，之后通过 `paos task stop` 正常取消。此验收不等同于三方块物理排列、Verifier、benchmark 或累计视频成功。(local)
- [Eval] [Exp] [Completed] Read-only replay of frozen `task_509ab4935dc34d7a` records produced `red_manipulation_prepare -> arguments_or_argument_sources`; the task contained only discovery and `grasp.propose` Queries and zero Actions, then was cancelled through `paos task stop`. This acceptance does not establish physical three-block placement, Verifier, benchmark, or cumulative-video success. (local)

### 实际修改与验收 / Completed changes and acceptance

- [agent] [fix] [完成] `PhyAgentOS/agent/planning_loop.py:L599-L640` 在未显式指定预算时按 `ceil(execution_timeout_s / poll_interval_s)` 推导默认轮询次数；生产默认配置为 `600` 次，显式 `max_action_polls` 仍优先。(local)
- [Agent] [Fix] [Completed] `PhyAgentOS/agent/planning_loop.py:L599-L640` derives the default poll count as `ceil(execution_timeout_s / poll_interval_s)` when no explicit budget is supplied; the production default is `600` polls and an explicit `max_action_polls` still wins. (local)
- [tests] [feat] [完成] `tests/test_planning_effect_recovery.py:L396-L414` 覆盖默认预算推导与显式覆盖；原有 unknown、同 invocation 和无重发回归保持通过。(local)
- [Tests] [Feat] [Completed] `tests/test_planning_effect_recovery.py:L396-L414` covers default budget derivation and explicit override; existing unknown, same-invocation, and no-resend regressions remain passing. (local)
- [eval] [test] [完成] Planning/Prompt 专项 `103 passed`；Core 全量在显式加载 `pytest_asyncio` 并隔离 ROS 外部插件后 `557 passed`；Ruff、compileall、`git diff --check` 通过。(local)
- [Eval] [Test] [Completed] Planning/Prompt focused tests passed `103`; the full Core suite passed `557` with `pytest_asyncio` explicitly loaded while ROS external plugins were isolated; Ruff, compileall, and `git diff --check` passed. (local)

### 代码 Diff / Code diff

```diff
# PhyAgentOS/agent/planning_loop.py:L599-L640
- max_action_polls: int = 100,
+ max_action_polls: int | None = None,
+ max_action_polls = math.ceil(execution_timeout_s / action_poll_interval_s)

# tests/test_planning_effect_recovery.py:L396-L414
+ assert executor.max_action_polls == 600
+ assert explicit_executor.max_action_polls == 1
```

### Git 提交 / Git commit

- Commit: pending
- Branch: `feature/planning-loop`

## v11.5.1 (2026-09-24 10:30) - codex

### 变更计划 / Planned changes

- [agent] [fix] [完成] 将所有返回完整 AgentTask 的 `forge_task_*` 最新结果纳入已有有界投影，修复 `forge_task_create` 在 discovery 下一轮重复注入 ToolSpec schema、造成 prompt 膨胀并触发 provider timeout 的问题。(local)
- [Agent] [Fix] [Completed] Route latest `forge_task_*` results that return a full AgentTask through the existing bounded projection, fixing `forge_task_create` reinjecting ToolSpec schemas into discovery and causing prompt inflation and provider timeouts. (local)
- [tests] [feat] [完成] 扩展 `forge_task_create`/`forge_task_get` 压缩回归，明确验证 `task_id`、`status`、revision/plan/invocation 引用保留以及大型 ToolSpec/SkillUse 字段被裁减。(local)
- [Tests] [Feat] [Completed] Extend the `forge_task_create`/`forge_task_get` compaction regressions to verify retention of `task_id`, `status`, revision/plan/invocation references, and removal of oversized ToolSpec/SkillUse fields. (local)

### 失败场景与架构依据 / Failure scenario and architecture rationale

`AgentTask` 的完整持久化记录由 Coordinator 负责，AgentLoop 已经注入了独立的 bounded task projection；保留最新 `forge_task_create` 原文会重复携带 ToolSpec schema、SkillUse 和 revision 内容，使模型无法稳定进入 discovery。统一压缩 task lifecycle 响应只改变 prompt 投影，不改变 Coordinator 状态、Tool 参数、Runtime ownership 或 Action 对账，不新增 hash 或状态门禁。

The Coordinator owns the complete persisted `AgentTask`, while AgentLoop already injects a bounded task projection. Keeping the latest `forge_task_create` response raw duplicates ToolSpec schemas, SkillUse, and revision data, destabilizing discovery. Compacting task lifecycle responses changes only the prompt projection; it does not change Coordinator state, Tool arguments, Runtime ownership, or Action reconciliation, and adds no hash or state gate.

### 实际修改与验收 / Completed changes and acceptance

- [agent] [fix] [完成] `PhyAgentOS/agent/prompt_context.py:L719-L734` 对所有 `forge_task_*` 最新和历史结果使用既有 bounded reference projection。(local)
- [Agent] [Fix] [Completed] `PhyAgentOS/agent/prompt_context.py:L719-L734` applies the existing bounded reference projection to latest and historical `forge_task_*` results. (local)
- [tests] [feat] [完成] `tests/test_prompt_context.py:L136-L169` 覆盖 task/revision/plan/invocation/destination 身份字段保留，并验证 ToolSpec schema 与大型 SkillUse 内容被裁减。(local)
- [Tests] [Feat] [Completed] `tests/test_prompt_context.py:L136-L169` verifies retention of task/revision/plan/invocation/destination identities and removal of ToolSpec schemas and oversized SkillUse content. (local)
- [eval] [test] [完成] `tests/test_prompt_context.py` `26 passed`；Core `558 passed`（`PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 pytest -p pytest_asyncio.plugin -q tests`）；Ruff、compileall、`git diff --check` 通过。(local)
- [Eval] [Test] [Completed] `tests/test_prompt_context.py` passed `26`; Core passed `558` (`PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 pytest -p pytest_asyncio.plugin -q tests`); Ruff, compileall, and `git diff --check` passed. (local)

### 代码 Diff / Code diff

```diff
# PhyAgentOS/agent/prompt_context.py:L724-L734
- name == "forge_task_get"
+ name.startswith("forge_task_")

# tests/test_prompt_context.py:L141-L169
+ assert result["task_id"] == "task-rgb"
+ assert result["status"] == "executing"
+ assert result["plan_graph_ref"] == "artifact://plan/rgb-2"
+ assert result["invocation_id"] == "invocation-place-red-1"
+ assert "input_schema" not in content
```

### Git 提交 / Git commit

- Commit: `eee2a2a`
- Branch: `feature/planning-loop`

## v11.4.12 (2026-09-23 23:05) - codex

### 变更计划 / Planned changes

- [agent] [fix] [计划] 修正 `manipulation_intent_v2` 选择参数合并：当节点已冻结完整 `intent`、Agent 仅补充来源得到的 `allowed_arms` 等语义字段时，按字段合并；相同字段冲突仍拒绝，Coordinator 身份字段仍禁止由 Agent 提供。(local)
- [Agent] [Fix] [Planned] Fix `manipulation_intent_v2` selection merging so a node-frozen complete `intent` can be supplemented by source-derived fields such as `allowed_arms`; conflicting fields remain rejected and Coordinator identity fields remain Agent-owned forbidden. (local)
- [tests] [feat] [计划] 增加完整冻结 intent、部分补充、冲突字段和 Coordinator-owned 字段的多输入无运动回归，并复跑 Core 与相关静态检查。(local)
- [Tests] [Feat] [Planned] Add varied no-motion regressions for complete frozen intent, partial supplementation, conflicting fields, and Coordinator-owned fields, then rerun Core and static checks. (local)

### 失败场景与 Anti-OverDefense 依据 / Failure scenario and rationale

最新任务日志显示 `allowed_arms` 已由当前 capability 快照提供，但 Agent 反复在选择层被拒绝。现有实现把部分 flat intent 当作完整字典与冻结节点 intent 做全量相等比较，导致合法补充无法合并；这不是需要新 gate/hash 的问题，普通字段合并与冲突校验即可保持现有 Coordinator/ToolSpec 约束。

The latest task log shows `allowed_arms` available from the current capability snapshot while selection repeatedly fails. The implementation treats a partial flat intent as a complete dictionary and compares it for full equality with the frozen node intent, preventing valid supplementation. No new gate or hash is needed; field merge with conflict checks preserves existing Coordinator and ToolSpec constraints.

## v11.4.13 (2026-09-24 00:20) - codex

### 变更计划 / Planned changes

- [agent] [fix] [计划] 修正 `PlanningLoopAdapter` 在 Action `outcome_unknown` 后只返回 blocked 但不持久化任务恢复状态的问题；复用既有 `record_planning_node_blocked` 将任务收敛到 `awaiting_replan`，不重发 Action、不伪造终态。(local)
- [Agent] [Fix] [Planned] Fix `PlanningLoopAdapter` returning blocked after Action `outcome_unknown` without persisting recovery state; reuse existing `record_planning_node_blocked` to converge the task to `awaiting_replan`, without resending the Action or fabricating a terminal outcome. (local)
- [tests] [feat] [计划] 增加 unknown settlement 生命周期回归，验证任务不再保持 executing、Action invocation 只保留一次，并保持现有 Core/AgentLoop 测试通过。(local)
- [Tests] [Feat] [Planned] Add an unknown-settlement lifecycle regression proving the task does not remain executing, the Action invocation is retained once, and existing Core/AgentLoop tests remain passing. (local)

### 失败场景与架构依据 / Failure scenario and architecture rationale

真实任务 `task_de08ab12b38b4afa` 的 `red_place` 已记录 `outcome_unknown/action_poll_budget_exhausted`，但任务仍为 `executing`。这违反 PAOS 中 Coordinator 负责生命周期权威、AgentLoop 失败必须可恢复或安全停止的原则；已有 `record_planning_node_blocked` 正是该边界的持久化机制，因此不新增状态、重试或安全 gate。

The real task `task_de08ab12b38b4afa` recorded `outcome_unknown/action_poll_budget_exhausted` for `red_place` while the task remained `executing`. That violates PAOS ownership of lifecycle state and the AgentLoop requirement that failures converge to recovery or safe stop. The existing `record_planning_node_blocked` is the proper persistence boundary, so no new state, retry, or safety gate is added.

### 实际修改与验收 / Completed changes and acceptance

- [agent] [fix] [完成] `PhyAgentOS/agent/planning_dispatch.py:L696-L732` 增加字段级 intent 合并；冻结节点提供完整语义时，Agent 只补充 `allowed_arms` 等缺失字段，冲突字段仍拒绝，Coordinator-owned 字段仍由后续白名单拒绝。(local)
- [Agent] [Fix] [Completed] `PhyAgentOS/agent/planning_dispatch.py:L696-L732` adds field-level intent merging; a frozen node supplies complete semantics while the Agent may supplement missing fields such as `allowed_arms`, conflicting fields remain rejected, and Coordinator-owned fields remain rejected by the existing allowlist. (local)
- [tests] [feat] [完成] `tests/test_planning_selection.py:L1118-L1185` 覆盖完整冻结 intent + 部分 `allowed_arms` 输入；原有缺失参数、冲突语义和身份字段回归保持通过。(local)
- [Tests] [Feat] [Completed] `tests/test_planning_selection.py:L1118-L1185` covers complete frozen intent plus partial `allowed_arms` input; existing missing-argument, semantic-conflict, and identity-field regressions remain passing. (local)
- [eval] [test] [完成] 相关专项连续三轮均 `91 passed`；Core 全量 `552 passed`；Ruff 与 `git diff --check` 通过，未调用 Gateway 或产生 Action。(local)
- [Eval] [Test] [Completed] The focused suite passed `91` in each of three consecutive runs; Core passed `552`; Ruff and `git diff --check` passed, with no Gateway call or Action produced. (local)

### 实际修改与验收 / Completed changes and acceptance

- [agent] [fix] [完成] `PhyAgentOS/agent/planning_loop.py:L1316-L1324` 在 unknown recovery 分支持久化 `planning_node_blocked`，复用 Coordinator 既有 `awaiting_replan` 生命周期；不重发 invocation。(local)
- [Agent] [Fix] [Completed] `PhyAgentOS/agent/planning_loop.py:L1316-L1324` persists `planning_node_blocked` in the unknown recovery branch and reuses the Coordinator's existing `awaiting_replan` lifecycle without resending the invocation. (local)
- [tests] [feat] [完成] `tests/test_planning_loop.py:L1080-L1110` 覆盖 unknown settlement、任务状态收敛和无 Action 重试；原有规划与 AgentLoop 回归保持通过。(local)
- [Tests] [Feat] [Completed] `tests/test_planning_loop.py:L1080-L1110` covers unknown settlement, task-state convergence, and no Action retry; existing planning and AgentLoop regressions remain passing. (local)
- [eval] [test] [完成] PlanningLoop `53 passed`，扩大 Core/AgentLoop 套件 `144 passed`，Ruff 与 diff 检查通过。(local)
- [Eval] [Test] [Completed] PlanningLoop passed `53`, the expanded Core/AgentLoop suite passed `144`, and Ruff plus diff checks passed. (local)
- [eval] [exp] [完成] 三轮真实 CLI AgentLoop 验收：第一轮旧 owner 任务安全取消；第二轮新任务完成 discovery、PlanGraph、grasp/prepare/acquire，并在真实 `object.place` 后按 invocation 记录 `outcome_unknown`，无重发 Action；第三轮 provider timeout 在建任务前停止，数据库无新任务、无 Action。(local)
- [Eval] [Exp] [Completed] Three live CLI AgentLoop runs: the first stale owner task was safely cancelled; the second new task completed discovery, PlanGraph, grasp/prepare/acquire, then recorded `outcome_unknown` after a real `object.place` with no Action resend; the third provider timeout stopped before task creation with no new task or Action in the database. (local)
- [eval] [exp] [完成] 第二轮任务最终通过 Coordinator `task stop` 收敛为 `failed`，保留 `red_place` `outcome_unknown/action_poll_budget_exhausted` settlement；未宣称三方块排列或 Verifier 成功。(local)
- [Eval] [Exp] [Completed] The second task converged to `failed` through Coordinator `task stop`, retaining the `red_place` `outcome_unknown/action_poll_budget_exhausted` settlement; no three-block arrangement or Verifier success is claimed. (local)
## v11.4.14 (2026-09-24 08:35) - codex

### 变更计划 / Planned changes

- [agent] [fix] [完成] 将已有 `ForgeConfig.poll_interval_s` 接入 `AgentLoopNodeExecutor` 的 Action invocation 对账轮询，避免零间隔消耗有限轮询预算后错误产生 `outcome_unknown`；继续由 Coordinator 保存 invocation 生命周期，不重发 Action、不伪造成功。(local)
- [Agent] [Fix] [Completed] Wire the existing `ForgeConfig.poll_interval_s` into `AgentLoopNodeExecutor` Action invocation reconciliation so a zero-delay loop cannot exhaust the bounded poll budget and produce a false `outcome_unknown`; keep invocation lifecycle persistence in the Coordinator without resending Actions or fabricating success. (local)
- [tests] [feat] [完成] 增加配置接线和 pending 到 terminal result 的回归，验证同一 invocation 完成 settlement，unknown 仍保持 fail-closed。(local)
- [Tests] [Feat] [Completed] Add regressions for configuration wiring and pending-to-terminal reconciliation, proving settlement uses the same invocation and unknown remains fail-closed. (local)

### 失败场景与架构依据 / Failure scenario and architecture rationale

真实 `object.place` invocation 在 Gateway 仍未返回 terminal result 时，以零秒间隔连续读取 100 次，随后被本地轮询预算标记为 `action_poll_budget_exhausted` 和 `outcome_unknown`。现有 Forge 配置已经规定了 0.5 秒轮询间隔，但 AgentLoop 没有消费它；这属于参数传递缺失，不需要新增 hash、状态或重试机制。修复只把已有配置传递到现有对账路径，保留 unknown 的安全收敛语义。

When a real `object.place` invocation had not produced a terminal Gateway result, the loop issued 100 reads with zero delay and then recorded `action_poll_budget_exhausted` and `outcome_unknown`. The existing Forge configuration already specifies a 0.5 second poll interval, but the AgentLoop did not consume it. This is a missing parameter handoff, not a need for a new hash, state, or retry mechanism. The fix wires the existing value into the current reconciliation path while preserving fail-closed unknown handling.

### 实际修改与验收 / Completed changes and acceptance

- [agent] [fix] [完成] `PhyAgentOS/agent/planning_loop.py:L592-L625` 将 `action_poll_interval_s` 改为可选；未显式覆盖时读取 `coordinator.config.poll_interval_s`，没有 Forge 配置的测试替身保持零延迟兼容，现有对账循环仍只使用原 invocation。(local)
- [Agent] [Fix] [Completed] `PhyAgentOS/agent/planning_loop.py:L592-L625` makes `action_poll_interval_s` optional; when no override is supplied it reads `coordinator.config.poll_interval_s`, while test doubles without Forge config retain zero-delay compatibility and the reconciliation loop continues using only the original invocation. (local)
- [tests] [feat] [完成] `tests/test_planning_effect_recovery.py:L368-L393` 增加配置接线回归：pending 结果后返回 terminal succeeded，验证同一 invocation 两次 status/result、只启动一次 Action、最终 settlement 成功。(local)
- [Tests] [Feat] [Completed] `tests/test_planning_effect_recovery.py:L368-L393` adds the configuration wiring regression: a pending result followed by terminal success uses two status/result reads for the same invocation, starts one Action, and settles successfully. (local)
- [eval] [test] [完成] `tests/test_planning_effect_recovery.py tests/test_planning_loop.py` 共 `76 passed`；Core 全量在显式加载 `pytest_asyncio` 并隔离 ROS 外部插件后 `554 passed`；Ruff、compileall、`git diff --check` 通过。(local)
- [Eval] [Test] [Completed] `tests/test_planning_effect_recovery.py tests/test_planning_loop.py` passed `76`; the full Core suite passed `554` with `pytest_asyncio` explicitly loaded while ROS external plugins were isolated; Ruff, compileall, and `git diff --check` passed. (local)

### 代码 Diff / Code diff

```diff
# PhyAgentOS/agent/planning_loop.py:L599-L625
- action_poll_interval_s: float = 0.0,
+ action_poll_interval_s: float | None = None,
+ if action_poll_interval_s is None:
+     action_poll_interval_s = getattr(
+         getattr(coordinator, "config", None), "poll_interval_s", 0.0
+     )

# tests/test_planning_effect_recovery.py:L368-L393
+ coordinator.config = SimpleNamespace(poll_interval_s=0.01)
+ executor = AgentLoopNodeExecutor(agent, coordinator)
+ assert executor.action_poll_interval_s == 0.01
+ assert projected.status == "succeeded"
+ assert coordinator.started == 1
+ assert client.status_calls == client.result_calls == 2
```

## v11.4.15 (2026-09-24 09:35) - codex

### 变更计划 / Planned changes

- [agent] [fix] [完成] 压缩重复 discovery 中的 `forge_task_get` 原始全量任务结果；当前 AgentTask 投影已提供权威状态摘要，避免每次只读状态查询把全部 revisions、Tool records 和 SkillUse 重新注入 prompt，导致上下文膨胀和 provider timeout。(local)
- [Agent] [Fix] [Completed] Compact raw `forge_task_get` results during repeated discovery; the existing AgentTask projection already provides the authoritative state summary, so full revisions, Tool records, and SkillUse data are not re-injected on every read-only status query, avoiding context inflation and provider timeouts. (local)
- [tests] [feat] [完成] 增加 latest `forge_task_get` 结果也必须使用有界投影的回归，同时保持完整 Core 与 prompt assembly 测试通过。(local)
- [Tests] [Feat] [Completed] Add a regression proving the latest `forge_task_get` result also uses a bounded projection, while preserving full Core and prompt assembly coverage. (local)

### 失败场景与架构依据 / Failure scenario and architecture rationale

真实 discovery 会话在没有产生新执行事实时连续调用 `forge_task_get`；该工具的最新原始响应绕过了历史 Forge 结果压缩，重复携带完整任务记录，使模型请求从约 18k 增长到约 57k token 并最终超时。Coordinator 的任务投影已经是 AgentLoop 的只读事实边界，压缩 `forge_task_get` 不丢失执行权威，也不改变任务状态、参数或动作对账；普通字段投影足以解决该具体失败场景，无需新增 hash 或状态门禁。

During a real discovery session with no new execution facts, the Agent repeatedly called `forge_task_get`; its latest raw response bypassed historical Forge-result compaction and repeatedly carried the full task record, growing model requests from roughly 18k to 57k tokens before timeout. The Coordinator task projection is already the AgentLoop's read-only fact boundary. Compacting `forge_task_get` does not alter lifecycle state, arguments, or Action reconciliation; the existing bounded projection addresses this concrete failure without a new hash or state gate.

### 实际修改与验收 / Completed changes and acceptance

- [agent] [fix] [完成] `PhyAgentOS/agent/prompt_context.py:L706-L732` 对 `forge_task_get` 的每次结果强制使用现有 bounded reference projection；其余 Tool 的 latest-result 保留策略不变。(local)
- [Agent] [Fix] [Completed] `PhyAgentOS/agent/prompt_context.py:L706-L732` forces every `forge_task_get` result through the existing bounded reference projection; latest-result retention for other Tools is unchanged. (local)
- [tests] [feat] [完成] `tests/test_prompt_context.py:L136-L155` 验证最新 `forge_task_get` 结果会移除大 `revisions/skill_uses` 内容并保持摘要版本标识。(local)
- [Tests] [Feat] [Completed] `tests/test_prompt_context.py:L136-L155` verifies the latest `forge_task_get` result removes oversized `revisions/skill_uses` content while retaining the summary version marker. (local)
- [eval] [test] [完成] Prompt/planning 专项 `117 passed`；Core 全量在显式加载 `pytest_asyncio` 并隔离 ROS 外部插件后 `555 passed`；Ruff、compileall、`git diff --check` 通过。(local)
- [Eval] [Test] [Completed] Prompt/planning focused tests passed `117`; the full Core suite passed `555` with `pytest_asyncio` explicitly loaded while ROS external plugins were isolated; Ruff, compileall, and `git diff --check` passed. (local)

### 代码 Diff / Code diff

```diff
# PhyAgentOS/agent/prompt_context.py:L724-L732
- and (aggressive or latest_by_name.get(name) != index)
+ and (
+     name == "forge_task_get"
+     or aggressive
+     or latest_by_name.get(name) != index
+ )

# tests/test_prompt_context.py:L136-L155
+ projected = _compact_forge_results(messages, aggressive=False)
+ assert len(projected[0]["content"]) < len(raw)
+ assert json.loads(projected[0]["content"])["version"] == "agent_tool_result_summary_v1"
```
## v11.5.2 (2026-09-24 02:38) - codex

### 变更计划 / Planned changes

- [agent] [fix] [完成] 将已有精确 `argument_sources` 解析复用于 PlanGraph 前的 task-bound `forge_tool_query`，允许后续 Query 从同一 AgentTask、当前 revision 的成功 Query 记录复制字段，避免 Agent 手工重组 scene.observe 参数；保留 Gateway/schema 校验与 Coordinator 审计。(local)
- [Agent] [Fix] [Completed] Reuse exact `argument_sources` resolution for task-bound `forge_tool_query` before PlanGraph materialization, allowing follow-up Queries to copy fields from successful Query records in the same AgentTask and active revision instead of manually rebuilding scene observation arguments; retain Gateway/schema validation and Coordinator audit. (local)
- [tests] [feat] [完成] 覆盖三种 scene/frame/artifact 输入形状、成功与失败来源筛选、literal/source 冲突、artifact `map_field` 提取及 Action Tool schema 隔离。(local)
- [Tests] [Feat] [Completed] Cover three scene/frame/artifact input shapes, successful and failed source filtering, literal/source conflicts, artifact `map_field` extraction, and isolation from the Action Tool schema. (local)

### 失败场景与架构依据 / Failure scenario and architecture rationale

真实任务 `task_f391f6a0149b43b7` 的 `scene.observe` 成功记录已含 `scene_revision`、frame、calibration、freshness 和 artifacts；Agent 的 `scene.understand` 仅传了 `observation_ref`，Gateway 因 `scene_revision` 缺失返回 `invalid_scene_revision`，之后观测源又不可用。现有 `argument_sources` 解析只挂在 PlanGraph node selection 上，而 discovery Query 必须在 PlanGraph 物化前完成，所以 Agent 被迫手工复制上游字段。复用同任务成功 Query 记录的精确路径解析可避免字段值重组；不增加 hash、状态门禁或物理重试。

In real task `task_f391f6a0149b43b7`, a successful `scene.observe` record contained `scene_revision`, frame, calibration, freshness, and artifacts; the Agent sent only `observation_ref` to `scene.understand`, Gateway returned `invalid_scene_revision`, and the observation source later became unavailable. Existing `argument_sources` resolution is available only during PlanGraph node selection, while discovery Queries must finish before graph materialization, forcing the Agent to rebuild upstream values manually. Reusing exact paths from successful same-task Query records removes this field-copy failure without adding hashes, state gates, or physical retries.

### 实际修改与验收 / Completed changes and acceptance

- [agent] [refactor] [完成] `PhyAgentOS/agent/argument_sources.py:L1-L143` 提取路径校验、记录读取、嵌套目标写入和数组 `map_field` 投影；`planning_loop.py:L379-L388,L422-L446` 复用该实现。(local)
- [Agent] [Refactor] [Completed] `PhyAgentOS/agent/argument_sources.py:L1-L143` extracts path validation, record reads, nested target writes, and array `map_field` projection; `planning_loop.py:L379-L388,L422-L446` reuses it. (local)
- [agent] [fix] [完成] `PhyAgentOS/agent/tools/forge_tool_api.py:L123-L136,L147-L214,L582-L602` 为 task-bound `forge_tool_query` 增加来源选择；只暴露当前 revision 中每个 Tool 最新、成功且 domain status 可用的 Query 记录，复制后仍由 Coordinator/Gateway 正常校验和留档。(local)
- [Agent] [Fix] [Completed] `PhyAgentOS/agent/tools/forge_tool_api.py:L123-L136,L147-L214,L582-L602` adds source selection to task-bound `forge_tool_query`; it exposes only the latest successful Query per Tool in the active revision with an available domain status, then retains normal Coordinator/Gateway validation and recording. (local)
- [agent] [fix] [完成] `PhyAgentOS/agent/tools/planning.py:L86-L87,L184-L200` 复用路径 schema，并允许数组 source 通过显式 `map_field` 提取 `ref`，Action 参数 schema 不开放该能力。(local)
- [Agent] [Fix] [Completed] `PhyAgentOS/agent/tools/planning.py:L86-L87,L184-L200` reuses the path schema and permits explicit `map_field` extraction such as artifact `ref`; the Action argument schema does not expose this capability. (local)
- [tests] [feat] [完成] `tests/test_forge_tool_api.py:L43-L257` 覆盖 1/2/3 个 artifact 的三种输入、active revision/latest query 过滤、invalid latest 不回退、来源不可见、冲突拒绝、task owner 必需及只读 Query/Action schema 边界。(local)
- [Tests] [Feat] [Completed] `tests/test_forge_tool_api.py:L43-L257` covers three inputs with one/two/three artifacts, active-revision/latest-query filtering, no fallback from an invalid latest result, invisible sources, conflict rejection, required task ownership, and the read-only Query/Action schema boundary. (local)
- [eval] [test] [完成] 相关 AgentLoop/参数来源专项连续三轮均 `100 passed`；Core `567 passed`；Ruff、compileall、`git diff --check` 通过。(local)
- [Eval] [Test] [Completed] The focused AgentLoop/argument-source suite passed `100` in each of three consecutive runs; Core passed `567`; Ruff, compileall, and `git diff --check` passed. (local)
- [eval] [exp] [完成] 前置复现任务 `task_f391f6a0149b43b7` 在 `scene.observe` 成功后因漏传 `scene_revision` 被 Gateway 拒绝并安全取消；零 Action、零 PlanGraph、未运行 Verifier，不能计为 RGB 任务成功。(local)
- [Eval] [Exp] [Completed] Pre-fix reproduction `task_f391f6a0149b43b7` was safely cancelled after Gateway rejected `scene.understand` for missing `scene_revision`; zero Actions and no PlanGraph or Verifier ran, so this is not an RGB task success. (local)

### 代码 Diff / Code diff

```diff
# PhyAgentOS/agent/tools/forge_tool_api.py:L129-L136
+ use argument_sources to copy exact fields from a successful same-task Query
+ scene.understand artifact refs use map_field="ref"

# PhyAgentOS/agent/tools/forge_tool_api.py:L188-L198
+ resolved_arguments = resolve_argument_sources(
+     _task_query_source_records(task), arguments, argument_sources
+ )

# tests/test_forge_tool_api.py:L116-L130
+ "scene_revision": {"record_id": ..., "path": ["response", "scene_revision"]}
+ "artifacts": {"record_id": ..., "path": ["response", "artifacts"], "map_field": "ref"}
```

### Git 提交 / Git commit

- Implementation commit: `0932ca5`
- Branch: `feature/planning-loop`

## v11.5.3 (2026-09-24 02:52) - codex

### 变更计划 / Planned changes

- [agent] [fix] [完成] `persistent_host.py:L249-L273,L613-L619` 将 profile 的 `sensor_ref`/`max_observation_age_ms` 发布为 `scene.observe` schema defaults；`persistent_runtime.py:L165-L170,L294-L299` 保留这些 defaults；`forge/task.py:L2165-L2194` 仅补 Query 缺失字段并持久化 effective arguments。(local)
- [Agent] [Fix] [Completed] `persistent_host.py:L249-L273,L613-L619` publishes profile `sensor_ref`/`max_observation_age_ms` as `scene.observe` schema defaults; `persistent_runtime.py:L165-L170,L294-L299` preserves them; `forge/task.py:L2165-L2194` fills only missing Query fields and persists effective arguments. (local)
- [agent] [fix] [完成] `prompt_context.py:L83-L92,L244-L249` 只为带 invocation ID 的 unresolved Action 开放 terminal-task status/result；`forge/task.py:L2312-L2368,L3220-L3223` 接受迟到 Gateway 观察、保留任务 terminal 状态，并在所有 Action/Session 均明确终结且不含 unknown 时释放 Runtime binding。(local)
- [Agent] [Fix] [Completed] `prompt_context.py:L83-L92,L244-L249` exposes terminal-task status/result only for unresolved Actions with an invocation ID; `forge/task.py:L2312-L2368,L3220-L3223` persists late Gateway observations, preserves task terminality, and releases the Runtime binding only after every Action/Session is known terminal with no unknown outcomes. (local)
- [tests] [feat] [完成] profile 默认值/ToolSpec 投影、Coordinator 缺失字段填充/显式覆盖/来源值优先/Query-only 与无默认值不猜值均有回归；AgentLoop/Core 专项连续三轮每轮 `105 passed`，Core `572 passed`、Skill `347 passed`、Adapter `643 passed, 1 skipped`。(local)
- [Tests] [Feat] [Completed] Regressions cover profile defaults/ToolSpec projection, Coordinator missing-field filling/explicit override/source precedence/Query-only behavior, and no inference without defaults; AgentLoop/Core focused tests passed `105` in each of three runs, Core `572`, Skill `347`, and Adapter `643 passed, 1 skipped`. (local)
- [tests] [chore] [完成] 整理扩展后 `test_prepared_routes.py:L1-L10` 的 Ruff import 分组，不改变测试行为。(local)
- [Tests] [Chore] [Completed] Normalize Ruff import grouping in the extended `test_prepared_routes.py:L1-L10` without changing test behavior. (local)

### 失败场景与 Anti-OverDefense 依据 / Failure scenario and Anti-OverDefense rationale

新任务 `task_8422730f808f44bf` 的 Oracle Runtime 及 Gateway 均 ready，但当前 Runtime context 只声明 readiness，不含 `scene.observe` 必填 `sensor_ref` 与 `max_age_ms`。`camera/head` 已由本任务所绑定的 RobotWin profile 声明；年龄阈值此前仅散见于旧调用/测试，未由运行配置所有。Agent 因无权猜测而请求用户补值，任务进入 `waiting_for_user`，零 Query、零 PlanGraph、零 Action。现有 Runtime ToolSpec、Task binding 和 Gateway schema 未传递实际 profile 参数；把 profile 值写入冻结 ToolSpec 并由 Coordinator 补缺能修复来源断层，不新增 hash、状态 gate 或重试。

For new task `task_8422730f808f44bf`, the Oracle Runtime and Gateway were ready, but live context exposed readiness only and omitted the required `scene.observe` `sensor_ref` and `max_age_ms`. The bound RobotWin profile declares `camera/head`; the age threshold existed only in prior calls/tests and had no runtime configuration owner. Since the Agent could not invent either value, it requested user input and the task entered `waiting_for_user` with zero Queries, PlanGraph, or Actions. Existing Runtime ToolSpec, task binding, and Gateway schema did not carry the configured profile values. Freezing profile defaults into ToolSpec and letting the Coordinator fill omissions closes that source gap without adding hashes, state gates, or retries.

此前的 `object.place` invocation 在有限轮询预算后被记录为 `unknown`，AgentTask 随后进入 terminal `failed`。status/result Agent tools 虽已注册，`visible_tool_names` 却对 terminal task 隐藏了对账工具，`AgentTaskCoordinator.observe_action` 也会直接丢弃 terminal task 的后续观察，因此即使 Gateway result 仍可读取，原 invocation 与 Runtime binding 仍无法对账。只允许针对同一任务已记录的 invocation 做只读对账；任务保持 terminal，绝不重发 Action。

The previous `object.place` invocation was recorded as `unknown` after its bounded poll budget, and the AgentTask then became terminal `failed`. Although the status/result Agent tools are registered, `visible_tool_names` hid them for terminal tasks and `AgentTaskCoordinator.observe_action` discarded later observations, leaving the original invocation and Runtime binding unreconciled even when Gateway result remained readable. Permit read-only reconciliation only for an invocation already recorded by the same task; preserve terminal task status and never resend the Action.

### 预期影响文件 / Expected files

- `PhyAgentOS/forge/task.py`
- `examples/forge-skills/pick-place-workflow/src/pick_place_workflow/persistent_runtime.py`
- `examples/forge-adapters/robotwin20/src/robotwin20_adapter/persistent_deployment.py`
- `examples/forge-adapters/robotwin20/src/robotwin20_adapter/persistent_host.py`
- `examples/forge-adapters/robotwin20/runtime/robotwin_backend.py`
- `examples/forge-adapters/robotwin20/profiles/robotwin20/franka-blocks-ranking.yaml`
- `examples/forge-skills/pick-place-workflow/skill.yaml`, `pyproject.toml`, and package changelog
- Core and RoboTwin tests for frozen Query defaults
- `examples/forge-skills/pick-place-workflow/tests/test_grasp_propose.py` for the v2.6.18 bundle/package version assertion
- `PhyAgentOS/agent/prompt_context.py` and `tests/test_prompt_context.py` for terminal-task reconciliation visibility
- `PhyAgentOS/forge/task.py` and `tests/test_agent_foundation.py` for late result persistence and invocation ownership

### 实际修改 / Completed changes

- [adapter] [fix] [完成] `franka-blocks-ranking.yaml:L6-L7` 配置正整数观察年龄；`robotwin_backend.py:L104-L125` 加载并校验 profile 值；`persistent_deployment.py:L104-L128` 将当前 profile 传入 Host；`persistent_host.py:L249-L273,L613-L619` 生成并发布 `scene.observe` defaults；`prepared_routes.py:L69-L76` 保持来源 provenance 校验，并允许 place 执行 scene revision 前进。(local)
- [Adapter] [Fix] [Completed] `franka-blocks-ranking.yaml:L6-L7` configures a positive observation age; `robotwin_backend.py:L104-L125` loads and validates profile values; `persistent_deployment.py:L104-L128` passes the profile into the Host; `persistent_host.py:L249-L273,L613-L619` publishes `scene.observe` defaults; `prepared_routes.py:L69-L76` preserves source provenance checks while allowing the place execution scene revision to advance. (local)
- [skill] [fix] [完成] `persistent_runtime.py:L165-L170,L294-L299` 保留 schema defaults；`object_place.py:L40-L46,L325-L354` 将当前 scene revision 与 acquire provenance 分开校验；`README.md:L71-L77`、`SKILL.md:L256-L262` 更新运行约束；包/manifest 升至 `2.6.18`，Node 锁升级至 `0.6.15`；`test_grasp_propose.py:L260-L270` 修正版本断言。(local)
- [Skill] [Fix] [Completed] `persistent_runtime.py:L165-L170,L294-L299` preserves schema defaults; `object_place.py:L40-L46,L325-L354` validates the current scene revision separately from acquire provenance; `README.md:L71-L77` and `SKILL.md:L256-L262` document the runtime contract; package/manifest is `2.6.18` with Node lock `0.6.15`; `test_grasp_propose.py:L260-L270` aligns the version assertion. (local)
- [agent] [fix] [完成] `prompt_context.py:L83-L92,L244-L249` terminal task 只暴露已记录 unresolved Action 的 status/result；`forge/task.py:L2312-L2368` 保存同一 invocation 的迟到响应，只在 Gateway 确定终态时释放 invocation；`L2349-L2368,L3220-L3223` 仅当所有 Action/Session 明确终结后释放 Runtime task binding，任务状态不变。(local)
- [Agent] [Fix] [Completed] `prompt_context.py:L83-L92,L244-L249` exposes status/result only for a recorded unresolved Action on a terminal task; `forge/task.py:L2312-L2368` persists a late response for that invocation and releases it only on a known Gateway terminal result; `L2349-L2368,L3220-L3223` releases the Runtime task binding only after all Actions/Sessions are known terminal, without changing task status. (local)
- [tests] [feat] [完成] `test_persistent_host.py:L35-L50,L309-L315`、`test_persistent_deployment.py:L149-L162`、`test_robotwin_backend_contract.py:L96-L119`、`test_prepared_routes.py:L48-L52` 覆盖 profile defaults 与 route 输入；`test_object_place.py:L140-L153`、`test_persistent_runtime.py:L81-L114` 覆盖 Skill contract；`test_planning_task_integration.py:L205-L281` 覆盖 Query defaults/覆盖；`test_prompt_context.py:L390-L416`、`test_agent_foundation.py:L1046-L1090` 覆盖 terminal Action 对账。(local)
- [Tests] [Feat] [Completed] `test_persistent_host.py:L35-L50,L309-L315`, `test_persistent_deployment.py:L149-L162`, `test_robotwin_backend_contract.py:L96-L119`, and `test_prepared_routes.py:L48-L52` cover profile defaults and route inputs; `test_object_place.py:L140-L153` and `test_persistent_runtime.py:L81-L114` cover the Skill contract; `test_planning_task_integration.py:L205-L281` covers Query defaults/overrides; `test_prompt_context.py:L390-L416` and `test_agent_foundation.py:L1046-L1090` cover terminal Action reconciliation. (local)

### 代码 Diff / Code Diff

```diff
# AgentLoop terminal-task projection
-terminal task -> task create/get/context only
+terminal task with unresolved owned Action -> status/result for that invocation

# Coordinator late invocation observation
-if task.terminal: return
+persist status/result against the existing task-owned invocation
+release invocation and Runtime binding only after known Gateway terminal result

# Coordinator Query argument completion
+copy frozen ToolSpec defaults into missing Query fields
+persist and invoke with those effective arguments; explicit values remain unchanged

# object.place scene/provenance contract
-require acquire observation scene == current place execution scene
+validate provenance references against their source scene and validate the current execution scene separately
```

### 验证边界 / Verification boundary

- AgentLoop/Core focused suites: three runs, each `105 passed`.
- Core: `572 passed`; Skill: `347 passed`; RobotWin Adapter: `643 passed, 1 skipped`.
- Ruff, compileall, and `git diff --check` passed. No Gateway Action, Dora, or robot was started. This verifies local control-plane and contract behavior; it does not count as physical RGB task acceptance.

### Git 提交 / Git Commit

- Implementation commit: `a5ed78b`
- Branch: `feature/planning-loop`
- Time: `2026-09-24 03:29` Asia/Shanghai

## v11.5.4 (2026-09-24 03:56) - codex

### 变更内容 / Changes

- [sense] [fix] [完成] 将 `OpenAIResponsesSceneUnderstandingInference._user_prompt` 中固定的 desktop organization 描述改为通用可见实体、颜色/外观和相对布局任务；保留已有 base64 RGB 传递与 provenance 验证，不使用仿真真值。(local)
- [Sense] [Fix] [Completed] Replace the fixed desktop-organization description in `OpenAIResponsesSceneUnderstandingInference._user_prompt` with a generic visible-entity, appearance/color, and relative-layout task; retain existing base64 RGB transport and provenance validation without using simulator truth. (local)
- [tests] [feat] [完成] 扩展 provider payload 回归，验证多模态请求文本没有桌面任务偏置、RGB artifact 仍绑定 image input；升级 Adapter/Node/Skill 版本并重建 bundle。(local)
- [Tests] [Feat] [Completed] Extend provider-payload regressions to verify that multimodal request text has no desktop-task bias while the RGB artifact remains bound to the image input; bump Adapter/Node/Skill versions and rebuild the bundle. (local)

### 失败场景与 Anti-OverDefense 依据 / Failure scenario and rationale

Fresh task `task_fd06bac326e24839` captured a valid 320x240 RGB artifact (94,731 bytes) plus depth/state artifacts. The exact `artifact://` RGB reference resolved to that saved image, which visibly contains the red, green, and blue blocks. The OpenAI-compatible provider received a base64 `input_image`, but `scene.understand` returned `entity_count_uncertain` with “observation image is unavailable in the supplied conversation.” The existing request already had image resolution, base64 transport, and artifact provenance; these mechanisms were not the gap. Its user text hard-coded `desktop organization scene understanding`, while tests used only desktop-tidy fixtures, so the cross-task prompt mismatch was untested. Remove that stale domain label and test the actual generic prompt. The task stopped before any Action.

### 预期影响文件 / Expected files

- `examples/forge-adapters/robotwin20/src/robotwin20_adapter/openai_scene_understanding.py`
- `examples/forge-adapters/robotwin20/tests/test_openai_scene_understanding.py`
- `examples/forge-adapters/robotwin20/pyproject.toml`
- `examples/forge-skills/pick-place-workflow/skill.yaml`, `pyproject.toml`, and `CHANGELOG.md`
- `examples/forge-skills/pick-place-workflow/tests/test_grasp_propose.py`
- `CHANGELOG.md`
- Local self-contained Node and Skill Bundle artifacts under `/tmp/paos-robotwin20-release`

### 实际修改与无运动验收 / Completed changes and no-motion acceptance

- [sense] [fix] [完成] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/openai_scene_understanding.py:L415-L430` 将固定 desktop task label 改为通用可见实体、外观和相对布局请求；保留现有 image artifact/base64 和 provenance 路径。(local)
- [Sense] [Fix] [Completed] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/openai_scene_understanding.py:L415-L430` replaces the fixed desktop task label with a generic visible-entity, appearance, and relative-layout request; existing image artifact/base64 and provenance paths are retained. (local)
- [tests] [feat] [完成] `examples/forge-adapters/robotwin20/tests/test_openai_scene_understanding.py:L87-L102` 验证无 desktop 文本偏置且图像仍作为 base64 image input；Adapter `13 passed`。(local)
- [Tests] [Feat] [Completed] `examples/forge-adapters/robotwin20/tests/test_openai_scene_understanding.py:L87-L102` verifies no desktop prompt bias and retained base64 image input; Adapter passed `13`. (local)
- [release] [chore] [完成] Adapter `pyproject.toml:L1-L4`、Skill `pyproject.toml:L1-L4`、`skill.yaml:L151-L158`、`tests/test_grasp_propose.py:L260-L270` 对齐 Adapter/Node `0.7.0`、Skill `2.6.19`；bundle 已构建并安装到 Oracle Runtime。(local)
- [Release] [Chore] [Completed] Adapter `pyproject.toml:L1-L4`, Skill `pyproject.toml:L1-L4`, `skill.yaml:L151-L158`, and `tests/test_grasp_propose.py:L260-L270` align Adapter/Node `0.7.0` and Skill `2.6.19`; the bundle was built and installed into the Oracle Runtime. (local)
- [eval] [test] [完成] Skill `347 passed`、Core `579 passed`；无运动。完整 RGB AgentLoop 尚待执行。(local)
- [Eval] [Test] [Completed] Skill passed `347`, Core passed `579`; no motion. Full RGB AgentLoop remains pending. (local)

```diff
-"task": "desktop organization scene understanding"
+"task": "Identify visible entities, their count and appearance, and their relative spatial layout from the supplied RGB image."
```

## v11.5.5 (2026-09-24 04:13) - codex

### 变更内容 / Changes

- [agent] [fix] [完成] 对 follow-up `scene.understand` 和 `manipulation.capabilities`，由 PAOS 从当前 revision 最新成功的 `scene.observe` Query 收据投影 scene identity、frame、calibration、freshness 和 artifact refs；这些来源字段不再由 Agent 重组或覆盖。(local)
- [Agent] [Fix] [Completed] For follow-up `scene.understand` and `manipulation.capabilities`, PAOS projects scene identity, frame, calibration, freshness, and artifact refs from the latest successful `scene.observe` Query receipt in the active revision; the Agent no longer reconstructs or overrides these source fields. (local)
- [tests] [feat] [完成] 覆盖三种传感器/artifact 数量、错误 literal 被来源值替代、来源 Query 失效时 no-Gateway、以及保留调用者拥有的非来源字段；运行 Forge Tool API/AgentLoop/Core no-motion 回归。(local)
- [Tests] [Feat] [Completed] Cover three sensor/artifact shapes, replacement of wrong literals by source values, no Gateway call for unavailable source Queries, and preservation of caller-owned non-source fields; run Forge Tool API, AgentLoop, and Core no-motion regressions. (local)

### 失败场景与 Anti-OverDefense 依据 / Failure scenario and rationale

新任务 `task_0dae469e6f064b63` 的 `scene.observe` 成功并返回 calibration ref `artifact://3958293cf4524b1dbc16f383e6ed3b8a-1/.../calibration`。随后 Agent 手工复制时将其改成 `...3958293cf4524b4b1dbc...`，产生 schema 格式合法但磁盘不存在的 opaque ref；Adapter 只返回 `provider_unavailable: RouteEvidenceError`，任务卡在 executing，零 PlanGraph、零 Action。v11.5.2 已提供 Agent 选择 `argument_sources` 的可选机制，但本次真实 loop 未采用它。普通 schema 只能验证 URI 形状，不能发现 opaque ref 的字符抄错；将标准 follow-up Query 的来源字段改由 PAOS 从本 task、active revision 的成功 scene.observe 收据自动解析，能移除 LLM 重组值这一故障路径，不新增 hash、重试或物理 Action gate。任务已通过 `paos task stop` 取消。

In fresh task `task_0dae469e6f064b63`, `scene.observe` succeeded and returned calibration ref `artifact://3958293cf4524b1dbc16f383e6ed3b8a-1/.../calibration`. The Agent manually retyped it as `...3958293cf4524b4b1dbc...`, creating a syntactically valid but nonexistent opaque ref; the Adapter returned only `provider_unavailable: RouteEvidenceError`, the task remained executing, and no PlanGraph or Action ran. v11.5.2 already offered optional Agent-selected `argument_sources`, but this live loop did not use them. Ordinary schema validation checks URI shape, not copied opaque-ref identity. Resolving standard follow-up Query fields inside PAOS from the successful `scene.observe` receipt owned by the same task and active revision removes this LLM reconstruction failure path without adding hashes, retries, or physical Action gates. The task was cancelled with `paos task stop`.

### 预期影响文件 / Expected files

- `PhyAgentOS/agent/tools/forge_tool_api.py`
- `tests/test_forge_tool_api.py`
- `CHANGELOG.md`
- `changelog/2026-09_part13.md`

### 实际修改与验收 / Completed changes and acceptance

- [agent] [fix] [完成] `PhyAgentOS/agent/tools/forge_tool_api.py:L32-L52,L145-L161,L211-L253,L651-L681` 为 `manipulation.capabilities`、`scene.understand` 声明 observation-owned 参数投影；从 active revision 最新成功、domain status 可用的 `scene.observe` receipt 复制 scene/ref/frame/freshness/artifact 字段，Agent 提供的同名值不参与最终 Query 参数。(local)
- [Agent] [Fix] [Completed] `PhyAgentOS/agent/tools/forge_tool_api.py:L32-L52,L145-L161,L211-L253,L651-L681` declares observation-owned argument projections for `manipulation.capabilities` and `scene.understand`; it copies scene/ref/frame/freshness/artifact fields from the latest successful, domain-available `scene.observe` receipt in the active revision, excluding same-named Agent values from effective Query arguments. (local)
- [tests] [feat] [完成] `tests/test_forge_tool_api.py:L155-L259` 覆盖 RGB-only、RGB+depth、RGB+depth+state 三类输入对两种 follow-up Query 的投影；错误 literal 被准确 receipt 替代，其他业务参数保留，缺失有效 scene.observe 时 Gateway 不调用。Core `579 passed`，AgentLoop/API 专项 `70 passed`；Ruff、compileall、`git diff --check` 通过。(local)
- [Tests] [Feat] [Completed] `tests/test_forge_tool_api.py:L155-L259` covers RGB-only, RGB+depth, and RGB+depth+state projection for both follow-up Queries; wrong literals are replaced by the exact receipt, other business arguments are preserved, and Gateway is not called without an available scene observation. Core passed `579`, AgentLoop/API focused tests `70`; Ruff, compileall, and `git diff --check` passed. (local)

```diff
-agent-provided observation refs -> passed through as Query literals
+scene-bound fields -> exact same-task scene.observe receipt projection
+missing available scene.observe -> fail before Gateway
```

### 验收边界 / Acceptance boundary

The reproduced AgentTask was cancelled before PlanGraph materialization and made no physical Action. These tests validate PAOS argument provenance and control flow; the RGB arrangement, final ForgeTaskVerifier, and complete saved video are still pending.

### Git 提交 / Git Commit

- Commit: `376bd13`
- Branch: `feature/planning-loop`
- Time: `2026-09-24 04:28` Asia/Shanghai
## v11.5.6 (2026-09-24 04:46) - codex

### 预期修改 / Planned Changes [完成]

- [sense] [fix] 修复 OpenAI 场景理解把真实可见物体漏检为 `entities=[]` 且 `ambiguities=[]` 时仍返回无歧义可用结果的问题；加强对简单几何物体、积木和颜色的图像检查提示；不猜造实体，在空实体且无已有歧义时投影 `entity_count_uncertain`。
- [Sense] [Fix] Fix OpenAI scene understanding returning a clean available result when visible objects are missed as `entities=[]` and `ambiguities=[]`; strengthen image inspection instructions for simple geometric objects, blocks, and colors; do not invent entities, and project `entity_count_uncertain` when entities and ambiguities are both empty.
- [eval] [test] 增加无运动回归，验证空语义结果被标记为不确定、已有歧义不重复添加、已有实体输出不变，并验证完整 OpenAI request prompt 覆盖几何积木/颜色检查。
- [Eval] [Test] Add no-motion regressions proving empty semantic output becomes uncertain, existing ambiguities are not duplicated, existing entity output is unchanged, and the full OpenAI request prompt covers geometric blocks and color inspection.
- [release] [chore] Adapter、persistent Node、Pick-Place Skill 版本分别从 `0.7.0`、`0.7.0`、`2.6.19` 升级到 `0.7.1`、`0.7.1`、`2.6.20`；重建 Node 与 Bundle 并本地安装，运行相关 Adapter/Skill/Core 测试；随后用已修复 Runtime 继续 RGB 三方块 AgentLoop 验收。
- [Release] [Chore] Bump the Adapter, persistent Node, and Pick-Place Skill from `0.7.0`, `0.7.0`, and `2.6.19` to `0.7.1`, `0.7.1`, and `2.6.20`; rebuild and locally install the Node and Bundle, run relevant Adapter/Skill/Core tests, then continue RGB three-block AgentLoop acceptance with the fixed Runtime.
- [agent] [fix] 为模型 turn timeout/error 增加 Coordinator-owned terminal failure 收敛：只有不存在未终态 task-owned Action/Session 时才将当前任务置为 `failed`，并记录结构化 failure event；不改变未知物理状态的 Action 对账。
- [Agent] [Fix] Add Coordinator-owned terminal failure settlement for model turn timeout/error: mark the current task `failed` and record a structured failure event only when no non-terminal task-owned Action/Session exists; keep unknown physical Action reconciliation unchanged.
- [eval] [test] 增加 AgentLoop/Coordinator 回归，验证无执行记录的模型超时不留下 executing 任务，且存在未终态 Action 时不会越过物理对账门槛。
- [Eval] [Test] Add AgentLoop/Coordinator regressions proving a model timeout with no execution record cannot leave an executing task, while a non-terminal Action prevents bypassing physical reconciliation.

### 失败场景与 Anti-OverDefense 依据 / Failure scenario and rationale

Fresh task `task_8629086363d2431d` supplied a valid RGB artifact visibly containing red, green, and blue blocks, while `scene.understand` returned `status=available`, `entities=[]`, and `ambiguities=[]`. Existing artifact/provenance validation proves which image was sent, but cannot prove that the model enumerated its visible objects; normal schema validation accepts empty arrays. This lets a perception miss look like a complete, unambiguous scene and can derail the AgentLoop at later planning. Reuse the existing semantic `entity_count_uncertain` vocabulary at the adapter projection boundary; do not fabricate entity refs, add hashes, duplicate state in PAOS Core, or introduce an execution gate. The reproducer was stopped before any Action.

### 实际修改与无运动验收 / Completed changes and no-motion acceptance

- [sense] [fix] [完成] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/openai_scene_understanding.py:L350-L371,L410-L427` 在 OpenAI 语义投影处对空实体/空歧义结果追加既有 `entity_count_uncertain`，不生成实体；系统提示要求枚举简单几何积木、颜色和左右布局。(local)
- [Sense] [Fix] [Completed] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/openai_scene_understanding.py:L350-L371,L410-L427` adds the existing `entity_count_uncertain` ambiguity to empty-entity/empty-ambiguity OpenAI projections without inventing entities; the system prompt requires enumeration of simple geometric blocks, colors, and left-to-right layout. (local)
- [tests] [feat] [完成] `examples/forge-adapters/robotwin20/tests/test_openai_scene_understanding.py:L89-L100,L150-L186` 覆盖请求提示和空结果投影；Adapter 场景理解专项 `14 passed`。(local)
- [Tests] [Feat] [Completed] `examples/forge-adapters/robotwin20/tests/test_openai_scene_understanding.py:L89-L100,L150-L186` covers the request prompt and empty-result projection; Adapter scene-understanding focused tests passed `14`. (local)
- [agent] [fix] [完成] `PhyAgentOS/forge/task.py:L2029-L2070` 新增 `fail_task`，无未终态 task-owned Action/Session 时将模型/控制面失败持久化为 terminal `failed`；`PhyAgentOS/agent/loop.py:L1586-L1608` 在 turn/model failure 后调用它，遇到未终态物理执行则保留既有对账路径。(local)
- [Agent] [Fix] [Completed] `PhyAgentOS/forge/task.py:L2029-L2070` adds `fail_task` to persist terminal `failed` only when no non-terminal task-owned Action/Session exists; `PhyAgentOS/agent/loop.py:L1586-L1608` invokes it after turn/model failures and preserves existing reconciliation when physical execution remains unresolved. (local)
- [tests] [feat] [完成] `tests/test_agent_foundation.py:L897-L928` 覆盖两种 failure settlement；Core `581 passed`，Skill `347 passed`，Adapter `14 passed`，Ruff、compileall、`git diff --check` 通过；Node `0.7.1` 与 Skill `2.6.20` 已构建、安装和锁定校验。(local)
- [Tests] [Feat] [Completed] `tests/test_agent_foundation.py:L897-L928` covers both failure-settlement cases; Core passed `581`, Skill `347`, Adapter `14`, with Ruff, compileall, and `git diff --check` passing; Node `0.7.1` and Skill `2.6.20` were built, installed, and lock-verified. (local)

```diff
# OpenAI scene projection
{} -> {"ambiguities": [{"code": "entity_count_uncertain", ...}]}

# AgentTask lifecycle after model failure
executing + no unresolved Action/Session -> failed + task_failed_control_plane event
executing + unresolved Action/Session -> preserve status and reconcile invocation
```

### 运行时验收边界 / Runtime acceptance boundary

The new `2.6.20` Skill and `0.7.1` Node are running under the Oracle profile with all ten Forge Tool contexts ready. A fresh AgentLoop created `task_8555ff32abc543ab`, then the provider timed out during discovery before any Query or Action; the new source was not yet installed when that run started, so the task was manually cancelled via the Coordinator. No physical Action or video was produced. A fresh post-fix run is required for RGB acceptance.

### Git 提交 / Git Commit

- Implementation commit: `8481ea1`
- Branch: `feature/planning-loop`
- Time: 2026-09-24 05:24 Asia/Shanghai
## v11.5.7 (2026-09-24 05:40) - codex

### 预期修改 / Planned Changes [完成]

- [env] [tune] 将 Oracle RGB AgentLoop 配置模型从 `gpt-5.6-sol` 切换到 `gpt-6-sol`，保持 custom provider、API key 文件、上下文预算和 `reasoningEffort=high` 不变；当前活动 task 继续使用同一 session，不创建新任务。
- [Env] [Tune] Switch the Oracle RGB AgentLoop configuration model from `gpt-5.6-sol` to `gpt-6-sol`, keeping the custom provider, API key file, context budget, and `reasoningEffort=high` unchanged; continue the active task in the same session without creating a new task.
- [eval] [test] 只读校验配置解析、模型/provider 匹配与当前 task 状态，然后继续受治理的 RGB 三方块 AgentLoop；不把模型切换本身当作物理验收成功。
- [Eval] [Test] Verify configuration parsing and model/provider matching plus the current task state, then continue the governed RGB three-block AgentLoop; do not treat the model switch itself as physical acceptance success.

### 实际修改与验证 / Completed Changes and Validation

- [env] [tune] [完成] `/home/yanxu/.PhyAgentOS/config-rgb-no-evolution-long.json:L4-L15` 已将 Oracle 模型设为 `gpt-6-sol`，保持 custom provider、API key 文件、272K context、`reasoningEffort=high` 和 3600s turn timeout；配置解析与 provider/model 读取校验通过。(local)
- [Env] [Tune] [Completed] `/home/yanxu/.PhyAgentOS/config-rgb-no-evolution-long.json:L4-L15` now selects `gpt-6-sol` for the Oracle run while retaining the custom provider, API key file, 272K context, `reasoningEffort=high`, and 3600s turn timeout; config parsing and provider/model inspection passed. (local)

## v11.5.8 (2026-09-24 06:20) - codex

### 预期修改 / Planned Changes [完成]

- [agent] [fix] 在 Coordinator 计划物化时从同一份有效 `CapabilitySnapshot` 推导 `coordination_mode` 与可用 `arm_id`，冻结到 `manipulation.prepare` 节点；避免 AgentLoop 反复猜测或重新组装运行时参数。
- [Agent] [Fix] During Coordinator plan materialization, derive `coordination_mode` and available `arm_id` values from the same valid `CapabilitySnapshot` and freeze them into `manipulation.prepare` nodes; prevent AgentLoop from repeatedly guessing or reassembling Runtime parameters.
- [eval] [test] 增加单臂、双独立臂和显式参数冲突的无运动回归，验证选择可直接消费冻结参数且不调用 Gateway；运行 Core/Planning 专项与静态检查。
- [Eval] [Test] Add no-motion regressions for single-arm, dual-independent, and explicit-parameter-conflict cases, proving selection can consume frozen parameters without Gateway calls; run Core/Planning focused tests and static checks.

### 实际修改与验证 / Completed Changes and Validation

- [agent] [fix] [完成] `PhyAgentOS/agent/plan_proposal.py:L186-L188,L239-L256,L346-L376` 读取同一有效 `CapabilitySnapshot` 的 `topology` 与可用 arm_id；`single_arm`、`dual_independent`、`dual_coordinated` 分别物化为 `single_arm`、`alternative_arm`、`bimanual`，并冻结可用 arm_id，保留显式非法 arm 的拒绝路径。(local)
- [Agent] [Fix] [Completed] `PhyAgentOS/agent/plan_proposal.py:L186-L188,L239-L256,L346-L376` reads `topology` and available arm_id values from the same valid `CapabilitySnapshot`; materializes `single_arm`, `alternative_arm`, and `bimanual` for the three topologies, freezes available arm IDs, and preserves explicit invalid-arm rejection. (local)
- [tests] [feat] [完成] `tests/test_plan_proposal_bindings.py:L115-L147` 增加双独立臂和单臂缺失语义物化回归；PAOS 环境专项 `47 passed`，完整 Core 在禁用错误 ROS 插件自动加载后 `583 passed`，其中异步专项 `34 passed`；compileall、Ruff、`git diff --check` 通过。该测试文件当前受仓库既有 `tests/*` 忽略规则影响，保留为本地回归证据，不纳入本次提交。(local)
- [Tests] [Feat] [Completed] `tests/test_plan_proposal_bindings.py:L115-L147` adds dual-independent and single-arm missing-semantic materialization regressions; the PAOS focused suite passed `47`, the full Core run passed `583` after disabling the broken ROS plugin autoload, and the async subset passed `34`; compileall, Ruff, and `git diff --check` passed. The test file is affected by the repository's existing `tests/*` ignore rule, so it remains local evidence and is not included in this commit. (local)

```diff
# Coordinator plan materialization
capability snapshot -> available alternative-resource arm IDs only
capability snapshot -> topology-derived coordination_mode + all available arm IDs

# AgentLoop selection
repeated model guesses for allowed_arms/coordination_mode
frozen node bindings consumed directly by the next selection
```

### 验收边界 / Acceptance Boundary

The interrupted run stopped before any Gateway Action and is not a physical acceptance. A fresh post-fix RGB run is still required to complete red, green, and blue placement, terminal retreat evidence, ForgeTaskVerifier, and cumulative video.

### Git 提交 / Git Commit

- Implementation commit: `8e4fe50`
- Branch: `feature/planning-loop`
- Time: 2026-09-24 06:20 Asia/Shanghai

## v11.5.9 (2026-09-24 06:45) - codex

### 预期修改 / Planned Changes [完成]

- [env] [tune] 针对 `gpt-6-sol/high` 在约 13K token 任务创建上下文下的可复现 provider timeout，将 Oracle 输出上限从 `8192` 调整为 `4096`；模型、reasoning、provider、上下文窗口和物理执行约束不变。
- [Env] [Tune] In response to the reproducible provider timeout for `gpt-6-sol/high` on the approximately 13K-token task-creation context, reduce the Oracle output limit from `8192` to `4096`; keep the model, reasoning, provider, context window, and physical execution constraints unchanged.
- [eval] [test] 先做配置解析和小请求连通性校验，再重新执行一次完整三方块 AgentLoop；不把小请求成功视为方块排列成功。
- [Eval] [Test] Validate config parsing and a small-request connectivity smoke before one new full three-block AgentLoop run; do not treat the smoke success as block-arrangement acceptance.

### 实际修改与验证 / Completed Changes and Validation

- [env] [tune] [完成] `/home/yanxu/.PhyAgentOS/config-rgb-no-evolution-long.json:L4-L15` 将 `maxTokens` 从 `8192` 调整为 `4096`，并保持 `model=gpt-6-sol`、`reasoningEffort=high`、custom provider、272K context 与 3600s turn timeout；配置解析通过。(local)
- [Env] [Tune] [Completed] `/home/yanxu/.PhyAgentOS/config-rgb-no-evolution-long.json:L4-L15` reduces `maxTokens` from `8192` to `4096` while retaining `model=gpt-6-sol`, `reasoningEffort=high`, the custom provider, 272K context, and 3600s turn timeout; config parsing passed. (local)
- [env] [chore] [完成] 重启 persistent Oracle Runtime dataflow，并确认运行进程环境为 `ROBOTWIN20_MODEL=gpt-6-sol`、`ROBOTWIN20_REASONING_EFFORT=high`；Gateway 19020 恢复监听。(local)
- [Env] [Chore] [Completed] Restarted the persistent Oracle Runtime dataflow and verified the process environment uses `ROBOTWIN20_MODEL=gpt-6-sol` and `ROBOTWIN20_REASONING_EFFORT=high`; Gateway port 19020 is listening again. (local)
- [eval] [test] [完成] `gpt-6-sol/high` 小请求连通性成功；新 task `task_fd27ab4083cd43dd` 完成感知、绑定、grasp.propose 和 Coordinator 参数选择后在 `manipulation.prepare` 返回 `preparation_provider_error`，无 Action；新 task `task_d0d45cfcd2364588` 完成两轮 observe/understand/capabilities/bind 后在 staging 判断 provider timeout，零 Action。其余尝试均在 discovery/任务规划门禁前安全终止。(local)
- [Eval] [Test] [Completed] A `gpt-6-sol/high` small-request smoke succeeded; `task_fd27ab4083cd43dd` reached perception, binding, grasp.propose, and Coordinator parameter selection before `manipulation.prepare` returned `preparation_provider_error`, with zero Actions; `task_d0d45cfcd2364588` completed two observe/understand/capabilities/bind rounds before a provider timeout during staging analysis, with zero Actions. Other attempts stopped safely before discovery or planning gates. (local)

```diff
# PAOS Oracle configuration
maxTokens: 8192 -> 4096
model/reasoning/provider/context: unchanged

# Runtime process environment
ROBOTWIN20_MODEL=gpt-5.6-sol -> gpt-6-sol
ROBOTWIN20_REASONING_EFFORT=high (unchanged)
```

### 验收边界 / Acceptance Boundary

The parameter-freezing implementation is verified and the live AgentLoop no longer repeats `allowed_arms`/`coordination_mode` assembly. The physical RGB arrangement is not accepted: no run reached `object.acquire`, `object.place`, ForgeTaskVerifier, or cumulative video. The remaining blockers are Runtime preparation/provider failure and the absence of a sensor-backed staging destination for the observed blue-green-red swap; no coordinates or simulator truth were fabricated.

### Git 提交 / Git Commit

- Implementation commit: `0baa2a3`
- Branch: `feature/planning-loop`
- Time: 2026-09-24 06:45 Asia/Shanghai

## v11.5.13 (2026-09-24 09:30) - codex

### 变更计划 / Planned changes [计划]

- [agent] [fix] 为 discovery 阶段的 `forge_tool_context`、`forge_tool_query` 和任务读取结果增加有界、来源保留的 prompt 投影，避免不同 ToolSpec 的完整 schema 和重复任务记录挤占 `gpt-6-sol/high` 的请求上下文；不改变 Coordinator、Gateway、Action reconciliation 或运动门禁。
- [Agent] [Fix] Add bounded, reference-preserving prompt projections for discovery `forge_tool_context`, `forge_tool_query`, and task-read results so full schemas and duplicate task records do not consume the `gpt-6-sol/high` request context; leave Coordinator, Gateway, Action reconciliation, and motion gates unchanged.
- [tests] [feat] 增加不同 discovery 工具结果形状、失败响应和语义结果保留的回归测试，并运行 Prompt/Agent 专项、Ruff、compileall、`git diff --check` 与配置验证。
- [Tests] [Feat] Add regressions for different discovery result shapes, failure responses, and semantic-result retention, then run Prompt/Agent focused tests, Ruff, compileall, `git diff --check`, and configuration validation.

### 失败场景与 Anti-OverDefense 依据 / Failure scenario and rationale

最近一次 `gpt-6-sol/high` 运行在创建任务后将五个不同 ToolSpec 的完整 schema 和 `forge_task_get` 完整任务记录带入后续请求，discovery prompt 从约 18K 增长到约 25K，随后 `scene.understand` provider failure 后等待超时；现有按同名 Tool 保留最新结果的压缩策略无法消除不同工具名和任务读取的重复。投影只删除模型已有 Tool 定义和 Coordinator 持久化投影中可重新获得的冗余字段，保留实时 readiness、参数必需项、输出关键字段、引用和失败状态，不新增 hash、状态机或动作重试。
- [Failure scenario and rationale] In the latest `gpt-6-sol/high` run, five different ToolSpec results and a full `forge_task_get` task record remained in later requests; the discovery prompt grew from about 18K to 25K, then `scene.understand` hit a provider failure and the turn timed out. The existing same-tool latest-result compaction cannot remove duplication across different tool names and task reads. The projection removes only redundant fields already available in model Tool definitions and Coordinator persistence, while retaining live readiness, required arguments, key output fields, references, and failure status; it adds no hash, state machine, or Action retry.

### 预期影响文件 / Expected files

- `PhyAgentOS/agent/prompt_context.py`
- `tests/test_prompt_context.py`

### 实际修改与验证 / Completed changes and validation

- [agent] [fix] [完成] `PhyAgentOS/agent/prompt_context.py:L208-L224,L356-L448,L462-L467` 新增 schema 结构投影、ToolSpec/context 有界投影和任务生命周期结果投影；保留 required/properties/items、tool identity、planning/readiness、task/revision/invocation/destination refs 与 `motion_authorized=false`，去除 provider 描述、完整任务记录和重复 Skill/Tool 文本。(local)
- [Agent] [Fix] [Completed] `PhyAgentOS/agent/prompt_context.py:L208-L224,L356-L448,L462-L467` adds schema-shape, bounded ToolSpec/context, and task-lifecycle projections; retains required/properties/items, tool identity, planning/readiness, task/revision/invocation/destination refs, and `motion_authorized=false`, while dropping provider prose, full task records, and duplicate Skill/Tool text. (local)
- [tests] [feat] [完成] `tests/test_prompt_context.py:L561-L635` 增加不同 discovery ToolSpec schema、readiness、任务身份和大字段去重回归；`tests/test_prompt_context.py` 与 `tests/test_agent_foundation.py` 专项 `84 passed`，规划/Agent/Prompt 合并专项 `137 passed`；Ruff、compileall、`git diff --check` 和 `gpt-6-sol/high` 配置解析通过。(local)
- [Tests] [Feat] [Completed] `tests/test_prompt_context.py:L561-L635` adds regressions for discovery ToolSpec schemas, readiness, task identity, and large-field deduplication; Prompt/Agent focused tests passed `84`, combined planning/Agent/Prompt tests passed `137`; Ruff, compileall, `git diff --check`, and `gpt-6-sol/high` config parsing passed. (local)

```diff
# Discovery result projection
full forge_tool_context ToolSpec/schema + full forge_task_get record
-> bounded schema shape + readiness/identity/status/reference facts

# Execution ownership
Coordinator/Gateway records and Action admission: unchanged
motion_authorized: false: retained
```

### Git 提交 / Git Commit

- Implementation commit: `27a3a60`
- Documentation commit: `4cfe107`
- Branch: `feature/planning-loop`
- Time: 2026-09-24 09:38 Asia/Shanghai

## v11.5.14 (2026-09-24 10:00) - codex

### 变更计划 / Planned changes [计划]

- [agent] [fix] 修复持久化会话将大 `forge_task_create/get` 结果截断成非法 JSON 后，下一轮 prompt 投影无法解析而原样保留 16K 文本的问题；在保存会话时先对 Forge 结果执行同一有界投影，再保留可审计的结构化引用。
- [Agent] [Fix] Fix persisted sessions truncating large `forge_task_create/get` results into invalid JSON, which prevents the next prompt projection from parsing them and leaves 16K text verbatim; apply the same bounded projection during session save while retaining auditable structured references.
- [tests] [feat] 增加会话保存与重新加载的 Forge 结果压缩回归，验证 JSON 可解析、任务身份/引用保留和大字段删除；运行 Agent/Prompt 专项与静态检查。
- [Tests] [Feat] Add save-and-reload regressions for Forge result compaction, verifying parseable JSON, task identity/reference retention, and removal of oversized fields; run Agent/Prompt focused tests and static checks.

### 失败场景与 Anti-OverDefense 依据 / Failure scenario and rationale

真实 `gpt-6-sol/high` 日志显示 `forge_task_create` 与 `forge_task_get` 在 session 持久化时各被截断到 16,016 字符并追加 `(truncated)`；后续 `compact_tool_result` 的 JSON 解析失败后原样返回，导致发现阶段仍携带完整任务/schema 文本并达到约 29K prompt。已有有界投影未覆盖持久化边界；复用该投影修复数据形状，不新增 hash、状态机、动作重试或运动门禁。
- [Failure scenario and rationale] The real `gpt-6-sol/high` log showed `forge_task_create` and `forge_task_get` persisted at 16,016 characters with a `(truncated)` suffix; `compact_tool_result` then failed JSON parsing and returned them verbatim, leaving full task/schema text in discovery and reaching about a 29K prompt. The existing bounded projection did not cover the persistence boundary; reuse that projection to repair the data shape without adding hashes, state machines, Action retries, or motion gates.

### 预期影响文件 / Expected files

- `PhyAgentOS/agent/loop.py`
- `tests/test_agent_foundation.py`

### 实际修改与验证 / Completed changes and validation

- [agent] [fix] [完成] `PhyAgentOS/agent/loop.py:L21-L25,L1741-L1764` 在写入 session 前对 Forge Tool 结果执行已有的结构化投影，并保留原 `ok/data/error` envelope；普通 Tool 的截断行为保持原样。(local)
- [Agent] [Fix] [Completed] `PhyAgentOS/agent/loop.py:L21-L25,L1741-L1764` applies the existing structured projection before persisting Forge Tool results while retaining the original `ok/data/error` envelope; ordinary Tool truncation remains unchanged. (local)
- [agent] [fix] [完成] `PhyAgentOS/agent/prompt_context.py:L445-L470` 在任务和通用投影中保留顶层 `ok`，确保会话恢复能区分成功与失败。(local)
- [Agent] [Fix] [Completed] `PhyAgentOS/agent/prompt_context.py:L445-L470` retains top-level `ok` in task and generic projections so resumed sessions distinguish success from failure. (local)
- [tests] [feat] [完成] `tests/test_agent_foundation.py:L65-L87` 验证大于 16K 的 Forge 任务结果持久化为可解析 JSON，保留 task/revision/plan refs 且删除重复大 schema；包含四种中断/超时恢复组合的 Agent/Prompt/Planning 专项 `138 passed`，Ruff、compileall、`git diff --check` 通过。(local)
- [Tests] [Feat] [Completed] `tests/test_agent_foundation.py:L65-L87` verifies Forge task results over 16K persist as parseable JSON with task/revision/plan refs and without duplicated large schemas; the Agent/Prompt/Planning suite, including four interruption/timeout recovery combinations, passed `138`, with Ruff, compileall, and `git diff --check` passing. (local)

```diff
# Session persistence
- forge_result[:16000] + "... (truncated)"
+ compact_tool_result(forge_result)["result"]  # valid Forge envelope

# Result projection
+ retain top-level ok for success/failure interpretation
```

## v11.5.15 (2026-09-24 11:15) - codex

### 变更计划 / Planned changes [计划]

- [agent] [fix] 将任务结果专用投影限定于返回完整 AgentTask 的 `forge_task_create/get`，保留其他生命周期接口原有的诊断/计划引用投影；为 ToolSpec schema 保留 `$ref/$defs` 与组合约束，为 planning policy 保留模型选择所需的结构化字段。
- [Agent] [Fix] Scope the task-record projection to `forge_task_create/get`, which return full AgentTasks, while preserving existing diagnostics and plan references for other lifecycle tools; retain `$ref/$defs`, composition constraints, and planning-policy fields in ToolSpec projections.
- [tests] [feat] 增加嵌套 schema 引用、planning 元数据和 lifecycle 错误字段保留回归，运行 Agent/Planning/Prompt 专项及静态检查。
- [Tests] [Feat] Add regressions for nested schema references, planning metadata, and lifecycle error retention; run Agent/Planning/Prompt focused tests and static checks.

### 失败场景与依据 / Failure scenario and rationale

`manipulation.prepare` 的 ToolSpec 使用 `$defs` 和 `$ref` 表达 coordination mode；当前 schema 投影删去它们，模型会看到不完整的输入契约。其他 `forge_task_*` 接口返回 plan/continuation diagnostics 而非完整 AgentTask，通用白名单可能丢失可纠正错误。使用现有结果类型与 schema 结构修正投影，不新增门禁或动作重试。
- [Failure scenario and rationale] The `manipulation.prepare` ToolSpec uses `$defs` and `$ref` for coordination mode; the current schema projection removes them, leaving an incomplete model-facing input contract. Other `forge_task_*` operations return plan/continuation diagnostics rather than a full AgentTask, and the broad whitelist can drop correctable errors. Adjust the existing projection by result type and schema shape without adding gates or Action retries.

### 预期影响文件 / Expected files

- `PhyAgentOS/agent/prompt_context.py`
- `tests/test_prompt_context.py`

### 实际修改与验证 / Completed changes and validation

- [agent] [fix] [完成] `PhyAgentOS/agent/prompt_context.py:L147-L150,L212-L245,L377-L418,L488-L492` 保留错误的 missing Query 诊断、schema `$ref/$defs` 和 planning policy 字段，并将完整 AgentTask 专用投影限定到 `forge_task_create/get`。(local)
- [Agent] [Fix] [Completed] `PhyAgentOS/agent/prompt_context.py:L147-L150,L212-L245,L377-L418,L488-L492` retains missing-Query diagnostics, schema `$ref/$defs`, and planning-policy fields, while restricting full-AgentTask projection to `forge_task_create/get`. (local)
- [tests] [feat] [完成] `tests/test_prompt_context.py:L561-L671` 覆盖上述不同输入；Agent/Planning/Prompt 专项 `139 passed`，Ruff、compileall、`git diff --check` 通过。(local)
- [Tests] [Feat] [Completed] `tests/test_prompt_context.py:L561-L671` covers the varied inputs; Agent/Planning/Prompt focused tests passed `139`, with Ruff, compileall, and `git diff --check` passing. (local)

```diff
# ToolSpec projection
- omit $ref/$defs and planning selection metadata
+ retain schema references and structured planning metadata

# Lifecycle projection
- compact every forge_task_* result as an AgentTask
+ compact only forge_task_create/get as full AgentTask results
```

### 真实验收边界 / Live acceptance boundary

- Session `cli:oracle-v11-5-13-rgb-20260924-1`，task `task_ad903be1b0c64ee7`：`scene.understand` provider `transport+transport`，随后主模型超时；0 Action。日志暴露完整任务结果先被截成非法 JSON 的缺陷。(local)
- Session `cli:oracle-v11-5-14-rgb-20260924-2`，task `task_58eb757d7d35400f`：修复后 `forge_task_create/get` 各为 145 字符有效 JSON，discovery prompt 约 18.5K 到 21.4K；主模型在后续请求 300 秒超时，只有 observe，0 Action。(local)
- Session `cli:oracle-v11-5-14-rgb-20260924-3`，task `task_38a1be9ce54a4c42`：约 21.2K prompt 的主模型请求 150 秒后超时，0 Action。(local)
- 已启动 profile 配置的 loopback Qwen3-VL vLLM `127.0.0.1:8012`，`/health` 与 `/v1/models` 成功，模型 `qwen3-vl-4b-awq`；不会将本地 provider 可用性视为三方块验收。(local)
- 三轮均未完成 RGB、Verifier 和视频，按照用户的“一轮三块全通”标准，验收未通过；没有重发 unknown Action 或伪造物理成功。

### Git 提交 / Git Commit

- v11.5.14 implementation: `7eda145`
- v11.5.15 implementation: `495ac63`
- Branch: `feature/planning-loop`
## v11.5.10 (2026-09-24 07:24) - codex

### 变更计划 / Planned changes [完成]

- [agent] [fix] 修复 discovery 门禁按 Query ID 去重而忽略最新 `scene.observe` capture/calibration 的问题；场景绑定 Query 必须与当前观测身份一致，防止 stale `capability_snapshot_ref` 进入 `manipulation.prepare`。
- [Agent] [Fix] Fix discovery admission treating Query IDs as sufficient while ignoring the latest `scene.observe` capture/calibration; scene-bound Queries must match the current observation identity so stale `capability_snapshot_ref` cannot reach `manipulation.prepare`.
- [tests] [feat] 增加同一 `scene_revision` 下不同 capture 的 capabilities/understand/bind freshness 回归，验证过期 Query 被重新要求且不调用 Gateway；运行 Core/Skill 专项、静态检查和 gpt-6-sol RGB AgentLoop 验收。
- [Tests] [Feat] Add freshness regressions for capabilities/understand/bind with different captures under one `scene_revision`, proving stale Queries are required again without Gateway calls; run Core/Skill focused suites, static checks, and the gpt-6-sol RGB AgentLoop acceptance.

### 失败场景与依据 / Failure scenario and rationale

`task_fd27ab4083cd43dd` 在第二次 `scene.observe` 产生 `...000001/calibration` 后，Coordinator 仍按已完成的 `manipulation.capabilities` Query ID 允许物化；准备请求混用旧 `...000000/calibration` capability snapshot 与新 grasp/scene 证据，Runtime 以 `capability snapshot is stale` 拒绝。现有普通 ID 去重不足以表达同一 scene revision 内的 capture 身份变化；使用已有 observation/capability 引用字段做 freshness 比较，不新增 hash、Action retry 或运动门禁。

`task_fd27ab4083cd43dd` produced `...000001/calibration` on a second `scene.observe`, but Coordinator admitted planning because the `manipulation.capabilities` Query ID was already complete; preparation then mixed the old `...000000/calibration` capability snapshot with new grasp/scene evidence and Runtime rejected it as `capability snapshot is stale`. Existing Query-ID deduplication cannot represent capture changes within one scene revision; compare the existing observation/capability identity fields without adding hashes, Action retries, or motion gates.

### 预期影响文件 / Expected files

- `PhyAgentOS/forge/binding.py`
- `PhyAgentOS/forge/task.py`
- `PhyAgentOS/agent/prompt_context.py`
- `PhyAgentOS/agent/tools/forge_task.py`
- `PhyAgentOS/agent/plan_proposal.py`
- `tests/test_plan_proposal_bindings.py`
- `tests/test_prompt_context.py`

### 实际修改与验收 / Completed changes and acceptance

- [agent] [fix] [完成] `PhyAgentOS/agent/loop.py:L90-L100,L729-L729,L751-L777,L918-L943,L975-L1004` 增加已激活 `pick-place-workflow` 的创建阶段工具投影与执行侧拒绝；Coordinator 返回成功 `task_id` 后清除门控并恢复正常任务投影。(local)
- [Agent] [Fix] [Completed] `PhyAgentOS/agent/loop.py:L90-L100,L729,L751-L777,L918-L943,L975-L1004` adds a creation-phase tool projection and execution-side rejection after `pick-place-workflow` activation; the gate clears on a Coordinator-issued `task_id` and normal task projection resumes. (local)
- [tests] [feat] [完成] `tests/test_agent_foundation.py:L209-L268` 验证激活后只显示创建/只读工具、Coordinator 创建成功后恢复普通工具；`81 passed`，compileall、Ruff、`git diff --check` 通过。(local)
- [Tests] [Feat] [Completed] `tests/test_agent_foundation.py:L209-L268` verifies the restricted creation/read-only set after activation and restoration of ordinary tools after Coordinator creation; `81 passed`, compileall, Ruff, and `git diff --check` passed. (local)

### 关键 Diff / Key diff

```diff
+ _PICK_PLACE_CREATION_TOOLS = {
+     "activate_skill", "forge_task_create", "forge_tool_context", "forge_tool_query"
+ }
+ if pick_place_creation_mode and active_task is None:
+     creation_tool_names = tuple(name for name in prompt_tool_names
+                                  if name in _PICK_PLACE_CREATION_TOOLS)
+ if disallowed_tool_call:
+     return forge_task_creation_required
+ if forge_task_create returned a persisted task_id:
+     pick_place_creation_mode = False
```

### 补充计划 / Additional planned change [完成]

- [agent] [fix] 在语义 PlanGraph 的 Runtime binding 补全过程中仅接受与 active revision 最新 `scene.observe` 同源的 capability snapshot；无匹配 snapshot 时保持未绑定并由正常 schema/Coordinator 诊断要求刷新，不把旧引用静默带入准备节点。
- [Agent] [Fix] During semantic PlanGraph Runtime binding completion, accept only capability snapshots sharing the active revision's latest `scene.observe` identity; when none matches, leave the binding absent so normal schema/Coordinator diagnostics require refresh instead of silently carrying an old reference into preparation.
- [tests] [feat] 增加直接编译 stale capability 不传播的无运动回归。
- [Tests] [Feat] Add a no-motion regression proving direct compilation does not propagate a stale capability snapshot.

### 实际修改与验证 / Completed changes and validation

- [agent] [fix] [完成] `PhyAgentOS/forge/binding.py:L117-L190` 新增 `missing_preplan_queries`，按最新成功 `scene.observe` 的 `scene_revision`、`observation_ref`、`calibration_ref` 校验场景绑定 discovery Query；`PhyAgentOS/forge/task.py:L1801-L1807` 将该结果作为 Coordinator 物化门禁，`PhyAgentOS/agent/prompt_context.py:L30-L32` 同步隐藏计划工具直到刷新完成，`PhyAgentOS/agent/tools/forge_task.py:L288-L295` 保持 discovery 错误语义。(local)
- [Agent] [Fix] [Completed] `PhyAgentOS/forge/binding.py:L117-L190` adds `missing_preplan_queries` to compare scene-bound discovery Queries with the latest successful `scene.observe` scene/capture/calibration identity; `PhyAgentOS/forge/task.py:L1801-L1807` uses it as the Coordinator materialization gate, `PhyAgentOS/agent/prompt_context.py:L30-L32` hides planning tools until refresh completes, and `PhyAgentOS/agent/tools/forge_task.py:L288-L295` preserves discovery error semantics. (local)
- [agent] [fix] [完成] `PhyAgentOS/agent/plan_proposal.py:L214-L277` 在 Runtime binding 补全时过滤与最新观测不一致的 capability snapshot，避免旧引用进入 `manipulation.prepare`。(local)
- [Agent] [Fix] [Completed] `PhyAgentOS/agent/plan_proposal.py:L214-L277` filters capability snapshots that do not match the latest observation during Runtime binding completion, preventing old references from entering `manipulation.prepare`. (local)
- [tests] [feat] [完成] `tests/test_plan_proposal_bindings.py:L157-L190` 覆盖同 scene revision 不同 capture 的 stale discovery 和直接编译过滤；Core 全量 `585 passed`，Planning/Agent 专项 `120 passed`、`45 passed`，Skill 全量 `347 passed`，Ruff、compileall、`git diff --check` 通过。(local)
- [Tests] [Feat] [Completed] `tests/test_plan_proposal_bindings.py:L157-L190` covers stale discovery and direct compilation filtering across captures under one scene revision; Core passed `585`, Planning/Agent focused suites passed `120` and `45`, Skill passed `347`, with Ruff, compileall, and `git diff --check` passing. (local)

```diff
# Discovery admission
completed Query IDs -> latest scene-bound Query identity must equal latest observe

# Runtime binding completion
any capability snapshot -> latest-observation-matching capability snapshot only
```

### 验收边界 / Acceptance boundary

软件与无运动回归已通过；真实 RGB 三方块 AgentLoop、Action terminal retreat 证据、ForgeTaskVerifier 和累计视频仍待本次修复安装后的运行时验收。

Software and no-motion regressions pass; the live RGB three-block AgentLoop, terminal retreat evidence, ForgeTaskVerifier, and cumulative video remain pending a post-fix runtime acceptance.
## v11.5.11 (2026-09-24 15:00) - codex

### 变更计划 / Planned changes

- [agent] [fix] [完成] 在 `pick-place-workflow` 已激活但 AgentTask 尚未创建的当前回合，限制 AgentLoop 到任务创建/只读恢复工具，防止模型调用 shell 帮助或扫描本地状态后反复组装缺失任务 ID；任务创建成功后恢复 Coordinator 驱动的完整工具投影。(local)
- [Agent] [Fix] [Completed] When `pick-place-workflow` is active but no AgentTask exists yet, restrict the AgentLoop to task creation and read-only recovery tools so the model cannot fall back to shell help or local-state scans and repeatedly assemble a missing task ID; restore the full Coordinator-driven tool projection after task creation succeeds. (local)
- [tests] [feat] [完成] 增加创建阶段工具投影回归，覆盖激活后的受限工具集、创建成功后的任务投影恢复和普通未激活回合不受影响；运行 AgentLoop/Prompt 专项、Core 静态检查与配置验证。(local)
- [Tests] [Feat] [Completed] Add task-creation tool-projection regressions covering the restricted post-activation set, restoration after successful creation, and unchanged ordinary pre-activation turns; run AgentLoop/Prompt focused tests, Core static checks, and configuration validation. (local)

### 失败场景与 Anti-OverDefense 依据 / Failure scenario and rationale

最近一次 `gpt-6-sol/high` RGB 运行在 task_creation 阶段连续四轮，模型因尚未获得任务 ID 而调用任务帮助/状态查询，最终 provider timeout；没有创建 AgentTask，也没有 Gateway/Action。现有工具可见性允许所有通用工具，不能保证 Forge 创建顺序；本门控只约束已明确激活的 pick-place 工作流创建边界，不新增 hash、状态持久化或动作重试。(local)

The latest `gpt-6-sol/high` RGB run spent four task-creation iterations calling task help/status queries because no task ID had been established, then hit a provider timeout; no AgentTask, Gateway call, or Action was created. Existing visibility allowed all generic tools and did not enforce Forge creation ordering. This narrow gate applies only after the named pick-place workflow is explicitly activated; it adds no hash, persistent state machine, or Action retry. (local)

### 预期影响 / Expected files

- `PhyAgentOS/agent/loop.py`
- `tests/test_agent_foundation.py`
- `tests/test_prompt_context.py`
## v11.5.12 (2026-09-24 15:30) - codex

### 变更计划 / Planned changes

- [agent] [fix] [计划] 在 AgentTask 已由 Coordinator 创建后压缩历史 `activate_skill` 结果，只保留 activation 身份和 lessons 元数据，避免完整 Skill 文档在 discovery/规划请求中重复拼接；首次激活回合仍保留原文供任务创建。(local)
- [Agent] [Fix] [Planned] After the Coordinator creates an AgentTask, compact historical `activate_skill` results to activation identity and lesson metadata so the full Skill document is not repeatedly appended to discovery/planning requests; retain the original text during the initial activation turn for task creation. (local)
- [tests] [feat] [计划] 增加 Skill activation 结果压缩回归，并重复运行 Prompt/Agent 专项、静态检查和配置验证。(local)
- [Tests] [Feat] [Planned] Add a Skill activation-result compaction regression and rerun Prompt/Agent focused tests, static checks, and configuration validation. (local)

### 失败场景与 Anti-OverDefense 依据 / Failure scenario and rationale

修复创建阶段门控后，真实 `gpt-6-sol/high` 运行已创建任务并进入 discovery，但 prompt 从约 17K 增至约 23K，完整 Skill 文档随 `activate_skill` tool result 被继续携带，182 秒后 provider timeout；任务只有创建记录、零 Query/Action。已有 Coordinator/SkillUse 持久化身份和内容归属，后续节点不需要重复原文；只做有界上下文投影，不新增 hash、状态机或动作重试。(local)

After the creation gate was fixed, the real `gpt-6-sol/high` run created the task and entered discovery, but the prompt grew from about 17K to 23K because the full Skill document remained in the `activate_skill` tool result, followed by a provider timeout after 182 seconds; the task had only a creation record and zero Queries/Actions. Coordinator/SkillUse already persist Skill identity and ownership, so later nodes do not need the full text repeated. This is a bounded context projection only, with no new hash, state machine, or Action retry. (local)

### 预期影响 / Expected files

- `PhyAgentOS/agent/prompt_context.py`
- `tests/test_prompt_context.py`

### 实际修改与验收 / Completed changes and acceptance

- [agent] [fix] [完成] `PhyAgentOS/agent/prompt_context.py:L748-L779,L871-L874` 在任务投影存在时将 `activate_skill` tool result 压缩为 activation/lessons 元数据和持久化 SkillUse 提示；首次无 task 的激活回合保持完整 Skill 文档。(local)
- [Agent] [Fix] [Completed] `PhyAgentOS/agent/prompt_context.py:L748-L779,L871-L874` compacts `activate_skill` tool results to activation/lesson metadata and a persisted SkillUse notice once a task projection exists; the initial no-task activation turn retains the full Skill document. (local)
- [tests] [feat] [完成] `tests/test_prompt_context.py:L173-L193` 验证大文档压缩、身份/lesson 保留和未启用时不变；Prompt/Agent 专项 `82 passed`，Ruff、compileall、`git diff --check` 通过。(local)
- [Tests] [Feat] [Completed] `tests/test_prompt_context.py:L173-L193` verifies large-document compaction, identity/lesson retention, and unchanged disabled behavior; Prompt/Agent focused tests passed `82`, with Ruff, compileall, and `git diff --check` passing. (local)

### 关键 Diff / Key diff

```diff
+ if task is not None:
+     compact activate_skill result to activation + applicable_lessons
+     retain a persisted SkillUse notice instead of full skill prose
- otherwise leave the initial activation result unchanged
```

## v11.6.0 (2026-09-24 11:40) - codex

### 变更计划 / Planned changes [完成]

- [agent] [fix] 修复 `activate_skill` 在 AgentTask 创建后仍以 16K 非法 JSON 进入 session，导致既有激活结果压缩不能解析的路径；保留 activation/lesson 身份与持久化 SkillUse 提示。
- [Agent] [Fix] Fix the path where `activate_skill` is persisted as 16K invalid JSON after AgentTask creation, defeating the existing activation compactor; retain activation/lesson identity and the persisted SkillUse notice.
- [tests] [feat] 覆盖同一回合激活并创建任务后的会话保存与恢复，以及未创建任务时不伪称 SkillUse 持久化；运行 Agent/Prompt/Planning 专项和静态检查。
- [Tests] [Feat] Cover session save/reload after activation and task creation in one turn, and avoid claiming a persisted SkillUse when no task exists; run Agent/Prompt/Planning focused tests and static checks.

### 失败场景与依据 / Failure scenario and rationale

第二、三轮真实会话的 `activate_skill` 结果均为 16,016 字符并附加 `(truncated)`，不再是 JSON；`_compact_activation_results` 解析失败后不能删除 Skill 全文。已有 Coordinator SkillUse 可保存任务绑定身份，故只修改会话投影，不改变 Skill 激活、任务归属或动作门禁。
- [Failure scenario and rationale] In live runs two and three, `activate_skill` results were 16,016 characters plus `(truncated)`, no longer JSON; `_compact_activation_results` could not parse them to remove the full Skill text. Coordinator SkillUse already retains task-bound identity, so this changes only the session view, not Skill activation, task ownership, or motion gates.

### 预期影响文件 / Expected files

- `PhyAgentOS/agent/loop.py`
- `tests/test_agent_foundation.py`

### 实际修改与验证 / Completed changes and validation

- [agent] [fix] [完成] `PhyAgentOS/agent/loop.py:L1759-L1765` 将 `activate_skill` 从通用大结果字符串截断分支排除，保留可解析的完整激活结果；任务创建后的已有 prompt 投影继续压缩 Skill 正文。(local)
- [Agent] [Fix] [Completed] `PhyAgentOS/agent/loop.py:L1759-L1765` excludes `activate_skill` from generic large-result string truncation, preserving parseable activation data; existing prompt projection still compacts Skill prose after task creation. (local)
- [tests] [feat] [完成] `tests/test_agent_foundation.py:L20,L90-L122` 覆盖大于 16K 的激活结果、同回合任务创建、session 读取和无任务时保留完整 Skill；专项 `112 passed`，Ruff 与 `git diff --check` 通过。(local)
- [Tests] [Feat] [Completed] `tests/test_agent_foundation.py:L20,L90-L122` covers activation results above 16K, task creation in the same turn, session recovery, and retention of full Skill instructions without a task; focused tests passed `112`, with Ruff and `git diff --check` passing. (local)
- [env] [tune] [完成] 外部 PAOS 默认与短 RGB 配置的模型从 `gpt-5.6-sol` 调整为 `gpt-6-sol`，三份配置均验证为 `gpt-6-sol/high`；这些用户配置不在本仓库版本控制中。(local)
- [Env] [Tune] [Completed] Changed the external PAOS default and short RGB config model from `gpt-5.6-sol` to `gpt-6-sol`; all three configs now validate as `gpt-6-sol/high`. These user configs are outside this repository's version control. (local)

### 关键 Diff / Key diff

```diff
# PhyAgentOS/agent/loop.py:L1759-L1765
+ and entry.get("name") != "activate_skill"
  and len(content) > self._TOOL_RESULT_MAX_CHARS

# tests/test_agent_foundation.py:L90-L122
+ assert json.loads(history[0]["content"])["skill"].startswith("workflow instructions")
+ assert json.loads(compacted[0]["content"])["skill"]["status"] == "persisted_in_coordinator_skill_use"
+ assert _compact_activation_results(history_without_task, enabled=False) == history_without_task
```

### 验收边界 / Acceptance boundary

这次通过的是会话持久化与 AgentLoop 专项无运动验证；三块排列、Action terminal retreat、ForgeTaskVerifier 和视频仍须真实验收。

This validates session persistence and focused no-motion AgentLoop behavior. Live three-block placement, terminal retreat evidence, ForgeTaskVerifier, and video still require acceptance.

### Git 提交 / Git commit

- Commit: `7956e46`
- Branch: `feature/planning-loop`

## v11.6.1 (2026-09-24 11:19) - codex

### 变更计划 / Planned changes [完成]

- [sense] [fix] 约束本地 Qwen vLLM 场景理解输出的歧义码为既有语义码集合，并在 provider 解析边界拒绝无效码；仅对这类输出契约错误启用配置的远程 fallback，保留 Grounding 对真实歧义的拒绝。
- [Sense] [Fix] Restrict local Qwen vLLM ambiguity codes to the existing semantic set and reject invalid codes at the provider parsing boundary; enable the configured remote fallback only for this output contract failure while preserving Grounding rejection of real ambiguity.
- [tests] [feat] 增加错误码 `e1` 的无运动回归，运行 Qwen provider、Grounding 与 AgentLoop 专项。
- [Tests] [Feat] Add a no-motion regression for invalid code `e1` and run Qwen provider, Grounding, and AgentLoop focused tests.

### 失败场景与依据 / Failure scenario and rationale

真实任务 `task_c409c97bde5d4437` 的两次 `scene.understand` 均输出 `ambiguities.code=e1`；本地 provider schema 接受任意字符串且投影不校验，Grounding 按未知码拒绝三方块绑定，0 Action。已有 canonical 码及 fallback 路由足以处理此场景，不增加新 hash、状态机或动作门禁。

The live task `task_c409c97bde5d4437` produced `ambiguities.code=e1` twice. The local provider schema accepted arbitrary strings and projected them without validation; Grounding rejected the unknown code, leaving zero Actions. Existing canonical codes and fallback routing cover this case without a new hash, state machine, or motion gate.

### 预期影响文件 / Expected files

- `examples/forge-adapters/robotwin20/src/robotwin20_adapter/qwen3_vl_vllm_scene_understanding.py`
- `examples/forge-adapters/robotwin20/src/robotwin20_adapter/persistent_host.py`
- `examples/forge-adapters/robotwin20/tests/test_qwen3_vl_vllm_scene_understanding.py`
- `examples/forge-adapters/robotwin20/tests/test_scene_understanding_fallback.py`
- `examples/forge-skills/pick-place-workflow/skill.yaml`

### 实际修改与验证 / Completed changes and validation

- [sense] [fix] [完成] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/qwen3_vl_vllm_scene_understanding.py:L20-L58,L315-L323` 在 Qwen schema 和输出投影中约束既有语义歧义码，拒绝 `e1` 等实体 ID 误用为 code。(local)
- [Sense] [Fix] [Completed] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/qwen3_vl_vllm_scene_understanding.py:L20-L58,L315-L323` constrains Qwen schema and output projection to existing semantic ambiguity codes, rejecting entity IDs such as `e1` as codes. (local)
- [sense] [fix] [完成] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/persistent_host.py:L49-L53,L462-L469` 将这一类契约错误交给配置的 fallback；生命周期错误和其他推断错误仍维持各自原有路由。(local)
- [Sense] [Fix] [Completed] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/persistent_host.py:L49-L53,L462-L469` routes this contract error to the configured fallback while preserving existing treatment of lifecycle and other inference errors. (local)
- [tests] [feat] [完成] `examples/forge-adapters/robotwin20/tests/test_qwen3_vl_vllm_scene_understanding.py:L14-L18,L115-L118,L223-L259` 与 `examples/forge-adapters/robotwin20/tests/test_scene_understanding_fallback.py:L5-L9,L124-L135` 覆盖 schema、合法歧义、非法码拒绝和 fallback；结合 Grounding 专项 `64 passed`，Ruff、diff 检查通过。(local)
- [Tests] [Feat] [Completed] `examples/forge-adapters/robotwin20/tests/test_qwen3_vl_vllm_scene_understanding.py:L14-L18,L115-L118,L223-L259` and `examples/forge-adapters/robotwin20/tests/test_scene_understanding_fallback.py:L5-L9,L124-L135` cover schema, valid ambiguity, invalid-code rejection, and fallback; focused suites including Grounding passed `64`, with Ruff and diff checks passing. (local)
- [env] [chore] [完成] `examples/forge-skills/pick-place-workflow/skill.yaml:L3,L151-L158` 更新 Skill `2.6.21` 与 Node `0.7.2` 锁定值；Node 归档构建、Skill 安装、Node verify 与 Gateway readiness 均通过。(local)
- [Env] [Chore] [Completed] `examples/forge-skills/pick-place-workflow/skill.yaml:L3,L151-L158` updates Skill `2.6.21` and Node `0.7.2` lock values; Node build, Skill install, Node verify, and Gateway readiness passed. (local)

### 关键 Diff / Key diff

```diff
- ambiguity.code: arbitrary string
+ ambiguity.code: canonical semantic enum, validated at provider output
- fallback_exceptions=(Qwen3VLVLLMLifecycleError,)
+ fallback_exceptions=(Qwen3VLVLLMLifecycleError, Qwen3VLVLLMContractError)
```

### 真实验收边界 / Live acceptance boundary

修复前任务 `task_c409c97bde5d4437` 两次输出非法歧义码并在绑定阶段停止，0 Action；已正常取消。修复后新任务 `task_f38561fa6f464090` 创建并完成一次 `scene.observe` 后主 Agent 请求 `provider_timeout`，状态 `failed`，0 Action；未到 `scene.understand`，所以线上 fallback 尚未被触发。三块排列、Verifier 与视频未通过验收。

Before the fix, task `task_c409c97bde5d4437` returned invalid ambiguity codes twice and stopped at binding with zero Actions; it was cancelled normally. After the fix, task `task_f38561fa6f464090` was created and completed one `scene.observe`, then the main Agent request timed out; status is `failed` with zero Actions. It did not reach `scene.understand`, so the live fallback was not exercised. Three-block placement, Verifier, and video remain unaccepted.

### Git 提交 / Git commit

- Commit: `9371c1d`
- Branch: `feature/planning-loop`

## v11.6.2 (2026-09-24 12:05) - codex

### 变更计划 / Planned changes [完成]

- [agent] [fix] 在既有 node-turn continuation 预算内，对没有 ToolExecutionRecord 的 `provider_timeout` 做一次同节点重试；保持已持久化 selection 的恢复、Action 对账和其他失败的原有行为。
- [Agent] [Fix] Within the existing node-turn continuation budget, retry `provider_timeout` once on the same node when there is no ToolExecutionRecord; retain persisted-selection recovery, Action reconciliation, and existing handling of other failures.
- [tests] [feat] 覆盖选择前超时后成功，以及连续超时后仍按原边界阻塞；运行规划和 AgentLoop 专项。
- [Tests] [Feat] Cover a pre-selection timeout followed by success and repeated timeouts that still block at the existing boundary; run planning and AgentLoop focused tests.

### 失败场景与依据 / Failure scenario and rationale

任务 `task_75a41e9cf23b49e2` 已完成 discovery、唯一 RGB 绑定和第一段 PlanGraph 物化；首个规划节点在任何 selection/ToolExecutionRecord 产生前遭遇一次上游 `provider_timeout`，立即进入 `awaiting_replan`，0 Action。节点执行器已有 bounded continuation 与执行记录对账，扩展这个现有条件足以恢复纯模型决策超时，无需新状态、hash 或动作重发。

Task `task_75a41e9cf23b49e2` completed discovery, unique RGB binding, and first-segment PlanGraph materialization. The first planning node hit one upstream `provider_timeout` before any selection or ToolExecutionRecord and immediately entered `awaiting_replan`, with zero Actions. The executor already has bounded continuation and execution-record reconciliation, so extending that condition recovers this model-only timeout without new state, hashes, or Action resend.

### 预期影响文件 / Expected files

- `PhyAgentOS/agent/planning_loop.py`
- `tests/test_planning_loop.py`

### 实际修改与验证 / Completed changes and validation

- [agent] [fix] [完成] `PhyAgentOS/agent/planning_loop.py:L606-L623` 仅在 `provider_timeout` 前后均无 pending selection、且本节点无执行记录时使用现有下一次 node turn；若 selection 已固化则保留旧恢复规则，执行记录仍先对账。(local)
- [Agent] [Fix] [Completed] `PhyAgentOS/agent/planning_loop.py:L606-L623` uses the existing next node turn only when `provider_timeout` occurs with no pending selection before or after and no execution record; persisted selections retain the previous recovery rule and execution records remain reconciled first. (local)
- [tests] [feat] [完成] `tests/test_planning_loop.py:L1857-L1914` 覆盖超时后成功、两次超时仍阻塞；原有 selection/Action 对账回归通过。规划、AgentLoop、超时专项 `144 passed`，Ruff 和 diff 检查通过。(local)
- [Tests] [Feat] [Completed] `tests/test_planning_loop.py:L1857-L1914` covers success after one timeout and blocking after two; existing selection and Action reconciliation regressions pass. Planning, AgentLoop, and timeout suites passed `144`, with Ruff and diff checks passing. (local)

### 关键 Diff / Key diff

```diff
- retry after provider failure only when a new selection was persisted
+ retry after provider_timeout also when neither turn has a pending selection
  reconcile execution records before either retry decision
```

### 验收边界 / Acceptance boundary

真实任务 `task_75a41e9cf23b49e2` 验证 RGB discovery、绑定和首段图物化成功，但首节点在旧版本中一次超时后进入 `awaiting_replan`；0 Action，三块验收未通过。新重试行为仍需真实任务验证。

Live task `task_75a41e9cf23b49e2` proves RGB discovery, binding, and first-segment materialization, but the first node entered `awaiting_replan` after one timeout on the old version. It has zero Actions; three-block acceptance did not pass. The new retry behavior still needs a live run.

### Git 提交 / Git commit

- Commit: `9a44883`
- Branch: `feature/planning-loop`

## v11.6.3 (2026-09-24 12:22) - codex

### 变更摘要 / Changes [完成]

- [agent] [fix] 在 AgentLoop 的任务创建与 discovery 回合，对无工具响应的模型 `provider_timeout` 使用同一 prompt 一次有界重试；node turn 继续由既有节点执行器负责，任何 Gateway Tool/Action 不重发。
- [Agent] [Fix] In AgentLoop task creation and discovery turns, retry a model `provider_timeout` without tool calls once using the same prompt; node turns remain owned by the existing node executor, and no Gateway Tool or Action is resent.
- [tests] [feat] 覆盖模型超时后成功、连续超时终止以及 node turn 不叠加重试；运行 Agent/Planning/Timeout 专项。
- [Tests] [Feat] Cover success after a model timeout, termination after repeated timeouts, and no layered retry in node turns; run Agent, Planning, and Timeout focused suites.

### 失败场景与依据 / Failure scenario and rationale

新验收 `cli:oracle-v11-6-2-rgb-20260924-2` 在首次任务创建模型请求约 50 秒后收到上游 `provider_timeout`，0 Tool、0 Action；PAOS 立即结束，单次上游抖动阻断整轮。Provider 保持通用 fail-fast，AgentLoop 只在没有 Tool 响应可执行的模型请求边界复用现有请求预算做一次重试；不增加持久状态或动作重试。

New acceptance session `cli:oracle-v11-6-2-rgb-20260924-2` received upstream `provider_timeout` after about 50 seconds on the first task-creation model request, with zero Tools and Actions; PAOS ended immediately. Keep provider-wide fail-fast behavior and retry once only at the AgentLoop model-request boundary where there is no Tool response to execute, without persistent state or Action retry.

### 文件变更详情 / File changes

#### [修改 / Modified] `PhyAgentOS/agent/loop.py` L809-L878

**修改前 / Before:**
```python
response = await bounded_decision(self.provider.chat_with_retry(...))
# provider_timeout ended the task-creation or discovery turn immediately
```

**修改后 / After:**
```python
retry_timeout = projection_scope == "task" and request_view.phase in {
    "task_creation", "discovery"
}
for model_attempt in range(2 if retry_timeout else 1):
    response = await bounded_decision(self.provider.chat_with_retry(...))
    if not (retry_timeout and model_attempt == 0
            and response.finish_reason == "error"
            and not response.has_tool_calls
            and self.provider.classify_error(response.content) == "provider_timeout"):
        break
```

仅重试无 Tool call 的模型超时，复用相同请求参数；节点执行器继续独立承担节点 continuation 和执行记录对账。 / Retry only model timeouts without Tool calls using the same request; the node executor retains node continuation and execution reconciliation.

#### [新增 / Added] `tests/test_agent_foundation.py` L333-L381

**新增 / Added:** 四种 task_creation/discovery 超时恢复组合和 node turn 不叠加重试测试。 / Four task-creation/discovery timeout recovery combinations and a node-turn no-layered-retry test.

### 验证 / Validation

- `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 /home/yanxu/miniconda3/envs/paos/bin/python -m pytest -q tests/test_agent_foundation.py tests/test_planning_loop.py tests/test_prompt_context.py tests/test_turn_timeouts.py tests/test_custom_provider_timing.py`: `158 passed`。
- `ruff check PhyAgentOS/agent/loop.py tests/test_agent_foundation.py`: passed.
- `git diff --check`: passed.
- 真实 RGB 三块验收仍待新任务验证。 / A fresh live RGB three-block acceptance run remains required.

### Git 提交 / Git commit

- Commit: `1832a39`
- Branch: `feature/planning-loop`

## v11.6.4 (2026-09-24 17:04) - codex

### 变更记录 / Changes [完成]

- [env] [tune] 按用户要求，将外部 PAOS 默认、RGB 短配置及 RGB 长配置的 `agents.defaults.model` 从 `gpt-6-sol` 改为 `gpt-5.6-sol`，推理强度保持 `high`。使用实际配置加载器验证。(local)
- [Env] [Tune] Change the external default, short RGB, and long RGB PAOS configurations from `gpt-6-sol` to `gpt-5.6-sol` with `high` reasoning as requested; validate through the configuration loader. (local)

### 文件与 Diff / Files and diff

- `/home/yanxu/.PhyAgentOS/config.json:L5`、`/home/yanxu/.PhyAgentOS/config-rgb-no-evolution.json:L5`、`/home/yanxu/.PhyAgentOS/config-rgb-no-evolution-long.json:L5`：`"model": "gpt-6-sol"` → `"model": "gpt-5.6-sol"`。
- 三份外部配置经 `PhyAgentOS.config.loader.load_config` 加载，均确认 `gpt-5.6-sol/high`。配置含本地凭据，Git 仅记录本次变更说明。
- All three external configurations resolve to `gpt-5.6-sol/high` through `load_config`; only the change record is tracked in Git because local configurations contain credentials.

### Git 提交 / Git commit

- Commit: `ffba060`
- Branch: `feature/planning-loop`
