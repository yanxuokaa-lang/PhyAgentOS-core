# 2026-09 Changelog - Part 8

## v10.1.15 (2026-09-15 11:20) - codex

### 变更摘要 / Change Summary [完成]

- [policy] [fix] 修复 Agent-composed PlanGraph 将自然语言/未产生的条件事实与语义证据别名误当作 readiness 输入，导致合法根节点 `ready_nodes=[]`；增加逐节点 readiness 诊断并保持 Coordinator、Gateway 与运动授权边界不变。(local)
- [Policy] [Fix] Fix Agent-composed PlanGraph readiness so natural-language/unproduced condition facts and semantic evidence aliases cannot block valid root nodes; add per-node readiness diagnostics while preserving Coordinator, Gateway, and motion-authorization boundaries. (local)

### 文件变更详情 / File Changes

- [修改] `PhyAgentOS/planning/dag.py:L44-L120`：新增纯 `explain_node_readiness` 诊断投影，报告依赖、证据、未知/false 条件，并让 `derive_ready_nodes` 复用同一判定；不把别名或未知事实变成 true。
- [Modified] `PhyAgentOS/planning/dag.py:L44-L120`: added the pure `explain_node_readiness` projection for dependency, evidence, unknown/false-condition blockers, and reused it in `derive_ready_nodes` without promoting aliases or unknown facts.
- [修改] `PhyAgentOS/agent/planning_dispatch.py:L100-L165,L256-L295`：`forge_plan_ready` 增加 `node_diagnostics`；`scene_current=false` 时只投影具备 `refreshes_scene` 的 Query 节点，禁止 Action 伪 ready。
- [Modified] `PhyAgentOS/agent/planning_dispatch.py:L100-L165,L256-L295`: added `node_diagnostics` and limited stale-scene ready projection to Tools explicitly marked `refreshes_scene`, preventing Action false-readiness.
- [修改] `PhyAgentOS/agent/plan_proposal.py:L19-L105`：区分“无上下文”与“空 trusted evidence”，对有上下文的根节点严格校验 exact evidence refs 和当前 condition facts。
- [Modified] `PhyAgentOS/agent/plan_proposal.py:L19-L105`: distinguish absent context from an empty trusted evidence set and strictly validate root exact evidence refs and current condition facts when context exists.
- [修改] `PhyAgentOS/agent/tools/forge_task.py:L215-L293`：semantic/full PlanGraph 两种提交形式都拒绝伪造 evidence_refs，只允许 Coordinator 已持久化引用；保留 scene-free、无证据图的兼容路径。
- [Modified] `PhyAgentOS/agent/tools/forge_task.py:L215-L293`: reject fabricated `evidence_refs` for both semantic-node and complete-graph submissions, while retaining the compatible scene-free/no-evidence path.
- [修改] `PhyAgentOS/forge/task.py:L1140-L1189`：物化后尚无 execution/settlement/counterevidence/verification facts 时允许 append-only discovery correction；执行事实出现后转入既有 recovery/replan。
- [Modified] `PhyAgentOS/forge/task.py:L1140-L1189`: allow append-only discovery correction only before execution, settlement, counterevidence, or verification facts; afterward require existing recovery/replan.
- [修改] `examples/forge-skills/pick-place-workflow/skill.yaml:L3,L36-L39`、`profiles/robotwin-persistent/dataflow.yaml:L12-L17`：Skill 2.0.2 的 Qwen 环境声明与 dataflow 保持集合一致；当前 Provider 仍由 GPT-Sol High 配置选择，不在本次切换。
- [Modified] `examples/forge-skills/pick-place-workflow/skill.yaml:L3,L36-L39` and `profiles/robotwin-persistent/dataflow.yaml:L12-L17`: aligned Skill 2.0.2 Qwen environment declarations with dataflow; the active Provider remains GPT-Sol High and is not switched here.
- [新增] `.task/current.md`：补充 Implementation Principles Gate、Agent Route Contract、方向 closeout 和所有权/证据/安全边界。
- [Added] `.task/current.md`: added the Implementation Principles Gate, Agent Route Contract, direction closeout, and ownership/evidence/safety boundaries.

### 关键 Diff / Key Diff

```diff
- evidence_refs=tuple(evidence_refs or ())
+ reject refs not present in trusted Coordinator context
+ compile root nodes against exact persisted evidence and condition facts

- stale scene => every dependency-complete node appears ready
+ stale scene => only a declared refreshes_scene Query appears ready

- materialized graph was always immutable before execution correction
+ append-only correction is allowed only with zero execution facts; otherwise recovery/replan
```

### 六维代码审查 / Six-Dimension Code Review

| 维度 / Dimension | 结果 / Result | 证据 / Evidence |
| --- | --- | --- |
| 架构集成 / Architecture integration | PASS | 语义编译在 `agent`，纯 readiness 在 `planning`，持久修订在 Coordinator；未新增 scheduler/store/provider route。 |
| 失败路径 / Failure paths | PASS | 50 项 focused；覆盖 fabricated evidence、unknown condition、stale refresh、post-execution correction rejection；failed/unknown/stale 仍 fail-closed。 |
| 权限与安全 / Authority and safety | PASS | `motion_authorized=false` 保持；无 Gateway、Action、Runtime、仿真、硬件调用；scene stale 只允许 refresh Query。 |
| 配置与可复现性 / Configuration and reproducibility | PASS | 375 core、330 Skill、465 Adapter；显式 asyncio 插件、PYTHONPATH、Ruff、compileall、diff check 和 principles route 命令可复现。 |
| 可维护性 / Maintainability | PASS | 诊断为纯函数；复用现有 Coordinator/AdmissionContext/NodeSettlement；文档与 Skill 指南同步；无新 hash 或第二状态所有者。 |
| 可观察性 / Observability | PASS | `node_diagnostics` 暴露依赖、evidence、condition、Tool candidate blockers；append-only correction 记录独立事件。 |

### 验证 / Validation

- `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 pytest -p pytest_asyncio.plugin -q`: `375 passed`。
- Focused planning/task/dispatch/DAG: `50 passed`。
- Pick-place Skill: `330 passed`。
- RoboTwin Adapter: `465 passed`（通过加入 `src/runtime/scripts` 的显式 `PYTHONPATH`）。
- `ruff check` changed files、`python -m compileall`、`git diff --check`: passed。
- `python /home/yanxu/Hephaestus/tools/principles_check.py --path .task/current.md --complete --agent-route`: passed。
- 未启动 Runtime、Gateway Query/Action、仿真、硬件或运动；GPT-Sol High Provider 未切换。

### Review Outcome

`aligned`：本切片保持 PAOS 的单一 truth owner、精确证据边界、append-only task history 和 motion separation。残余项是现有 live task 的后续合法规划/执行，不在本次代码修改中代办。

`aligned`: this slice preserves PAOS's single truth owner, exact evidence boundary, append-only task history, and motion separation. Remaining work is the live task's subsequent legal planning/execution and is not performed here.

### Git 提交 / Git Commit

- Functional commit: `d037c1d`; branch: `feature/planning-loop`.

### 预期影响文件 / Expected Files

- `PhyAgentOS/agent/planning_dispatch.py`
- `PhyAgentOS/agent/tools/planning.py`
- `PhyAgentOS/agent/plan_proposal.py`
- `PhyAgentOS/planning/dag.py`
- `tests/test_planning_dispatch.py`
- `tests/test_planning_dag.py`
- `docs/forge/PLANNING_MODULE_DESIGN.md`
- `docs/forge/MANIPULATION_DAG_DEVELOPER_GUIDE.md`
- `docs/forge/AGENT_LOOP_FOUNDATION_DIAGNOSIS_20260910.md`

### 失败场景与边界 / Failure Scenario and Boundary

- 当前任务的根节点同时使用了未由 Coordinator 产生的 symbolic conditions 和不会自动映射到真实 `evidence_refs` 的语义别名，纯 reducer 因此正确返回空 ready 集；仅增加条件事实默认值会错误地授权执行，必须改为明确诊断并要求提交真实证据引用。
- The affected task root used symbolic conditions never produced by the Coordinator and semantic aliases never mapped to real `evidence_refs`; the pure reducer therefore correctly returned an empty ready set. Defaulting facts to true would authorize incorrectly, so the fix must expose diagnostics and require real evidence references.

## v10.1.14 (2026-09-15 00:45) - codex

### 变更摘要 / Change Summary [完成]

- [model] [exp] 验证已完成下载的 Qwen3-VL-4B compressed-tensors 模型，并使用现有 RoboTwin RGB 中间产物执行隔离 worker 场景理解验收；不启动 Runtime 或动作。
- [Model] [Exp] Validate the fully downloaded Qwen3-VL-4B compressed-tensors model and run isolated-worker scene-understanding acceptance using existing RoboTwin RGB intermediates; do not start the Runtime or actions.

### 追加计划 / Additional Plan

- [model] [fix] 对 worker 返回的 singleton `ambiguities` 做有限、类型保持的数组归一化；继续拒绝未知字段、未知实体关系和任何 metric envelope。
- [Model] [Fix] Add a bounded, type-preserving normalization for a singleton worker `ambiguities` object while continuing to reject unknown fields, unknown entity relations, and all metric envelopes.

- [model] [fix] 强化通用视觉提示，要求检查完整画面、边缘及小型或低对比度物体，避免把可见目标误判为空；不注入任务答案或模拟器事实。
- [Model] [Fix] Strengthen the generic visual prompt to inspect the complete image, edges, and small or low-contrast objects, reducing false-empty detections without injecting task answers or simulator facts.

- [model] [fix] 为通用语义 JSON 增加不含任务对象的 schema skeleton，改善 4B 模型的结构化输出遵循，同时继续由 provider 校验实体、关系和证据边界。
- [Model] [Fix] Add a task-neutral semantic JSON skeleton to improve structured-output adherence for the 4B model while retaining provider validation of entities, relations, and evidence boundaries.

### 文件变更详情 / File Changes

- [修改] `examples/forge-adapters/robotwin20/runtime/qwen3_vl_worker.py` L101-L122：将 singleton ambiguity 归一化为数组，并使用任务无关的完整画面检查和 JSON skeleton 提示；未加入颜色、方块或目标顺序等 benchmark 答案。
- [Modified] `examples/forge-adapters/robotwin20/runtime/qwen3_vl_worker.py` L101-L122: normalized a singleton ambiguity to an array and added task-neutral full-image and JSON-skeleton instructions; no benchmark color, cube, or ordering answer was injected.

### 验证结果 / Validation Result

- 4B 模型 GPU worker：加载、`worker_ready`、真实 RGB 生成、`request_completed` 和 shutdown 均通过；峰值显存未触发 OOM（GPU 独占运行）。
- Qwen 4B GPU worker passed model load, `worker_ready`, real RGB generation, `request_completed`, and shutdown without OOM when GPU-exclusive.
- 使用现有中间产物 `/tmp/paos-robotwin20-agent-preflight-20260911/...-000001/rgb.png`，worker 返回合法的四字段 JSON 和空数组结构，但本次结构化提示下 `entities=[]`，未达到红/绿/蓝实体识别验收标准；因此没有继续 scene.bind、PlanGraph 或动作。
- Using the existing RGB intermediate, the worker returned valid four-field JSON with array shapes, but `entities=[]` under the structured prompt and did not meet the red/green/blue entity-recognition acceptance criterion; scene.bind, PlanGraph, and actions were not attempted.
- 直接自然语言对照提示在同一模型/图像上正确识别三种颜色，说明模型视觉能力可用，但当前 JSON prompt-following 仍需进一步优化或改用受约束生成 backend；不能将该结果报告为完成。
- A direct natural-language control prompt on the same model/image correctly identified the three colors, indicating usable visual capability but insufficient structured-prompt adherence; this result must not be reported as task completion.

### 六维验收状态 / Six-Dimension Status

1. 架构集成：通过；provider 仍位于 adapter worker seam。
2. Architecture integration: passed; the provider remains behind the adapter worker seam.
3. 证据边界：通过；输入来自真实 RGB artifact，未使用 simulator truth，metric geometry 仍为空并由 RGB-D 管线负责。
4. Evidence boundary: passed; input came from the real RGB artifact, simulator truth was excluded, and metric geometry remains owned by RGB-D.
5. 生命周期与安全：通过；GPU-exclusive worker 完成后关闭，未启动 Runtime、Action、仿真运动或硬件。
6. Lifecycle and safety: passed; the GPU-exclusive worker shut down after the query, with no Runtime, Action, simulator motion, or hardware started.
7. 结构化语义质量：未通过；实体数组为空，不能进入 scene.bind 或后续规划。
8. Structured semantic quality: not passed; the entity array was empty, so scene.bind and later planning remain blocked.

## v10.1.13 (2026-09-15 00:20) - codex

### 变更摘要 / Change Summary [完成]

- [env] [tune] 将 operator-owned `robotwin-persistent` deployment 环境切换到已下载的 Qwen3-VL-4B 模型目录和 `qwen3vl-4b` Python 解释器，并在不启动动作的前提下执行路径、依赖和中间产物验收。
- [Env] [Tune] Switch the operator-owned `robotwin-persistent` deployment environment to the downloaded Qwen3-VL-4B model directory and `qwen3vl-4b` interpreter, then validate paths, dependencies, and intermediate artifacts without starting actions.

### 预期修改 / Planned Changes

- `/home/yanxu/.PhyAgentOS/deployments/robotwin-persistent/runtime.env`
- `changelog/2026-09_part8.md`
- `CHANGELOG.md`

### 文件变更详情 / File Changes

- [修改] `/home/yanxu/.PhyAgentOS/deployments/robotwin-persistent/runtime.env` L12-L13：将 `ROBOTWIN20_QWEN_MODEL_PATH` 指向 `/home/yanxu/models/qwen3-vl-4b-instruct-awq-4bit`，将 `ROBOTWIN20_QWEN_PYTHON` 指向 `/home/yanxu/miniconda3/envs/qwen3vl-4b/bin/python`。
- [Modified] `/home/yanxu/.PhyAgentOS/deployments/robotwin-persistent/runtime.env` L12-L13: set `ROBOTWIN20_QWEN_MODEL_PATH` to `/home/yanxu/models/qwen3-vl-4b-instruct-awq-4bit` and `ROBOTWIN20_QWEN_PYTHON` to `/home/yanxu/miniconda3/envs/qwen3vl-4b/bin/python`.

### 验证结果 / Validation Result

- 4B 环境依赖检查通过：Python 3.11、Torch 2.9.0+cu128、Transformers 4.57.6、Accelerate 1.15.0、compressed-tensors 0.12.2。
- The 4B environment dependency check passed: Python 3.11, Torch 2.9.0+cu128, Transformers 4.57.6, Accelerate 1.15.0, and compressed-tensors 0.12.2.
- 模型配置检查通过：`Qwen3VLForConditionalGeneration`、`quant_method=compressed-tensors`、`num_bits=4`。
- Model configuration inspection passed: `Qwen3VLForConditionalGeneration`, `quant_method=compressed-tensors`, and `num_bits=4`.
- Worker 加载验证暂缓：模型目录约 3.6 GB，权重仍为 `.incomplete` 文件，缺少完整 safetensors index/shards；worker 已按设计返回 `worker_unavailable`，未启动 Runtime、Gateway Action 或仿真运动。
- Worker load verification is pending: the model directory is about 3.6 GB with an `.incomplete` weight file and no complete safetensors index/shards; the worker correctly returned `worker_unavailable`, and no Runtime, Gateway Action, or simulator motion was started.

## v10.1.12 (2026-09-14 21:00) - codex

### 变更摘要 / Change Summary [完成]

- [model] [feat] 接入 adapter-owned Qwen3-VL-8B compressed-tensors 4-bit 场景语义 provider；通过独立 JSONL worker、配置选择和既有 `scene.understand` seam 提供实体/关系/歧义语义，不把模型依赖导入 PAOS Core，不产生运动授权。
- [model] [feat] Add an adapter-owned Qwen3-VL-8B compressed-tensors 4-bit semantic provider through an isolated JSONL worker, profile selection, and the existing `scene.understand` seam; keep model dependencies out of PAOS Core and never produce motion authorization.

### 文件变更详情 / File Changes

- [新增] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/qwen3_vl_scene_understanding.py` L1-L185：实现 Qwen provider seam、artifact path 解析、语义字段校验、provenance 注入和 metric envelope 拒绝；每次查询后释放 worker。
- [Added] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/qwen3_vl_scene_understanding.py` L1-L185: implemented the Qwen provider seam, artifact-path resolution, semantic validation, provenance injection, metric-envelope rejection, and per-query worker release.
- [新增] `examples/forge-adapters/robotwin20/runtime/qwen3_vl_worker.py` L1-L135：实现隔离 JSONL worker、本地 Transformers 加载、CPU/CUDA 显式设备映射和严格 JSON 解析；不导入 PAOS、Gateway 或 simulator。
- [Added] `examples/forge-adapters/robotwin20/runtime/qwen3_vl_worker.py` L1-L135: added the isolated JSONL worker with local Transformers loading, explicit CPU/CUDA placement, and strict JSON parsing; it imports no PAOS, Gateway, or simulator code.
- [修改] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/persistent_host.py` L35-L36,L232-L298：增加 `qwen3_vl_local` provider 分支，使用独立 Python、脚本、工作目录和超时配置；保留 OpenAI provider 兼容路径。
- [Modified] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/persistent_host.py` L35-L36,L232-L298: added the `qwen3_vl_local` provider branch with isolated Python, script, cwd, and timeout settings while retaining OpenAI compatibility.
- [修改] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/openai_scene_understanding.py` L20-L34,L68-L72：为 filesystem artifact payload 保留 adapter-local RGB path，OpenAI 公共接口仍只接收 bytes/URI。
- [Modified] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/openai_scene_understanding.py` L20-L34,L68-L72: retained an adapter-local RGB path for filesystem workers while the OpenAI public seam continues to receive bytes/URIs only.
- [修改] `examples/forge-adapters/robotwin20/profiles/forge-persistent/persistent-host.yaml` L18-L31；`examples/forge-skills/pick-place-workflow/profiles/robotwin-persistent/runtime.env.example` L13-L14：加入 Qwen 本地模型路径和 worker Python 配置。
- [Modified] `examples/forge-adapters/robotwin20/profiles/forge-persistent/persistent-host.yaml` L18-L31 and `examples/forge-skills/pick-place-workflow/profiles/robotwin-persistent/runtime.env.example` L13-L14: added local Qwen model and worker-Python configuration.
- [修改] `examples/forge-adapters/robotwin20/pyproject.toml` L9-L16；`examples/forge-adapters/robotwin20/src/robotwin20_adapter/__init__.py` L91-L95：声明可选 Qwen 依赖并导出 provider 类型。
- [Modified] `examples/forge-adapters/robotwin20/pyproject.toml` L9-L16 and `examples/forge-adapters/robotwin20/src/robotwin20_adapter/__init__.py` L91-L95: declared optional Qwen dependencies and exported provider types.
- [新增] `examples/forge-adapters/robotwin20/tests/test_qwen3_vl_scene_understanding.py` L1-L91：覆盖语义投影、provenance、metric 输出拒绝、未知关系和模型目录校验。
- [Added] `examples/forge-adapters/robotwin20/tests/test_qwen3_vl_scene_understanding.py` L1-L91: covered semantic projection, provenance, metric-output rejection, unknown relations, and model-directory validation.
- [修改] `examples/forge-adapters/robotwin20/README.md` L120-L145：记录 Qwen 安装、配置、顺序 worker 生命周期和低显存 CPU fallback。
- [Modified] `examples/forge-adapters/robotwin20/README.md` L120-L145: documented Qwen installation, configuration, sequential worker lifetime, and low-VRAM CPU fallback.

### 真实中间产物验收 / Intermediate-Artifact Acceptance

- 使用 `/tmp/paos-robotwin20-agent-preflight-20260911/...-000001/rgb.png` 及同一 capture 的 scene revision `e2f84bbf24f84792b3af3cbe637ab1d6-1` 完成 adapter-level acceptance：三实体、两条 `left_of` 关系、RGB artifact provenance、空 metric envelopes、worker release 均通过；未启动 Runtime、Gateway Action、仿真运动或硬件。
- Using the existing RGB capture and scene revision `e2f84bbf24f84792b3af3cbe637ab1d6-1`, adapter-level acceptance passed for three entities, two `left_of` relations, RGB provenance, empty metric envelopes, and worker release; no Runtime, Gateway Action, simulator motion, or hardware was started.
- Qwen worker CPU load passed with `transformers 4.57.6` and `compressed-tensors 0.12.2`; safetensors inspection confirmed compressed-tensors int32 weight packing plus BF16 vision/non-quantized tensors, with model parameters resident at about 7.6 GB. GPU generation still peaked at about 14.8 GiB on the 16-GiB card, so a concurrently resident RoboTwin process failed closed with CUDA OOM. This is an operational sequencing constraint, not a safety-gate relaxation.

### 六维验收 / Six-Dimension Acceptance

1. 架构集成：通过；PAOS Core 只看到既有 `scene.understand` seam，Qwen 依赖隔离在 adapter worker。
2. Architecture integration: passed; PAOS Core sees only the existing `scene.understand` seam and Qwen dependencies remain inside the adapter worker.
3. 证据与边界：通过；语义 claims 绑定本次 RGB artifact，metric geometry 继续由 RGB-D/depth/calibration 管线拥有，禁止 simulator truth。
4. Evidence and boundaries: passed; semantic claims bind to the current RGB artifact, while metric geometry remains owned by RGB-D/depth/calibration and simulator truth is excluded.
5. 错误与可观测性：通过；模型目录缺失、worker 不可用、非 JSON、未知实体关系和 metric envelope 均 fail-closed；JSONL lifecycle events 可观测。
6. Errors and observability: passed; missing model files, worker unavailability, non-JSON, unknown relations, and metric envelopes fail closed, with JSONL lifecycle events exposed.
7. 显存与生命周期：有条件通过；CPU fallback 通过，GPU 生成需独占显存并在 Qwen、LocateAnything、SAM2 之间顺序释放，不能并行常驻。
8. VRAM and lifecycle: conditionally passed; CPU fallback passed, while GPU generation requires exclusive VRAM and sequential release between Qwen, LocateAnything, and SAM2.
9. 测试与可维护性：通过；RoboTwin adapter 全套 `463 passed`，Qwen provider 聚焦测试 `6 passed`，changed-file Ruff、compileall、`git diff --check` 全部通过。
10. Testing and maintainability: passed; the full RoboTwin adapter suite reports `463 passed`, the Qwen provider focused tests report `6 passed`, and changed-file Ruff, compileall, and `git diff --check` pass.
11. 机器人安全：通过；未新增动作、授权或 verifier 旁路，motion separation 和既有 readiness gate 保持不变。
12. Robotics safety: passed; no action, authorization, or verifier bypass was added, and motion separation plus existing readiness gates remain unchanged.

### 预期修改 / Planned Changes

- `examples/forge-adapters/robotwin20/src/robotwin20_adapter/qwen3_vl_scene_understanding.py`
- `examples/forge-adapters/robotwin20/runtime/qwen3_vl_worker.py`
- `examples/forge-adapters/robotwin20/src/robotwin20_adapter/persistent_host.py`
- `examples/forge-adapters/robotwin20/profiles/forge-persistent/persistent-host.yaml`
- `examples/forge-adapters/robotwin20/pyproject.toml`
- `examples/forge-adapters/robotwin20/tests/test_qwen3_vl_scene_understanding.py`
- `examples/forge-adapters/robotwin20/tests/test_persistent_host.py`
- `examples/forge-adapters/robotwin20/README.md`

### 失败场景与边界 / Failure Scenario and Boundary

Qwen provider 不可用、模型目录不完整、worker 超时或输出非 JSON 时，`scene.understand` 必须返回有界 provider failure；不得回退到 simulator truth、旧场景证据或伪造语义结果。Qwen、LocateAnything 与 SAM2 仍按顺序 worker 生命周期运行，以适配 16GB 显存。

When the Qwen provider is unavailable, the model directory is incomplete, the worker times out, or output is not JSON, `scene.understand` must expose a bounded provider failure; it must not fall back to simulator truth, stale evidence, or fabricated semantics. Qwen, LocateAnything, and SAM2 retain sequential worker lifetimes for the 16GB GPU.

## v10.1.10 (2026-09-14 19:30) - codex

### 变更摘要 / Change Summary [完成]

- [env] [feat] 为 `paos skill start` 增加显式 operator-owned `--env-file`，由 Skill Runtime 统一解析、合并、校验并投影启动环境，不把环境值写入 Runtime state、日志或 evolution evidence。(local)
- [policy] [fix] 统一 Runtime preflight、Bundle `start.sh`、Dora coordinator 与 flow 使用的环境，并移除自包含 RoboTwin Node 已内部持有的 `PAOS_ROBOTWIN20_ADAPTER_ROOT` 外部要求。(local)
- [docs] [docs] 记录 deployment env、secret、Bundle 与 Runtime-generated state 的所有权边界，并提供不含主机值或凭据的 RoboTwin 环境模板。(local)
- [Env] [Feat] Add an explicit operator-owned `--env-file` to `paos skill start`, with Skill Runtime owning parsing, precedence, validation, and launch projection without persisting values to Runtime state, logs, or evolution evidence. (local)
- [Policy] [Fix] Use one launch environment for Runtime preflight, Bundle `start.sh`, the Dora coordinator, and the flow, and remove the external `PAOS_ROBOTWIN20_ADAPTER_ROOT` requirement already owned by the self-contained RoboTwin Node. (local)
- [Docs] [Docs] Document ownership boundaries for deployment environment, secrets, Bundles, and Runtime-generated state, with a RoboTwin template containing no host values or credentials. (local)

### 预期影响文件 / Expected Files

- `PhyAgentOS/skill_runtime/environment.py`
- `PhyAgentOS/skill_runtime/manager.py`
- `PhyAgentOS/cli/commands.py`
- `examples/forge-skills/pick-place-workflow/skill.yaml`
- `examples/forge-skills/pick-place-workflow/pyproject.toml`
- `examples/forge-skills/pick-place-workflow/profiles/robotwin-persistent/dataflow.yaml`
- `examples/forge-skills/pick-place-workflow/profiles/robotwin-persistent/runtime.env.example`
- `tests/test_skill_runtime_environment.py`
- `examples/forge-skills/pick-place-workflow/tests/test_runtime_install_discovery.py`
- `examples/forge-skills/pick-place-workflow/tests/test_grasp_propose.py`
- `docs/user_development_guide/README.md`
- `docs/zh/04-forge-configuration-reference.md`
- `CHANGELOG.md`
- `changelog/2026-09_part8.md`

### 失败场景 / Failure Scenario

当前 robotwin-persistent profile 依赖大量交互式 shell exports；新环境遗漏任一变量时只能在启动时逐项失败。与此同时，preflight 只读取 `os.environ`，实际 Dora 启动却另行覆盖 `profile.environment`，造成校验输入与执行输入不一致。自包含 Node 已把 adapter root 绑定到其嵌入 payload，但 Skill 仍要求操作员提供源码树路径，导致发布制品无法真正脱离仓库启动。

The robotwin-persistent profile currently depends on many interactive shell exports, so a clean host fails incrementally when any value is omitted. Preflight reads only `os.environ` while Dora launch separately overlays `profile.environment`, so validation and execution do not use the same input. The self-contained Node already binds its adapter root to the embedded payload, yet the Skill still requires an operator-supplied source-tree path, preventing repository-independent startup.

### 防御机制边界 / Defensive Mechanism Boundary

本次不新增 hash、baseline、冻结 contract 或运动 gate。环境文件只在现有 Runtime 启动这一跨进程部署边界执行普通语法、重复键与必需变量校验；既有 Gateway readiness、planning binding、Action admission、frame/calibration 和 verifier 门禁保持不变。

This change adds no hash, baseline, frozen contract, or motion gate. The environment file receives ordinary syntax, duplicate-key, and required-variable validation only at the existing cross-process Runtime startup boundary; existing Gateway readiness, planning binding, Action admission, frame/calibration, and verifier gates remain unchanged.

### 验证计划 / Validation Plan

- 覆盖 env 文件解析、注释/空行、重复键、无效名称、空必需值、优先级和错误信息不泄露值。
- 覆盖同一合并环境进入 preflight、start hook、Dora up 与 Dora flow；确认 Runtime state 不持久化环境值。
- 覆盖 CLI `--env-file` 帮助与调用传递、RoboTwin dataflow/manifest 外部占位符一致性、Node 内部 adapter-root ownership。
- 运行核心 PAOS suite、pick-place Runtime 安装专项、Ruff、compileall 与 `git diff --check`；不启动 Runtime、Gateway Action、仿真运动或硬件。

### 文件变更详情 / File Changes

- [新增] `PhyAgentOS/skill_runtime/environment.py` L1-L67：实现非执行式 UTF-8 `KEY=VALUE` 解析及 `manifest environment < env file < process environment` 合并；拒绝不可读文件、非法名称、重复键和未闭合引号，错误不回显值。
- [修改] `PhyAgentOS/skill_runtime/manager.py` L23-L26、L94-L190、L335-L480：`RuntimeManager.start()` 接收 `env_file`，只构造一次 launch environment，并将同一对象用于 preflight、`start.sh`、Dora coordinator 和 flow；required environment 从实际 launch environment 校验，Runtime state schema 不增加环境字段。
- [修改] `PhyAgentOS/cli/commands.py` L1546-L1566：增加 `paos skill start --env-file <path>` 并传递至 RuntimeManager。
- [修改] `examples/forge-skills/pick-place-workflow/skill.yaml` L1-L57、`pyproject.toml` L1-L3：移除自包含 Node 内部持有的 adapter-root 外部要求，并将变更后的 Skill/包版本从 `2.0.0` 提升到 `2.0.1`，避免同版本不同 Bundle 内容。
- [修改] `examples/forge-skills/pick-place-workflow/profiles/robotwin-persistent/dataflow.yaml` L1-L34：删除外部 `PAOS_ROBOTWIN20_ADAPTER_ROOT` 占位符；Node launcher 继续把嵌入 payload 投影为该内部变量。
- [新增] `examples/forge-skills/pick-place-workflow/profiles/robotwin-persistent/runtime.env.example` L1-L33：提供只含非 secret 变量名的部署模板；API key 明确由 operator secret environment 注入。
- [新增测试] `tests/test_skill_runtime_environment.py` L1-L285：覆盖解析、失败不泄露、优先级、单一环境贯穿四阶段、必需值、Runtime state 不持久化 secret，以及 CLI 参数传递。
- [修改测试] `examples/forge-skills/pick-place-workflow/tests/test_runtime_install_discovery.py` L63-L91、`test_grasp_propose.py` L259-L269：覆盖 dataflow/manifest/template 集合一致性、adapter root 内部 ownership 与 Skill 版本同步。
- [修改文档] `docs/user_development_guide/README.md` L171-L190、`README_en.md` L179-L201、`docs/zh/04-forge-configuration-reference.md` L168-L175、`docs/en/04-forge-configuration-reference.md` L169-L178：记录 env-file 格式、优先级、实例目录、secret 和 Runtime-generated state 边界。
- [修改文档] `examples/forge-adapters/robotwin20/README.md` L606-L626：给出自包含 Node 的 operator-owned deployment 文件及独立 secret 启动示例。
- [修改日志] `CHANGELOG.md` L3-L147、`changelog/2026-09_part8.md` L1-L108：新增归档与最近版本记录。

### 关键 Diff / Key Diff

```diff
- state = RuntimeManager().start(skill_name, profile)
+ state = RuntimeManager().start(skill_name, profile, env_file=env_file)
```

```diff
- missing = [name for name in required_environment if not os.environ.get(name)]
- dora_up_env = {**os.environ, **profile.environment, ...}
- dora_flow_env = {**os.environ, **profile.environment, ...}
+ launch_environment = compose_runtime_environment(profile.environment, env_file=env_file)
+ missing = [name for name in required_environment if not launch_environment.get(name)]
+ preflight/start_hook/dora_up/dora_flow use the same launch_environment
```

```diff
- required_environment: [PAOS_ROBOTWIN20_ADAPTER_ROOT, ROBOTWIN20_PAOS_PYTHON, ...]
+ required_environment: [ROBOTWIN20_PAOS_PYTHON, ...]
+ # Node launcher owns PAOS_ROBOTWIN20_ADAPTER_ROOT from its embedded payload.
```

### 六维审核 / Six-Dimension Review

1. 架构集成：通过；解析属于 Skill Runtime，CLI 只传路径，Bundle 只声明名字，Node 继续持有 adapter payload。
2. 配置外部化：通过；真实主机路径与 secret 不进入源码模板或 Bundle 固定值。
3. 错误与可观测性：通过；语法/重复/缺失错误在 Dora admission 前返回，错误不回显配置值。
4. 测试与复现：通过；新增专项、核心、Skill、Adapter、打包和静态检查均有可重复命令。
5. 机器人安全：通过；未修改 readiness、planning binding、Action admission、frame/calibration 或 verifier，也未启动 Runtime 或运动。
6. 可维护性与进化边界：通过；没有旁路 loader、TODO 或 fake success；环境不进入 Runtime state、trace 或 evolution experience。

### 验证 / Validation

- Environment/Runtime focused：`16 passed`。
- Core PAOS：`366 passed`。
- Pick-place Skill：`330 passed`。
- RoboTwin adapter：`459 passed`。
- Skill Bundle：成功生成 `pick-place-workflow-2.0.1.tar.gz`，`110760` bytes；打包校验通过。
- Changed-file Ruff、compileall、CLI help 与 `git diff --check`：通过。
- 初次 pytest 被外部 ROS Jazzy `launch_testing` 插件的缺失 `lark` 阻断；使用 `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1` 隔离非项目插件后通过。初次 Adapter 命令漏加 `runtime/` 和 `scripts/` 的 `PYTHONPATH`，补齐真实套件路径后 `459 passed`；两者均不是生产代码失败。
- 没有启动 Runtime、Query、Gateway Action、仿真、硬件或运动。

### Git 提交 / Git Commit

- 功能提交 / Functional commit: `a1aa477`；分支 / branch: `feature/planning-loop`。
- 日志回执提交将在写入上述不可自引用的功能提交 hash 后生成，并推送同一分支。
- The log receipt commit is created after recording the non-self-referential functional commit hash and is pushed to the same branch.
