# 2026-09 Part 12

## v11.3.1 (2026-09-21 18:20) - codex

### 变更计划 / Planned changes

- [env] [tune] [完成] 将本次 RoboTwin Oracle 验收使用的 Agent/感知模型从显式配置的 `gpt-5.6-terra`、medium 调整为 `gpt-5.6-sol`、high；不改变 Provider、Tool 契约、AgentLoop 重试或运动安全边界。(local)
- [Env] [Tune] [Completed] Change the explicitly configured Agent/perception model for this RoboTwin Oracle acceptance run from `gpt-5.6-terra`, medium to `gpt-5.6-sol`, high; do not change the Provider, Tool contracts, AgentLoop retry behavior, or motion-safety boundaries. (local)

### 预期影响文件 / Expected affected files

- `/home/yanxu/.PhyAgentOS/config-rgb-no-evolution-long.json`
- `/home/yanxu/.PhyAgentOS/deployments/robotwin-persistent/runtime.env`
- `changelog/2026-09_part12.md`

### 实际修改 / Completed changes

- [env] [tune] `/home/yanxu/.PhyAgentOS/config-rgb-no-evolution-long.json:L5,L13` changed `model=gpt-5.6-terra` → `gpt-5.6-sol` and `reasoningEffort=medium` → `high`; `/home/yanxu/.PhyAgentOS/deployments/robotwin-persistent/runtime.env:L11` changed `ROBOTWIN20_MODEL=gpt-5.6-terra` → `gpt-5.6-sol`.(local)
- [Env] [Tune] `/home/yanxu/.PhyAgentOS/config-rgb-no-evolution-long.json:L5,L13` changed `model=gpt-5.6-terra` → `gpt-5.6-sol` and `reasoningEffort=medium` → `high`; `/home/yanxu/.PhyAgentOS/deployments/robotwin-persistent/runtime.env:L11` changed `ROBOTWIN20_MODEL=gpt-5.6-terra` → `gpt-5.6-sol`. (local)

### 验证 / Validation

- JSON parsing and environment inspection passed. `task.goal` read-only smoke under `gpt-5.6-sol/high` succeeded with benchmark destinations; the full Oracle task later stopped at a non-retryable `scene.understand` provider failure before any Action.

## v11.3.0 release closeout (2026-09-21 15:10) - codex

### 变更计划 / Planned changes

- [chore] [chore] [完成] 在停止运行中的 Skill 2.6.5 后，将已验证的 benchmark-first 实现递增为 Skill 2.6.6 / Node 0.6.6，重建并隔离安装；运行核心、Skill、Adapter 测试和 Oracle benchmark 验收，保存七维报告及独立的 PAOS/RoboTwin/视频证据。(local)
- [Chore] [Chore] [Completed] After stopping the running Skill 2.6.5, advance the verified benchmark-first implementation to Skill 2.6.6 / Node 0.6.6, rebuild and install it in isolation; run core, Skill, Adapter tests and Oracle benchmark acceptance, preserving the seven-dimension report and independent PAOS/RoboTwin/video evidence. (local)

### 预期影响文件 / Expected affected files

- `examples/forge-skills/pick-place-workflow/skill.yaml`
- `examples/forge-skills/pick-place-workflow/pyproject.toml`
- `examples/forge-adapters/robotwin20/pyproject.toml`
- `examples/forge-skills/pick-place-workflow/tests/test_grasp_propose.py`
- `docs/forge/IMPLEMENTATION_REVIEW_V11_3_0.md`
- `changelog/2026-09_part12.md`

### 实际修改 / Completed changes

- [chore] [chore] `examples/forge-skills/pick-place-workflow/skill.yaml:L3,L145-L152`, `examples/forge-skills/pick-place-workflow/pyproject.toml:L3`, and `examples/forge-adapters/robotwin20/pyproject.toml:L3` advanced the immutable release to Skill 2.6.6 / Node 0.6.6; Node SHA `a888f999cbfae2b8b7f3bea9d3bf045f4dc043ba6d7cb168e3cd6d169824b042` was verified. The bundle was installed locally without overwriting 2.6.5.
- [eval] [exp] Core `509 passed`, Skill `342 passed`; Adapter `623 passed, 1 skipped, 9` SciPy-environment failures under PAOS, with the dedicated SciPy environment previously recording `632 passed, 1 skipped`. Oracle direct goal/capability smoke passed; live full-task runs stopped before any Action. Report: `docs/forge/IMPLEMENTATION_REVIEW_V11_3_0.md:L1-L85`.

### Git 提交 / Git commit

- Implementation commit: `f8ba9d8` (benchmark-first binding and seven-dimension report).
- Release-log commit: `2243f56` (records the Sol/high acceptance configuration and implementation commit reference).
- Model configuration `gpt-5.6-sol/high` is operator-owned under `/home/yanxu/.PhyAgentOS`; it is intentionally not part of the repository commit.

## v11.2.3 (2026-09-21 13:58) - codex

### 变更计划 / Planned changes

- [agent] [docs] [计划] 保存当前长程任务四类诊断：模型由配置明确选择 `gpt-5.6-terra`；`manipulation.prepare` 的 Coordinator 拒绝来自缺少 `destination_ref`/`capability_snapshot_ref` 的图绑定传播缺陷；AgentLoop 将确定性绑定缺失当作可重复选择错误；当前任务没有采用 benchmark 目标区域，后续将以显式 `task.goal` 基线模式跑通流程。(local)
- [Agent] [Docs] [Planned] Preserve four diagnostics from the current long-horizon task: the configured model is explicitly `gpt-5.6-terra`; the `manipulation.prepare` Coordinator rejection comes from PlanGraph binding propagation missing `destination_ref`/`capability_snapshot_ref`; AgentLoop treats deterministic binding absence as a repeatable selection error; the current task did not use benchmark target regions, and a later explicit `task.goal` baseline profile will be used to run the full workflow first. (local)

### 变更摘要 / Change summary

- [docs] [新增] 保存 `docs/forge/DIAGNOSTIC_BENCHMARK_GOAL_BASELINE_V11_2_3.md`，记录事实、证据、架构判断和暂不修改边界；本次仅保存诊断，不修改运行逻辑、不恢复或重试活动任务。
- [docs] [Added] Save `docs/forge/DIAGNOSTIC_BENCHMARK_GOAL_BASELINE_V11_2_3.md` with facts, evidence, architectural findings, and non-goals; this change records diagnosis only and does not alter runtime logic or resume/retry the active task.

### 预期影响文件 / Expected affected files

- `docs/forge/DIAGNOSTIC_BENCHMARK_GOAL_BASELINE_V11_2_3.md`
- `changelog/2026-09_part12.md`

### 实际修改 / Completed changes

- [docs] [新增] `docs/forge/DIAGNOSTIC_BENCHMARK_GOAL_BASELINE_V11_2_3.md:L1-L179` 保存四类问题的可复核证据、PAOS 所有权判断、benchmark 目标注入基线边界和后续实现前的验收条件；本次未修改运行逻辑、未恢复或重试活动任务。(local)
- [Docs] [Added] `docs/forge/DIAGNOSTIC_BENCHMARK_GOAL_BASELINE_V11_2_3.md:L1-L179` records reproducible evidence for the four issues, PAOS ownership decisions, the benchmark-goal injection boundary, and pre-implementation acceptance conditions; runtime logic was not changed and the active task was not resumed or retried. (local)
- [docs] [修改] `changelog/2026-09_part12.md:L3-L31` 将本次诊断从计划状态补全为实际文档记录。(local)
- [Docs] [Modified] `changelog/2026-09_part12.md:L3-L31` completes the planned diagnostic entry with the actual documentation record. (local)

### 验证 / Validation

- `git diff --check`: passed.
- 文档以 UTF-8 写入并可正常读取；活动任务数据库未写入任何状态变化。(local)
- `git diff --check`: passed.
- The document is UTF-8 and readable; no active-task state was changed in the task database. (local)

### Anti-OverDefense 依据 / Anti-OverDefense rationale

本次只保存已经由任务记录和源码定位支持的诊断，不新增 hash、冻结 contract、任务状态机、重试、目标改写或安全 gate。后续 benchmark 目标注入必须通过现有 `task.goal` Runtime Query，并保持与 observation-owned 模式隔离。(local)

This change records only diagnoses supported by task records and source inspection. It adds no hash, frozen contract, task state machine, retry, goal rewrite, or safety gate. Later benchmark-goal injection must use the existing Runtime `task.goal` Query and remain isolated from observation-owned mode. (local)

### Git 提交 / Git commit

- Commit: `9caef21`
- Branch: `feature/planning-loop`
- 时间 / Time: 2026-09-21 13:58 Asia/Shanghai

## v11.2.2 (2026-09-21 16:00) - codex

### 变更计划 / Planned changes

- [policy] [fix] [计划] 修复语义节点 Agent 回合重复注入完整 Skill 指令和节点上下文的问题；保留 Coordinator 中的完整 SkillUse 审计记录，仅向模型发送一次能力相关的有界 Skill 摘要和唯一 node-scoped projection，避免 `manipulation.target` 之后上下文重复导致 420 秒回合超时。(local)
- [Policy] [Fix] [Planned] Fix semantic-node Agent turns duplicating the full Skill instructions and node context; retain complete SkillUse instructions in the Coordinator audit record while sending the model one capability-scoped bounded Skill summary and one node-scoped projection, preventing duplicated context from causing 420-second turn timeouts after `manipulation.target`. (local)

### 预期影响文件 / Expected affected files

- `PhyAgentOS/agent/loop.py`
- `PhyAgentOS/agent/prompt_context.py`
- `tests/test_agent_foundation.py`
- `tests/test_prompt_context.py`
- `docs/forge/PLANNING_MODULE_DESIGN.md`
- `docs/forge/MANIPULATION_DAG_DEVELOPER_GUIDE.md`

### 实际修改 / Completed changes

- [agent] [fix] [完成] `PhyAgentOS/agent/loop.py:L1039-L1071` 移除节点回合对完整 `primary_skill_instructions` 的重复注入；保留原始目标、验证契约、Skill/Runtime binding identity、SkillUse identity 和 bounded node context。`PhyAgentOS/agent/prompt_context.py:L477-L480` 移除任务投影中的完整 Skill 文档字段，完整内容继续由 Coordinator 持久化用于审计和验证。(local)
- [Agent] [Fix] [Completed] `PhyAgentOS/agent/loop.py:L1039-L1071` removes duplicate full `primary_skill_instructions` from node-turn messages while retaining the original goal, verification contract, Skill/Runtime binding identity, SkillUse identity, and bounded node context. `PhyAgentOS/agent/prompt_context.py:L477-L480` removes the full Skill document from task prompt projections; Coordinator persistence remains the audit and verification source. (local)
- [tests] [fix] [完成] `tests/test_agent_foundation.py:L657-L695` 和 `tests/test_prompt_context.py:L805-L820` 验证完整 Skill 文本不进入节点/任务投影，且 binding、revision、node obligation 与 evidence refs 仍保留。(local)
- [Tests] [Fix] [Completed] `tests/test_agent_foundation.py:L657-L695` and `tests/test_prompt_context.py:L805-L820` verify that full Skill text is absent from node/task projections while binding, revision, node obligations, and evidence refs remain present. (local)
- [docs] [docs] [完成] `docs/forge/PLANNING_MODULE_DESIGN.md:L169-L173`、`docs/forge/MANIPULATION_DAG_DEVELOPER_GUIDE.md:L586-L588` 明确完整 Skill 只留在 Coordinator 审计域，节点模型只接收有界执行上下文。(local)
- [Docs] [Docs] [Completed] `docs/forge/PLANNING_MODULE_DESIGN.md:L169-L173` and `docs/forge/MANIPULATION_DAG_DEVELOPER_GUIDE.md:L586-L588` document that complete Skill content remains in the Coordinator audit domain and node models receive only bounded execution context. (local)

### 验证 / Validation

- Core: `505 passed`。
- Skill: `342 passed`。
- Planning/prompt focused regression: `71 passed`。
- Adapter: `622 passed, 9 failed, 1 skipped`; all 9 failures are the pre-existing `scipy`-missing `observed_collision` tests in the PAOS interpreter, not failures from this context change.
- Ruff, `compileall`, and `git diff --check`: passed.
- Acceptance task `task_249b7c3f82a243e5` was stopped through Coordinator as `cancelled`; it had six discovery Query records, no PlanGraph, no planning selection, no Action, no world change, and no video.

### Anti-OverDefense 依据 / Anti-OverDefense rationale

具体失败场景是当前节点回合将约 22.6K 字符完整 Skill 文档和 node-scoped projection 同时发给模型，导致 `manipulation.target` 后的 420 秒决策回合耗尽；Coordinator 已保存完整 Skill 内容、SkillUse、版本和 binding identity，因此无需新增 hash、状态机、重试或安全 gate。/ The concrete failure scenario was a node turn sending the approximately 22.6K-character full Skill document together with the node-scoped projection until the 420-second decision budget was exhausted after `manipulation.target`; Coordinator already persists complete Skill content, SkillUse, version, and binding identity, so no new hash, state machine, retry, or safety gate was needed.

### Git 提交 / Git commit

- Commit: `91ea918cea54b689550554860c11dc5ad1b9d414`
- Branch: `feature/planning-loop`
- 时间 / Time: 2026-09-21 16:00 Asia/Shanghai

## v11.2.1 (2026-09-21 03:05) - codex

### 变更计划 / Planned changes

- [env] [fix] [计划] 修复 Oracle Runtime 重启后仍加载旧 Skill/scene-understanding 配置的问题；将外部 scene understanding fallback 与当前验收模型、推理预算和唯一 artifact root 对齐，保留 observed profile 的 fail-closed 行为。(local)
- [Env] [Fix] [Planned] Fix the Oracle Runtime restart loading stale Skill/scene-understanding configuration; align the external scene-understanding fallback with the current acceptance model, reasoning budget, and unique artifact root while preserving fail-closed observed behavior. (local)
- [eval] [fix] [计划] 为 scene.understand 的远端 Provider timeout 持久化实际阶段与 bounded reason，避免把 180 秒 Gateway deadline 误报为 Agent/provider timeout；unknown Query 仍须 Coordinator 对账后才能恢复。(local)
- [Eval] [Fix] [Planned] Persist the actual stage and bounded reason for remote scene.understand Provider timeouts so the 180-second Gateway deadline is not reported as an Agent/provider timeout; unknown Queries still require Coordinator reconciliation before recovery. (local)
- [chore] [chore] [计划] 停止本次无法恢复的 task_873fc01cc8ef4c97，重建/安装修复后的 Skill/Node，使用独立验收目录重新执行完整 AgentLoop、双重 verdict 与累计双视角视频验收。(local)
- [Chore] [Chore] [Planned] Stop unrecoverable task_873fc01cc8ef4c97, rebuild/install the repaired Skill/Node, and rerun complete AgentLoop, dual verdict, and cumulative two-view video acceptance in a unique run directory. (local)
- [policy] [fix] [计划] 修复显式 Oracle grasp Provider 与公共 `grasp.propose` schema 的输入错位：Oracle 仅在同一当前 observation 的空间包络 provenance 可确认时使用 actor 几何生成候选；观测/GraspGen 路径仍要求绑定 `object_point_cloud`，不改变 Core、动作授权或安全检查。(local)
- [Policy] [Fix] [Planned] Fix the explicit Oracle grasp Provider mismatch with the public `grasp.propose` schema: Oracle may use actor geometry only when the same current observation's spatial-envelope provenance is confirmed; observed/GraspGen paths still require a bound `object_point_cloud`, with no Core, motion-authority, or safety-check changes. (local)

### 失败场景与 Anti-OverDefense 依据 / Failure scenario and Anti-OverDefense rationale

- 具体失败场景：`task_873fc01cc8ef4c97` 的 `scene.understand` 在 worker deadline 约 180 秒后返回 unknown；运行进程环境仍显示 Skill `2.0.1`、GPT fallback `gpt-5.6-sol/high`，与已安装 `2.6.0` 和 Agent `terra/medium` 不一致。仅提高 Core timeout、重试未知 Query 或绕过 Coordinator 都不能修复部署身份与远端首响应瓶颈；因此只修复现有 profile/安装启动链，不新增任务状态机、hash 或自动重试。(local)
- 具体失败场景：`task_9ce8752d4e184cc9` 已在 Coordinator 持久化唯一 `grasp.propose` selection，Query 实际进入 Oracle Runtime，但 Adapter 因 target 未带可选的 `geometry_artifacts` 返回 `oracle grasp target requires one current geometry artifact`，最终被 Core 压缩为 `grasp_proposal_provider_error`；普通类型校验和事务无法表达“Oracle 只需同场景包络 provenance、GraspGen 必须点云”的 Provider-owned 差异，因此仅在 Oracle Adapter 收窄既有来源解析，不增加新 gate、重试或运动路径。(local)
- Concrete failure scenario: `task_9ce8752d4e184cc9` persisted one Coordinator selection and entered the Oracle Runtime, but the Adapter returned `oracle grasp target requires one current geometry artifact` because the target omitted optional `geometry_artifacts`; Core compressed this to `grasp_proposal_provider_error`. Ordinary schema and transactions cannot express the Provider-owned distinction that Oracle needs same-scene envelope provenance while GraspGen requires a point cloud, so the fix narrows existing source resolution only in the Oracle Adapter without a new gate, retry, or motion path. (local)
- Concrete failure scenario: `scene.understand` for `task_873fc01cc8ef4c97` became unknown after roughly the worker's 180-second deadline; the running process still exposed Skill `2.0.1` and GPT fallback `gpt-5.6-sol/high`, diverging from installed `2.6.0` and Agent `terra/medium`. Increasing Core timeouts, retrying an unknown Query, or bypassing the Coordinator cannot fix stale deployment identity or upstream first-response latency, so this change repairs the existing profile/install/start path without a new task state machine, hash, or automatic retry. (local)

## v11.1.0 (2026-09-20 21:22) - codex

- [policy] [fix] [完成] Adapter/Runtime 区分实际关节测量、限位内张开预测和未来抓持预测缺失；同步详细几何、单/批量 Curobo rollout 与对臂投影，实际执行每段读取关节测量。(local)
- [policy] [fix] [Completed] Separate measured joints, physically bounded open prediction and unavailable future holding prediction in Adapter/Runtime; synchronize detailed geometry, single/batch CuRobo rollouts and peer projection, measuring joints for each execution segment. (local)
- [sense] [fix] [完成] 支撑面附近使用 10×10×1 mm 网格，保留全部观测点和原有 1 mm 外扩；范围来自现有 profile，Core 与 AgentLoop 不增加几何逻辑。(local)
- [sense] [fix] [Completed] Use 10×10×1 mm cells near observed support, retaining every return and existing 1 mm padding through the profile; Core/AgentLoop gain no geometry policy. (local)
- [policy] [fix] [完成] 用户审核指出未知区间导致球额外膨胀 20 mm 属于过度拒绝，已撤回实现与 close 拒绝结论。缺少预测仅记录不可用并保留测量参考，不扩大球、不单独拒绝路线、不宣称已预测真实抓持宽度。(local)
- [policy] [fix] [Completed] User review identified 20 mm sphere enlargement for unknown holding intervals as excessive rejection; withdraw that implementation and its close-rejection conclusion. Missing prediction retains measured reference with explicit unavailable provenance; no sphere enlargement, independent rejection or claimed actual holding prediction. (local)
- [eval] [exp] [完成] 已按原七维进行源码/无运动验收；不新增释放 gate、hash、任务状态机、自动重试或目标改写，不安装或恢复现场任务。(local)
- [eval] [exp] [Completed] Complete established seven-dimension source/no-motion review; no release gate, hash, task state machine, automatic retry, goal rewrite, live install or task resume. (local)
- Anti-OverDefense: concrete failures are out-of-limit command geometry versus fixed-open planning and coarse grid overfill at a millimeter-scale support gap. Correct existing representation/state plumbing, retaining existing safety checks and native sphere margins.

### 验证与结论 / Validation and conclusions

- [eval] [exp] Core 504 passed; Adapter 591 passed, 1 skipped (Pillow unavailable); Skill 337 passed. Final Adapter rerun after withdrawing sphere enlargement: 591 passed. Ruff, Python 3.10 compile and diff checks pass.
- [eval] [exp] 76277 环境点及其不确定性全部保留，目标 523 点；5862 cells / 303 boxes；同一附着球净空 -4.1247→+4.1052 mm。/ All 76277 environment points and uncertainty retained, 523 target points; 5862 cells / 303 boxes; identical attachment-sphere clearance -4.1247→+4.1052 mm.
- [eval] [exp] 单/批量状态同步、异常恢复、附着物保留通过；同状态球/详细 FK 位移误差≤3.40e-8 m；缺失抓持预测不改变原有球。/ Single/batch state, exception restoration and attachment preservation pass; same-state sphere/detailed FK displacement error ≤3.40e-8 m; missing holding prediction leaves native spheres unchanged.
- [eval] [exp] 最终隔离运行 35.805 s；右臂通过 close/lift/transport 后在 descent 报 IK_FAIL；新增 close 拒绝已消除。未来抓持宽度仍未预测，不能据此证明真实方案不可行。/ Final isolated lifecycle 35.805 s; right arm passes close/lift/transport then rejects at descent IK_FAIL; introduced close rejection removed. Future holding width remains unpredicted, so physical impossibility is not established.
- [eval] [exp] 初始化后仿真步进/Gateway/Action 为 0，未更新 Skill/Node/Runtime、未恢复任务、无执行视频。/ Zero post-initialization steps/Gateway/Actions; no Skill/Node/Runtime update, task resume or execution video.
- Evidence: `/home/yanxu/tmp/hephaestus/gripper-support-validation-AbSpTs/{run.py,result.json,runtime.log,source/}`. Earlier unique roots preserved; withdrawn interval results are not final acceptance.
- Report: `docs/forge/IMPLEMENTATION_REVIEW_V11_1_0.md`.

### 文件变更详情 / Exact changed ranges

- [修改/新增 / Changed/Added] `docs/forge/MANIPULATION_DAG_DEVELOPER_GUIDE.md` L184-L215.
- [修改/新增 / Changed/Added] `examples/forge-adapters/robotwin20/profiles/robotwin20/route-inputs-persistent.yaml` L7-L9.
- [修改/新增 / Changed/Added] `examples/forge-adapters/robotwin20/runtime/robotwin_contact_qualification.py` L20, L28-L38.
- [修改/新增 / Changed/Added] `examples/forge-adapters/robotwin20/runtime/robotwin_curobo_world_port.py` L88-L91.
- [修改/新增 / Changed/Added] `examples/forge-adapters/robotwin20/runtime/robotwin_observed_collision.py` L56-L60, L85-L87, L95-L97.
- [修改/新增 / Changed/Added] `examples/forge-adapters/robotwin20/runtime/robotwin_planning_geometry.py` L250-L254, L256-L259.
- [修改/新增 / Changed/Added] `examples/forge-adapters/robotwin20/runtime/robotwin_route_planner.py` L14, L97-L103, L174, L195-L200, L202-L208, L216, L235.
- [修改/新增 / Changed/Added] `examples/forge-adapters/robotwin20/runtime/robotwin_simulation_probe_worker.py` L1421-L1426, L1434, L1442, L1459-L1460.
- [修改/新增 / Changed/Added] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/observed_collision.py` L14, L22-L23, L44-L49, L51-L60.
- [修改/新增 / Changed/Added] `examples/forge-adapters/robotwin20/tests/test_observed_collision.py` L20-L41.
- [修改/新增 / Changed/Added] `examples/forge-adapters/robotwin20/tests/test_route_planner.py` L1, L23.
- [修改/新增 / Changed/Added] `examples/forge-adapters/robotwin20/tests/test_simulation_probe.py` L269-L271, L1671-L1673.
- [修改/新增 / Changed/Added] `examples/forge-adapters/robotwin20/runtime/robotwin_gripper_geometry.py` L1-L139.
- [修改/新增 / Changed/Added] `examples/forge-adapters/robotwin20/tests/test_gripper_geometry.py` L1-L35.
- [修改/新增 / Changed/Added] `docs/forge/IMPLEMENTATION_REVIEW_V11_1_0.md` L1-L119.

### 关键 Diff / Key diff

```diff
- geometry_qpos[index] = real_value * float(multiplier) + float(offset)
+ configuration = gripper_configuration(task, arm, gripper_state)
+ # Open: physically bounded command prediction. Closed: measured reference,
+ # holding prediction unavailable. Native sphere radii remain unchanged.
+ with planner_gripper_state(task, arm, phase["gripper_state"]) as gripper:
+     result = plan_path_with_status(task, arm, pose, last_qpos=predicted.tolist())
- result = fn(waypoint)
+ with planner_gripper_state(task, arm) as measured_gripper:
+     result = fn(waypoint)
- gripper_state=phase["gripper_state"],
+ gripper_state=None,  # actual execution detailed geometry uses measured joints
- boxes, cell_count = voxel_boxes(environment, policy.voxel_size_m, policy.uncertainty_m)
+ boxes, cell_count = voxel_boxes(environment, policy.voxel_size_m, policy.uncertainty_m,
+     support_z=support_z, refinement_band=policy.support_refinement_band_m)
+ near = np.abs(points[:, 2] - support_z) <= refinement_band
+ fine, fine_count = _grid_boxes(points[near],
+     np.array([size, size, min(size, padding)]), padding)
+ return coarse + fine, coarse_count + fine_count
```

### Git 提交 / Git commit

- Implementation commit: `b3611ab`
- Branch: `feature/planning-loop`
- 时间 / Time: 2026-09-20 21:49 Asia/Shanghai
- [chore] [chore] 追加实现提交记录并同步最近五条。/ Record implementation commit and synchronize latest-five entry. (local)
- [agent] [fix] [计划] 修复 Agent-composed `manipulation.prepare` 节点未冻结 `destination_ref` 与 `capability_snapshot_ref`、导致候选成功后反复在 Coordinator selection 阶段拒绝的问题；复用现有节点绑定与 Tool schema，不新增任务状态机、动作权限或自动重试。(local)
- [Agent] [Fix] [Planned] Fix Agent-composed `manipulation.prepare` nodes that omit frozen `destination_ref` and `capability_snapshot_ref`, causing repeated Coordinator selection rejection after successful candidates; reuse existing node bindings and Tool schema without adding a task state machine, motion authority, or automatic retry. (local)

## v11.0.0 (2026-09-20 18:05) - codex

- [sense] [feat] [完成] 观测深度/目标掩码经现有 Grounding 与碰撞制品进入 Runtime，完整可见环境生成不丢占据的体素盒，机器人自体过滤只用本体几何与当前关节。(local)
- [sense] [feat] [Completed] Carry observed depth/target masks through existing Grounding and collision artifacts; voxelize complete visible environment without dropping occupancy and remove robot self returns only through embodiment geometry/current joints. (local)
- [policy] [fix] [完成] 持久接触资格化使用真实手指/掌部凸碰撞形状与接近扫掠体，区分夹持区域，替换全物体盒装入开口的条件；保持完整附着运输/释放后退检查。(local)
- [policy] [fix] [Completed] Qualify persistent contact using actual finger/palm convex collision shapes and approach sweeps, distinguishing the inner grasp region from forbidden solid penetration; retain complete attached transport and released-object retreat validation. (local)
- [eval] [exp] [完成] 保存单视角未知范围及分阶段失败，现有 AgentLoop 决定补观察；无运动冻结回放、负例测试、七维验收，不更新 live Runtime。(local)
- [eval] [exp] [Completed] Persist single-view uncertainty and phase failures for existing AgentLoop recovery; run frozen no-motion replay, negative controls and seven-dimension review without live Runtime changes. (local)
- Affected: adapter observed geometry/collision projection, persistent deployment profile, runtime world/contact/planning/execution integration, tests, replay and developer documentation.
- Anti-OverDefense: measured 288 aperture and 246 palm-box rejections do not establish actual contact collisions; existing object-only worlds omit unclassified visible occupancy. Reuse existing artifact integrity and route safety boundaries, ordinary geometry/configuration and tests; no new hash, task state or approval system.
- Version: minor feature after v10.10.3 carries to v11.0.0 according to repository numbering rules.

### 验证与结论 / Validation and findings

- [eval] [exp] [完成] Core 504 passed；Skill 337 passed；隔离 Adapter 588 passed、1 skipped（Pillow 不可用）；新增边界回归 5 passed；Ruff、Python 3.10 编译和 diff 检查通过。(local)
- [eval] [exp] [Completed] Core 504 passed; Skill 337 passed; isolated Adapter 588 passed, one Pillow-dependent module skipped; final boundary regressions five passed; Ruff, Python 3.10 compilation and diff checks pass. (local)
- [eval] [exp] [完成] 24 个提案保留 19 个；304 个臂/退让变体中 26 个进入接触规划、1 个通过：候选 16、右臂、退让 20 mm、净空约 11.21 mm。完整路线下降段 IK_FAIL，无 assignment/Gateway/仿真步进。(local)
- [eval] [exp] [Completed] 24 proposals retain 19 candidates; of 304 arm/backoff variants, 26 reach contact planning and one passes: candidate 16, right arm, 20 mm backoff, 11.21 mm clearance. Full-route descent fails with IK_FAIL; no assignment, Gateway calls or simulator steps. (local)
- [sense] [feat] [完成] 保留目标 523 点，环境 76277 点体素化为 4934 格/121 合并盒；未知遮挡的端点采样统计与精确证据引用进入原有公开错误，不强制 Agent 重试或补观察。(local)
- [sense] [feat] [Completed] Retain all 523 target points and 76277 environment points as 4934 occupied cells/121 merged boxes; existing public errors carry unknown/occluded endpoint sample counts and exact evidence references without forcing Agent retry or re-observation. (local)
- [policy] [fix] [完成] 审查补齐有限非负净空、观测支撑面来源、按机器人世界位姿失效的缓存和变体 deadline；复用原有物理与数据完整性边界。(local)
- [policy] [fix] [Completed] Review repairs finite nonnegative clearance, observed support provenance, robot-world-pose cache invalidation and variant deadline checks, reusing existing physical and integrity boundaries. (local)
- [chore] [chore] [完成] collision 可选依赖加入 Adapter；测试 SciPy 转入隔离环境，PAOS 临时安装已卸载；未安装新 Skill/Node、未重启 live Runtime、未恢复原任务。(local)
- [chore] [chore] [Completed] Add adapter collision extra; move test SciPy into an isolated environment and remove the temporary PAOS install. No Skill/Node install, live Runtime restart or original task resume. (local)
- Build: `/home/yanxu/tmp/hephaestus/observed-prepare-ptvqs95r`.
- Geometry measurement: `/home/yanxu/tmp/hephaestus/observed-readiness-k8wdi2pv` (preparation 24.986 s; worker lifecycle 38.965 s).
- Public evidence confirmation: `/home/yanxu/tmp/hephaestus/observed-readiness-aqcu4zbi` (preparation 23.955 s; worker lifecycle 37.748 s).
- 七维报告 / Seven-dimension report: `docs/forge/IMPLEMENTATION_REVIEW_V11_0_0.md`. No physical execution or continuous multi-Action video acceptance is claimed.

### 文件变更详情 / Exact changed ranges

- [修改/新增 / Changed/Added] `docs/forge/MANIPULATION_DAG_DEVELOPER_GUIDE.md` L147-L148, L155-L183.
- [修改/新增 / Changed/Added] `examples/forge-adapters/robotwin20/README.md` L11-L18.
- [修改/新增 / Changed/Added] `examples/forge-adapters/robotwin20/profiles/robotwin20/route-inputs-persistent.yaml` L3-L7.
- [修改/新增 / Changed/Added] `examples/forge-adapters/robotwin20/pyproject.toml` L12-L12.
- [修改/新增 / Changed/Added] `examples/forge-adapters/robotwin20/runtime/robotwin_contact_qualification.py` L4-L4, L10-L11, L18-L66, L89-L89, L92-L93, L106-L123.
- [修改/新增 / Changed/Added] `examples/forge-adapters/robotwin20/runtime/robotwin_curobo_world_port.py` L282-L291, L442-L443.
- [修改/新增 / Changed/Added] `examples/forge-adapters/robotwin20/runtime/robotwin_persistent_engine.py` L328-L329, L353-L354.
- [修改/新增 / Changed/Added] `examples/forge-adapters/robotwin20/runtime/robotwin_planning_geometry.py` L269-L277.
- [修改/新增 / Changed/Added] `examples/forge-adapters/robotwin20/runtime/robotwin_route_planner.py` L329-L330, L335-L336.
- [修改/新增 / Changed/Added] `examples/forge-adapters/robotwin20/runtime/robotwin_simulation_probe_worker.py` L2015-L2015, L2045-L2056.
- [修改/新增 / Changed/Added] `examples/forge-adapters/robotwin20/scripts/materialize_complete_route.py` L145-L145.
- [修改/新增 / Changed/Added] `examples/forge-adapters/robotwin20/scripts/replay_observed_preparation.py` L40-L40, L49-L50, L76-L78, L269-L269.
- [修改/新增 / Changed/Added] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/collision_world.py` L176-L177, L190-L190.
- [修改/新增 / Changed/Added] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/grounding.py` L25-L25, L28-L28, L440-L457.
- [修改/新增 / Changed/Added] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/persistent_deployment.py` L14-L14, L114-L116.
- [修改/新增 / Changed/Added] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/persistent_preparation.py` L71-L84, L213-L216.
- [修改/新增 / Changed/Added] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/persistent_route_builder.py` L231-L237, L241-L244.
- [修改/新增 / Changed/Added] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/route_inputs.py` L146-L146.
- [修改/新增 / Changed/Added] `examples/forge-adapters/robotwin20/tests/test_curobo_world_port.py` L89-L105.
- [修改/新增 / Changed/Added] `examples/forge-adapters/robotwin20/tests/test_persistent_contact_qualification.py` L2-L2, L74-L91.
- [修改/新增 / Changed/Added] `examples/forge-adapters/robotwin20/tests/test_persistent_preparation.py` L98-L112.
- [修改 / Changed] `examples/forge-adapters/robotwin20/tests/test_persistent_route_evaluator.py` L46-L51, L56-L60, L83-L83: reject observed worlds with benchmark source facts; normalize imports.
- [修改/新增 / Changed/Added] `docs/forge/IMPLEMENTATION_REVIEW_V11_0_0.md` L1-L159.
- [修改/新增 / Changed/Added] `examples/forge-adapters/robotwin20/runtime/robotwin_observed_collision.py` L1-L93.
- [修改/新增 / Changed/Added] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/observed_collision.py` L1-L131.
- [修改/新增 / Changed/Added] `examples/forge-adapters/robotwin20/tests/test_observed_collision.py` L1-L79.
- [修改/新增 / Changed/Added] `examples/forge-adapters/robotwin20/tests/test_runtime_observed_collision.py` L1-L93.

### 关键 Diff / Key diff

```diff
- mesh = qualify_geometry_artifact(bound, geometry, arm_id=arm, **kwargs)
+ mesh = (qualify_point_contact(task, bound, geometry, arm, hand, distances, approach, scene, deadline)
+         if scene is not None else qualify_geometry_artifact(bound, geometry, arm_id=arm, **kwargs))
+ # Separate measured target, self and complete visible environment occupancy.
+ environment = points[~selected & ~robot]
+ boxes, cell_count = voxel_boxes(environment, policy.voxel_size_m, policy.uncertainty_m)
+ # Preserve individual convex shapes; boxes are broad phase, not final contact verdicts.
+ swept = np.vstack((vertices, vertices + approach_delta))
+ hits = int(inside_convex(points, swept, policy.uncertainty_m).sum())
+ # Actual approach/contact trajectory samples also check target against link solids.
+ if inside_convex(observed["target"], vertices, observed["policy"].uncertainty_m).any():
+     raise SimulationProbeError(f"{phase} observed target intersects robot link {link.get_name()}")
+ # Prefix success never bypasses full route or existing dynamics/stop evidence.
+ elif evaluation["clearance_m"] is None or not isfinite(evaluation["clearance_m"]) or evaluation["clearance_m"] < 0:
+     variant["status"] = "rejected"
+     variant["rejection_reasons"].append("contact_clearance_unproven_or_negative")
```

### Git 提交 / Git

- Branch: `feature/planning-loop`; implementation commit: `350b9aa`; documentation closeout follows on the same branch.
- 日志范围 / Log ranges: `changelog/2026-09_part12.md` L3-L86; `CHANGELOG.md` L19-L102. Latest-five boundary now precedes v10.9.7; historical version contents are unchanged.

## v10.10.3 (2026-09-20 17:39) - codex

- [eval] [exp] [完成] 新增传感器侧离线诊断：校验掩码/深度/点云对应，量化物体与支撑掩码交集、近面点、邻域支持与 RGB 差异；全部点保留。(local)
- [eval] [exp] [Completed] Add sensor-only offline diagnostics for mask/depth/cloud correspondence, object/support overlap, near-plane points, neighboring support and RGB differences; retain every point. (local)
- [eval] [exp] [完成] 对原候选和退让变体比较局部可见点宽度、接触距离及完整包络失败，不将局部可见几何冒充完整碰撞或抓取证明。(local)
- [eval] [exp] [Completed] Compare local visible-point width and contact distances against complete-envelope failures for existing candidates/backoffs, without treating local visibility as full collision or grasp proof. (local)
- Files: adapter diagnostic script and regression tests; seven-dimension evidence report; CHANGELOG index.
- Anti-OverDefense: no new production gate, geometry filter, hash, model or motion path. Existing observation/calibration, support estimator, candidate artifacts and tests suffice for diagnosis.
- [docs] [docs] [完成] 根据用户纠正，对比 GraspGen、GraspNet、Contact-GraspNet 和 MoveIt GitHub 源码，区分局部碰撞、完整物体盒与未知遮挡空间；本轮只交付诊断及修改建议。(local)
- [docs] [docs] [Completed] Following user clarification, compare GraspGen, GraspNet, Contact-GraspNet and MoveIt GitHub source; distinguish local collision, whole-object boxes and occluded unknown space. Deliver diagnosis and recommendations only this round. (local)

### 验证与结论 / Validation and findings

- [eval] [exp] 523 个物体点中 31 个在支撑面 ±2 mm 带内，22 个邻接支撑内点；两掩码重叠 0。RGB/深度边界证据不能直接证明点的归属，不删除任何点。(local)
- [eval] [exp] Of 523 object points, 31 lie within the support +/-2 mm band and 22 neighbor support inliers; masks overlap at zero pixels. Boundary evidence does not establish ownership; no points removed. (local)
- [policy] [docs] GitHub 对照表明全局包络适合作粗筛，不应替代实际手指/掌部/接近扫掠体的局部碰撞；未知空间与实测占据分开。生产改动留待后续等效验证。(local)
- [policy] [docs] GitHub comparison supports broad-phase boxes rather than whole-box local contact verdicts; actual finger/palm/approach volumes and observed/unknown space require separate treatment. Production changes remain proposed pending validation. (local)
- [eval] [exp] 4 passed；Ruff/CLI 帮助/diff 检查通过。输出 `/home/yanxu/tmp/hephaestus/observed-contact-diagnosis-3740n476`；304 个变体的局部观测诊断，无 Gateway、仿真步进、动作或 live 修改。(local)
- [eval] [exp] Four regressions passed; Ruff/CLI help/diff checks pass. Output at `/home/yanxu/tmp/hephaestus/observed-contact-diagnosis-3740n476` covers 304 variants without Gateway, simulation steps, Actions or live mutation. (local)

### 文件变更详情 / Exact changed ranges

- [新增 / Added] `examples/forge-adapters/robotwin20/scripts/diagnose_observed_contact.py` L1–L210: reproducible sensor/point/contact diagnostic CLI, unique JSON/image output.
- [新增 / Added] `examples/forge-adapters/robotwin20/tests/test_observed_contact_diagnosis.py` L1–L48: pixel alignment, adjacency, local geometry and frame tests.
- [新增 / Added] `docs/forge/OBSERVED_CONTACT_DIAGNOSIS_V10_10_3.md` L1–L152: source-pinned GitHub comparison, measured evidence, recommended design and established seven dimensions.

### 关键 Diff / Key diff

```diff
+ys, xs, residual = aligned_cloud(mask, depth, calibration["intrinsic_cv"], cloud, args.depth_scale)
+near = np.abs(obj["world"][:, 2] - height) <= policy.inlier_distance_m
+neighbor = adjacent(inlier_mask)[obj["ys"], obj["xs"]]
+# Diagnostic subsets only; original cloud and production qualification stay unchanged.
+local = local_visible_geometry(obj["world"], moved_hand, variant["contact_center_position_m"], args.local_half_spans)
+result["geometry_modified"] = False
```

### Git 提交 / Git

- Branch: `feature/planning-loop`; diagnostic implementation commit: `ab4eb54`; log closeout follows on the same branch.
- 日志范围 / Log ranges: `changelog/2026-09_part12.md` L3–L43; `CHANGELOG.md` L19–L59. Latest-five boundary moves before v10.9.6; historical contents remain intact.

## v10.10.2 (2026-09-20 06:24) - codex

- [policy] [fix] [完成] 拆分手指包络资格化失败原因与距离，定位开口、接触位置、掌部重叠；保持现有通过条件。(local)
- [policy] [fix] [Completed] Expose finger-envelope failure causes and distances for aperture, contact location and palm overlap, preserving qualification conditions. (local)
- [eval] [exp] [完成] 对同一冻结观测进行点云/包络与接触资格化无运动对照；只根据传感器证据选择几何修复，七维验收明确剩余阻塞。(local)
- [eval] [exp] [Completed] Compare frozen cloud/envelope and contact qualification without motion; derive geometry changes only from sensor evidence and record remaining blockers in seven-dimension acceptance. (local)
- Expected files: adapter `grasp_postprocessing.py`, qualification tests, replay script and architecture/review documentation.
- Anti-OverDefense: the existing boolean collapses three different physical failures into one code, preventing attributable recovery. Ordinary diagnostic fields and tests suffice; no new admission check, hash or task state is needed.
- [sense] [fix] [完成] 默认 box_envelope 从完整点云先变换再取世界轴包络，消除相机盒空角落；不删除点、不借用仿真尺寸，保留无点云的既有回退。(local)
- [sense] [fix] [Completed] Bound the complete transformed cloud in world axes for default box_envelope models, removing artificial camera-box corners without discarding points or borrowing simulator dimensions; retain legacy no-cloud behavior. (local)
- Additional files: adapter `grounding.py` and observed route geometry tests. Evidence: 320/320 palm overlap, 304/320 aperture rejection, 0 contact-span rejection; observed world cloud height 38.77 mm versus camera-box world height 106.20 mm.
- [policy] [fix] [完成] 回放发现候选 22 接触点超出观测盒被误报为整个 materializer 故障；使用候选拒绝类型持久化，继续处理其他候选，其他输入错误仍失败。(local)
- [policy] [fix] [Completed] Replay exposed candidate 22 outside the observed contact shell being reported as a materializer failure; persist typed candidate rejection and continue other candidates, preserving failure for other input errors. (local)
- [eval] [fix] [完成] 回放保留旧观测模型到目标的刚体位移，将目标重表达于新观测模型坐标；避免把坐标基改变误当成新放置朝向。(local)
- [eval] [fix] [Completed] Replay preserves the old observed source-to-target rigid displacement while expressing the target in the new observed model frame, avoiding an unintended placement rotation. (local)

### 七维验收 / Seven-dimension acceptance

- [eval] [exp] Adapter 564 passed / 1 skipped；Core 504 passed；Skill 337 passed。分进程验证通过；合并进程的 import-isolation 测试失败按原样记录。(local)
- [eval] [exp] Adapter 564 passed / 1 skipped; Core 504 passed; Skill 337 passed in separate processes. Combined-process import-isolation failure is documented without weakening the check. (local)
- [eval] [exp] 24→19 候选、304 变体；294 手指包络拒绝、262 支撑碰撞、0 IK、0 Action、0 实测步进；最终 no_qualified_contacts，未通过操作任务验收。(local)
- [eval] [exp] 24 to 19 candidates / 304 variants; 294 finger-envelope rejections, 262 support collisions, zero IK, Actions or measured steps; final no_qualified_contacts, manipulation remains unaccepted. (local)
- Seven dimensions and evidence: `docs/forge/IMPLEMENTATION_REVIEW_V10_10_2.md`. Final build `observed-prepare-4nb_bsh4` (7.906571 s); evaluation `observed-readiness-qoxxzuyc` (qualification 9.873165 s, lifecycle 22.748214 s), under `/home/yanxu/tmp/hephaestus`.
- [env] [chore] 本轮仅源代码修复与回放，不发布新 Node/Skill，不替换 live Runtime；完整多 Action 视频仍待执行验收。(local)
- [env] [chore] Source repair and replay only; no new Node/Skill release or live Runtime replacement; full multi-Action video still requires execution acceptance. (local)

### 文件变更详情 / Exact changed ranges

- [修改/新增 / Modified/added] `docs/forge/VISUAL_GEOMETRY_EVIDENCE_ARCHITECTURE.md`: L59–L81.
- [修改/新增 / Modified/added] `examples/forge-adapters/robotwin20/scripts/materialize_complete_route.py`: L46–L46, L855–L855.
- [修改/新增 / Modified/added] `examples/forge-adapters/robotwin20/scripts/replay_observed_preparation.py`: L26–L35, L80–L83, L103–L103.
- [修改/新增 / Modified/added] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/grasp_postprocessing.py`: L185–L185, L204–L204, L206–L207, L209–L224, L298–L298, L301–L301, L315–L315.
- [修改/新增 / Modified/added] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/grounding.py`: L347–L374, L379–L379.
- [修改/新增 / Modified/added] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/persistent_route_builder.py`: L239–L239.
- [修改/新增 / Modified/added] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/route_inputs.py`: L28–L31, L287–L287.
- [修改/新增 / Modified/added] `examples/forge-adapters/robotwin20/tests/test_grasp_postprocessing.py`: L36–L66.
- [修改/新增 / Modified/added] `examples/forge-adapters/robotwin20/tests/test_materializer_diagnostics.py`: L8–L8, L14–L14.
- [修改/新增 / Modified/added] `examples/forge-adapters/robotwin20/tests/test_observed_route_geometry.py`: L13–L70, L72–L73, L83–L83.
- [修改/新增 / Modified/added] `docs/forge/IMPLEMENTATION_REVIEW_V10_10_2.md`: L1–L128.
- [修改/新增 / Modified/added] `examples/forge-adapters/robotwin20/tests/test_observed_replay_target.py`: L1–L15.

### 关键代码 Diff / Key code diff

```diff
diff --git a/examples/forge-adapters/robotwin20/scripts/materialize_complete_route.py b/examples/forge-adapters/robotwin20/scripts/materialize_complete_route.py
index 26b7d73..7c8a568 100644
--- a/examples/forge-adapters/robotwin20/scripts/materialize_complete_route.py
+++ b/examples/forge-adapters/robotwin20/scripts/materialize_complete_route.py
@@ -44,4 +44,5 @@ from robotwin20_adapter.route_generation import RouteCandidateRejectedError, gen
 from robotwin20_adapter.route_inputs import (
     ROUTE_INPUT_PROFILE_SCHEMA_VERSION,
+    ContactShellRejectedError,
     canonical_json,
     derive_bound_route_inputs,
@@ -852,5 +853,5 @@ def main() -> int:
     try:
         review = materialize(args)
-    except (MaterializationError, RouteCandidateRejectedError) as exc:
+    except (MaterializationError, RouteCandidateRejectedError, ContactShellRejectedError) as exc:
         diagnostic = {
             "code": exc.code if isinstance(exc, MaterializationError) else "route_candidate_rejected",
diff --git a/examples/forge-adapters/robotwin20/scripts/replay_observed_preparation.py b/examples/forge-adapters/robotwin20/scripts/replay_observed_preparation.py
index 0b7786e..1e9ac8f 100644
--- a/examples/forge-adapters/robotwin20/scripts/replay_observed_preparation.py
+++ b/examples/forge-adapters/robotwin20/scripts/replay_observed_preparation.py
@@ -24,4 +24,14 @@ def write(path, value):


+def reexpress_target(target_world, old_model_world, new_model_world):
+    """Preserve commanded rigid displacement when observation model axes change."""
+    import numpy as np
+
+    from robotwin20_adapter.observed_binding import rigid_transform
+
+    return (rigid_transform(target_world) @ np.linalg.inv(rigid_transform(old_model_world))
+            @ rigid_transform(new_model_world)).reshape(-1).tolist()
+
+
 def build(args):
     import yaml
@@ -68,4 +78,8 @@ def build(args):
     bound = grounding.bind({**identity, "entity_refs": list(binding["objects"])})
     target_request = {**target["requested_pose"], "binding_ref": bound["binding_ref"]}
+    entity = target_request["entity_ref"]
+    target_request.update(frame_id="world", frame_T_object_target=reexpress_target(
+        target["object"]["world_T_object_target"], binding["objects"][entity]["world_T_object"],
+        grounding.bindings[bound["binding_ref"]]["objects"][entity]["world_T_object"]))
     destination = grounding.target(target_request)
     request["destination_ref"] = destination["destination_ref"]
@@ -87,4 +101,5 @@ def build(args):
     write(root / "request.json", request)
     write(root / "replay.json", {"task_id": args.task_id, "source_root": str(source), "source_binding": target["binding_ref"],
+                                "original_target": target["requested_pose"], "reexpressed_target": target_request,
                                 "binding_ref": bound["binding_ref"], "candidate_count": len(request["candidates"]),
                                 "git_commit": subprocess.check_output(["git", "rev-parse", "HEAD"], text=True).strip(),
diff --git a/examples/forge-adapters/robotwin20/src/robotwin20_adapter/grasp_postprocessing.py b/examples/forge-adapters/robotwin20/src/robotwin20_adapter/grasp_postprocessing.py
index 50f08a6..71c3cc5 100644
--- a/examples/forge-adapters/robotwin20/src/robotwin20_adapter/grasp_postprocessing.py
+++ b/examples/forge-adapters/robotwin20/src/robotwin20_adapter/grasp_postprocessing.py
@@ -183,5 +183,5 @@ def _object_contains(point: Sequence[float], center: Sequence[float], half_exten


-def _pinch_geometry(contact: Sequence[float], center: Sequence[float], extents: Sequence[float], object_rotation: Sequence[Sequence[float]], hand_pose: Mapping[str, Any], links: Mapping[str, Any], delta: Sequence[float]) -> bool:
+def _pinch_geometry_diagnostics(contact: Sequence[float], center: Sequence[float], extents: Sequence[float], object_rotation: Sequence[Sequence[float]], hand_pose: Mapping[str, Any], links: Mapping[str, Any], delta: Sequence[float]) -> dict[str, Any]:
     """Conservative Panda open-finger envelope, not a force-closure verdict."""
     position, quaternion = _pose(hand_pose, "hand pose")
@@ -202,12 +202,25 @@ def _pinch_geometry(contact: Sequence[float], center: Sequence[float], extents:
         bounds[name] = ([min(p[i] for p in points) for i in range(3)], [max(p[i] for p in points) for i in range(3)])
     fingers = sorted((bounds[name] for name in ("panda_leftfinger", "panda_rightfinger")), key=lambda bound: bound[0][1])
-    if object_low[1] < fingers[0][1][1] or object_high[1] > fingers[1][0][1]:
-        return False
+    aperture_margins = [object_low[1] - fingers[0][1][1], fingers[1][0][1] - object_high[1]]
     center_local = local(contact)
-    for low, high in fingers:
-        if any(not low[i] <= center_local[i] <= high[i] for i in (0, 2)):
-            return False
+    contact_margins = [min(center_local[i] - low[i], high[i] - center_local[i])
+                       for low, high in fingers for i in (0, 2)]
     hand_low, hand_high = bounds["panda_hand"]
-    return any(object_high[i] < hand_low[i] or object_low[i] > hand_high[i] for i in range(3))
+    palm_separation = max(max(hand_low[i] - object_high[i], object_low[i] - hand_high[i]) for i in range(3))
+    reasons = [reason for failed, reason in (
+        (min(aperture_margins) < 0, "object_exceeds_finger_aperture"),
+        (min(contact_margins) < 0, "contact_outside_finger_span"),
+        (palm_separation <= 0, "object_overlaps_palm_envelope"),
+    ) if failed]
+    return {"fit": not reasons, "rejection_reasons": reasons,
+            "aperture_margins_m": aperture_margins,
+            "contact_span_margins_m": contact_margins,
+            "palm_separation_m": palm_separation,
+            "object_width_m": object_high[1] - object_low[1],
+            "finger_aperture_m": fingers[1][0][1] - fingers[0][1][1]}
+
+
+def _pinch_geometry(*args, **kwargs) -> bool:
+    return _pinch_geometry_diagnostics(*args, **kwargs)["fit"]


@@ -283,7 +296,8 @@ def qualify_contact_variants(
         clearance = min(sum(normal[index] * point[index] for index in range(3)) - support_offset for point in world_vertices)
         pinch = _object_contains(contact, center, extents, object_rotation)
-        finger_fit = None if gripper_links_m is None else _pinch_geometry(
+        finger_diagnostics = None if gripper_links_m is None else _pinch_geometry_diagnostics(
             contact, center, extents, object_rotation, hand_pose, gripper_links_m, delta
         )
+        finger_fit = None if finger_diagnostics is None else finger_diagnostics["fit"]
         curobo_clearance = (
             None if curobo_clearance_m is None else curobo_clearance_m[index]
@@ -299,4 +313,5 @@ def qualify_contact_variants(
             "pinch_center_inside_object": pinch,
             "finger_envelope_fit": finger_fit,
+            **({"finger_envelope_diagnostics": finger_diagnostics} if finger_diagnostics is not None else {}),
             **({"curobo_clearance_m": curobo_clearance} if curobo_clearance_m is not None else {}),
             "rejection_reasons": [reason for rejected, reason in (
diff --git a/examples/forge-adapters/robotwin20/src/robotwin20_adapter/grounding.py b/examples/forge-adapters/robotwin20/src/robotwin20_adapter/grounding.py
index 9d5ee99..80b90ad 100644
--- a/examples/forge-adapters/robotwin20/src/robotwin20_adapter/grounding.py
+++ b/examples/forge-adapters/robotwin20/src/robotwin20_adapter/grounding.py
@@ -345,9 +345,37 @@ class Grounding:
             visual_pose[:3, :3] = camera_to_world[:3, :3]
             visual_pose[:3, 3] = center_world[:3]
+            model_frame = "observed-envelope"
+            if geometry_present and geometry.get("shape_class") == "box_envelope" and geometry.get("orientation_reliable") is False:
+                clouds = [a for a in understanding.get("derived_artifacts", [])
+                          if a.get("kind") == "object_point_cloud" and a.get("entity_ref") == ref]
+                if len(clouds) > 1:
+                    self._reject("observed object point cloud is ambiguous", stage="visual_geometry", entity_ref=ref)
+                if clouds:
+                    cloud = clouds[0]
+                    if (any(cloud.get(k) != understanding[k] for k in IDENTITY_KEYS)
+                            or cloud.get("frame_id") != understanding["frame"]["frame_id"]):
+                        self._reject("observed object cloud lineage differs from binding", stage="visual_geometry", entity_ref=ref)
+                    points = np.load(_artifact_path(self.root, cloud["artifact_ref"] + ".npy"), allow_pickle=False)
+                    if points.ndim != 2 or points.shape[1] != 3 or len(points) == 0 or not np.isfinite(points).all():
+                        self._reject("observed object point cloud is invalid", stage="visual_geometry", entity_ref=ref)
+                    # Bound measured points after calibration, not the empty
+                    # corners of a camera-axis box. Retain every point and any
+                    # producer-supplied envelope padding, projected into world.
+                    cloud_low, cloud_high = points.min(axis=0), points.max(axis=0)
+                    padding = np.maximum.reduce([np.zeros(3), cloud_low - low, high - cloud_high,
+                                                 (dimensions - (cloud_high - cloud_low)) / 2])
+                    world = points @ camera_to_world[:3, :3].T + camera_to_world[:3, 3]
+                    padding_world = np.abs(camera_to_world[:3, :3]) @ padding
+                    world_low = world.min(axis=0) - padding_world
+                    world_high = world.max(axis=0) + padding_world
+                    dimensions = np.maximum(world_high - world_low, 1e-4)
+                    visual_pose = np.eye(4)
+                    visual_pose[:3, 3] = (world_low + world_high) / 2
+                    model_frame = "observed-envelope/world"
             runtime = objects[ref]
             updated = deepcopy(runtime)
             # This is an estimated envelope frame, not the physical actor frame.
             # Its origin is the observed centroid; no hidden functional point is used.
-            updated["object_frame_id"] = f"observed-envelope/{ref.removeprefix('entity://')}"
+            updated["object_frame_id"] = f"{model_frame}/{ref.removeprefix('entity://')}"
             updated["world_T_object"] = visual_pose.reshape(-1).tolist()
             updated["half_extents_m"] = (dimensions / 2.0).tolist()
diff --git a/examples/forge-adapters/robotwin20/src/robotwin20_adapter/persistent_route_builder.py b/examples/forge-adapters/robotwin20/src/robotwin20_adapter/persistent_route_builder.py
index 4cc3d1d..2802ba1 100644
--- a/examples/forge-adapters/robotwin20/src/robotwin20_adapter/persistent_route_builder.py
+++ b/examples/forge-adapters/robotwin20/src/robotwin20_adapter/persistent_route_builder.py
@@ -237,4 +237,5 @@ class PersistentRouteBuilder:
                 for variant in attempt["qualification"]["variants"]:
                     rejections.update(variant["rejection_reasons"])
+                    rejections.update(variant.get("finger_envelope_diagnostics", {}).get("rejection_reasons", []))
             if result["status"] != "qualified":
                 continue
diff --git a/examples/forge-adapters/robotwin20/src/robotwin20_adapter/route_inputs.py b/examples/forge-adapters/robotwin20/src/robotwin20_adapter/route_inputs.py
index 3e8963f..66b98e1 100644
--- a/examples/forge-adapters/robotwin20/src/robotwin20_adapter/route_inputs.py
+++ b/examples/forge-adapters/robotwin20/src/robotwin20_adapter/route_inputs.py
@@ -26,4 +26,8 @@ class RouteInputError(ValueError):


+class ContactShellRejectedError(RouteInputError):
+    """One candidate misses the bound object's contact shell."""
+
+
 def canonical_json(value: Mapping[str, Any]) -> bytes:
     return (json.dumps(value, sort_keys=True, separators=(",", ":")) + "\n").encode()
@@ -281,5 +285,5 @@ def derive_bound_route_inputs(
         for value, extent in zip(contact_translation, item["half_extents_m"])
     ):
-        raise RouteInputError("canonical contact center does not intersect the object contact shell")
+        raise ContactShellRejectedError("canonical contact center does not intersect the object contact shell")
     geometry = {
         "schema_version": OBJECT_GEOMETRY_SCHEMA_VERSION,
```

### Git 提交 / Git

- Branch: `feature/planning-loop`; implementation commit: `b45bf3e`; log closeout follows on the same branch.
- 日志范围 / Log ranges: `changelog/2026-09_part12.md` L1–L235; `CHANGELOG.md` new archive link L5, latest entry L19–L251; latest-five boundary moved before v10.9.5 without changing historical content.

## v11.2.0 (2026-09-20 22:59) - codex

### 变更计划 / Planned changes

- [policy] [feat] [计划] 为 `blocks_ranking_rgb` 增加专用 RoboTwin task adapter，分别提供仿真执行场景事实、benchmark 目标事实和只读 benchmark 结果；不把固定三方块映射扩展为 RoboTwin 全任务通用逻辑。(local)
- [policy] [feat] [Planned] Add a dedicated RoboTwin task adapter for `blocks_ranking_rgb`, separately providing simulator execution-scene facts, benchmark goal facts, and a read-only benchmark result without presenting fixed three-block mappings as a universal RoboTwin task layer. (local)
- [env] [feat] [计划] 增加 `robotwin-blocks-ranking-oracle` 与 `robotwin-blocks-ranking-observed` 两个明确 Runtime profile；在 Adapter 部署组装层选择最终 scene source，PAOS Core、ToolSpec、Gateway 与 AgentTask 生命周期保持不变。(local)
- [env] [feat] [Planned] Add explicit `robotwin-blocks-ranking-oracle` and `robotwin-blocks-ranking-observed` Runtime profiles; select the final scene source in Adapter deployment composition while leaving PAOS Core, ToolSpecs, Gateway, and AgentTask lifecycle unchanged. (local)
- [eval] [feat] [计划] 将 PAOS ForgeTaskVerifier verdict、RoboTwin benchmark 结果和最终 task-owner 累计视频 manifest/双视角 MP4 分开持久化到唯一运行目录；隐藏目标不冒充传感器证据，benchmark 分数不替代 PAOS 语义验证。(local)
- [eval] [feat] [Planned] Persist the PAOS ForgeTaskVerifier verdict, RoboTwin benchmark result, and final task-owner cumulative video manifest/two-view MP4s separately in a unique run directory; hidden goals never masquerade as sensor evidence and benchmark scoring never replaces PAOS semantic verification. (local)
- [agent] [fix] [计划] 保留 Coordinator 精确 source selector，验证 24 个完整抓取候选不重新进入模型上下文；oracle 场景不被错误宣称为解决 selection 前 `provider_timeout`。(local)
- [agent] [fix] [Planned] Preserve Coordinator exact source selection and verify that 24 complete grasp candidates do not re-enter model context; do not claim the oracle scene itself fixes pre-selection `provider_timeout`. (local)
- [docs] [docs] [计划] 保存 oracle-first 设计、profile/Adapter 所有权、50-task 扩展边界、执行与复现命令，并按既有七维完成源码、无运动、准备、单次 Action、完整三方块、双重判定、累计视频及 observed 回归验收。(local)
- [docs] [docs] [Planned] Save the oracle-first design, profile/Adapter ownership, 50-task extension boundary, execution/reproduction commands, and perform the established seven-dimension acceptance across source, no-motion, preparation, one Action, full three-block execution, dual verdicts, cumulative video, and observed regression. (local)

### 预期影响文件 / Expected affected files

- `docs/forge/ROBOTWIN_ORACLE_BASELINE_DESIGN_V11_2_0.md`
- `examples/forge-adapters/robotwin20/runtime/robotwin_blocks_ranking_adapter.py`
- `examples/forge-adapters/robotwin20/runtime/robotwin_route_input_worker.py`
- `examples/forge-adapters/robotwin20/runtime/robotwin_persistent_engine.py`
- `examples/forge-adapters/robotwin20/src/robotwin20_adapter/persistent_deployment.py`
- `examples/forge-adapters/robotwin20/src/robotwin20_adapter/persistent_host.py`
- `examples/forge-adapters/robotwin20/profiles/forge-persistent/*`
- `examples/forge-skills/pick-place-workflow/profiles/*`
- `examples/forge-skills/pick-place-workflow/skill.yaml`
- RoboTwin Adapter/Runtime、Skill 与 Core 选择/验证测试
- `docs/forge/MANIPULATION_DAG_DEVELOPER_GUIDE.md`、`CHANGELOG.md`

### 失败场景与 Anti-OverDefense 依据 / Failure scenarios and Anti-OverDefense rationale

- 当前 `build_persistent_deployment` 无条件用 observation-owned grounding 覆盖调用方 scene source；仅增加 oracle profile 不会改变实际 route geometry。普通 profile 命名无法修复该组合错误，因此必须让 Adapter 组装显式选择最终 source。
- 当前固定 `_ENTITIES` 与 `block*_target_pose` 只适用于 `blocks_ranking_rgb`；将其直接扩展为 50-task 通用 worker 会把交接、按压、旋转、开门等不同原子能力错误压缩为 pick/place。
- 当前累计视频已经按 task owner 拼接 Action；不新增第二录像系统，只补终态引用、独立 benchmark 报告与完整任务验收。
- 最新 selection 超时发生在 Runtime `manipulation.prepare` 调用前；oracle 几何不能替代现有 source selector、模型时延记录或 bounded prompt。
- 本版本不新增 hash、第二任务状态机、自动 oracle fallback、固定 Agent 操作顺序、运动授权或额外安全 gate；复用现有 profile、事务、Gateway、settlement、Verifier 和视频归档。
- [env] [fix] [计划] 实际安装启动证明 manifest profile `environment` 已进入 Dora，但 dataflow 的 Node 环境白名单未转发 route source；补充显式透传，避免 profile 名称与实际 source 分离。(local)
- [Env] [Fix] [Planned] Installed startup proved that manifest profile `environment` reaches Dora but the dataflow Node environment allowlist did not forward the route source; add the explicit forwarding so the profile name cannot diverge from the actual source. (local)
- [policy] [fix] [计划] 实际 oracle preparation 暴露 readiness 把观测身份绑定误当成观测几何所有权；为 oracle scene facts 标记 `oracle_actor`，并按冻结 attached-geometry source 选择 actor 或 observed 校验，observed 路径保持原有 fail-closed 规则。(local)
- [Policy] [Fix] [Planned] Actual oracle preparation exposed readiness treating observed identity binding as observed-geometry ownership; mark oracle scene facts as `oracle_actor` and select actor versus observed validation from the frozen attached-geometry source while preserving the existing fail-closed observed path. (local)
- [model] [feat] [计划] 实际 oracle 无运动 preparation 已排除 Provider 与几何所有权错误，但观测 GraspGen 候选缺少已知可达的近垂直 top grasp，46 个左右臂选项均在 approach 阶段 `IK_FAIL`；为显式 oracle profile 增加 task-adapter-owned 抓取候选 Provider，经现有 `grasp.propose` 标准契约输出并继续接受完整 readiness/Action admission，observed profile 仍使用 GraspGen，禁止自动 fallback。(local)
- [Model] [Feat] [Planned] Actual oracle no-motion preparation eliminated provider and geometry-ownership errors, but the observed GraspGen set lacked the known-reachable near-vertical top grasp and all 46 arm options failed approach with `IK_FAIL`; add a task-adapter-owned grasp-candidate provider selected only by the explicit oracle profile, emit through the existing `grasp.propose` contract, retain full readiness and Action admission, keep GraspGen for the observed profile, and provide no automatic fallback. (local)
- [policy] [fix] [计划] 修复 observed identity 与 execution identity 同名时的误拒绝：仅当保留身份被映射到不同执行实体或 actor 时拒绝；唯一对应、同一 actor、当前 captured pose 的同名绑定继续通过现有无运动激活路径，并保留失败时不发布部分 alias。(local)
- [Policy] [Fix] [Planned] Fix false rejection when observed and execution identities have the same name: reject only when a reserved identity maps to a different execution entity or actor; allow a unique same-actor binding with the current captured pose through the existing no-motion activation path while preserving atomic alias publication on failure. (local)
- [chore] [chore] [计划] 将本轮实际安装身份更新为 Skill `2.5.6` / Node `0.5.6`，重建、隔离安装并验证 manifest、artifact digest、profile 与启动 Runtime 一致。(local)
- [Chore] [Chore] [Planned] Advance the installed immutable identities to Skill `2.5.6` / Node `0.5.6`, rebuild, install in isolation, and verify manifest, artifact digest, profile, and running Runtime consistency. (local)

- 具体失败场景：真实 `scene.understand` 合法地产生 `entity://block-green-1` 等稳定 observed ref，且持久化 binding 同时证明其 execution ref 与 actor 均相同；现有保留名集合仍在 actor、captured pose 与唯一对应检查之前无条件抛出 `observed identity must not shadow execution identity`。主键和唯一约束只能防重复记录，不能表达“同名同实体合法、同名异实体非法”的语义，因此需要收窄现有检查，而不是增加新 gate。
- Concrete failure scenario: real `scene.understand` legitimately emits stable observed refs such as `entity://block-green-1`, and the persisted binding proves the execution ref and actor are the same; the current reserved-name set still raises `observed identity must not shadow execution identity` before actor, captured-pose, and unique-correspondence checks. Primary keys and uniqueness constraints prevent duplicate records but cannot express the semantic distinction between a same-name same-entity binding and a same-name different-entity binding, so the existing check must be narrowed rather than adding a new gate.
- [model] [fix] [计划] 真实 `0.5.6` 查询已生成 4 个候选和 oracle evidence，但 `PersistentManipulationProvider` 自动附加的 holding-state envelope 被 oracle Provider 误判为候选字段；按现有 persistent route source 模式剥离传输状态字段，并将 `oracle_evidence_ref` 追加到候选 provenance 后再交给通用 `grasp.propose` schema。(local)
- [Model] [Fix] [Planned] The real `0.5.6` query generated four candidates and oracle evidence, but the oracle Provider mistook the holding-state envelope automatically added by `PersistentManipulationProvider` for candidate fields; strip transport state fields following the existing persistent route-source pattern and append `oracle_evidence_ref` to candidate provenance before the generic `grasp.propose` schema receives the result. (local)
- 具体失败场景：`oracle_grasp_candidates` 的领域结果合法，worker response 额外含 `holding_state`、`owner`、`acquire_invocation_id`、`entity_ref`；普通输出 schema 无法在 Provider 检查前区分 worker envelope。若只删除严格字段检查会吞入未知 payload，若不修则所有真实 oracle grasp 永远返回 `grasp_proposal_provider_error`。因此仅剥离该进程协议已有的固定 envelope，并保留精确领域字段集合。
- Concrete failure scenario: the `oracle_grasp_candidates` domain result is valid while the worker response additionally contains `holding_state`, `owner`, `acquire_invocation_id`, and `entity_ref`; the ordinary output schema cannot distinguish the worker envelope before the Provider check. Removing strict field validation would admit unknown payloads, while leaving it unchanged makes every real oracle grasp return `grasp_proposal_provider_error`. Therefore only the process protocol's established fixed envelope is stripped and the exact domain-field set remains enforced.
- [chore] [chore] [计划] `2.5.6/0.5.6` 已作为身份修复构建并安装，真实集成随后暴露 worker envelope 问题；修复后的不可变发布身份推进为 Skill `2.5.7` / Node `0.5.7`，不覆盖已安装内容。(local)
- [Chore] [Chore] [Planned] `2.5.6/0.5.6` was already built and installed for the identity repair before the real integration exposed the worker-envelope issue; advance the repaired immutable release identities to Skill `2.5.7` / Node `0.5.7` instead of overwriting installed content. (local)
- [agent] [feat] [计划] 为通用 `GraspProposalSnapshot` 增加 Provider 内部 `produced_evidence_refs` 声明，使候选 provenance 可引用本次 Query 新持久化且由 Provider 明确声明的 artifact；未声明引用、跨目标输入引用和非法 URI 继续拒绝，公开 Tool 输出结构不增加旁路字段。(local)
- [Agent] [Feat] [Planned] Add an internal Provider `produced_evidence_refs` declaration to generic `GraspProposalSnapshot`, allowing candidate provenance to cite artifacts newly persisted by that Query and explicitly declared by the Provider; undeclared references, cross-target input references, and invalid URIs remain rejected, with no parallel field added to the public Tool output. (local)
- 具体失败场景：oracle grasp evidence 只能在候选生成时写入，因而不可能预先存在于 consumer 请求；现有 provenance 子集规则会把合法的 producer-owned 新证据判为 `invalid_provenance`。直接允许任意 artifact ref 会丢失来源约束，直接删除 oracle ref 又会把 actor-derived 候选错误表现为纯观测产物；显式 produced-evidence 声明是现有 Producer/Consumer 结构所需的最小扩展。
- Concrete failure scenario: oracle grasp evidence can only be persisted while candidates are produced, so it cannot already exist in the consumer request; the current provenance-subset rule therefore reports valid producer-owned new evidence as `invalid_provenance`. Allowing arbitrary artifact refs would remove lineage constraints, while dropping the oracle ref would misrepresent actor-derived candidates as observation-only products; an explicit produced-evidence declaration is the smallest extension consistent with the existing Producer/Consumer structure.
- [chore] [chore] [计划] `2.5.7/0.5.7` 的真实查询将新 oracle artifact 正确推进到 Core provenance 校验后暴露缺失的 produced-evidence 声明；最终修复使用新不可变身份 Skill `2.5.8` / Node `0.5.8`。(local)
- [Chore] [Chore] [Planned] The real `2.5.7/0.5.7` query correctly advanced the new oracle artifact to Core provenance validation and exposed the missing produced-evidence declaration; publish the final repair under new immutable identities Skill `2.5.8` / Node `0.5.8`. (local)
- [policy] [fix] [计划] 修复 route materializer 将所有候选 provenance 强制按 `.npy` 复制的问题：从 artifact root 精确解析唯一的 `.npy` 点云或 `.json` producer evidence 并原类型复制；缺失或同 ref 多类型继续失败，路线几何仍只由既有 adaptation 输入生成。(local)
- [Policy] [Fix] [Planned] Fix the route materializer forcing every candidate provenance artifact through `.npy`: resolve exactly one `.npy` point cloud or `.json` producer-evidence file from the artifact root and copy it with its original type; missing or multi-type refs still fail, and route geometry remains generated only from the existing adaptation inputs. (local)
- 具体失败场景：真实 preparation 的候选同时引用 observation `.npy` 与 oracle `.json`，现有循环对两者都追加 `.npy`，于是已存在的 oracle evidence 被误报 `source artifact is unavailable`。类型、普通测试和 artifact URI 不编码文件扩展名，无法自行修复；按源文件唯一解析即可，不需要新增 schema 或 gate。
- Concrete failure scenario: real preparation candidates cite both observation `.npy` and oracle `.json` artifacts, while the current loop appends `.npy` to both and reports the existing oracle evidence as `source artifact is unavailable`. Types, ordinary tests, and artifact URIs do not encode the file extension and cannot resolve this by themselves; unique source-file resolution fixes it without a new schema or gate.
- [chore] [chore] [计划] `2.5.8/0.5.8` 已用于真实 preparation 并确认 materializer 类型解析失败；包含该修复的下一不可变安装身份为 Skill `2.5.9` / Node `0.5.9`。(local)
- [Chore] [Chore] [Planned] `2.5.8/0.5.8` was used for the real preparation that confirmed the materializer type-resolution failure; publish that repair under the next immutable identities Skill `2.5.9` / Node `0.5.9`. (local)
- [policy] [fix] [计划] 将无运动路线准备与仿真 Action 执行期证据分开：oracle profile 仅允许 `contact_dynamics` 与 `stop_control` 明确标记为 `deferred`，静态完整路线检查仍须通过；observed 与未授权 profile 继续把缺失动态证据判为不可用。(local)
- [Policy] [Fix] [Planned] Separate no-motion route preparation from simulation Action execution evidence: only the oracle profile may mark `contact_dynamics` and `stop_control` explicitly as `deferred`, while every static complete-route check must still pass; observed and unauthorized profiles continue to classify missing dynamic evidence as unavailable. (local)
- [policy] [feat] [计划] 在 Adapter 准备边界增加显式 simulation-only route approval issuer，将当前 scene、route、candidate、assignment 与执行期必检项绑定后写入既有 PreparedRoutes；Runtime 在任何仿真步进前重新验证该批准，并继续由现有控制器、接触监测、停止、Gateway 与 settlement 路径执行。(local)
- [Policy] [Feat] [Planned] Add an explicit simulation-only route approval issuer at the Adapter preparation boundary, binding the current scene, route, candidate, assignment, and mandatory execution-time checks into the existing PreparedRoutes; Runtime revalidates that approval before any simulator step and continues through the existing controller, contact monitor, stop, Gateway, and settlement path. (local)
- [env] [chore] [计划] 仅为 `robotwin-blocks-ranking-oracle` profile 转发 `ROBOTWIN20_SIMULATION_ACTION_MODE=runtime_monitored`；observed/legacy profile 固定为 `disabled`，并将发布身份推进为 Skill `2.6.0` / Node `0.6.0` 后重建、安装和完整验收。(local)
- [Env] [Chore] [Planned] Forward `ROBOTWIN20_SIMULATION_ACTION_MODE=runtime_monitored` only for the `robotwin-blocks-ranking-oracle` profile; keep observed/legacy profiles explicitly `disabled`, advance release identities to Skill `2.6.0` / Node `0.6.0`, then rebuild, install, and run complete acceptance. (local)

- 具体失败场景：真实 oracle preparation 已证明右臂静态完整路线可行，但 no-motion worker 必然无法在不步进世界的前提下产生未来接触结果；它同时把 Runtime 已实现的 bounded controller stop 能力表述为未连接，导致所有候选在 Action/Gateway 之前被归类为 infrastructure unavailable。即便静态选择放行，当前 `PersistentPreparationProvider` 仍以 `approval_ref=None` 注册路线，Action 必然被 `PreparedRoutes` 拒绝。普通类型、主键、事务和测试不能表达“仅显式仿真 profile 可把两个动态结果推迟到同一已批准 Action 内验证”的阶段所有权，因此需要 Adapter-owned deferred 状态与 route-bound approval；该机制位于仿真 world-change 边界，不适用于 observed/硬件路径。
- Concrete failure scenario: real oracle preparation proves a statically complete right-arm route, but a no-motion worker cannot produce a future contact outcome without stepping the world; it also describes the Runtime's existing bounded-controller stop capability as disconnected, classifying every candidate as infrastructure unavailable before Action/Gateway. Even if static selection were admitted, `PersistentPreparationProvider` currently registers the route with `approval_ref=None`, so `PreparedRoutes` must reject the Action. Ordinary types, primary keys, transactions, and tests cannot express the phase ownership rule that only an explicit simulation profile may defer exactly two dynamic outcomes to validation inside the same admitted Action; therefore an Adapter-owned deferred state and route-bound approval are required at the simulation world-change boundary and do not apply to observed/hardware paths.
- [policy] [fix] [计划] 修复 Oracle preparation 将 `prepared_with_execution_checks_deferred` 写入 Core 仅允许 `prepared` 的公开候选契约；动态 deferred 继续保留在既有 readiness evidence、assignment decision basis 和 route-bound approval 中，不放宽 Core schema、不升级为物理通过。(local)
- [Policy] [Fix] [Planned] Fix Oracle preparation emitting `prepared_with_execution_checks_deferred` into the Core public candidate contract, which only permits `prepared`; retain dynamic deferral in the existing readiness evidence, assignment decision basis, and route-bound approval without widening the Core schema or upgrading it to physical success. (local)
- 具体失败场景：`red_prepare_1` 的右臂路线静态检查通过、`contact_dynamics`/`stop_control` 合法 deferred，Runtime 已生成 assignment 和 simulation-only approval，但 Core 因 qualification 枚举不匹配返回 `invalid_prepared_candidate`。普通类型和事务无法修复 Adapter 输出与既有契约的错位；若放宽 Core 枚举会把阶段状态扩散到所有 Provider，因此只修正 Oracle Adapter 的公开映射并增加 deferred 回归测试。
- Concrete failure scenario: `red_prepare_1` had a statically valid right-arm route with legal `contact_dynamics`/`stop_control` deferral, and the Runtime persisted an assignment plus simulation-only approval, but Core returned `invalid_prepared_candidate` because the qualification enum did not match. Types and transactions cannot repair this Adapter-to-existing-contract mismatch; widening the Core enum would spread phase state to every Provider, so only the Oracle Adapter mapping is corrected and a deferred regression test is added.
- [chore] [chore] [计划] 将已安装的 Skill `2.6.3` / Node `0.6.3` 递增为不可覆盖的 `2.6.4` / `0.6.4`，重建并隔离安装修复后的 Oracle Runtime；保留旧版本和既有任务记录。(local)
- [Chore] [Chore] [Planned] Advance the installed Skill `2.6.3` / Node `0.6.3` to non-overwriting `2.6.4` / `0.6.4`, rebuild and install the repaired Oracle Runtime in isolation, and preserve the prior versions and task records. (local)
- [sense] [fix] [计划] 修复 RGB scene-understanding Provider 对空 semantic provenance 的最小绑定：仅把缺失/空 provenance 绑定到本次请求实际解析并发送的 RGB artifact；显式未知、跨场景或非当前 artifact 引用继续拒绝，保留 Core 的 fail-closed contract 校验。(local)
- [Sense] [Fix] [Planned] Fix the RGB scene-understanding Provider's minimal binding for empty semantic provenance: bind only missing/empty provenance to the RGB artifact actually resolved and sent for this request; continue rejecting explicit unknown, cross-scene, or non-current artifact references while retaining Core fail-closed contract validation. (local)
- 具体失败场景：`task_1166bb21a6604397` 的首次理解结果在实体字段进入 Core 时返回 `invalid_entity_claim`，原因是 Responses schema 允许空 provenance，导致合法的当前 RGB 语义声明不能进入 RGB-D 几何组合；普通类型无法把“模型省略来源”与“模型伪造来源”区分，直接放宽 Core 会影响所有 Provider，因此只在 Adapter 用已解析的当前 RGB 输入补齐空值，显式错误引用仍失败。
- Concrete failure scenario: the first understanding result for `task_1166bb21a6604397` reached Core as `invalid_entity_claim` because the Responses schema allowed empty provenance, preventing a legitimate current-RGB semantic claim from entering RGB-D composition. Ordinary types cannot distinguish an omitted model source from a forged source; widening Core would affect every Provider, so the Adapter fills only empty provenance from the resolved current RGB input and still rejects explicit bad references.
- [chore] [chore] [计划] 将已安装的 Skill `2.6.4` / Node `0.6.4` 递增为不可覆盖的 `2.6.5` / `0.6.5`，包含空 provenance Adapter 修复并重新隔离安装；保留 2.6.4 任务 binding 与历史 artifact。(local)
- [Chore] [Chore] [Planned] Advance the installed Skill `2.6.4` / Node `0.6.4` to non-overwriting `2.6.5` / `0.6.5` with the empty-provenance Adapter fix and reinstall it in isolation; preserve the 2.6.4 task binding and historical artifacts. (local)

## v11.3.0 (2026-09-21 14:31) - codex

### 变更计划 / Planned changes

- [policy] [feat] [计划] 增加显式 `goal_source=benchmark_task_definition` 基线，将 RoboTwin `task.goal` 的 opaque `destination_ref` 作为 benchmark 目标输入；保留 observation-owned profile 的 Agent 自主 `manipulation.target` 路径，禁止隐式 fallback。(local)
- [Policy] [Feat] [Planned] Add an explicit `goal_source=benchmark_task_definition` baseline that consumes RoboTwin `task.goal` opaque `destination_ref` values; preserve the observation-owned Agent-selected `manipulation.target` path and forbid implicit fallback. (local)
- [agent] [fix] [计划] 让 PlanGraph materialization 从同任务、同场景的已完成 goal/target/capability predecessor 传播 `entity_ref`、`destination_ref` 和 `capability_snapshot_ref`；传播失败时在 ready projection 阶段报告不可绑定，不交给模型猜测。(local)
- [Agent] [Fix] [Planned] Propagate `entity_ref`, `destination_ref`, and `capability_snapshot_ref` during PlanGraph materialization from completed same-task, same-scene goal/target/capability predecessors; report unbindable nodes in ready projection instead of asking the model to guess. (local)
- [agent] [fix] [计划] 将确定性 plan-contract selection rejection 在当前 node turn 内短路，复用现有 `awaiting_replan` 生命周期，避免重复模型确认和表面 Provider timeout；不新增任务状态机或自动 Action 重试。(local)
- [Agent] [Fix] [Planned] Short-circuit deterministic plan-contract selection rejections within the current node turn and reuse the existing `awaiting_replan` lifecycle, avoiding repeated model confirmation and apparent Provider timeouts without adding a second task state machine or automatic Action retries. (local)
- [eval] [exp] [计划] 增加 benchmark/observed profile 正负测试、no-motion baseline、任务级可复现元数据与七维验收报告；PAOS verdict、RoboTwin benchmark result 和累计多 Action 视频继续独立持久化。(local)
- [Eval] [Exp] [Planned] Add positive/negative benchmark/observed profile tests, a no-motion baseline, task-level reproducibility metadata, and the seven-dimension acceptance report; keep PAOS verdict, RoboTwin benchmark result, and cumulative multi-Action video independently persisted. (local)

### 预期影响文件 / Expected affected files

- `PhyAgentOS/agent/plan_proposal.py`
- `PhyAgentOS/agent/planning_dispatch.py`
- `PhyAgentOS/agent/planning_loop.py`
- `PhyAgentOS/forge/task.py`
- `examples/forge-adapters/robotwin20/src/robotwin20_adapter/persistent_deployment.py`
- `examples/forge-adapters/robotwin20/src/robotwin20_adapter/persistent_host.py`
- `examples/forge-adapters/robotwin20/profiles/forge-persistent/persistent-host.yaml`
- `examples/forge-skills/pick-place-workflow/skill.yaml`
- focused tests and `docs/forge/ROBOTWIN_ORACLE_BASELINE_DESIGN_V11_2_0.md`
