# 2026-09 Changelog — Part 4

## v7.4.0 (2026-09-09 10:52) - codex

### 预期修改 / Planned Changes

- [计划] [eval] [exp] 将 v7.3.15 已资格评估的 GraspGen `0.015 m` ingress-backoff variant 物化为新的 RoboTwin 双臂 route package，并显式绑定 qualification artifact；不改变 GraspGen depth、碰撞策略、PAOS planning、Skill 或 Gateway。
- [Planned] [Eval] [Exp] Materialize the v7.3.15 qualified GraspGen `0.015 m` ingress-backoff variant as a new RoboTwin dual-arm route package with an explicit qualification-artifact binding; do not change GraspGen depth, collision policy, PAOS planning, Skills, or Gateway.
- [计划] [eval] [test] 独立校验 package 的 route、source manifest、双臂 capability、controller qualification、worker 与 contact-qualification binding；完成后停在新的 simulation-only 人工审批边界，不运行 planner、`scene.step()`、Gateway、Dora、Action 或硬件。
- [Planned] [Eval] [Test] Independently validate the package route, source manifest, dual-arm capabilities, controller qualification, worker, and contact-qualification binding; then stop at a fresh simulation-only human-approval boundary without running planner, `scene.step()`, Gateway, Dora, Action, or hardware.

### 预期影响文件 / Planned Files

- `changelog/2026-09_part4.md`
- `/home/yanxu/robotwin20-runtime/artifacts/paos-route-v7.4.0-graspgen-contact-backoff-*/`

### 实际结果 / Actual Results

- [完成] [eval] [exp] 已物化 `/home/yanxu/robotwin20-runtime/artifacts/paos-route-v7.4.0-graspgen-contact-backoff-20260909T1100Z/`；route geometry digest 为 `7f9055c2090c64ee88f42e00657e3c7800173c7e5c2c53ec5bd303921083935a`，source manifest digest 为 `8fb85da145f034a4a3283bbfa43bdd4378499d78443b15ce8d882b911b8bffeb`。/ [Completed] Materialized `/home/yanxu/robotwin20-runtime/artifacts/paos-route-v7.4.0-graspgen-contact-backoff-20260909T1100Z/`; route geometry digest is `7f9055c2090c64ee88f42e00657e3c7800173c7e5c2c53ec5bd303921083935a`, and source manifest digest is `8fb85da145f034a4a3283bbfa43bdd4378499d78443b15ce8d882b911b8bffeb`.
- [完成] [policy] [fix] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/grasp_adaptation.py:L201-L213,L228-L240` 允许 route profile 已定义的 `contact_backoff_candidates_m` 和既有 `grasp_geometry` proposal 字段通过 schema 兼容检查；未改变坐标变换、GraspGen depth、碰撞策略或执行权限。/ [Completed] [Policy] [Fix] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/grasp_adaptation.py:L201-L213,L228-L240` allows the route profile's existing `contact_backoff_candidates_m` and `grasp_geometry` proposal field through schema compatibility checks; transforms, GraspGen depth, collision policy, and execution authority are unchanged.
- [完成] [eval] [test] contact qualification 选择 `0.015 m` variant：support clearance `+0.002807598225252672 m` 且 pinch containment 有效；`0/0.005/0.01 m` 因桌面穿透拒绝，`0.02 m` 因 pinch containment 失败拒绝。/ [Completed] [Eval] [Test] Contact qualification selected the `0.015 m` variant with `+0.002807598225252672 m` support clearance and valid pinch containment; `0/0.005/0.01 m` were rejected for table-plane penetration and `0.02 m` for failed pinch containment.
- [完成] [eval] [test] route 通过 `validate_route_request()`，包含完整 `approach/contact/close/lift/transport/descent/release/retreat` 阶段；candidate provenance 绑定 contact-qualification，source manifest 绑定双臂 capability、controller qualification、collision world 与 worker `6534f093bb4fb76f747a9532646be056520ef69c91c9a61b8a096797d3e092cc`。/ [Completed] [Eval] [Test] The route passed `validate_route_request()` with complete `approach/contact/close/lift/transport/descent/release/retreat` phases; candidate provenance binds contact qualification, and the source manifest binds both arm capabilities, controller qualification, collision world, and worker `6534f093bb4fb76f747a9532646be056520ef69c91c9a61b8a096797d3e092cc`.

### 六维验收 / Six-Dimension Acceptance

1. 架构集成：通过。改动位于 RoboTwin adapter 的 grasp adaptation 兼容层；PAOS planning、Skill、Gateway 和仿真执行面未被接管。/ Passed. The change stays in the RoboTwin adapter's grasp-adaptation compatibility layer; PAOS planning, Skills, Gateway, and simulation execution remain untouched.
2. 失败路径：通过。未资格化的 backoff、非法字段、缺失绑定和 route 结构错误仍由既有 qualification/materialization/readiness 校验拒绝。/ Passed. Unqualified backoffs, invalid fields, missing bindings, and malformed route structure remain rejected by the existing qualification, materialization, and readiness checks.
3. 权威与安全边界：通过。package 和 review request 均保持 `simulation_only=true`、`motion_authorized=false`；未运行 planner、`scene.step()`、Gateway、Dora、Action 或硬件。/ Passed. The package and review request retain `simulation_only=true` and `motion_authorized=false`; no planner, `scene.step()`, Gateway, Dora, Action, or hardware was run.
4. 配置与复现：通过。route、qualification、双臂 capability、controller qualification、collision world、worker 和原始 GraspGen bundle 均由 package manifest 绑定，可由 digest 重现。/ Passed. The route, qualification, dual-arm capabilities, controller qualification, collision world, worker, and original GraspGen bundle are bound by the package manifest and reproducible from its digests.
5. 可维护性：通过。仅复用已有 profile/materialization/route-readiness 流程；没有新增 planner service、store、scheduler 或执行通道。/ Passed. The implementation reuses the existing profile, materialization, and route-readiness flow without adding a planner service, store, scheduler, or execution channel.
6. 防止过度防御编程：通过。兼容修复对应一个已复现的 materialization 失败，不新增 hash、baseline、冻结 contract 或额外 gate；现有 digest 仅用于 package 原有 provenance。/ Passed. The compatibility fix addresses a reproduced materialization failure without adding hashes, baselines, frozen contracts, or extra gates; existing digests remain part of the package's established provenance.

### 验证 / Validation

- `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=.:examples/forge-adapters/robotwin20/src:examples/forge-adapters/robotwin20/runtime:examples/forge-adapters/robotwin20/scripts python -m pytest -q -p pytest_asyncio.plugin examples/forge-adapters/robotwin20/tests/test_grasp_adaptation.py examples/forge-adapters/robotwin20/tests/test_grasp_postprocessing.py examples/forge-adapters/robotwin20/tests/test_qualify_grasp_contact.py examples/forge-adapters/robotwin20/tests/test_route_generation.py examples/forge-adapters/robotwin20/tests/test_route_readiness.py examples/forge-adapters/robotwin20/tests/test_collision_world.py`: `65 passed`。
- 独立 package 验证：`validate_route_request()` 通过；selected backoff、contact center、robot target、route phases、candidate provenance、双臂 capability、controller qualification、collision world 和 worker 绑定全部通过；`motion_authorized=false`。
- Independent package validation: `validate_route_request()` passed; selected backoff, contact center, robot target, route phases, candidate provenance, dual-arm capabilities, controller qualification, collision world, and worker bindings all passed; `motion_authorized=false`.

### Git 提交 / Git Commit

- Commit: `57539a2`；Branch: `feature/planning-loop`；已推送。/ Commit: `57539a2`; Branch: `feature/planning-loop`; pushed.

## v7.3.11 (2026-09-09 02:10) - codex

### 预期修改 / Planned Changes

- [完成] [policy] [fix] 将 Future-use comparison 幂等范围收窄到完整 parent/candidate pair 的 observation key 集合；同一 candidate 的新增 held-out/hazard pair 会产生新比较，重复或未配对 observation 不会重触发 sink。
- [Completed] [Policy] [Fix] Narrow Future-use comparison idempotence to complete parent/candidate-pair observation keys; a new held-out/hazard pair for one candidate produces a new comparison, while duplicate or unpaired observations do not retrigger the sink.
- [完成] [policy] [feat] 增加 PAOS 宿主显式 Skill candidate review/promotion 入口，复用 `SkillEvolutionManager` 的现有写入、验证、回滚路径；EvoPhy candidate 不再只能停留在 collecting。
- [Completed] [Policy] [Feat] Add an explicit PAOS-host Skill candidate review/promotion entry point that reuses `SkillEvolutionManager` writing, validation, and rollback; EvoPhy candidates will no longer remain collecting forever.
- [完成] [eval] [fix] 将 extension 的评测 receipt 和 selection decision 通过可选 lifecycle port 回写既有事件账本；Skill review 只接受当前 receipt 集的 `promote` 决定，避免未评测或旧决定覆盖后续风险证据。
- [Completed] [Eval] [Fix] Return extension evaluation receipts and selection decisions through an optional lifecycle port into the existing event ledger; Skill review accepts only a `promote` decision for the current receipt set, preventing missing evaluation or stale decisions from overriding later hazard evidence.
- [完成] [eval] [test] 增加同 candidate 新 pair、重复 pair、未配对 observation、显式 review/promotion 和 extension selection 转发的 fake-facts 测试。
- [Completed] [Eval] [Test] Add fake-facts coverage for new pairs on one candidate, duplicate pairs, unpaired observations, explicit review/promotion, and extension selection forwarding.

### 预期影响文件 / Planned Files

- `PhyAgentOS/agent/experience/evolution_extension_adapter.py`
- `PhyAgentOS/agent/experience/evolution.py`
- `PhyAgentOS/agent/experience/coordinator.py`
- `PhyAgentOS/agent/experience/store.py`
- `tests/test_evolution_extension_adapter.py`
- `extensions/evolution/evolution/api.py`
- `extensions/evolution/evolution/evaluation.py`
- `extensions/evolution/evolution/plugin.py`
- `extensions/evolution/README.md`
- `docs/zh/06-consequence-driven-skill-evolution-design.md`

### 实际结果 / Actual Results

- [完成] [policy] [fix] `PhyAgentOS/agent/experience/store.py:L759-L765,L920-L947` 增加 candidate 单项读取，并将 event 幂等比较改为解码后的 JSON 值比较，避免 JSON 键顺序使相同 Future-use receipt 重复入账。/ [Completed] [Policy] [Fix] `PhyAgentOS/agent/experience/store.py:L759-L765,L920-L947` adds single-candidate lookup and compares decoded JSON event values for idempotence, so JSON key order cannot duplicate an equivalent Future-use receipt.
- [完成] [policy] [feat] `PhyAgentOS/agent/experience/evolution.py:L528-L580` 和 `coordinator.py:L183-L189` 增加具名 reviewer 的显式 Skill review 入口；EvoPhy candidate 必须具有独立 episode 支持、matched/held-out/hazard 的 passing receipt，以及绑定当前 receipt 集的 `promote` decision，之后才复用既有 Skill 写入与回滚路径。/ [Completed] [Policy] [Feat] `PhyAgentOS/agent/experience/evolution.py:L528-L580` and `coordinator.py:L183-L189` add a named-reviewer Skill review entry point; an EvoPhy candidate needs independent episode support, passing matched/held-out/hazard receipts, and a `promote` decision bound to the current receipt set before reusing the existing Skill writer and rollback path.
- [完成] [eval] [fix] `extensions/evolution/evolution/api.py:L275-L330`、`plugin.py:L134-L176` 与 `PhyAgentOS/agent/experience/evolution_extension_adapter.py:L63-L94` 使 receipt 与 selection 都经 `EvaluationLifecyclePort` 回写现有事件账本；`on_candidate_selected(...)` 仅传递选择请求，不授予 promotion 权限。/ [Completed] [Eval] [Fix] `extensions/evolution/evolution/api.py:L275-L330`, `plugin.py:L134-L176`, and `PhyAgentOS/agent/experience/evolution_extension_adapter.py:L63-L94` return both receipts and selections through `EvaluationLifecyclePort` into the existing event ledger; `on_candidate_selected(...)` only transports a selection request and grants no promotion authority.
- [完成] [policy] [fix] `PhyAgentOS/agent/experience/evolution_extension_adapter.py:L190-L239` 只在完整 parent/candidate pair 集合变化时调用 Future-use comparison；未配对 observation 仍持久化，待另一侧到达后参与比较，但不重复调用 metrics sink。/ [Completed] [Policy] [Fix] `PhyAgentOS/agent/experience/evolution_extension_adapter.py:L190-L239` invokes Future-use comparison only when the complete parent/candidate-pair set changes; an unpaired observation remains persisted and joins when its counterpart arrives without re-invoking the metrics sink.
- [完成] [eval] [test] `tests/test_evolution_extension_adapter.py:L63-L295` 与 `extensions/evolution/tests/test_evolution_extension.py:L930-L1000` 覆盖 adapter 重建、重复/未配对 Future-use、旧 selection、完整 extension receipt/selection 回流、显式 reviewer 与写入拒绝。/ [Completed] [Eval] [Test] `tests/test_evolution_extension_adapter.py:L63-L295` and `extensions/evolution/tests/test_evolution_extension.py:L930-L1000` cover adapter reconstruction, duplicate/unpaired Future-use, stale selection, full extension receipt/selection delivery, explicit reviewer behavior, and rejected writes.
- [完成] [docs] [docs] `extensions/evolution/README.md:L101-L150` 与 `docs/zh/06-consequence-driven-skill-evolution-design.md:L718-L732` 说明 selection 回流 API、完整 pair 的 comparison 语义及仍缺少真实多 episode 端到端证据的边界。/ [Completed] [Docs] [Docs] `extensions/evolution/README.md:L101-L150` and `docs/zh/06-consequence-driven-skill-evolution-design.md:L718-L732` document the selection-return API, complete-pair comparison semantics, and the remaining absence of real multi-episode end-to-end evidence.

### 关键代码 Diff / Key Code Diff

```python
# Before: receipt could return through the extension, but selection had no extension entry point.
extension.on_candidate_evaluated(receipt)

# After: evaluator facts and its non-authoritative request use the same extension boundary.
extension.on_candidate_evaluated(receipt)
extension.on_candidate_selected(decision)
```

```python
# Before: an unrelated unpaired observation changed the completion payload.
observation_keys = all_observation_keys

# After: only a newly complete parent/candidate pair can retrigger aggregation.
observation_keys = paired_observation_keys(observations)
```

### 六维验收 / Six-Dimension Acceptance

1. 架构集成：通过。`evolution` 保持 optional extension；PAOS Core 只提供 existing store、review、Skill writer 与 lifecycle adapter。/ Passed. `evolution` remains an optional extension; PAOS Core provides only the existing store, review, Skill writer, and lifecycle adapter.
2. 失败路径：通过。缺失 split、未知/失败 receipt、旧 selection、未配对 Future-use 与无效 Skill 内容都不能写入或激活 Skill。/ Passed. Missing splits, unknown/failed receipts, stale selections, unpaired Future-use, and invalid Skill content cannot write or activate a Skill.
3. 权威与安全边界：通过。selection 是请求，具名 reviewer 才能调用 host writer；未新增自动 promotion、runtime Tool、Gateway、硬件或执行通道。/ Passed. Selection is a request and only a named reviewer can invoke the host writer; no automatic promotion, runtime Tool, Gateway, hardware, or execution path was added.
4. 配置与复现：通过。receipt payload、method ID、trial identity、当前 receipt 集与 Future-use pair keys 都保留在现有事件账本；extension wheel 构建成功。/ Passed. Receipt payloads, method IDs, trial identities, current receipt sets, and Future-use pair keys remain in the existing event ledger; the extension wheel builds successfully.
5. 可维护性：通过。receipt/selection 共用 `EvaluationLifecyclePort`；Future-use 使用现有 `ExperienceStore`，没有第二 store、scheduler、thread 或 background worker。/ Passed. Receipt and selection share `EvaluationLifecyclePort`; Future-use uses the existing `ExperienceStore` with no second store, scheduler, thread, or background worker.
6. 防止过度防御编程：通过。改动针对两个已复现失败场景：JSON 键顺序导致重复入账、未配对 observation 导致重复指标；未新增 hash、baseline、冻结 contract 或额外审批 gate。/ Passed. Changes address two reproduced failures, JSON key-order duplicate recording and unpaired-observation metric replay; no hash, baseline, frozen contract, or extra approval gate was added.

### 验证 / Validation

- `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 uv run --extra dev python -m pytest -q tests/test_evolution_extension_adapter.py tests/test_evolution_extension_hook.py`: `11 passed`（仅现有 pytest asyncio 配置 warning）。
- `cd extensions/evolution && PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 uv run --group dev python -m pytest -q`: `68 passed`; Ruff 与 `uv build` 通过。
- `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 uv run --extra dev --extra tui python -m pytest -q -p pytest_asyncio.plugin`: `258 passed`。
- PAOS Ruff、`python -m compileall -q PhyAgentOS/agent/experience extensions/evolution/evolution` 与 `git diff --check` 通过。

### Git 状态 / Git Status

- Commit: 无（用户要求仅本地保存）
- Push: 未执行（用户要求仅本地保存）

## v7.3.10 (2026-09-09 01:45) - codex

### 预期修改 / Planned Changes

- [计划] [policy] [fix] 修复 Future-use 配对完成后的重复 comparison：复用 `evolution_events` 记录 comparison 完成状态，重复 observation 不再次触发扩展比较或指标 sink。
- [Planned] [Policy] [Fix] Prevent repeated Future-use comparisons after a pair is complete by reusing `evolution_events` to record completion; duplicate observations will not retrigger extension comparison or metric sinks.
- [计划] [eval] [test] 运行重复提交、扩展和 PAOS 全量回归。
- [Planned] [Eval] [Test] Run duplicate-submission, extension, and full PAOS regressions.

### 预期影响文件 / Planned Files

- `PhyAgentOS/agent/experience/evolution_extension_adapter.py`
- `tests/test_evolution_extension_adapter.py`

### 实际结果 / Actual Results

- [完成] [policy] [fix] `PhyAgentOS/agent/experience/evolution_extension_adapter.py:L140-L177` 在 pair comparison 成功后写入 `future_use_comparison` event；重建 adapter 或重复提交 observation 时先读取该 event，避免重复比较和重复指标 sink。/ [Completed] [Policy] [Fix] `PhyAgentOS/agent/experience/evolution_extension_adapter.py:L140-L177` records a `future_use_comparison` event after a successful pair comparison; reconstructed adapters and duplicate submissions check this event first, preventing repeated comparisons and metric sinks.
- [完成] [eval] [test] `tests/test_evolution_extension_adapter.py:L60-L88` 覆盖跨 adapter 重建和重复 candidate observation；extension `66 passed`，PAOS 全量 `254 passed`，Ruff、compileall、git diff --check 通过。/ [Completed] [Eval] [Test] `tests/test_evolution_extension_adapter.py:L60-L88` covers adapter reconstruction and duplicate candidate observations; extension `66 passed`, full PAOS `254 passed`, with Ruff, compileall, and git diff --check passing.

### 六维验收 / Six-Dimension Acceptance

1. 架构集成：通过。幂等状态复用 PAOS 现有事件账本，无新持久化组件。/ Passed. Idempotence reuses the existing PAOS event ledger without a new persistence component.
2. 失败路径：通过。comparison 未产生时不写完成标记，后续重放仍可继续。/ Passed. No completion marker is written when comparison is incomplete, so replay can continue.
3. 权威与安全边界：通过。只记录比较完成，不改变 Skill、binding 或 promotion。/ Passed. Only comparison completion is recorded; Skills, bindings, and promotion remain untouched.
4. 配置与复现：通过。完成事件按 candidate 记录 observation keys，重放可识别已完成 pair。/ Passed. Completion is recorded per candidate with observation keys for replay identification.
5. 可维护性：通过。复用 `record_event_once` 和 `list_event_payloads`，未增加 scheduler、线程或第二套 store。/ Passed. Existing event APIs are reused without a scheduler, thread, or second store.
6. 可观察性与 Anti-OverDefense：通过。修复对应具体重复指标场景，未增加 hash、baseline 或 gate。/ Passed. The fix addresses a concrete duplicate-metric scenario without adding hashes, baselines, or gates.

## v7.3.9 (2026-09-09 01:20) - codex

### 预期修改 / Planned Changes

- [计划] [policy] [fix] Future-use event adapter 对重复 observation 采用现有 `record_event_once` 幂等语义，重复提交不再次触发 comparison。
- [Planned] [Policy] [Fix] Make the Future-use event adapter use the existing `record_event_once` idempotence so duplicate observations do not trigger repeated comparisons.
- [计划] [eval] [test] 增加重复提交测试并运行扩展和 PAOS 全量回归。
- [Planned] [Eval] [Test] Add duplicate-submission coverage and rerun extension and full PAOS regressions.

### 预期影响文件 / Planned Files

- `PhyAgentOS/agent/experience/evolution_extension_adapter.py`
- `tests/test_evolution_extension_adapter.py`

## v7.3.8 (2026-09-09 00:55) - codex

### 预期修改 / Planned Changes

- [计划] [policy] [feat] 在 PAOS 宿主 adapter 中复用现有 evolution_events 持久化 Future-use observations，并在同一 candidate 配对齐全后调用 extension comparison；不新增表、store 或 scheduler。
- [Planned] [Policy] [Feat] Reuse existing evolution_events in the PAOS host adapter to persist Future-use observations and invoke extension comparison once a candidate has complete pairs; add no table, store, or scheduler.
- [计划] [eval] [test] 增加跨 episode 配对、重复 observation 幂等与进程重建后继续比较的 fake-facts 测试。
- [Planned] [Eval] [Test] Add fake-facts tests for cross-episode pairing, duplicate-observation idempotence, and comparison after adapter reconstruction.

### 预期影响文件 / Planned Files

- `PhyAgentOS/agent/experience/store.py`
- `PhyAgentOS/agent/experience/evolution_extension_adapter.py`
- `tests/test_evolution_extension_adapter.py`

## v7.3.15 (2026-09-09 02:35) - codex

### 预期修改 / Planned Changes

- [计划] [policy] [fix] 根据真实 no-motion 证据，在 RoboTwin route profile 中声明有限的 ingress backoff 候选（0–20 mm 范围内由 provider 明确列出），由 qualification CLI 逐一评估桌面净空和 pinch 约束；不修改 GraspGen depth，不加入隐式偏移。
- [Planned] [Policy] [Fix] Based on the real no-motion evidence, declare a finite provider-owned ingress-backoff candidate list in the RoboTwin route profile and have the qualification CLI evaluate each candidate against table clearance and pinch constraints; do not modify GraspGen depth or add implicit offsets.
- [计划] [eval] [test] 覆盖 profile 候选列表解析、15 mm 合法变体、20 mm pinch 失败、空列表/乱序输入和实际 artifact 重新 qualification；保持 no-motion，不运行 planner、scene.step、Gateway、Dora、Action 或硬件。
- [Planned] [Eval] [Test] Cover profile-list parsing, the valid 15 mm variant, 20 mm pinch failure, empty/unsorted inputs, and requalification of the real artifact; remain no-motion and run no planner, scene.step, Gateway, Dora, Action, or hardware.

### 实际结果 / Actual Results

- [完成] [policy] [fix] `examples/forge-adapters/robotwin20/profiles/robotwin20/route-inputs.yaml:L23-L25` 声明 `contact_backoff_candidates_m=[0.0, 0.005, 0.010, 0.015, 0.020]`；候选是 RoboTwin provider 的显式 ingress route 变体，不改变 GraspGen `depth_m=0.10527314`。
- [Completed] [Policy] [Fix] `examples/forge-adapters/robotwin20/profiles/robotwin20/route-inputs.yaml:L23-L25` declares `contact_backoff_candidates_m=[0.0, 0.005, 0.010, 0.015, 0.020]`; the values are explicit RoboTwin-provider ingress-route variants and leave GraspGen `depth_m=0.10527314` unchanged.
- [完成] [policy] [fix] `examples/forge-adapters/robotwin20/scripts/materialize_complete_route.py:L152-L179,L631-L634` 验证并物化该有限列表；`examples/forge-adapters/robotwin20/scripts/qualify_grasp_contact.py:L65-L78,L118-L125` 使用同一列表评估实际 hand pose，拒绝空、乱序、负值或非有限输入。
- [Completed] [Policy] [Fix] `examples/forge-adapters/robotwin20/scripts/materialize_complete_route.py:L152-L179,L631-L634` validates and materializes the finite list; `examples/forge-adapters/robotwin20/scripts/qualify_grasp_contact.py:L65-L78,L118-L125` evaluates actual hand poses with that list and rejects empty, unordered, negative, or non-finite inputs.
- [完成] [eval] [test] `examples/forge-adapters/robotwin20/tests/test_qualify_grasp_contact.py:L60-L124` 覆盖 15 mm 首个合法 variant、空列表与乱序 profile；专项 `30 passed`，RoboTwin adapter 全量 `341 passed, 1 skipped`。
- [Completed] [Eval] [Test] `examples/forge-adapters/robotwin20/tests/test_qualify_grasp_contact.py:L60-L124` covers the first valid 15 mm variant plus empty and unordered profiles; focused tests report `30 passed`, and the full RoboTwin adapter suite reports `341 passed, 1 skipped`.
- [完成] [eval] [exp] 实际 no-motion artifact `/home/yanxu/robotwin20-runtime/artifacts/paos-grasp-contact-v7.3.15-20260909T1045Z/qualification.json` 选择 `0.015 m`：`0/5/10 mm` 的支持面净空分别为 `-12.1636/-7.1732/-2.1828 mm`，`15 mm` 为 `+2.8076 mm` 且 pinch 仍在对象内；`20 mm` 虽为 `+7.7980 mm`，但 pinch 已离开对象而被拒绝。
- [Completed] [Eval] [Exp] The real no-motion artifact `/home/yanxu/robotwin20-runtime/artifacts/paos-grasp-contact-v7.3.15-20260909T1045Z/qualification.json` selects `0.015 m`: support clearance is `-12.1636/-7.1732/-2.1828 mm` at `0/5/10 mm`; `15 mm` reaches `+2.8076 mm` while retaining pinch; `20 mm` reaches `+7.7980 mm` but is rejected because the pinch exits the object.

### 六维验收 / Six-Dimension Acceptance

1. 架构集成：通过。候选定义与 qualification 都留在 RoboTwin provider adapter；PAOS planning、Skill、Gateway、任务生命周期和执行面未改变。 / Passed. Candidate definition and qualification remain in the RoboTwin provider adapter; PAOS planning, Skills, Gateway, task lifecycle, and execution remain unchanged.
2. 失败路径：通过。无候选、乱序、负值、非有限值、桌面净空不足和 pinch 失效均不能产生 qualified variant。 / Passed. Empty, unordered, negative, non-finite, insufficient-clearance, and pinch-invalid cases cannot yield a qualified variant.
3. 权威与安全边界：通过。结果仅为 `motion_authorized=false` 的 no-motion evidence；它不授权 route 执行，也不放宽桌面碰撞。 / Passed. The result is no-motion evidence with `motion_authorized=false`; it authorizes neither route execution nor relaxed table collision.
4. 配置与复现：通过。候选由 versioned provider profile 声明，并在 materialized adaptation artifact 中保留；实际 evidence 使用独立时间戳目录。 / Passed. Candidates are declared by the versioned provider profile and retained in the materialized adaptation artifact; real evidence uses an independent timestamped directory.
5. 可维护性：通过。复用现有 `qualify_geometry_artifact` 和 route materializer，不新增 planner、Grasp provider、store、scheduler 或平行执行路径。 / Passed. Existing `qualify_geometry_artifact` and the route materializer are reused without a new planner, Grasp provider, store, scheduler, or parallel execution path.
6. 防止过度防御编程：通过。新增检查只约束本功能需要的有限、单调、非负候选列表；未新增 hash、baseline、冻结 contract 或审批 gate。 / Passed. New checks only constrain the finite, monotonic, non-negative candidate list required by this feature; no hash, baseline, frozen contract, or approval gate was added.

### 下一步 / Next Step

重新物化包含 selected contact variant 的 route package，独立校验其绑定后，再申请新的 simulation-only 人工审批。此前不得执行 planner、`scene.step()`、Gateway、Dora、Action 或硬件。 / Rematerialize a route package containing the selected contact variant, independently validate its bindings, then request fresh simulation-only human approval. Until then, do not run planner, `scene.step()`, Gateway, Dora, Action, or hardware.

## v7.3.7 (2026-09-09 00:35) - codex

### 预期修改 / Planned Changes

- [计划] [policy] [fix] 要求 Future-use builder 接收宿主确认的 Skill binding，未确认激活时不得生成 candidate observation，避免把 candidate promotion 或候选存在误当成真实使用。
- [Planned] [Policy] [Fix] Require explicit host confirmation of Skill binding in the Future-use builder; an unconfirmed activation must not produce a candidate observation, preventing promotion or candidate existence from being treated as actual use.
- [计划] [eval] [test] 增加未确认 binding 的 fake-facts 回归测试并重新运行扩展与 PAOS 全量测试。
- [Planned] [Eval] [Test] Add a fake-facts regression for unconfirmed binding and rerun extension and full PAOS tests.

### 预期影响文件 / Planned Files

- `extensions/evolution/evolution/observation.py`
- `extensions/evolution/tests/test_evolution_extension.py`

## v7.3.13 (2026-09-09 02:10) - codex

### 预期修改 / Planned Changes

- [计划] [policy] [feat] 增加 provider-owned no-motion GraspGen contact qualification CLI：读取已物化 candidate、contact geometry 和 route profile，使用 profile 声明的 RoboTwin target reference distance、gripper bias 与 delta matrix 派生实际 hand pose，再调用纯 qualification 内核；不调用 planner、scene.step、Gateway、Dora、Action 或硬件。
- [Planned] [Policy] [Feat] Add a provider-owned no-motion GraspGen contact-qualification CLI that reads a materialized candidate, contact geometry, and route profile, derives the actual hand pose from the profile-declared RoboTwin target reference distance, gripper bias, and delta matrix, then invokes the pure qualification kernel without planner, scene.step, Gateway, Dora, Action, or hardware.
- [计划] [eval] [test] 覆盖 profile/candidate/geometry 绑定、缺失或非法 provider transform、输出不可覆盖及 `unavailable` 结果；保持无额外固定偏移和无 speculative 防御。
- [Planned] [Eval] [Test] Cover profile/candidate/geometry bindings, missing or invalid provider transforms, non-overwritable output, and `unavailable` results while adding no fixed offset or speculative defense.

### 实际结果 / Actual Results

- [完成] [policy] [feat] `examples/forge-adapters/robotwin20/scripts/qualify_grasp_contact.py:L1-L145` 新增 no-motion CLI；从 candidate、contact geometry、scene facts 和 route profile 读取输入，显式派生实际 RoboTwin hand pose 后调用既有纯 qualification 内核，输出仅包含 `motion_authorized: false` 的 qualification artifact。
- [Completed] [Policy] [Feat] `examples/forge-adapters/robotwin20/scripts/qualify_grasp_contact.py:L1-L145` adds a no-motion CLI that reads candidate, contact geometry, scene facts, and route profile inputs, explicitly derives the actual RoboTwin hand pose, then invokes the existing pure qualification kernel and emits only a qualification artifact with `motion_authorized: false`.
- [完成] [eval] [test] `examples/forge-adapters/robotwin20/tests/test_qualify_grasp_contact.py:L1-L107` 覆盖 profile 派生、candidate/scene/geometry 失配和不可覆盖输出；CLI 与 postprocessing 专项 `21 passed`，Ruff 通过。未运行 planner、scene.step、Gateway、Dora、Action 或硬件。
- [Completed] [Eval] [Test] `examples/forge-adapters/robotwin20/tests/test_qualify_grasp_contact.py:L1-L107` covers profile derivation, candidate/scene/geometry mismatch, and non-overwritable output; CLI plus postprocessing focused tests report `21 passed`, with Ruff passing. No planner, scene.step, Gateway, Dora, Action, or hardware was run.

### 实际结果 / Actual Results

- [完成] [eval] [exp] 使用 `/home/yanxu/robotwin20-runtime/artifacts/paos-route-v7.3.2-graspgen-contact-20260908T2345Z` 的实际 route candidate、scene facts 和 contact geometry，生成 `/home/yanxu/robotwin20-runtime/artifacts/paos-grasp-contact-v7.3.14-20260909T0215Z/qualification.json`；结果为 `unavailable`，接触中心仍在物体内，但实际 hand pose 投影后的桌面净空为 `-0.0121636496 m`。未修改 GraspGen depth，未运行 planner、scene.step、Gateway、Dora、Action 或硬件。
- [Completed] [Eval] [Exp] Using the actual route candidate, scene facts, and contact geometry from `/home/yanxu/robotwin20-runtime/artifacts/paos-route-v7.3.2-graspgen-contact-20260908T2345Z`, generated `/home/yanxu/robotwin20-runtime/artifacts/paos-grasp-contact-v7.3.14-20260909T0215Z/qualification.json`; the result is `unavailable`: the pinch center remains inside the object, but projected actual-hand clearance is `-0.0121636496 m`. GraspGen depth was unchanged and no planner, scene.step, Gateway, Dora, Action, or hardware was run.

### 六维验收 / Six-Dimension Acceptance

1. 架构集成：通过。真实验证调用 provider-owned CLI 和 adapter 纯计算，不接管 route 生命周期。 / Passed. The real check uses the provider-owned CLI and pure adapter computation without taking over route lifecycle.
2. 失败路径：通过。负净空保持 `unavailable`，没有把“动作可能成功”解释成安全成功。 / Passed. Negative clearance remains `unavailable`; possible physical success is not reinterpreted as safe success.
3. 权威边界：通过。未调用 planner、执行器或 Gateway，motion authority 保持 false。 / Passed. No planner, actuator, or Gateway was called; motion authority remains false.
4. 配置：通过。计算使用现有 route profile 和已物化 provider artifacts，没有新增参数源。 / Passed. Computation uses the existing route profile and materialized provider artifacts without a new parameter source.
5. 可维护性：通过。结果可由相同输入和 CLI 重现，证据独立保存。 / Passed. The result is reproducible from the same inputs and stored independently.
6. 防止过度防御编程：通过。没有新增哈希、冻结 contract、baseline 或额外 gate。 / Passed. No new hash, frozen contract, baseline, or extra gate was introduced.

### 六维验收 / Six-Dimension Acceptance

1. 架构集成：通过。CLI 仅编排 provider artifact 和纯 adapter 内核，不创建任务生命周期或执行通道。 / Passed. The CLI only composes provider artifacts and the pure adapter kernel; it creates no task lifecycle or execution channel.
2. 失败路径：通过。缺失对象、stale geometry、非法 profile 和已有 qualification 错误均显式失败。 / Passed. Missing objects, stale geometry, invalid profiles, and existing qualification errors fail explicitly.
3. 权威边界：通过。输出不授予 motion authority，且不接触 planner/Gateway。 / Passed. Output grants no motion authority and never touches the planner or Gateway.
4. 配置：通过。RoboTwin transform 只来自 route profile，CLI 不维护第二份参数。 / Passed. RoboTwin transforms come only from the route profile; the CLI maintains no second parameter source.
5. 可维护性：通过。复用 `derive_robot_hand_pose` 和 `qualify_geometry_artifact`，未复制几何算法。 / Passed. Existing `derive_robot_hand_pose` and `qualify_geometry_artifact` are reused without duplicating geometry algorithms.
6. 防止过度防御编程：通过。仅保留输入文件、几何绑定和输出不可覆盖所需检查，没有新增 hash、baseline 或 gate。 / Passed. Checks are limited to file, geometry binding, and non-overwrite requirements; no new hash, baseline, or gate was added.


## v7.3.12 (2026-09-09 01:30) - codex

### 预期修改 / Planned Changes

- [计划] [model] [fix] 修正 contact qualification 将 RoboTwin `robot_target_pose` 误当作 `panda_hand` pose 的语义错误；复用 provider 声明的 gripper bias 与 delta matrix 推导实际 hand/endlink pose 后再评估桌面净空。
- [Planned] [Model] [Fix] Correct the contact-qualification semantic error that treated RoboTwin `robot_target_pose` as the `panda_hand` pose; derive the actual hand/endlink pose from the provider-declared gripper bias and delta matrix before evaluating table clearance.
- [计划] [eval] [test] 增加 robot-target→hand pose 变换、缺失 provider transform 和真实 12 mm 净空失败证据测试；仅执行 no-motion 几何计算，不运行 scene.step、Gateway、Dora、Action 或硬件。
- [Planned] [Eval] [Test] Add robot-target-to-hand transform, missing-provider-transform, and real 12 mm clearance-failure coverage; run geometry qualification only with no scene.step, Gateway, Dora, Action, or hardware.

### 实际结果 / Actual Results

- [完成] [model] [fix] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/grasp_postprocessing.py:L82-L126` 新增显式 `reference_distance_m`，按 provider profile 的参考距离、夹爪 bias 与 delta matrix 推导 RoboTwin 实际 hand/endlink pose；移除函数内部隐含的 `0.12`，不改变 GraspGen depth 或 route target。
- [Completed] [Model] [Fix] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/grasp_postprocessing.py:L82-L126` adds explicit `reference_distance_m` and derives RoboTwin hand/endlink pose from provider-profile reference distance, gripper bias, and delta matrix; removes the function-local `0.12` without changing GraspGen depth or route targets.
- [完成] [model] [fix] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/grasp_postprocessing.py:L274-L325` 保留 `target_hand_pose` 作为 qualification 输入，避免把 `robot_target_pose` 误当作实际 hand pose；更新 `docs/forge/GRASPGEN_CONTACT_DEPTH_POSTPROCESSING.md:L32-L39` 明确调用方必须先使用 provider profile 派生 hand pose。
- [Completed] [Model] [Fix] `examples/forge-adapters/robotwin20/src/robotwin20_adapter/grasp_postprocessing.py:L274-L325` keeps `target_hand_pose` as an explicit qualification input so `robot_target_pose` is not mistaken for the physical hand pose; `docs/forge/GRASPGEN_CONTACT_DEPTH_POSTPROCESSING.md:L32-L39` now states that callers must derive the hand pose from the provider profile first.
- [完成] [eval] [test] `examples/forge-adapters/robotwin20/tests/test_grasp_postprocessing.py:L189-L260` 覆盖 profile 距离、输入不可变、非法 transform 与约 `-12.164 mm` 净空失败；专项 `17 passed`，RoboTwin adapter 全量 `335 passed, 1 skipped`，Ruff 与 `git diff --check` 通过。仅执行 no-motion 测试。
- [Completed] [Eval] [Test] `examples/forge-adapters/robotwin20/tests/test_grasp_postprocessing.py:L189-L260` covers profile distance, input immutability, invalid transforms, and the approximately `-12.164 mm` clearance failure; focused tests report `17 passed`, the full RoboTwin adapter suite `335 passed, 1 skipped`, with Ruff and `git diff --check` passing. No-motion tests only.

### 六维验收 / Six-Dimension Acceptance

1. 架构集成：通过。hand-pose 推导仍在 RoboTwin adapter 的纯几何层，qualification 通过显式输入消费；不接管任务生命周期、planner 或执行。 / Passed. Hand-pose derivation remains in the RoboTwin adapter's pure geometry layer and qualification consumes it through an explicit input; no task lifecycle, planner, or execution ownership is added.
2. 失败路径：通过。缺失/非法参考距离、bias、delta matrix 和实际负净空均显式失败或返回 `unavailable`。 / Passed. Missing or invalid reference distance, bias, delta matrix, and negative physical clearance fail explicitly or return `unavailable`.
3. 权威边界：通过。GraspGen proposal、provider geometry、PAOS route 与 Gateway authority 未被改写；未授权 motion。 / Passed. GraspGen proposals, provider geometry, PAOS routes, and Gateway authority remain unchanged; motion is not authorized.
4. 配置：通过。参考距离来自 provider profile，由调用方传入；未在新函数中新增第二配置源。 / Passed. Reference distance comes from the provider profile and is passed by the caller; no second configuration source was introduced.
5. 可维护性：通过。复用现有 profile/adaptation seam，保持输入不变并补充可重复测试。 / Passed. Existing profile/adaptation seams are reused, inputs remain immutable, and repeatable tests were added.
6. 防止过度防御编程：通过。新增校验仅覆盖几何计算必需的有限性与矩阵形状；没有新增 speculative hash、baseline、冻结 contract 或 motion gate。 / Passed. Added checks are limited to finiteness and matrix shape required for geometry; no speculative hash, baseline, frozen contract, or motion gate was added.

## v7.3.8 (2026-09-09 00:30) - codex

### 预期修改 / Planned Changes

- [计划] [policy] [diagnosis] 对 RoboTwin/Curobo table world projection 做 provider-only no-motion 对照，确认 SAPIEN table pose 与 Curobo planner base frame 的真实语义；不修改 GraspGen、PAOS Core 或碰撞规则。
- [Planned] [Policy] [Diagnosis] Run a provider-only no-motion comparison of RoboTwin/Curobo table-world projection to establish the actual SAPIEN-table to Curobo-planner-base semantics; do not change GraspGen, PAOS Core, or collision rules.
- [计划] [eval] [test] 分别测试原生 table pose、当前 `_native_table_geometry_for_planner()` 投影和候选 frame 映射对左右首个 target 的规划结果，保存可复现诊断；仅在证据明确后实施最小 provider 修复。
- [Planned] [Eval] [Test] Compare the native table pose, current `_native_table_geometry_for_planner()` projection, and candidate frame mappings for both arms' first target, preserving reproducible diagnostics; apply a minimal provider fix only if evidence is conclusive.

## v7.3.5 (2026-09-08 23:05) - codex

### 预期修改 / Planned Changes

- [计划] [policy] [feat] 增加 extension 的 Future-use 回流接口：宿主提交 parent/candidate 观察后，由 `FutureUseObserver` 生成配对比较，扩展只发送结果事件，不新增持久化或绑定状态。
- [Planned] [Policy] [Feat] Add a Future-use feedback interface: after the host submits parent/candidate observations, `FutureUseObserver` produces paired comparisons and the extension emits result events without adding persistence or binding state.
- [计划] [eval] [test] 增加 held-out、hazard、unknown 和未配对观察的 fake-facts 测试，并运行扩展与 PAOS 全量回归。
- [Planned] [Eval] [Test] Add fake-facts coverage for held-out, hazard, unknown, and unpaired observations, then run extension and full PAOS regressions.

### 预期影响文件 / Planned Files

- `extensions/evolution/evolution/api.py`
- `extensions/evolution/evolution/observation.py`
- `extensions/evolution/evolution/plugin.py`
- `extensions/evolution/evolution/__init__.py`
- `extensions/evolution/tests/test_evolution_extension.py`

### 实际结果 / Actual Results

- [完成] [policy] [feat] `extensions/evolution/evolution/observation.py:L145-L186` 新增 `FutureUseObservationBuilder`；它要求宿主显式传入 binding_candidate_id、binding_role、comparison_key、split 和 outcome，只有 candidate 绑定 ID 与 proposal 一致时才生成 candidate observation，避免把“候选存在”误当成“候选已使用”。
- [Completed] [Policy] [Feat] `extensions/evolution/evolution/observation.py:L145-L186` adds `FutureUseObservationBuilder`; it requires host-supplied binding_candidate_id, binding_role, comparison_key, split, and outcome, and emits a candidate observation only when the binding ID matches, preventing candidate existence from being mistaken for candidate use.
- [完成] [policy] [feat] `extensions/evolution/evolution/__init__.py:L24-L37` 导出 builder；PAOS SkillActivation 的 `binding_candidate_id` 可直接作为宿主输入，不需要 extension 查询 Core。
- [Completed] [Policy] [Feat] `extensions/evolution/evolution/__init__.py:L24-L37` exports the builder; PAOS SkillActivation.binding_candidate_id can be passed directly by the host without extension-side Core queries.
- [完成] [eval] [test] `extensions/evolution/tests/test_evolution_extension.py:L517-L548` 增加 candidate binding 一致性与 parent 误带 candidate ID 的测试；扩展测试 `65 passed`，PAOS 全量 `253 passed`，Ruff、compileall、git diff --check 通过。
- [Completed] [Eval] [Test] `extensions/evolution/tests/test_evolution_extension.py:L517-L548` adds candidate-binding consistency and parent-ID rejection tests; extension tests report `65 passed`, full PAOS `253 passed`, with Ruff, compileall, and git diff --check passing.

### 六维验收 / Six-Dimension Acceptance

1. 架构集成：通过。builder 位于 extension observation 层，宿主仍决定何时调用及提供哪些事实。 / Passed. The builder lives in the extension observation layer while the host controls invocation and facts.
2. 失败路径：通过。候选 ID 不一致、parent 携带 candidate ID、空比较键均显式拒绝。 / Passed. Mismatched candidate IDs, parent IDs, and empty comparison keys are rejected explicitly.
3. 权威与安全边界：通过。builder 不判断 promotion，不修改 Skill，不触发执行。 / Passed. The builder does not infer promotion, modify Skills, or trigger execution.
4. 配置与复现：通过。binding role、split、comparison key 和 outcome 都是显式输入，可审计、可复现。 / Passed. Binding role, split, comparison key, and outcome are explicit and reproducible.
5. 可维护性：通过。复用现有 FutureUseObservation/FutureUseObserver，无新 store 或 scheduler。 / Passed. Existing Future-use contracts are reused without a new store or scheduler.
6. 可观察性与 Anti-OverDefense：通过。只加入与错误绑定场景直接对应的校验，没有新增 hash、baseline 或 gate。 / Passed. Validation is limited to the concrete binding-error scenario; no new hash, baseline, or gate was added.

## v7.3.6 (2026-09-09 00:10) - codex

### 预期修改 / Planned Changes

- [计划] [policy] [feat] 增加 provider-neutral `FutureUseObservationBuilder`，要求宿主显式提供 candidate binding、comparison key、split 与 verification outcome；builder 只生成可比较 observation，不查询 PAOS 状态。
- [Planned] [Policy] [Feat] Add a provider-neutral `FutureUseObservationBuilder` that requires the host to explicitly provide candidate binding, comparison key, split, and verification outcome; it only creates comparable observations and never queries PAOS state.
- [计划] [eval] [test] 增加 parent/candidate binding、未激活 candidate、未知结果和显式 split 的 fake-facts 测试。
- [Planned] [Eval] [Test] Add fake-facts tests for parent/candidate binding, inactive candidates, unknown outcomes, and explicit splits.

### 预期影响文件 / Planned Files

- `extensions/evolution/evolution/observation.py`
- `extensions/evolution/evolution/__init__.py`
- `extensions/evolution/tests/test_evolution_extension.py`

### 实际结果 / Actual Results

- [完成] [policy] [feat] `extensions/evolution/evolution/plugin.py:L20-L30,L154-L194` 增加 `observe_future_use` 回流接口：宿主提交 parent/candidate observations 后，扩展调用 `FutureUseObserver` 生成 paired comparison，并通过事件或可选 sink 输出结果；未配对、unknown 或 split 不一致时保持 incomplete，不伪造 forward-transfer。
- [Completed] [Policy] [Feat] `extensions/evolution/evolution/plugin.py:L20-L30,L154-L194` adds `observe_future_use`: after the host submits parent/candidate observations, the extension uses `FutureUseObserver` to produce a paired comparison and emits it through events or an optional sink; unpaired, unknown, or split-mismatched inputs remain incomplete and never fabricate forward transfer.
- [完成] [policy] [feat] `extensions/evolution/evolution/__init__.py:L24-L35` 导出 Future-use observation/comparison 类型，保持扩展 API 可发现。
- [Completed] [Policy] [Feat] `extensions/evolution/evolution/__init__.py:L24-L35` exports Future-use observation and comparison types for discoverable extension APIs.
- [完成] [eval] [test] `extensions/evolution/tests/test_evolution_extension.py:L464-L515` 增加 held-out 成功增益和未配对观察测试；扩展测试 `63 passed`，PAOS 全量 `253 passed`，Ruff、compileall、git diff --check 通过。
- [Completed] [Eval] [Test] `extensions/evolution/tests/test_evolution_extension.py:L464-L515` adds held-out gain and unpaired-observation tests; extension tests report `63 passed`, full PAOS `253 passed`, with Ruff, compileall, and git diff --check passing.

### 六维验收 / Six-Dimension Acceptance

1. 架构集成：通过。Future-use 只作为宿主回流 seam，不拥有 candidate binding 或持久化状态。 / Passed. Future-use is a host feedback seam and owns neither candidate binding nor persistence.
2. 失败路径：通过。缺少 parent/candidate pair、unknown outcome、split mismatch 和 sink failure 都显式报告。 / Passed. Missing pairs, unknown outcomes, split mismatches, and sink failures are explicit.
3. 权威与安全边界：通过。接口只接收已完成的观察，不触发执行、激活或 promotion。 / Passed. The interface consumes completed observations only and triggers no execution, activation, or promotion.
4. 配置与复现：通过。comparison_key、method_id、candidate_id、split 和 binding_role 由宿主提供并保留。 / Passed. comparison_key, method_id, candidate_id, split, and binding_role remain host-provided and preserved.
5. 可维护性：通过。复用现有 `FutureUseObserver`，未新增 store、scheduler 或后台线程。 / Passed. Existing `FutureUseObserver` is reused without a new store, scheduler, or background thread.
6. 可观察性与 Anti-OverDefense：通过。输出 paired_tasks、success_gain 和 forward_transfer_gain 事件，不新增 speculative gate。 / Passed. paired_tasks, success_gain, and forward_transfer_gain are observable without speculative gates.

## v7.3.2 (2026-09-08 22:10) - codex

### 预期修改 / Planned Changes

- [计划] [model] [fix] 修正 GraspGen 接触几何资格计算：将 RoboTwin reset 姿态下的 world 顶点通过 provider 导出的 reference hand pose 投影到候选 robot-target pose，再评估桌面净空；不修改 GraspGen depth、不增加固定世界偏移。
- [Planned] [Model] [Fix] Correct GraspGen contact-geometry qualification by projecting RoboTwin reset-world vertices through the provider-exported reference hand pose into the candidate robot-target pose before evaluating table clearance; do not change GraspGen depth or add a fixed world offset.
- [计划] [eval] [test] 增加 reference pose 缺失、目标姿态旋转/平移和 scene revision 失配测试；重新运行 no-motion geometry worker、专项与 RoboTwin adapter 回归，不执行 scene.step、Gateway、Dora、Action 或硬件。
- [Planned] [Eval] [Test] Add regressions for missing reference pose, target-pose rotation/translation, and stale scene revision; rerun the no-motion geometry worker, focused tests, and the RoboTwin adapter suite without scene.step, Gateway, Dora, Action, or hardware.

### 预期影响文件 / Planned Files

- `examples/forge-adapters/robotwin20/runtime/robotwin_grasp_contact_geometry_worker.py`
- `examples/forge-adapters/robotwin20/src/robotwin20_adapter/grasp_postprocessing.py`
- `examples/forge-adapters/robotwin20/tests/test_grasp_postprocessing.py`
- `examples/forge-adapters/robotwin20/tests/test_grasp_contact_geometry_worker.py`

### 实际结果 / Actual Results

- [完成] [model] [fix] `robotwin_grasp_contact_geometry_worker.py:L43-L56` 为每个 Panda arm 输出 `reference_hand_pose`（world frame、position、orientation），并使用非弃用的 `get_entity_pose()` 读取 reset 姿态。
- [Completed] [Model] [Fix] `robotwin_grasp_contact_geometry_worker.py:L43-L56` now emits a per-arm `reference_hand_pose` (world frame, position, orientation) and reads the reset pose through non-deprecated `get_entity_pose()`.
- [完成] [model] [fix] `grasp_postprocessing.py:L40-L87,L231-L240` 增加 quaternion 刚体变换桥接：reset-world link vertices → reference-hand local → candidate robot-target world；缺失或失配 reference pose 显式失败。
- [Completed] [Model] [Fix] `grasp_postprocessing.py:L40-L87,L231-L240` adds quaternion rigid-transform bridging from reset-world link vertices through reference-hand local coordinates into the candidate robot-target world pose, with explicit failure for missing or mismatched reference poses.
- [完成] [eval] [test] `test_grasp_postprocessing.py:L141-L185` 与 `test_grasp_contact_geometry_worker.py:L55-L61` 覆盖目标位姿变换、缺 reference pose 和 worker artifact 字段；专项 `12 passed`，adapter 全量 `326 passed, 1 skipped`，Ruff、compileall、diff check 通过。
- [Completed] [Eval] [Test] `test_grasp_postprocessing.py:L141-L185` and `test_grasp_contact_geometry_worker.py:L55-L61` cover target-pose projection, missing reference pose, and worker artifact fields; focused tests pass `12`, the adapter suite passes `326 passed, 1 skipped`, and Ruff, compileall, and diff check pass.

### 六维验收 / Six-Dimension Acceptance

- 架构集成 / Architecture: 通过；投影仍位于 RoboTwin provider adapter，planning、task lifecycle、Gateway 和执行面未改变。
- 失败路径 / Failure paths: 通过；缺 reference pose、错误 frame、stale scene、无效 quaternion 和无可行 variant 均显式失败或 unavailable。
- 权威边界 / Authority: 通过；GraspGen 原始 pose/depth 保持不变，投影只产生 no-motion qualification，不授予 motion authority。
- 配置 / Configuration: 通过；没有新增固定 Z 偏移或全局 depth，backoff 仍由调用方提供的候选集合决定。
- 可维护性 / Maintainability: 通过；纯几何变换与 RoboTwin snapshot 分离，复用现有 pose/vertex helpers，测试覆盖真实接口字段。
- 防止过度防御编程 / Anti-overdefense: 通过；新增校验仅对应已确认的 reset-vs-target 坐标错误，不引入额外 hash、冻结 contract、store、scheduler 或无关 gate。

### 真实 no-motion 证据 / Real No-Motion Evidence

- Geometry artifact: `/home/yanxu/robotwin20-runtime/artifacts/paos-grasp-contact-v7.3.2-20260908T2240Z/contact-geometry.json`
- Qualification artifact: `/home/yanxu/robotwin20-runtime/artifacts/paos-grasp-contact-v7.3.2-20260908T2240Z/qualification.json`
- 结果 / Result: `status=qualified`, `selected_backoff_m=0.0`, `support_clearance_m=0.11025242347559439`, `motion_authorized=false`。
- 仅执行 reset 与几何读取，未调用 `scene.step()`、Gateway、Dora、Action 或硬件。
- Reset and geometry extraction only; no `scene.step()`, Gateway, Dora, Action, or hardware was invoked.

### Git 提交 / Git Commit

- Implementation commit: `6dd4ff5`
- Branch: `feature/planning-loop`
- 时间 / Time: 2026-09-08 (Asia/Shanghai)

## v7.3.4 (2026-09-08 23:50) - codex

### 实际结果 / Actual Results

- [完成] [eval] [exp] 基于 v7.3.2 几何投影修复物化新的 GraspGen route package：`/home/yanxu/robotwin20-runtime/artifacts/paos-route-v7.3.2-graspgen-contact-20260908T2345Z/`。
- [Completed] [Eval] [Exp] Materialized a new GraspGen route package from the v7.3.2 geometry-projection fix at `/home/yanxu/robotwin20-runtime/artifacts/paos-route-v7.3.2-graspgen-contact-20260908T2345Z/`.
- [完成] [eval] [test] 新 package 绑定 candidate `candidate://block-green-1/0`、scene revision `blocks_ranking_rgb-0-1`、最新 simulation probe worker `6534f093…`，并通过 `validate_route_request()`；`motion_authorized=false`，状态为 `awaiting_human_review`。
- [Completed] [Eval] [Test] The package binds candidate `candidate://block-green-1/0`, scene revision `blocks_ranking_rgb-0-1`, and the current simulation-probe worker `6534f093…`; `validate_route_request()` passes, `motion_authorized=false`, and status is `awaiting_human_review`.
- [完成] [safety] [policy] 未执行 `scene.step()`、Gateway、Dora、Action、benchmark motion 或硬件；历史 approval 未复用。
- [Completed] [Safety] [Policy] No `scene.step()`, Gateway, Dora, Action, benchmark motion, or hardware was executed; no historical approval was reused.

### Package Binding / 包绑定

- request id: `franka-blocks-green0-graspgen-contact-v732-20260908`
- route geometry digest: `61e9f7fbe1d61aadc5be9b089eb3afeb31d1e11430288d01ee9bce7d463c1723`
- source manifest digest: `6ba15e3b8d9a2bec1721990d46175421e8348ecbbe5c60230bbd881a0d61ac1c`
- required decision: `approved_independent_simulation_probe`

### 下一步 / Next Step

等待新的人工 `simulation-only` approval。批准后才可运行完整双视角 RoboTwin probe；在批准前不执行任何场景步进。

Await fresh human `simulation-only` approval. Only after approval may the complete dual-view RoboTwin probe run; no scene stepping occurs before approval.

## v7.3.6 (2026-09-09 00:05) - codex

### 预期修改 / Planned Changes

- [计划] [eval] [exp] 物化用户批准的 `franka-blocks-green0-graspgen-contact-v732-20260908` simulation-only approval，并运行绑定最新 route/source-manifest/worker 的完整 RoboTwin probe。
- [Planned] [Eval] [Exp] Materialize the user-approved simulation-only approval for `franka-blocks-green0-graspgen-contact-v732-20260908` and run the complete RoboTwin probe bound to the current route, source manifest, and worker.
- [计划] [safety] [test] 保存 attached、lift、transport、descent、release、retreat、contact、stop/reset、semantic-verification 以及 head-camera/observer-camera 双视角视频 evidence；不调用 Gateway、Dora、Action 或硬件。
- [Planned] [Safety] [Test] Persist attached, lift, transport, descent, release, retreat, contact, stop/reset, semantic-verification, and head-camera/observer-camera dual-view video evidence; do not invoke Gateway, Dora, Action, or hardware.

### 实际结果 / Actual Results

- [完成] [eval] [exp] 用户批准已物化为 `/home/yanxu/robotwin20-runtime/artifacts/paos-route-v7.3.2-graspgen-contact-20260908T2345Z/probe/approval.json`，审批记录绑定 route digest `61e9f7fbe1d61aadc5be9b089eb3afeb31d1e11430288d01ee9bce7d463c1723` 与 worker digest `6534f093…`。
- [Completed] [Eval] [Exp] The user approval was materialized at `/home/yanxu/robotwin20-runtime/artifacts/paos-route-v7.3.2-graspgen-contact-20260908T2345Z/probe/approval.json`, bound to route digest `61e9f7fbe1d61aadc5be9b089eb3afeb31d1e11430288d01ee9bce7d463c1723` and worker digest `6534f093…`.
- [完成] [eval] [test] 完整 probe 返回 `status=unavailable`：失败发生在 `preflight`，左右臂首个 `robot_target_pose` planner segment 均失败；`simulator_steps=0`、`contact_trace=[]`、`world_change_completed=false`，未生成双视角视频或语义结果。
- [Completed] [Eval] [Test] The complete probe returned `status=unavailable`: both arms failed the first `robot_target_pose` planner segment during `preflight`; `simulator_steps=0`, `contact_trace=[]`, and `world_change_completed=false`, so no dual-view video or semantic result was produced.
- [完成] [policy] [diagnosis] 该失败不证明夹爪接触桌面；因为未执行任何 `scene.step()`。独立 no-motion planner 对照显示：加载同一 collision world 后 right arm target 可返回 `Success`，left arm 为 `Fail`，说明当前失败需要继续定位 RoboTwin/Curobo preflight 的非确定性或双臂 planner 状态，不应继续调 GraspGen depth/backoff。
- [Completed] [Policy] [Diagnosis] This failure does not prove gripper-table contact because no `scene.step()` ran. An independent no-motion planner check with the same collision world returned `Success` for the right-arm target and `Fail` for the left arm, so the next investigation is RoboTwin/Curobo preflight state or nondeterminism, not GraspGen depth/backoff tuning.

### 六维验收 / Six-Dimension Acceptance

1. 架构集成：通过；审批和 probe 仍在 RoboTwin provider/runtime seam，未改变 PAOS planning 或 Gateway ownership。 / Passed; approval and probe remain in the RoboTwin provider/runtime seam.
2. 失败路径：通过；preflight 失败生成 failure、before/after snapshot、stop/reset 状态，且未把失败解释为成功。 / Passed; preflight failure produced failure, snapshots, and stop/reset state without being reported as success.
3. 权威边界：通过；probe 仅在人工批准后运行，未触发 Gateway、Dora、Action 或硬件。 / Passed; the probe ran only after human approval and touched no Gateway, Dora, Action, or hardware.
4. 配置与复现：通过；route、candidate、scene revision、source manifest 和 worker 均绑定到同一 package。 / Passed; route, candidate, scene revision, source manifest, and worker are bound to one package.
5. 可维护性：部分通过；失败 artifact 完整，但 probe 尚未证明 attached/lift/transport/descent/release/retreat 闭环，需要后续针对 preflight 状态定位。 / Partially passed; failure evidence is complete, but the full route lifecycle remains unproven pending preflight diagnosis.
6. 防止过度防御编程：通过；本轮未新增代码、哈希或额外 gate，仅记录真实 probe 结果与已有证据。 / Passed; no code, hashes, or extra gates were added in this run.

### Preflight 根因诊断 / Preflight Root-Cause Diagnosis

- [完成] [eval] [diagnosis] 在不执行 `scene.step()` 的独立复现中确认：worker 注入真实 table pose 后，Curobo 左右臂首个 target 均返回 `Fail`；不注入该 pose 时，右臂同一 target 返回 `Success`。
- [Completed] [Eval] [Diagnosis] In an independent no-motion reproduction, injecting the worker's real table pose made both Curobo arms return `Fail` for the first target; without that pose, the right arm returned `Success` for the same target.
- [完成] [policy] [diagnosis] 当前 provider 原生 table world config 为 `[-0.65,0,-0.01]`，而 `_native_table_geometry_for_planner()` 将 SAPIEN world table center `[0,0,0.715]` 投影为左 `[0.65,-0.4,-0.035]`、右 `[0.65,0.4,-0.035]`；该差异是下一步必须验证的 frame/pose 语义问题。
- [Completed] [Policy] RoboTwin's native table world config is `[-0.65,0,-0.01]`, while `_native_table_geometry_for_planner()` projects the SAPIEN world table center `[0,0,0.715]` to left `[0.65,-0.4,-0.035]` and right `[0.65,0.4,-0.035]`; this frame/pose semantic discrepancy is the next provider fix target.
- [完成] [safety] [policy] 不修改 GraspGen depth、backoff、桌面碰撞规则或 motion authority；在 table frame 语义修正并通过 no-motion planner 对照前，不重新运行完整 probe。
- [Completed] [Safety] [Policy] GraspGen depth, backoff, table collision rules, and motion authority remain unchanged; the complete probe will not be rerun until table-frame semantics are corrected and requalified no-motion.

### 实际结果 / Actual Results

- [完成] [policy] [fix] `robotwin_curobo_world_port.py:L105-L145` 修正 table projection：实测 SAPIEN table 的中心和尺寸仍投影到 planner frame，但 orientation 复用 Curobo 原生 planner table orientation，避免将 support slab 的薄轴旋转到 planner vertical 轴。
- [Completed] [Policy] [Fix] `robotwin_curobo_world_port.py:L105-L145` now projects the measured SAPIEN table center and dimensions while preserving Curobo's native planner-table orientation, preventing the support slab's thin axis from being rotated into planner vertical.
- [完成] [eval] [test] `test_curobo_world_port.py:L89-L124` 增加 native orientation 保留回归；Curobo world-port 专项 `12 passed`，RoboTwin adapter 全量 `327 passed, 1 skipped`，Ruff、compileall、diff check 通过。
- [Completed] [Eval] [Test] `test_curobo_world_port.py:L89-L124` adds native-orientation preservation coverage; the world-port focus passes `12`, the RoboTwin adapter suite passes `327 passed, 1 skipped`, and Ruff, compileall, and diff check pass.
- [完成] [eval] [diagnosis] 真实 RoboTwin no-motion 检查确认 provider 输出 table pose orientation 为 `[1,0,0,0]`，中心分别为 left `[0.65,-0.4,-0.035]`、right `[0.65,0.4,-0.035]`；未执行 `scene.step()`。
- [Completed] [Eval] [Diagnosis] A real RoboTwin no-motion check confirms the provider now emits table orientation `[1,0,0,0]` with centers left `[0.65,-0.4,-0.035]` and right `[0.65,0.4,-0.035]`; no `scene.step()` was executed.

### Follow-up Evidence / 后续证据

- [完成] [eval] [diagnosis] 保持 Curobo 原生 table orientation 后，使用真实 SAPIEN table center/dimensions 的 right target 仍返回 `Fail`；使用 RoboTwin 原生 table pose 时同一 target 可稳定返回 `Success`。因此剩余问题是 table center/base-frame 语义或真实桌面导致的可达性差异，不是 orientation 轴交换 alone。
- [Completed] [Eval] [Diagnosis] With Curobo's native table orientation preserved, the right target still returns `Fail` when using the measured SAPIEN table center/dimensions, while the same target is stably `Success` with RoboTwin's native table pose. The remaining issue is table-center/base-frame semantics or true-table reachability, not orientation alone.
- [完成] [policy] [safety] 不将原生错误 table pose 作为修复，不放宽桌面碰撞，不修改 GraspGen depth/backoff；在 provider frame qualification 前不重新运行完整 probe。
- [Completed] [Policy] [Safety] The native but incorrect table pose is not reinstated as a fix, table collision is not relaxed, and GraspGen depth/backoff remain unchanged; the complete probe will not rerun before provider frame qualification.

### 实际结果 / Actual Results

- [完成] [policy] [fix] `robotwin_curobo_world_port.py:L136-L151` 将 table half-extents 通过 robot base rotation 的绝对矩阵投影到 Curobo planner frame，并保留 planner-native table orientation；未引入固定尺寸或坐标常量。
- [Completed] [Policy] [Fix] `robotwin_curobo_world_port.py:L136-L151` projects table half-extents into the Curobo planner frame through the absolute robot-base rotation matrix while preserving the planner-native table orientation; no fixed dimensions or coordinate constants were introduced.
- [完成] [eval] [test] `test_curobo_world_port.py:L117-L140` 增加旋转 base 的 half-extents 投影测试；world-port 专项 `13 passed`，RoboTwin adapter 全量 `328 passed, 1 skipped`，Ruff、compileall、diff check 通过。
- [Completed] [Eval] [Test] `test_curobo_world_port.py:L117-L140` adds rotated-base half-extents projection coverage; world-port focus passes `13`, the RoboTwin adapter suite passes `328 passed, 1 skipped`, and Ruff, compileall, and diff check pass.
- [完成] [eval] [diagnosis] 真实 no-motion 对照显示，即使 table center、尺寸和 orientation 均按 provider frame 投影，right target 在完整物理 table 模型下仍返回 `Fail`；原生错误 table pose 才返回 `Success`。这不是 GraspGen depth 问题，当前 route 不能声称已完成物理桌面防碰撞。
- [Completed] [Eval] [Diagnosis] Real no-motion comparison shows that the right target still returns `Fail` with the table center, dimensions, and orientation projected into the provider frame; `Success` occurs only with RoboTwin's incorrect native table pose. This is not a GraspGen depth issue, and the route cannot yet claim physical-table collision safety.

### Frame Matrix Evidence / Frame 矩阵证据

- [完成] [eval] [diagnosis] 固定 right target、远离其它方块并仅做 no-motion planner 对照：`native pose + native dims = Success`、`actual center + native dims = Success`、`native pose + actual dims = Success`、`actual center + actual dims = Fail`。
- [Completed] [Eval] [Diagnosis] With the right target fixed, other blocks moved out of range, and no-motion planning only: `native pose + native dims = Success`, `actual center + native dims = Success`, `native pose + actual dims = Success`, and `actual center + actual dims = Fail`.
- [完成] [policy] [diagnosis] 证据表明失败由“真实 table center 与真实 dimensions 的组合”触发，而非单独 orientation 或单独 center；当前不能把它归因于 GraspGen，也不能用原生错误 table pose 绕过。
- [Completed] [Policy] [Diagnosis] The evidence shows the failure is triggered by the combination of the measured table center and measured dimensions, not orientation or center alone; it is not a GraspGen issue and cannot be bypassed with the native incorrect table pose.
- [完成] [safety] [policy] 保持 fail-closed：在确认 Curobo planner frame 与 RoboTwin 物理 table 的一致表达前，不重新运行完整 probe，不放宽碰撞，不修改 route target。
- [Completed] [Safety] [Policy] Fail-closed behavior remains: do not rerun the complete probe, relax collision, or modify the route target until Curobo planner-frame semantics are qualified against the physical RoboTwin table.

## v7.3.0 (2026-09-08 19:06) - codex

### 预期修改 / Planned Changes

- [计划] [model] [feat] 增加 RoboTwin adapter-owned GraspGen contact-depth 后处理：保留原始 candidate/depth，按真实 Panda hand/finger 几何、目标物和支撑平面评估有限 ingress-backoff 变体；不改 PAOS Core、全局 depth 或世界 Z 偏移。
- [Planned] [Model] [Feat] Add RoboTwin adapter-owned GraspGen contact-depth post-processing that preserves the original candidate/depth and evaluates finite ingress-backoff variants against real Panda geometry, object, and support plane; do not change PAOS Core, global depth, or world-Z offsets.
- [计划] [eval] [test] 增加 no-motion geometry worker、route materializer 可选 qualification 接口和专项测试，完成六维验收；不运行 scene.step、Gateway、Dora、Action 或硬件。
- [Planned] [Eval] [Test] Add the no-motion geometry worker, optional route-materializer qualification interface, and focused tests, followed by six-dimension acceptance without scene.step, Gateway, Dora, Action, or hardware.

### 实际结果 / Actual Results

- [完成] [model] [feat] `grasp_postprocessing.py:L1-L201` 提供有限 backoff、支撑净空、pinch containment、geometry-artifact bridge 和显式 unavailable 结果；`robotwin_grasp_contact_geometry_worker.py:L1-L98` 提取双臂真实 Panda 碰撞顶点与 table top，保持 `motion_authorized=false`。
- [Completed] [Model] [Feat] `grasp_postprocessing.py:L1-L201` provides finite backoff, support clearance, pinch containment, geometry-artifact bridging, and explicit unavailable results; `robotwin_grasp_contact_geometry_worker.py:L1-L98` extracts real dual-arm Panda vertices and table top while retaining `motion_authorized=false`.
- [完成] [model] [feat] `materialize_complete_route.py:L635-L685,L671-L683,L796-L810` 在人工审批前按显式 qualification artifact 应用 execution variant并写入 candidate provenance；未提供时 baseline route 不变。
- [Completed] [Model] [Feat] `materialize_complete_route.py:L635-L685,L671-L683,L796-L810` applies an execution variant before human approval only from an explicit qualification artifact and records it in candidate provenance; omission preserves the baseline route.
- [完成] [docs] [docs] `docs/forge/GRASPGEN_CONTACT_DEPTH_POSTPROCESSING.md:L1-L67` 记录 depth 语义、坐标、失败和 PAOS ownership 边界。
- [Completed] [Docs] [Docs] `docs/forge/GRASPGEN_CONTACT_DEPTH_POSTPROCESSING.md:L1-L67` records depth semantics, frames, failure behavior, and PAOS ownership boundaries.
- [完成] [eval] [test] `test_grasp_postprocessing.py:L1-L135` 与 `test_grasp_contact_geometry_worker.py:L1-L81` 两个专项测试 `10 passed`；正确依赖路径下 RoboTwin adapter 全量 `323 passed, 1 skipped`；Ruff、compileall、git diff --check 通过；未执行仿真动作。
- [Completed] [Eval] [Test] The two focused suites pass `10`; the RoboTwin adapter suite passes `323 passed, 1 skipped` with the correct dependency path; Ruff, compileall, and git diff --check pass; no simulator motion executed.

### 六维验收 / Six-Dimension Acceptance

- 架构集成 / Architecture: 通过；实现位于 provider adapter/runtime/materializer，不接管 PAOS lifecycle、planning、Gateway 或执行状态机。
- 失败路径 / Failure paths: 通过；无效 geometry、stale revision、缺 link、无可行 variant、缺 qualification ref 均显式失败或 unavailable。
- 权威边界 / Authority: 通过；原始 GraspGen proposal/depth 保持事实，variant 仅用于审批前 route 物化，provider artifact 不授予 motion authority。
- 配置 / Configuration: 通过；backoff 由调用方声明的有限集合提供，不新增固定世界偏移或修改全局 depth。
- 可维护性 / Maintainability: 通过；纯计算与 RoboTwin runtime 分离，复用现有碰撞顶点/table helper，提供文档与测试。
- 防止过度防御编程 / Anti-overdefense: 通过；仅增加当前几何资格问题所需的约束，不新增无关 hash、冻结 contract、重复 store 或额外 gate。

### Git 提交 / Git Commit

- Commit: `f7eea46`; Branch: `feature/planning-loop`; 时间 / Time: 2026-09-08 (Asia/Shanghai)

## v7.3.1 (2026-09-08 20:05) - codex

### 预期修改 / Planned Changes

- [计划] [evolution] [fix] 按 code review 修复 EvoPhy 真实闭环缺口：校验 transition predicate 绑定、拒绝无证据候选、处理低置信度 owner、修正 TRACE 排序与 joint-cause 关联语义。
- [Planned] [Evolution] [Fix] Fix EvoPhy closure gaps found in code review: validate transition-predicate binding, reject evidence-free candidates, handle low-confidence owners, and correct TRACE ranking and joint-cause association semantics.
- [计划] [evolution] [feat] 增加 CandidateProposal 到 PAOS 既有 SkillCandidate 生命周期的显式 adapter，并提供基于现有 composition root 的可选装载路径；不新增 store、scheduler、Verifier 或运动权限。
- [Planned] [Evolution] [Feat] Add an explicit CandidateProposal-to-PAOS SkillCandidate lifecycle adapter and an opt-in composition-root loading path using existing PAOS stores and callbacks; add no store, scheduler, Verifier, or motion authority.
- [计划] [eval] [test] 增加 verification-off、重复/未配对评测、失败重放、真实 settlement adapter 和四类 patch selector 的 fake-facts 回归，并完成六维验收。
- [Planned] [Eval] [Test] Add fake-facts regressions for verification-off isolation, duplicate/unpaired evaluation, failure replay, the settlement adapter, and four-surface patch selection, then complete six-dimension acceptance.

### 预期影响文件 / Planned Files

- `extensions/evolution/evolution/api.py`
- `extensions/evolution/evolution/projection.py`
- `extensions/evolution/evolution/trace.py`
- `extensions/evolution/evolution/methods/evophy/method.py`
- `extensions/evolution/evolution/plugin.py`
- `extensions/evolution/evolution/lifecycle.py`
- `extensions/evolution/evolution/adapter.py`
- `extensions/evolution/tests/`
- `PhyAgentOS/agent/experience/coordinator.py`
- `PhyAgentOS/agent/experience/source.py`
- `PhyAgentOS/cli/commands.py`
- `tests/`
- `CHANGELOG.md`
- `changelog/2026-09_part4.md`

### 实际结果 / Actual Results

- [完成] [evolution] [fix] `extensions/evolution/evolution/api.py:L158-L184` 现在拒绝未声明的 transition predicate；`evolution/methods/evophy/method.py:L21-L83` 要求 evidence references，按 owner 生成 expectation/observation/decision/recovery 四类局部 patch，并对有下游影响的归因生成 recovery patch。
- [Completed] [Evolution] [Fix] `extensions/evolution/evolution/api.py:L158-L184` now rejects undeclared transition predicates; `evolution/methods/evophy/method.py:L21-L83` requires evidence references, generates expectation/observation/decision/recovery local patches by owner, and emits a recovery patch when attribution has downstream impact.
- [完成] [evolution] [fix] `settlement.py:L21-L24,L120-L129,L190-L210` 与 `trace.py:L12-L19,L80-L90` 增加最低 owner confidence；TRACE 默认保留 earliest-observable 顺序，同时通过 `score_primary` 显式开启 score-first 排序；joint cause 仅由共享 evidence reference 建立；unknown predicate 带 transition identity。
- [Completed] [Evolution] [Fix] `settlement.py:L21-L24,L120-L129,L190-L210` and `trace.py:L12-L19,L80-L90` add minimum owner confidence; TRACE preserves earliest-observable order by default and exposes explicit `score_primary` score-first ordering; joint causes require shared evidence references; unknown predicates include transition identity.
- [完成] [evolution] [feat] `api.py:L267-L312` 增加 `trial_id`、`comparison_key` 与 `CandidateLifecyclePort`；`plugin.py:L25-L105` 支持宿主 lifecycle adapter，保留 candidate sink 观察模式；`evaluation.py:L49-L75` 对缺失 trial identity 的重复 receipt 也保持 hold。
- [Completed] [Evolution] [Feat] `api.py:L267-L312` adds `trial_id`, `comparison_key`, and `CandidateLifecyclePort`; `plugin.py:L25-L105` supports a host lifecycle adapter while retaining observation-only candidate sinks; `evaluation.py:L49-L75` holds duplicate receipts even when trial identity is absent.
- [完成] [agent] [fix] `TaskOutcomeEnvelope`/`TaskEpisode` 增加 verification mode 传播，extension 在 `verification_mode=off` 时跳过 episode；`FutureUseComparison.forward_transfer_gain` 在没有 held-out pair 时为 `None`，不再把 matched gain 冒充 forward transfer。
- [Completed] [Agent] [Fix] `TaskOutcomeEnvelope`/`TaskEpisode` now propagate verification mode, and the extension skips episodes with `verification_mode=off`; `FutureUseComparison.forward_transfer_gain` is `None` without a held-out pair, so matched gain is not reported as forward transfer.
- [完成] [eval] [test] 扩展测试从 48 增至 60 个；新增 predicate binding、evidence-free candidate、verification-off、owner confidence、四类 patch、重复 receipt、future-transfer 和 score-primary 回归。PAOS 全量回归为 `250 passed`。
- [Completed] [Eval] [Test] Extension coverage increased from 48 to 60 tests with predicate binding, evidence-free candidate, verification-off, owner confidence, four patch surfaces, duplicate receipts, future-transfer, and score-primary regressions. Full PAOS regression reports `250 passed`.
- [完成] [docs] [docs] 实际变更还包括 `PhyAgentOS/agent/experience/contracts.py:L139-L206`、`source.py:L237-L267` 与 `coordinator.py:L205-L235` 的 verification mode 传播；`extensions/evolution/README.md:L91-L108` 更新 CandidateLifecyclePort 使用说明；本地设计文档 `L717-L731` 记录复核后的剩余宿主接入缺口。
- [Completed] [Docs] [Docs] Additional changes include verification-mode propagation in `PhyAgentOS/agent/experience/contracts.py:L139-L206`, `source.py:L237-L267`, and `coordinator.py:L205-L235`; `extensions/evolution/README.md:L91-L108` documents CandidateLifecyclePort usage; the local design document `L717-L731` records the remaining host-integration gaps after review.

### 关键代码 Diff / Key Code Diff

#### [修改] `extensions/evolution/evolution/api.py:L166-L187`

**修改前 / Before:**
```python
if any(item.transition_id not in known for item in self.observations):
    raise ValueError("outcome observation references an unknown transition")
```

**修改后 / After:**
```python
predicates_by_transition = {
    item.transition_id: set(item.expected_predicates) for item in self.transitions
}
if any(
    item.predicate not in predicates_by_transition[item.transition_id]
    for item in self.observations
):
    raise ValueError("outcome observation references an unexpected predicate")
```

#### [修改] `extensions/evolution/evolution/methods/evophy/method.py:L37-L75`

**修改前 / Before:**
```python
proposals.append(local_observation_patch(...))
```

**修改后 / After:**
```python
if not evidence_refs:
    continue
surface = "recovery" if hypothesis.downstream_violations else owner_surface[hypothesis.owner]
proposals.append(local_skill_patch(..., patch=(PatchInstruction(surface=surface, ...),)))
```

**修改说明 / Change:** 候选必须带 evidence references，并根据归因 owner/下游影响选择四类受限 patch surface。

#### [修改] `extensions/evolution/evolution/plugin.py:L51-L105`

```python
if getattr(episode, "verification_mode", None) == "off":
    self._emit("episode_skipped", ...)
    return ()
submit = self.candidate_lifecycle.submit if self.candidate_lifecycle is not None else self.candidate_sink
```

**修改说明 / Change:** verification-off episode 不进入 EvoPhy；候选提交通过显式宿主 lifecycle port。

### 六维验收 / Six-Dimension Acceptance

1. **架构集成 / Architecture integration — 部分通过 / Partial**：extension 仍独立且 host lifecycle port 已定义；标准 CLI composition root 尚未自动发现并装载 `EvolutionExtension`，真实 PAOS 运行仍需显式注入。
2. **失败路径 / Failure behavior — 通过 / Pass**：新增非法 predicate、无证据、低置信度 owner、重复 receipt、verification-off 与 future-use 不可评估路径。
3. **权威与安全边界 / Authority and safety — 通过 / Pass**：候选仍是非权威 proposal，promotion、Skill binding、Verifier、Runtime 和运动权限保持宿主所有。
4. **配置与可复现性 / Configuration and reproducibility — 通过 / Pass**：阈值和排序策略外部化为 policy；receipt identity 和 future-use pairing 语义可复现；独立环境测试通过。
5. **可维护性 / Maintainability — 通过 / Pass**：没有新增 store、scheduler、Gateway 或全局状态；四类 patch 共用同一结构化 patch builder。
6. **可观察性与 Anti-OverDefense / Observability and Anti-OverDefense — 通过 / Pass**：保留 transition-scoped unknown、evidence refs、trial identity、candidate lifecycle 事件；未新增 speculative hash、冻结 contract 或额外 gate。

### 未解决项 / Remaining Items

- 标准 `paos agent`/gateway composition root 仍没有自动加载 extension wheel；需要宿主选择 projection ports 与 CandidateLifecyclePort 后才能启用，不能把当前手工注入测试写成生产闭环。
- `CandidateLifecyclePort` 当前是显式宿主协议，尚未提供 PAOS `SkillCandidate`/`WorkflowPolicyCandidate` 的具体 adapter；candidate promotion、真实 settlement adapter 和失败 episode replay 仍需下一轮实现。
- 因上述宿主装载与生命周期 adapter 尚未完成，本版本不宣称“完整自我进化闭环”或真实 benchmark 结果。

### 验证 / Validation

- [完成] `cd extensions/evolution && uv run ruff check evolution tests && PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 uv run python -m pytest -q`：`60 passed`。
- [Completed] `cd extensions/evolution && uv run ruff check evolution tests && PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 uv run python -m pytest -q`: `60 passed`.
- [完成] `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 uv run --extra dev --extra tui python -m pytest -q -p pytest_asyncio.plugin`：PAOS 全量 `250 passed`。
- [Completed] `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 uv run --extra dev --extra tui python -m pytest -q -p pytest_asyncio.plugin`: full PAOS suite `250 passed`.
- [完成] compileall、extension wheel build、`git diff --check` 通过；未启动 Gateway、Dora、simulation step、Action 或硬件。
- [Completed] compileall, extension wheel build, and `git diff --check` passed; no Gateway, Dora, simulation step, Action, or hardware was started.

## v7.2.0 (2026-09-08 19:00) - codex

### 预期修改 / Planned Changes

- [计划] [cli] [feat] 增加可选 Textual dashboard presentation layer，复用现有 AgentLoop、MessageBus、LongHorizonTaskController 和 AgentTaskCoordinator，提供对话、thinking/progress、任务状态、DAG 节点、事件和输入面板；不新增 scheduler/store/execution plane。
- [Planned] [CLI] [Feat] Add an optional Textual dashboard presentation layer reusing the existing AgentLoop, MessageBus, LongHorizonTaskController, and AgentTaskCoordinator, with conversation, thinking/progress, task status, DAG nodes, events, and input panels; add no scheduler/store/execution plane.
- [计划] [config] [feat] 将 Textual 作为可选 `tui` 依赖，通过 `paos agent --ui textual` 显式启用；默认 prompt_toolkit 路径保持兼容。
- [Planned] [Config] [Feat] Make Textual an optional `tui` dependency enabled explicitly via `paos agent --ui textual`; keep the prompt_toolkit path as the default compatibility mode.
- [计划] [eval] [test] 增加 dashboard 的无依赖 import、事件关联和任务/DAG 投影测试；不启动 Gateway、Dora、仿真或硬件。
- [Planned] [Eval] [Test] Add dependency-free import, event-correlation, and task/DAG projection tests for the dashboard without starting Gateway, Dora, simulator, or hardware.

### 实际结果 / Actual Results

- [完成] [cli] [feat] `PhyAgentOS/cli/textual_app.py:L1-L332` 新增可选 Textual dashboard；从同一 Coordinator 投影任务、active revision、DAG dependencies/settlements、完成进度、clarification 和持久化事件，复用同一 AgentLoop/MessageBus/LongHorizonTaskController，不创建 scheduler、store、PlanRevision 或 Gateway 路径。
- [Completed] [CLI] [Feat] `PhyAgentOS/cli/textual_app.py:L1-L332` adds the optional Textual dashboard; it projects tasks, active revisions, DAG dependencies/settlements, completion progress, clarifications, and persisted events from the same Coordinator, reuses the same AgentLoop/MessageBus/LongHorizonTaskController, and creates no scheduler, store, PlanRevision, or Gateway path.
- [完成] [cli] [feat] `PhyAgentOS/cli/commands.py:L900-L1003` 增加 `--ui textual` 显式入口，默认 `prompt_toolkit` 保持兼容；Textual 缺失时返回可操作的 optional-extra 错误，支持 `/task status|start|pause|resume|stop|replay TASK_ID`。
- [Completed] [CLI] [Feat] `PhyAgentOS/cli/commands.py:L900-L1003` adds the explicit `--ui textual` entry while keeping `prompt_toolkit` as the default; missing Textual reports an actionable optional-extra error, and the dashboard supports `/task status|start|pause|resume|stop|replay TASK_ID`.
- [完成] [config] [feat] `pyproject.toml:L68-L70` 增加 `tui` optional extra；`docs/forge/MANIPULATION_DAG_DEVELOPER_GUIDE.md:L297-L326` 与 `docs/forge/PLANNING_MODULE_DESIGN.md:L484-L524` 明确 presentation-only ownership boundary、安装和使用方式。
- [Completed] [Config] [Feat] `pyproject.toml:L68-L70` adds the `tui` optional extra; `docs/forge/MANIPULATION_DAG_DEVELOPER_GUIDE.md:L297-L326` and `docs/forge/PLANNING_MODULE_DESIGN.md:L484-L524` define the presentation-only ownership boundary, installation, and usage.
- [完成] [eval] [test] `tests/test_textual_app.py:L1-L192` 覆盖任务/DAG投影、持久化事件、缺少依赖错误、session 归一化、pause/clarification 展示、Textual headless 挂载、输入回到同一 MessageBus 以及 Ctrl+C 清理；规划/TUI/控制器专项 `41 passed, 1 warning`，Ruff、compileall、`git diff --check` 通过，未运行 Gateway、Dora、仿真或硬件。
- [Completed] [Eval] [Test] `tests/test_textual_app.py:L1-L192` covers task/DAG projection, persisted events, missing-dependency error, session normalization, pause/clarification rendering, headless Textual mounting, routing input to the same MessageBus, and Ctrl+C cleanup; the planning/TUI/controller focus suite passes `41 passed, 1 warning`, Ruff/compileall/`git diff --check` pass, and no Gateway, Dora, simulator, or hardware was run.

### 文件变更详情 / File Change Details

#### [修改 / Modified] `PhyAgentOS/cli/commands.py:L900-L1003`

**修改前 / Before:**

```python
agent_loop.set_long_horizon_controller(long_horizon_controller)
# The prompt_toolkit interactive presentation starts directly.
```

**修改后 / After:**

```python
agent_loop.set_long_horizon_controller(long_horizon_controller)
if ui == "textual":
    run_textual_app(
        agent_loop=agent_loop,
        bus=bus,
        controller=long_horizon_controller,
        coordinator=forge_task_coordinator,
        session_key=session_id,
    )
```

**修改说明 / Rationale:** Textual 仅接收已经创建的运行时对象，默认 prompt_toolkit 路径不变。Textual only receives already-created runtime objects, while the default prompt_toolkit path remains unchanged.

#### [新增 / Added] `PhyAgentOS/cli/textual_app.py:L18-L332`

```python
rows = _task_rows(coordinator, normalized_session_key)
await bus.publish_inbound(InboundMessage(...))
result = controller.replay(task_id)
```

**新增说明 / Addition:** dashboard 只读投影 Coordinator facts，并通过既有 MessageBus/Controller 发送输入和控制任务。The dashboard read-only projects Coordinator facts and sends input/task control only through the existing MessageBus and Controller.

### 六维验收 / Six-Dimension Review

- 架构集成 / Architecture: 通过；Textual 只替换 presentation layer，Controller、Coordinator、AgentLoop、PlanningLoopAdapter、MessageBus 仍是唯一运行时路径。
- 失败路径 / Failure paths: 通过；缺依赖、AgentLoop 异常、task command 异常、clarification、pause/resume/stop/replay 和退出清理均可见且不伪造 Action 终态。
- 权威边界 / Authority: 通过；Textual 不写 Store、不直接调用 Gateway、不创建 scheduler/PlanRevision、不授予运动权限。
- 配置与 provenance / Configuration and provenance: 通过；session、turn_id、task_id、revision_id、node settlement 和 Coordinator event 继续来自既有持久化/消息元数据。
- 可维护性 / Maintainability: 通过；可选依赖、默认路径兼容、只读投影和 headless smoke test 均位于 CLI presentation 边界。
- 防止过度防御编程 / Anti-overdefense: 通过；只增加真实生命周期/事件丢失场景所需的清理和投影，不新增 hash、重复 store、额外 gate 或 Textual 专用执行协议。

### 验证 / Validation

- `python -m pip install -e '.[tui]'`：Textual 0.89.1 安装成功。
- `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 pytest -q tests/test_textual_app.py tests/test_tui_long_horizon.py tests/test_long_horizon_controller.py tests/test_planning_loop.py tests/test_planning_task_integration.py`：`41 passed, 1 warning`。
- `ruff check PhyAgentOS/cli/textual_app.py tests/test_textual_app.py PhyAgentOS/cli/commands.py`、`python -m compileall -q PhyAgentOS tests/test_textual_app.py`、`git diff --check`：通过。
- 未运行 Gateway、Dora、Action、仿真器或硬件动作。

### Git 提交 / Git Commit

- Implementation commit: `be13b77`
- Branch: `feature/planning-loop`
- 时间 / Time: 2026-09-08 (Asia/Shanghai)
## v7.2.6 (2026-09-08 19:20) - codex

### 预期修改 / Planned Changes

- [计划] [cli] [fix] 修复 Textual Tasks / DAG 面板只显示当前会话持久化 AgentTask、而普通任务理解结果未落库导致面板为空的问题；复用现有 AgentTaskCoordinator，不新增任务存储或调度器。
- [Planned] [CLI] [Fix] Fix the Textual Tasks / DAG panel appearing empty when task-understanding results are not persisted as an AgentTask for the current session; reuse the existing AgentTaskCoordinator without adding a store or scheduler.
- [计划] [cli] [fix] 将 DAG 节点状态渲染为纯文本，避免 `[pending]` 被 Rich/Textual markup 解释后不可见，并补充任务创建失败与无任务状态的可见诊断。
- [Planned] [CLI] [Fix] Render DAG node statuses as plain text so `[pending]` is not consumed as Rich/Textual markup, and expose visible diagnostics for task-creation failures and the no-task state.

### 预期影响文件 / Planned Files

- `PhyAgentOS/cli/textual_app.py`
- `PhyAgentOS/agent/loop.py`
- `PhyAgentOS/agent/tools/forge_task.py`
- `tests/test_textual_app.py`
- `tests/test_task_creation_diagnostics.py`

### 实际结果 / Actual Results

- [完成] [cli] [fix] `PhyAgentOS/cli/textual_app.py:L240-L250` 在没有当前 session 持久化 AgentTask 时显示明确的 session 和落库提示；未改变 Coordinator 的任务所有权或跨 session 过滤。
- [Completed] [CLI] [Fix] `PhyAgentOS/cli/textual_app.py:L240-L250` now shows the exact session and persistence guidance when no AgentTask is projected; Coordinator ownership and cross-session filtering remain unchanged.
- [完成] [cli] [fix] `PhyAgentOS/cli/textual_app.py:L267-L273` 将节点状态从方括号形式改为 `status=<state> node=<id>` 纯文本，避免 Rich/Textual markup 吞掉 `pending`。
- [Completed] [CLI] [Fix] `PhyAgentOS/cli/textual_app.py:L267-L273` renders node states as `status=<state> node=<id>` plain text instead of bracket markup, keeping `pending` visible under Rich/Textual.
- [完成] [eval] [test] `tests/test_textual_app.py:L128-L207` 新增节点状态可见性和空任务诊断测试；Textual/planning 回归 `44 passed, 1 warning`，Ruff、compileall 与 `git diff --check` 通过。
- [Completed] [Eval] [Test] `tests/test_textual_app.py:L128-L207` adds node-status visibility and empty-task diagnostics tests; the Textual/planning regression reports `44 passed, 1 warning`, with Ruff, compileall, and `git diff --check` passing.

### 文件变更详情 / File Change Details

#### [修改 / Modified] `PhyAgentOS/cli/textual_app.py:L240-L250,L267-L273`

**修改前 / Before:**

```python
if not rows:
    self.query_one("#tasks", static_type).update("No AgentTask")
lines.append(f"  [{node['status']}] {node['node_id']} ...")
```

**修改后 / After:**

```python
if not rows:
    self.query_one("#tasks", static_type).update(
        f"No AgentTask for session {normalized_session_key}\n"
        "Submit a task that materializes an AgentTask + PlanGraph."
    )
lines.append(f"  status={node['status']} node={node['node_id']} ...")
```

**修改说明 / Rationale:** 空面板明确区分“当前 session 没有任务”和“DAG 读取失败”；节点状态不再依赖 Rich 方括号 markup。The empty panel distinguishes “no task for this session” from a DAG read failure, and node status no longer depends on Rich bracket markup.

### 设计边界 / Design Boundary

本次没有自动从普通聊天创建 AgentTask，也没有放宽 Forge Skill binding 要求。没有安装/运行 Forge Skill 时，系统仍会正确显示无任务提示；只有 Agent 沿既有 `forge_task_create` + primary Skill activation 路径成功落库后，Tasks / DAG 才会显示真实任务。

This change does not auto-create AgentTasks from ordinary chat and does not relax Forge Skill binding requirements. Without an installed/running Forge Skill, the dashboard correctly reports no task; a real Tasks / DAG row appears only after the existing `forge_task_create` plus primary Skill activation path persists a task.

### Git 提交 / Git Commit

- Implementation commit: `7e4e99e`
- Branch: `feature/planning-loop`
- 时间 / Time: 2026-09-08 (Asia/Shanghai)
## v7.3.3 (2026-09-08 22:20) - codex

### 预期修改 / Planned Changes

- [计划] [policy] [feat] 增加 PAOS 宿主侧 Evolution CandidateLifecycle 适配器，将扩展提案映射到现有 SkillCandidate/WorkflowPolicyCandidate 生命周期，不新增存储或晋级路径。
- [Planned] [Policy] [Feat] Add a PAOS-host Evolution CandidateLifecycle adapter that maps extension proposals into the existing SkillCandidate/WorkflowPolicyCandidate lifecycle without adding storage or promotion paths.
- [计划] [cli] [feat] 为标准 CLI/Gateway 增加可选 extension composition 工厂；未安装扩展时保持现有行为。
- [Planned] [CLI] [Feat] Add an optional extension composition factory to standard CLI/Gateway roots while preserving behavior when the extension is unavailable.
- [计划] [policy] [fix] 将 extension 回调纳入既有 evolution job 重放语义，覆盖首次失败、重启后重试与幂等候选提交。
- [Planned] [Policy] [Fix] Integrate extension callbacks with the existing evolution-job replay semantics, covering first-failure retry, restart replay, and idempotent candidate submission.

### 预期影响文件 / Planned Files

- `PhyAgentOS/agent/experience/evolution_extension_adapter.py`
- `PhyAgentOS/agent/experience/coordinator.py`
- `PhyAgentOS/agent/loop.py`
- `PhyAgentOS/cli/commands.py`
- `tests/test_evolution_extension_adapter.py`

## v7.3.4 (2026-09-08 22:45) - codex

### 预期修改 / Planned Changes

- [计划] [policy] [feat] 增加 provider-neutral settled outcome projection adapter，将宿主提供的 SettlementTarget 与 OutcomeEvidenceSample 交给 ConsequenceSettler，再回流为 EpisodeProjection observations；不轮询传感器、不调用 Tool、不硬编码机器人谓词。
- [Planned] [Policy] [Feat] Add a provider-neutral settled-outcome projection adapter that feeds host-supplied SettlementTarget and OutcomeEvidenceSample values into ConsequenceSettler and returns EpisodeProjection observations; it will not poll sensors, invoke Tools, or hard-code robot predicates.
- [计划] [eval] [test] 增加 delayed、deadline、低覆盖与冲突 evidence 的 fake-facts 集成测试，并运行扩展与 PAOS 全量回归。
- [Planned] [Eval] [Test] Add fake-facts integration coverage for delayed, deadline, low-coverage, and conflicting evidence, then run extension and full PAOS regressions.

### 预期影响文件 / Planned Files

- `extensions/evolution/evolution/projection.py`
- `extensions/evolution/evolution/__init__.py`
- `extensions/evolution/tests/test_evolution_extension.py`

### 实际结果 / Actual Results

- [完成] [policy] [feat] `extensions/evolution/evolution/projection.py:L42-L81` 新增 `SettledOutcomeProjectionPort`；它只编排 provider 提供的 targets/samples、时间和关闭原因，并统一调用 `ConsequenceSettler`，不读取传感器、不调用 Tool。
- [Completed] [Policy] [Feat] `extensions/evolution/evolution/projection.py:L42-L81` adds `SettledOutcomeProjectionPort`; it coordinates provider-supplied targets/samples, timing, and close reason through `ConsequenceSettler` without reading sensors or invoking Tools.
- [完成] [policy] [feat] `extensions/evolution/evolution/__init__.py:L25-L43` 导出新适配器，保持独立扩展包 API 可发现性。
- [Completed] [Policy] [Feat] `extensions/evolution/evolution/__init__.py:L25-L43` exports the adapter so the standalone extension API remains discoverable.
- [完成] [eval] [test] `extensions/evolution/tests/test_evolution_extension.py:L430-L461` 增加 deadline settlement fake-facts 集成测试；扩展测试 `61 passed`，PAOS 全量 `253 passed`，Ruff、compileall、git diff --check 通过。
- [Completed] [Eval] [Test] `extensions/evolution/tests/test_evolution_extension.py:L430-L461` adds a deadline-settlement fake-facts integration test; extension tests report `61 passed`, full PAOS `253 passed`, with Ruff, compileall, and git diff --check passing.

### 六维验收 / Six-Dimension Acceptance

1. 架构集成：通过。结算适配器位于独立 extension projection 层，provider 仍拥有物理语义。 / Passed. Settlement lives in the standalone extension projection layer while providers retain physical semantics.
2. 失败路径：通过。settler 原有的 deadline、低覆盖、冲突和未知 target 校验继续生效。 / Passed. Existing deadline, low-coverage, conflict, and unknown-target checks remain active.
3. 权威与安全边界：通过。适配器不接触执行、传感器、Verifier 或运动权限。 / Passed. The adapter touches no execution, sensors, verifier, or motion authority.
4. 配置与复现：通过。时间和 close reason 可由宿主显式投影，默认仅使用 episode 的 time_cost_ms。 / Passed. Timing and close reason are explicit host projections, with episode time_cost_ms as the default.
5. 可维护性：通过。复用既有 ConsequenceSettler，不新增 store、scheduler 或并行结算实现。 / Passed. The existing ConsequenceSettler is reused without a new store, scheduler, or parallel settlement implementation.
6. 可观察性与 Anti-OverDefense：通过。settlement 语义保持可解释，没有新增 speculative hash、baseline 或 gate。 / Passed. Settlement remains inspectable without speculative hashes, baselines, or gates.

### 实际结果 / Actual Results

- [完成] [policy] [feat] `PhyAgentOS/agent/experience/evolution_extension_adapter.py:L1-L132` 新增宿主侧适配器：将带有 skill/workflow scope 的扩展提案转换为现有 `SkillCandidate`，保留 method、transition、surface、patch 与 evidence provenance；decision 提案可通过显式 digest resolver 接入现有 `WorkflowPolicyCandidateManager`。不执行 promotion 或 Skill 写入。
- [Completed] [Policy] [Feat] `PhyAgentOS/agent/experience/evolution_extension_adapter.py:L1-L132` adds a host-side adapter that maps scoped extension proposals into existing `SkillCandidate` records while preserving method, transition, surface, patch, and evidence provenance; decision proposals can use an explicit digest resolver with the existing `WorkflowPolicyCandidateManager`. No promotion or Skill writes occur.
- [完成] [policy] [fix] `PhyAgentOS/agent/experience/coordinator.py:L80-L90,L239-L251,L282-L330` 将 extension callback 保持 episode 创建后的即时观察，同时把失败纳入既有 evolution job retry；重启后 pending job 可重放，达到三次失败后记录 exhausted 并继续主经验处理。
- [Completed] [Policy] [Fix] `PhyAgentOS/agent/experience/coordinator.py:L80-L90,L239-L251,L282-L330` keeps immediate observation after episode creation and routes delivery failures through the existing evolution-job retry; pending jobs replay after restart, and three exhausted failures are recorded before main experience processing continues.
- [完成] [policy] [feat] `PhyAgentOS/agent/experience/contracts.py:L342-L367` 为 `SkillWorkflowProposal` 增加可选 `evolution_metadata`，使扩展 provenance 进入现有候选记录而不改变激活语义。
- [Completed] [Policy] [Feat] `PhyAgentOS/agent/experience/contracts.py:L342-L367` adds optional `evolution_metadata` to `SkillWorkflowProposal`, carrying extension provenance into existing candidate records without changing activation semantics.
- [完成] [eval] [test] `tests/test_evolution_extension_adapter.py:L1-L65` 新增三项 fake-facts 测试，验证候选持久化、跨 episode support 聚合和无 scope 提案的 fail-closed；专项 `6 passed`，PAOS 全量 `253 passed`，Ruff、compileall、git diff --check 通过。
- [Completed] [Eval] [Test] `tests/test_evolution_extension_adapter.py:L1-L65` adds three fake-facts tests for candidate persistence, cross-episode support aggregation, and fail-closed handling of unscoped proposals; focused tests report `6 passed`, full PAOS reports `253 passed`, with Ruff, compileall, and git diff --check passing.

### 六维验收 / Six-Dimension Acceptance

1. 架构集成：部分通过。扩展仍为独立包，宿主 adapter 复用 ExperienceStore、SkillCandidate 与 WorkflowPolicyCandidateManager；AgentLoop 在传入 extension 且未提供 lifecycle 时自动注入 adapter。标准 CLI/Gateway 仍需由调用方提供 provider-specific projection 和 registry，因此本轮不猜测自动装载。 / Partially passed. The extension remains standalone and reuses existing stores and candidate managers; AgentLoop injects the adapter when an extension is supplied. Standard CLI/Gateway still require a caller-supplied provider-specific projection and registry, so automatic loading is intentionally not guessed in this round.
2. 失败路径：通过。无 scope 不落库，callback 失败可重试，连续失败达到上限后记录 exhausted 且不阻塞主路径。 / Passed. Unscoped proposals are not persisted, callback failures retry, and exhausted delivery does not block the main path.
3. 权威与安全边界：通过。适配器只提交 collecting/pending_review 候选，不写 Skill、不调用 Verifier、Runtime 或运动接口。 / Passed. The adapter only submits collecting/pending-review candidates and never writes Skills or invokes verifier, runtime, or motion APIs.
4. 配置与复现：通过。candidate_id 与 source episode 保留，patch provenance 和显式 policy digest resolver 可追踪；未提供 resolver 时不猜测 policy。 / Passed. Candidate and episode identity plus patch provenance are retained; policy mapping requires an explicit resolver.
5. 可维护性：通过。未新增 store、scheduler、Gateway 或全局状态；重放复用既有 evolution_jobs。 / Passed. No new store, scheduler, Gateway, or global state was added; replay reuses evolution_jobs.
6. 可观察性与 Anti-OverDefense：通过。记录 unmapped、submitted、delivery exhausted 事件；未新增 speculative hash/baseline/gate。 / Passed. Unmapped, submitted, and exhausted events are recorded without speculative hashes, baselines, or gates.

### 验证与 Git 状态 / Verification and Git Status

- `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 uv run --extra dev --extra tui python -m pytest -q -p pytest_asyncio.plugin` -> `253 passed`。
- `uv run ruff check ...`、`python -m compileall -q ...`、`git diff --check` -> 通过。
- 未运行 Gateway、Dora、simulation step、Action 或硬件；无 commit，未 push（用户明确要求仅本地保存）。
- Gateway, Dora, simulation step, Action, and hardware were not run; no commit and no push were performed per user instruction.
