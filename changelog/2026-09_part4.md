# 2026-09 Changelog — Part 4

## v7.3.5 (2026-09-08 23:05) - codex

### 预期修改 / Planned Changes

- [计划] [policy] [feat] 增加 extension 的 Future-use 回流接口：宿主提交 parent/candidate 观察后，由 `FutureUseObserver` 生成配对比较，扩展只发送结果事件，不新增持久化或绑定状态。
- [Planned] [Policy] [Feat] Add a Future-use feedback interface: after the host submits parent/candidate observations, `FutureUseObserver` produces paired comparisons and the extension emits result events without adding persistence or binding state.
- [计划] [eval] [test] 增加 held-out、hazard、unknown 和未配对观察的 fake-facts 测试，并运行扩展与 PAOS 全量回归。
- [Planned] [Eval] [Test] Add fake-facts coverage for held-out, hazard, unknown, and unpaired observations, then run extension and full PAOS regressions.

### 预期影响文件 / Planned Files

- `extensions/evolution/evolution/api.py`
- `extensions/evolution/evolution/observation.py`
- `extensions/evolution/evolution/plugin.py`
- `extensions/evolution/evolution/__init__.py`
- `extensions/evolution/tests/test_evolution_extension.py`

### 实际结果 / Actual Results

- [完成] [policy] [feat] `extensions/evolution/evolution/plugin.py:L20-L30,L154-L194` 增加 `observe_future_use` 回流接口：宿主提交 parent/candidate observations 后，扩展调用 `FutureUseObserver` 生成 paired comparison，并通过事件或可选 sink 输出结果；未配对、unknown 或 split 不一致时保持 incomplete，不伪造 forward-transfer。
- [Completed] [Policy] [Feat] `extensions/evolution/evolution/plugin.py:L20-L30,L154-L194` adds `observe_future_use`: after the host submits parent/candidate observations, the extension uses `FutureUseObserver` to produce a paired comparison and emits it through events or an optional sink; unpaired, unknown, or split-mismatched inputs remain incomplete and never fabricate forward transfer.
- [完成] [policy] [feat] `extensions/evolution/evolution/__init__.py:L24-L35` 导出 Future-use observation/comparison 类型，保持扩展 API 可发现。
- [Completed] [Policy] [Feat] `extensions/evolution/evolution/__init__.py:L24-L35` exports Future-use observation and comparison types for discoverable extension APIs.
- [完成] [eval] [test] `extensions/evolution/tests/test_evolution_extension.py:L464-L515` 增加 held-out 成功增益和未配对观察测试；扩展测试 `63 passed`，PAOS 全量 `253 passed`，Ruff、compileall、git diff --check 通过。
- [Completed] [Eval] [Test] `extensions/evolution/tests/test_evolution_extension.py:L464-L515` adds held-out gain and unpaired-observation tests; extension tests report `63 passed`, full PAOS `253 passed`, with Ruff, compileall, and git diff --check passing.

### 六维验收 / Six-Dimension Acceptance

1. 架构集成：通过。Future-use 只作为宿主回流 seam，不拥有 candidate binding 或持久化状态。 / Passed. Future-use is a host feedback seam and owns neither candidate binding nor persistence.
2. 失败路径：通过。缺少 parent/candidate pair、unknown outcome、split mismatch 和 sink failure 都显式报告。 / Passed. Missing pairs, unknown outcomes, split mismatches, and sink failures are explicit.
3. 权威与安全边界：通过。接口只接收已完成的观察，不触发执行、激活或 promotion。 / Passed. The interface consumes completed observations only and triggers no execution, activation, or promotion.
4. 配置与复现：通过。comparison_key、method_id、candidate_id、split 和 binding_role 由宿主提供并保留。 / Passed. comparison_key, method_id, candidate_id, split, and binding_role remain host-provided and preserved.
5. 可维护性：通过。复用现有 `FutureUseObserver`，未新增 store、scheduler 或后台线程。 / Passed. Existing `FutureUseObserver` is reused without a new store, scheduler, or background thread.
6. 可观察性与 Anti-OverDefense：通过。输出 paired_tasks、success_gain 和 forward_transfer_gain 事件，不新增 speculative gate。 / Passed. paired_tasks, success_gain, and forward_transfer_gain are observable without speculative gates.

## v7.3.2 (2026-09-08 22:10) - codex

### 预期修改 / Planned Changes

- [计划] [model] [fix] 修正 GraspGen 接触几何资格计算：将 RoboTwin reset 姿态下的 world 顶点通过 provider 导出的 reference hand pose 投影到候选 robot-target pose，再评估桌面净空；不修改 GraspGen depth、不增加固定世界偏移。
- [Planned] [Model] [Fix] Correct GraspGen contact-geometry qualification by projecting RoboTwin reset-world vertices through the provider-exported reference hand pose into the candidate robot-target pose before evaluating table clearance; do not change GraspGen depth or add a fixed world offset.
- [计划] [eval] [test] 增加 reference pose 缺失、目标姿态旋转/平移和 scene revision 失配测试；重新运行 no-motion geometry worker、专项与 RoboTwin adapter 回归，不执行 scene.step、Gateway、Dora、Action 或硬件。
- [Planned] [Eval] [Test] Add regressions for missing reference pose, target-pose rotation/translation, and stale scene revision; rerun the no-motion geometry worker, focused tests, and the RoboTwin adapter suite without scene.step, Gateway, Dora, Action, or hardware.

### 预期影响文件 / Planned Files

- `examples/forge-adapters/robotwin20/runtime/robotwin_grasp_contact_geometry_worker.py`
- `examples/forge-adapters/robotwin20/src/robotwin20_adapter/grasp_postprocessing.py`
- `examples/forge-adapters/robotwin20/tests/test_grasp_postprocessing.py`
- `examples/forge-adapters/robotwin20/tests/test_grasp_contact_geometry_worker.py`

### 实际结果 / Actual Results

- [完成] [model] [fix] `robotwin_grasp_contact_geometry_worker.py:L43-L56` 为每个 Panda arm 输出 `reference_hand_pose`（world frame、position、orientation），并使用非弃用的 `get_entity_pose()` 读取 reset 姿态。
- [Completed] [Model] [Fix] `robotwin_grasp_contact_geometry_worker.py:L43-L56` now emits a per-arm `reference_hand_pose` (world frame, position, orientation) and reads the reset pose through non-deprecated `get_entity_pose()`.
- [完成] [model] [fix] `grasp_postprocessing.py:L40-L87,L231-L240` 增加 quaternion 刚体变换桥接：reset-world link vertices → reference-hand local → candidate robot-target world；缺失或失配 reference pose 显式失败。
- [Completed] [Model] [Fix] `grasp_postprocessing.py:L40-L87,L231-L240` adds quaternion rigid-transform bridging from reset-world link vertices through reference-hand local coordinates into the candidate robot-target world pose, with explicit failure for missing or mismatched reference poses.
- [完成] [eval] [test] `test_grasp_postprocessing.py:L141-L185` 与 `test_grasp_contact_geometry_worker.py:L55-L61` 覆盖目标位姿变换、缺 reference pose 和 worker artifact 字段；专项 `12 passed`，adapter 全量 `326 passed, 1 skipped`，Ruff、compileall、diff check 通过。
- [Completed] [Eval] [Test] `test_grasp_postprocessing.py:L141-L185` and `test_grasp_contact_geometry_worker.py:L55-L61` cover target-pose projection, missing reference pose, and worker artifact fields; focused tests pass `12`, the adapter suite passes `326 passed, 1 skipped`, and Ruff, compileall, and diff check pass.

### 六维验收 / Six-Dimension Acceptance

- 架构集成 / Architecture: 通过；投影仍位于 RoboTwin provider adapter，planning、task lifecycle、Gateway 和执行面未改变。
- 失败路径 / Failure paths: 通过；缺 reference pose、错误 frame、stale scene、无效 quaternion 和无可行 variant 均显式失败或 unavailable。
- 权威边界 / Authority: 通过；GraspGen 原始 pose/depth 保持不变，投影只产生 no-motion qualification，不授予 motion authority。
- 配置 / Configuration: 通过；没有新增固定 Z 偏移或全局 depth，backoff 仍由调用方提供的候选集合决定。
- 可维护性 / Maintainability: 通过；纯几何变换与 RoboTwin snapshot 分离，复用现有 pose/vertex helpers，测试覆盖真实接口字段。
- 防止过度防御编程 / Anti-overdefense: 通过；新增校验仅对应已确认的 reset-vs-target 坐标错误，不引入额外 hash、冻结 contract、store、scheduler 或无关 gate。

### 真实 no-motion 证据 / Real No-Motion Evidence

- Geometry artifact: `/home/yanxu/robotwin20-runtime/artifacts/paos-grasp-contact-v7.3.2-20260908T2240Z/contact-geometry.json`
- Qualification artifact: `/home/yanxu/robotwin20-runtime/artifacts/paos-grasp-contact-v7.3.2-20260908T2240Z/qualification.json`
- 结果 / Result: `status=qualified`, `selected_backoff_m=0.0`, `support_clearance_m=0.11025242347559439`, `motion_authorized=false`。
- 仅执行 reset 与几何读取，未调用 `scene.step()`、Gateway、Dora、Action 或硬件。
- Reset and geometry extraction only; no `scene.step()`, Gateway, Dora, Action, or hardware was invoked.

### Git 提交 / Git Commit

- Implementation commit: `6dd4ff5`
- Branch: `feature/planning-loop`
- 时间 / Time: 2026-09-08 (Asia/Shanghai)

## v7.3.4 (2026-09-08 23:50) - codex

### 实际结果 / Actual Results

- [完成] [eval] [exp] 基于 v7.3.2 几何投影修复物化新的 GraspGen route package：`/home/yanxu/robotwin20-runtime/artifacts/paos-route-v7.3.2-graspgen-contact-20260908T2345Z/`。
- [Completed] [Eval] [Exp] Materialized a new GraspGen route package from the v7.3.2 geometry-projection fix at `/home/yanxu/robotwin20-runtime/artifacts/paos-route-v7.3.2-graspgen-contact-20260908T2345Z/`.
- [完成] [eval] [test] 新 package 绑定 candidate `candidate://block-green-1/0`、scene revision `blocks_ranking_rgb-0-1`、最新 simulation probe worker `6534f093…`，并通过 `validate_route_request()`；`motion_authorized=false`，状态为 `awaiting_human_review`。
- [Completed] [Eval] [Test] The package binds candidate `candidate://block-green-1/0`, scene revision `blocks_ranking_rgb-0-1`, and the current simulation-probe worker `6534f093…`; `validate_route_request()` passes, `motion_authorized=false`, and status is `awaiting_human_review`.
- [完成] [safety] [policy] 未执行 `scene.step()`、Gateway、Dora、Action、benchmark motion 或硬件；历史 approval 未复用。
- [Completed] [Safety] [Policy] No `scene.step()`, Gateway, Dora, Action, benchmark motion, or hardware was executed; no historical approval was reused.

### Package Binding / 包绑定

- request id: `franka-blocks-green0-graspgen-contact-v732-20260908`
- route geometry digest: `61e9f7fbe1d61aadc5be9b089eb3afeb31d1e11430288d01ee9bce7d463c1723`
- source manifest digest: `6ba15e3b8d9a2bec1721990d46175421e8348ecbbe5c60230bbd881a0d61ac1c`
- required decision: `approved_independent_simulation_probe`

### 下一步 / Next Step

等待新的人工 `simulation-only` approval。批准后才可运行完整双视角 RoboTwin probe；在批准前不执行任何场景步进。

Await fresh human `simulation-only` approval. Only after approval may the complete dual-view RoboTwin probe run; no scene stepping occurs before approval.

## v7.3.6 (2026-09-09 00:05) - codex

### 预期修改 / Planned Changes

- [计划] [eval] [exp] 物化用户批准的 `franka-blocks-green0-graspgen-contact-v732-20260908` simulation-only approval，并运行绑定最新 route/source-manifest/worker 的完整 RoboTwin probe。
- [Planned] [Eval] [Exp] Materialize the user-approved simulation-only approval for `franka-blocks-green0-graspgen-contact-v732-20260908` and run the complete RoboTwin probe bound to the current route, source manifest, and worker.
- [计划] [safety] [test] 保存 attached、lift、transport、descent、release、retreat、contact、stop/reset、semantic-verification 以及 head-camera/observer-camera 双视角视频 evidence；不调用 Gateway、Dora、Action 或硬件。
- [Planned] [Safety] [Test] Persist attached, lift, transport, descent, release, retreat, contact, stop/reset, semantic-verification, and head-camera/observer-camera dual-view video evidence; do not invoke Gateway, Dora, Action, or hardware.

### 实际结果 / Actual Results

- [完成] [eval] [exp] 用户批准已物化为 `/home/yanxu/robotwin20-runtime/artifacts/paos-route-v7.3.2-graspgen-contact-20260908T2345Z/probe/approval.json`，审批记录绑定 route digest `61e9f7fbe1d61aadc5be9b089eb3afeb31d1e11430288d01ee9bce7d463c1723` 与 worker digest `6534f093…`。
- [Completed] [Eval] [Exp] The user approval was materialized at `/home/yanxu/robotwin20-runtime/artifacts/paos-route-v7.3.2-graspgen-contact-20260908T2345Z/probe/approval.json`, bound to route digest `61e9f7fbe1d61aadc5be9b089eb3afeb31d1e11430288d01ee9bce7d463c1723` and worker digest `6534f093…`.
- [完成] [eval] [test] 完整 probe 返回 `status=unavailable`：失败发生在 `preflight`，左右臂首个 `robot_target_pose` planner segment 均失败；`simulator_steps=0`、`contact_trace=[]`、`world_change_completed=false`，未生成双视角视频或语义结果。
- [Completed] [Eval] [Test] The complete probe returned `status=unavailable`: both arms failed the first `robot_target_pose` planner segment during `preflight`; `simulator_steps=0`, `contact_trace=[]`, and `world_change_completed=false`, so no dual-view video or semantic result was produced.
- [完成] [policy] [diagnosis] 该失败不证明夹爪接触桌面；因为未执行任何 `scene.step()`。独立 no-motion planner 对照显示：加载同一 collision world 后 right arm target 可返回 `Success`，left arm 为 `Fail`，说明当前失败需要继续定位 RoboTwin/Curobo preflight 的非确定性或双臂 planner 状态，不应继续调 GraspGen depth/backoff。
- [Completed] [Policy] [Diagnosis] This failure does not prove gripper-table contact because no `scene.step()` ran. An independent no-motion planner check with the same collision world returned `Success` for the right-arm target and `Fail` for the left arm, so the next investigation is RoboTwin/Curobo preflight state or nondeterminism, not GraspGen depth/backoff tuning.

### 六维验收 / Six-Dimension Acceptance

1. 架构集成：通过；审批和 probe 仍在 RoboTwin provider/runtime seam，未改变 PAOS planning 或 Gateway ownership。 / Passed; approval and probe remain in the RoboTwin provider/runtime seam.
2. 失败路径：通过；preflight 失败生成 failure、before/after snapshot、stop/reset 状态，且未把失败解释为成功。 / Passed; preflight failure produced failure, snapshots, and stop/reset state without being reported as success.
3. 权威边界：通过；probe 仅在人工批准后运行，未触发 Gateway、Dora、Action 或硬件。 / Passed; the probe ran only after human approval and touched no Gateway, Dora, Action, or hardware.
4. 配置与复现：通过；route、candidate、scene revision、source manifest 和 worker 均绑定到同一 package。 / Passed; route, candidate, scene revision, source manifest, and worker are bound to one package.
5. 可维护性：部分通过；失败 artifact 完整，但 probe 尚未证明 attached/lift/transport/descent/release/retreat 闭环，需要后续针对 preflight 状态定位。 / Partially passed; failure evidence is complete, but the full route lifecycle remains unproven pending preflight diagnosis.
6. 防止过度防御编程：通过；本轮未新增代码、哈希或额外 gate，仅记录真实 probe 结果与已有证据。 / Passed; no code, hashes, or extra gates were added in this run.

## v7.3.0 (2026-09-08 19:06) - codex

### 预期修改 / Planned Changes

- [计划] [model] [feat] 增加 RoboTwin adapter-owned GraspGen contact-depth 后处理：保留原始 candidate/depth，按真实 Panda hand/finger 几何、目标物和支撑平面评估有限 ingress-backoff 变体；不改 PAOS Core、全局 depth 或世界 Z 偏移。
- [Planned] [Model] [Feat] Add RoboTwin adapter-owned GraspGen contact-depth post-processing that preserves the original candidate/depth and evaluates finite ingress-backoff variants against real Panda geometry, object, and support plane; do not change PAOS Core, global depth, or world-Z offsets.
- [计划] [eval] [test] 增加 no-motion geometry worker、route materializer 可选 qualification 接口和专项测试，完成六维验收；不运行 scene.step、Gateway、Dora、Action 或硬件。
- [Planned] [Eval] [Test] Add the no-motion geometry worker, optional route-materializer qualification interface, and focused tests, followed by six-dimension acceptance without scene.step, Gateway, Dora, Action, or hardware.

### 实际结果 / Actual Results

- [完成] [model] [feat] `grasp_postprocessing.py:L1-L201` 提供有限 backoff、支撑净空、pinch containment、geometry-artifact bridge 和显式 unavailable 结果；`robotwin_grasp_contact_geometry_worker.py:L1-L98` 提取双臂真实 Panda 碰撞顶点与 table top，保持 `motion_authorized=false`。
- [Completed] [Model] [Feat] `grasp_postprocessing.py:L1-L201` provides finite backoff, support clearance, pinch containment, geometry-artifact bridging, and explicit unavailable results; `robotwin_grasp_contact_geometry_worker.py:L1-L98` extracts real dual-arm Panda vertices and table top while retaining `motion_authorized=false`.
- [完成] [model] [feat] `materialize_complete_route.py:L635-L685,L671-L683,L796-L810` 在人工审批前按显式 qualification artifact 应用 execution variant并写入 candidate provenance；未提供时 baseline route 不变。
- [Completed] [Model] [Feat] `materialize_complete_route.py:L635-L685,L671-L683,L796-L810` applies an execution variant before human approval only from an explicit qualification artifact and records it in candidate provenance; omission preserves the baseline route.
- [完成] [docs] [docs] `docs/forge/GRASPGEN_CONTACT_DEPTH_POSTPROCESSING.md:L1-L67` 记录 depth 语义、坐标、失败和 PAOS ownership 边界。
- [Completed] [Docs] [Docs] `docs/forge/GRASPGEN_CONTACT_DEPTH_POSTPROCESSING.md:L1-L67` records depth semantics, frames, failure behavior, and PAOS ownership boundaries.
- [完成] [eval] [test] `test_grasp_postprocessing.py:L1-L135` 与 `test_grasp_contact_geometry_worker.py:L1-L81` 两个专项测试 `10 passed`；正确依赖路径下 RoboTwin adapter 全量 `323 passed, 1 skipped`；Ruff、compileall、git diff --check 通过；未执行仿真动作。
- [Completed] [Eval] [Test] The two focused suites pass `10`; the RoboTwin adapter suite passes `323 passed, 1 skipped` with the correct dependency path; Ruff, compileall, and git diff --check pass; no simulator motion executed.

### 六维验收 / Six-Dimension Acceptance

- 架构集成 / Architecture: 通过；实现位于 provider adapter/runtime/materializer，不接管 PAOS lifecycle、planning、Gateway 或执行状态机。
- 失败路径 / Failure paths: 通过；无效 geometry、stale revision、缺 link、无可行 variant、缺 qualification ref 均显式失败或 unavailable。
- 权威边界 / Authority: 通过；原始 GraspGen proposal/depth 保持事实，variant 仅用于审批前 route 物化，provider artifact 不授予 motion authority。
- 配置 / Configuration: 通过；backoff 由调用方声明的有限集合提供，不新增固定世界偏移或修改全局 depth。
- 可维护性 / Maintainability: 通过；纯计算与 RoboTwin runtime 分离，复用现有碰撞顶点/table helper，提供文档与测试。
- 防止过度防御编程 / Anti-overdefense: 通过；仅增加当前几何资格问题所需的约束，不新增无关 hash、冻结 contract、重复 store 或额外 gate。

### Git 提交 / Git Commit

- Commit: `f7eea46`; Branch: `feature/planning-loop`; 时间 / Time: 2026-09-08 (Asia/Shanghai)

## v7.3.1 (2026-09-08 20:05) - codex

### 预期修改 / Planned Changes

- [计划] [evolution] [fix] 按 code review 修复 EvoPhy 真实闭环缺口：校验 transition predicate 绑定、拒绝无证据候选、处理低置信度 owner、修正 TRACE 排序与 joint-cause 关联语义。
- [Planned] [Evolution] [Fix] Fix EvoPhy closure gaps found in code review: validate transition-predicate binding, reject evidence-free candidates, handle low-confidence owners, and correct TRACE ranking and joint-cause association semantics.
- [计划] [evolution] [feat] 增加 CandidateProposal 到 PAOS 既有 SkillCandidate 生命周期的显式 adapter，并提供基于现有 composition root 的可选装载路径；不新增 store、scheduler、Verifier 或运动权限。
- [Planned] [Evolution] [Feat] Add an explicit CandidateProposal-to-PAOS SkillCandidate lifecycle adapter and an opt-in composition-root loading path using existing PAOS stores and callbacks; add no store, scheduler, Verifier, or motion authority.
- [计划] [eval] [test] 增加 verification-off、重复/未配对评测、失败重放、真实 settlement adapter 和四类 patch selector 的 fake-facts 回归，并完成六维验收。
- [Planned] [Eval] [Test] Add fake-facts regressions for verification-off isolation, duplicate/unpaired evaluation, failure replay, the settlement adapter, and four-surface patch selection, then complete six-dimension acceptance.

### 预期影响文件 / Planned Files

- `extensions/evolution/evolution/api.py`
- `extensions/evolution/evolution/projection.py`
- `extensions/evolution/evolution/trace.py`
- `extensions/evolution/evolution/methods/evophy/method.py`
- `extensions/evolution/evolution/plugin.py`
- `extensions/evolution/evolution/lifecycle.py`
- `extensions/evolution/evolution/adapter.py`
- `extensions/evolution/tests/`
- `PhyAgentOS/agent/experience/coordinator.py`
- `PhyAgentOS/agent/experience/source.py`
- `PhyAgentOS/cli/commands.py`
- `tests/`
- `CHANGELOG.md`
- `changelog/2026-09_part4.md`

### 实际结果 / Actual Results

- [完成] [evolution] [fix] `extensions/evolution/evolution/api.py:L158-L184` 现在拒绝未声明的 transition predicate；`evolution/methods/evophy/method.py:L21-L83` 要求 evidence references，按 owner 生成 expectation/observation/decision/recovery 四类局部 patch，并对有下游影响的归因生成 recovery patch。
- [Completed] [Evolution] [Fix] `extensions/evolution/evolution/api.py:L158-L184` now rejects undeclared transition predicates; `evolution/methods/evophy/method.py:L21-L83` requires evidence references, generates expectation/observation/decision/recovery local patches by owner, and emits a recovery patch when attribution has downstream impact.
- [完成] [evolution] [fix] `settlement.py:L21-L24,L120-L129,L190-L210` 与 `trace.py:L12-L19,L80-L90` 增加最低 owner confidence；TRACE 默认保留 earliest-observable 顺序，同时通过 `score_primary` 显式开启 score-first 排序；joint cause 仅由共享 evidence reference 建立；unknown predicate 带 transition identity。
- [Completed] [Evolution] [Fix] `settlement.py:L21-L24,L120-L129,L190-L210` and `trace.py:L12-L19,L80-L90` add minimum owner confidence; TRACE preserves earliest-observable order by default and exposes explicit `score_primary` score-first ordering; joint causes require shared evidence references; unknown predicates include transition identity.
- [完成] [evolution] [feat] `api.py:L267-L312` 增加 `trial_id`、`comparison_key` 与 `CandidateLifecyclePort`；`plugin.py:L25-L105` 支持宿主 lifecycle adapter，保留 candidate sink 观察模式；`evaluation.py:L49-L75` 对缺失 trial identity 的重复 receipt 也保持 hold。
- [Completed] [Evolution] [Feat] `api.py:L267-L312` adds `trial_id`, `comparison_key`, and `CandidateLifecyclePort`; `plugin.py:L25-L105` supports a host lifecycle adapter while retaining observation-only candidate sinks; `evaluation.py:L49-L75` holds duplicate receipts even when trial identity is absent.
- [完成] [agent] [fix] `TaskOutcomeEnvelope`/`TaskEpisode` 增加 verification mode 传播，extension 在 `verification_mode=off` 时跳过 episode；`FutureUseComparison.forward_transfer_gain` 在没有 held-out pair 时为 `None`，不再把 matched gain 冒充 forward transfer。
- [Completed] [Agent] [Fix] `TaskOutcomeEnvelope`/`TaskEpisode` now propagate verification mode, and the extension skips episodes with `verification_mode=off`; `FutureUseComparison.forward_transfer_gain` is `None` without a held-out pair, so matched gain is not reported as forward transfer.
- [完成] [eval] [test] 扩展测试从 48 增至 60 个；新增 predicate binding、evidence-free candidate、verification-off、owner confidence、四类 patch、重复 receipt、future-transfer 和 score-primary 回归。PAOS 全量回归为 `250 passed`。
- [Completed] [Eval] [Test] Extension coverage increased from 48 to 60 tests with predicate binding, evidence-free candidate, verification-off, owner confidence, four patch surfaces, duplicate receipts, future-transfer, and score-primary regressions. Full PAOS regression reports `250 passed`.
- [完成] [docs] [docs] 实际变更还包括 `PhyAgentOS/agent/experience/contracts.py:L139-L206`、`source.py:L237-L267` 与 `coordinator.py:L205-L235` 的 verification mode 传播；`extensions/evolution/README.md:L91-L108` 更新 CandidateLifecyclePort 使用说明；本地设计文档 `L717-L731` 记录复核后的剩余宿主接入缺口。
- [Completed] [Docs] [Docs] Additional changes include verification-mode propagation in `PhyAgentOS/agent/experience/contracts.py:L139-L206`, `source.py:L237-L267`, and `coordinator.py:L205-L235`; `extensions/evolution/README.md:L91-L108` documents CandidateLifecyclePort usage; the local design document `L717-L731` records the remaining host-integration gaps after review.

### 关键代码 Diff / Key Code Diff

#### [修改] `extensions/evolution/evolution/api.py:L166-L187`

**修改前 / Before:**
```python
if any(item.transition_id not in known for item in self.observations):
    raise ValueError("outcome observation references an unknown transition")
```

**修改后 / After:**
```python
predicates_by_transition = {
    item.transition_id: set(item.expected_predicates) for item in self.transitions
}
if any(
    item.predicate not in predicates_by_transition[item.transition_id]
    for item in self.observations
):
    raise ValueError("outcome observation references an unexpected predicate")
```

#### [修改] `extensions/evolution/evolution/methods/evophy/method.py:L37-L75`

**修改前 / Before:**
```python
proposals.append(local_observation_patch(...))
```

**修改后 / After:**
```python
if not evidence_refs:
    continue
surface = "recovery" if hypothesis.downstream_violations else owner_surface[hypothesis.owner]
proposals.append(local_skill_patch(..., patch=(PatchInstruction(surface=surface, ...),)))
```

**修改说明 / Change:** 候选必须带 evidence references，并根据归因 owner/下游影响选择四类受限 patch surface。

#### [修改] `extensions/evolution/evolution/plugin.py:L51-L105`

```python
if getattr(episode, "verification_mode", None) == "off":
    self._emit("episode_skipped", ...)
    return ()
submit = self.candidate_lifecycle.submit if self.candidate_lifecycle is not None else self.candidate_sink
```

**修改说明 / Change:** verification-off episode 不进入 EvoPhy；候选提交通过显式宿主 lifecycle port。

### 六维验收 / Six-Dimension Acceptance

1. **架构集成 / Architecture integration — 部分通过 / Partial**：extension 仍独立且 host lifecycle port 已定义；标准 CLI composition root 尚未自动发现并装载 `EvolutionExtension`，真实 PAOS 运行仍需显式注入。
2. **失败路径 / Failure behavior — 通过 / Pass**：新增非法 predicate、无证据、低置信度 owner、重复 receipt、verification-off 与 future-use 不可评估路径。
3. **权威与安全边界 / Authority and safety — 通过 / Pass**：候选仍是非权威 proposal，promotion、Skill binding、Verifier、Runtime 和运动权限保持宿主所有。
4. **配置与可复现性 / Configuration and reproducibility — 通过 / Pass**：阈值和排序策略外部化为 policy；receipt identity 和 future-use pairing 语义可复现；独立环境测试通过。
5. **可维护性 / Maintainability — 通过 / Pass**：没有新增 store、scheduler、Gateway 或全局状态；四类 patch 共用同一结构化 patch builder。
6. **可观察性与 Anti-OverDefense / Observability and Anti-OverDefense — 通过 / Pass**：保留 transition-scoped unknown、evidence refs、trial identity、candidate lifecycle 事件；未新增 speculative hash、冻结 contract 或额外 gate。

### 未解决项 / Remaining Items

- 标准 `paos agent`/gateway composition root 仍没有自动加载 extension wheel；需要宿主选择 projection ports 与 CandidateLifecyclePort 后才能启用，不能把当前手工注入测试写成生产闭环。
- `CandidateLifecyclePort` 当前是显式宿主协议，尚未提供 PAOS `SkillCandidate`/`WorkflowPolicyCandidate` 的具体 adapter；candidate promotion、真实 settlement adapter 和失败 episode replay 仍需下一轮实现。
- 因上述宿主装载与生命周期 adapter 尚未完成，本版本不宣称“完整自我进化闭环”或真实 benchmark 结果。

### 验证 / Validation

- [完成] `cd extensions/evolution && uv run ruff check evolution tests && PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 uv run python -m pytest -q`：`60 passed`。
- [Completed] `cd extensions/evolution && uv run ruff check evolution tests && PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 uv run python -m pytest -q`: `60 passed`.
- [完成] `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 uv run --extra dev --extra tui python -m pytest -q -p pytest_asyncio.plugin`：PAOS 全量 `250 passed`。
- [Completed] `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 uv run --extra dev --extra tui python -m pytest -q -p pytest_asyncio.plugin`: full PAOS suite `250 passed`.
- [完成] compileall、extension wheel build、`git diff --check` 通过；未启动 Gateway、Dora、simulation step、Action 或硬件。
- [Completed] compileall, extension wheel build, and `git diff --check` passed; no Gateway, Dora, simulation step, Action, or hardware was started.

## v7.2.0 (2026-09-08 19:00) - codex

### 预期修改 / Planned Changes

- [计划] [cli] [feat] 增加可选 Textual dashboard presentation layer，复用现有 AgentLoop、MessageBus、LongHorizonTaskController 和 AgentTaskCoordinator，提供对话、thinking/progress、任务状态、DAG 节点、事件和输入面板；不新增 scheduler/store/execution plane。
- [Planned] [CLI] [Feat] Add an optional Textual dashboard presentation layer reusing the existing AgentLoop, MessageBus, LongHorizonTaskController, and AgentTaskCoordinator, with conversation, thinking/progress, task status, DAG nodes, events, and input panels; add no scheduler/store/execution plane.
- [计划] [config] [feat] 将 Textual 作为可选 `tui` 依赖，通过 `paos agent --ui textual` 显式启用；默认 prompt_toolkit 路径保持兼容。
- [Planned] [Config] [Feat] Make Textual an optional `tui` dependency enabled explicitly via `paos agent --ui textual`; keep the prompt_toolkit path as the default compatibility mode.
- [计划] [eval] [test] 增加 dashboard 的无依赖 import、事件关联和任务/DAG 投影测试；不启动 Gateway、Dora、仿真或硬件。
- [Planned] [Eval] [Test] Add dependency-free import, event-correlation, and task/DAG projection tests for the dashboard without starting Gateway, Dora, simulator, or hardware.

### 实际结果 / Actual Results

- [完成] [cli] [feat] `PhyAgentOS/cli/textual_app.py:L1-L332` 新增可选 Textual dashboard；从同一 Coordinator 投影任务、active revision、DAG dependencies/settlements、完成进度、clarification 和持久化事件，复用同一 AgentLoop/MessageBus/LongHorizonTaskController，不创建 scheduler、store、PlanRevision 或 Gateway 路径。
- [Completed] [CLI] [Feat] `PhyAgentOS/cli/textual_app.py:L1-L332` adds the optional Textual dashboard; it projects tasks, active revisions, DAG dependencies/settlements, completion progress, clarifications, and persisted events from the same Coordinator, reuses the same AgentLoop/MessageBus/LongHorizonTaskController, and creates no scheduler, store, PlanRevision, or Gateway path.
- [完成] [cli] [feat] `PhyAgentOS/cli/commands.py:L900-L1003` 增加 `--ui textual` 显式入口，默认 `prompt_toolkit` 保持兼容；Textual 缺失时返回可操作的 optional-extra 错误，支持 `/task status|start|pause|resume|stop|replay TASK_ID`。
- [Completed] [CLI] [Feat] `PhyAgentOS/cli/commands.py:L900-L1003` adds the explicit `--ui textual` entry while keeping `prompt_toolkit` as the default; missing Textual reports an actionable optional-extra error, and the dashboard supports `/task status|start|pause|resume|stop|replay TASK_ID`.
- [完成] [config] [feat] `pyproject.toml:L68-L70` 增加 `tui` optional extra；`docs/forge/MANIPULATION_DAG_DEVELOPER_GUIDE.md:L297-L326` 与 `docs/forge/PLANNING_MODULE_DESIGN.md:L484-L524` 明确 presentation-only ownership boundary、安装和使用方式。
- [Completed] [Config] [Feat] `pyproject.toml:L68-L70` adds the `tui` optional extra; `docs/forge/MANIPULATION_DAG_DEVELOPER_GUIDE.md:L297-L326` and `docs/forge/PLANNING_MODULE_DESIGN.md:L484-L524` define the presentation-only ownership boundary, installation, and usage.
- [完成] [eval] [test] `tests/test_textual_app.py:L1-L192` 覆盖任务/DAG投影、持久化事件、缺少依赖错误、session 归一化、pause/clarification 展示、Textual headless 挂载、输入回到同一 MessageBus 以及 Ctrl+C 清理；规划/TUI/控制器专项 `41 passed, 1 warning`，Ruff、compileall、`git diff --check` 通过，未运行 Gateway、Dora、仿真或硬件。
- [Completed] [Eval] [Test] `tests/test_textual_app.py:L1-L192` covers task/DAG projection, persisted events, missing-dependency error, session normalization, pause/clarification rendering, headless Textual mounting, routing input to the same MessageBus, and Ctrl+C cleanup; the planning/TUI/controller focus suite passes `41 passed, 1 warning`, Ruff/compileall/`git diff --check` pass, and no Gateway, Dora, simulator, or hardware was run.

### 文件变更详情 / File Change Details

#### [修改 / Modified] `PhyAgentOS/cli/commands.py:L900-L1003`

**修改前 / Before:**

```python
agent_loop.set_long_horizon_controller(long_horizon_controller)
# The prompt_toolkit interactive presentation starts directly.
```

**修改后 / After:**

```python
agent_loop.set_long_horizon_controller(long_horizon_controller)
if ui == "textual":
    run_textual_app(
        agent_loop=agent_loop,
        bus=bus,
        controller=long_horizon_controller,
        coordinator=forge_task_coordinator,
        session_key=session_id,
    )
```

**修改说明 / Rationale:** Textual 仅接收已经创建的运行时对象，默认 prompt_toolkit 路径不变。Textual only receives already-created runtime objects, while the default prompt_toolkit path remains unchanged.

#### [新增 / Added] `PhyAgentOS/cli/textual_app.py:L18-L332`

```python
rows = _task_rows(coordinator, normalized_session_key)
await bus.publish_inbound(InboundMessage(...))
result = controller.replay(task_id)
```

**新增说明 / Addition:** dashboard 只读投影 Coordinator facts，并通过既有 MessageBus/Controller 发送输入和控制任务。The dashboard read-only projects Coordinator facts and sends input/task control only through the existing MessageBus and Controller.

### 六维验收 / Six-Dimension Review

- 架构集成 / Architecture: 通过；Textual 只替换 presentation layer，Controller、Coordinator、AgentLoop、PlanningLoopAdapter、MessageBus 仍是唯一运行时路径。
- 失败路径 / Failure paths: 通过；缺依赖、AgentLoop 异常、task command 异常、clarification、pause/resume/stop/replay 和退出清理均可见且不伪造 Action 终态。
- 权威边界 / Authority: 通过；Textual 不写 Store、不直接调用 Gateway、不创建 scheduler/PlanRevision、不授予运动权限。
- 配置与 provenance / Configuration and provenance: 通过；session、turn_id、task_id、revision_id、node settlement 和 Coordinator event 继续来自既有持久化/消息元数据。
- 可维护性 / Maintainability: 通过；可选依赖、默认路径兼容、只读投影和 headless smoke test 均位于 CLI presentation 边界。
- 防止过度防御编程 / Anti-overdefense: 通过；只增加真实生命周期/事件丢失场景所需的清理和投影，不新增 hash、重复 store、额外 gate 或 Textual 专用执行协议。

### 验证 / Validation

- `python -m pip install -e '.[tui]'`：Textual 0.89.1 安装成功。
- `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 pytest -q tests/test_textual_app.py tests/test_tui_long_horizon.py tests/test_long_horizon_controller.py tests/test_planning_loop.py tests/test_planning_task_integration.py`：`41 passed, 1 warning`。
- `ruff check PhyAgentOS/cli/textual_app.py tests/test_textual_app.py PhyAgentOS/cli/commands.py`、`python -m compileall -q PhyAgentOS tests/test_textual_app.py`、`git diff --check`：通过。
- 未运行 Gateway、Dora、Action、仿真器或硬件动作。

### Git 提交 / Git Commit

- Implementation commit: `be13b77`
- Branch: `feature/planning-loop`
- 时间 / Time: 2026-09-08 (Asia/Shanghai)
## v7.2.6 (2026-09-08 19:20) - codex

### 预期修改 / Planned Changes

- [计划] [cli] [fix] 修复 Textual Tasks / DAG 面板只显示当前会话持久化 AgentTask、而普通任务理解结果未落库导致面板为空的问题；复用现有 AgentTaskCoordinator，不新增任务存储或调度器。
- [Planned] [CLI] [Fix] Fix the Textual Tasks / DAG panel appearing empty when task-understanding results are not persisted as an AgentTask for the current session; reuse the existing AgentTaskCoordinator without adding a store or scheduler.
- [计划] [cli] [fix] 将 DAG 节点状态渲染为纯文本，避免 `[pending]` 被 Rich/Textual markup 解释后不可见，并补充任务创建失败与无任务状态的可见诊断。
- [Planned] [CLI] [Fix] Render DAG node statuses as plain text so `[pending]` is not consumed as Rich/Textual markup, and expose visible diagnostics for task-creation failures and the no-task state.

### 预期影响文件 / Planned Files

- `PhyAgentOS/cli/textual_app.py`
- `PhyAgentOS/agent/loop.py`
- `PhyAgentOS/agent/tools/forge_task.py`
- `tests/test_textual_app.py`
- `tests/test_task_creation_diagnostics.py`

### 实际结果 / Actual Results

- [完成] [cli] [fix] `PhyAgentOS/cli/textual_app.py:L240-L250` 在没有当前 session 持久化 AgentTask 时显示明确的 session 和落库提示；未改变 Coordinator 的任务所有权或跨 session 过滤。
- [Completed] [CLI] [Fix] `PhyAgentOS/cli/textual_app.py:L240-L250` now shows the exact session and persistence guidance when no AgentTask is projected; Coordinator ownership and cross-session filtering remain unchanged.
- [完成] [cli] [fix] `PhyAgentOS/cli/textual_app.py:L267-L273` 将节点状态从方括号形式改为 `status=<state> node=<id>` 纯文本，避免 Rich/Textual markup 吞掉 `pending`。
- [Completed] [CLI] [Fix] `PhyAgentOS/cli/textual_app.py:L267-L273` renders node states as `status=<state> node=<id>` plain text instead of bracket markup, keeping `pending` visible under Rich/Textual.
- [完成] [eval] [test] `tests/test_textual_app.py:L128-L207` 新增节点状态可见性和空任务诊断测试；Textual/planning 回归 `44 passed, 1 warning`，Ruff、compileall 与 `git diff --check` 通过。
- [Completed] [Eval] [Test] `tests/test_textual_app.py:L128-L207` adds node-status visibility and empty-task diagnostics tests; the Textual/planning regression reports `44 passed, 1 warning`, with Ruff, compileall, and `git diff --check` passing.

### 文件变更详情 / File Change Details

#### [修改 / Modified] `PhyAgentOS/cli/textual_app.py:L240-L250,L267-L273`

**修改前 / Before:**

```python
if not rows:
    self.query_one("#tasks", static_type).update("No AgentTask")
lines.append(f"  [{node['status']}] {node['node_id']} ...")
```

**修改后 / After:**

```python
if not rows:
    self.query_one("#tasks", static_type).update(
        f"No AgentTask for session {normalized_session_key}\n"
        "Submit a task that materializes an AgentTask + PlanGraph."
    )
lines.append(f"  status={node['status']} node={node['node_id']} ...")
```

**修改说明 / Rationale:** 空面板明确区分“当前 session 没有任务”和“DAG 读取失败”；节点状态不再依赖 Rich 方括号 markup。The empty panel distinguishes “no task for this session” from a DAG read failure, and node status no longer depends on Rich bracket markup.

### 设计边界 / Design Boundary

本次没有自动从普通聊天创建 AgentTask，也没有放宽 Forge Skill binding 要求。没有安装/运行 Forge Skill 时，系统仍会正确显示无任务提示；只有 Agent 沿既有 `forge_task_create` + primary Skill activation 路径成功落库后，Tasks / DAG 才会显示真实任务。

This change does not auto-create AgentTasks from ordinary chat and does not relax Forge Skill binding requirements. Without an installed/running Forge Skill, the dashboard correctly reports no task; a real Tasks / DAG row appears only after the existing `forge_task_create` plus primary Skill activation path persists a task.

### Git 提交 / Git Commit

- Implementation commit: `7e4e99e`
- Branch: `feature/planning-loop`
- 时间 / Time: 2026-09-08 (Asia/Shanghai)
## v7.3.3 (2026-09-08 22:20) - codex

### 预期修改 / Planned Changes

- [计划] [policy] [feat] 增加 PAOS 宿主侧 Evolution CandidateLifecycle 适配器，将扩展提案映射到现有 SkillCandidate/WorkflowPolicyCandidate 生命周期，不新增存储或晋级路径。
- [Planned] [Policy] [Feat] Add a PAOS-host Evolution CandidateLifecycle adapter that maps extension proposals into the existing SkillCandidate/WorkflowPolicyCandidate lifecycle without adding storage or promotion paths.
- [计划] [cli] [feat] 为标准 CLI/Gateway 增加可选 extension composition 工厂；未安装扩展时保持现有行为。
- [Planned] [CLI] [Feat] Add an optional extension composition factory to standard CLI/Gateway roots while preserving behavior when the extension is unavailable.
- [计划] [policy] [fix] 将 extension 回调纳入既有 evolution job 重放语义，覆盖首次失败、重启后重试与幂等候选提交。
- [Planned] [Policy] [Fix] Integrate extension callbacks with the existing evolution-job replay semantics, covering first-failure retry, restart replay, and idempotent candidate submission.

### 预期影响文件 / Planned Files

- `PhyAgentOS/agent/experience/evolution_extension_adapter.py`
- `PhyAgentOS/agent/experience/coordinator.py`
- `PhyAgentOS/agent/loop.py`
- `PhyAgentOS/cli/commands.py`
- `tests/test_evolution_extension_adapter.py`

## v7.3.4 (2026-09-08 22:45) - codex

### 预期修改 / Planned Changes

- [计划] [policy] [feat] 增加 provider-neutral settled outcome projection adapter，将宿主提供的 SettlementTarget 与 OutcomeEvidenceSample 交给 ConsequenceSettler，再回流为 EpisodeProjection observations；不轮询传感器、不调用 Tool、不硬编码机器人谓词。
- [Planned] [Policy] [Feat] Add a provider-neutral settled-outcome projection adapter that feeds host-supplied SettlementTarget and OutcomeEvidenceSample values into ConsequenceSettler and returns EpisodeProjection observations; it will not poll sensors, invoke Tools, or hard-code robot predicates.
- [计划] [eval] [test] 增加 delayed、deadline、低覆盖与冲突 evidence 的 fake-facts 集成测试，并运行扩展与 PAOS 全量回归。
- [Planned] [Eval] [Test] Add fake-facts integration coverage for delayed, deadline, low-coverage, and conflicting evidence, then run extension and full PAOS regressions.

### 预期影响文件 / Planned Files

- `extensions/evolution/evolution/projection.py`
- `extensions/evolution/evolution/__init__.py`
- `extensions/evolution/tests/test_evolution_extension.py`

### 实际结果 / Actual Results

- [完成] [policy] [feat] `extensions/evolution/evolution/projection.py:L42-L81` 新增 `SettledOutcomeProjectionPort`；它只编排 provider 提供的 targets/samples、时间和关闭原因，并统一调用 `ConsequenceSettler`，不读取传感器、不调用 Tool。
- [Completed] [Policy] [Feat] `extensions/evolution/evolution/projection.py:L42-L81` adds `SettledOutcomeProjectionPort`; it coordinates provider-supplied targets/samples, timing, and close reason through `ConsequenceSettler` without reading sensors or invoking Tools.
- [完成] [policy] [feat] `extensions/evolution/evolution/__init__.py:L25-L43` 导出新适配器，保持独立扩展包 API 可发现性。
- [Completed] [Policy] [Feat] `extensions/evolution/evolution/__init__.py:L25-L43` exports the adapter so the standalone extension API remains discoverable.
- [完成] [eval] [test] `extensions/evolution/tests/test_evolution_extension.py:L430-L461` 增加 deadline settlement fake-facts 集成测试；扩展测试 `61 passed`，PAOS 全量 `253 passed`，Ruff、compileall、git diff --check 通过。
- [Completed] [Eval] [Test] `extensions/evolution/tests/test_evolution_extension.py:L430-L461` adds a deadline-settlement fake-facts integration test; extension tests report `61 passed`, full PAOS `253 passed`, with Ruff, compileall, and git diff --check passing.

### 六维验收 / Six-Dimension Acceptance

1. 架构集成：通过。结算适配器位于独立 extension projection 层，provider 仍拥有物理语义。 / Passed. Settlement lives in the standalone extension projection layer while providers retain physical semantics.
2. 失败路径：通过。settler 原有的 deadline、低覆盖、冲突和未知 target 校验继续生效。 / Passed. Existing deadline, low-coverage, conflict, and unknown-target checks remain active.
3. 权威与安全边界：通过。适配器不接触执行、传感器、Verifier 或运动权限。 / Passed. The adapter touches no execution, sensors, verifier, or motion authority.
4. 配置与复现：通过。时间和 close reason 可由宿主显式投影，默认仅使用 episode 的 time_cost_ms。 / Passed. Timing and close reason are explicit host projections, with episode time_cost_ms as the default.
5. 可维护性：通过。复用既有 ConsequenceSettler，不新增 store、scheduler 或并行结算实现。 / Passed. The existing ConsequenceSettler is reused without a new store, scheduler, or parallel settlement implementation.
6. 可观察性与 Anti-OverDefense：通过。settlement 语义保持可解释，没有新增 speculative hash、baseline 或 gate。 / Passed. Settlement remains inspectable without speculative hashes, baselines, or gates.

### 实际结果 / Actual Results

- [完成] [policy] [feat] `PhyAgentOS/agent/experience/evolution_extension_adapter.py:L1-L132` 新增宿主侧适配器：将带有 skill/workflow scope 的扩展提案转换为现有 `SkillCandidate`，保留 method、transition、surface、patch 与 evidence provenance；decision 提案可通过显式 digest resolver 接入现有 `WorkflowPolicyCandidateManager`。不执行 promotion 或 Skill 写入。
- [Completed] [Policy] [Feat] `PhyAgentOS/agent/experience/evolution_extension_adapter.py:L1-L132` adds a host-side adapter that maps scoped extension proposals into existing `SkillCandidate` records while preserving method, transition, surface, patch, and evidence provenance; decision proposals can use an explicit digest resolver with the existing `WorkflowPolicyCandidateManager`. No promotion or Skill writes occur.
- [完成] [policy] [fix] `PhyAgentOS/agent/experience/coordinator.py:L80-L90,L239-L251,L282-L330` 将 extension callback 保持 episode 创建后的即时观察，同时把失败纳入既有 evolution job retry；重启后 pending job 可重放，达到三次失败后记录 exhausted 并继续主经验处理。
- [Completed] [Policy] [Fix] `PhyAgentOS/agent/experience/coordinator.py:L80-L90,L239-L251,L282-L330` keeps immediate observation after episode creation and routes delivery failures through the existing evolution-job retry; pending jobs replay after restart, and three exhausted failures are recorded before main experience processing continues.
- [完成] [policy] [feat] `PhyAgentOS/agent/experience/contracts.py:L342-L367` 为 `SkillWorkflowProposal` 增加可选 `evolution_metadata`，使扩展 provenance 进入现有候选记录而不改变激活语义。
- [Completed] [Policy] [Feat] `PhyAgentOS/agent/experience/contracts.py:L342-L367` adds optional `evolution_metadata` to `SkillWorkflowProposal`, carrying extension provenance into existing candidate records without changing activation semantics.
- [完成] [eval] [test] `tests/test_evolution_extension_adapter.py:L1-L65` 新增三项 fake-facts 测试，验证候选持久化、跨 episode support 聚合和无 scope 提案的 fail-closed；专项 `6 passed`，PAOS 全量 `253 passed`，Ruff、compileall、git diff --check 通过。
- [Completed] [Eval] [Test] `tests/test_evolution_extension_adapter.py:L1-L65` adds three fake-facts tests for candidate persistence, cross-episode support aggregation, and fail-closed handling of unscoped proposals; focused tests report `6 passed`, full PAOS reports `253 passed`, with Ruff, compileall, and git diff --check passing.

### 六维验收 / Six-Dimension Acceptance

1. 架构集成：部分通过。扩展仍为独立包，宿主 adapter 复用 ExperienceStore、SkillCandidate 与 WorkflowPolicyCandidateManager；AgentLoop 在传入 extension 且未提供 lifecycle 时自动注入 adapter。标准 CLI/Gateway 仍需由调用方提供 provider-specific projection 和 registry，因此本轮不猜测自动装载。 / Partially passed. The extension remains standalone and reuses existing stores and candidate managers; AgentLoop injects the adapter when an extension is supplied. Standard CLI/Gateway still require a caller-supplied provider-specific projection and registry, so automatic loading is intentionally not guessed in this round.
2. 失败路径：通过。无 scope 不落库，callback 失败可重试，连续失败达到上限后记录 exhausted 且不阻塞主路径。 / Passed. Unscoped proposals are not persisted, callback failures retry, and exhausted delivery does not block the main path.
3. 权威与安全边界：通过。适配器只提交 collecting/pending_review 候选，不写 Skill、不调用 Verifier、Runtime 或运动接口。 / Passed. The adapter only submits collecting/pending-review candidates and never writes Skills or invokes verifier, runtime, or motion APIs.
4. 配置与复现：通过。candidate_id 与 source episode 保留，patch provenance 和显式 policy digest resolver 可追踪；未提供 resolver 时不猜测 policy。 / Passed. Candidate and episode identity plus patch provenance are retained; policy mapping requires an explicit resolver.
5. 可维护性：通过。未新增 store、scheduler、Gateway 或全局状态；重放复用既有 evolution_jobs。 / Passed. No new store, scheduler, Gateway, or global state was added; replay reuses evolution_jobs.
6. 可观察性与 Anti-OverDefense：通过。记录 unmapped、submitted、delivery exhausted 事件；未新增 speculative hash/baseline/gate。 / Passed. Unmapped, submitted, and exhausted events are recorded without speculative hashes, baselines, or gates.

### 验证与 Git 状态 / Verification and Git Status

- `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 uv run --extra dev --extra tui python -m pytest -q -p pytest_asyncio.plugin` -> `253 passed`。
- `uv run ruff check ...`、`python -m compileall -q ...`、`git diff --check` -> 通过。
- 未运行 Gateway、Dora、simulation step、Action 或硬件；无 commit，未 push（用户明确要求仅本地保存）。
- Gateway, Dora, simulation step, Action, and hardware were not run; no commit and no push were performed per user instruction.
